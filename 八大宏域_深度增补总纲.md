# 八大宏域·深度增补总纲

这份文件是现有 590 个可识别标题之上的“增补层”。它不替代逐课教案，而是规定每个宏域要补什么、怎样练、怎样证明已经掌握。逐课阅读顺序仍以 `教案_*.md` 中的编号为准。

## 使用方式

每次只推进一个课号。先读原课的 11 段，再读本文件中对应宏域的“深挖清单”和“实验包”，把证据写入 `evidence/<课号>/`。如果原课与本文件冲突，优先采用：事实核验结果 > 当前官方文档 > 本增补层 > 历史叙述或比喻。

八大宏域不是八个互相隔离的专业，而是一条依赖图：

```text
预备与编程
   ├── 算法与数学
   └── 计算机系统
          └── 网络与分布式
                 ├── 数据库与数据工程
                 └── Web 与产品工程
安全与逆向横切以上所有层
AI、云原生与端侧工程整合以上所有层
```

---

## I. 预备与编程（PRE + 方向 1）

### 能力出口

从“不会操作电脑”到能独立配置可复现环境，读懂 Python/C++/Rust/Java/JavaScript/Go 的核心差异，使用测试、调试、版本控制和文档把程序交给别人运行。语言数量不是目标；能够迁移概念、解释运行时差异、选择合适工具才是目标。

### 必补深度

1. **输入与表示**：字符集、编码、字节序、浮点误差、时区、路径和换行符；每种语言都做同一份“损坏输入”实验。
2. **内存与所有权**：Python 引用计数/垃圾回收、C 的生命周期、C++ RAII/移动、Rust 借用检查、Java GC、Go GC；用同一张对象生命周期图比较。
3. **控制流与并发**：同步调用、事件循环、线程、协程、actor/CSP；至少用 Python asyncio、Go channel、Rust async 各实现一次有界队列。
4. **工程化**：包管理、锁文件、语义化版本、构建缓存、静态检查、单元/属性/模糊测试、发布和回滚。
5. **语言设计**：词法、语法、作用域、类型、异常/错误、宏/元编程；在 PY25/CMP 课程中完成一个小型解释器前端。

### 五关实验包

- **Lv1**：用 Python 读取 UTF-8、UTF-16、带 BOM 和错误字节的文件，打印每一步的字节长度与解码结果。
- **Lv2**：把脚本改为命令行工具，加入参数校验、退出码、日志和 `--help`；为错误输入写 5 个测试。
- **Lv3**：用同一接口在 Python、Go、Rust 中实现生产者-消费者队列，限制容量并证明不会无界增长。
- **Lv4**：对三个实现做基准，记录吞吐、延迟、内存和失败行为；解释语言运行时与操作系统调用的差异。
- **Lv5**：设计一个最小语言：给出 EBNF、AST、解释器、错误恢复策略和 10 个性质测试，并写一篇“为什么没有加入某特性”的设计决策记录。

### 交付与研究

交付一个多语言 CLI 工具箱：同一功能有 Python 参考实现、C/Rust 高性能实现、Go 服务包装器和 JavaScript/TypeScript 客户端。资料锚点使用 [Python Tutorial](https://docs.python.org/3/tutorial/)、[Rust Book](https://doc.rust-lang.org/book/)、[Dev.java](https://dev.java/learn/) 和 [A Tour of Go](https://go.dev/tour/)。

---

## II. 算法与数学（方向 2）

### 能力出口

遇到陌生问题先建模，再选择结构和算法；能给出正确性证明、最坏/均摊复杂度、空间上界和可测量实现。刷题数量不作为毕业标准。

### 必补深度

1. 离散数学：命题、集合、关系、归纳、图、组合、概率基础。
2. 算法分析：渐进符号、递推、摊还、主定理、下界、随机化和缓存复杂度。
3. 正确性：循环不变量、归纳、交换论证、反证、势能法；每类算法至少写一种证明。
4. 结构全景：线性结构、哈希、平衡树、堆、图、字符串、几何、并查集、线段树；比较内存布局与实际常数。
5. 工程性能：基准设计、生成器、对照组、输入分布、分支预测、缓存和并行化；禁止只报一次运行时间。

### 五关实验包

- **Lv1**：实现数组、链表、栈、队列和哈希表，附边界测试和复杂度表。
- **Lv2**：将同一问题分别用 BFS/DFS、Dijkstra、A* 解，构造一个让错误算法失效的输入。
- **Lv3**：实现带随机测试的排序/查找库，使用性质测试验证排序不变式、稳定性和边界。
- **Lv4**：实现红黑树或线段树，进行 10 万到 1000 万规模的基准，解释缓存和分配器影响。
- **Lv5**：选择一个真实系统问题（调度、路由、推荐或压缩），写模型、证明、复杂度、实验设计、失败结果和改进方案。

### 交付与研究

完成“算法实验室”：每个实现都能生成输入、运行基准、输出 JSON 指标并绘图。主教材锚点为 [MIT 6.006](https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-fall-2011/)，扩展阅读需标注章节而不是只贴书名。

---

## III. 计算机系统（方向 3）

### 能力出口

从晶体管抽象到 ISA、编译器、进程、线程、虚拟内存、文件系统和调试器，能用实验解释“一个源代码字符怎样变成一次系统调用”。

### 必补深度

1. **硬件**：逻辑门、组合/时序电路、缓存层次、TLB、流水线、乱序、分支预测、SIMD、缓存一致性。
2. **工具链**：预处理、编译、汇编、链接、装载、符号、重定位、DWARF/调试信息；比较 ELF 与 PE。
3. **操作系统**：进程/线程/调度、同步/死锁、分页/缺页、分配器、VFS、日志、设备与中断、容器隔离。
4. **并发内存模型**：原子操作、happens-before、锁自由算法、可见性与伪共享；用 ThreadSanitizer 或等价工具留证。
5. **恢复与可靠性**：断电、崩溃、损坏、回滚、检查点和幂等；每个存储实验必须有故障注入和恢复记录。

### 五关实验包

- **Lv1**：用 Nand2Tetris 项目完成 NAND→ALU→CPU 的最小链路，并通过官方测试脚本。
- **Lv2**：用 C 写一个内存映射文件和一个多线程计数器，观察错误结果并加锁修复。
- **Lv3**：在 xv6 或等价教学内核中增加一个系统调用，提交用户态测试和内核路径说明。
- **Lv4**：用 gdb、反汇编和 perf/Windows 等价工具定位一个真实崩溃或性能回归，提交调用链、寄存器和证据。
- **Lv5**：实现一个简化文件系统或垃圾收集器，设计崩溃测试、恢复协议和性能模型，比较至少两种设计。

### 交付与研究

采用 [OSTEP](https://pages.cs.wisc.edu/~remzi/OSTEP/) 的虚拟化/并发/持久化主线和 [Nand2Tetris](https://www.nand2tetris.org/) 的项目链；把“看源码”改为“提出假设→写实验→收集指标→解释反例”。

---

## IV. 网络与分布式（方向 4 + 方向 10 相关课）

### 能力出口

能从网卡、以太网、IP、TCP/UDP、TLS、HTTP、DNS 追踪一次请求；能设计超时、重试、幂等、限流、选主、复制、故障转移和容量边界。

### 必补深度

1. 地址与路由：MAC/ARP、IPv4/IPv6、子网、NAT、路由表、BGP、RPKI、DNS/DNSSEC。
2. 传输语义：TCP 状态机、序列号、SACK、RTO、拥塞控制；UDP/QUIC 的取舍。
3. 应用协议：HTTP/1.1、HTTP/2、HTTP/3、缓存、代理、WebSocket、gRPC、邮件和时间同步。
4. 分布式基础：部分失败、时钟、顺序、线性一致性、共识、复制、分区容忍、消息语义。
5. 排障与观测：抓包过滤、服务端日志、指标、trace、合成探针和故障域；每个结论都关联一份 pcap 或 trace。

### 五关实验包

- **Lv1**：在本机抓取 DNS、TCP 三次握手和 HTTPS ClientHello，标注五层字段。
- **Lv2**：写一个超时、重试、指数退避、幂等键齐全的 HTTP 客户端，模拟服务器慢、断开和重复响应。
- **Lv3**：用三个本地进程实现带日志的主从复制，注入延迟、丢包和重启，说明哪些保证做不到。
- **Lv4**：用 Wireshark/tshark 和 OpenTelemetry 关联一次请求的包、日志、trace，完成“从用户感觉慢到根因”的报告。
- **Lv5**：设计多区域服务：画拓扑、故障域、容量模型、RTO/RPO、升级/回滚和安全边界，并用本地故障注入验证两个关键假设。

### 交付与研究

协议字段以 [RFC 9293 TCP](https://www.rfc-editor.org/rfc/rfc9293) 和 IETF RFC 为准；抓包工具以 [Wireshark 文档](https://www.wireshark.org/docs/) 为准。不要用“网络不稳定”作为根因，必须说明丢在哪一层、由什么证据支持。

---

## V. 数据库与数据工程（方向 5 + 方向 9 DAT1–DAT24）

### 能力出口

能从需求建立数据模型，选择索引和事务隔离，解释执行计划、WAL、MVCC、复制、备份恢复和数据质量，并将批处理/流处理做成可观测管道。

### 必补深度

1. 关系模型与约束：候选键、函数依赖、范式、反范式、时间模型和数据契约。
2. 执行引擎：解析、优化、算子、连接算法、代价估计、统计信息、向量化和列式执行。
3. 事务与恢复：ACID、MVCC、隔离级别、WAL、检查点、崩溃恢复、两阶段提交的代价。
4. 存储与扩展：B+Tree、LSM、分区、分片、复制、一致性哈希、缓存和冷热分层。
5. 数据治理：质量规则、血缘、版本、脱敏、保留、删除权、访问审计和合成数据。

### 五关实验包

- **Lv1**：用 SQLite/PostgreSQL 建学生选课模型，添加约束、索引和 20 条失败插入测试。
- **Lv2**：对同一查询建立三种索引，保存 `EXPLAIN (ANALYZE, BUFFERS)` 前后结果，解释变化。
- **Lv3**：用两个会话复现实验隔离级别、死锁和幻读，提交时间线和修复方案。
- **Lv4**：手写一个最小 WAL/日志恢复演示器，模拟中途崩溃、重放、重复提交和校验失败。
- **Lv5**：搭建可重跑数据管道，含数据契约、质量门、血缘、失败重试、回填和脱敏；用故障数据证明它会“响亮失败”。

### 交付与研究

数据库事实以 [PostgreSQL Documentation](https://www.postgresql.org/docs/current/) 为准；不把某个产品的实现细节当成关系数据库的普遍规律。每个设计选择都写“为什么不用另一方案”。

---

## VI. Web 与产品工程（方向 6 + 方向 11）

### 能力出口

能把需求转成可访问的界面、稳定 API、数据模型、测试、部署和运行手册；能用用户证据而不是个人偏好做产品决策。

### 必补深度

1. 浏览器模型：解析、DOM/CSSOM、布局/绘制、事件循环、存储、同源策略、缓存和 Service Worker。
2. 前端工程：组件边界、状态、表单、错误、a11y、国际化、性能预算、可观测性和供应链。
3. 后端工程：认证/授权、输入验证、幂等、事务边界、队列、缓存、限流、版本化和迁移。
4. 产品方法：问题陈述、用户研究、任务流、原型、启发式评审、指标、实验、无障碍和隐私。
5. 交付：CI、预览环境、迁移/回滚、容量、SLO、事故沟通和数据删除流程。

### 五关实验包

- **Lv1**：做一个可访问的套餐列表，键盘可操作、语义 HTML、表单错误可读，附 Lighthouse/axe 结果。
- **Lv2**：为 API 增加鉴权、幂等键、分页、错误码、OpenAPI 文档和契约测试。
- **Lv3**：把“闪电号卡”拆成用户任务流、页面状态、领域模型和端到端测试，覆盖成功、取消、重复提交、库存不足。
- **Lv4**：进行一次性能预算实验：记录 LCP/INP、数据库 P95、错误率、缓存命中率，定位一个瓶颈并验证改动。
- **Lv5**：提交完整产品包：研究记录、PRD、设计系统、威胁模型、ADR、部署手册、SLO、回滚演练和用户反馈闭环。

### 交付与研究

Web 技术边界参照 [MDN](https://developer.mozilla.org/en-US/docs/Web)，交互评审参照 [NN/g 十项启发式](https://www.nngroup.com/articles/ten-usability-heuristics/)。页面“能打开”不等于完成；可访问性、隐私、失败状态和可恢复性都是一等验收项。

---

## VII. 安全与逆向（方向 7a/7b/7c）

### 能力出口

能对自有或明确授权的系统进行威胁建模、代码审计、日志分析、二进制理解、漏洞验证和修复回归；能写出开发者可以执行的报告。能力以防御证据衡量，不以“攻击他人”衡量。

### 必补深度

1. 安全基础：资产、信任边界、威胁、风险、身份、密钥、供应链、最小权限和纵深防御。
2. Web 安全：输入/输出、会话、访问控制、注入、SSRF、文件处理、业务逻辑、API 和云配置。
3. 二进制：ABI、调用约定、内存布局、调试信息、静态/动态分析、崩溃分类和修复验证。
4. 检测与响应：日志规范、时间线、IOC、Sigma/YARA（只用于自有样本）、告警质量、取证保全和复盘。
5. 治理：漏洞生命周期、CVE/CVSS、SBOM、依赖升级、披露、合规、证据链和报告写作。

### 五关实验包

- **Lv1**：在本地故意写一个输入验证错误，运行测试发现它，提交修复前后差异和回归用例。
- **Lv2**：使用 OWASP Juice Shop/WebGoat 等授权靶场完成一条低风险练习链，记录范围、时间、证据和修复建议。
- **Lv3**：对自建 Flask/Node 服务做威胁模型、SAST/DAST、依赖扫描和手工审计，输出按风险排序的报告。
- **Lv4**：对自己编译的教学二进制做反汇编和崩溃根因分析，使用安全编译选项修复并用回归测试验证。
- **Lv5**：搭建隔离检测实验室：样本、日志、规则、时间线、处置、回滚和复盘齐全；提交一份面向管理者和开发者的双版本报告。

### 交付与研究

风险分类参考 [OWASP Top 10](https://owasp.org/www-project-top-ten/)、控制要求参考 [OWASP ASVS](https://owasp.org/www-project-application-security-verification-standard/)，岗位能力参考 [NIST NICE](https://www.nist.gov/itl/applied-cybersecurity/nice)。所有实验都要写“授权证明/隔离方式/清理步骤”；不提供针对真实目标的攻击成品，不处理真实凭据。

---

## VIII. AI、云原生与端侧工程（方向 8 + 方向 9 DAT25–DAT51 + 方向 10）

### 能力出口

能训练和评估模型，理解数据/模型/特征/提示/推理的生命周期；能把服务部署到容器、Kubernetes、移动/桌面/嵌入式端，建立成本、SLO、安全和回滚机制。

### 必补深度

1. AI 基础：张量、自动微分、优化、正则化、评估集、数据泄漏、校准、不确定性和可解释性。
2. 现代模型：注意力、Transformer、微调、检索增强、工具调用、评估、提示注入和模型供应链。
3. MLOps：实验追踪、模型注册、数据/特征血缘、漂移、灰度、回滚、成本和 GPU 利用率。
4. 云原生：镜像、运行时、Kubernetes 工作负载/网络/存储/策略、IaC、密钥、备份和多集群。
5. 端侧：权限、沙箱、签名、崩溃、功耗、离线、OTA、隐私和商店/发布流程。

### 五关实验包

- **Lv1**：用 PyTorch 完成张量、数据集、模型、autograd、优化和保存加载的最小闭环，固定随机种子。
- **Lv2**：加入错误分析、混淆矩阵、阈值、数据切分和基线比较，解释一次不收敛或过拟合。
- **Lv3**：将模型封装为 API，加入输入校验、限流、日志、trace、模型版本和回滚开关。
- **Lv4**：用 Docker/Kubernetes 部署，注入资源不足、探针失败、节点重启和依赖不可用，验证恢复时间。
- **Lv5**：完成端到端 AI 产品：数据治理、模型卡、风险评估、红队防御、SLO、成本仪表盘、OTA/灰度/回滚和事故复盘。

### 交付与研究

训练基础参照 [PyTorch Learn the Basics](https://pytorch.org/tutorials/beginner/basics/intro.html)；集群概念参照 [Kubernetes Concepts](https://kubernetes.io/docs/concepts/)；可观测性参照 [OpenTelemetry](https://opentelemetry.io/docs/)。每个模型结果必须携带数据版本、代码版本、随机种子、硬件、指标和已知失效场景。

---

## 跨宏域毕业工程

最终作品不再新增“第 12 个方向”，而是把八大宏域串成一个可审计系统：

1. **需求与风险**：PD/SEC 给出用户任务、数据分类、威胁模型和成功指标。
2. **实现**：编程、算法、系统、网络、数据库完成核心服务和客户端。
3. **智能能力**：AI 轨道接入一个可替换模型或检索组件，提供离线基线和人工兜底。
4. **交付**：容器/Kubernetes 部署，OpenTelemetry 三信号，SLO/错误预算，备份和回滚。
5. **答辩**：现场演示正常路径、三种故障、一种安全事件和一次成本/性能权衡；提交代码、测试、图表、ADR、运行手册和复盘。

跨域工程通过才算“从零到宗师”的阶段性完成；单纯读完 590 个标题不算毕业。
