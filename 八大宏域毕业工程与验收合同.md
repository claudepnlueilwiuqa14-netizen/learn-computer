# 八大宏域毕业工程与验收合同

这份文件把“学会很多课”连接到可演示作品。每个宏域完成一个作品，作品必须有源码/配置、自动化测试、运行手册、指标、故障注入、回滚、威胁模型和复盘。只在本机、隔离容器/虚拟机、模拟器或明确授权环境运行；不使用真实用户数据和生产凭据。

## 统一交付目录

```text
capstone/<宏域>/
  README.md              # 目标、架构、快速开始、限制
  docs/adr/              # 关键取舍
  src/                   # 源码/配置
  tests/                 # 单元、集成、性质或 e2e
  evidence/              # 指标、日志、trace、截图、pcap（脱敏）
  runbook.md             # 启动、排障、回滚、清理
  threat-model.md        # 资产、信任边界、滥用案例、控制
  postmortem.md          # 故障时间线、根因、行动项
```

## I · 预备与编程

- 作品：多语言 CLI 工具箱 + 最小解释器前端。Python 做参考实现，C/Rust 做高性能实现，Go 做服务包装，JS/TS 做客户端。
- 必测：UTF-8/UTF-16/错误字节、空输入、非法参数、幂等重跑、跨平台路径、退出码、锁文件和版本兼容。
- 指标：正确率、P50/P95 延迟、峰值内存、二进制/包体大小、冷启动时间、测试覆盖和可复现构建成功率。
- 故障：删权限、改编码、截断输入、缺少依赖、并发重复执行；必须恢复并保留失败证据。
- 通过：10 个以上性质测试、三种语言结果一致、干净环境一键构建、ADR 解释为何不继续增加语言特性。

## II · 算法与数学

- 作品：算法实验室，包含结构库、输入生成器、性质测试、证明笔记和基准可视化。
- 必测：空/重复/最坏输入、溢出、稳定性、随机种子、规模从 10^2 到 10^6 的趋势、对照算法。
- 指标：正确率、最坏/均摊复杂度、P50/P95 时间、峰值内存、分配次数、缓存/并行影响。
- 故障：故意破坏不变量或边界条件，测试必须缩减出最小反例并阻止回归。
- 通过：至少 5 个数据结构、3 类证明、2 个高级算法、可复现 JSON 基准和限制分析。

## III · 计算机系统

- 作品：Nand2Tetris 或 xv6 子系统，外加一次真实崩溃根因报告。
- 必测：编译/链接/装载、系统调用、页表/缺页、线程同步、文件持久化和调试符号。
- 指标：指令数、系统调用数、上下文切换、缺页、锁等待、吞吐/尾延迟、崩溃恢复时间。
- 故障：竞态、非法地址、磁盘写入中断、ABI 不匹配；只在隔离实验环境注入并可回滚。
- 通过：源码→汇编→系统调用→内核路径图，至少一个机制手写实现，完整时间线和修复回归。

## IV · 网络与分布式

- 作品：本地多节点服务，含协议客户端、服务发现、超时/重试、故障注入和恢复。
- 必测：DNS、TCP/TLS、HTTP/2/3、IPv4/IPv6、分区、乱序、丢包、时钟偏差和副本延迟。
- 指标：可用性、P50/P95/P99、重传率、吞吐、连接数、收敛时间、错误预算消耗。
- 故障：端口关闭、DNS SERVFAIL、MTU 变化、单节点宕机、网络延迟/丢包；保存 pcap 和 trace。
- 通过：从抓包字段回到 RFC 条款，三种故障自动恢复，容量预算和回滚方案可执行。

## V · 数据库与数据工程

- 作品：可恢复的 OLTP/分析混合数据平台，含 schema、索引、事务、WAL/备份、数据质量和血缘。
- 必测：约束冲突、死锁、热点、慢查询、崩溃恢复、复制延迟、迁移回滚、脱敏和权限。
- 指标：查询 P50/P95、吞吐、锁等待、WAL/备份大小、RPO/RTO、恢复成功率、数据质量通过率。
- 故障：杀进程/模拟磁盘满/副本延迟/错误迁移；仅使用合成数据，记录时间线和校验和。
- 通过：恢复演练达成声明 RPO/RTO，至少 3 个执行计划优化有前后基线，权限和审计可复核。

## VI · Web、产品与人机工程

- 作品：“闪电号卡”生产化版本，含可访问界面、后端 API、认证、支付模拟、观测和部署回滚。
- 必测：键盘/屏幕阅读器、窄屏/慢网、重复提交、权限错误、依赖不可用、数据库超时、版本回滚。
- 指标：任务成功率、WCAG 检查、LCP/INP/CLS、API P95、错误率、部署频率、回滚时间。
- 故障：模拟支付回调重复、缓存失效、服务超时、发布半成品；不接真实支付和个人信息。
- 通过：PRD、设计 Token、威胁模型、自动化 e2e、性能报告、运行手册和一次可演示回滚。

## VII · 安全、逆向与可信工程

- 作品：隔离靶场 + 检测规则 + 漏洞修复回归集 + 技术/管理双版本报告。
- 必测：资产清单、授权范围、输入校验、认证/授权、日志、依赖、密钥生命周期、证据保全。
- 指标：风险覆盖、误报/漏报、修复回归通过率、检测延迟、证据完整性、残余风险。
- 故障：只在自有代码/明确授权靶场制造无害拒绝路径；不得扫描公共目标、处理真实凭据或部署恶意代码。
- 通过：每项发现绑定请求/日志/哈希和授权证明，修复后测试通过，披露时间线和停止条件完整。

## VIII · AI、云原生与端侧

- 作品：端到端 AI 服务：合成数据、训练/评估、模型服务、API、容器/Kubernetes 部署、SLO、成本和端侧演示。
- 必测：数据版本、切分泄漏、漂移、提示注入防御、模型回滚、资源限制、离线/低电量、签名和 OTA 回滚。
- 指标：任务质量、校准/召回、延迟、吞吐、GPU/CPU/内存、成本、漂移、SLO、功耗、崩溃率。
- 故障：评估集污染、依赖不可用、Pod 重启、节点资源耗尽、模型版本回退、设备离线；全部在隔离环境执行。
- 通过：模型/数据/镜像可追溯，三信号观测齐全，至少三种故障有自动化恢复或明确人工 Runbook，成本与风险有 ADR。

## 跨宏域综合演练

把八个作品串成一个本地系统，依次验证：正常路径、三种独立故障、一次授权安全事件、一次成本约束。提交统一时间线、影响范围、指标、决策、回滚、复盘和后续行动项。完成这项演练，才可在看板把“结构补强”推进为“毕业项目已验证”。

### 当前证据（2026-09-09）

`capstone/cross_domain/` 已完成一次本地合成数据演练：算法路由、HTTP KV、SQLite、Web、输入安全和线性模型串联；依赖 404、数据库约束冲突、模型输入错误均恢复；`../secret` 输入被安全边界阻断；内存与 SQLite 延迟/恢复价值形成成本决策。证据：`cross_domain/evidence/cross-domain-2026-09-09.json`。这只完成跨域演练合同，不会自动把八个单域项目标为毕业。

## 深度基线快照（2026-09-09）

`capstone/run_deep_validation.py` 已对八个单域项目执行一次本地深度基线，统一写入各项目 `evidence/deep-baseline-2026-09-09.json`，汇总见 `capstone/evidence/DEEP-SUMMARY-2026-09-09.md`。每条记录至少包含：真实 smoke 测试命令和输出哈希、项目特有测量、一个受控故障、失败是否被观察、恢复时间、回滚验证、资产/信任边界/滥用案例/控制、三个明确缺口，并固定 `graduation_decision=NOT_READY`。

结果为 8/8 `BASELINE_PASS`、8/8 故障已观察且恢复、8/8 回滚字段已执行。该结果证明的是机器基线可复现，不证明跨语言、多节点、生产容量、真实授权靶场、资料阅读或毕业合同通过；严格核验命令为 `核验毕业工程深度基线.ps1 -Strict`。

## 第三轮宗师挑战接入毕业闸门（2026-09-09）

第三轮挑战不是额外观看材料，而是本合同的高阶证据入口。每个单域项目申请人工验收时，必须从对应宏域的 A1–H5 中完成至少 4 个，且至少 1 个达到 L9 或 L10；每个挑战必须绑定具体课号并放入 `evidence/<课号>/boss/<挑战编号>/`。

| 工程 | Boss 范围 | 至少覆盖的难点 |
|---|---|---|
| 01 预备与编程 | A1–A5 | 跨语言语义、反例缩减、生命周期并发、可复现签名发布、解释器设计 |
| 02 算法与数学 | B1–B5 | 证明审查、领域反例、缓存布局、并行伸缩、算法选择答辩 |
| 03 计算机系统 | C1–C5 | 竞态、崩溃恢复、系统调用路径、内存/页表、根因答辩 |
| 04 网络与分布式 | D1–D5 | pcap/RFC、分区乱序、协议矩阵、时钟/quorum、容量回滚 |
| 05 数据库与数据工程 | E1–E5 | 多进程复制、三执行计划、崩溃恢复、契约血缘、幂等回填 |
| 06 Web、产品与人机工程 | F1–F5 | WCAG、性能 trace、进程外回滚、安全容量、用户证据 |
| 07 安全、逆向与可信工程 | G1–G5 | 多版本回归、授权隔离、二进制根因、供应链、双版本报告 |
| 08 AI、云原生与端侧 | H1–H5 | 资源/探针、注册表、签名 OTA、成本功耗、安全评估 |

挑战通过的最低证据合同：控制组与变体、三次重复、至少一个失败或反例、恢复/回滚或人工介入、清理、资料命题、关键文件哈希以及 L6–L10 自评。挑战结构核验 `核验第三轮宗师挑战.ps1 -Strict` 通过，只证明合同存在；八项工程的 `graduation_decision` 仍必须由学习者证据和人工答辩改写，机器基线不得代改。

## 第二轮深度增补（2026-09-09）

本轮把合同中最容易被“最小闭环”掩盖的高难项落到可运行资产：

- 预备与编程：四实现向量扩展到 16 个，Python 固定种子性质样例 200 个；测试覆盖空白、括号、负数、除零、非法字符和截断表达式。
- 算法与数学：新增有界 `shrink_counterexample`，能把固定失败序列缩减为最小样本；基准、证明和结构性质仍与学习者独立审查分开计分。
- 算法与数学：新增固定 10000 项输入的两线程 `parallel_merge_sort` 串行/并行对照，记录正确性和 P50；结果只作为线程开销基线，不宣称并行加速。
- 计算机系统：新增 pthread mutex 线程探针（4 x 25000），GDB batch 对自有 SIGABRT 生成源码行/主函数帧证据；MinGW 缺少 `threads.h` 的工具事实已记录。
- 网络与分布式：新增 HTTP 状态/版本/Content-Type/Content-Length 观察和有界重试；复制与 failover 仍只在 `127.0.0.1`。
- 网络与分布式：新增 `ScriptedPartition` 两次丢失后恢复的合成状态机，记录 attempts/errors/backoff/收敛；真实 packet-level pcap 和分区仍需单独验收。
- 数据库与数据工程：新增 SQLite 一致快照复制、独立副本读取、reader/writer/operator 角色矩阵和 `lineage/v1` 合成 DAG。
- 数据库与数据工程：新增教学日志流 `FencedStreamCoordinator`，真实记录 LSN/lag、分批 apply、epoch promote 和旧主拒绝；生产多进程复制仍需独立验收。
- Web 产品：新增持久化 SQLite 订单、唯一幂等键、订单重启读取和 PlanCatalog v1/v2 激活回滚。
- Web 产品：新增本地浏览器基线 `capstone/06_Web产品/evidence/browser-2026-09-09.json`，实际验证语义 DOM、Tab 焦点、状态反馈、390×844 无横溢出和重载 P50/P95；仍不等于 axe/WCAG 或生产 Web Vitals 验收。
- Web 产品：新增无依赖 `a11y_audit.py`，深度基线硬检查文档语言、main/h1、label 关联、按钮名称和 live status；仍不等于完整 axe/WCAG 覆盖。
- 安全逆向：新增 Python AST v2 修复对照，在同一合成 corpus 上记录修复前后 TP/FP/FN 和证据哈希。
- 安全逆向：新增 `repair-diff.md` 与 `authorized-lab-snapshot.json`，机器基线校验修复根因、回归合同、目标/输入范围和停止条件；快照文件明确仍是 descriptor-only。
- AI、云原生与端侧：新增确定性训练/评估切分与指纹、泄漏拒绝和有限 payload 推理边界。
- AI、云原生与端侧：新增回环模型服务 `/health`/`/ready`/`/predict`/`/metrics`、超大请求 413 故障与恢复、以及带 CPU/内存 requests/limits 和探针路径的 JSON 运行合同；这仍不等于真实容器/Kubernetes 资源耗尽演练。

新增课包的完整五级闯关、5 Lab、资料锚点、失败注入和证据字段见 `深度增补_2026-09-09_第二轮.md`。这些资产将八项毕业工程推进到更深的机器基线，但所有项目仍固定 `graduation_decision=NOT_READY`；学习者必须在各自 evidence 目录独立重放、解释、变体、复盘后才可申请合同验收。
