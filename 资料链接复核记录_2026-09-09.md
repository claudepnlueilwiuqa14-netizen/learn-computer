# 资料链接复核记录 · 2026-09-09

## 方法

- 从 `权威资料目录与复核计划.md` 提取基线 60 个唯一 URL；第二轮又登记 11 个入口，当前目录共 71 个唯一 URL。
- 先用 PowerShell `HEAD` 请求并跟随重定向；HEAD 被拒绝、超时或返回 5xx 的链接再用 `GET` 复核。
- 这只是可达性检查，不等价于阅读、版本确认或命题正确性；课程卡仍要求记录页面标题、章节、版本和已验证命题。

## 结果

| 项目 | 数量 |
|---|---:|
| 基线唯一 URL | 60 |
| 第二轮新增 URL | 11 |
| 当前目录唯一 URL | 71 |
| HEAD 直接成功 | 47 |
| HEAD 失败后 GET 成功 | 8 |
| 通过 GET/HEAD 可达 | 55 |
| HEAD/GET 受限但 curl 可达 | 1 |
| 仍受限/超时/访问控制 | 4 |

## 仍受限或需人工复核

| URL | 现象 | 处理 |
|---|---|---|
| https://developer.android.com/ | GET 超时/请求取消 | 使用 Android Developers 同域具体文档页或本地 SDK 文档；下次复核 |
| https://docs.docker.com/ | PowerShell TLS 错误；`curl.exe` 返回 200 | 计入可达（56/60），记录客户端差异 |
| https://huggingface.co/learn/nlp-course | GET 超时/请求取消 | 使用 Hugging Face 官方 API/仓库 README 或下载的课程版本；下次复核 |
| https://web.dev/learn/performance/ | GET 超时/请求取消 | 使用 MDN Performance 和 Chrome Developers 同主题文档作为替代入口 |
| https://www.intel.com/content/www/us/en/developer/articles/technical/intel-sdm.html | 返回 403 | 使用 Intel 官方下载页/镜像或 AMD/RISC-V 官方手册；不绕过访问控制 |

## 结论

基线集合有 56/60 个入口在本次客户端检查中可达；第二轮新增集合有 10/11 个入口返回 HTTP 200，POSIX pthread 入口仍需人工复核。合并口径为 66/71 个入口已有可达性证据，5 个仍受网络、超时或访问控制限制，不据此断言资料内容失效。未验证链接不作为唯一依据，相关课卡必须采用同机构替代入口并在证据 README 记录限制。

## 本轮页面核验（2026-09-09）

以下页面通过只读页面检查，记录了页面标题、版本信号和可直接命题的章节；这不是对全部课程内容的阅读完成证明。

| 页面 | 页面/版本信号 | 可直接命题的边界 | 受影响课群 |
|---|---|---|---|
| RFC 9293 | `STD 7: Transmission Control Protocol (TCP)`；Internet Standard；2022 IETF Trust 版权声明 | TCP 是可靠、有序字节流；序列号/校验和/重传构成可靠性；连接不自带 liveness；规范关键词 MUST/SHOULD 需按 BCP 14 解读 | NET6、NET17–NET24 |
| PostgreSQL MVCC | PostgreSQL 18 current；Chapter 13 Concurrency Control | 事务隔离、显式锁、死锁、序列化失败和恢复处理是独立可测主题 | DB6、DB7、DB10、DB11 |
| MDN HTTP Caching | HTTP caching 指南；private/shared cache、fresh/stale、Vary、validation、request collapse | 个性化响应不得进入共享缓存；缓存命中/验证/失效必须用响应头与时间线证明 | FE/BE 缓存与性能课 |
| Kubernetes Concepts | Concepts 章节；Workloads、Services/Networking、Storage、Configuration、Security、Policies、Scheduling | Kubernetes 是声明式配置与自动化平台；Pod 是最小可部署计算对象；网络、存储、策略和调度分开验收 | OPS1–OPS49、MOB 云原生交叉课 |
| OpenTelemetry Observability primer | Observability primer 页面；页面正文可达 | traces、metrics、logs 的关联、语义约定、采样和 Collector 需要分开设计 | OPS 观测、BE 可观测课 |
| NIST AI RMF | AI RMF 1.0 发布 2023-01-26；Generative AI Profile 于 2024-07-26；页面注明框架正在修订 | AI 风险管理用于设计、开发、使用和评估；生成式 AI 风险须单独登记；版本变化要写入课次证据 | DAT、SEC AI、OPS 模型服务课 |

浏览器只读核验脚本没有提交表单、登录、上传或下载用户数据。Android Developers、Hugging Face Course、web.dev 和 Intel SDM 仍按上表“受限/替代入口”规则处理，不把超时页面当作已验证事实。

## 第二轮新增入口复核（2026-09-09）

对本轮新增的 11 个入口使用 `curl.exe -L --max-time 25` 只读请求：10 个返回 HTTP 200，POSIX pthread 页面因 Windows Schannel `0x80092013` TLS 错误超时/失败。该失败不代表页面失效；课程使用同机构 POSIX 规范入口并要求在可用网络或本地镜像人工复核。

| 新增资料 | 本次结果 | 课程处理 |
|---|---|---|
| `https://sourceware.org/gdb/current/onlinedocs/gdb.html`、`https://www.rfc-editor.org/rfc/rfc9110`、`https://www.sqlite.org/wal.html`、`https://www.sqlite.org/backup.html` | HTTP 200 | 可作为课包 C/D/E 的直接命题入口，仍需记录章节和版本 |
| `https://www.w3.org/WAI/ARIA/apg/`、`https://docs.python.org/3/library/ast.html`、`https://scikit-learn.org/stable/model_selection.html` | HTTP 200 | 可作为课包 A/F/G/H 的直接命题入口 |
| `https://openlineage.io/docs/`、`https://theupdateframework.io/security/`、`https://docs.sigstore.dev/` | HTTP 200 | 可作为血缘、签名和 OTA 课的规范入口 |
| `https://pubs.opengroup.org/onlinepubs/9699919799/functions/pthread_mutex_lock.html` | TLS 失败/待人工复核 | 使用同域 POSIX Issue 7 入口或本地手册替代，不把本次失败当作内容结论 |
