# 教案 · 方向7a · 安全基础 + Web 安全（白帽）

> **本文件覆盖 SEC1–SEC22，共 22 节（白帽）**，严格按"11 段浅→深"模板 + 实战 Lab 逐节展开。
> **学生画像**：真·零基础、术后留置针手不便、时间极充裕、目标是计算机顶级大佬（白帽黑客方向）。
> **写法约定**：能复制就复制，少打字（护手）；语言通俗、循序渐进、不跳步。
> **白帽红线（贯穿全文）**：只对自己写的程序、授权靶场、CTF、合法赏金项目出手；绝不攻击他人系统、不登录他人账号爬受限内容、不绕实名监管。讲攻击原理，**唯一目的**是学会更好防御。
> **配套总纲**：见《完整课程安排.md》方向7a 表格。

---

# SEC1 · 安全思维 / 威胁建模（STRIDE + 攻击树）

## 🎯 目标
学完这节，你能在拿到任何一个系统（哪怕是"我的局域网文件共享"）时，用一张图把它可能被怎么搞坏、怎么防住，系统地想清楚——而不是凭感觉。

## 📋 小白前置
- 方向4 网络（NET1–NET6：懂什么是请求、响应、客户端、服务器即可）
- 方向5 数据库（DB1–DB3：懂"数据被存起来、被查询"即可）
- 方向6 Web 全栈基础（FE1、BE1–BE3：懂"网页点一下，浏览器给服务器发请求，服务器返回页面"即可）
- 本节课是方向7a 第 1 节，跨文件前置就是上面三块的基础层。

## 🟢 部分一·最浅层（生活比喻）
把软件想成你家的房子。
- **资产** = 你舍不得丢的东西（钱、证件、照片）。
- **威胁** = 可能来偷/砸的人（小偷、暴雨、自己忘了锁门）。
- **漏洞** = 房子上的弱点（窗户没关、钥匙藏在门垫下）。
- **攻击** = 小偷真的翻窗进来了。
- **防御** = 装防盗门、装监控、把钥匙收好。

"威胁建模"就是：在装修前先画一张图——这房子有哪些入口、谁可能从哪进、进了能拿什么、我怎么挡。高手和菜鸟的区别，就是高手**先画图再动手**，菜鸟**建好了才想"会不会被偷"**。

## 🟡 部分二·动手层（逐字操作）
我们装一个画图+笔记工具，用 **draw.io**（浏览器即可，零安装）来画你的第一张威胁模型。

1. 打开浏览器，访问 `https://app.diagrams.net`（或装 VS Code 插件 `hediet.vscode-drawio`，见 PRE4）。
2. 新建空白图，文件名填 `my-first-threat-model`。
3. 在画布左侧"形状"里搜 `AWS` 或 `Flowchart`，拖 4 个方块：
   - 方块A：写 `浏览器(你)`
   - 方块B：写 `Web服务器`
   - 方块C：写 `数据库`
   - 箭头：A→B（你发请求）、B→C（服务器查库）、C→B（返回数据）、B→A（返回页面）。
4. 在每根箭头旁贴一个便利贴（用"形状→通用→便签"），写一句话：**"如果这根线被坏人监听/篡改会怎样？"**
   - 例如 A→B 旁写：密码明文被截获 → 后果=账号被盗。
5. 保存图（File → Save）。这就是你的**第一份威胁模型**。

> 💡 护手提示：全程鼠标拖拽 + 复制粘贴文字，不需要敲命令。

## 🔵 部分三·原理层（底层发生了什么）
你刚画的图，在安全技术里叫**数据流图（DFD, Data Flow Diagram）**。威胁建模的标准流程（微软 STRIDE 法）：
- 先画出"数据从哪来到哪去"（你画的 4 个箭头）。
- 再对**每一个元素/每一条边界**问 6 类问题（STRIDE 首字母）：
  - **S**poofing 伪装：谁能假装成"你"？
  - **T**ampering 篡改：路上的数据能被改吗？
  - **R**epudiation 抵赖：干了坏事能不能赖掉（无日志）？
  - **I**nformation Disclosure 信息泄露：不该看的人看到了？
  - **D**enial of Service 拒绝服务：把服务搞瘫？
  - **E**levation of Privilege 提权：普通用户变管理员？
- 每条威胁 → 一个**缓解措施**（对应 DFD 上画一道"盾牌"）。

## 🟣 部分四·深挖层（为什么 & 延伸）
为什么安全高手都先做威胁建模？因为**亡羊补牢的成本是未雨绸缪的 10–100 倍**（IBM 每年《数据泄露成本报告》都印证）。历史著名案例：
- **2007 年 TJX 信用卡泄露**：没把无线 POS 流量加密，被蹭网监听，4500 万卡号泄露。如果当初威胁建模时问了"无线链路会不会被监听"，就不会犯这错。
- **攻击树（Attack Tree）**：贝尔实验室的 Schneier 提出——把"攻破系统"作为树根，子树是子目标（拿到密码、拿到服务器、提权…），叶子是具体手段。顶级工程师用它算"最低成本攻击路径"，优先堵最便宜的那条。

## 🔴 部分五·顶级视角
顶级安全工程师把威胁建模当**设计阶段的强制环节**（"没有威胁模型，不许写代码"）。它和架构设计（方向6 BE16 微服务）、合规（方向7c SEC87）、风险评估（SEC2）完全相通。高手甚至会在**代码合并前**用工具（如 Microsoft Threat Modeling Tool、OWASP Threat Dragon）自动比对设计图和实现。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 你画的图、做的实验，**对象是你自己搭的环境**（本地 Flask、自己云账号的测试实例）。
- ✅ 威胁建模是**纯思考+画图**，本身就完全合法，是防御工作的核心。
- 🚫 绝不把威胁建模用到"怎么打别人网站"上；只用来"怎么守住自己的系统"。
- 🚫 不登录他人账号、不爬受限内容、不绕实名监管。

## ✅ 验收
交一份 `my-first-threat-model.drawio`（或导出的图片），包含：浏览器→服务器→数据库三条数据流 + 每流至少 1 条 STRIDE 风险 + 对应缓解。发我，我批改。

## ⚠️ 常见坑
- 只画"正常流程"，不画"坏人视角"——威胁建模的灵魂是**站在攻击者角度**。
- 想一口吃成胖子，列 100 条威胁导致放弃。先只做"最小可用模型"，3 条威胁就够。
- 手疼就停，画图很耗眼，注意间隔。

## ➡️ 下一步
**SEC2 · CIA / 风险管理**（把"资产/威胁/暴露"用一套标准语言量化，承接本节的"威胁"概念）。

---

# SEC2 · CIA 三性 / 风险管理（资产·威胁·暴露）

## 🎯 目标
学完这节能用 **CIA 三性**（机密性/完整性/可用性）给任何系统的安全要求贴标签，并能用"资产—威胁—暴露—风险"四步法做一次真实的风险评估。

## 📋 小白前置
- SEC1（已会画威胁模型、知道资产/威胁/漏洞是什么）

## 🟢 部分一·最浅层（生活比喻）
CIA 三性就像你日记本的三道保护：
- **机密性（Confidentiality）**：只有你能看——上锁。别人偷看就破功。
- **完整性（Integrity）**：内容不被偷偷改——你写"今天开心"，坏人不能改成"今天破产"。
- **可用性（Availability）**：你想看时打不开——锁锈死了、被人泼了胶水，也算被破坏。

一个系统只要这三样里任一被打破，就"不安全"了。风险管理就是：先数数你有多少宝贝（资产）、谁想偷（威胁）、门漏不漏（暴露）、值不值得请保镖（风险等级）。

## 🟡 部分二·动手层（逐字操作）
我们用一个表格做一次"家庭账本 App"的风险评估。**只动脑子+表格，不用装环境**。

1. 新建一个 Excel / 记事本表格，列 5 列：`资产 | 威胁 | 暴露点 | CIA影响 | 风险等级(高/中/低)`。
2. 填 3 行（可直接复制）：
```
资产=用户密码  | 威胁=数据库被拖库 | 暴露点=密码明文存储 | CIA=机密性 | 风险=高
资产=记账金额  | 威胁=请求被篡改   | 暴露点=前端可改金额 | CIA=完整性 | 风险=高
资产=App本身   | 威胁=服务器被打瘫 | 暴露点=无限流       | CIA=可用性 | 风险=中
```
3. 对"风险=高"的两行，各加一列 `缓解`：
   - 密码明文 → 改成哈希存储（接 SEC4/方向6 BE6）。
   - 金额可篡改 → 服务端再校验（接 SEC10/SEC11）。
4. 截图发我。

## 🔵 部分三·原理层（底层发生了什么）
- **机密性**靠"访问控制 + 加密"：只有持钥匙（密钥/权限）的人能读。
- **完整性**靠"校验"：哈希值、数字签名能证明"没被改过"（SEC4 会讲）。
- **可用性**靠"冗余 + 抗洪峰"：多副本、限流、CDN（方向6 BE13/BE15）。
- **风险 = 威胁发生概率 × 影响程度**。安全预算永远有限，所以按风险排序修——这叫**风险管理（Risk Management）**。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：**CIA 三性**源自 1970 年代美军/NSA 的计算机安全评估标准（TCSEC，"橘皮书"）。它把"安全"从模糊口号变成可考核的三指标。
- 延伸：现代社会又加了几条——**可抵赖性（Non-repudiation，干了事赖不掉，靠日志+签名）**、**真实性（Authenticity）**。顶级工程师眼里，安全是**一个连续谱**，不是"安全/不安全"开关。

## 🔴 部分五·顶级视角
顶级工程师做安全决策时，先问"我们要保的是 CIA 里哪一条？"
- 银行系统：机密性+完整性 > 可用性（宁可暂时打不开，也不能错账）。
- 新闻门户：可用性 > 机密性（内容本就公开，怕的是宕机）。
这就是**安全需求优先级**，直接决定技术选型。和 SEC1 威胁建模、SEC87 合规一脉相承。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 风险评估是**防御设计**动作，完全合法，是白帽基本功。
- ✅ 你评估的对象 = 自己项目 / 公司授权范围。
- 🚫 不拿"风险评估"当借口去扫描别人资产；扫描别人 = 违法。

## ✅ 验收
交一份风险评估表（≥3 行资产，含 CIA 标注 + 风险等级 + 缓解），发我批改。

## ⚠️ 常见坑
- 把"威胁"和"漏洞"混为一谈：威胁是"谁想搞你"，漏洞是"你哪漏了"。
- 风险等级全标"高"——等于没排序，失去管理意义。按真实概率/影响分档。

## ➡️ 下一步
**SEC3 · 攻击面 / 信任边界**（把"暴露点"具体化：系统哪些地方是对外开口的、哪里是绝不能信的）。

---

# SEC3 · 攻击面 / 信任边界（边界与隐式信任）

## 🎯 目标
学完这节能在一张架构图上，用红线圈出系统的**全部攻击面**（对外开口），并标出**信任边界**（"这边的东西不能信那边"），理解"隐式信任"为何是万恶之源。

## 📋 小白前置
- SEC1（威胁模型、DFD）
- SEC2（资产/暴露点）
- 方向6 BE1–BE3（请求-响应、路由、中间件概念）

## 🟢 部分一·最浅层（生活比喻）
攻击面 = 你房子**所有对外开口**：门、窗、排气扇、甚至邮筒。开口越多，小偷机会越多。
信任边界 = 你家**院墙**：墙内是"自己人/可信"，墙外是"外人/不可信"。
**隐式信任** = 你以为"反正进了院墙就是好人"，结果小偷从邮筒塞了张假快递单，你家佣人（内部组件）就乖乖照做。很多大漏洞，就是"墙内的组件傻乎乎相信墙外来的数据"。

## 🟡 部分二·动手层（逐字操作）
我们在 Docker 里跑一个**合法靶场 OWASP Juice Shop**（开源、专为练手设计），观察它的攻击面。

> ⚠️ 仅在你自己电脑的 Docker 里运行，这是授权学习环境。

1. 打开终端（PRE6），复制粘贴：
```bash
docker run -d -p 3000:3000 --name juice bkimminich/juice-shop
```
2. 浏览器开 `http://localhost:3000`。
3. 任务（纯观察，不攻击）：用浏览器"按 F12 → 网络(Network)"，刷新页面，看它发了多少请求（这就是攻击面的一部分：每个接口都可能是入口）。
4. 在纸上画：浏览器 →(HTTP)→ Juice Shop →(SQL)→ 数据库。用红笔在"浏览器→服务器"之间画一道**信任边界**。
5. 在边界旁写："这里进来的数据（搜索框、登录框）都不可信。"——这就是本节核心。

## 🔵 部分三·原理层（底层发生了什么）
- **攻击面（Attack Surface）** = 所有**未授权用户能输入数据或获取数据的入口**：URL 参数、表单、HTTP 头、文件上传、API、甚至是错误消息里的版本号。
- **信任边界（Trust Boundary）** = 数据从"不可信区"进入"可信区"的那条线。跨过这条线的**每一份数据都必须被验证、清洗、转义**。
- **隐式信任（Implicit Trust）** = 代码在边界处"偷懒"，假设"进来的肯定是好人给的"。80% 的注入/越权漏洞（SEC10/SEC11）根因在此。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：**"从不信任，始终验证"（Zero Trust，零信任）** 由 Forrester 的 Kindervag 在 2010 年提出，彻底推翻"内网=安全"的旧假设。Google 的 BeyondCorp 是标杆实践。
- 案例：2013 年 **Target 被黑**，攻击者先从 HVAC（空调供应商）的弱账号进内网，内网各系统"隐式信任"供应商网络，结果一路摸到收银机。根因就是**信任边界画错/没画**。

## 🔴 部分五·顶级视角
顶级工程师在**设计评审**时第一句话就是："信任边界画哪？"他们把"边界处必须做输入验证"写成**架构硬性规则**（接 SEC7 SDLC、SEC84 DevSecOps）。在微服务（BE16）里，服务间也要 mTLS（双向认证），因为"内网也不可信"。这和 SEC1 威胁模型、SEC2 风险管理打通。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ Juice Shop 是 OWASP 官方授权靶场，本地 Docker 跑，**完全合法**。
- ✅ 本节只"观察攻击面、画边界"，不动手打。
- 🚫 绝不在未经授权的网站做"端口扫描/接口探测"；那等于去别人家门口数窗户，违法。

## ✅ 验收
交一张"Juice Shop 攻击面 + 信任边界"手绘/电子图，标注≥3 个入口 + 1 条边界 + "边界处必须验证"说明。发我。

## ⚠️ 常见坑
- 以为"内网/后端"就安全——错，零信任时代没有内部好人。
- 攻击面只数"网页输入框"，漏掉 HTTP 头、API、错误信息等隐蔽入口。
- 手疼别硬画，截图+简单标注即可。

## ➡️ 下一步
**SEC4 · 密码学基础**（信任边界处怎么用加密/哈希/签名，把"不可信数据"变可信）。

---

# SEC4 · 密码学基础（哈希 / 对称 / 非对称 / 签名 / PKI）

## 🎯 目标
学完这节能说清：**哈希、对称加密、非对称加密、数字签名**分别解决什么问题；能手写一段 Python 用 `bcrypt`/ `hashlib` 做密码哈希，并理解"为什么不能自己发明加密算法"。

## 📋 小白前置
- 方向1a PY1–PY12（变量、字符串、函数、文件、字节串基础）
- SEC2（机密性/完整性概念）
- SEC3（信任边界处需要加密保护）

## 🟢 部分一·最浅层（生活比喻）
- **哈希（Hash）** = 榨汁机。苹果进去，出来一杯固定颜色的汁；你没法从汁还原苹果。用来"验真身"：两次输入得到同一杯汁，说明是同一人。
- **对称加密** = 同一把钥匙锁门和开门。快，但钥匙得安全送给对方。
- **非对称加密** = 邮箱：公钥（挂在门口的投信口）谁都能往里塞信，私钥（只有你有）才能打开取信。
- **数字签名** = 你在合同上按手印（用私钥），别人拿你的公开手印模板（公钥）一比对，知道确实是你签的、且没被改。

## 🟡 部分二·动手层（逐字操作）
我们在**自己电脑**用 Python 实操（合法、本地）。

1. 装库（复制粘贴）：
```bash
pip install bcrypt
```
2. 新建 `hash_demo.py`，复制以下内容（护手：直接粘贴）：
```python
import bcrypt, hashlib

# 1) 密码哈希（正确做法）
pw = b"MySecret123!"
hashed = bcrypt.hashpw(pw, bcrypt.gensalt())
print("哈希后:", hashed)
print("验证正确密码:", bcrypt.checkpw(pw, hashed))
print("验证错误密码:", bcrypt.checkpw(b"wrong", hashed))

# 2) 简单哈希（仅演示，别用于密码）
print("sha256:", hashlib.sha256(b"hello").hexdigest())
```
3. 运行：
```bash
python hash_demo.py
```
你会看到：正确密码验证 `True`、错误密码 `False`。这就是"密码从不明文存"的原理。

## 🔵 部分三·原理层（底层发生了什么）
- **哈希**：把任意长度输入映射成固定长度输出（如 SHA-256 出 256 位）。特性：单向、雪崩（改一个字符输出全变）、抗碰撞（难找两个输入同输出）。**哈希 ≠ 加密**（加密可逆，哈希不可逆）。
- **对称加密（AES）**：用同一密钥加解密，快，适合大量数据。难点是"密钥怎么安全分发"。
- **非对称（RSA / ECC）**：公钥加密、私钥解密（保密）；私钥签名、公钥验签（认证+完整性）。慢，通常只用来**协商一个对称密钥**，之后用对称加密传数据（TLS 就是这么干的，见 NET9）。
- **数字签名** = 对数据哈希后用私钥加密哈希值；验证方用公钥解密哈希并比对。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：1976 年 Diffie–Hellman 提出公钥思想（打破"必须预先共享密钥"），1977 年 RSA 落地。现代 **PKI（公钥基础设施）** = 用"证书颁发机构 CA"给公钥背书（你访问 https 时浏览器验证的就是 CA 链条，见 NET9）。
- 为什么"加盐（salt）"？因为相同密码哈希值相同，攻击者可拿"彩虹表"反查。`bcrypt.gensalt()` 就是自动加盐。顶级工程师从不为密码用裸 `md5/sha256`——那会被秒破。

## 🔴 部分五·顶级视角
顶级工程师的密码学铁律：**绝不用自己写的加密、绝不用已被淘汰的算法（MD5/DES/SHA1）、绝不放盐/密钥到代码仓库**。他们用**经过审计的库**（bcrypt/argon2/scrypt，接方向6 BE6），并理解**前向安全**（NET9 TLS1.3）。密码学是数学，不是魔法——边界错一点点，全盘崩。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 本地跑 Python 哈希、**自己给自己账号做实验**，完全合法。
- ✅ 理解加密是为了**保护用户数据**（防御），不是用来藏违法东西。
- 🚫 不试图破解他人密码、不写"密码恢复/绕过"工具给别人用；不绕实名监管。
- 🚫 不把密钥/盐硬编码进要发布的代码（那是给攻击者送礼）。

## ✅ 验收
交 `hash_demo.py` 运行截图：显示正确/错误密码验证结果。并写一句话：为什么密码存储要用 bcrypt 而不是 sha256 明文。发我。

## ⚠️ 常见坑
- 把哈希当加密（哈希不可逆，丢了就找不回明文）。
- 自己发明"加密算法"——99.9% 一戳就破，用标准库。
- 复用同一盐 / 盐写死——失去防彩虹表意义。

## ➡️ 下一步
**SEC5 · 认证框架（OAuth2 / OIDC / SAML / SSO）**（在边界处，用什么协议证明"你是你"）。

---

# SEC5 · 认证框架（OAuth2 / OIDC / SAML / SSO）

## 🎯 目标
学完这节能分清**认证（Authentication，你是谁）vs 授权（Authorization，你能干啥）**；能说清 OAuth2 的"授权码流程"每一步在干嘛，并**自己搭一个用 GitHub 登录的小页面**（合法，用你自己的 GitHub OAuth App）。

## 📋 小白前置
- SEC4（非对称/签名，理解 token 怎么被验）
- 方向6 BE5（认证授权基础：session/JWT）
- 方向6 FE5–FE6（前端发请求、处理跳转）

## 🟢 部分一·最浅层（生活比喻）
- **认证** = 门禁刷卡证明"你是公司员工张三"（你是谁）。
- **授权** = 你的卡只能开 3 楼，开不了机房（你能干啥）。
- **SSO（单点登录）** = 用一张园区总卡，刷完能进食堂、健身房、办公室，不用每张门各办一张。
- **OAuth2** = 你把"代取快递"的临时权限（不是家门钥匙！）给了快递柜，它只能开那个柜子，且到点失效。

## 🟡 部分二·动手层（逐字操作）
我们用 **PortSwigger Web Security Academy** 的免费在线 lab 理解 OAuth（授权靶场，合法）。

> ⚠️ 仅在你自己浏览器 + 官方授权 lab 进行。

1. 浏览器开 `https://portswigger.net/web-security/oauth`（官方免费实验区）。
2. 读页面顶部"OAuth 认证漏洞"介绍，点开第一个 lab（如 OAuth 认证绕过），按它给的步骤走：**全程在它提供的沙箱浏览器里操作**，不要去真实网站试。
3. 同时在本地新建笔记，画**授权码流程**四步（可直接复制背下来）：
```
1. 你点"用GitHub登录" → 跳转GitHub，带 client_id + 重定向地址
2. GitHub问你"授权吗？" → 你点允许
3. GitHub重定向回我的网站，带 code(一次性授权码)
4. 我的服务器拿 code 私聊GitHub换 access_token → 用token调API拿你的昵称
```
4. 截图 lab 完成页发我。

## 🔵 部分三·原理层（底层发生了什么）
- **OAuth2** 是**授权框架**：第三方应用拿**受限的、可撤销的**访问令牌（access_token），**不是**拿到用户密码。四种授权模式里，**授权码模式（Authorization Code）** 最安全（token 只在后端交换，前端只见一次性 code）。
- **OIDC（OpenID Connect）** = 在 OAuth2 上叠一层**身份认证**（返回 id_token，用 JWT 装着"此用户是谁"）。
- **SAML** = 老牌企业级 SSO，用 XML 断言传递身份（银行/高校常用）。
- **SSO** 是目标，OAuth2/OIDC/SAML 是实现手段。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：OAuth 源自 2007 年 Twitter/Google 的"委托授权"需求——用户不想把密码给第三方 App。OAuth2（2012）和 OIDC（2014）逐步标准化。
- 案例：**2018 年 Facebook / 各类 App 的 OAuth 误配**，把 `redirect_uri` 校验做松了，攻击者能劫持授权码换成别人的登录态。根因：没严格校验回调地址（接 SEC11 越权/SEC16）。

## 🔴 部分五·顶级视角
顶级工程师配置 OAuth 时死守几条：**严格白名单校验 redirect_uri、用 PKCE 防授权码拦截（公开客户端必备）、access_token 短命+refresh_token 轮换（接 SEC6）、id_token 必须验签名和 audience**。他们知道：认证协议 90% 的漏洞在**实现细节**（重定向、state 防 CSRF），不是协议本身。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 官方授权 lab（PortSwigger）里练，**完全合法**。
- ✅ 自己注册 OAuth App（GitHub 给开发者免费建）做"用 GitHub 登录我的站点"，**合法且是真实技能**。
- 🚫 不伪造他人 OAuth 登录、不劫持别人授权码；不搭"钓鱼登录页"套别人账号——那是犯罪。

## ✅ 验收
交 PortSwigger lab 完成截图 + 你手写的"授权码四步"说明。发我批改。

## ⚠️ 常见坑
- 把认证和授权混为一谈（OAuth2 是授权，OIDC 才是认证）。
- 以为"用了 OAuth 就安全"——配置错（redirect_uri 不严、缺 state）照样崩。
- 手疼别硬敲，复制官方文字即可。

## ➡️ 下一步
**SEC6 · 鉴权 / 会话管理（令牌 / 轮换 / 绑定）**（拿到 token 后，怎么管才不被偷、不被顶号）。

---

# SEC6 · 鉴权 / 会话管理（令牌 / 轮换 / 绑定）

## 🎯 目标
学完这节能讲清 **Session vs JWT** 的取舍；能**自己写一段 Flask 登录+会话代码**，并指出 3 个常见会话漏洞（固定/劫持/无失效）及修法。

## 📋 小白前置
- SEC5（认证框架、token 概念）
- 方向6 BE5（session/JWT/OAuth2 入门）
- 方向6 FE8（浏览器存储：cookie/localStorage 区别）

## 🟢 部分一·最浅层（生活比喻）
登录就像住酒店：
- **Session** = 前台给你一张房卡（session id），前台电脑里存着"这卡对应 302 房"。卡丢了，捡到的人能进你房（会话劫持）。
- **JWT** = 房卡上直接印着"302 房、有效期到今晚、酒店防伪签名"（自包含 token）。服务员扫一眼签名就信，不用查前台。但卡一旦发出，酒店**没法提前作废**它（除非另建黑名单）。
- **轮换** = 每次刷次卡，前台给你换新卡，旧卡失效——小偷捡到旧卡也进不去。

## 🟡 部分二·动手层（逐字操作）
本地搭一个**合法 Flask 登录实验**（自己写、自己测，完全授权）。

1. 装库：
```bash
pip install flask
```
2. 新建 `login_lab.py`，复制：
```python
from flask import Flask, request, session, redirect, url_for
app = Flask(__name__)
app.secret_key = b"CHANGE_ME_TO_RANDOM_32BYTES"  # 真实项目用随机密钥!

users = {"alice": "password123"}

@app.route("/login", methods=["POST"])
def login():
    u, p = request.form.get("user"), request.form.get("pwd")
    if users.get(u) == p:
        session["user"] = u          # 写入会话(服务端session)
        return "登录成功"
    return "失败", 401

@app.route("/me")
def me():
    return f"当前用户: {session.get('user', '未登录')}"

if __name__ == "__main__":
    app.run(debug=True, port=5000)
```
3. 运行 `python login_lab.py`，浏览器开 `http://localhost:5000/me` → 显示"未登录"。
4. 用 curl 测登录（复制）：
```bash
curl -X POST -d "user=alice&pwd=password123" -c cookie.txt http://localhost:5000/login
curl -b cookie.txt http://localhost:5000/me
```
第二次应显示"当前用户: alice"。**这就是会话在干活**。

## 🔵 部分三·原理层（底层发生了什么）
- **Session**：服务端存状态（内存/Redis），浏览器只持 `session id`（通常放 httpOnly cookie）。优点：可随时吊销；缺点：服务端要存、扩展开销大。
- **JWT**：服务端**不存状态**，token 自带签名+声明（sub/exp）。优点：天然适合分布式/微服务；缺点：**无法主动失效**（撤销需黑名单或短过期+刷新）。
- **关键安全属性**：cookie 设 `HttpOnly`（防 JS 偷）、`Secure`（仅 HTTPS）、`SameSite`（防 CSRF，接 SEC16）；token 要绑 **IP/设备指纹** 防盗用。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：早期 Web 无状态，Session 是补丁；JWT（2014，RFC 7519）为移动/API 而生。
- 案例：**Session 固定攻击**——攻击者先拿一个 session id 塞给你，你登录后该 id 直接绑定你的账号，攻击者凭同 id 进你账户。修法：登录成功**立刻重新生成 session id**。

## 🔴 部分五·顶级视角
顶级工程师的会话设计：**访问令牌短命（15min）+ 刷新令牌长命且可轮换/可吊销（SEC5 PKCE）**；刷新令牌存 HttpOnly cookie 并绑设备；JWT 用非对称签名（RS256）让资源服务器只持公钥验签。他们把"会话失效"当成一等公民需求，而不是事后补丁。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 自己写 Flask 登录、自己 curl 自己，**完全合法**。
- ✅ 理解会话劫持是为了**写更安全的登录**（防御）。
- 🚫 不窃取他人 cookie、不种 session 固定陷阱到别人站、不做"账号盗用"工具。

## ✅ 验收
交 `login_lab.py` 代码 + 两次 curl 结果截图，并文字写出 3 个会话漏洞名称及修法。发我。

## ⚠️ 常见坑
- `secret_key` 写死成示例值（真实项目用 `os.urandom(32)` 或从环境变量读）。
- 以为 JWT 能随便吊销——需在架构上设计刷新/黑名单。
- `debug=True` 仅本地练习用，真实部署必须关（会泄露堆栈/可远程执行）。

## ➡️ 下一步
**SEC7 · 安全开发生命周期 SDLC（左移 / 威胁建模）**（把前面学的思维，嵌进"写代码的每一步"）。

---

# SEC7 · 安全开发生命周期 SDLC（左移 / 威胁建模）

## 🎯 目标
学完这节能画出"带安全关卡的软件开发流程图"，理解**左移（Shift Left）** 为什么省钱，并**自己给一个小项目加一个 CI 安全检查步骤**（用 GitHub Actions 跑 `bandit` 扫 Python）。

## 📋 小白前置
- SEC1（威胁建模）、SEC2（风险管理）
- 方向10 OPS29–OPS31（Git / GitHub Actions 基础，至少懂 commit/push）

## 🟢 部分一·最浅层（生活比喻）
普通开发像"盖好楼再请消防来检查"——查出来要拆墙，贵。
**安全 SDLC** 像"从画图纸那天，消防就坐旁边审"——边画边改，便宜。
**左移** = 把"安全检查"从最右（上线后）移到最左（写第一行代码前）。越靠左发现问题，改的成本越低。

## 🟡 部分二·动手层（逐字操作）
我们在**自己 GitHub 仓库**加一个免费安全检查（合法、你自己的项目）。

1. 在本地产一个含故意"危险函数"的 Python 文件 `demo.py`：
```python
import os
def f(user_input):
    os.system("echo " + user_input)   # 故意：命令注入风险，仅演示
```
2. 本地先跑扫描器 `bandit`（复制）：
```bash
pip install bandit
bandit -r demo.py
```
你会看到它**报警** `B605: 使用 os.system 可能命令注入`。这就是"左移"——写代码时立刻发现。
3. 推到你的 GitHub 仓库，加文件 `.github/workflows/security.yml`：
```yaml
name: security
on: [push]
jobs:
  bandit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: pip install bandit && bandit -r .
```
4. 每次 push，GitHub 自动扫。截图 Actions 运行结果发我。

## 🔵 部分三·原理层（底层发生了什么）
- **SDLC（Software Development Life Cycle）** = 需求→设计→编码→测试→上线→运维。安全 SDLC 在**每一阶段**嵌安全检查：设计阶段做威胁建模（SEC1）、编码阶段用 SAST（静态扫，如 bandit）、测试阶段用 DAST（动态扫）、上线后做监控。
- **左移** 的底层逻辑：**缺陷修复成本随阶段指数上升**（IBM 数据：需求期改 1 元，上线后改 100 元）。
- **DevSecOps（SEC84）** = 把安全自动化嵌进 CI/CD 流水线，不靠人工。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：微软 2004 年发布**SDL（Security Development Lifecycle）**，是首个企业级安全开发强制流程（比尔·盖茨"可信计算"倡议）。后来演进出 **BSIMM**（软件安全成熟度模型）让企业自评。
- 顶级视角：安全不是"测试阶段插进来"，而是**贯穿的"质量属性"**，和性能、可维护性并列。

## 🔴 部分五·顶级视角
顶级团队把安全做成**自动化门禁**：PR 不通过 SAST/依赖扫描（SCA，如 Dependabot）就不让合并；镜像不扫漏洞（Trivy，OPS46）不让部署。他们用**威胁建模卡（Threat Model Card）** 随设计文档一起评审。安全左移 = 把"人肉把关"变成"工具卡位 + 人审例外"。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 在你**自己仓库**跑 bandit、加安全 CI，**完全合法**，是白帽/工程师真实技能。
- ✅ 目的是"让自己的代码更稳"。
- 🚫 不把安全扫描器指向别人仓库/网站（那是未授权扫描，违法）。
- 🚫 不为绕过别人扫描写"规避规则"。

## ✅ 验收
交 GitHub Actions 安全扫描运行截图（显示 bandit 报警或修复后通过）+ 一句话解释"左移为什么省钱"。发我。

## ⚠️ 常见坑
- 只在"上线前"才想安全——成本最高。
- 装了扫描器但"报警就忽略"——等于没装。先修高危（B 级命令注入类）。
- 手疼用语音/复制，别硬敲 YAML。

## ➡️ 下一步
**SEC8 · 威胁情报 / MITRE ATT&CK（战术·技术映射）**（从"攻击者视角"看一整套标准打法图谱）。

---

# SEC8 · 威胁情报 / MITRE ATT&CK（战术·技术映射）

## 🎯 目标
学完这节能看懂 **MITRE ATT&CK 矩阵**，知道"初始访问、执行、提权、横向移动…"每个战术下有哪些技术，并能把一次真实攻击（如某勒索软件报告）**对照矩阵标注出来**——这是蓝队/防御方的"敌人字典"。

## 📋 小白前置
- SEC1（威胁建模思维）
- SEC3（攻击面/信任边界，理解"入口"概念）
- 方向7c SEC56–SEC59（恶意软件分类，可选先扫一眼）

## 🟢 部分一·最浅层（生活比喻）
ATT&CK 矩阵像一本"小偷作案手法大全"，按**作案步骤**分成 14 个货架（战术）：怎么进门（初始访问）、怎么动手（执行）、怎么升职（提权）、怎么串门（横向移动）…
每个小格是一种具体手法（技术），比如"钓鱼邮件""用被盗账号"。防御方背熟这本字典，监控时一眼认出"哦，他正在用'有效账号'这招"。

## 🟡 部分二·动手层（逐字操作）
我们**只读官方公开资料 + 做标注练习**（不下载任何恶意样本，合法）。

1. 浏览器开 `https://attack.mitre.org/matrices/enterprise/`（官方公开矩阵）。
2. 看顶部分类：Initial Access / Execution / Persistence / Privilege Escalation / Lateral Movement …
3. 找一个你熟悉的技术点，比如 `T1190 Exploit Public-Facing Application`（利用面向公网的漏洞）——这正是 SEC10–SEC22 要练的。
4. 新建笔记，画 5 行表格：`战术 | 技术编号 | 技术名 | 我学过的对应课次`。填 5 行，例如：
```
初始访问 | T1190 | 利用公网应用漏洞 | SEC10 注入
执行     | T1059 | 命令与脚本解释器 | SEC10 命令注入
凭据访问 | T1110 | 暴力破解        | SEC5/SEC6 认证
横向移动 | T1021 | 远程服务        | SEC3 信任边界
提权     | T1068 | 利用漏洞提权    | (方向7b SEC46)
```
5. 截图/文件发我。

## 🔵 部分三·原理层（底层发生了什么）
- **ATT&CK（Adversarial Tactics, Techniques, and Common Knowledge）** 是 MITRE 维护的**基于真实观察**的攻击者行为知识库。它不是"理论"，是"真黑客用过什么"的实证汇编。
- **战术（Tactic）** = 攻击者的目标阶段（为什么做）；**技术（Technique）** = 达成目标的具体手段（怎么做）。
- 它和威胁建模（SEC1）互补：STRIDE 是"从资产出发想风险"，ATT&CK 是"从攻击者出发想手法"。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：MITRE 在 2013 年发布 ATT&CK，源自 Lockheed Martin 的 **Kill Chain（杀伤链）** 模型，但更细、更实证。如今成行业事实标准，SIEM/EDR（SEC64）都按它写检测规则。
- **威胁情报（Threat Intel）** = 关于敌人的结构化信息（谁、用什么技战术、针对谁）。分战略/战术/作战/技术四层。顶级防御方用 ATT&CK 做**差距分析**："矩阵里 100 个技术，我监控能覆盖几个？"

## 🔴 部分五·顶级视角
顶级安全运营（SEC89 威胁狩猎）的核心工作："**假设驱动**"——基于 ATT&CK 提假设（"攻击者可能用 T1059 命令执行"），然后去日志里找证据。他们建**检测覆盖率仪表盘**，按 ATT&CK 技术逐格填"已覆盖/未覆盖"，把防御做到可度量。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 读 **MITRE 官方公开矩阵**、做标注练习，**完全合法**（这是防御方必备知识）。
- ✅ 学 ATT&CK 是为了**建检测、做防御**，不是照着去打人。
- 🚫 不下载/运行任何 ATT&CK 里提及的恶意工具或样本；本环境只做"读+标注"，绝不实践攻击技术。

## ✅ 验收
交一张"5 行 ATT&CK 对照表"（战术/编号/技术名/对应课次）。发我批改。

## ⚠️ 常见坑
- 把 ATT&CK 当"攻击教程"去试——错，它是防御字典，本环境严禁实践。
- 只看技术名不懂"它属于哪个战术目标"——一定要配对战术。
- 信息量大，手疼就每次只标 5 行，慢慢来。

## ➡️ 下一步
**SEC9 · OWASP Top10（分类与根因）**（Web 安全最权威的"十大风险榜"，把前面零散概念收口）。

---

# SEC9 · OWASP Top10（分类与根因）

## 🎯 目标
学完这节能背出 **OWASP Top 10（2021 版）** 十大 Web 风险，并理解每一条的**根因**；能手查 Juice Shop 里对应哪几条（观察级，不攻击）。

## 📋 小白前置
- SEC1–SEC8（已具备威胁建模、CIA、边界、密码学、认证、会话、ATT&CK 基础）
- 方向6 Web 全栈（FE/BE 基础）

## 🟢 部分一·最浅层（生活比喻）
OWASP Top10 = Web 安全的"交通事故十大高发原因"年报。每年更新一次，列出"最容易出事、出了事最惨"的十类问题。学安全先背这十类，就像学医先背"十大常见病"。

## 🟡 部分二·动手层（逐字操作）
我们**读官方清单 + 在本地 Juice Shop 做"找对应"观察**（靶场合法，本次只观察不攻击）。

1. 浏览器开 `https://owasp.org/Top10/`（官方 2021 版，公开免费）。
2. 把十大抄进笔记（可直接复制）：
```
A01 失效的访问控制(Broken Access Control)
A02 加密机制失效(Cryptographic Failures)
A03 注入(Injection)
A04 不安全设计(Insecure Design)
A05 安全配置错误(Security Misconfiguration)
A06 易受攻击和过时的组件(Vulnerable Components)
A07 身份和认证失败(Identification & Auth Failures)
A08 软件和数据完整性故障(Software & Data Integrity)
A09 安全日志和监控失败(Logging & Monitoring Failures)
A10 服务端请求伪造(SSRF)
```
3. 启动本地 Juice Shop（`docker run -d -p 3000:3000 bkimminich/juice-shop`），开 `http://localhost:3000`。
4. 在首页找"搜索框""登录框""商品评论"——分别对应 A03 注入、A07 认证、A01 越权。只在纸上标注，**不提交任何攻击 payload**。

## 🔵 部分三·原理层（底层发生了什么）
- OWASP（开放 Web 应用安全项目）是**非营利安全组织**，Top10 是行业共识的"Web 最高风险榜"。
- 每条背后是**根因模式**：
  - A01 根因 = 没在**每个请求**做"你有权看这个吗"（SEC3 信任边界没守）。
  - A03 根因 = 把**数据当代码**执行（SEC10 详解）。
  - A07 根因 = 认证逻辑脆弱（SEC5/SEC6）。
  - A10 SSRF = 服务器被诱导访问**内部资源**（SEC12 详解）。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：OWASP Top10 自 2003 年起每 3–4 年更新；2021 版把"不安全设计"单列（A04），强调**设计期就要安全**（呼应 SEC7 左移）。
- 案例：A06"过时组件"——**2017 Equifax 泄露 1.47 亿人信息**，根因是 Struts2 框架一个已知漏洞（CVE-2017-5638）没打补丁。一个该更新的组件，毁了一家公司。

## 🔴 部分五·顶级视角
顶级工程师把 Top10 当**需求清单+测试清单**：写代码时对照"这条我防了吗"，上线前用 DAST/依赖扫描（SEC7/SEC84）逐条核对。他们知道 Top10 不是全部风险，只是"最高频十类"——真正高手还会看 OWASP **Top 10 Proactive Controls（主动防护十法）**，从正面建安全。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 读 OWASP 官方文档、在**自己 Docker 靶场**做"风险对应观察"，**完全合法**。
- ✅ 理解 Top10 是为了**按图索骥地防御**。
- 🚫 不在任何非授权网站"对照 Top10 找漏洞"；那是未授权测试，违法。

## ✅ 验收
交"OWASP Top10 十类 + 每类一句根因 + 在 Juice Shop 标出的 3 个对应入口"笔记。发我。

## ⚠️ 常见坑
- 把 Top10 当"攻击清单"去逐条试——错，它是"防护清单"。
- 记混 A01/A07（越权 vs 认证）——记住：认证是"你是谁"，访问控制是"你能不能动这个"。
- 手疼别硬背，先抄官方，慢慢理解。

## ➡️ 下一步
**SEC10 · 注入 SQL / XSS / 命令 / 模板（解析器混淆 + 参数化）**（Top10 里最高频的 A03，逐类拆开练）。

---

# SEC10 · 注入 SQL / XSS / 命令 / 模板（解析器混淆 / 参数化）

## 🎯 目标
学完这节能讲清**注入的本质 = 数据被当成代码执行**；能在**自己搭的漏洞环境**里复现 SQL 注入与 XSS，并**写出参数化/转义的修复代码**。这是 Web 安全最核心的一节。

## 📋 小白前置
- SEC3（信任边界：外来数据不可信）
- SEC9（A03 注入是 Top10 之一）
- 方向5 DB3–DB4（SQL 查询、JOIN 基础）
- 方向6 BE1–BE4（后端接收参数、模板渲染）

## 🟢 部分一·最浅层（生活比喻）
你让厨师按"菜单+你的备注"做菜。正常备注："少放辣"。
**注入** = 你在备注里写："少放辣；顺便把厨房门锁打开"。如果厨师**不分清"菜名"和"指令"**，就会照做——把你的数据（备注）当成了操作指令（开厨房门）。SQL 注入、命令注入、XSS 都是同一毛病：**没把"数据"和"代码"分开**。

## 🟡 部分二·动手层（逐字操作）
搭一个**故意带漏洞的本地 Flask + SQLite**（自己写、自己打自己，合法）。

1. 装库：`pip install flask`
2. 新建 `sqli_lab.py`（漏洞版）：
```python
import sqlite3
from flask import Flask, request
app = Flask(__name__)

def get_user(name):
    conn = sqlite3.connect("test.db")
    # 漏洞：字符串拼接，把用户输入直接当SQL
    cur = conn.execute("SELECT * FROM users WHERE name = '" + name + "'")
    return cur.fetchall()

@app.route("/user")
def user():
    return str(get_user(request.args.get("name", "")))

if __name__ == "__main__":
    import sqlite3 as s
    c = s.connect("test.db"); c.execute("CREATE TABLE IF NOT EXISTS users(name TEXT)")
    c.execute("INSERT INTO users VALUES('alice')"); c.commit()
    app.run(port=5001)
```
3. 运行，浏览器开：
   - 正常：`http://localhost:5001/user?name=alice` → 返回 `alice`
   - **注入**（复制）：`http://localhost:5001/user?name=alice' OR '1'='1` → 返回所有用户！说明 SQL 被"续写"了。
4. **修复**（改 `get_user` 为参数化）：
```python
cur = conn.execute("SELECT * FROM users WHERE name = ?", (name,))
```
重跑注入 URL → 不再泄露。✅ 这就是**参数化查询**的威力。

## 🔵 部分三·原理层（底层发生了什么）
- 注入根因：**解释器/解析器分不清"数据"和"指令"**。SQL 引擎收到一串文本，分不出哪段是程序员写的、哪段是用户给的。
- **参数化查询（Prepared Statement）** = 先发"模板 `... WHERE name = ?`"，再单独发"参数值"。数据库**永远把参数当数据**，绝不执行。这是根治 SQL 注入的唯一正道。
- **XSS（跨站脚本）** = 把用户输入当 HTML/JS 渲染。修法：**输出转义**（`<` 变 `&lt;`）或 CSP（方向6 FE15）。
- **命令注入** = 用 `os.system(shell=True)` 拼接；修法：用参数列表调用、白名单校验。
- **模板注入 SSTI（SEC19）** = 把用户输入当模板语法执行。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：1998 年第一次公开 SQL 注入；**2014 年 Sony 图片泄露、2019 年 Bulgaria 税务库**都因注入。它二十多年稳居 Top10，因为"拼接字符串"太好写了。
- 顶级视角：**永不拼接、永远参数化**；XSS 用"上下文相关转义"（HTML 体/属性/JS 串 转义规则不同）；命令处理用**无 shell 的 execve**。

## 🔴 部分五·顶级视角
顶级工程师在代码评审时**看到字符串拼接 SQL/HTML/命令就报警**。他们用 **ORM（方向6 BE8/DB26）** 天然参数化，用**自动转义模板引擎**，用 SAST（SEC7 bandit/CodeQL）扫"拼接点"。他们理解注入是**解析器边界问题**——凡是有"解释器"的地方（SQL、Shell、正则、模板、JSON Path）都可能是注入点。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 在**自己写的本地 Flask** 上复现+修复注入，**完全合法**（这是学习的黄金法：建漏洞→打→修）。
- ✅ 一切为了学防御。
- 🚫 绝不拿 `OR '1'='1` 去任何非授权网站试；那是犯罪级未授权测试。
- 🚫 不写"一键注入工具"给人用。

## ✅ 验收
交 `sqli_lab.py` 漏洞版+修复版的代码对比 + 注入 URL 前后结果截图 + 一句话解释"参数化为什么能防住"。发我。

## ⚠️ 常见坑
- 以为"过滤掉单引号"就够了——攻击者用十六进制/注释/编码绕过；只有**参数化**根治。
- 修完只在自己测，忘了 XSS/命令也要各自修。
- 手疼就复制粘贴代码，别硬敲。

## ➡️ 下一步
**SEC11 · 认证授权漏洞（IDOR / JWT 伪造 / 越权）**（Top10 A01/A07 的实战：你能不能动"不该你动"的东西）。

---

# SEC11 · 认证授权漏洞（IDOR / JWT 伪造 / 越权）

## 🎯 目标
学完这节能识别 **水平越权（看别人数据）、垂直越权（普通用户变管理员）、IDOR（改个 ID 就看到别人）**；能**在自己 Flask 里复现 IDOR 并修复**（加"这资源属于你吗"校验）。

## 📋 小白前置
- SEC5（认证：你是谁）、SEC6（会话/令牌）
- SEC9（A01 失效访问控制、A07 认证失败）
- 方向6 BE5（认证授权基础）

## 🟢 部分一·最浅层（生活比喻）
- **水平越权** = 你翻到邻居的信箱（都是住户，但那是别人的）。
- **垂直越权** = 你拿着住户卡刷开了机房（你没这权限）。
- **IDOR** = 你的快递单号是 #1001，你把网址改成 #1002，系统傻乎乎把别人的包裹给你看——因为系统**没问"这包裹是你的吗"**。

## 🟡 部分二·动手层（逐字操作）
搭一个**带 IDOR 的本地笔记服务**（自己写自己测，合法）。

1. 新建 `idor_lab.py`：
```python
from flask import Flask, request, session
app = Flask(__name__)
app.secret_key = b"rand"
notes = {1: "alice的日记", 2: "bob的日记"}

@app.route("/note")
def note():
    nid = int(request.args.get("id", 1))
    return notes.get(nid, "无")

if __name__ == "__main__":
    app.run(port=5002)
```
2. 运行，开：
   - `http://localhost:5002/note?id=1` → `alice的日记`
   - 改 `id=2` → 直接看到 `bob的日记`！**IDOR 漏洞**：任何人改 ID 看别人。
3. **修复**（加归属校验——假设登录用户存 session）：
```python
owners = {1: "alice", 2: "bob"}
@app.route("/note")
def note():
    nid = int(request.args.get("id", 1))
    user = session.get("user", "guest")
    if owners.get(nid) != user:
        return "无权访问", 403
    return notes.get(nid, "无")
```
现在没登录（user=guest）看 #2 → 403。✅ 越权修好了。

## 🔵 部分三·原理层（底层发生了什么）
- **失效的访问控制（Broken Access Control）** = 服务端**没在每个请求验证"调用者对被请求对象有权"**。任何"靠前端隐藏按钮"的权限都是假的——攻击者直接改请求。
- **IDOR（不安全的直接对象引用）** = 用可猜测的 ID（自增数字、UUID）直接定位资源，缺归属检查。
- **JWT 伪造** = 若服务端用 `alg=none` 或弱密钥（HS256 可被爆破），攻击者可改 `{"admin":true}` 重签；修法：用非对称 RS256 + 验 audience/过期（SEC4/SEC6）。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：IDOR 是 2010 年代最多"赏金报告"的类型之一。**2018 年 Facebook/各类社交 App** 因 IDOR 泄露私信/照片。
- 顶级视角：**所有对象访问都必须"基于当前用户身份 + 对象所有者"双重判断**，用**间接引用（映射表）/UUID** 替代自增 ID，后端**永远不信任前端传的权限字段**（"role=admin"绝不来自客户端）。

## 🔴 部分五·顶级视角
顶级工程师把访问控制做成**框架级中间件**（如 Spring Security、Casbin 策略引擎），而非在每个 handler 手写 if。他们做**权限矩阵测试**（每个角色×每个接口自动化验权）。理解"**默认拒绝**"原则：没显式允许的，一律拒绝。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 自己写、自己改 ID 测自己、**本地**，完全合法。
- ✅ 学越权是为了写"每个请求都验权"的稳健后端。
- 🚫 不拿"改 ID"手法去任何非授权网站试；那是违法的未授权访问。

## ✅ 验收
交 `idor_lab.py` 漏洞版+修复版对比 + 改 id 前后结果截图 + 一句话："为什么前端隐藏按钮不等于有权限"。发我。

## ⚠️ 常见坑
- 以为"按钮不显示=没权限"——前端只是体验，后端才是真相。
- 修复时只拦了页面、忘了 API 接口同样要拦。
- 手疼复制代码即可。

## ➡️ 下一步
**SEC12 · SSRF（服务端请求伪造：内网探测 / 云元数据）**（让服务器替你访问"它才能访问的地方"）。

---

# SEC12 · SSRF（服务端请求伪造：内网探测 / 云元数据）

## 🎯 目标
学完这节能讲清 **SSRF = 让服务器替你发请求到它才够得着的地方**；在**自己 Flask** 复现一个"抓取用户输入 URL"的 SSRF，并理解云环境 `169.254.169.254` 元数据危害与修复（禁用内网地址）。

## 📋 小白前置
- SEC3（信任边界、内网概念）、SEC9（A10 SSRF）
- 方向4 NET4（IP/子网，懂 127.0.0.1/内网段）、NET12（代理）
- 方向6 BE1（HTTP 请求）

## 🟢 部分一·最浅层（生活比喻）
你让公司前台（服务器）"去帮我取个快递（URL）"。前台权限大，能进员工区。攻击者骗前台："去 169 号储物柜（云元数据）拿钥匙。" 前台傻乎乎去了——因为它**不觉得"取快递"有啥危险**。SSRF 就是把"取外部 URL"的功能，诱导去碰**只有服务器能碰的内部资源**。

## 🟡 部分二·动手层（逐字操作）
本地复现（自己打自己，合法）。

1. 新建 `ssrf_lab.py`：
```python
import requests
from flask import Flask, request
app = Flask(__name__)

@app.route("/fetch")
def fetch():
    url = request.args.get("url")
    # 漏洞：直接请求用户给的任意URL
    return requests.get(url, timeout=3).text[:500]

if __name__ == "__main__":
    app.run(port=5003)
```
2. 另开一个**只监听本地的服务**模拟"内网秘密"：
```python
# secret.py
from flask import Flask
app = Flask(__name__)
@app.route("/secret")
def s(): return "内部机密: root_password_123"
app.run(port=6000)
```
3. 运行两者。访问：
   - `http://localhost:5003/fetch?url=http://localhost:6000/secret` → 返回"内部机密"！**SSRF**：通过公网接口读到了本应只内网可见的内容。
4. **修复**（黑名单/白名单，示意）：
```python
from urllib.parse import urlparse
def safe(url):
    h = urlparse(url).hostname or ""
    if h in ("localhost","127.0.0.1") or h.endswith(".internal"):
        return False
    return True
```
真实生产还要解析 IP 后判私有段（生产级用 `ipaddress` 模块）。

## 🔵 部分三·原理层（底层发生了什么）
- 服务器常需"替用户抓图/预览链接"，若**不对目标 URL 做限制**，攻击者可指向：
  - 内网服务（`http://169.254.169.254/` 云元数据，拿临时凭证）
  - 本机管理口（`http://localhost:6379` Redis）
  - 其他云内服务。
- 根因：把"用户给的 URL"当**完全可信**去请求（违反 SEC3 边界原则）。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：**2019 年 Capital One 泄露 1 亿用户**，根因是 WAF 配置错 + SSRF 拿到 EC2 元数据 IAM 凭证。云元数据地址 `169.254.169.254` 从此成 SSRF 经典目标。
- 顶级修复：**IMDSv2（带 token 的元数据）**、网络层禁止实例互访、应用层用**允许列表 + 解析后 IP 段校验 + 禁止重定向**。

## 🔴 部分五·顶级视角
顶级工程师防 SSRF 用**纵深防御**：① 应用层严格允许列表；② 解析域名拿真实 IP，拒绝私有段（`10.0.0.0/8`、`172.16/12`、`192.168/16`、`169.254/16`）；③ 网络层让应用服务器**根本访问不到元数据/内网**（分网段、出网白名单）；④ 禁用跟随重定向。多管齐下。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 本地两个 Flask 互相访问、自己测，**完全合法**。
- ✅ 理解 SSRF 是为了在云上**正确配置网络隔离**。
- 🚫 不去探测任何真实云/他人内网元数据；那是严重违法（涉入侵）。

## ✅ 验收
交 `ssrf_lab.py` + `secret.py` + 漏洞复现截图 + 修复思路（允许列表/私有段拒绝）。发我。

## ⚠️ 常见坑
- 只挡 `localhost` 字符串，攻击者用 `127.0.0.1`、`0.0.0.0`、十进制 IP、短域名绕过——必须**解析后判 IP 段**。
- 忘了重定向能绕过校验。
- 手疼复制代码，别硬敲。

## ➡️ 下一步
**SEC13 · 反序列化（对象注入 / gadget）**（把"数据"变回"对象"时，坏人塞了恶意指令）。

---

# SEC13 · 反序列化（对象注入 / gadget）

## 🎯 目标
学完这节能讲清 **反序列化漏洞 = 把"用户控制的字节流"还原成对象时，触发了其中藏好的代码**；在**自己 Python/Java 环境**复现一个不安全反序列化并理解"gadget 链"。

## 📋 小白前置
- SEC10（注入本质：数据当代码）
- 方向1a PY11（面向对象、类）、方向1b JV1（JVM/字节码，Java 反序列化更典型）
- 方向6 BE8（ORM，理解对象↔存储）

## 🟢 部分一·最浅层（生活比喻）
你收到一个"乐高说明书（序列化数据）"，按图拼出玩具（反序列化）。正常说明书拼出小鸭。
**漏洞** = 坏人改了说明书，让你拼到一半**顺手把门也打开了**（执行了恶意代码）。因为"拼装过程"本身会调用某些零件的自带动作（gadget），坏人挑了会干坏事的零件串成链。

## 🟡 部分二·动手层（逐字操作）
用 Python 的 `pickle` 演示（**只在本地、自己序列化自己反序列化**，合法观察）。

1. 新建 `pickle_lab.py`：
```python
import pickle, os
class Evil:
    def __reduce__(self):           # pickle 反序列化时会调用
        return (os.system, ("echo 被执行了恶意代码",))
payload = pickle.dumps(Evil())      # 攻击者构造的字节流
print("载荷:", payload[:40], "...")
pickle.loads(payload)                # 反序列化 → 触发 __reduce__
```
2. 运行 `python pickle_lab.py` → 屏幕打印"被执行了恶意代码"。**这就是反序列化执行**：你只"读数据"，却跑了代码。
3. **修复原则**（写笔记）：**绝不 `pickle.loads` 用户数据**；用 `json` + 显式字段解析；Java 端禁用危险 `ObjectInputStream` 类、用白名单（ValidatingObjectInputStream）。

## 🔵 部分三·原理层（底层发生了什么）
- **序列化** = 对象→字节流（便于传输/存储）；**反序列化** = 字节流→对象。
- 危险在于：某些语言（Java `ObjectInputStream`、Python `pickle`、`__reduce__`、PHP `unserialize`）在反序列化时会**自动调用对象的方法**（如 `readObject`/`__reduce__`）。攻击者在流里塞"会干坏事的对象"，还原即触发。
- **gadget chain（小工具链）** = 单独一个方法不够，攻击者把多个"本来无害的方法"串起来，最终达成命令执行。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：**2015 年 Apache Commons Collections 反序列化 RCE**、**2017 年 Equifax（又是它）因 Struts2 反序列化 RCE 泄露**，都是 gadget 链经典。Java 生态因"原生序列化 + 庞大类库"成重灾区。
- 顶级视角：根本解法是**不用危险序列化格式**、**白名单允许的类**、**在反序列化边界做完整性校验（签名）**。

## 🔴 部分五·顶级视角
顶级工程师严守：**跨信任边界绝不接收原生序列化对象**；API 只用 JSON/Protocol Buffers 等"数据格式"而非"代码格式"；Java 用 `SerialKiller`/ `ValidatingObjectInputStream` 限制类；对必须反序列化的数据，**先验签名再还原**。他们视 `pickle.loads(untrusted)` 为严重反模式。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 本地 Python 自己序列化自己还原、观察触发，**合法学习**。
- ✅ 目的是"以后写代码绝不反序列化不可信数据"。
- 🚫 不把此类 payload 发向任何真实服务/他人系统；不写"反序列化利用工具"害人。

## ✅ 验收
交 `pickle_lab.py` 运行截图（显示"被执行了恶意代码"）+ 一句话修复原则。发我。

## ⚠️ 常见坑
- 以为"数据格式无所谓"——JSON 安全是因它**不会自动执行方法**；pickle 危险正因它会。
- 只在 Python 测，忘了 Java/PHP/Node 同样有，原理相通。
- 手疼复制即可。

## ➡️ 下一步
**SEC14 · XXE（外部实体 / 文件读取）**（XML 解析器去读了"它不该读的文件"）。

---

# SEC14 · XXE（外部实体 / 文件读取）

## 🎯 目标
学完这节能讲清 **XXE = 在 XML 里定义一个"外部实体"，让解析器去读本地文件/发请求**；在**自己本地 XML 解析**复现文件读取，并学会**禁用外部实体（防御）**。

## 📋 小白前置
- SEC10（注入本质）、SEC13（解析器边界问题）
- SEC9（A05/A03 相关）
- 方向6 BE4（模板/XML 处理概念）

## 🟢 部分一·最浅层（生活比喻）
XML 像一份带"引用"的文档："见附录 @file"。正常附录是文档内的一段。
**XXE** = 你在文档里写"附录 = 去把 C:\密码.txt 的内容抄过来"。如果解析器**太听话**，就把密码抄进结果返回给你——它读了**本不该读的文件**。

## 🟡 部分二·动手层（逐字操作）
Python 的 `xml.etree` 默认安全，我们用能触发的方式演示（**仅本地、读你自己建的文件**，合法）。

1. 先建一个你自己要"被读"的文件 `secret.txt`：
```
echo 这是我的私有内容 > secret.txt
```
2. 新建 `xxe_lab.py`（用会解析实体的库，仅本地演示）：
```python
from lxml import etree
payload = """<?xml version="1.0"?>
<!DOCTYPE foo [ <!ENTITY xxe SYSTEM "file:///full/path/secret.txt"> ]>
<foo>&xxe;</foo>"""
# 危险配置：resolve_entities=True（仅演示！生产应 False）
parser = etree.XMLParser(resolve_entities=True, no_network=False)
try:
    root = etree.fromstring(payload, parser)
    print("解析结果:", root.text)
except Exception as e:
    print("被阻止:", e)
```
3. 把 `file:///full/path/secret.txt` 改成你自己文件的**绝对路径**（Windows 用 `file:///C:/Users/.../secret.txt`）。
4. 运行 → 若打印出"这是我的私有内容"，说明 XXE 文件读取成功（你自己读自己，合法观察）。
5. **修复**：把 `resolve_entities=False`（或换 `defusedxml` 库）。重跑 → 被阻止。✅

## 🔵 部分三·原理层（底层发生了什么）
- XML 有"实体（entity）"机制，`<!ENTITY xxe SYSTEM "URI">` 让解析器**去取 URI 内容**替换实体。URI 可以是**本地文件**（`file://`）或**网络**（`http://`，变成 SSRF）。
- 漏洞 = 应用**把用户 XML 直接交给会解析外部实体的解析器**。防御 = 关掉外部实体解析（默认安全配置）、用 `defusedxml`、禁用 DTD。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：XXE 2010 年代频发，因很多 XML 库**默认开启外部实体**。常见于 SOAP/旧版 Spring/PDF 工具。
- 顶级视角：现代替代是 **JSON**（无实体机制），从源头消除；必须 XML 时用**安全解析器 + 禁用 DTD**。

## 🔴 部分五·顶级视角
顶级工程师处理 XML 一律：**用 `defusedxml`/安全解析器、禁用 DTD 与外部实体、对上传的 XML 做 schema 校验**。他们知道"解析器配置"和"算法选择"一样是安全必检项，列入 SEC7 的编码规范。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 本地读**你自己建的 secret.txt**、自己解析自己，**完全合法**。
- ✅ 学 XXE 是为了"配置安全解析器"。
- 🚫 不向任何非授权服务提交 XXE payload；不读他人文件。

## ✅ 验收
交 `xxe_lab.py` 运行截图（危险配置读到内容 + 修复后被阻止）+ 一句话修复要点。发我。

## ⚠️ 常见坑
- 用 `xml.etree.ElementTree`（默认安全）演示不出效果，误以为"没漏洞"——要理解是**解析器配置**决定。
- 手疼复制，路径改自己绝对路径即可。

## ➡️ 下一步
**SEC15 · 文件上传（绕过 / webshell）**（让服务器替你存了一个"会执行的坏文件"）。

---

# SEC15 · 文件上传（绕过 / webshell）

## 🎯 目标
学完这节能讲清 **文件上传漏洞 = 服务器没严格校验"文件类型/内容"，导致传上去的脚本被执行**；在**自己 Flask** 复现"只验后缀"的绕过，并写出**正确校验四要素**（类型/内容/重命名/不执行目录）。

## 📋 小白前置
- SEC10（注入/执行本质）、SEC11（访问控制）
- 方向6 BE12（表单/文件上传）、方向6 BE23（部署，懂 web 目录可执行）

## 🟢 部分一·最浅层（生活比喻）
快递柜规定"只收照片"。坏人把**炸弹伪装成照片**（后缀改 .jpg，里面是代码）。如果保安**只看包装写没写'照片'**，就收下了；更糟的是，柜子还把"收下的东西"放到**会被自动拆开运行**的房间——炸弹就爆了（webshell 被执行）。

## 🟡 部分二·动手层（逐字操作）
本地搭漏洞上传（自己传自己，合法）。

1. 新建 `upload_lab.py`：
```python
from flask import Flask, request
app = Flask(__name__)
import os
@app.route("/upload", methods=["POST"])
def upload():
    f = request.files["file"]
    # 漏洞：只验后缀
    if f.filename.endswith(".jpg"):
        f.save("uploads/" + f.filename)
        return "已保存"
    return "类型不对", 400
if __name__ == "__main__":
    os.makedirs("uploads", exist_ok=True)
    app.run(port=5004)
```
2. 建一个伪装文件 `shell.jpg`（内容其实是 Python 脚本）：
```
echo "print('我是被执行的上传文件')" > shell.jpg
```
3. 上传：`curl -F "file=@shell.jpg" http://localhost:5004/upload` → "已保存"。**绕过成功**：.jpg 后缀骗过校验，但内容是代码。
4. **修复四要素**（写笔记/改代码）：
   - ① 校验 **MIME + 真实文件头**（magic bytes），不只后缀；
   - ② 用**白名单**允许的扩展名；
   - ③ **重命名为随机名**（uuid），不保留用户文件名；
   - ④ 存到**不可执行目录**（不对外解析脚本），或用对象存储（SEC22）。

## 🔵 部分三·原理层（底层发生了什么）
- 漏洞链：**校验缺失** → 坏文件落地 → **所在目录被 web 服务器当作可执脚本** → 攻击者访问该 URL 即触发执行（webshell）。
- 绕过手法：双后缀（`shell.php.jpg`）、空字节（`shell.php%00.jpg`）、改 Content-Type、SVG 内嵌脚本（XSS）、压缩包嵌套。
- 根因：**把"文件名/后缀"当真理**，且**上传目录可被脚本引擎执行**。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：文件上传是**拿服务器 shells 的最常见入口**之一，几乎每个老 CMS（WordPress 插件、Discuz 等）都出过。
- 顶级修复：**存储与执行分离**（上传到对象存储/S3，永不走应用服务器脚本目录）、**图片用库重绘**（去元数据/去脚本）、**病毒扫描 + 类型强校验**。

## 🔴 部分五·顶级视角
顶级工程师的文件上传设计：**前端校验只是体验、后端校验才是真相**；落地即**随机名 + 类型白名单 + 内容校验 + 存不可执行区 + CDN/对象存储分发**；对图片用 Pillow/ImageMagick 重编码消灭隐藏负载。他们把"上传目录可执行"视为严重架构错误。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 本地 Flask 上传**自己建的测试文件**、自己保存自己，**完全合法**。
- ✅ 学上传漏洞是为了"写不会被执行坏文件的安全上传"。
- 🚫 不向任何非授权网站传 webshell；不写"getshell 工具"。

## ✅ 验收
交 `upload_lab.py` + 绕过截图 + "上传安全四要素"笔记。发我。

## ⚠️ 常见坑
- 以为"前端 JS 校验类型"有用——攻击者直接 curl 绕过前端。
- 只改后缀不验内容，等于没防。
- 手疼复制代码。

## ➡️ 下一步
**SEC16 · CORS / CSRF（跨域 / 伪造请求）**（浏览器同源策略的两类"越界"问题）。

---

# SEC16 · CORS / CSRF（跨域 / 伪造请求）

## 🎯 目标
学完这节能分清 **CORS（跨域资源共享，浏览器管"谁能动我资源"）vs CSRF（跨站请求伪造，"借你的登录态干坏事"）**；在**本地双站点**复现 CSRF 并修复（加 CSRF token / SameSite）。

## 📋 小白前置
- SEC6（会话/cookie、SameSite 概念）
- 方向6 FE8（浏览器存储/cookie）、FE15（Web 安全基础：同源/CSP）
- 方向4 NET8（HTTP 头）

## 🟢 部分一·最浅层（生活比喻）
- **同源策略** = 你家门（A 网站）不让隔壁（B 网站）的工人随便进你卧室拿东西。
- **CORS** = 你**主动贴告示**："允许隔壁工人来拿客厅的报纸"。配错告示（写 `Access-Control-Allow-Origin: *` 还带凭据） = 谁都能来拿。
- **CSRF** = 坏人骗你点链接，你带着自家门禁卡（cookie）去 ATM，机器见卡就办了转账——**不是密码被盗，是"带着登录态被利用"**。

## 🟡 部分二·动手层（逐字操作）
本地复现 CSRF（自己站点骗自己，合法）。

1. 用 SEC6 的 `login_lab.py`（确保登录态在 cookie）。
2. 另建恶意页 `csrf.html`（模拟攻击者网站）：
```html
<!-- 攻击者网站：用户已登录 victim.com，打开此页会自动发请求 -->
<form action="http://localhost:5000/transfer" method="POST">
  <input name="to" value="attacker">
  <input name="amount" value="1000">
</form>
<script>document.forms[0].submit()</script>
```
3. 若 `login_lab` 有个 `/transfer` 接口只看 cookie 不验来源 → 打开此 html 即转账成功。**CSRF 成立**。
4. **修复**（任选）：
   - 加 **CSRF Token**：表单藏随机 token，服务端校验；
   - 或 cookie 设 `SameSite=Lax/Strict`（现代浏览器默认 Lax 已挡多数）；
   - 或校验 `Origin`/`Referer` 头。
5. 在笔记里写明选哪种 + 为什么。

## 🔵 部分三·原理层（底层发生了什么）
- **同源策略** = 协议+域名+端口三者同才"同源"，跨源 JS 默认不能读响应。CORS 是**服务端用响应头**授权哪些源可跨读。
- **CSRF** = 利用"浏览器自动带 cookie"特性，诱导用户浏览器发**非用户本意的请求**。防御靠"证明请求是用户真实意图"（token/二次确认）+ "限制跨站携带凭证"（SameSite）。
- **CORS 配错**：`Allow-Origin: *` + `Allow-Credentials: true` 在规范里不被允许，但 `Allow-Origin: 任意具体域 + 凭据` 若动态反射攻击者域，也泄露数据。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：CSRF 2000 年代普遍；**2008 年 Gmail/各大站** 曾因 CSRF 被改设置。现代框架（Django/Rails/Spring）默认带 CSRF 防护，但手动写接口易漏。
- 顶级视角：**CORS 用白名单固定源**（绝不反射 `Origin`）、**凭据接口必须 SameSite+token 双保险**。

## 🔴 部分五·顶级视角
顶级工程师把 CSRF 防御做成**框架默认开启**（不关不掉）；CORS 用**集中配置 + 严格白名单 + 禁止凭据配通配**；他们还理解"**CORS 不是 CSRF 的防护**"——两者解决不同问题，常被混淆。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 本地双页面自己骗自己、**不开真实转账**，完全合法。
- ✅ 学 CSRF 是为"给每个敏感接口加防护"。
- 🚫 不拿 CSRF 去任何真实网站诱导他人；不做钓鱼页骗别人点击。

## ✅ 验收
交 `csrf.html` + 修复说明（token/SameSite 选一）+ 一句话"CORS 和 CSRF 区别"。发我。

## ⚠️ 常见坑
- 以为"CORS 挡住了就防 CSRF"——CORS 管"读响应"，CSRF 管"发请求"，是两回事。
- 只在前端加 token 不校验——必须后端验。
- 手疼复制 HTML 即可。

## ➡️ 下一步
**SEC17 · 服务端请求 / 巡游（协议走私 HTTP Request Smuggling）**（前后端对"请求边界"理解不一致）。

---

# SEC17 · 服务端请求 / 巡游（协议走私 HTTP Request Smuggling）

## 🎯 目标
学完这节能讲清 **HTTP 请求走私 = 前端与后端对"一个请求在哪结束"理解不一，导致请求被"拼接/藏匿"**；在**自己用 Docker 搭的反向代理实验**里理解 CL/TE 差异（观察级，不攻击真实站）。

## 📋 小白前置
- SEC3（信任边界）、SEC12（SSRF 相关，服务器代发请求）
- 方向4 NET8（HTTP/2/3、请求结构）、NET12（反向代理/NAT）
- 方向6 BE23（Nginx 部署）

## 🟢 部分一·最浅层（生活比喻）
两个快递员接力送信（前端代理→后端）。信上写"这封信长 100 字（Content-Length）"。
**走私** = 前端按 A 规则裁信、后端按 B 规则裁，裁出来的边界不一样，结果**后半截被当成"下一封信"**偷偷塞给别人。攻击者借此藏一条"管理员请求"，绕过后端校验。

## 🟡 部分二·动手层（逐字操作）
理解原理用**读 + 画图**即可（走私实战需特定代理配置，本环境做观察练习，合法）。

1. 读 PortSwigger 免费 smuggling 介绍：`https://portswigger.net/web-security/request-smuggling`（官方授权资料）。
2. 在笔记画两种边界头：
   - **CL（Content-Length）**："正文有 N 字节" → 数到 N 结束。
   - **TE（Transfer-Encoding: chunked）**："分块，每块先报长度，0 结束"。
3. 构造一个"CL 与 TE 冲突"的请求示意（仅文字）：
```
POST / HTTP/1.1
Host: x
Content-Length: 4
Transfer-Encoding: chunked

0

GET /admin HTTP/1.1
Host: x
```
若前端信 CL（截断到 4 字节，把后面当新请求）、后端信 TE（把 `0` 后内容当新请求）→ **走私出一条 `/admin` 请求**。
4. 在本地用 `docker run --rm -p 8080:80 kennethreitz/httpbin` 之类搭个靶，仅**观察请求如何被转发**，不向外部站发送此类请求。

## 🔵 部分三·原理层（底层发生了什么）
- 现代架构常是"**CDN/反向代理（前端）↔ 应用服务器（后端）**"，两者都解析 HTTP 头决定请求边界。
- 若前端用 **CL**、后端用 **TE**（或反之），且攻击者能发同时带两者的请求，就能让两端"切"出不同的请求边界 → 后半段被后端当作**独立请求**处理（可能绕过前端 WAF/ACL）。
- 根因：**同一份字节流，两个解析器理解不同**——又是 SEC10 的"解析器边界"老问题。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：2005 年 Watchfire 首次系统描述；**2019–2021 年因云原生（CDN+微服务）爆发**，PortSwigger 的 James Kettle 研究让其重回视野。
- 顶级修复：**前后端统一用 HTTP/2（h2）**（天然无 CL/TE 歧义）、**禁用 TE**、**前后端用同一解析库**、WAF 与后端配置一致。

## 🔴 部分五·顶级视角
顶级工程师部署时**强制边缘到源用 HTTP/2**、**禁止 chunked 转发**、做"**请求边界一致性测试**"。他们把走私归入"**解析器分歧类漏洞**"，与 HTTP 参数污染（HPP）、缓存投毒同族——本质是"多个组件对同一输入理解不同"。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 读官方资料 + 本地 httpbin 观察，**合法**。
- ✅ 理解走私是为了"部署时统一前后端解析、配 HTTP/2"。
- 🚫 绝不向**任何非授权网站**发送 CL/TE 冲突请求做探测；那是未授权测试，违法。

## ✅ 验收
交"CL vs TE 走私原理图 + 一句修复（统一用 HTTP/2 / 禁 TE）"笔记。发我。

## ⚠️ 常见坑
- 以为"用了 HTTPS 就安全"——走私在 TLS 内部照样发生。
- 手疼就画图+文字，不必真搭复杂代理。
- 混淆走私与 SSRF——前者是"请求边界歧义"，后者是"服务器被诱导访问内网"。

## ➡️ 下一步
**SEC18 · 逻辑漏洞（业务流 / 并发）**（功能没错，但"组合顺序/并发"被钻空子）。

---

# SEC18 · 逻辑漏洞（业务流 / 并发）

## 🎯 目标
学完这节能识别 **逻辑漏洞 = 功能各自正常，但业务规则/流程/并发被绕过**（如改价格、重复领券、越权改他人单）；在**自己 Flask** 复现"负数/并发抢券"并修复（服务端校验+锁）。

## 📋 小白前置
- SEC11（越权/IDOR）、SEC10（输入校验）
- 方向5 DB6（事务/ACID、并发）、方向6 BE9（REST 设计：幂等）

## 🟢 部分一·最浅层（生活比喻）
自动售货机：投币→扣款→出货，三步都对。
**逻辑漏洞** = 你同时狂按"出货"十次（并发），机器还没扣完款就先出了十罐（竞态）；或者你把"价格"改成 `-5`，机器倒找你钱。功能没崩，但**业务规则被钻了空子**。

## 🟡 部分二·动手层（逐字操作）
本地复现并发/负数漏洞（自己测自己，合法）。

1. 新建 `logic_lab.py`：
```python
from flask import Flask, request
app = Flask(__name__)
balance = {"alice": 100}
coupons = {"alice": 1}   # 每人限领1张

@app.route("/buy")
def buy():
    price = int(request.args.get("price", 10))
    if balance["alice"] >= price:
        balance["alice"] -= price
        return "购买成功"
    return "钱不够", 400

@app.route("/coupon")
def coupon():
    if coupons["alice"] > 0:
        coupons["alice"] -= 1
        return "领到券"
    return "已领完", 400
```
2. **负数漏洞**：`http://localhost:5005/buy?price=-50` → 余额变 150（倒赚）。修复：校验 `price > 0`。
3. **并发漏洞**（用 Python 多线程模拟）：
```python
import threading, requests
def grab():
    for _ in range(5):
        requests.get("http://localhost:5005/coupon")
# 起多个线程同时抢 → 可能领到超过1张（若未加锁/原子）
```
4. **修复**：用**数据库事务+行锁/原子操作**（DB6 MVCC）、或 `threading.Lock` 演示原子自减；价格加服务端校验。重测不再超领。

## 🔵 部分三·原理层（底层发生了什么）
- 逻辑漏洞**不靠技术 bug**，靠**业务规则缺失**：
  - 价格/数量**服务端未复核**（只信前端传值）；
  - 关键操作**非原子**（读-改-写之间被并发插入）；
  - 流程步骤可**跳过/重放**（如支付回调未验重→重复发货）；
  - 状态机可**逆向**（已发货订单改回待支付）。
- 防御 = **服务端始终复核业务不变量 + 关键操作原子化（锁/事务）+ 幂等（BE9）**。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：逻辑漏洞是**赏金平台最高价值**类型之一（没有通用扫描器能查，靠人脑）。**2018 年 Apple/各类电商** 因"改单价/重复支付"被薅。
- 顶级视角：用**状态机建模**核心业务（订单有哪些合法状态迁移），任何非法迁移直接拒绝；用**不变式测试**自动验"余额永不负数"。

## 🔴 部分五·顶级视角
顶级工程师把逻辑安全当**产品需求的一部分**：写"业务不变量清单"（余额≥0、每人限领1、订单不可回退），并配**自动化测试 + 代码评审卡点**。他们知道：自动化扫描器查不出逻辑漏洞，**人和设计**才是关键——这正呼应 SEC1 威胁建模要从业务流出发。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 本地 Flask 自己改价格、自己并发抢，**完全合法**。
- ✅ 学逻辑漏洞是为"写不会被人薅的业务"。
- 🚫 不去任何真实电商/网站试"改价格/重复领"；那是诈骗级违法。

## ✅ 验收
交 `logic_lab.py` 漏洞+修复代码 + 负数/并发复现说明。发我。

## ⚠️ 常见坑
- 只修前端校验——后端照样能被 curl 绕过。
- 忘了并发（单测通过，多用户就崩）。
- 手疼复制代码。

## ➡️ 下一步
**SEC19 · SSTI（服务端模板注入：模板引擎执行）**（把用户输入当模板语法，引擎帮你跑了代码）。

---

# SEC19 · SSTI（服务端模板注入：模板引擎执行）

## 🎯 目标
学完这节能讲清 **SSTI = 把用户输入当模板表达式，引擎求值执行了代码**；在**自己 Flask/Jinja2** 复现并修复（不把用户输入当模板、用沙箱/转义）。

## 📋 小白前置
- SEC10（注入本质、命令注入）、SEC13（执行边界）
- 方向6 BE4（模板/SSR）、FE9–FE10（前端模板，懂 `{{ }}` 语法）

## 🟢 部分一·最浅层（生活比喻）
模板像填空信："亲爱的 {{name}}，欢迎！" 系统把 `name` 填进去。
**SSTI** = 你在 name 里填"亲爱的 `{{7*7}}`"，系统不仅填字，还**顺手算了一下**变成 49——因为它把"填空内容"当成了**要执行的公式**。坏人填 `{{删库命令}}`，引擎就真去执行了。

## 🟡 部分二·动手层（逐字操作）
本地复现（自己打自己，合法）。

1. 新建 `ssti_lab.py`：
```python
from flask import Flask, request, render_template_string
app = Flask(__name__)
@app.route("/hello")
def hello():
    name = request.args.get("name", "guest")
    # 漏洞：把用户输入直接当模板字符串渲染
    return render_template_string("你好 " + name)
if __name__ == "__main__":
    app.run(port=5006)
```
2. 访问：`http://localhost:5006/hello?name={{7*7}}` → 返回"你好 49"。**SSTI 成立**：模板引擎执行了你的表达式。
3. 进一步（仅自己观察）：`?name={{config}}` 可能暴露配置。
4. **修复**：**绝不拼接用户输入进模板**，改为传变量：
```python
return render_template_string("你好 {{ name }}", name=name)
```
重测 `{{7*7}}` → 原样显示，不再执行。✅

## 🔵 部分三·原理层（底层发生了什么）
- 模板引擎（Jinja2/Django/Twig/Freemarker）支持**表达式求值**（`{{ }}`、`${}`）。
- 漏洞 = 把**用户数据**拼进"模板源码"再编译执行，等于把数据当代码（SEC10 老毛病）。
- 危险链：Jinja2 里 `{{ ''.__class__.__mro__[1].__subclasses__() }}` 可遍历类找能执行命令的对象（gadget），最终 RCE。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：SSTI 在 2015 年后随模板引擎普及而高发，**Flask/Jinja2、Django、Spring 都中过**。
- 顶级修复：**用户输入永远作为"数据"传入（render(name=user)），绝不作为"模板文本"**；若必须动态模板，做严格沙箱 + 白名单 + 禁用危险属性（Jinja 的 `autoescape` + 自定义沙箱）。

## 🔴 部分五·顶级视角
顶级工程师把"模板只渲染数据不渲染输入"写入**编码规范**；用 SAST（CodeQL）扫"render_template_string 拼接变量"模式；理解 SSTI 与 SEC10 注入、SEC13 反序列化同属"**执行边界失控**"大家族——统一解法都是"**数据与代码分离**"。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 本地 Flask 自己传 `{{7*7}}` 测自己，**完全合法**。
- ✅ 学 SSTI 是为"写不拼接用户输入的模板"。
- 🚫 不向任何非授权网站试 SSTI；不写"一键 getshell"工具。

## ✅ 验收
交 `ssti_lab.py` 漏洞+修复对比 + `{{7*7}}` 前后结果截图 + 一句话修复要点。发我。

## ⚠️ 常见坑
- 以为"前端模板（Vue/React）的 `{{}}` 一样危险"——前端的是在用户浏览器跑自己的代码，风险不同；SSTI 是**服务端**引擎执行。
- 手疼复制代码。

## ➡️ 下一步
**SEC20 · 代码审计（污点分析 / 模式）**（用"安全眼光"通读代码，找上面 11 节的所有影子）。

---

# SEC20 · 代码审计（污点分析 / 模式）

## 🎯 目标
学完这节能用 **"污点追踪"法** 审计一段代码：从**不可信输入（source）** 一路跟到**危险函数（sink）**，中间没净化就报漏洞；对自己写的 SEC10–SEC19 代码做一次完整审计清单。

## 📋 小白前置
- SEC10–SEC19（已学全部具体漏洞，审计就是"找它们的影子"）
- 方向1a PY10–PY14（读代码、函数、装饰器，便于理解审计）
- SEC7（SAST 工具 bandit 已会用）

## 🟢 部分一·最浅层（生活比喻）
审计像"顺着水管查毒"：自来水厂（用户输入）出来水，经过一堆管子（函数调用），最后流进游泳池（数据库/系统命令/网页）。只要**有一段管子没装过滤器（净化）**，毒就进池子了。审计员的工作 = 拿图纸，从水厂一路摸到泳池，看哪段漏了过滤。

## 🟡 部分二·动手层（逐字操作）
用 **bandit + 人工** 审计你之前写的代码（合法、自己的）。

1. 把 SEC10 的 `sqli_lab.py`、SEC19 的 `ssti_lab.py` 放一个文件夹。
2. 跑：`bandit -r .` → 它会标出 `B608: 可能 SQL 注入`、`B701: Jinja2 自动转义` 等（不同版本提示不同）。
3. 人工审计清单（复制成检查表，逐项打勾）：
```
[ ] 所有 request.args/form 出来的值，到 SQL 前是否参数化？(SEC10)
[ ] 到 os/system/subprocess 前是否白名单/无shell？(SEC10)
[ ] 到 render_template_string 前是否当数据而非代码？(SEC19)
[ ] 文件上传是否验类型+内容+重命名+不可执行目录？(SEC15)
[ ] 对象反序列化是否拒绝不可信来源？(SEC13)
[ ] 每个请求是否验"这资源属于当前用户"？(SEC11)
[ ] 外部 URL 是否禁内网/元数据？(SEC12)
```
4. 对自己代码逐条过，写出"发现 X 处问题 + 已用 Y 修"。

## 🔵 部分三·原理层（底层发生了什么）
- **污点分析（Taint Analysis）**：标记**源（source）**= 不可信输入（`request.*`、`argv`、文件上传、`unserialize`）；标记**汇（sink）**= 危险函数（`execute`、`.system`、`render_template_string`、文件写）。若从 source 到 sink 的**数据流上没有净化（sanitizer：参数化/转义/白名单）**，即漏洞。
- **模式匹配（Pattern）**：已知危险写法（拼接 SQL、禁用实体解析）直接扫。这是 SAST（SEC7）的底层。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：人工审计是安全最古老手艺；现代用 **CodeQL（语义级查询）、Semgrep（规则）、Coverity** 把"污点追踪"自动化。顶级审计员兼用工具+人脑（逻辑漏洞工具查不出，见 SEC18）。
- 案例：**Heartbleed（2014）** 是人工代码审查发现的——OpenSSL 心跳包没校验长度，读越界内存泄露私钥。

## 🔴 部分五·顶级视角
顶级安全工程师做审计时：**先画"信任边界 + 数据流"，再跑 SAST 缩小范围，最后人工确认每个告警（降误报）**。他们写**自定义 CodeQL/Semgrep 规则**覆盖本项目的特有危险模式；把审计融入 SEC7 的 PR 卡点。理解"**工具发现、人脑定案**"。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 审计**自己写的代码**，完全合法，是白帽核心能力。
- ✅ 学审计是为"写出更安全的代码 / 帮公司做授权代码评审"。
- 🚫 不审计任何**非授权**的他人/公司代码仓库；那未经授权，违法。授权审计必须走正规合同（SEC67 PTES）。

## ✅ 验收
交一份"对我的 X 代码审计清单"：≥5 条检查项 + 每条结论（通过/已修）+ bandit 运行截图。发我。

## ⚠️ 常见坑
- 只信工具"没报警就安全"——工具误报漏报都多，逻辑漏洞查不出。
- 审计别人代码练手——绝对不可，必须授权。
- 手疼用语音写结论，少敲字。

## ➡️ 下一步
**SEC21 · API 安全（速率 / 越权 / 泄露）**（现代前后端分离，攻击面全在 API）。

---

# SEC21 · API 安全（速率 / 越权 / 泄露）

## 🎯 目标
学完这节能识别 **API 特有风险：未授权访问、IDOR 越权、敏感数据过度返回、缺乏限流被刷/DoS、错误码泄露堆栈**；在**自己 Flask API** 复现"无速率限制被刷"并加限流（Flask-Limiter）。

## 📋 小白前置
- SEC11（越权/IDOR）、SEC9（Top10）、SEC18（逻辑/并发）
- 方向6 BE9（REST API 设计）、BE15（限流/熔断）

## 🟢 部分一·最浅层（生活比喻）
API 像自助售货机的"补货口"：设计给配送员批量拿货。
**API 漏洞** = ① 没查配送员证件（越权）；② 一次让一个人把全店搬空（无限流/DoS）；③ 补货单上印着"本店保险箱密码"（过度返回/泄露）；④ 报错小票把内部地图全打印了（堆栈泄露）。

## 🟡 部分二·动手层（逐字操作）
本地复现+修复限流（自己测自己，合法）。

1. 装：`pip install flask-limiter`
2. 新建 `api_lab.py`：
```python
from flask import Flask, request, jsonify
from flask_limiter import Limiter
from flask_limiter.util import get_remote_address
app = Flask(__name__)
limiter = Limiter(get_remote_address, app=app, default_limits=["5 per minute"])

@app.route("/api/price")
@limiter.limit("3 per minute")   # 每IP每分钟3次
def price():
    return jsonify({"price": 99})

if __name__ == "__main__":
    app.run(port=5007)
```
3. 快速刷：`for i in $(seq 1 10); do curl -s http://localhost:5007/api/price; done` → 前 3 次成功，后面返回 `429 Too Many Requests`。**限流生效**。
4. 笔记写：API 还要做 ① 每接口验权（SEC11）② 响应只返回必要字段（不返回密码/ token）③ 统一错误格式（不泄露堆栈）。

## 🔵 部分三·原理层（底层发生了什么）
- 现代 App 几乎全是 **前后端分离 + JSON API**，攻击面从"网页"转到"API"。OWASP 另有 **API Security Top 10**（2023）：对象级授权失效（BOLA，即 IDOR）、认证失效、无限流、自动绑定过多、功能不全鉴权、敏感数据暴露等。
- **限流（Rate Limiting）** = 按 IP/用户统计请求数，超阈值拒绝（429），防刷/暴力破解/DoS。底层用令牌桶（BE15）。
- **过度返回** = 一个 `/user/me` 把 `passwordHash`、`internalNote` 也返了——前端不用但攻击者要。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：**API 风险随移动互联网爆发**；2018–2023 大量数据泄露源于"API 未鉴权/越权"（如某车企 API 可遍历车辆）。OWASP 因此单列 API Top10。
- 顶级修复：**对象级授权（BOLA）当成头号威胁**、用 **API 网关统一鉴权+限流+审计日志**、**响应里用 DTO 只暴露必要字段**。

## 🔴 部分五·顶级视角
顶级工程师把 API 安全当**产品级工程**：用 OpenAPI 规范定义每个接口的输入输出，自动生成**参数校验 + 鉴权 + 限流 + 文档**；用 **API 网关（Kong/Envoy）** 统一做安全卡点；对敏感接口加 **mTLS/设备绑定**。他们理解"**前端隐藏字段 ≠ 安全**"，API 必须独立加固。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 本地 Flask 加限流、自己刷自己，**完全合法**。
- ✅ 学 API 安全是为"写不被刷、不泄露的接口"。
- 🚫 不对任何非授权 API 做"遍历 ID/暴力刷"；那是违法未授权测试。

## ✅ 验收
交 `api_lab.py` + 限流 429 截图 + "API 安全三要点（鉴权/限流/最小返回）"笔记。发我。

## ⚠️ 常见坑
- 把限流当"防攻击万能"——它只防刷，不防越权/注入。
- 只在首页加限流，忘了每个敏感 API 都要。
- 手疼复制代码。

## ➡️ 下一步
**SEC22 · 云上 Web 安全（配置错误 / 对象暴露）**（最后收口到"云时代最常见的翻车：配置错）。

---

# SEC22 · 云上 Web 安全（配置错误 / 对象暴露）

## 🎯 目标
学完这节能讲清 **云上最高频风险 = 配置错误（公开 S3/对象存储、过度 IAM 权限、安全组全开）**；在**自己本地 MinIO / 或 PortSwigger 云 lab** 复现"对象存储公开可读"并修复为私有 + 签名 URL。

## 📋 小白前置
- SEC12（SSRF 云元数据）、SEC11（权限/越权）、SEC15（文件存储）
- 方向10 OPS12–OPS18（AWS 核心、对象存储、VPC/安全组、云安全责任共担）
- 方向5 DB15–DB17（NoSQL/对象概念）

## 🟢 部分一·最浅层（生活比喻）
云像租了个带智能锁的仓库（对象存储）。
**配置错误** = 你图省事把仓库设成"任何人凭链接都能进"，还把链接发到了公开论坛——结果全网都能搬你东西。**过度权限** = 给保洁阿姨（普通服务账号）配了"能开所有门+删监控"的万能卡。**安全组全开** = 仓库大门 0–65535 端口全对外敞着。

## 🟡 部分二·动手层（逐字操作）
用 **PortSwigger 云安全免费 lab** 或本地 **MinIO**（合法环境）理解对象暴露。

> ⚠️ 仅在你自己的云测试账号 / 本地 MinIO / 官方授权 lab 进行。绝不碰他人存储桶。

1. 本地起 MinIO（对象存储，Docker）：
```bash
docker run -d -p 9000:9000 -p 9001:9001 --name minio \
  -e MINIO_ROOT_USER=test -e MINIO_ROOT_PASSWORD=test1234 \
  minio/minio server /data --console-address ":9001"
```
2. 浏览器开 `http://localhost:9001`（账号 test / test1234，你自己建的）。
3. 建一个 bucket，上传一个 `private.txt`。
4. 观察两种访问策略（在 MinIO 控制台操作，仅自己环境）：
   - **公开（错误示范）**：把 bucket 设 Public → 任何人凭 URL 可读。这就是"对象暴露"翻车。
   - **私有 + 签名 URL（正确）**：设 Private，用临时签名 URL 限时分享：
```bash
docker exec minio mc alias set local http://localhost:9000 test test1234
docker exec minio mc share download --expire 1h local/mybucket/private.txt
```
生成的签名 URL 1 小时失效，且需正确签名。**这才是正确做法**。
5. 截图"Private 桶 + 签名 URL"发我。

## 🔵 部分三·原理层（底层发生了什么）
- **对象存储（S3/OSS/COS/MinIO）** 是云上存文件的主流。每个对象有 **ACL/桶策略** 控制"谁可读/写"。
- **配置错误** = 桶设 `public-read`、IAM 角色权限 `*`、安全组 `0.0.0.0/0` 全开、元数据服务未设 IMDSv2（接 SEC12）。
- 云是**责任共担模型（OPS18）**：云厂商保"基础设施"，你保"配置/数据/身份"——所以**配置错 = 你的责任**，也是泄露头号原因。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：**2017–2023 年几乎每年都有"某某公司 S3 桶公开致千万数据泄露"**——Uber、五角大楼承包商、各大医院都中招。根因全是"一个勾选框点错"。
- 顶级防护：**基础设施即代码（Terraform，OPS33）强制安全默认 + 自动合规扫描（如 AWS Config、CSP 的 Access Analyzer）持续审计 + 最小权限 IAM + 存储桶公开访问块（Block Public Access）**。

## 🔴 部分五·顶级视角
顶级云安全工程师把"防配置错"做成**自动化护栏**：① 默认开启"阻止公开访问"；② 用 **IAM 角色最小权限 + 定期权限审计**；③ 用 **CSPM（云安全态势管理）** 工具每天扫"错误配置"并告警；④ 敏感数据用 **KMS 加密 + 密钥轮换**。他们信"人会点错，所以让机器不允许错"。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 在**你自己 MinIO/自己云测试账号/官方 lab** 做对象暴露实验，**完全合法**。
- ✅ 学云配置是为了"正确保护自己和用户的数据"。
- 🚫 **绝不**扫描/访问他人云存储桶、不试 `s3://` 遍历别人的桶；那是严重违法入侵。云上"配置错误"只可在自己环境学修。
- 🚫 不绕实名/监管，云上活动全在授权范围。

## ✅ 验收
交 MinIO "Private 桶 + 签名 URL 分享"截图 + 一句话："为什么生产环境要开 Block Public Access + 最小权限 IAM"。发我。

## ⚠️ 常见坑
- 以为"对象存储像自己硬盘，默认私"——很多 SDK/控制台**默认或误点是公开**，必须显式设为私有 + 阻止公开访问。
- 把云配置当"一次性"——环境会漂移，需持续扫描（CSPM）。
- 手疼复制命令，控制台点点即可。

## ➡️ 下一步
**SEC23（方向7b · 二进制/漏洞利用/Hook）**：从 Web 白帽转向**二进制逆向与漏洞利用**——你会学 PE/ELF 文件格式、反汇编、栈/堆溢出、ROP、Fuzzing 等，进入"更底层、更接近系统"的白帽世界。注意方向7b 全为**自己程序/授权样本**的合法分析，绝不碰他人软件。

---

# SEC-W1 · 密码存储进阶（明文→哈希→加盐→自适应/慢哈希→Pepper→TOTP）

## 🎯 目标
学完这节你能**自己实现一个达到 2026 行业标准的密码存储方案**：绝不明文、用自适应慢哈希（argon2id/bcrypt）、加盐、加全局 pepper、配 TOTP 二次因子；并能在自己环境复现「彩虹表攻击为什么失效」「定时攻击为什么危险」。

## 📋 小白前置
- SEC1（认证基础）、SEC2（密码学基础：哈希/盐概念）
- 方向1a PY（函数/标准库）、方向5 DB（存用户表）
- 方向6 BE6（密码学应用背景）

## 🟢 部分一·最浅层（生活比喻）
存密码像**存贵重物品**：
- 明文 = 把钥匙挂在门上（直接被盗）。
- 哈希 = 把钥匙熔了存"钥匙的形状摘要"（知道形状也复原不了钥匙）。
- 加盐 = 每把钥匙熔之前先混一段**只有这把锁知道的随机杂质**，让"同一把钥匙"每次熔出来都不一样（防万能对照表）。
- 慢哈希 = 故意把熔炼炉调慢（argon2/bcrypt），小偷想批量试 100 万把钥匙要几十年。
- Pepper = 把"杂质配方"锁在全公司保险柜（不在数据库里），DB 泄露也复原不了。

## 🟡 部分二·动手层（逐字操作）
装库（复制即可，已验证可用性）：
```bash
pip install argon2-cffi pyotp qrcode
```
① 正确存储（自己环境）：
```python
from argon2 import PasswordHasher
from argon2.exceptions import VerifyMismatchError
ph = PasswordHasher()                      # 默认 argon2id，自动加盐
hash1 = ph.hash("MyStr0ng#Pass!")
hash2 = ph.hash("MyStr0ng#Pass!")          # 同密码，两次哈希不同（盐随机）
print(hash1 != hash2)                       # True
# 校验
assert ph.verify(hash1, "MyStr0ng#Pass!")
try:    ph.verify(hash1, "wrong")
except VerifyMismatchError: print("拒绝错误密码")
```
② TOTP 二次因子（自己生成密钥、自己扫自己）：
```python
import pyotp
secret = pyotp.random_base32()              # 存用户表，不在代码里硬编码
uri = pyotp.totp.TOTP(secret).provisioning_uri("zeze@local", issuer_name="Lightning")
print(uri)                                 # 用认证器 App 扫（自己手机）
code = pyotp.TOTP(secret).now()
print("当前动态码:", code)
assert pyotp.TOTP(secret).verify(code)      # 校验
```

## 🔵 部分三·原理层（底层发生了什么）
- **哈希不是加密**：哈希单向，无法解密；验证=把输入再哈希一次比对。
- **盐（salt）** 是每用户随机值，拼在密码前再哈希，存进哈希串里（argon2 的 hash 自带 salt）。它让**相同的密码产生不同摘要** → 预计算的「彩虹表」失效（彩虹表是"常见密码→哈希"的对照表，没盐就直接反查）。
- **自适应慢哈希（argon2id/bcrypt/scrypt）** 故意吃 CPU/内存，参数可调：攻击者每秒只能试几千次而非几十亿次。argon2id 还抗 GPU/ASIC（吃内存）。
- **Pepper** = 全局密钥，独立于 DB，存环境变量/密钥管理（BE16/BE23）。即使 DB 整个泄露，没有 pepper 也无法离线爆破。
- **TOTP** = 基于时间的一次性密码（HMAC(密钥, 当前30秒)），server 和 App 共享 secret 各自算，比对即二次认证。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：LinkedIn（2012，明文/弱哈希泄露）、Adobe（2013，可逆加密）、各大站反复翻车，根因都是"密码存储不达标"。
- **不要自己设计哈希**（别用 MD5+盐就觉得安全——MD5 太快，GPU 一秒几十亿次）。用**经过审计的库**（argon2-cffi / passlib / bcrypt）。
- **定时攻击**：用 `==` 逐字节比较哈希会泄露"前几位对了"，攻击者据此逐位猜。正确用**常量时间比较**（`secrets.compare_digest` / argon2 自带）。
- **密码重置**正确姿势：发**单次有效、短时过期、绑用户的**令牌，绝不发"新密码明文"邮件；令牌存哈希、用完即焚。
- **泄露后**：强制全量重置 + 通知 + 看日志是否已被利用。

## 🔴 部分五·顶级视角
顶级工程师把"密码存储"当**不可妥协的基线**：默认 argon2id（或至少 bcrypt）、pepper 走 KMS、TOTP/WebAuthn 做 2FA、登录失败**恒定时间 + 限流（BE15）+ 审计日志（BE21）**；他们绝不会"为了开发方便"存明文或弱哈希，因为这是数据泄露后最致命的一环。这呼应 SEC1 认证与闪电号卡 M3 的实人验证——身份安全是信任底座。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 在自己环境、对自己账号做所有实验，**完全合法**。
- ✅ 学密码存储是为了"保护用户"。
- 🚫 不收集/破解任何**他人**密码；不对真实站点做撞库/爆破；不把 pepper/secret 提交进代码仓库。
- 🚫 不绕实名/监管；2FA 只用于保护自己与用户账户。

## ✅ 验收
交 `passwd_store.py`：用 argon2id 注册+登录校验跑通 + 同密码两次哈希不同截图 + TOTP 自验代码 + 一句话"为什么盐能防彩虹表"。

## ⚠️ 常见坑
- 用 MD5/SHA1 + 盐就当安全：太快，GPU 可暴力。
- 盐写死成同一个常量：等于没盐。
- 用 `==` 比较哈希：定时攻击。
- pepper 硬编码进代码：泄露即失效。
- 手疼：库都装好，复制代码即可。

## ➡️ 下一步
**SEC-W2 · JWT 安全（令牌伪造 / 算法混淆 / 密钥爆破）**——现代 API 的身份凭证，坑极多。

### ⚔️ 闯关路线（SEC-W1）
- 🟢 **Lv1 新手村**：用 `argon2-cffi` 跑通"注册哈希+登录校验"，截图同密码两次哈希不同。徽章🥉
- 🔵 **Lv2 熟练工**：给存储方案加密 pepper（环境变量读取），说明盐和 pepper 的区别。徽章🥈
- 🟣 **Lv3 进阶者**：自己写一段"无盐 MD5 被彩虹表命中"的对照 demo（用自己建的小彩虹表），证明盐的必要性。徽章🥇
- 🟠 **Lv4 高手**：用 `secrets.compare_digest` 替换 `==` 做常量时间校验，写测试证明"错误密码返回时间不随前缀长度变化"。徽章🏆
- 🔴 **Lv5 宗师**：给你"闪电号卡"用户表设计完整密码存储方案文档（argon2id 参数 + pepper via KMS + TOTP 可选 + 重置令牌设计 + 泄露应急预案），并做一次同行评审式自查。徽章👑

---

# SEC-W2 · JWT 安全（令牌伪造 / 算法混淆 / 密钥爆破）

## 🎯 目标
学完这节你能讲清 **JWT（JSON Web Token）三种攻击面**：① `alg:none` 伪造 ② 算法混淆（RS→HS 用公钥当 HMAC 密钥）③ 弱密钥爆破；并在**自己签发的 token** 上复现并修复，理解"签名验证不可省"。

## 📋 小白前置
- SEC2（密码学：对称/非对称、签名）
- SEC1（认证/session）、SEC-W1（密码存储）
- 方向6 BE5（认证授权）、BE9（REST）

## 🟢 部分一·最浅层（生活比喻）
JWT 像一张**带防伪印章的会员卡**：服务器盖章（签名）后给你，之后你每次出示，服务器看章对不对就知道是不是真的。
- **alg:none** = 你拿张白卡自己写"我是 VIP"，还把"需要盖章"那栏划掉 → 服务器居然信了（没验证签名）。
- **算法混淆** = 会员卡本来该用"只有总部有的私钥"盖章；你却用"总部公开贴出来的公章图案"当私章来盖 → 服务器误以为真的是总部盖的。
- **弱密钥爆破** = 总部用的章密码是"123456"，你拿字典试出来了，就能自己盖任意卡。

## 🟡 部分二·动手层（逐字操作）
装：`pip install pyjwt cryptography`
① 正常签发+验证（自己环境）：
```python
import jwt
secret = "super-secret-key-only-server-knows"   # 仅演示，生产用强随机+pepper
tok = jwt.encode({"user":"alice","role":"user"}, secret, algorithm="HS256")
print(tok)
decoded = jwt.decode(tok, secret, algorithms=["HS256"])
print(decoded)                                  # {'user':'alice','role':'user'}
```
② **alg:none 伪造**（自己对自己，演示服务端若漏验会怎样）：
```python
# 攻击者改 payload 为 admin，并把算法设为 none（无签名）
forged = jwt.encode({"user":"alice","role":"admin"}, key="", algorithm="none")
# 若服务端用 jwt.decode(tok, options={"verify_signature":False}) 或 alg=none 被接受 → 越权
print(forged)
```
**修复**：服务端**强制指定算法 + 必须验签**，绝不开 `verify_signature=False`。
③ **弱密钥爆破**（自己爆破自己签的弱 token，合法）：用 `jwt` 或 `john` 思路，对 `secret="123456"` 的 token 试字典——证明弱密钥必被破。生产用**长随机密钥（≥32字节）+ 走 KMS**。

## 🔵 部分三·原理层（底层发生了什么）
- JWT 结构：`header.payload.signature`，base64 三段，签名=HMAC-SHA256(header+payload, 密钥)（HS256）或 RSA 签名（RS256）。
- **alg:none**：规范允许"无签名"，若服务端没硬编码允许算法列表，攻击者可把 alg 改成 none 骗过验证。
- **算法混淆（key confusion）**：RS256 该用**公钥验证**；但若服务端代码用 HS256 验证（把公钥当 HMAC 密钥），攻击者拿公开的公钥就能自己签合法 token。
- **弱密钥**：HS256 的密钥若弱（"secret"/"123456"），可被离线爆破（有 `jwt-cracker` 类工具，自己对自己练）。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：2015+ JWT 普及，alg:none、key confusion、弱密钥导致大量越权。**2018 年某大厂因 alg 配置错误致越权**。
- 顶级修复：**① 硬编码 `algorithms=["HS256"]` 或 `["RS256"]` 不允许 none；② RS256 验证端绝不用对称验证；③ 密钥强随机+KMS；④ 令牌设短过期+刷新令牌（refresh）+ 吊销名单（黑名单/版本号）；⑤ 不在 JWT 存敏感明文**。
- **刷新令牌（refresh token）**：access token 短命（15min），refresh 长命但可吊销——平衡安全与体验。

## 🔴 部分五·顶级视角
顶级工程师把 JWT 当**"需验真的不信任凭证"**：默认不存敏感、强制验签、算法白名单、密钥走 KMS、配吊销机制；他们知道"无状态 token 的代价是难即时吊销"，所以用**短期 access + 可吊销 refresh** 折中。这衔接 BE5 认证授权与 SEC-W1 的身份底座。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 对自己签发的 token 做 all 三种攻击复现+修复，**完全合法**。
- ✅ 学 JWT 攻击是为"写不会越权的验证"。
- 🚫 不对任何真实站点做 token 爆破/伪造；不拿他人公钥做混淆攻击。

## ✅ 验收
交 `jwt_lab.py`：① 正常签发验证 ② alg:none 伪造代码+服务端修复点说明 ③ 弱密钥爆破自演示 + 一句话"算法白名单为什么必须硬编码"。

## ⚠️ 常见坑
- 服务端 `jwt.decode(tok)` 不传 `algorithms=` → 库可能接受 none。
- 把密钥写死在代码/前端 → 泄露即全崩。
- JWT 里塞密码/身份证 → 泄露即裸奔。
- 手疼复制代码即可。

## ➡️ 下一步
**SEC-W3 · 缓存投毒与子域名接管（Web 进阶链）**——两类"配置/架构级"高危。

### ⚔️ 闯关路线（SEC-W2）
- 🟢 **Lv1 新手村**：用 pyjwt 签发+验证一个 HS256 token，截图 payload。徽章🥉
- 🔵 **Lv2 熟练工**：在自己服务里演示 alg:none 被接受的条件，并写出"硬编码 algorithms 白名单"的修复代码。徽章🥈
- 🟣 **Lv3 进阶者**：用弱密钥"123456"签 token，自己用脚本爆破成功，证明弱密钥必破。徽章🥇
- 🟠 **Lv4 高手**：实现"access(15min)+refresh(可吊销)"双令牌登录流程，refresh 存数据库且注销即失效。徽章🏆
- 🔴 **Lv5 宗师**：给"闪电号卡"设计完整 JWT 方案（算法选择/密钥KMS/短期+刷新+吊销/不在载荷存敏感/跨服务mTLS），输出设计文档。徽章👑

---

# SEC-W3 · 缓存投毒与子域名接管（Web 进阶链）

## 🎯 目标
学完这节能识别两类**架构级 Web 高危**：① **缓存投毒（Cache Poisoning）**——用"未计入缓存键的输入"把恶意响应污染给所有后续用户；② **子域名接管（Subdomain Takeover）**——废弃的第三方服务 DNS 记录未清理，被攻击者认领。两项均在**自己/授权环境**理解原理，绝不对外探测。

## 📋 小白前置
- SEC3（信任边界）、SEC12（SSRF 相关"内部可达"）、SEC17（请求走私，同属"解析/边界"族）
- 方向6 BE13（缓存）、BE23（Nginx/部署）、方向4 NET8（HTTP 头）、方向10 OPS（DNS/CDN）

## 🟢 部分一·最浅层（生活比喻）
- **缓存投毒** = 公共饮水机（CDN 缓存），你往里滴一滴墨（恶意响应），后面每个人接的水都带墨。墨能滴进去，是因为饮水机"只认水杯外观（URL），没看你是否偷偷加了料（某个头字段）"。
- **子域接管** = 你搬家了但门牌（DNS 记录）没摘，别人搬进来挂了自己招牌（建了个同名服务），访客以为还是你家——其实已经被冒名顶替。

## 🟡 部分二·动手层（逐字操作）
两项都以**理解+自己环境复现**为主，对外只做授权测试。
① **缓存投毒原理（本地 MinIO/Nginx 演示）**：
```nginx
# 危险配置：用未计入缓存键的 X-Forwarded-Host 拼进响应
location / {
    add_header X-Backend $http_x_forwarded_host;   # 把不可信头回显
    proxy_cache_valid 200 60s;
}
```
攻击者发 `X-Forwarded-Host: evil.com` → 响应带着 `evil.com` 被缓存 60 秒 → 后续用户也拿到含 `evil.com` 的响应（若页面用它拼 URL 就中毒）。
**修复**：缓存键只含「方案+Host(可信)+路径+查询」，绝不把 `X-Forwarded-Host`/`X-Original-URL` 等未授权头计入键或回显。
② **子域接管原理（理解）**：公司曾用 `shop.example.com` 指向 `shop.saas-vendor.com`，后来停用 SaaS 但 DNS 的 CNAME 没删。攻击者在 SaaS 平台**认领** `shop.saas-vendor.com` → 现在 `shop.example.com` 展示攻击者内容（钓鱼/偷 cookie）。自查：**用 `dig`/`nslookup` 看自己域名的 CNAME 是否指向已注销的服务**，发现就删记录。

## 🔵 部分三·原理层（底层发生了什么）
- **缓存投毒** = 缓存服务器以「缓存键」（通常=URL+Host）决定"这是哪个请求的响应"。若应用把**未计入键的不可信输入**（如 `X-Forwarded-Host`、`X-Original-URL`、未规范化的 query）反射进响应（重定向/URL/脚本），攻击者用该输入构造响应并被缓存 → **所有命中的用户都中招**。常配合 XSS（缓存投毒型 XSS 持续危害大）。
- **子域接管** = DNS CNAME 指向第三方（GitHub Pages/Heroku/AWS S3/过期域名），第三方资源被释放后，攻击者用同名重新认领 → 控制该子域内容。影响：钓鱼、Cookie 作用域窃取（`*.example.com` 的 cookie 可被子域读）、OAuth 回调冒名。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：缓存投毒 2018+ 由 PortSwigger 研究 popularized；子域接管 2014+ 频发（曾影响各大公司子域）。
- 顶级修复：**缓存投毒**→ 缓存键白名单 + 不回显未授权头 + 上游响应规范化 + 用 `Vary` 正确 + WAF 规则；**子域接管**→ 资产清点（所有 CNAME 归属）+ 停用即删 DNS + 用 DNS 提供商"域名锁" + 持续监控（如每周 `dig` 全子域）。

## 🔴 部分五·顶级视角
顶级工程师把这两类归为**"供应链/边界完整性"**问题：缓存投毒是"响应边界被未授权输入污染"，子域接管是"域名边界被废弃资产污染"。他们做**资产清点（asset inventory）**作为安全基线——不知道自己有哪些子域/CNAME/缓存层，就不可能防住。这呼应 SEC3 信任边界与方向10 的云配置管理。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 在自己 Nginx/本地环境复现缓存键错误、用 `dig` 查**自己**域名，完全合法。
- ✅ 学这两类是为"清自己资产、修自己配置"。
- 🚫 绝不向**非授权网站**发缓存投毒 payload；不尝试认领**他人**子域（那是入侵）。仅可在**自己域名/授权渗透测试合同**内做。

## ✅ 验收
交笔记：① 画"缓存键未含 X-Forwarded-Host 致投毒"的链路图 ② 自己域名的 `dig` 自查截图（无废弃 CNAME，或发现即删）③ 一句话修复两类。

## ⚠️ 常见坑
- 以为"用了 HTTPS/CDN 就安全"——恰恰 CDN 是投毒载体。
- 只删网页不删 DNS → 子域仍可被接管。
- 手疼：理解原理+画图即可，不必全搭复杂环境。

## ➡️ 下一步
**SEC-W4 · OAuth2 / OIDC 攻击面（重定向 / token 泄露 / 授权码拦截）**——现代第三方登录的高危面。

### ⚔️ 闯关路线（SEC-W3）
- 🟢 **Lv1 新手村**：在自己 Nginx 配出"回显 X-Forwarded-Host"的错误响应，截图。徽章🥉
- 🔵 **Lv2 熟练工**：改配置使该头不计入缓存键、不回显，复测投毒失效。徽章🥈
- 🟣 **Lv3 进阶者**：用 `dig`/`nslookup` 查自己所有子域 CNAME，列出"指向第三方且可能过期"的清单并说明处置。徽章🥇
- 🟠 **Lv4 高手**：写一份"公司资产清点脚本思路"（遍历 DNS 全子域+标 CNAME+比对在册服务），用于防接管。徽章🏆
- 🔴 **Lv5 宗师**：给自己项目设计"缓存安全配置规范 + 子域接管监控流程"，作为团队安全标准文档。徽章👑

---

> **方向7a 进阶补遗说明（SEC-W 系列）**：SEC1–SEC22 之上的 Web 深度补遗，仍属白帽范围。后续将补 SEC-W4（OAuth/OIDC 攻击面）、SEC-W5（GraphQL 安全）、SEC-W6（WebSocket 安全）、SEC-W7（客户端/浏览器安全进阶）、SEC-W8（业务逻辑进阶：验证码绕过/短信炸弹/并发薅）。全部在自己/授权环境演练，红线不松。方向7b（SEC23+ 二进制/逆向/Hook）与方向7c（SEC56+ 恶意代码/渗透/CTF）独立成文件，见对应教案。

---

# SEC-W4 · OAuth2 / OIDC 攻击面（重定向 / 授权码拦截 / token 泄露）

## 🎯 目标
学完这节能讲清 **OAuth2 四种授权模式**与**现代第三方登录的 6+ 类攻击面**：重定向 URI 绕过、授权码拦截/泄露、open redirect、token 注入、state 缺失 CSRF、IdP 混淆；并在**自己搭的授权服务器（或本地 mock）** 复现"无 state 的 CSRF"并修复。

## 📋 小白前置
- SEC1（认证）、SEC-W2（JWT，OIDC 用 JWT 做 id_token）
- SEC16（CSRF/CORS）、SEC11（越权）
- 方向6 BE5（认证授权）、BE9（REST）

## 🟢 部分一·最浅层（生活比喻）
OAuth 像"用微信账号登别的 App"：App 说"我去微信开个临时授权单，你签字，微信给我个临时通行证"。攻击面都在"这张单和通行证怎么被调包"：
- **重定向绕过** = 骗子把"签字后送回的地址"改成自己家，通行证寄到骗子那。
- **授权码拦截** = 通行证在邮寄路上被偷（比如手机恶意 App 抢了 `redirect_uri` scheme）。
- **无 state** = 单子没写"这是泽泽的单"，骗子拿自己的单骗你签，结果你的账号被绑到骗子会话（CSRF 登录）。

## 🟡 部分二·动手层（逐字操作）
用 Flask 写一个**极简授权服务器+客户端**（自己全套，合法），复现并修复 state 缺失：
```python
# 客户端（简化，仅演示）
import secrets, requests
from flask import Flask, request, session
app = Flask(__name__); app.secret_key = "dev"
@app.route("/login")
def login():
    # 漏洞：没生成/校验 state
    auth_url = "http://localhost:9000/authorize?client_id=demo&redirect_uri=http://localhost:5008/cb"
    return f'<a href="{auth_url}">用本站账号登录</a>'
@app.route("/cb")
def cb():
    code = request.args.get("code")
    # 直接用 code 换 token —— 没有校验 state，可被 CSRF 触发
    return f"拿到 code={code}，换 token"
# 修复版：登录时 session['state']=secrets.token_urlsafe(); 回调校验 request.args['state']==session['state']
```
自己访问 `/login` → 点链接 → 回调拿到 code。然后**加 state**：登录生成 `state` 存 session，回调比对，不一致拒绝。**这就是防 CSRF 登录的关键**。

## 🔵 部分三·原理层（底层发生了什么）
- **授权码模式（Authorization Code + PKCE）** 是现代标准（公开客户端必用 PKCE 防授权码拦截）。流程：客户端重定向到 IdP → 用户登录授权 → IdP 用 `redirect_uri` 回 `code` → 客户端用 code + client_secret（机密端）或 PKCE 证明换 token。
- **攻击面**：
  - **redirect_uri 校验不严** → code 泄露到攻击者域名。
  - **state 缺失/可预测** → 登录 CSRF（SEC16）。
  - **授权码拦截（无 PKCE）**：公共客户端（SPA/移动）无 secret，code 被本地恶意 App 截获即换 token → PKCE 用 `code_verifier` 破解。
  - **open redirect**：站点自身有跳转漏洞，被当合法 redirect_uri 中转。
  - **token 注入/泄露**：token 存 `localStorage` 被 XSS 偷（SEC5）、日志打印 token、Referrer 泄露。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：OAuth 因"实现复杂"反复出事，**2019–2022 多家大厂 IdP/依赖方因 redirect_uri/state 配置错致账户接管**。
- 顶级修复：**① 精确匹配 redirect_uri 白名单（不接受子串/后缀）② 必用 state（随机不可预测）③ 公开客户端强制 PKCE ④ token 存 HttpOnly cookie + 短命 + 刷新可吊销 ⑤ 不在 URL/日志/Referer 泄露 token ⑥ 用 OIDC 的 nonce 防重放**。

## 🔴 部分五·顶级视角
顶级工程师视 OAuth 为"**信任链**"：IdP 可信吗？redirect_uri 真的只回我？state/nonce 每请求唯一？token 存哪不被 XSS 偷？他们用**授权服务器成熟库（不自己写签名）**、做 **redirect_uri 严格白名单 + PKCE 强制 + token 绑定（DPoP/绑定 cookie）**，并理解"SSO 的便利 = 单点泄露影响面大，所以 IdP 要最硬"。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 在自己本地搭授权服务器+客户端做全实验，**完全合法**。
- ✅ 学 OAuth 攻击面是为"正确接入第三方登录、不被接管"。
- 🚫 不对任何真实 IdP/网站做 redirect_uri 探测、授权码截获、token 窃取；那是未授权入侵。仅限自己环境与授权渗透合同。

## ✅ 验收
交 `oauth_lab.py`：无 state 版（标注漏洞）+ 加 state 修复版 + 一句话"为什么 redirect_uri 要严格白名单"。

## ⚠️ 常见坑
- 以为"用了 OAuth 就安全"——错误配置照样账户接管。
- token 存 localStorage 被 XSS 一锅端。
- 手疼：用成熟库（authlib）演示即可，不必手搓。

## ➡️ 下一步
**SEC-W5 · GraphQL 安全（内省/批量查询/越权）**

### ⚔️ 闯关路线（SEC-W4）
- 🟢 Lv1：用 authlib/Flask 跑通"授权码+state"登录，截图流程。🥉
- 🔵 Lv2：演示无 state 的 CSRF 登录，并加 state 修复。🥈
- 🟣 Lv3：自己配置 redirect_uri 白名单校验逻辑，绕过尝试失败。🥇
- 🟠 Lv4：实现 PKCE（code_verifier/challenge）保护公开客户端。🏆
- 🔴 Lv5：给闪电号卡设计"第三方登录接入规范"（IdP 选型/白名单/state+nonce/PKCE/token 存储/撤销），输出文档。👑

---

# SEC-W5 · GraphQL 安全（内省 / 批量查询 / 越权 / DoS）

## 🎯 目标
学完这节能讲清 **GraphQL 相较 REST 的新攻击面**：内省暴露 schema、字段级越权（比 REST 更难统一管控）、批量查询/深度嵌套致 DoS、NoSQL 注入；并在**自己 GraphQL 服务** 复现"未授权查到他人字段"并加字段级鉴权。

## 📋 小白前置
- SEC11（越权/IDOR）、SEC10（注入）
- 方向6 BE10（GraphQL 基础）、方向5 DB（查询）

## 🟢 部分一·最浅层（生活比喻）
REST 像**点套餐**（每个接口是固定菜）；GraphQL 像**自助点单**（客户端自己写"我要 A 菜的 x、B 菜的 y"）。
危险在于：① 菜单（schema）默认公开，谁都能看你有啥菜（内省）；② 客人自己写"我要所有桌子所有菜"（批量/嵌套查询）→ 厨房爆了（DoS）；③ 服务员没核对"这桌是不是你的菜"就给（字段级越权）。

## 🟡 部分二·动手层（逐字操作）
装：`pip install graphene flask`
```python
from flask import Flask, request, g
from graphene import ObjectType, String, Field, Schema, List
# 极简：用户有公开名 + 私密邮箱
class User(ObjectType):
    name = String(); email = String()   # email 应仅本人可见
    def resolve_email(self, info):
        # 漏洞：没检查"当前用户是否本人"
        return self.email
# ... 省略 schema 装配，仅演示 resolve_email 是鉴权点
```
复现越权：查询 `user(id:2){name email}` → 拿到了**别人**的 email。**修复**：在 `resolve_email` 里查 `g.current_user.id == self.id`，否则抛错/返回 null。再演示**深度查询 DoS**：写 `query{a{b{a{b{...}}}}}` 嵌套 100 层 → 服务卡死；修复用**查询深度/复杂度限制**（graphql-validation 库或自写 middleware）。

## 🔵 部分三·原理层（底层发生了什么）
- **内省（introspection）** = GraphQL 自带 `__schema` 查询，能列出所有类型/字段。生产应**关闭内省**（或仅内网开），否则攻击者直接拿到全 API 地图。
- **字段级鉴权** = 每个 `resolve_*` 都要验"当前用户有权读此字段"，REST 可统一在路由层做，GraphQL 须在 **resolver 层**逐字段做（易漏 → 越权）。
- **批量/嵌套 DoS** = 客户端一次查"用户→订单→商品→评论→用户…"无限展开，服务端资源耗尽。需**深度限制 + 复杂度计分（每个字段加权）+ 分页**。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：GraphQL 2015+ 普及，因"灵活"带来"鉴权碎片化"，**Face****book/各大采用方** 都需自研 resolver 鉴权。常见漏洞：内省泄露、字段越权、批量查询 DoS、GraphQL 注入（拼字符串构造查询时）。
- 顶级修复：**① 生产关内省 ② resolver 统一鉴权中间件（不逐字段手写）③ 深度+复杂度限制 ④ 分页强制 ⑤ 用 Persisted Queries（只允许预注册查询，杜绝任意嵌套）**。

## 🔴 部分五·顶级视角
顶级工程师把 GraphQL 安全当**"resolver 即信任边界"**：每个字段都是潜在越权点，必须用**统一鉴权框架（如 graphql-shield/策略模式）**而非散落 `if`；他们理解"GraphQL 把 API 灵活性交给客户端 = 把 DoS/越权风险也交给客户端"，所以用**Persisted Queries + 复杂度计费**收回控制。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 在自己 GraphQL 服务做越权复现+修复，**合法**。
- ✅ 学 GraphQL 安全是为"写不越权的 API"。
- 🚫 不对非授权 GraphQL 端点做内省扫描/批量 DoS。

## ✅ 验收
交代码：修复后的 `resolve_email` 鉴权 + 深度限制 middleware + 一句话"为什么 GraphQL 越权比 REST 更易漏"。

## ⚠️ 常见坑
- 只在根 resolver 鉴权，忘了嵌套字段也需鉴权。
- 生产开着内省 = 给攻击者地图。
- 手疼：用 graphene 现成 demo 改即可。

## ➡️ 下一步
**SEC-W6 · WebSocket 安全（劫持 / 注入 / 认证）**

### ⚔️ 闯关路线（SEC-W5）
- 🟢 Lv1：起一个 graphene 服务，用内省查到 schema 截图。🥉
- 🔵 Lv2：复现"查到他人 email"越权，加 resolver 鉴权修复。🥈
- 🟣 Lv3：写深度嵌套查询让服务变慢，加复杂度限制。🥇
- 🟠 Lv4：实现 Persisted Queries（只允许白名单查询）。🏆
- 🔴 Lv5：给闪电号卡设计"GraphQL 安全规范"（关内省/统一鉴权中间件/复杂度限制/分页）。👑

---

# SEC-W6 · WebSocket 安全（劫持 / 注入 / 认证 / 跨站）

## 🎯 目标
学完这节能讲清 **WebSocket（全双工长连接）特有风险**：握手阶段没鉴权、消息无 CSRF 保护、WS 消息里的注入、跨站 WebSocket（CSWSH）、WS 连接被中间人劫持；在**自己 Flask+WS** 复现"未鉴权连接收推送"并修复（握手验 token + wss）。

## 📋 小白前置
- 方向6 BE19（WebSocket 基础）、SEC1/SEC6（认证/cookie）、SEC16（CSRF）、SEC2（TLS）

## 🟢 部分一·最浅层（生活比喻）
HTTP 像寄信（每封独立）；WebSocket 像**打通的电话线**（一直连着）。
危险：① 接电话时没问"你是谁"就通报机密（握手无鉴权）；② 电话里说的"转 100 块"没确认是不是本人意愿（无 CSRF 防护，因 WS 不受同源 cookie 保护那套）；③ 电话线没加密（ws:// 非 wss）被窃听；④ 别人用你的分机号冒充你打进来（跨站 WS）。

## 🟡 部分二·动手层（逐字操作）
参考方向6 BE19 的 `flask-sock` 代码，故意留漏洞再修：
```python
# 漏洞版：握手不验身份，任意连接都能收订单推送
@sock.route("/ws/orders")
def orders(ws):
    while True:
        ws.send(get_all_orders())   # 没判断当前用户，泄露全部订单
```
**修复**：握手时从 cookie/token 取用户并校验；只推"属于该用户"的订单：
```python
@sock.route("/ws/orders")
def orders(ws):
    user = require_login()           # 从握手请求的 cookie/JWT 取
    while True:
        ws.send(get_orders_of(user.id))
```
再加 **`wss://`（TLS）**（BE23 Nginx 终止 TLS）；对收到的消息也做**输入校验/长度限制**（防注入/洪水）。演示"未登录被拒、登录只收自己订单"。

## 🔵 部分三·原理层（底层发生了什么）
- WS 握手是 HTTP Upgrade，之后脱离 HTTP 语义，**不受 SameSite/CSRF token 那套浏览器保护** → 跨站页面可建 WS 连你（CSWSH），若服务端不独立鉴权就中招。
- **鉴权要在握手阶段做**（读 `request` 的 cookie/JWT），且之后每条消息都可带上下文。
- **wss** = WS over TLS，防中间人读/改消息（同 HTTPS 之于 HTTP）。
- 消息层也要**校验输入**（长度/类型/权限），防止"客户端发恶意指令"或"洪水消息"耗尽服务端。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：WS 因"实时"流行，**聊天/行情/协作** 大量用；因握手鉴权易漏，出现"跨站 WS 劫持"类问题。
- 顶级修复：**① 握手强鉴权（JWT/cookie+CSRF 等价物）② 强制 wss ③ 消息 schema 校验（如 JSON Schema）④ 连接限频/限并发（BE15）⑤ 跨域白名单（Origin 校验）**。

## 🔴 部分五·顶级视角
顶级工程师视 WS 为"**需要独立信任边界的持久通道**"：它绕过 HTTP 的许多默认保护，所以鉴权/加密/校验一个都不能少；他们用**统一的 WS 网关**集中做鉴权+限流+Origin 校验，业务代码只管推送。这衔接 BE19 实时推送与 SEC16 CSRF。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 在自己 Flask+WS 做鉴权复现+修复，合法。
- ✅ 学 WS 安全是为"写不被劫持的实时服务"。
- 🚫 不对非授权站点建 WS 连接做劫持。

## ✅ 验收
交代码：修复版 WS（握手鉴权+只推本人+输入校验）+ 未登录被拒截图 + 一句话"为什么 WS 不受 CSRF token 保护还需独立鉴权"。

## ⚠️ 常见坑
- 只在 HTTP 路由鉴权，忘了 WS 握手也要。
- 用 `ws://`（明文）在公网传输敏感 → 用 `wss://`。
- 手疼：复制 BE19 代码改即可。

## ➡️ 下一步
**SEC-W7 · 客户端/浏览器安全进阶（DOM XSS / 扩展安全 / 浏览器利用）**

### ⚔️ 闯关路线（SEC-W6）
- 🟢 Lv1：起 WS 服务，未鉴权连接能收推送截图（演示漏洞）。🥉
- 🔵 Lv2：加握手鉴权，未登录被拒、登录只收本人。🥈
- 🟣 Lv3：用 wss（Nginx 配 TLS）跑通加密连接。🥇
- 🟠 Lv4：加消息 JSON Schema 校验 + 限频。🏆
- 🔴 Lv5：给闪电号卡设计"WS 安全规范"（鉴权/Origin/限流/加密/重连安全）。👑

---

# SEC-W7 · 客户端 / 浏览器安全进阶（DOM XSS / 浏览器利用 / 扩展安全）

## 🎯 目标
学完这节能识别 **客户端侧高危**：DOM 型 XSS（不经过服务器）、不安全的 `eval`/`innerHTML`、浏览器扩展权限滥用、CORS 误配致数据跨域泄露；并在**自己页面** 复现 DOM XSS 并修复（安全 DOM API）。

## 📋 小白前置
- SEC5（XSS）、SEC16（CORS）、FE8–FE10（浏览器存储/框架，懂 DOM）
- 方向6 BE4（前端渲染）

## 🟢 部分一·最浅层（生活比喻）
- **DOM XSS** = 你家的"智能屏"（浏览器）自己把门外递来的纸条（URL 片段 `#`）直接投影到大屏（innerHTML），纸条上写着"把门打开"它也照做。危险在于：纸条根本没经过保安（服务器），保安拦不住。
- **不安全的 innerHTML** = 把别人给的字直接当"装修指令"执行，而非当"纯文字"显示。
- **扩展滥用** = 装了个"万能助手"浏览器插件，它有权读你所有网页——若它作恶或被黑，你全网裸奔。

## 🟡 部分二·动手层（逐字操作）
DOM XSS 复现+修复（自己页面，合法）：
```html
<!-- vuln.html：把 URL 的 #name 直接塞进页面 -->
<script>
  // 漏洞：用 innerHTML 插用户输入
  document.getElementById("box").innerHTML = location.hash.slice(1);
</script>
<!-- 访问 vuln.html#<img src=x onerror=alert(1)> 触发 XSS -->
```
**修复**：用 `textContent`（纯文本，不解析 HTML）：
```js
document.getElementById("box").textContent = decodeURIComponent(location.hash.slice(1));
```
或若必须富文本，用 DOMPurify 消毒：
```js
box.innerHTML = DOMPurify.sanitize(location.hash.slice(1));
```
在笔记写：DOM XSS 与 SEC5 反射/存储 XSS 区别 = **不经过服务器、纯前端执行**，所以 WAF/服务器过滤拦不住，只能前端修。

## 🔵 部分三·原理层（底层发生了什么）
- **DOM XSS** = 恶意数据流经 **JavaScript（DOM API）** 而非服务端响应，触发点在客户端。`innerHTML`/`document.write`/`eval`/`setTimeout(string)` 都是危险 sink。
- **CORS 误配** = 服务器 `Access-Control-Allow-Origin: *` 又带 `Credentials`，或反射任意 Origin → 恶意站点 JS 能读你域的响应（如带 cookie 的 API 数据）。
- **扩展安全** = 浏览器扩展声明 `permissions`（如 `<all_urls>`、`cookies`）过大 = 攻击面；恶意/被劫持扩展可读所有网页、改 DOM、偷 token。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：DOM XSS 随 SPA（单页应用）普及而重要，**AngularJS 表达式注入（client-side template injection）** 曾是大类；现代框架（React/Vue）默认转义大幅降低风险，但 `dangerouslySetInnerHTML`/`v-html` 仍会重燃。
- 顶级修复：**① 默认用 textContent / 框架绑定（自动转义）② 富文本用 DOMPurify ③ CSP（SEC9）限制 script 源 ④ 扩展最小权限 + 审查来源 ⑤ CORS 固定白名单不反射**。

## 🔴 部分五·顶级视角
顶级工程师把客户端当**"不可信执行环境"**：浏览器里的 JS 永远可能被注入，所以**即使后端安全，前端也要防 DOM XSS**；他们用**CSP + 自动转义框架 + 杜绝危险 sink** 三层防护，并审**第三方脚本（供应链，如被投毒的 npm 包）**。这衔接 SEC5/SEC9 与方向6 前端。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 在自己本地 html 复现 DOM XSS + 修复，合法。
- ✅ 学客户端安全是为"写不被注入的前端"。
- 🚫 不对非授权网页注入脚本；不滥用扩展权限窃他人数据。

## ✅ 验收
交 `vuln.html` + 修复版 + 一句话"DOM XSS 为什么服务器 WAF 拦不住"。

## ⚠️ 常见坑
- 以为"后端过滤了 XSS 就安全"——DOM XSS 不过后端。
- 用 `eval` 解析 JSON（`eval(json)`）→ 用 `JSON.parse`。
- 手疼：复制 html 改一处即可。

## ➡️ 下一步
**SEC-W8 · 业务逻辑进阶（验证码绕过 / 短信炸弹 / 并发薅 / 流程乱序）**

### ⚔️ 闯关路线（SEC-W7）
- 🟢 Lv1：写 vuln.html 用 innerHTML 触发 DOM XSS 截图。🥉
- 🔵 Lv2：改 textContent / DOMPurify 修复。🥈
- 🟣 Lv3：配一条 CSP 头 `default-src 'self'` 阻拦内联脚本，复测。🥇
- 🟠 Lv4：审查一个第三方扩展的 `permissions` 声明，列出过度权限风险。🏆
- 🔴 Lv5：给闪电号卡前端写"客户端安全规范"（禁危险 sink/CSP/扩展治理/供应链审查）。👑

---

# SEC-W8 · 业务逻辑进阶（验证码绕过 / 短信炸弹 / 并发薅 / 流程乱序）

## 🎯 目标
学完这节能识别 **更难扫出来的逻辑漏洞**：验证码可绕过/可复用、短信/邮件炸弹（无限发）、并发薅（重复领/超领）、业务流程乱序（跳过支付直接发货）；在**自己 Flask** 复现"短信炸弹（无限发）+ 并发超领"并修复（限频+幂等+状态机）。

## 📋 小白前置
- SEC18（逻辑漏洞基础）、SEC21（API 限流）、SEC11（越权）
- 方向6 BE15（限流/幂等）、BE20（支付幂等对账）、方向5 DB（事务）

## 🟢 部分一·最浅层（生活比喻）
- **短信炸弹** = 自动售货机按钮没限次，小孩狂按"免费饮料"键，机器吐了一地（你被短信轰炸/服务商收费爆表）。
- **并发薅** = 优惠券"每人限领1"，但十个人同时伸手，机器没数清发了 10 张（SEC18 竞态）。
- **流程乱序** = 网购"付款→出库"，你直接把"出库"按钮点了，跳过付款（状态机可逆/可跳）。

## 🟡 部分二·动手层（逐字操作）
① 短信炸弹复现+修复（自己 Flask，合法）：
```python
from flask import Flask, request
from flask_limiter import Limiter
from flask_limiter.util import get_remote_address
app = Flask(__name__)
limiter = Limiter(get_remote_address, app=app)
@app.route("/sms")
@limiter.limit("1 per 60 seconds")          # 每IP每分钟1条
def send_sms():
    return "已发送"
# 漏洞版：去掉 limiter → 可无限刷（自己试自己，观察限频生效）
```
② 并发超领（复用 SEC18 的 coupon 代码，加 `threading.Lock` 或 DB 行锁修复，截图"并发后仍只领1张"）。
③ 流程乱序：把"订单状态"建模成状态机（待支付→已支付→已发货），任何**逆向/跳步迁移直接拒绝**（写 `if next_state not in allowed[cur]: abort`）。

## 🔵 部分三·原理层（底层发生了什么）
- **验证码/短信**：无频次上限 + 无单用户配额 = 炸弹；验证码若**服务端不校验/可复用/前端可绕过**（如验证码答案在 HTML 里）即失效。
- **并发**：读-改-写非原子（SEC18）→ 用锁/事务/原子操作（DB6 MVCC、Redis `INCR`+`EXPIRE`）。
- **流程乱序**：缺少**集中状态机**约束，各接口各自改状态，攻击者直接调"后步骤"接口。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：电商/金融逻辑漏洞是**赏金最高**类（2018 Apple、各类打车/外卖曾被薅到崩）。自动化扫描器查不出"业务不变量被破坏"，**靠威胁建模+不变式测试**（SEC18）。
- 顶级修复：**① 所有对外动作限频+配额 ② 关键操作状态机集中校验 ③ 业务不变量写自动化测试（余额≥0/限领1）④ 幂等键防重放（BE20）⑤ 验证码服务端校验+限尝试+绑会话**。

## 🔴 部分五·顶级视角
顶级工程师把"业务逻辑安全"当**产品正确性的延伸**：不与"安全团队"割裂，而是**写在验收标准里**——每个核心流程都有"不变量 + 状态机 + 幂等 + 限频"四件套，并配**资金/数量对账**（BE20 闪电报结算同源）。这呼应 SEC1 威胁建模从业务流出发。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 在自己 Flask 复现+修复所有逻辑漏洞，合法。
- ✅ 学逻辑漏洞是为"写薅不动的业务"。
- 🚫 不去任何真实业务系统试"绕过/并发薅"；那是违法级别的薅羊毛/诈骗。

## ✅ 验收
交代码：限频短信接口 + 并发安全领券（锁/事务） + 订单状态机拒绝乱序 + 一句话"为什么逻辑漏洞扫描器查不出"。

## ⚠️ 常见坑
- 只在首页限频，忘了每个敏感动作都要。
- 状态机只在前端控制 → 后端直调绕过。
- 手疼：复制 SEC18 代码改即可。

## ➡️ 下一步
**方向7b（SEC23+ 二进制/漏洞利用/Hook）**——进入更底层白帽世界。

### ⚔️ 闯关路线（SEC-W8）
- 🟢 Lv1：起 Flask 短信接口，去掉限频狂刷截图（演示炸弹），再加限频修复。🥉
- 🔵 Lv2：并发领券复现超领，加锁修复。🥈
- 🟣 Lv3：把订单建模成状态机，写"逆向迁移拒绝"测试。🥇
- 🟠 Lv4：给关键业务写"不变量自动化测试"（余额≥0/限领1）。🏆
- 🔴 Lv5：给闪电号卡设计"业务逻辑安全 checklist"（限频/配额/状态机/幂等/对账）并落地到 M5 结算。👑

---

> **方向7a 进阶补遗完结（SEC-W1–W8）**：Web 安全深度从 SEC1–SEC22 扩展到 W1–W8，覆盖密码存储、JWT、缓存/子域、OAuth、GraphQL、WebSocket、客户端、业务逻辑八大进阶面，全部白帽、全部带 Lv1–Lv5 闯关 + 可运行 Lab。下一步进方向7b 二进制（SEC23+），仍在自己/授权样本范围。
