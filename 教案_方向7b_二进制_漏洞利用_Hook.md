# 教案 · 方向7b · 二进制 / 漏洞利用 / Hook（白帽）

> **本文件覆盖**：SEC23–SEC55，共 **33 节**（白帽二进制 / 逆向 / 漏洞利用 / Hook）。
> **写法**：每节严格按 11 段深模板（🎯目标 → 📋小白前置 → 🟢最浅层 → 🟡动手层 → 🔵原理层 → 🟣深挖层 → 🔴顶级视角 → 🟠安全合规种子 → ✅验收 → ⚠️常见坑 → ➡️下一步）+ 实战 Lab。
> **工具环境**：Windows；已装 Cutter、x64dbg、Ghidra、Rizin、MSYS2（含 gcc / nasm / gdb）。所有动手命令可直接复制，少打字护手。
> **学生画像**：真·零基础、术后留置针手不便、时间极充裕、目标计算机顶级大佬（白帽方向）。语言通俗、循序渐进、不跳步。
> **铁律（贯穿全文）**：逆向 / 调试 / 漏洞实验**只对自己编译的程序、授权样本、CTF 二进制**；绝不逆向他人商业软件、绝不注入/Hook 他人进程偷密、绝不绕正版验证、绝不写恶意模块；讲利用原理是为学会**防护与检测**。身体优先，手疼就停。

---

## SEC23 · 逆向工程概论 / 法律边界

### 🎯 目标
学完能用一句话说清"逆向工程是什么"，并在 Windows 上打开自己写的小程序，用 Cutter 看到它的函数列表；同时能背诵白帽红线与法律边界，知道什么能做、什么绝不碰。

### 📋 小白前置
- 方向1：C1（C 入门与 Hello，知道程序怎么编译成 exe）、C2（数据类型与表示，知道内存里是字节）
- 方向3：COMP1（冯·诺依曼，知道程序=指令序列）、ASM1（x86-64 寄存器，先有个印象即可）、OS1（进程/线程，知道程序跑起来是进程）
- 本文件内：无（这是方向7b 第 1 节，作为起点）

### 🟢 部分一·最浅层（生活比喻）
把软件想成一台**自动售货机**。你按按钮（输入），机器掉出饮料（输出）。正常用户只关心"投币→拿可乐"。但工程师想知道机器**内部机械结构**——齿轮怎么咬合、弹簧怎么弹。拆开看内部结构，就是"逆向"。注意：拆**你自己买的**机器研究没问题；拆邻居家锁着的机器、为了偷可乐，就是另一回事了。

### 🟡 部分二·动手层（逐字操作）
1. 先写一个极简 C 程序 `hello.c`（复制下面代码，存到 `D:/lab/sec23/hello.c`）：
```c
#include <stdio.h>
int add(int a, int b){ return a + b; }
int main(){ printf("result=%d\n", add(2,3)); return 0; }
```
2. 打开 **MSYS2 MinGW 64-bit** 终端（开始菜单搜 MSYS2），逐字执行：
```bash
mkdir -p /d/lab/sec23 && cd /d/lab/sec23
gcc hello.c -o hello.exe
```
3. 打开 **Cutter**，点「Open File」选 `hello.exe`，默认选项直接 OK。左侧点「Functions」，能看到 `main`、`add`、`printf` 等函数列表——这就是程序内部的"零件清单"。

### 🔵 部分三·原理层（底层发生了什么）
`gcc` 把人类可读的 C 源码，经过**预处理→编译→汇编→链接**（见 CMP1-CMP9）变成机器能执行的 PE 文件（下节 SEC24 细讲）。Cutter 做的事是把机器码"翻译回"人类能读的形式：它先做**反汇编**（机器码→汇编，SEC27），再尝试**反编译**（汇编→类 C 伪代码）。你看到的 `add` 函数，就是它从字节里识别出的一个"代码块"。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么需要逆向？因为发布出去的软件几乎都只有"机器码"，没有源码。安全研究员逆向是为了：找漏洞写补丁、分析自己中招的样本、做 CTF。历史上逆向伴随软件业而生——早期没有文档，只能"看机器码学"。顶级工程师会追问：反编译出的伪代码和真源码差多远？编译器优化（CMP7）会让结构"变形"多少？这引出下节 PE 格式与反汇编的精确性。

### 🔴 部分五·顶级视角
顶级二进制研究员把逆向当"在没图纸的情况下修引擎"。他们看一眼函数调用图（call graph）就能猜出这是登录校验还是网络发送。跨领域：逆向和编译器（CMP）、OS（OS10 系统调用）、甚至语言学（AST）相通。会逆向的人，写代码也更懂"编译器会把我写的什么优化掉"，天然写出更稳的程序。

### 🟠 部分六·安全/合规种子
**白帽红线**：逆向**只限**①你自己写并编译的程序；②明确授权的样本（如厂商给研究员的 PoC）；③CTF 比赛提供的二进制。🚫 绝不逆向他人商业软件提取算法/绕过授权；🚫 绝不为了"免费用"去破注册码；🚫 绝不逆向含他人隐私的程序。学逆向是为了**防护**——懂攻击者怎么看你的程序，才能写得更安全。法律上：各国对"绕过技术保护措施"有严格规定（如 DMCA），授权范围外一律不做。

### ✅ 验收
交：①`hello.exe` 文件；② Cutter 里 `Functions` 列表截图（能看到 main/add）；③ 一句话写清"逆向是什么 + 你只对自己程序做"。

### ⚠️ 常见坑
- 小白最容易在 MSYS2 路径写错：`/d/lab` 对应 `D:/lab`，不是 `C:`。手不便就复制整句。
- Cutter 第一次打开大程序会卡几秒，正常现象，别以为坏了。
- 误以为逆向=破解。记住：看自己程序合法，绕别人验证违法。

### ➡️ 下一步
**SEC24 · PE 文件格式**——搞懂 Windows 的 exe 到底由哪些"零件"拼成（DOS 头/NT 头/节区/导入表）。

---

## SEC24 · PE 文件格式

### 🎯 目标
学完能说出 Windows 的 `.exe` 由哪几大块组成，并用 Cutter/Rizin 在自己的 `hello.exe` 上指出 DOS 头、NT 头、"导入表"在哪里、里面列了哪些 DLL 函数。

### 📋 小白前置
- 方向1：C2（数据类型与表示，字节/对齐概念）、C13（构建系统，知道链接这一步）
- 方向3：CMP9（链接与装载，ELF/PE/重定位，先建立"文件有结构"的认知）、OS1（进程，稍后理解"装载"）
- 本文件内：SEC23（已会打开程序看函数）

### 🟢 部分一·最浅层（生活比喻）
PE 文件像一封**标准格式的快递箱**。箱子最外面贴一张固定格式的"面单"（DOS 头+NT 头），告诉系统"我是谁、多大、怎么开"；里面分几个**隔间**（节区 .text/.data/.rdata），分别装"说明书文字""零件""易碎品"；还有一张**配件清单**（导入表），写着"我需要外借螺丝刀（kernel32 的某函数）"。系统按面单把箱子摆到桌上（装载进内存）才运行。

### 🟡 部分二·动手层（逐字操作）
1. 打开 Rizin（命令行，护手复制）：
```bash
rizin -q -c 'i; izz~KERNEL32' /d/lab/sec23/hello.exe
```
   说明：`i` 打印文件信息；`izz` 列出字符串，`~KERNEL32` 过滤出含 KERNEL32 的行（即它导入的 DLL）。
2. 用 Cutter：打开 `hello.exe` → 左侧「Sections」看到 `.text`（代码）、`.rdata`（只读数据/字符串）、`.data`（全局变量）。点「Imports」看到从 `KERNEL32.dll` / `msvcrt.dll` 导入的函数（如 `printf` 实际来自 msvcrt）。

### 🔵 部分三·原理层（底层发生了什么）
PE = Portable Executable。结构自上而下：
- **DOS 头**（开头 `MZ` 两个字节）：历史兼容，老 DOS 系统看到会打印"请在 Win 下运行"。
- **NT 头**（含 `PE\0\0` 签名 + 文件头 + 可选头）：可选头里最关键的是 **ImageBase（建议装载地址）** 和 **Entry Point（入口偏移）**。
- **节区表**：描述每个节在文件/内存的位置、大小、权限（读/写/执行）。
- **导入表（IAT）**：记录本程序运行前要系统帮它"连上"的外部函数地址。SEC49 会改写它做 Hook。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么分节区？因为 CPU 对"代码"和"数据"要求不同：代码通常**只读可执行**、数据**可读写但不执行**（防攻击，见 SEC44 DEP）。历史上 PE 脱胎于 UNIX 的 COFF，微软改出自己的" portable "版以跨 Win 版本。顶级工程师追问：节区对齐（文件 512B vs 内存 4KB）为何不同？这关系到"文件偏移 vs 虚拟地址（VA）"换算——Rizin 的 `iS` 能看到。

### 🔴 部分五·顶级视角
顶级研究员看 PE 像看 X 光：一个程序若有一个**同时可写又可执行**的节（W+X），立刻警觉"可能要装 shellcode"。他们用 PE 特征做恶意软件**分类**与**检测规则**（YARA）。跨领域：PE 结构与 OS 内存管理（OS5 分页）、链接器（CMP9）深度相关；懂 PE 才懂"为什么 ASLR 要随机化基址"。

### 🟠 部分六·安全/合规种子
只看**自己编译**的 exe 结构，或 CTF/授权样本的 PE。🚫 不去修改他人软件的导入表以绕过验证；🚫 不分析来路不明样本（可能含真 malware，需在隔离沙箱，见 SEC55）。本文件一切"改写导入表"实验（SEC49）都只对自己的程序、改自己程序行为。

### ✅ 验收
交：① Rizin 输出的导入 DLL 列表截图；② 在 Cutter 里指出 `.text`/`.rdata`/`.data` 三节各自权限；③ 写清 Entry Point 是什么。

### ⚠️ 常见坑
- `izz~KERNEL32` 的 `~` 是 Rizin/rizin shell 的过滤符（类似 grep），不是命令的一部分拼错。
- 把"虚拟地址 VA"和"文件偏移"搞混：内存里地址含 ImageBase，文件里是偏移，二者不同。
- 找不到导入：用 `-static` 编译会没有导入表，请用默认动态链接。

### ➡️ 下一步
**SEC25 · ELF 文件格式**——对比 Linux 的兄弟格式（段/节/PLT/GOT），建立跨平台视野。

---

## SEC25 · ELF 文件格式

### 🎯 目标
学完能说清 ELF（Linux 程序）和 PE 的异同，理解"段(segment)"与"节(section)"区别，并讲出 PLT/GOT 是什么（为 SEC49 导入表 Hook 打底）。

### 📋 小白前置
- 方向1：C2（字节/对齐）、C13（链接）
- 方向3：CMP9（链接与装载，ELF/PE/重定位）、OS5（分页）、OS10（系统调用）
- 本文件内：SEC23、SEC24（已懂 PE 头/节/导入表）

### 🟢 部分一·最浅层（生活比喻）
ELF 是 Linux 版"快递箱"。和 PE 相似，但 ELF 多了个巧思：**段**是给"装卸工"（系统装载器）看的——告诉它怎么把箱子摆进内存；**节**是给"修理工"（链接器/调试器）看的——按功能细分零件。一个段里可以装好几个节。导入表在 ELF 里叫 **GOT（全局偏移表）**，像个"外借工具暂存架"，第一次用才去借（延迟绑定）。

### 🟡 部分二·动手层（逐字操作）
MSYS2 里我们可交叉思路，但 Windows 上做 ELF 最稳是用 WSL 或装个 Linux 虚拟机。为护手，这里用 **Rizin 也能读 ELF**（若你已有 Linux 编译的 elf 文件）。先看自己 Windows 程序对照即可；若暂时没有 elf，跳过实操，记下概念。
```bash
rizin -q -c 'i; iS; iE' your_program.elf
```
`iS` 列节(section)，`iE` 列入口(entry)。在 Cutter 打开 elf 同样能看到 Sections 与 Imports（ELF 叫 "Imports" 实为 PLT 桩）。

### 🔵 部分三·原理层（底层发生了什么）
- **ELF 头**：含魔数 `0x7F 'E' 'L' 'F'`、位数(32/64)、字节序、入口地址。
- **程序头表(Program Headers) = 段**：描述装载视图（哪些字节映射成可读/写/执行的内存）。
- **节头表(Section Headers) = 节**：`.text`/`.data`/`.bss`/`.symtab`/`.dynstr` 等，链接/调试用。
- **PLT/GOT**：调用外部函数（如 `puts`）时，先跳 PLT 桩，第一次通过 GOT 触发动态链接器填真实地址（延迟绑定），以后直接走 GOT。这就是"为什么 Hook GOT 能拦截函数"（SEC49 的 ELF 版）。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么分"段/节"两套视图？因为**装载**只关心"内存怎么映射"（段，少而粗），**分析**关心"代码数据怎么归类"（节，多而细）。历史上 UNIX 用 a.out 后来换 COFF 再演进成 ELF，目标是"跨 CPU/OS 通用"。顶级追问：`.symtab` 含符号名（调试信息），strip 后没了，逆向更难——这正是"去符号"对抗（SEC30）的源头。

### 🔴 部分五·顶级视角
顶级研究员在 ELF 上做的事和 PE 对称：查节权限、找 GOT 做函数监控、看 `.init_array`/`.fini_array`（类似构造/析构钩子）。跨平台能力是顶级标志——同一漏洞在 PE/ELF/Mach-O(SEC26) 三种格式下原理相通，只是"零件名"不同。

### 🟠 部分六·安全/合规种子
只对自己 Linux 程序或授权 CTF elf 做结构分析。🚫 不拿 ELF 分析工具去窥探他人服务器程序。GOT 改写技术（SEC49）只在自己进程/自己程序里练，用来理解"函数调用如何被拦截以利检测"。

### ✅ 验收
交：① 用文字对比 PE 与 ELF 的 3 点异同；② 说明 PLT/GOT 作用；③（有 elf 则）Rizin 节列表截图。

### ⚠️ 常见坑
- 把 section 和 segment 混为一谈：记住"段=装载视图，节=分析视图"。
- Windows 原生不能直接跑 ELF，需要 WSL/Linux；别用错工具环境。
- 以为 GOT 一定在运行时填好：延迟绑定下首次调用前 GOT 指向 PLT 后续指令。

### ➡️ 下一步
**SEC26 · Mach-O 文件格式**——macOS/iOS 的格式，补齐三大平台拼图。

---

## SEC26 · Mach-O 文件格式

### 🎯 目标
学完能说出 Mach-O（macOS/iOS 程序）的核心结构：Fat 通用二进制、加密(加密段)、绑定(binding)，并与 PE/ELF 对比，建立"三平台格式通吃"的框架。

### 📋 小白前置
- 方向3：CMP9（链接装载）、OS5（分页）
- 本文件内：SEC24、SEC25（已懂 PE/ELF，这里做第三块拼图）

### 🟢 部分一·最浅层（生活比喻）
Mach-O 是苹果家的"快递箱"，特点是能装**多份**（Fat 通用二进制：同一箱子里有 Intel 版和 ARM 版两份，按机型取对应那份）。它还带一把"锁"（加密段 LC_ENCRYPTION_INFO）——App Store 下载的程序部分加密，运行时才由系统解开。箱子里的"借工具清单"通过**绑定(binding)** 在启动时逐步连上。

### 🟡 部分二·动手层（逐字操作）
Windows 上你未必有 Mach-O 文件。护手方案：用 **Cutter 打开任意自己程序**，对照"Load Commands"概念即可；或安装 `otool` 等价思路用 Ghidra 加载 Mach-O 样本（仅授权/CTF）。本机若无苹果样本，记下结构图：
```
Mach-O = 头(magic) + 一系列 Load Commands(fat 时外包 Fat 头) + 数据
关键 LC：LC_SEGMENT_64(段/.text/.data) LC_DYLD_INFO(binding) LC_ENCRYPTION_INFO(加密)
```

### 🔵 部分三·原理层（底层发生了什么）
- **Fat 头**：`cafebabe`(旧)/`cafebabf`(新)，列出内含哪些 CPU 切片。
- **Mach 头**：magic（`0xfeedfacf` 64 位）、CPU 类型、文件类型。
- **Load Commands**：告诉内核怎么装载——`LC_SEGMENT_64` 定义每段虚拟地址/文件偏移/权限；`LC_MAIN` 入口；`LC_DYLD_INFO` 描述绑定/重定位；`LC_ENCRYPTION_INFO_64` 指出加密范围。
- **绑定(binding)**：动态链接器按规则把外部符号地址写进 `__DATA` 的桩，类似 GOT 但更结构化。

### 🟣 部分四·深挖层（为什么 & 延伸）
苹果用 Mach-O 源于 NeXTSTEP(Mach 微内核)。Fat 二进制解决"一台 macOS 同时跑 Intel 和 Apple Silicon"的过渡。加密段是 DRM 一环——但**分析加密段需越狱/授权**，白帽只做合法授权样本。顶级追问：IDE 如何把多架构合成 Fat？`lipo` 工具怎么拆片？

### 🔴 部分五·顶级视角
顶级移动安全研究员（MOB28/MOB29）天天和 Mach-O 打交道：找加密段判断是否需要脱壳、看 `bind` 表还原调用关系。三格式(PE/ELF/Mach-O)通吃，是"任何新平台几天上手"的能力体现。

### 🟠 部分六·安全/合规种子
只对自己/授权苹果样本分析；🚫 不绕 App Store 加密拿他人 App 代码；🚫 不逆向付费 App 白嫖。Mach-O 加密段分析仅用于理解 DRM 原理与自家 App 加固评估（MOB20）。

### ✅ 验收
交：① 手绘/文字描述 Mach-O 三层结构；② 对比 PE/ELF/Mach-O 各用啥机制记录"外部函数地址"（导入表/GOT/binding）；③ 说明 Fat 是什么。

### ⚠️ 常见坑
- 易把 Mach-O 段名 `__TEXT`/`__DATA` 和 PE 的 `.text`/`.data` 对应错：前缀双下划线是苹果风格。
- 加密段不是"全文件加密"，只密部分；理解错会以为整文件不可读。

### ➡️ 下一步
**SEC27 · 反汇编/反编译**——真正把机器码变回人读的代码，三平台通用核心技能。

---

## SEC27 · 反汇编 / 反编译

### 🎯 目标
学完能用 Cutter/Ghidra/Rizin 把自己的 `hello.exe` 反汇编成汇编、反编译成类 C 伪代码，并指出"main 里调用了 add"这件事在汇编里长什么样。

### 📋 小白前置
- 方向1：C1/C2/C3（C 基础，知道函数/指针）
- 方向3：ASM1-ASM4（寄存器/算术/控制流/栈帧——**重点先读 ASM1、ASM4**）、CMP4（AST，理解"结构化"）
- 本文件内：SEC23、SEC24（会打开程序看函数）

### 🟢 部分一·最浅层（生活比喻）
机器码像**摩斯电码**（一长串 0/1 点划），人直接读会疯。反汇编是把电码翻译成"单词"（汇编指令，如 `mov`、`add`）；反编译是把单词再串成"句子"（类 C 伪代码）。就像把乐谱(机器码)→唱名(汇编)→歌词大意(伪代码)。

### 🟡 部分二·动手层（逐字操作）
1. Cutter 打开 `hello.exe` → 双击 `main` 函数。
2. 上方切到 **Disassembly**（汇编）看指令；切到 **Decompiler**（伪代码）看类 C：
```c
// Ghidra/Cutter 反编译大致长这样
int main(void) { printf("result=%d\n", add(2,3)); return 0; }
```
3. Rizin 命令行看汇编（护手复制）：
```bash
rizin -q -c 'pdf @ main' /d/lab/sec23/hello.exe
```
`pdf` = print disassembly function，`@ main` 指定函数。

### 🔵 部分三·原理层（底层发生了什么）
CPU 只认**机器码**（如 `55`=`push rbp`）。反汇编器（如 Capstone，Cutter/Ghidra 都用类似引擎）做**线性扫描或递归下降**：从入口按指令长度逐条译回助记符。反编译器更难：要先做**反编译分析**（控制流图 CFG + 数据流，SEC29），把汇编"提升"成中间表示(IR)，再还原成 if/while/函数调用。编译器优化（CMP7 内联/循环展开）会让伪代码和源码不完全一样。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么反编译不完美？因为编译是"有损"的：变量名、注释、类型细节在编译时丢掉了，反编译只能**猜**（叫 `var_8`、`param_1`）。顶级工具 Ghidra 用"反编译脚本"可手动标注类型让伪代码更清晰。历史上 IDA 是商业标杆，Ghidra 是 NSA 开源的平替，Cutter 是 Rizin 的图形前端——三者底层都做同一件事。

### 🔴 部分五·顶级视角
顶级研究员把反编译器当"草稿纸"：先用伪代码快速理解逻辑，再回到汇编抠**精确字节**（因为伪代码可能误导，比如把有符号当无符号）。他们会给函数/变量**重命名+写注释**（Cutter 里按 N 改名），积累成"自己的源码库"。跨领域：反编译和编译器(CMP)、程序分析(CFG)同源。

### 🟠 部分六·安全/合规种子
反汇编/反编译**只对自己程序、授权样本、CTF**。🚫 不反编译他人商业软件窃取算法/数据；🚠 若拿到不明样本，先放隔离沙箱(SEC55)再分析，绝不本机直接跑。本技能用于：审计自己代码有无漏洞、做 CTF、研究授权 PoC。

### ✅ 验收
交：① Cutter 里 `main` 的反汇编截图（能看到 `call` 到 add）；② 反编译伪代码截图；③ 说明反汇编与反编译的区别。

### ⚠️ 常见坑
- Ghidra 初次分析大程序要几分钟，进度条在底部，别关。
- 伪代码里 `var_8`/`arg_1` 是工具占位名，不是真变量名，需自己改名理解。
- 误以为反编译 = 还原源码：函数名/类型已丢，只能近似。

### ➡️ 下一步
**SEC28 · 汇编级调试**——光看不够，要"让程序跑起来、在某一行停下看内存"，用 x64dbg/gdb。

---

## SEC28 · 汇编级调试

### 🎯 目标
学完能用 x64dbg（Windows）或 gdb（MSYS2）把自己程序"断在 main"，单步执行，实时看寄存器/内存变化；理解"调试器怎么让程序停下"。

### 📋 小白前置
- 方向1：C3（指针/内存）、C6（预处理，懂 `#include`）
- 方向3：ASM1-ASM4（寄存器/算术/控制流/栈帧——**必读 ASM4 栈帧**）、OS1（进程/线程）、COMP2（CPU 微架构，断点硬件原理）
- 本文件内：SEC27（会反汇编看指令）

### 🟢 部分一·最浅层（生活比喻）
调试器像给程序装了**隐形的暂停键 + 显微镜**。平时程序一闪而过；调试器说"到这一行先别动"（断点），你就能凑近看：此刻 CPU 手里拿的"便签纸"（寄存器）写了啥、桌上"抽屉"（内存）里放了啥。单步就是"一下走一行"。

### 🟡 部分二·动手层（逐字操作）
**用 x64dbg（图形，护手优先）：**
1. 打开 x64dbg → File → Open → `hello.exe`。
2. 右侧"Symbol"或左上"CPU"窗口，Ctrl+G 输入 `main` 回车，落在 main 开头。
3. 按 **F2** 设断点（行变红），按 **F9** 运行到断点暂停。
4. 按 **F8** 单步（步过），观察右侧寄存器 `RAX/RBX...` 变化；右下内存窗口看栈。

**用 gdb（命令行，备选项）：**
```bash
gdb /d/lab/sec23/hello.exe
(gdb) break main
(gdb) run
(gdb) stepi
(gdb) info registers
(gdb) x/10x $rsp
```

### 🔵 部分三·原理层（底层发生了什么）
- **软件断点**：调试器把目标指令首字节改成 `0xCC`(int3 中断)，CPU 跑到这触发异常，OS 把异常交给调试器，程序暂停；恢复时改回原字节。
- **硬件断点**：用 CPU 的 DR0-DR7 调试寄存器，不修改代码，适合在不可写处（如 ROM/只读节）设断。
- **单步**：置 EFLAGS 的 TF 陷阱标志，每条指令后触发异常交调试器。
- 调试器与被调试程序通过 **OS 提供的调试 API**（Windows `DebugActiveProcess` / Linux `ptrace`）建立"父子"关系，能读/写对方内存。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么能读别人内存？因为 OS 设计上允许"调试器进程"访问被调试进程地址空间（权限更高）。顶级追问：多核下硬件断点如何绑定到某线程？反调试怎么检测 `0xCC`（SEC60 提原理）？这些对抗正是"攻防共生"的体现——本文件只讲**检测原理用于防御**，不练反调试规避。

### 🔴 部分五·顶级视角
顶级研究员用条件断点（命中 N 次才停）、内存写断点（某地址被改才停）精准定位 bug/漏洞触发点。他们调试内核用 WinDbg、调远程用 gdbserver。跨领域：调试和 OS 异常分发（OS10 中断与系统调用）、CPU 微架构（COMP2）深度耦合。

### 🟠 部分六·安全/合规种子
调试**只对自己程序、授权靶机程序、CTF**。🚫 不附加调试器到他人正在运行的进程（如游戏/软件）去改内存作弊或偷数据——这侵犯他人且常违法。🚫 不用调试器绕注册验证。本技能用于：找自己程序 bug、学汇编、CTF pwn。

### ✅ 验收
交：① x64dbg 断在 main 的截图（寄存器窗可见）；② 用 F8 单步 3 步后 `RAX` 的值记录；③ 说明软件断点 `0xCC` 原理。

### ⚠️ 常见坑
- x64dbg 有 x32/x64 两个快捷方式，32 位程序用 x32，64 位用 x64，选错打不开。
- F8(步过) 与 F7(步入) 区别：调函数时 F8 不进函数内部，F7 会进；初学易乱按。
- gdb 在 Windows 上对 MinGW 程序支持有限，复杂时用 x64dbg 更顺。

### ➡️ 下一步
**SEC29 · 控制流/数据流分析**——不止看单条指令，要还原"整个程序怎么走、数据从哪来"。

---

## SEC29 · 控制流 / 数据流分析

### 🎯 目标
学完能对着自己程序的反编译图，指出"控制流图(CFG)里哪条是 if 分支、哪条是循环"，并用数据流思路说明"某变量最终来自用户输入还是常量"——这是逆向算法的核心。

### 📋 小白前置
- 方向1：PY4（条件 if）、PY5（循环）——高级语言对应结构
- 方向3：CMP4（AST）、CMP6（IR，SSA 概念）、ASM3（控制流/调用约定）
- 本文件内：SEC27（会反编译）、SEC28（会调试）

### 🟢 部分一·最浅层（生活比喻）
控制流 = **地铁线路图**：站点是"基本块"(一串顺序指令)，线是"跳转"(if/循环/函数调用)。数据流 = **快递追踪**：一个包裹(数据)从哪发出(来源)、经哪几站(运算)、到哪签收(用到哪里)。逆向就是：给你一张乱线的地铁图，还原出"原来 3 号线和 8 号线在 X 换乘，且包裹在 Y 站被改过"。

### 🟡 部分二·动手层（逐字操作）
写个小程序含分支与循环，便于观察：
```c
// flow.c
#include <stdio.h>
int main(int argc, char**argv){
    int x = argc;            // 来自"用户输入"(命令行参数个数)
    int s = 0;
    for(int i=0;i<x;i++) s += i;
    if(s>10) printf("big\n"); else printf("small\n");
    return 0;
}
```
编译：`gcc flow.c -o flow.exe`。Cutter 打开 → 看 `main` 的 **Graph/Decompiler**：能看到 `for` 对应回边、`if` 对应两个分支块。Rizin 看 CFG：
```bash
rizin -q -c 'agf @ main' flow.exe
```

### 🔵 部分三·原理层（底层发生了什么）
- **基本块**：从入口到末尾（含跳转）的一段顺序指令，块内无分支。
- **CFG**：块间跳转构成有向图；`if` 产生两条出边（真/假），`循环`产生回边（回到块首）。
- **数据流分析**：在 CFG 上做"到达定义/活跃变量"等迭代计算。例如"变量 s 在 printf 前，定义来自循环里的 `s+=i`"——这就是**污点分析**雏形（SEC20 代码审计也用）。
- **SSA 形式**（CMP6）：每个变量只赋值一次，便于追踪来源，是高级分析器的内部表示。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么需要 CFG？因为汇编里只有 `jmp/jz` 等跳转，没有 `if/for` 字样；反编译器靠"识别回边=循环、条件跳转=分支"来重建结构。顶级追问：编译器优化会把 `for` 展开成无分支（向量化 COMP6），CFG 看起来就不像循环了——如何识别？这引出"语义等价但结构不同"的还原难题。

### 🔴 部分五·顶级视角
顶级研究员用 CFG+数据流**还原加密/校验算法**（如把一段乱跳的代码还原成"CRC32 计算"）。他们写 **IDApython/Cutter 脚本**自动标注数据流。跨领域：数据流分析和编译器优化(CMP7)、程序验证、甚至数据库查询优化（DB9 代价模型）共用"迭代不动点"思想。

### 🟠 部分六·安全/合规种子
控制流/数据流分析只用于自己程序、授权样本、CTF。🚫 不用于对他人软件做"逻辑还原"以抄袭算法或找绕过点。本技能用于：审计自己代码逻辑漏洞、CTF 逆向、理解编译器行为。

### ✅ 验收
交：① `flow.exe` 的 CFG 截图（标出循环回边与 if 两分支）；② 文字说明"变量 s 的来源路径"；③ 解释基本块定义。

### ⚠️ 常见坑
- 优化编译（`gcc -O2`）后 CFG 变形大，初学用默认 `-O0` 看结构更清晰。
- 把"跳转到自己"一律当死循环：其实常是 `for` 回边，需看条件。
- Rizin 的 `agf` 输出是 graphviz 文本，Cutter 图形更直观，二选一即可。

### ➡️ 下一步
**SEC30 · 去混淆/字符串解密**——很多样本把字符串加密藏起来，运行才解密，我们学"动静态结合"让它现形（仅自己/授权样本）。

---

## SEC30 · 去混淆 / 字符串解密

### 🎯 目标
学完能理解"为什么样本要把字符串加密藏起来"，并用"调试跑到解密函数后 dumping 内存"的方法，把自己写的小程序里被加密的字符串还原出来（动静态结合）。

### 📋 小白前置
- 方向1：C7（动态内存 malloc/free）、C9（位运算——异或加密常用）
- 方向3：ASM2（算术逻辑）、OS5（内存，运行时解密在内存）
- 本文件内：SEC27（反编译）、SEC28（调试设断点）

### 🟢 部分一·最浅层（生活比喻）
有些程序怕你一眼看穿，把"暗号本"（字符串/代码）先**锁进保险箱**（加密）。静态看只有乱码。但它运行时总得**打开保险箱**用，所以你在它"开锁那瞬间"用显微镜（调试器）拍照，就拿到明文了。这叫"动静态结合"：静态看箱子结构，动态抓开锁瞬间。

### 🟡 部分二·动手层（逐字操作）
写个"加密字符串"小程序（自己写的，练手用）：
```c
// secret.c  仅演示：把字符串 XOR 0x41 后运行时解密
#include <stdio.h>
#include <string.h>
int main(){
    char buf[32];
    unsigned char enc[] = {0x13,0x12,0x1a,0x1a,0x1e,0x00}; // "hello" XOR 0x41
    for(int i=0;i<strlen((char*)enc)+1;i++) buf[i]=enc[i]^0x41;
    printf("%s\n", buf);   // 运行时才出现 "hello"
    return 0;
}
```
编译：`gcc secret.c -o secret.exe`。
- **静态**：Cutter 看字符串窗，只看到 `enc` 的乱码字节，看不到 "hello"。
- **动态**：x64dbg 打开 → 在 `printf` 处 F2 设断 → F9 跑 → 暂停时看 `buf` 所在栈内存（右下内存窗跟随 `rsp`），已显示明文 "hello"。

### 🔵 部分三·原理层（底层发生了什么）
加密字符串在 **.rdata** 存的是密文；运行到解密循环，CPU 把密文逐字节 `xor key` 写进**栈上 buf**（运行时内存）。静态分析只看到文件里的密文；动态在解密完成后、使用前方下断，内存里就是明文。同理"代码混淆/加密"（SEC31 加壳）也是运行才解，断在"解密完、跳去执行"的瞬间(OEP)即可 dump。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么作者藏字符串？合法用途：保护配置/密钥、反作弊；非法用途： malware 藏 C2 域名逃避静态扫描。安全研究员必须会"让它现形"才能分析。顶级追问：若解密靠**多步+查表**(S 盒)，静态能不能靠符号执行(SEC32)直接算明文而不运行？这正是高级自动化解密方向。

### 🔴 部分五·顶级视角
顶级分析师建"解密脚本库"：识别常见 XOR/RC4/Base64 模式自动还原。他们用**仿真(SEC33 Unicorn)**在不开真实进程情况下模拟跑解密函数，直接出明文——更快更稳。跨领域：和加密学(SEC4)、编译优化(常量传播 CMP7)相通。

### 🟠 部分六·安全/合规种子
去混淆/解密**只对自己程序、授权样本、CTF**。🚫 不对他人软件做字符串解密以提取隐私/密钥/绕过验证。🚫 不拿此技术还原他人通信内容。本技能用于：分析自己加壳程序、CTF、理解 AV/EDR 为何要动态抓明文。

### ✅ 验收
交：① `secret.exe` 静态字符串窗截图（只有乱码）；② x64dbg 在 printf 断点处内存窗显示 "hello" 的截图；③ 说明为何静态看不到明文。

### ⚠️ 常见坑
- `strlen((char*)enc)` 对含 0x00 结尾的密文误判长度——本例 enc 以 0x00 结尾恰巧可，真实样本常另存长度。练手注意。
- 断点设太早（解密前）内存还是密文；要设在"解密完之后"。
- xor 密钥别用可见字符，调试时内存窗按 ASCII 看才显形。

### ➡️ 下一步
**SEC31 · 加壳与脱壳**——整段代码加密压缩，运行时自解，我们学"找 OEP Dump"原理（自己程序练）。

---

## SEC31 · 加壳与脱壳

### 🎯 目标
学完能说清"壳"是什么（压缩壳/加密壳/VM 壳），并对自己用简单壳压缩过的程序，理解"原始入口 OEP"概念与脱壳思路（原理+对自己程序实操）。

### 📋 小白前置
- 方向1：C7（动态内存）、C13（构建）
- 方向3：CMP9（装载/重定位）、OS5（内存页权限）、ASM4（栈帧/入口）
- 本文件内：SEC24（PE 节/导入表）、SEC28（调试找点）、SEC30（解密思路）

### 🟢 部分一·最浅层（生活比喻）
壳 = 程序外面的**俄罗斯套娃**。原程序被压缩/加密塞进"壳"里；运行时壳先跑，把自己解开、还原出真程序、再把控制权交给它。脱壳 = 在"壳刚解开、真程序要接手"的那一瞬间，把内存里已还原的真程序**抄下来**存成新文件。

### 🟡 部分二·动手层（逐字操作）
Windows 上可用开源压缩壳 **UPX**（合法、自用）演示：
```bash
# 先编译一个普通程序
gcc flow.c -o flow.exe
# 加 UPX 壳
upx --best flow.exe -o flow_packed.exe
# 看区别：Cutter 打开 flow_packed.exe，函数很少(只有 UPX 解压壳)，入口在壳代码
```
脱壳原理实操（用 x64dbg 找 OEP）：
1. x64dbg 打开 `flow_packed.exe`，F9 跑。
2. UPX 在解压完会 `jmp` 到原 `main` 入口(OEP)。x64dbg 有插件可自动脱，或手动：在疑似 OEP（长串正常代码开头）F2 断，暂停后 Plugins→Scylla 选 OEP→Dump。
> 护手提示：手动脱壳步骤多，初学理解"OEP=原程序真入口"即可，工具自动脱可后续练。

### 🔵 部分三·原理层（底层发生了什么）
- **压缩壳(UPX)**：把各节压缩，加一段解压 stub；入口指向 stub；stub 在内存解压还原节、修复**导入表(IAT)**、跳 OEP。
- **加密壳**：解压+解密+反调试(SEC60 提原理) + IAT 加密（导入函数名藏起，运行才填）。
- **VM 壳**：把原程序指令翻译成"自定义虚拟机字节码"，运行时虚拟机解释执行，极难静态还原（顶级方向）。
- 脱壳关键：① 找 OEP；② 修复 IAT（因为壳常破坏原 IAT）；③ Dump 内存为文件。

### 🟣 部分四·深挖层（为什么 & 延伸）
壳本为**减小体积/保护知识产权**而生（合法）。但 malware 滥用它隐藏自身，所以 AV 厂商必须会脱壳扫描。顶级追问：VM 壳能否被"优雅反编译"？目前靠**符号执行(SEC32)+仿真(SEC33)**穷举路径近似还原，极难全自动。这也是为什么安全界持续投入自动化分析。

### 🔴 部分五·顶级视角
顶级逆向员面对 VM 壳会写"自定义反编译器"把虚拟机字节码提回 IR。他们区分"压缩壳(易脱) vs 加密壳(中) vs VM 壳(难)"制定策略。跨领域：和 OS 装载器(CMP9)、代码混淆(CMP7)同源。

### 🟠 部分六·安全/合规种子
加壳/脱壳**只对自己程序、授权样本、CTF**。🚫 不对他人商业软件脱壳提取代码；🚫 用壳隐藏恶意行为本身是黑产，绝不写。本技能用于：理解"为什么 AV 要脱壳扫描"、保护自有软件、CTF。

### ✅ 验收
交：① `flow.exe`(未加壳)与 `flow_packed.exe` 在 Cutter 里函数数对比截图；② 文字说明 OEP 是什么、脱壳三步；③ 说明 VM 壳为何最难。

### ⚠️ 常见坑
- 误以为脱壳=解密文件：实际是"内存中已还原时 dump"，文件本身仍加密。
- 现代 Windows Defender 常把 UPX 壳程序当可疑拦截（误报），自用时加白或关实时防护测试（仅自己机器、自己程序）。
- IAT 不修复，dump 出的文件跑不起来——脱壳不止 dump。

### ➡️ 下一步
**SEC32 · 符号执行/约束求解**——用"数学表达式"代替"真运行"来探索程序所有路径（angr 入门概念）。

---

## SEC32 · 符号执行 / 约束求解

### 🎯 目标
学完能用大白话解释"符号执行"和"约束求解"，并在自己小程序上理解"怎么自动找到让 if 成立的那组输入"（概念+轻量实操，重点在原理）。

### 📋 小白前置
- 方向1：PY10（单元测，理解"找输入触发分支"）、DSA（算法思维）
- 方向3：CMP6（IR/SSA）、ASM3（控制流）
- 本文件内：SEC29（控制流/数据流，符号执行是其自动化升级）

### 🟢 部分一·最浅层（生活比喻）
普通运行是"开真车走一条路"。符号执行是"拿**地图+公式**推算：如果我想到达终点，油门(输入)该踩多少"。它不给具体值，而是列出"到达终点需满足：油门>50 且 左转"。约束求解器(如 Z3)再解这公式，直接告诉你"油门=60 即可"。一次算清所有路。

### 🟡 部分二·动手层（逐字操作）
用 Python + Z3（约束求解，最易上手）演示"自动找通关输入"：
```python
# solve_demo.py  —— 自己写的玩具逻辑
from z3 import *
a = BitVec('a', 32)
s = Solver()
s.add(a * 7 + 3 == 1000)      # 假设"正确密码"满足此式
if s.check() == sat:
    print("找到 a =", s.model()[a])   # 解出 a 使得等式成立
```
运行（需 `pip install z3-solver`）：`python solve_demo.py`。这展示"约束求解"核心：给条件，自动出输入。符号执行是把"程序每条分支"都变成这类条件，再批量求解（angr 框架自动化，进阶自练）。

### 🔵 部分三·原理层（底层发生了什么）
- **符号执行**：把输入当"符号变量 x"而非具体值；每条运算变成对 x 的表达式；遇 `if(x>0)` 就分两支，各带约束 `x>0` / `x<=0`。
- **路径爆炸**：分支指数增长，是主要难题。
- **约束求解(SMT)**：用 Z3 等求解器判断"此路径约束是否可满足(sat)"并给具体值。若 sat，说明存在输入走该路——可直接用来生成**触发某 bug 的输入**(和 SEC47 Fuzzing 互补)。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么重要？它能在**不运行**情况下穷举路径、自动找"到达漏洞点的输入"，比盲 fuzz 高效。顶级追问：浮点/系统调用怎么符号化？angr 用"符号化系统调用"近似；现实常混合"具体执行+符号执行"(concolic)。历史上 DART/KLEE 是奠基工作。

### 🔴 部分五·顶级视角
顶级研究员用符号执行**自动生成 PoC**：比如"自动找到让 `strcpy` 溢出的超长输入"。它和 Fuzzing(SEC47)、仿真(SEC33)并称三大自动化武器。跨领域：和编译器测试、程序验证(形式化方法)同源。

### 🟠 部分六·安全/合规种子
符号执行/约束求解**只对自己程序、授权靶标、CTF**。🚫 不对他人服务自动搜漏洞输入做未授权探测。本技能用于：验证自己程序边界、CTF、自动化测试(good fuzz 替身)。

### ✅ 验收
交：① `solve_demo.py` 运行结果（解出 a 的值）；② 文字解释"符号执行 vs 普通运行"区别；③ 说明路径爆炸是什么。

### ⚠️ 常见坑
- Z3 的 `BitVec` 是定长(32/64)，溢出会回绕，需注意宽度。
- 误以为符号执行能解一切：含复杂系统调用/浮点时很难，需 concolic 混合。
- angr 初学重量级，先吃透 Z3 概念再上框架，避免劝退。

### ➡️ 下一步
**SEC33 · 仿真执行 Unicorn**——把一段代码"拎出来单独跑"，不启完整进程，常用于解密/算法提取（自己程序练）。

---

## SEC33 · 仿真执行 Unicorn

### 🎯 目标
学完能理解"仿真执行"= 用 CPU 模拟器单独跑某段机器码（不需要完整 OS），并对自己程序里的一小段解密函数做 Unicorn 仿真，取出结果。

### 📋 小白前置
- 方向3：ASM1-ASM4（寄存器/指令，仿真就是模拟这些）、COMP1（CPU 执行模型）、OS5（内存，需映射内存给模拟器）
- 本文件内：SEC30（解密场景）、SEC32（符号执行，互补）

### 🟢 部分一·最浅层（生活比喻）
完整运行程序像"开整辆真车上路"（要路、油、驾照=OS）。仿真执行像"把发动机拆下，架在台架上单独点火"——只跑你想看的那段，给点假油(内存)、假仪表(寄存器)，它就能转。好处：快、不依赖系统、不触发反调试。

### 🟡 部分二·动手层（逐字操作）
概念演示（Unicorn 用 Python 绑定，需 `pip install unicorn`）：
```python
# emu_demo.py  —— 在自己进程里模拟跑一段 x64 机器码：把 2 个寄存器相加
from unicorn import *
from unicorn.x86_const import *
mu = Uc(UC_ARCH_X86, UC_MODE_64)
# 映射一块内存
mu.mem_map(0x1000, 0x1000)
# 机器码: mov eax,3; mov ebx,4; add eax,ebx  (手写示意)
code = b"\xb8\x03\x00\x00\x00\xbb\x04\x00\x00\x00\x01\xd8"
mu.mem_write(0x1000, code)
mu.emu_start(0x1000, 0x1000+len(code))
print("eax =", mu.reg_read(UC_REG_EAX))   # 应得 7
```
运行：`python emu_demo.py`。真实用法：把 SEC30 的 `secret.exe` 解密函数机器码抠出，映射内存、设好输入寄存器，emu 出明文——无需 x64dbg 动态抓。

### 🔵 部分三·原理层（底层发生了什么）
Unicorn 基于 QEMU 的 CPU 模拟核心。你给它：① 一块**虚拟内存**并写入机器码/数据；② 初始**寄存器**；③ 起始地址。它逐条**翻译执行**指令（二进制翻译 JIT），遇到访存就在你映射的内存里读。因为不跑 OS，没有系统调用——所以你的代码不能调用 `printf`（要自己实现或跳过）。这正是它"轻、快、可控"的原因。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么不直接调试？因为完整进程有反调试/反沙箱检测(SEC60/SEC58)；仿真"无进程"更难被检测，适合分析**纯算法片段**（解密/校验）。顶级追问：怎么自动从二进制里"抠出"某函数及其依赖？需结合 CFG(SEC29)切基本块、解析 import 桩。历史上 Unicorn 让"无痕分析"普及。

### 🔴 部分五·顶级视角
顶级分析师用 Unicorn **批量自动化**：写脚本自动定位解密函数、喂符号输入(SEC32)、仿真出所有字符串。跨领域：和虚拟机(QEMU/OPS11)、fuzz(SEC47)共用二进制翻译技术。

### 🟠 部分六·安全/合规种子
Unicorn 仿真**只对自己程序、授权样本、CTF**。🚫 不对他人软件做无痕剥离算法。本技能用于：自动化提取自己程序逻辑、CTF、理解 malware 分析为何用仿真避检测（防御视角）。

### ✅ 验收
交：① `emu_demo.py` 输出（eax=7）；② 说明仿真执行与"完整调试"的 3 点区别；③ 举一个自己程序可用仿真的场景。

### ⚠️ 常见坑
- 仿真代码里若含 `call printf` 会崩（无 OS 提供该函数）；初学只放纯运算指令。
- 内存映射地址/大小要对齐（常按 0x1000 页），否则 `UC_ERR`；用 `mem_map` 给够空间。
- x86 模式要选 `UC_MODE_64`（64 位）或 `UC_MODE_32`，和密码学代码位数一致。

### ➡️ 下一步
**SEC34 · 内存破坏基础**——漏洞利用的总地基：先搞懂栈/堆在内存里怎么摆，后面溢出/UAF 全建立在此。

---

## SEC34 · 内存破坏基础

### 🎯 目标
学完能画出"一个程序运行时的内存地图"（代码/堆/栈/内核各在哪），并说清"栈从高地址往低长、堆从低往高"，理解"写越界会踩到邻居"——这是后续所有内存漏洞的地基。

### 📋 小白前置
- 方向1：C3（指针/地址）、C7（动态内存 malloc）、C4（数组/字符串）
- 方向3：ASM4（栈帧/返回地址——**核心**）、OS5（分页/虚拟内存）、OS7（物理内存/slab）、COMP4（内存层次）
- 本文件内：SEC28（调试看内存）

### 🟢 部分一·最浅层（生活比喻）
程序的内存像一栋**公寓楼**：顶楼是"代码间"（只读，放指令）；中间是"堆区"（自己申请的大储物间，从下往上堆）；楼下是"栈区"（临时便签板，函数调用时往上贴、返回时撕掉，从高往低贴）。每个函数有自己的"便签板(栈帧)"。若你便签写太长，越界划到隔壁函数的便签——就乱套了，这就是内存破坏。

### 🟡 部分二·动手层（逐字操作）
写个程序打印地址，直观看布局：
```c
// mem.c
#include <stdio.h>
#include <stdlib.h>
int g;                      // 全局->数据段
int main(){
    int local;             // 局部->栈
    int *heap = malloc(8); // ->堆
    printf("代码main=%p\n栈&local=%p\n堆heap=%p\n全局&g=%p\n",
           (void*)main, (void*)&local, (void*)heap, (void*)&g);
    free(heap);
    return 0;
}
```
编译：`gcc mem.c -o mem.exe`，运行看输出。你会看到：栈地址最大（高）、堆其次、全局/代码较低（具体布局因 ASLR 随机，SEC44）。

### 🔵 部分三·原理层（底层发生了什么）
- **虚拟地址空间**（OS6）：每个进程以为自己独占 0~很大 的地址；OS 用页表映射到底层物理内存。
- **栈(Stack)**：由 CPU 的 `RSP` 寄存器指向栈顶；函数调用时 `push` 参数/返回地址/`rbp`，分配局部变量（地址变小→向低增长）。返回时 `ret` 弹返回地址。
- **堆(Heap)**：`malloc` 向 OS 要大块，自己切成小块分给程序（分配器 OS16：ptmalloc 等）。
- **越界写**：数组下标算错，写到相邻变量/返回地址/堆块头——后续漏洞的根。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么栈向低、堆向高？为"中间留大片空闲"让二者都能生长，历史上 UNIX 这样设计沿用至今。顶级追问：线程有自己的栈（OS1），多个栈怎么摆？栈溢出为何特别危险（能改返回地址控制流程）？这直接引出 SEC35。

### 🔴 部分五·顶级视角
顶级研究员脑中有"精确内存模型"：看到 `buf[256]` 立刻想"它上方 8 字节是不是返回地址"。他们用 **GDB/x64dbg 看栈布局**定位溢出偏移。跨领域：和 OS 内存管理(OS5/OS6)、分配器(OS16)、CPU 寻址(COMP3)密不可分。

### 🟠 部分六·安全/合规种子
内存破坏知识**只用于理解原理、分析自己程序、CTF**。🚫 不拿溢出技术去打他人程序/服务。本技能是"防御地基"：懂攻击者怎么改写内存，才能写 `strncpy`/边界检查/开缓解(SEC44)。

### ✅ 验收
交：① `mem.exe` 四种地址输出截图；② 手绘内存地图（标代码/堆/栈方向）；③ 说明"越界写"踩到什么会出事。

### ⚠️ 常见坑
- 地址每次运行不同（ASLR，SEC44），别以为程序坏了；这是保护机制。
- 把"栈向低增长"说反：记住调用时 RSP 减小。
- 局部变量地址比参数高还是低？取决于栈帧布局，需实际看，别想当然。

### ➡️ 下一步
**SEC35 · 栈溢出**——最经典漏洞，改写返回地址让程序跳去你的代码（在自己程序上演示防护认知）。

---

## SEC35 · 栈溢出

### 🎯 目标
学完能写出一个**故意有栈溢出**的小程序，用调试器看到"返回地址被覆盖"，并理解"Shellcode 怎么落地"——全程在自己编译的程序上演示（白帽：为懂原理与检测）。

### 📋 小白前置
- 方向1：C4（数组/字符串，越界源头）、C3（指针）
- 方向3：ASM4（栈帧/返回地址——**必读**）、OS5（分页）
- 本文件内：SEC34（内存布局）、SEC28（调试）、SEC44（缓解，先知道有防护）

### 🟢 部分一·最浅层（生活比喻）
栈帧像一张"便签板"，最底下钉着"回家地址"（返回地址）。你写便签(局部数组)写太满，墨水流到下面，把"回家地址"涂成了别处——函数结束按错地址"回家"，走到不该去的地方。攻击者就是把这地址涂成"自己代码的位置"。

### 🟡 部分二·动手层（逐字操作）
**为教学自写漏洞程序（仅自己机器、自己编译）：**
```c
// vuln_stack.c  —— 故意无边界检查，用于理解原理
#include <stdio.h>
#include <string.h>
void win(){ printf("*** 你控制了流程! ***\n"); }
void vuln(char* s){
    char buf[16];
    strcpy(buf, s);     // 危险：不检查长度
}
int main(int argc, char**argv){
    if(argc>1) vuln(argv[1]);
    printf("正常结束\n");
    return 0;
}
```
编译（**关掉栈保护便于看原理**，仅教学用，真实程序要开）：`gcc -fno-stack-protector -o vuln_stack.exe vuln_stack.c`
调试看溢出：x64dbg 打开，给长参数 `AAAAAAAAAAAAAAAAAAAAAAAA` 让 buf 溢出覆盖返回地址。你会看到返回地址变 `0x4141414141414141`(`AAAA`)。这证明"能改写返回地址"。

### 🔵 部分三·原理层（底层发生了什么）
`buf[16]` 在栈帧低位；返回地址在栈帧高位（距离 buf 偏移 = 16 + 对齐填充，常见 24/32）。`strcpy` 不检查 `\0` 前长度，超长输入从 buf 一路写上去，先盖局部变量、再盖 `rbp`、最后盖**返回地址**。函数 `ret` 时 CPU 把被盖的地址装入 RIP → 跳去攻击者指定的地方。若那里是 shellcode(SEC43)，就执行它。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么这种漏洞历史上这么多？因为 C 语言**不自动边界检查**（C4 数组退化成指针），`strcpy`/`gets`/`sprintf` 等"老函数"信任程序员。C11(未定义行为)下越界是 UB。顶级追问：返回地址在哪、偏移怎么算精？用** cyclic pattern**(pattern `Aa0Aa1...`) 让调试器自动定位偏移——这是 pwn 基本功。

### 🔴 部分五·顶级视角
顶级 pwn 手：① 算精确偏移；② 布置 shellcode + 覆盖返回地址指向它（或跳 ROP SEC41）；③ 绕过 DEP/ASLR(SEC44)。他们写 **exploit 脚本(pwntools)** 自动化。跨领域：和编译器(CMP)、OS 内存(OS5)、CPU 寻址(COMP3)全通。

### 🟠 部分六·安全/合规种子
**极度重要**：本实验**仅限你自己编译、自己机器、教学理解**。🚫 绝不对任何他人程序/服务/网站使用栈溢出利用。🚫 不写可攻击他人的 exploit 传播。学它是为：写代码时主动用 `strncpy`/`snprintf`、开栈保护(SEC44)、做代码审计(SEC20)。讲利用原理 = 学会防护与检测。

### ✅ 验收
交：① `vuln_stack.exe` 在 x64dbg 下返回地址被 `AAAA` 覆盖的截图；② 说明 buf 到返回地址的偏移概念；③ 写一句"真实开发如何避免此漏洞"。

### ⚠️ 常见坑
- 忘了 `-fno-stack-protector`，默认 GCC 有 Canary，溢出会"崩溃拦截"而非改地址——教学时明确关掉，但写真程序要**开**。
- 偏移算错：用 cyclic pattern 而非硬猜。
- 误以为"看到覆盖=拿到 shell"：还需 shellcode(SEC43)+绕过 DEP，本节点到"覆盖"即可。

### ➡️ 下一步
**SEC36 · 堆溢出/堆风水**——堆上内存破坏，比栈更隐蔽，靠"块结构"布局（自己程序演示）。

---

## SEC36 · 堆溢出 / 堆风水

### 🎯 目标
学完能理解"堆块有头部元数据"，堆溢出是"写穿一个块盖到相邻块的头"，并说清"堆风水"= 精心申请/释放顺序以让攻击者块挨着目标块。

### 📋 小白前置
- 方向1：C7（malloc/free/堆）
- 方向3：OS16（内存分配器 ptmalloc/jemalloc——**核心**）、OS7（slab）、ASM4（栈帧对照）
- 本文件内：SEC34（内存布局）、SEC35（溢出基础）

### 🟢 部分一·最浅层（生活比喻）
堆像一排**带标签的储物柜**，每个柜子除了你放的东西，柜门内侧还有张"管理员纸条"（块头：大小/是否空闲/前后柜指针）。堆溢出 = 你往自己柜子塞太多，撑破隔板，把隔壁柜子的"管理员纸条"涂改了。堆风水 = 你故意先借/还某些柜子，让"目标柜"恰好挨着"你的柜"，方便篡改。

### 🟡 部分二·动手层（逐字操作）
自写演示（看堆块相邻）：
```c
// vuln_heap.c —— 教学：两个相邻堆块，前者溢出改后者大小
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main(){
    char *a = malloc(32);
    char *b = malloc(32);
    printf("a=%p b=%p 相差约 %ld\n", (void*)a, (void*)b, (long)(b-a));
    strcpy(a, "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"); // 超出32写穿到b的头
    printf("b 的内容被改: %s\n", b);
    free(a); free(b);
    return 0;
}
```
编译：`gcc -o vuln_heap.exe vuln_heap.c`，用 x64dbg 看 `a`、`b` 相邻、a 溢出写进 b 的元数据/数据。

### 🔵 部分三·原理层（底层发生了什么）
glibc 的 ptmalloc 块结构：`malloc` 返回给你的指针前面有 **chunk header**（size 字段+标志）。写入超过你申请的大小，会覆盖**下一个 chunk 的 size / fd / bk 指针**。释放时分配器读这些指针做合并——被篡改后可能让 `free` 把任意地址链入空闲链表，下次 `malloc` 返回**攻击者控制地址**（即"任意分配"）。这就是堆利用的杠杆点。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么分配器要维护"前后指针"？为了 O(1) 合并相邻空闲块（减少碎片）。但元数据可由数据越界改，是设计权衡的代价。顶级追问：`fastbin`/`tcache`(SEC42) 单链表更简单也更易被利用；不同分配器(jemalloc/tcmalloc)结构不同，利用手法各异。

### 🔴 部分五·顶级视角
顶级 pwn 手做"堆风水"：精确申请/释放序列，让目标对象(如函数指针)落在可控块旁，再用溢出改写它，实现"任意写/任意读"迈向代码执行。跨领域：和 OS 内存(OS16)、并发分配(OS3)相关。

### 🟠 部分六·安全/合规种子
堆溢出利用**仅自己程序、CTF**。🚫 不用于他人软件/服务。学它为了：用安全分配器/边界检查、理解为何 `malloc` 后需校验、做审计(SEC20)。本节点到"理解块头被改"即可，不延伸写完整攻击链于他人目标。

### ✅ 验收
交：① 运行 `vuln_heap.exe` 输出（b 被改）截图；② 文字说明"堆块头/元数据"是什么、溢出怎么盖到它；③ 解释堆风水一句话。

### ⚠️ 常见坑
- 不同编译器/分配器块布局不同，偏移需实际调，别背数字。
- 忘记 `free` 顺序导致 Doule Free 崩溃，教学分步来。
- 把堆溢出当栈溢出算固定偏移：堆地址随机(ASLR)+块对齐，更依赖布局。

### ➡️ 下一步
**SEC37 · 格式化字符串**——一个 `%s` 写错，竟能"任意读写"内存（自己程序演示）。

---

## SEC37 · 格式化字符串

### 🎯 目标
学完能理解"为什么 `printf(user_str)` 危险"：攻击者用 `%x`/`%n`  leak 栈内容甚至**写内存**，并在自己程序上演示打印栈值。

### 📋 小白前置
- 方向1：C2（格式/字节）、C4（字符串）
- 方向3：ASM4（栈帧——`%x` 读的是栈上参数）、OS5（内存）
- 本文件内：SEC34（栈）、SEC35（控制流）

### 🟢 部分一·最浅层（生活比喻）
`printf` 像"填空打印机"：`%d` 说"去取一个整数填这"，`%s` 说"去取一个地址、打印那的字符串"。正常它按你给的参数取。但如果你把"填空指令"直接写进用户文本（如用户输入 `%x %x %x`），打印机就会**按指令去栈上挨个掏格子**——你没给参数，它自己顺手拿栈里的东西填，泄露内存；`%n` 更狠："把已打印字数写回这个地址"= 改内存。

### 🟡 部分二·动手层（逐字操作）
教学自写（自己机器）：
```c
// fmt.c —— 故意危险：直接把用户输入当格式串
#include <stdio.h>
int main(int argc, char**argv){
    if(argc>1) printf(argv[1]);   // 危险！应 printf("%s", argv[1])
    printf("\n");
    return 0;
}
```
编译：`gcc -o fmt.exe fmt.c`
运行泄露栈：`fmt.exe "%p %p %p %p %p"` → 打印出栈上 5 个值（地址/数据）。这证明能"读栈"。`%n` 写内存更高级（教学点到"能写"即可，不必完整利用）。

### 🔵 部分三·原理层（底层发生了什么）
`printf` 用**变长参数**，按格式串里的 `%x/%s/%n` 个数，从栈上**按顺序**取参数。`%x` 取一个机器字打印；`%s` 取一个指针解引用打印字符串；`%n` 取一个指针，把**已输出字节数**写回该指针。当用户控制格式串，参数位置被攻击者排布——`%N$x` 可跳过 N 个直接读第 N 个栈槽；配合写目标地址到栈上，用 `%n` 写任意内存。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么 C 留这种坑？因为 `printf` 设计早于"不可信输入"场景，信任程序员传正确参数。现代编译器有 `-Wformat` 警告。顶级追问：64 位下参数在寄存器不在栈(`%x` 先读寄存器残留)，需用 `%N$p` 定位——这是 pwn 里"leak libc 基址"的常见起点（配合 SEC44 ASLR 绕过）。

### 🔴 部分五·顶级视角
顶级 pwn：用格式化字符串先 **leak**(绕过 ASLR 拿 libc 地址)，再 `%n` 改 GOT(SEC25/SEC49) 劫持函数，一步到位。它是"信息泄露+写原语"的完美教具。跨领域：和变参调用约定(ASM3)、libc 布局相关。

### 🟠 部分六·安全/合规种子
格式化字符串利用**仅自己程序、CTF**。🚫 不用于他人程序/服务。正确写法永远是 `printf("%s", user)`。学它为了：代码审计(SEC20)一眼识破、编译器 warning 当错误、理解"不可信输入"铁律。

### ✅ 验收
交：① 运行 `fmt.exe "%p %p %p"` 输出截图（看到栈值）；② 说明 `%x` 与 `%n` 区别；③ 写正确写法 `printf("%s", user)`。

### ⚠️ 常见坑
- 以为 `%p` 一定读栈：64 位前几个参数在寄存器，要 `%6$p` 之类跳到栈。
- 真用 `%n` 写会崩（写到只读/随机地址），教学知原理即可，别硬试他人场景。
- 忘记 `-Wformat` 警告：真实项目应 `-Werror=format` 挡住。

### ➡️ 下一步
**SEC38 · 释放后使用 UAF**——内存还了还留着钥匙，再去开就出事（自己程序演示）。

---

## SEC38 · 释放后使用 UAF

### 🎯 目标
学完能说清"UAF = free 之后又用了指针"，以及它为何能"类型混淆/任意读写"，并在自己程序上看 free 后内存被复用导致混乱。

### 📋 小白前置
- 方向1：C7（free/悬空指针）、C3（指针/野指针）
- 方向3：OS16（分配器复用 chunk）、OS7（slab 缓存）
- 本文件内：SEC36（堆块）、SEC34（内存）

### 🟢 部分一·最浅层（生活比喻）
你退了储物柜( free )，钥匙(指针)却没扔还留着。后来柜子被别人借走存了别的东西。你用旧钥匙开门，看到的是**别人的东西**——甚至你还能往里塞，污染别人的数据。如果"别人"恰好是个重要对象(如带函数指针)，你改它就能控制程序。

### 🟡 部分二·动手层（逐字操作）
教学自写：
```c
// uaf.c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
struct Obj { void (*fn)(void); char name[16]; };
void normal(){ printf("正常函数\n"); }
int main(){
    struct Obj* o = malloc(sizeof(struct Obj));
    o->fn = normal;
    free(o);                      // 释放
    char* other = malloc(20);     // 分配器把刚释放的块复用给 other
    strcpy(other, "EVIL_PAYLOAD");// 写到 o 原来的内存
    o->fn();                      // UAF：用已释放的 o，此时 fn 字段已被 other 数据污染
    return 0;
}
```
编译：`gcc -o uaf.exe uaf.c`。用 x64dbg 看 `o` 释放后、other 复用同一地址、调用 `o->fn()` 时已不是 normal——演示"悬空指针读取被复用内存"。

### 🔵 部分三·原理层（底层发生了什么）
`free(o)` 后，o 指向的 chunk 回到分配器空闲链表；下次 `malloc(20)` 可能**原样返回同一地址**（空闲链表头）。于是 `other` 与 `o` 重叠。访问 `o->fn` 实际读到 `other` 写入的字节——若攻击者控制 `other` 内容，就能把 `fn` 改成恶意地址（类型混淆：把数据当函数指针）。这就是 UAF→代码执行的链路。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么危险？因为 C 不自动把 `free` 后的指针置 NULL（也不强制）。很多大型程序(C++/浏览器)因生命周期复杂产生 UAF——这是**现实最高危漏洞类之一**（如浏览器远程代码执行）。顶级追问：分配器用 `tcache/fastbin`(SEC42) 让复用更"可预测"，攻击者可精确布置类型实现利用。

### 🔴 部分五·顶级视角
顶级研究员在浏览器/CTF 中靠 UAF 做"类型混淆→任意读写→沙箱逃逸(SEC45)"。防御侧：Rust(方向1 RS2 所有权)用编译期杜绝 UAF——这是"语言级根治"范例，顶级工程会推崇内存安全语言。

### 🟠 部分六·安全/合规种子
UAF 利用**仅自己程序、CTF**。🚫 不用于他人软件/服务。学它为了：懂得 free 后置 NULL、用智能指针(CPP5)/Rust 所有权防 UAF、做审计。理解"为何内存安全语言是未来"。

### ✅ 验收
交：① `uaf.exe` 在调试下 `o` 与 `other` 同地址的截图/记录；② 说明"类型混淆"一句话；③ 写一条防御建议。

### ⚠️ 常见坑
- 复用地址不一定成功（分配器可能给新块），需多试或固定大小让 fastbin 命中；教学用同大小易命中。
- UAF 后程序可能"碰巧正常"——因为没改关键字段，要制造可见异常才看得清。
- 把 UAF 当"立刻崩溃"：有时静默错误，更危险。

### ➡️ 下一步
**SEC39 · 整数溢出**——数字算着算着"回绕"成负数/超大，导致分配太小被写穿（自己程序演示）。

---

## SEC39 · 整数溢出

### 🎯 目标
学完能理解"整数有宽度，运算会回绕/截断"，并在自己程序上演示 `size = a*b` 溢出导致 `malloc` 太小、随后堆溢出。

### 📋 小白前置
- 方向1：C2（整数宽度/表示）、C3（指针）、PY3（补码概念）
- 方向3：COMP2（CPU 运算/标志位）、ASM2（算术/溢出标志）
- 本文件内：SEC34（内存）、SEC36（堆溢出后果）

### 🟢 部分一·最浅层（生活比喻）
里程表只有 4 位，跑到 9999 再加 1 变 0000（回绕）。整数同理：32 位无符号最大约 42 亿，超出归零。若程序用"用户给的数 × 元素大小"算要分配多大，用户给超大数让乘积回绕成很小值——于是分配太小，随后往里写就**写穿**了（接 SEC36）。

### 🟡 部分二·动手层（逐字操作）
教学自写（Windows 64 位下演示 32 位回绕）：
```c
// intovf.c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main(){
    unsigned int n = 0x10000000;   // 约 2.68 亿
    unsigned int size = n * 4;     // 期望 10 亿，实际回绕
    printf("要分配 size=%u (0x%x)\n", size, size);  // 看是否变小
    char* p = malloc(size);
    if(p){ strcpy(p, "x"); }        // size 太小时这里会越界
    free(p);
    return 0;
}
```
编译：`gcc -o intovf.exe intovf.c`，观察 `size` 远小于预期（回绕成小值），使后续写入越界。

### 🔵 部分三·原理层（底层发生了什么）
无符号整数运算按模 2³²(或 2⁶⁴)回绕（C 标准定义）。`n*4` 当 `n` 大时高位丢弃，结果可能很小甚至 0。`malloc(size)` 拿到过小缓冲；之后按"原以为的大尺寸"写入→堆溢出(SEC36)。另一种：有符号整数溢出是**未定义行为**(C11)，编译器可能优化掉你的检查，更隐蔽。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么语言不报错？因为整数回绕在"性能/历史"权衡下被允许（底层 CPU 就是这样）。顶级追问：怎么安全算？用 **宽类型临时变量 + 检查上限**（`if(n > MAX/sizeof) err`）或 `calloc`(乘法带溢出检查)、或语言层面(BigInt/Rust `checked_mul`)。C 标准库也加 `reallocarray` 防此坑。

### 🔴 部分五·顶级视角
顶级研究员审计时**专门找"乘法分配"**（图像处理宽×高×通道、网络包 len×count）作为溢出高发点。他们用静态分析(SEC20)标注所有 `malloc(a*b)`。跨领域：和补码(PY3)、CPU 标志(COMP2/ASM2)、语言安全(RS2)相关。

### 🟠 部分六·安全/合规种子
整数溢出利用**仅自己程序、CTF**。🚫 不用于他人软件/服务。学它为了：写代码时检查乘积上限、用安全分配函数、审计高危乘法。

### ✅ 验收
交：① 运行 `intovf.exe` 看 size 回绕的输出截图；② 说明"无符号回绕 vs 有符号 UB"区别；③ 写一条防溢出写法。

### ⚠️ 常见坑
- 64 位下 `int*int` 也常是 32 位运算回绕，别以为"64 位系统就安全"。
- 用 `%u` 打印无符号，用 `%d` 会显示负数误导。
- 有符号溢出编译器可"优化掉"检查，需 `-fwrapv` 或改无符号+检查。

### ➡️ 下一步
**SEC40 · 竞态条件**——时间差里出漏洞（TOCTOU），多线程/文件场景（自己程序演示）。

---

## SEC40 · 竞态条件

### 🎯 目标
学完能理解"检查时机(check)和使用时机(use)之间，状态被另一线程改了"= TOCTOU，并在自己程序上看两个线程抢同一变量导致结果错乱。

### 📋 小白前置
- 方向1：C10（函数指针）、CPP7（并发/原子）、RS8（并发 Send/Sync）
- 方向3：OS3（同步原语 锁/原子——**核心**）、OS2（调度）、COMP7（多核/缓存一致性 MESI）
- 本文件内：SEC34（内存）、无更前依赖

### 🟢 部分一·最浅层（生活比喻）
你和室友约好"如果冰箱空了就买奶"。你看了眼：空。但室友同时也在看、也去买——结果买两盒。更糟的：你"看空→决定改文件"之间，室友把冰箱塞满了，你还按"空"处理→逻辑错。检查(看)和使用(改)不是一瞬间，中间被插队，就出竞态。

### 🟡 部分二·动手层（逐字操作）
教学自写（两线程抢计数器）：
```c
// race.c
#include <stdio.h>
#include <pthread.h>
int counter = 0;
void* inc(void* a){ for(int i=0;i<100000;i++) counter++; return NULL; }
int main(){
    pthread_t t1,t2;
    pthread_create(&t1,NULL,inc,NULL);
    pthread_create(&t2,NULL,inc,NULL);
    pthread_join(t1,NULL); pthread_join(t2,NULL);
    printf("期望 200000, 实际 %d\n", counter);  // 常小于 200000
    return 0;
}
```
编译：`gcc -pthread race.c -o race.exe`，多次运行常看到 `counter` < 200000——因为 `counter++` 非原子(读-改-写被穿插)。

### 🔵 部分三·原理层（底层发生了什么）
`counter++` 在汇编是 3 步：读内存到寄存器→+1→写回。两线程在多核上可能同时读旧值，都写回旧+1，丢失一次更新(丢失更新)。OS 调度(OS2)随时切换；多核下即使单核也可能因缓存一致性(CMP7 MESI)产生可见性问题。**TOCTOU**（Time-of-check to time-of-use）：如先 `access()` 文件权限再 `open()`，中间文件被替换成符号链接→绕过权限检查（经典漏洞）。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么难修？因为竞态**偶发**（看运气/时序），难复现难调试(SEC28 断点会改变时序反而掩盖)。顶级追问：原子操作(OS3 `atomic`)如何靠 CPU 的 `LOCK` 前缀保证"读改写一次性"？锁(互斥)如何让临界区串行？这引出"并发安全"根基。

### 🔴 部分五·顶级视角
顶级研究员找竞态类漏洞（如提权中的 TOCTOU、Web 并发逻辑 SEC18）。他们用**线程消毒器(ThreadSanitizer)**自动检测。跨领域：和 OS 调度(OS2)、并发模型(OTH7/Actor)、数据库事务隔离(DB6 MVCC)同源——"隔离"思想通用。

### 🟠 部分六·安全/合规种子
竞态利用**仅自己程序、授权靶场**。🚫 不用于他人系统抢权限。学它为了：用锁/原子、写并发安全代码、理解 TOCTOU 风险点(如文件权限检查)。

### ✅ 验收
交：① 运行 `race.exe` 多次看 `counter`≠200000 的截图；② 说明 `counter++` 三步为何丢更新；③ 举一个 TOCTOU 文件场景。

### ⚠️ 常见坑
- 有时恰好对（竞态偶发），要多跑几次才看到异常，别以为程序对。
- 单核也可能丢更新（抢占），不只多核。
- 加锁位置错(锁太小)仍竞态，需覆盖整个临界区。

### ➡️ 下一步
**SEC41 · ROP**——没有可写可执行内存？那就"借"程序里已有代码片段拼出攻击（自己程序/ret2win 演示）。

---

## SEC41 · ROP（返回导向编程）

### 🎯 目标
学完能理解"ROP = 把已有合法代码片段(gadget)用返回地址串成链，绕过 DEP"，并在自己程序上找到一个 `pop rdi; ret` 之类 gadget 概念。

### 📋 小白前置
- 方向1：C3（指针/返回地址）
- 方向3：ASM3（调用约定/ret）、ASM4（栈帧/返回地址）、COMP3（指令集）
- 本文件内：SEC35（栈溢出改返回地址——ROP 基础）、SEC44（DEP 为何需要 ROP）、SEC43（shellcode 备选）

### 🟢 部分一·最浅层（生活比喻）
DEP(SEC44) 说"数据区不能当代码跑"。那攻击者不写自己的代码，改去"借"程序里已有的小片段（gadget，如"把某数放进寄存器然后返回"），用栈上一连串"返回地址"把多个片段串起来——像用现成乐高积木拼出想要的动作，不自己造新积木(代码)，从而绕过"不能执行新代码"的限制。

### 🟡 部分二·动手层（逐字操作）
教学自写含 gadget 的程序（ret2win 思路）：
```c
// rop.c
#include <stdio.h>
void win(){ printf("*** ROP 跳到 win! ***\n"); }
void vuln(char* s){ char buf[16]; __builtin_memcpy(buf,s,40); } // 故意越界
int main(int c,char**v){ if(c>1) vuln(v[1]); return 0; }
```
编译：`gcc -fno-stack-protector -o rop.exe rop.c`
找 gadget：用 Rizin 搜 `pop; ret`：
```bash
rizin -q -c '=/x 5fc3' rop.exe     # 5f=pop rdi, c3=ret (示意)
# 或用罗列所有 ret 周边： /R  (Rzin 的 gadget 搜索)
```
概念：覆盖返回地址为 `win` 地址(最简单 ret2win)，或更复杂 gadget 链。调试器里看栈上布置的返回地址序列。

### 🔵 部分三·原理层（底层发生了什么）
程序二进制的 `.text` 里散布无数小指令序列，很多以 `ret`(c3) 结尾——这些叫 gadget。栈溢出控制返回地址后，让返回地址 = gadget1，gadget1 末尾 `ret` 又从栈取下一地址 = gadget2……如此"借程序自己代码"完成：设置参数(rdi/rsi)→调用系统函数(如 `system`)。因执行的都是**原文件里已有的、合法可执行的代码**，DEP 不拦。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么 ROP 能成？因为 x86 指令不等长、任意字节都可能是某指令中间——"指令复用"丰富（这点 RISC 架构 gadget 更少更难）。顶级追问：CFG(SEC44) 限制间接跳转目标，能否挡 ROP？能挡大部分但 gadget 伪装仍可能；于是衍生 "JIT ROP/假帧" 对抗——攻防持续升级。

### 🔴 部分五·顶级视角
顶级 pwn 手写 **ROP 链生成器(pwntools ROP)**：自动搜 gadget、算偏移、拼链调用 `open/read/write` 读 flag。跨领域：和编译器代码布局(CMP8)、CPU 指令集(COMP3)、ASLR(SEC44)全通。

### 🟠 部分六·安全/合规种子
ROP **仅自己程序、CTF**。🚫 不用于他人程序/服务。学它为了：理解 DEP 为何不够、需要 CFG/ASLR 组合、做漏洞检测。白帽视角：ROP 链是"攻击者的拼图"，防御者学会识别其模式(如大量 ret 链)做 EDR 检测。

### ✅ 验收
交：① Rizin 搜到的某个 gadget 截图（如 `pop; ret`）；② 文字说明 ROP 如何绕过 DEP；③ 说明 ret2win 最简单形式。

### ⚠️ 常见坑
- gadget 搜索语法因工具而异，`/R` 在 Rizin 是通用 ROP 搜索；命令不对就查 `rizin -h`。
- 64 位参数在寄存器(rdi/rsi)不在栈，ROP 链要先 `pop rdi; ret` 设参，比 32 位复杂。
- 偏移算错整条链乱掉，务必用 cyclic pattern 测。

### ➡️ 下一步
**SEC42 · 堆利用**——tcache/fastbin 等分配器特性带来的高级堆攻击（自己程序概念+演示）。

---

## SEC42 · 堆利用（tcache / fastbin）

### 🎯 目标
学完能理解 glibc 的 `tcache`（线程本地缓存）单链表结构，以及"double free / tcache poisoning"为何能让 `malloc` 返回**任意地址**（自己程序演示 tcache poisoning 概念）。

### 📋 小白前置
- 方向1：C7（malloc/free）
- 方向3：OS16（分配器 ptmalloc——**核心**）、OS3（并发/线程本地）
- 本文件内：SEC36（堆块头）、SEC38（UAF 基础）、SEC39（整数溢出辅助）

### 🟢 部分一·最浅层（生活比喻）
tcache 像你手边"刚退的柜子临时架"：为了快，free 的小柜子先放这（单链表，每个柜门写着"下一个空柜在哪"）。攻击者在"管理员纸条"(fd 指针)上做手脚，把链表指到**任意柜子**（比如存有密码的柜子）——下次 malloc 就"误发"那个柜子给你，你就能改里面的东西。double free 就是"同一柜子退两次"，让它进链表两次，被发两次，便于篡改。

### 🟡 部分二·动手层（逐字操作）
教学自写（tcache poisoning 概念，自己机器）：
```c
// tcache_poi.c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main(){
    unsigned long long target = 0;            // 我们要改的"任意地址"
    char *a = malloc(24);
    free(a);
    // 教学：这里如果 a 的 fd 被改为 &target (实际需 UAF 写), 下次 malloc 给 &target
    printf("target 地址=%p\n", (void*)&target);
    // 真实利用需先 UAF 改 a->fd=&target 再 malloc 两次拿到 target
    return 0;
}
```
编译：`gcc -o tcache_poi.exe tcache_poi.c`。完整利用需配合 UAF(SEC38) 改写 fd，调试器里演示"第二次 malloc 返回 target 地址"。

### 🔵 部分三·原理层（底层发生了什么）
glibc 2.26+ 的 tcache 是**每线程**单链表数组(按大小分桶)。free 时 chunk 头 `fd` 指向当前同尺寸空闲块，头插法入桶。分配时从桶头取。若攻击者借 UAF/溢出改写某空闲 chunk 的 `fd` 为 `&target`，下次 malloc 会返回 `&target`（因为分配器信任 fd）。于是 `malloc` 返回"任意可写地址"→ 后续写该地址=任意写原语。double free 让同一块在链表两次，扩大控制。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么加 tcache？为多线程性能(避免全局锁)。但"信任 fd"牺牲安全换速度——这是性能/安全经典权衡。顶级追问：tcache 有"key"防 double free 检测(2.29+)，但 key 也可被绕过；fastbin(老机制)结构类似更安全检查更少。不同 libc 版本利用手法年年变，是 pwn 持续战场。

### 🔴 部分五·顶级视角
顶级 pwn 手把堆利用当"内存操纵艺术"：组合 UAF+溢出+double free 实现"任意读写→改 __free_hook(历史)/或改 TLS 指针→执行"。跨领域：与 OS 分配器(OS16)、并发(OS3)深度耦合。

### 🟠 部分六·安全/合规种子
堆利用**仅自己程序、CTF**。🚫 不用于他人软件/服务。学它为了：理解为何需安全分配器、Rust/Go 内存安全语言根治此类问题、做 libc 漏洞响应。

### ✅ 验收
交：① 说明 tcache 单链表 + fd 指针作用；② 文字描述 tcache poisoning 如何让 malloc 返任意地址；③ 一条防御建议（如升级 libc/用内存安全语言）。

### ⚠️ 常见坑
- libc 版本差异极大（2.23/2.27/2.31/2.35 利用法不同），初学者固定一个版本练。
- tcache key 检测让"裸 double free"直接 abort，需先绕过（教学中点明即可）。
- 把堆地址当固定：ASLR 下需先 leak(SEC37) 拿堆/libc 基址。

### ➡️ 下一步
**SEC43 · Shellcode 编写**——自己写一段" syscall 调用"机器码，让程序打印/退出（仅自己进程）。

---

## SEC43 · Shellcode 编写

### 🎯 目标
学完能用 nasm 写一段最小 x64 shellcode（调用 `write` 打印 + `exit`），并理解"位置无关/去空字节"为何重要——全程在自己程序里执行。

### 📋 小白前置
- 方向1：OTH3（汇编 x86-64——**必读**）、C12（inline 汇编）
- 方向3：ASM1-ASM4（寄存器/系统调用）、OS10（系统调用/中断）、COMP3（指令集）
- 本文件内：SEC35（栈溢出想跳去 shellcode）、SEC44（DEP 限制执行）

### 🟢 部分一·最浅层（生活比喻）
Shellcode 就是"攻击者自己写的超短程序"，直接是机器码（不用编译好的 exe）。像你写张小便签，上面写"去厨房拿水(系统调用)"，塞进被控程序的流程里让它照做。但便签不能有"空格"(空字节 0x00)，否则程序读到空格以为"句子结束"就停了——所以要写"无空格"版。

### 🟡 部分二·动手层（逐字操作）
写 shellcode（Linux 风格 syscall，教学用 nasm 编；Windows 上用 WSL 或仅理解）：
```nasm
; hi.asm  —— 打印 "Hi" 并退出 (x64 Linux syscall)
bits 64
mov rax, 1      ; sys_write
mov rdi, 1      ; stdout
mov rsi, msg
mov rdx, 2      ; 长度
syscall
mov rax, 60     ; sys_exit
xor rdi, rdi
syscall
msg: db "Hi"
```
MSYS2 下若用 nasm：`nasm -f bin hi.asm -o hi.bin`（生成纯机器码）。可用 C 程序把它当字节数组执行（需关 DEP/用可写可执行页，仅教学）：
```c
// run_sc.c (教学) 把 hi.bin 字节放入可执行缓冲执行
```
> 护手：完整"执行 shellcode"步骤多且有 DEP 限制，初学理解"shellcode=位置无关的 syscall 机器码 + 去空字节"即可，可用 `objdump -d` 看编码。

### 🔵 部分三·原理层（底层发生了什么）
Shellcode 直接发 **系统调用(syscall 指令)**，不经 libc：把功能号(rax)与参数(rdi/rsi/rdx…)入寄存器，`syscall` 陷入内核(OS10)。它必须**位置无关(PIC)**——不能含绝对地址（因为加载位置随机），所以字符串/跳转都用相对寻址。`0x00` 是 C 字符串结束符，若 shellcode 含 0x00，`strcpy` 类复制会截断，故要编码避免（如 `xor reg,reg` 代替 `mov reg,0`）。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么 shellcode 难写？要在"极小空间 + 无库 + 无绝对地址 + 无空字节"约束下做事。顶级追问：Windows 上要用 `VirtualAlloc`+`CreateThread` 等 API(走 PEB 找 kernel32，Peb64)，比 Linux syscall 复杂；于是有**自动生成框架(msfvenom)**。但自动 shellcode 特征明显，EDR 易检——攻防博弈。

### 🔴 部分五·顶级视角
顶级研究员写**自定义无特征 shellcode**（加密+自解密，呼应 SEC30/31）绕过 AV；也写**检测规则**识别常见 shellcode 字节序。跨领域：与汇编(ASM)、OS 系统调用(OS10)、加壳(SEC31)同源。

### 🟠 部分六·安全/合规种子
Shellcode **仅自己进程、CTF**。🚫 不写针对他人系统的恶意 shellcode、不传播。学它为了：理解"为什么 DEP(SEC44) 要禁止数据执行"、EDR 为何扫 shellcode 特征、写检测规则。白帽视角为主。

### ✅ 验收
交：① `hi.asm` 源码 + `objdump -d` 看到机器码（说明无 0x00）；② 文字解释位置无关与去空字节；③ 说明 syscall 怎么陷入内核。

### ⚠️ 常见坑
- Windows 直接执行 shellcode 被 DEP 拦，需 `VirtualProtect` 改页属性（教学知原理，真实程序要开 DEP 防御）。
- nasm `bits 64` 不能漏，否则编出 32 位码。
- 空字节常来自 `mov rax, 0`，改用 `xor rax,rax`。

### ➡️ 下一步
**SEC44 · 缓解措施**——DEP/ASLR/CFG/Canary 怎么把前面漏洞全挡一层（重点防御章）。

---

## SEC44 · 缓解措施（DEP / ASLR / CFG / Canary）

### 🎯 目标
学完能说清四种主流缓解各防什么：Canary 防栈溢出改返回地址、DEP 防数据执行、ASLR 防地址预测、CFG 限间接调用——并理解"为什么仍需写安全代码"。

### 📋 小白前置
- 方向1：C14（防御性编程）、C7（malloc）
- 方向3：OS5（分页/页表权限——DEP 基础）、OS6（虚拟内存随机化）、COMP3（CPU 特性)
- 本文件内：SEC35（栈溢出→Canary/DEP）、SEC41（ROP→CFG/ASLR）、SEC34（布局→ASLR）

### 🟢 部分一·最浅层（生活比喻）
缓解措施像给公寓加**安保**：① Canary=门口放盆花，花倒了说明有人翻窗(栈溢出)立刻报警；② DEP=规定"储物间(数据)不能当舞台(执行代码)"；③ ASLR=每次搬家随机换门牌号，坏人记不住地址；④ CFG=只有"白名单名单"上的门能进，其它门全锁。四道锁层层加。

### 🟡 部分二·动手层（逐字操作）
对比编译看 Canary：
```bash
gcc -o vuln_nocanary.exe vuln_stack.c -fno-stack-protector   # SEC35 的程序
gcc -o vuln_canary.exe   vuln_stack.c                        # 默认开 Canary
```
用 x64dbg 看 `vuln_canary.exe` 的栈帧，会发现 `buf` 和返回地址之间多了一个 **stack canary（安全检查值）**；溢出时程序检测到 canary 被改直接 abort。看 ASLR：多次运行 `mem.exe`(SEC34) 地址都不同，即 ASLR 生效。

### 🔵 部分三·原理层（底层发生了什么）
- **Canary(栈保护)**：函数开头从特殊段取随机值放栈上(返回地址前)，返回前比较——被溢出改了就 `__stack_chk_fail`。靠编译器插桩(CMP7)。
- **DEP/NX**：页表项(OS5)标记内存"不可执行"，数据页(堆/栈)不能跑代码→挡 shellcode(SEC43)。
- **ASLR**：加载时随机化 exe/dll/堆/栈基址(OS6)，攻击者算不准地址→挡 ROP(需 leak)、挡 shellcode 定位。
- **CFG(/CET)**：编译器为间接调用(函数指针/vtable)插校验，目标不在合法表就拦→挡大多 ROP/gadget。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么还有漏洞？因为缓解是"提高成本"非"根除"。Canary 防不了堆(SEC36)；DEP 催生 ROP(SEC41)；ASLR 需 leak(SEC37)绕过；CFG 有绕过研究。顶级追问：**CET/Shadow Stack**(硬件)把返回地址存独立只读栈，直接废掉多数 ROP——是下一代方向。历史：这些缓解由微软/Google/Intel 持续推动。

### 🔴 部分五·顶级视角
顶级工程师**防御侧**会：开全缓解(-fstack-protector-strong / DEP / ASLR / CFG / RELRO)、用内存安全语言(Rust)、做编译加固。他们理解"缓解是纵深防御一层，代码正确才是根"。跨领域：与 CPU 特性(COMP3)、OS 内存(OS5/6)、编译器(CMP7/8)全通。

### 🟠 部分六·安全/合规种子
本章是**防御核心**：学所有利用(SEC35-43)最终被缓解挡，目的是让你写**默认安全**的代码。🚫 不研究"怎么逐个绕过缓解去打他人"；可理解绕过原理用于"评估自己防御是否够"，但实践只在 CTF/自己程序。

### ✅ 验收
交：① 对比 `vuln_nocanary` 与 `vuln_canary` 栈帧差异截图（能看到 canary）；② 表格列出四缓解各防哪种漏洞；③ 写一句"缓解不是万能，正确代码才是根"。

### ⚠️ 常见坑
- 以为开 Canary 就安全：堆溢出/UAF 它不管。
- 关掉缓解做教学后，**真实项目务必开回去**（别把教学设置带进产品）。
- ASLR 下地址每次变是正常的，不是程序坏。

### ➡️ 下一步
**SEC45 · 沙箱逃逸**——程序被关在"沙箱"里，怎么理解它、又怎么（理论）逃逸（仅分析原理，自己写沙箱练）。

---

## SEC45 · 沙箱逃逸（seccomp / 命名空间）

### 🎯 目标
学完能理解"沙箱=限制程序能做的系统调用/资源"，并说清 seccomp(限制 syscall) 与 namespace(隔离视图) 是什么；用自己程序演示"装 seccomp 后某 syscall 被拦"（Linux，WSL 可用）。

### 📋 小白前置
- 方向3：OS15（容器原理 namespace/cgroup/seccomp——**核心**）、OS10（系统调用/陷阱）、OS5（虚拟内存）
- 方向1：OTH7（并发模型，容器相关）
- 本文件内：SEC43（syscall 概念）、SEC44（缓解）、SEC34（内存）

### 🟢 部分一·最浅层（生活比喻）
沙箱像给小孩玩的"围栏游乐场"：里面能跑能跳(正常计算)，但不能翻墙出小区(不能访问敏感文件/网络)。seccomp 是"禁止按某些开关"(禁特定系统调用)；namespace 是"给他一副 VR 眼镜，让他以为整世界就这房间"(进程看到独立 PID/网络/挂载)。逃逸 = 找到围栏破洞溜出去。

### 🟡 部分二·动手层（逐字操作）
概念演示（WSL/Linux 下用 seccomp 拦 `fork`）：
```c
// sandbox.c (Linux) 用 seccomp 禁止 fork，自己进程演示"被拦"
#include <seccomp.h>
#include <unistd.h>
#include <sys/types.h>
#include <stdio.h>
int main(){
    scmp_filter_ctx ctx = seccomp_init(SCMP_ACT_ALLOW);
    seccomp_rule_add(ctx, SCMP_ACT_KILL, SCMP_SYS(fork), 0); // 禁 fork
    seccomp_load(ctx);
    if(fork()==0){ printf("child\n"); }   // 这里会被 seccomp 杀掉
    else { printf("parent\n"); }
    return 0;
}
```
（需 `libseccomp-dev`，WSL 可编译体验"fork 被拦"。）Windows 上可改用**Windows 沙箱/容器(OS15)**概念理解。

### 🔵 部分三·原理层（底层发生了什么）
- **seccomp**：内核机制，给进程挂一个 BPF 过滤器，每次 `syscall` 前检查是否允许，禁用的直接杀/报错（SEC43 的 syscall 被拦）。
- **namespace**：让进程看到独立的 PID/网络/挂载/用户视图（OS15），以为自己是唯一进程。
- **cgroup**：限制 CPU/内存用量。
- 组合成容器(OPS19)。逃逸指：利用内核漏洞(SEC46)或配置错误跳出隔离——**白帽只分析原理**。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么沙箱必要？因为浏览器/容器要跑"不完全可信代码"(网页 JS、第三方容器)，必须隔离。顶级追问：seccomp 默认 ALLOW 还是 DENY 策略？安全应"默认拒绝最小集"；现代 Chrome 用 seccomp + 命名空间做多进程沙箱。历史上沙箱逃逸常因内核 bug(SEC46)。

### 🔴 部分五·顶级视角
顶级研究员分两派：攻击者找逃逸(授权渗透)、防御者设计更强沙箱(浏览器/云原生)。理解沙箱=理解 OS 隔离边界(OS15)。跨领域：与容器(OPS19/OPS21)、云安全(OPS18)、浏览器(OS15)相通。

### 🟠 部分六·安全/合规种子
沙箱逃逸利用**仅理论分析 + 自己进程沙箱实验**；🚫 不对他人/云环境做逃逸。学它为了：懂容器安全(OPS26)、写更隔离的程序、做授权渗透时评估沙箱强度。本文件"分析不实践"对他人系统。

### ✅ 验收
交：① 说明 seccomp 与 namespace 各管什么；②（WSL 有则）`sandbox.c` 被拦截图，否则文字描述预期；③ 写一条容器安全建议。

### ⚠️ 常见坑
- Windows 原生无 seccomp，需用 WSL/容器；别在原生 cmd 硬编。
- seccomp 规则顺序/默认动作设错会"全拦"导致程序起不来。
- 把 namespace 当"真隔离"：配置错仍可能逃逸，需配合 seccomp/cgroup。

### ➡️ 下一步
**SEC46 · 内核漏洞入门**——漏洞在"内核"里，提权从普通用户变系统最高权限（仅原理+自己内核模块概念，不练攻击他人）。

---

## SEC46 · 内核漏洞入门（提权 / 越界）

### 🎯 目标
学完能理解"内核漏洞 = 内核代码里的越界/UAF/竞态"，后果是"普通用户提权成 root/SYSTEM"，并说清用户态与内核态边界(SEC44/OS10)为何关键。**仅原理+自己 LKM 读代码，不练攻击他人系统**。

### 📋 小白前置
- 方向3：OS1（进程/线程）、OS10（中断与系统调用——**核心**）、OS13（引导）、OS14（内核模块 LKM）、COMP5（特权级/ rings）
- 本文件内：SEC34（内存）、SEC38（UAF）、SEC40（竞态）、SEC45（沙箱）

### 🟢 部分一·最浅层（生活比喻）
电脑分"平民区"(用户态)和"皇宫"(内核态)。普通程序在平民区，想干敏感事(碰硬件/管内存)要"递折子"(系统调用)让皇宫代办。若皇宫自己的仆人(内核代码)出错(越界/用已释放内存)，坏人就能借机混进皇宫当皇帝=提权。内核漏洞比用户态更狠，因为内核管一切。

### 🟡 部分二·动手层（逐字操作）
概念演示：读一个**自己写的内核模块骨架**（仅展示结构，不在本机加载危险模块）：
```c
// demo_lkm.c 仅教学结构，不加载
#include <linux/module.h>
static int __init hi_init(void){ printk("hello kernel\n"); return 0; }
static void __exit hi_exit(void){ printk("bye\n"); }
module_init(hi_init); module_exit(hi_exit);
MODULE_LICENSE("GPL");
```
理解：内核模块运行在**内核态**(ring 0)，拥有全部权限(OS14)。漏洞若在此层，影响全局。仅阅读，不提倡加载不明/实验性模块到真实机器。

### 🔵 部分三·原理层（底层发生了什么）
CPU 有特权级（ring 0=内核，ring 3=用户）。系统调用(OS10)经 `syscall` 指令从 ring3 切到 ring0，内核代执行。内核里若有**越界写/UAF/竞态**(SEC35/38/40)，攻击者可改内核数据结构(如进程凭据 `cred`)让自己变 root。因为内核共享所有进程地址空间，一错全崩(蓝屏/panic)。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么内核漏洞难防？内核代码庞大、驱动来自多方(OS14)、历史 C 写就无内存安全。顶级追问：内核也有 KASLR/KPTI 缓解；现代方向是用 **Rust 写安全模块(如 Rust-for-Linux)** 根除类。历史上 Dirty COW(CVE-2016-5195) 是经典竞态提权，教学案例。

### 🔴 部分五·顶级视角
顶级内核研究员分：防御(写 fuzz 找内核 bug SEC47、做 KASLR)、攻击(授权红队找提权)。理解内核=理解 OS 全栈(OS1-17)。跨领域：与 CPU 特权(COMP5)、内存(OS5/6)、驱动(OS14)同源。

### 🟠 部分六·安全/合规种子
内核漏洞利用**仅原理学习 + 自己模块代码阅读**；🚫 绝不对真实系统/他人机器加载攻击性内核模块或提权利用。学它为了：懂为何要及时打补丁、理解 KASLR/KPTI、做授权安全评估时识别内核风险。本文件明确"分析不实践"对他人。

### ✅ 验收
交：① 说明 ring0/ring3 区别；② 文字解释"内核 UAF 为何能提权"；③ 写一条"为何要及时更新系统补丁"。

### ⚠️ 常见坑
- 误以为本机可随便加载实验内核模块：可能蓝屏，教学只读代码不加载。
- 把用户态利用(SEC35-43)直接当内核利用：内核无 libc、地址空间不同，手法差异大。
- KASLR 与用户态 ASLR 是两套，别混淆。

### ➡️ 下一步
**SEC47 · Fuzzing**——自动丢海量随机/变异输入，撞出崩溃找漏洞（自己程序练 AFL/libFuzzer）。

---

## SEC47 · Fuzzing（AFL / libFuzzer）

### 🎯 目标
学完能理解"模糊测试 = 自动喂变异输入、监控崩溃"找漏洞，并用 libFuzzer 对自己的解析函数跑出崩溃（自己程序）。

### 📋 小白前置
- 方向1：C7（内存/崩溃）、PY21（单测，fuzz 像自动测试）
- 方向3：OS5（内存，崩溃=越界）、CMP（编译器插桩）
- 本文件内：SEC35-39（各类内存漏洞都是 fuzz 目标）、SEC32（符号执行互补）

### 🟢 部分一·最浅层（生活比喻）
Fuzzing 像"猴子乱敲键盘测试 App"：你写个机器人，不停给程序喂**各种奇怪输入**(超长、乱码、半截)，同时盯着它"有没有崩溃/卡死"。哪次崩了，那个输入就是能触发漏洞的"钥匙"。比人工找快千万倍。

### 🟡 部分二·动手层（逐字操作）
教学自写 fuzz 目标（解析函数有隐藏 bug）：
```c
// fuzz_target.cpp
#include <stdint.h>
#include <stddef.h>
extern "C" int LLVMFuzzerTestOneInput(const uint8_t* d, size_t n){
    if(n>=4 && d[0]=='F' && d[1]=='U' && d[2]=='Z' && d[3]=='Z'){
        int arr[4];
        // 故意越界：用到 d[n] 当索引
        volatile int x = arr[d[4]];   // 若 d[4] 大则越界
        (void)x;
    }
    return 0;
}
```
编译（需 Clang/libFuzzer，MSYS2 装 clang）：`clang++ -fsanitize=fuzzer,address fuzz_target.cpp -o fuzz_target.exe`
运行：`./fuzz_target.exe` 自动变异输入，几秒内 report 崩溃(AddressSanitizer 报越界)。

### 🔵 部分三·原理层（底层发生了什么）
- **覆盖率引导 fuzz(AFL)**：给程序插桩(编译时)，记录每条路径是否走到；变异输入让"新路径"被探索，高效逼近深分支。
- **libFuzzer**：库形式，自己循环喂输入，配 **ASan/UBSan**(Clang 消毒器)检测越界/UB 即时崩溃。
- 崩溃输入被存为 `crash-*` 文件，即 PoC。比手工快千万倍，是现实漏洞发现主力(Chrome/OpenSSL 等)。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么 fuzz 强？因为"有效性检查"边界最难手工覆盖，机器暴力+智能变异(遗传算法)能撞到。顶级追问：怎么造好**语料(corpus)**？用符号执行(SEC32)生成定向输入、用**结构感知 fuzz**懂格式(JSON/PDF)。历史上 AFL(2013) 普及覆盖率引导。

### 🔴 部分五·顶级视角
顶级研究员把 fuzz 当"日常基础设施"：对自己项目 7×24 跑 fuzz、接 CI(OPS30)防回归。跨领域：与编译器插桩(CMP7)、 sanitizer、云规模化(OPS 弹性)结合。白帽用它**找自己/授权开源项目漏洞**并合法报告(SEC100)。

### 🟠 部分六·安全/合规种子
Fuzzing **仅对自己程序、授权开源项目、CTF**。🚫 不对他人服务/网站做未授权 fuzz（等于攻击）。发现他人项目漏洞走**负责任披露/赏金流程(SEC100)**。本技能是"防御性找洞"的核心生产力。

### ✅ 验收
交：① `fuzz_target.exe` 跑出崩溃的 ASan 报告截图；② 说明覆盖率引导是什么；③ 写"发现他人漏洞应走什么流程"。

### ⚠️ 常见坑
- libFuzzer 需 Clang，MinGW gcc 不支持 `-fsanitize=fuzzer`，装 clang 或用 WSL。
- 目标函数签名必须 `LLVMFuzzerTestOneInput`，写错编译不过。
- fuzz 没崩溃≠无漏洞，只是没碰到；要长期跑+好语料。

### ➡️ 下一步
**SEC48 · Hook 技术总论**——从"改返回地址"转向"改函数本身行为"，用户态/内核态 Hook 用途边界（白帽总览）。

---

## SEC48 · Hook 技术总论

### 🎯 目标
学完能说清"Hook = 拦截/修改函数调用，让它做你要的事"，区分用户态 Hook(改自己/目标进程函数)与内核态 Hook(改系统调用)，并明确**白帽用途边界**：只 Hook 自己程序做监控/教学。

### 📋 小白前置
- 方向1：C10（函数指针）、CPP2（vtable/虚函数）、OTH3（汇编）
- 方向3：ASM3（调用约定）、CMP9（PLT/GOT 重定位）、OS1（进程地址空间）
- 本文件内：SEC24/25（导入表/IAT/GOT）、SEC27（反编译找函数）、SEC49-55（各类 Hook 详述）

### 🟢 部分一·最浅层（生活比喻）
Hook 像在"水管"上接个**三通阀门**：水流(函数调用)经过时，你先量一下(记录)、或掺点东西(改参数)、或干脆换条管(替换功能)。正常用途：给自家 App 加日志、做无障碍辅助、做行为监控(SEC55)。坏用途：偷改他人程序逻辑偷密——白帽不做。

### 🟡 部分二·动手层（逐字操作）
概念演示：在 Cutter 里打开自己程序，看一个函数（如 `printf` 的导入项），理解"Hook 就是把这个调用点改成你的函数"。后续 SEC49/50 会实做。本节点到"识别 Hook 点"：
```bash
rizin -q -c 'ii~printf' /d/lab/sec23/hello.exe   # 看 printf 导入(可 Hook 点)
```

### 🔵 部分三·原理层（底层发生了什么）
函数调用在二进制里是"跳到某地址"。Hook 就是**改写这个地址或地址里的内容**：
- 用户态：改 **IAT/GOT**(SEC49，导入表)或**函数开头指令**(Inline Hook SEC50)，让调用落到你的 `my_func`。
- 内核态：改系统调用表/回调(SEC54)，影响所有进程——威力大、风险高。
Hook 后通常"先做自己的事→再调原函(或跳过)"，实现增强/监控/拦截。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么 Hook 强大？它能在**不重新编译/不改源码**情况下改行为，广泛用于：调试、性能剖析、补丁热修、安全监控(EDR 都用 Hook/回调)。顶级追问：Hook 与"插桩(instrumentation)"区别？Hook 偏"改行为"，插桩偏"观测"。微软 Detours、开源 minhook 是框架。

### 🔴 部分五·顶级视角
顶级工程师用 Hook 做：API 监控(SEC55)、沙箱行为捕获(SEC58)、兼容层(Wine)、游戏模组(自己游戏 MOB48)。跨领域：与链接(CMP9)、调用约定(ASM3)、OS 进程(OS1)相通。安全侧：EDR 靠内核回调(Hook 的合法形式)检测恶意行为。

### 🟠 部分六·安全/合规种子
**白帽红线（贯穿 SEC48–SEC55）**：Hook **只用于**①自己写的程序；②你自己拥有的进程；③授权测试环境。🚫 绝不 Hook 他人进程偷账号/改他人软件逻辑/绕验证/注入恶意代码。Hook 技术一旦用于他人程序即可能违法（如键盘记录器、外挂、盗版）。本文件所有 Hook 实验**仅对自己程序、改自己程序行为**。

### ✅ 验收
交：① 说明用户态 Hook 与内核态 Hook 区别；② 列出 3 个 Hook 的**合法**用途；③ 复述红线：绝不 Hook 他人进程。

### ⚠️ 常见坑
- 把"Hook 自己程序"与"Hook 他人程序"混为一谈：法律边界在"是否你拥有/授权"。
- 以为 Hook 要改源码：Hook 恰恰不改源码，改的是运行期/导入表。
- 内核 Hook 风险极高，本文件仅分析(SEC54)，不鼓励在真实系统练。

### ➡️ 下一步
**SEC49 · IAT Hook**——改导入表，让程序调用"系统函数"时先过你这关（自己程序实做）。

---

## SEC49 · IAT Hook（导入表改写）

### 🎯 目标
学完能对自己程序做 IAT Hook：把 `MessageBoxA`（或 `printf` 的 Windows 对应）的导入项改成自己的函数，让程序弹出你改的内容——**仅自己程序**。

### 📋 小白前置
- 方向1：C10（函数指针）、C2（指针/内存）
- 方向3：CMP9（重定位/PLT-GOT）、OS5（内存页权限，需改只读）
- 本文件内：SEC24（PE 导入表/IAT）、SEC25（ELF GOT 对应）、SEC48（Hook 总论+红线）

### 🟢 部分一·最浅层（生活比喻）
导入表(IAT)像程序的"外借工具登记本"：写着"要用螺丝刀，去仓库 A 架"。IAT Hook 把登记本改成"要用螺丝刀，先来我工位"——于是每次借，都先经过你，你还能换把不同的。只改自己程序的登记本。

### 🟡 部分二·动手层（逐字操作）
Windows 上自写目标程序 + Hook（用 `MessageBoxA` 直观）：
```c
// target_msg.c —— 自己程序，调用 MessageBoxA
#include <windows.h>
int main(){ MessageBoxA(NULL,"原始内容","标题",MB_OK); return 0; }
```
```c
// iat_hook.c —— 同一进程内改自己 IAT（教学骨架，需用 Windows API 找 IAT 并 VirtualProtect 改写）
#include <windows.h>
// 思路：遍历本模块导入表找到 MessageBoxA 项，改成 myMessageBox，需 VirtualProtect 改页可写
// 完整实现较长，核心步骤：
// 1) 用 GetModuleHandle/ImageDirectoryEntryToData 定位 IAT
// 2) 遍历找到目标函数指针
// 3) VirtualProtect(页, PAGE_READWRITE) 后写入新地址
// 4) 调用原 myMessageBox 或替换
```
编译：`gcc target_msg.c -o target_msg.exe`。Hook 程序在同一进程注入式改 IAT。护手：可用现成 minhook 库的 IAT 示例，重点理解"改 IAT 项=Hook"。

### 🔵 部分三·原理层（底层发生了什么）
PE 加载时，导入表(IAT, SEC24)被填成真实函数地址。调用 `MessageBoxA` 时 `call [IAT槽]`。IAT Hook 把该槽改成 `myMessageBox` 地址；调用即落你函数。因 IAT 在内存常只读，需 `VirtualProtect` 改成可写再写回(OS5 页权限)。ELF 对应是改 GOT(SEC25)。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么 IAT Hook 简单？因为它只改"一个指针"，不用动函数指令(对比 SEC50 Inline)。但局限：只 Hook **导入函数**(程序自己内部函数不在 IAT)。顶级追问：若程序用 `GetProcAddress` 动态取地址(不走 IAT)则 IAT Hook 失效→需 Inline/SEH 等。历史上 IAT Hook 是外挂/恶意软件常用，故 EDR 监控 IAT 改写。

### 🔴 部分五·顶级视角
顶级工程师用 IAT Hook 做：API 监控(SEC55)、补丁、兼容层。安全侧：EDR 检测"异常 IAT 改写"识别注入。跨领域：与 PE 结构(SEC24)、链接(CMP9)、页权限(OS5)相关。

### 🟠 部分六·安全/合规种子
IAT Hook **仅自己程序、自己进程**。🚫 不改他人程序 IAT 偷数据/绕验证/做外挂。本实验改的是你编译的 `target_msg.exe` 自己的导入，合法教学。红线重申：对他人进程做 IAT Hook = 违法边界。

### ✅ 验收
交：① 说明 IAT Hook 改的是"函数指针槽"而非指令；② 用自己的话描述改写步骤（找 IAT→改页权限→写新地址）；③ 复述：只 Hook 自己程序。

### ⚠️ 常见坑
- 忘记 `VirtualProtect` 改可写，写 IAT 会访问冲突崩溃。
- 改错槽（同名函数多版本 Ansi/Unicode），需精确匹配 `MessageBoxA`。
- 64 位地址写入要完整 8 字节，别截断。

### ➡️ 下一步
**SEC50 · Inline Hook**——直接改函数开头指令跳去你的代码（trampoline 保原功能），自己程序实做。

---

## SEC50 · Inline Hook（指令覆盖 / trampoline）

### 🎯 目标
学完能理解"Inline Hook = 覆盖目标函数开头的几条指令为 `jmp 我的函数`"，并用 trampoline(跳板)保留原函数可调用——**仅自己程序**。

### 📋 小白前置
- 方向1：C10（函数指针）、OTH3（汇编 jmp）
- 方向3：ASM1（寄存器）、ASM3（调用约定）、OS5（页权限）
- 本文件内：SEC49（IAT Hook 对比）、SEC48（红线）、SEC27（找函数地址）

### 🟢 部分一·最浅层（生活比喻）
Inline Hook 像在"剧本第一页开头"贴张纸条："看到这页，先去隔壁房间(你的函数)走一遍，再回来继续"。为保证原剧本还能演，你先把被遮住的几行抄到"跳板(trampoline)"上，跳板末尾再跳回原处——这样既插了你的戏，又不丢原戏。

### 🟡 部分二·动手层（逐字操作）
自写目标函数 + Hook（同进程内，教学骨架）：
```c
// inline_target.c
#include <stdio.h>
void greet(){ printf("你好\n"); }          // 要 Hook 的函数
void my_greet(){ printf("【被Hook】你好\n"); greet_org(); } // 我们的替换
// Hook 步骤(需汇编 jmp 覆盖 greet 开头 5/14 字节，并建 trampoline 跳回)
```
关键：在 `greet` 开头写入 `jmp my_greet`（x64 用 `E9 + 偏移` 或 `FF25` 绝对跳），把被覆盖的原指令搬到 trampoline，trampoline 末尾跳回 `greet+被覆盖长度`。可用 minhook 库简化（它自动处理 trampoline）。护手：用库示例理解，再读其源码看 trampoline 怎么建。

### 🔵 部分三·原理层（底层发生了什么）
- 目标函数前 N 字节被改成**无条件跳转**到 `my_func`(x64 绝对地址跳转需 14 字节 `mov rax,addr; jmp rax`，或相对 `E9` 5 字节)。
- 被覆盖的原指令复制到 **trampoline** 内存，末尾加跳回 `原函数+N`，使 `原函数_original` 仍可调用（避免无限递归）。
- 同样需 `VirtualProtect` 改函数页可写(OS5)。
- 难点：不能把一条指令**从中间**切断（要覆盖完整指令），故先反汇编(SEC27)算前几条指令总长度。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么比 IAT Hook 强？它能 Hook **任意函数**（含程序内部、系统 API 底层），不限于导入。顶级追问：多线程下改指令要停其它线程(避免 Half-hook 崩)；Hot-patching(微软预留 `mov edi,edi` 5 字节空位)就是为安全 inline patch 设计。历史上 Detours 库定义了 trampoline 标准做法。

### 🔴 部分五·顶级视角
顶级工程师用 Inline Hook 做：性能剖析(计时每个函数)、API 监控(SEC55)、调试器断点底层(SEC28 的 `0xCC` 其实也是一种 inline patch)。跨领域：与反汇编(SEC27)、调用约定(ASM3)、并发(OS3)相关。

### 🟠 部分六·安全/合规种子
Inline Hook **仅自己程序、自己进程**。🚫 不 Hook 他人进程函数（如游戏/软件）做外挂/偷密/绕验证——违法。本实验 Hook 你自己写的 `greet`。红线：对他人进程 inline hook = 恶意软件特征，绝不碰。

### ✅ 验收
交：① 说明 trampoline 作用（保留原功能）；② 说明为什么需先反汇编算"完整指令长度"再覆盖；③ 复述只 Hook 自己程序。

### ⚠️ 常见坑
- 覆盖从指令中间切断→崩溃；必须用反汇编算前几条完整指令长度。
- 忘了保存原指令到 trampoline→原函数调用死循环/崩。
- 多线程改指令无同步→偶发崩，教学单线程演示。

### ➡️ 下一步
**SEC51 · DLL 注入**——把你的代码"装进"目标进程（CreateRemoteThread+LoadLibrary），白帽仅授权/自己进程。

---

## SEC51 · DLL 注入（CreateRemoteThread + LoadLibrary）

### 🎯 目标
学完能理解"DLL 注入 = 让别的进程加载你的 dll，从而在其地址空间运行你的代码"，并明确**白帽边界：仅自己进程/授权测试**，不用于他人。

### 📋 小白前置
- 方向1：C13（构建 dll）、C10（函数指针）
- 方向3：OS1（进程/地址空间）、OS11（IPC）、OS10（系统调用）
- 本文件内：SEC48-50（Hook 基础）、SEC52（消息钩子对比）

### 🟢 部分一·最浅层（生活比喻）
目标进程像一栋楼。DLL 注入 = 你把自己的"工具间(dll)"搬进这栋楼，并在里面接上电(运行代码)。`LoadLibrary` 是"搬工具间"，`CreateRemoteThread` 是"雇个工人去搬"。这样你的代码就在那栋楼里跑，能 Hook 它(SEC50)、看它(SEC55)。白帽只在"你自己的楼"或授权楼做。

### 🟡 部分二·动手层（逐字操作）
教学骨架（**仅对自己进程演示原理**，完整跨进程注入码较长）：
```c
// mydll.c -> 编译成 dll，DllMain 里写"加载时打印"作为标记
#include <windows.h>
BOOL WINAPI DllMain(HINSTANCE h, DWORD r, LPVOID){ if(r==DLL_PROCESS_ATTACH) MessageBoxA(NULL,"DLL 已加载(自己进程)","",MB_OK); return TRUE; }
```
```c
// inject.c 思路：OpenProcess(自己/授权) -> VirtualAllocEx 写 dll 路径 -> CreateRemoteThread(LoadLibraryA, 路径)
// 关键 API：OpenProcess / VirtualAllocEx / WriteProcessMemory / CreateRemoteThread
```
护手：用 Windows 官方示例理解 `CreateRemoteThread + LoadLibraryW` 流程；实做时目标进程设为**你自己写的测试程序**（同目录），不碰任何他人软件。

### 🔵 部分三·原理层（底层发生了什么）
注入核心：① `OpenProcess` 拿目标句柄(需权限)；② `VirtualAllocEx` 在目标进程**分配内存**；③ `WriteProcessMemory` 写入你的 dll 路径字符串；④ `CreateRemoteThread` 在目标里建线程，入口=`LoadLibraryW`，参数=刚写的路径→目标进程加载你的 dll，dll 的 `DllMain` 在其空间执行。之后 dll 内可做 Inline Hook(SEC50) 监控。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么 DLL 注入常见？因为"在同一地址空间"才能改其内存/Hook。顶级追问：注入检测——EDR 监控 `LoadLibrary` 异常、线程创建来自外部；绕过手法(APC SEC53、SetWindowsHookEx SEC52)都是"换种方式塞代码"。历史上这也正是恶意软件(木马/外挂)手段，故防御侧极度关注。

### 🔴 部分五·顶级视角
顶级工程师用注入做：安全软件(杀软/EDR 合法用)、沙箱(SEC58)、无障碍工具、自己 App 模块化。跨领域：与 OS 进程(OS1)、内存(OS5)、API(SEC55)相关。安全研究员必须懂注入，才能**检测**恶意注入。

### 🟠 部分六·安全/合规种子
**红线重申**：DLL 注入**仅自己进程、自己写的测试目标、授权渗透**。🚫 绝不注入他人进程（游戏外挂、偷密码键盘记录、绕验证都在此列，违法）。学它是为：懂 EDR/沙箱原理、能检测恶意注入。对他人软件注入 = 犯罪边界。

### ✅ 验收
交：① 说明注入四步（开进程/分配/写路径/建线程 LoadLibrary）；② 写 2 个注入的**合法**用途；③ 复述：不注入他人进程。

### ⚠️ 常见坑
- `CreateRemoteThread` 需目标进程权限，跨进程常因 UAC/保护失败，教学用同用户自己的进程。
- 写路径要用目标进程编码(宽字符 `LoadLibraryW`)，ASCII 易错。
- 真注入杀软/系统进程会被拦截甚至报毒，自己程序练才安全。

### ➡️ 下一步
**SEC52 · 钩子 SetWindowsHookEx**——用 Windows 消息钩子把 dll 注入有消息循环的进程（白帽仅自己/授权）。

---

## SEC52 · 钩子 SetWindowsHookEx（消息钩子）

### 🎯 目标
学完能理解"SetWindowsHookEx = 借 Windows 消息机制把 dll 注入有消息循环的进程"，并明确**白帽边界**，不用于他人（如键盘记录器是典型违法用例）。

### 📋 小白前置
- 方向3：OS10（系统调用/消息）、OS1（进程）
- 方向1：C10（回调/函数指针）、MOB38（Windows 消息循环）
- 本文件内：SEC51（DLL 注入）、SEC48（红线）、SEC50（Inline Hook 后续）

### 🟢 部分一·最浅层（生活比喻）
Windows 像个大邮局，每个窗口程序都在"收信"(消息：点击/按键)。`SetWindowsHookEx` 是"在邮局设个代收员"：所有经过的信先给你 dll 看一眼(可记录/改)。因为邮局的机制，你的 dll 会被自动送到"收信人那栋楼"运行。白帽只用这机制做自己程序的辅助/监控，绝不做"偷看别人按键"。

### 🟡 部分二·动手层（逐字操作）
教学骨架（**仅自己进程/自己消息程序**）：
```c
// hookdll.c：WH_GETMESSAGE 钩子，DllMain 里 SetWindowsHookEx
#include <windows.h>
HHOOK g;
LRESULT CALLBACK proc(int c, WPARAM w, LPARAM l){
    return CallNextHookEx(g,c,w,l);   // 不修改，仅示意"路过"
}
extern "C" __declspec(dllexport) void install(){ g=SetWindowsHookEx(WH_GETMESSAGE,proc,GetModuleHandleA(0),0); }
```
目标程序(你自己写的窗口程序)调用 `install()` 即可。护手：完整 WH 例子较长，理解"钩子过程 + CallNextHookEx + 卸载"三要素即可；实做限自己窗口程序。

### 🔵 部分三·原理层（底层发生了什么）
`SetWindowsHookEx` 注册一个**钩子过程**并关联 dll；当目标线程产生对应消息(如按键 `WH_KEYBOARD`)，Windows 把该 dll **自动映射到目标进程**并执行钩子过程（本质是一种"系统帮忙的注入"，SEC51）。钩子处理完需 `CallNextHookEx` 把消息传下去，否则系统卡住。卸载 `UnhookWindowsHookEx` 移除。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么消息钩子普及？因为 Windows 消息机制天然跨进程派发 dll，是"最老牌"的注入方式之一。顶级追问：`WH_KEYBOARD_LL`(低级键盘) 甚至**不需注入 dll** 就能全局监听按键——这正是键盘记录器的技术来源，故必须**白帽自律**：绝用此做偷密。安全软件用同类机制做输入防护(合法)。

### 🔴 部分五·顶级视角
顶级工程师用消息钩子做：无障碍(读屏)、输入法、UI 自动化测试、自己 App 增强。安全侧：EDR 监控"可疑全局键盘钩子"识别木马。跨领域：与 Windows 消息(OS10/MOB38)、注入(SEC51)相关。

### 🟠 部分六·安全/合规种子
**最敏感红线之一**：`WH_KEYBOARD`/`WH_KEYBOARD_LL` 是键盘记录器技术。🚫 绝不装全局键盘钩子偷他人输入（密码/聊天）——**明确违法**。本文件只在自己窗口程序内装"不记录、只传递"的演示钩子，理解原理即可。白帽用途：自己输入法/辅助/授权测试。

### ✅ 验收
交：① 说明 SetWindowsHookEx 为何能"自动把 dll 送进目标进程"；② 列举 2 个**合法**钩子用途；③ 明确复述：绝不用键盘钩子偷密。

### ⚠️ 异常坑
- 钩子过程不调 `CallNextHookEx` 会卡死消息链，整个系统相关输入无响应——教学务必调。
- 全局钩子(`dwThreadId=0`)影响所有进程，调试不慎需重启资源管理器，护手优先用线程级钩子(指定自己线程)。
- 忘记 `UnhookWindowsHookEx` 卸载→dll 残留。

### ➡️ 下一步
**SEC53 · APC 注入**——借"异步过程调用"在目标线程Alertable时塞入代码（白帽仅自己/授权）。

---

## SEC53 · APC 注入（异步过程调用）

### 🎯 目标
学完能理解"APC 注入 = 往目标线程的 APC 队列塞一个函数，等它进入可警告等待(Alertable)时执行"，并明确**白帽边界**。

### 📋 小白前置
- 方向3：OS1（线程/TCB）、OS3（同步/等待）、OS11（IPC）
- 方向1：C13（dll）、C10（回调）
- 本文件内：SEC51（DLL 注入）、SEC52（钩子对比）

### 🟢 部分一·最浅层（生活比喻）
APC 像给工人(线程)留个"待办便签"：他在忙别的，但每当他"歇口气(进入可警告等待，如 `SleepEx`)"，就会翻便签执行你塞的事。你不用新建线程，借用他歇气时跑你的代码——前提是他在"可被提醒"的状态。

### 🟡 部分二·动手层（逐字操作）
教学骨架（**仅自己进程内演示原理**）：
```c
// apc_demo.c 同进程演示 APC：队列一个函数，SleepEx 触发
#include <windows.h>
#include <stdio.h>
void CALLBACK myapc(ULONG_PTR){ printf("APC 执行了\n"); }
int main(){
    QueueUserAPC(myapc, GetCurrentThread(), 0); // 给自己线程塞 APC
    SleepEx(100, TRUE);   // 可警告等待 -> 触发 APC
    return 0;
}
```
编译：`gcc apc_demo.c -o apc_demo.exe`，运行打印"APC 执行了"。跨进程版用 `QueueUserAPC` + 打开目标线程(需注入 dll 地址)，原理同 SEC51，白帽仅自己/授权。

### 🔵 部分三·原理层（底层发生了什么）
每个线程有 **APC 队列**。线程调用 `SleepEx`/`WaitForSingleObjectEx` 等 *Ex 可警告函数时，内核检查队列，有则执行 APC 过程。APC 注入 = 往目标线程队列 `QueueUserAPC(LoadLibrary, hThread, dllPath)`，目标进入 alertable 即加载你的 dll——比 `CreateRemoteThread`(SEC51)更隐蔽(复用已有线程)。缺点：目标线程得"愿歇气"(常调 *Ex 等待)才会触发。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么有 APC？它是 Windows 异步 I/O 完成通知的机制(OS9 I/O 模型)。被复用为注入是因为"自然跨线程执行代码"。顶级追问：如何强制目标进入 alertable？恶意软件会 `NtAlertResumeThread` 等；防御侧 EDR 监控 `QueueUserAPC` 异常(尤其指向新分配内存)。历史上 APC 注入是无文件攻击常用。

### 🔴 部分五·顶级视角
顶级工程师懂 APC 用于：合法异步处理、自己程序线程通信、安全软件注入扫描。跨领域：与 OS 线程(OS1)、I/O 模型(OS9)、注入(SEC51)相关。安全研究员懂 APC 才能检测"无文件注入"。

### 🟠 部分六·安全/合规种子
APC 注入**仅自己进程/授权测试**。🚫 不往他人进程塞 APC 做恶意加载/无文件攻击。学它为了：理解 EDR 为何监控 `QueueUserAPC`、识别无文件攻击。红线：对他人进程任何注入=违法边界。

### ✅ 验收
交：① 运行 `apc_demo.exe` 输出截图；② 说明"为何目标线程要进入 alertable 才触发"；③ 复述不注入他人进程。

### ⚠️ 常见坑
- 目标线程从不调用 *Ex 等待 → APC 永不触发（注入失败，非 bug）。
- 跨进程需先把 dll 路径写入目标(WriteProcessMemory)，否则 APC 取到坏地址崩。
- 误以为 APC 立刻执行：它等"可警告等待"，有延迟。

### ➡️ 下一步
**SEC54 · 驱动/内核 Hook**——改系统调用/回调影响全局（分析原理，不鼓励真实系统练）。

---

## SEC54 · 驱动 / 内核 Hook（回调 / IRP，分析不实践）

### 🎯 目标
学完能理解"内核 Hook = 改系统调用表/内核回调/IRP 分发，影响所有进程"，并明确**白帽只分析原理、不在真实系统练**（风险高、易蓝屏、易越界违法）。

### 📋 小白前置
- 方向3：OS10（系统调用/陷阱）、OS14（内核模块 LKM）、OS1（进程）、COMP5（ring0）
- 本文件内：SEC46（内核漏洞/提权）、SEC48-53（用户态 Hook 对比）

### 🟢 部分一·最浅层（生活比喻）
内核 Hook 像在"皇宫总水管"上接阀门：不只是某一栋楼，而是**全城**水流都过你手。威力巨大但一拧错全体停水(蓝屏)。用户态 Hook(SEC48-53)只改一栋楼，内核 Hook 改全城——所以白帽仅弄懂原理，绝不手痒去真实系统试。

### 🟡 部分二·动手层（逐字操作）
**仅阅读/分析，不加载内核驱动**。概念对照：
- **SSDT / 系统调用表 Hook**：改 `Nt*` 函数指针(类似用户态 IAT Hook SEC49，但在内核)。
- **回调(Callback)**：Windows 提供**合法**注册点(如 `PsSetCreateProcessNotifyRoutine` 进程创建通知)，安全软件正用这些——白帽鼓励用合法回调而非改表。
- **IRP Hook**：过滤驱动挂到设备栈，拦截 I/O 请求包(IRP)。
护手：用 Cutter/Ghidra 打开一个**自己编译的示例驱动源码**(仅静态读)理解结构，不加载。

### 🔵 部分三·原理层（底层发生了什么）
内核维护系统调用分发表(如 Windows SSDT、Linux sys_call_table)。内核 Hook 改写其中项，使所有进程的系统调用先经你的函数(全局拦截)。Windows 还提供**官方回调机制**(进程/线程/镜像/注册表通知)，这是"合法 Hook 形态"。IRP 是驱动间 I/O 请求包，过滤驱动可挂设备栈拦截(类似中间人)。因运行 ring0，出错直接 BSOD。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么现代系统抵制内核 Hook？因为不稳定(蓝屏)+被 Rootkit 滥用(SEC61，分析)。微软推 **PatchGuard(PG/CFG)** 检测非法内核修改、推官方回调替代 Hook。顶级追问：PG 如何定期校验关键结构？这催生"无 PatchGuard 平台"上的攻防研究。历史：Rootkit 靠内核 Hook 隐藏自己(SEC61 分析不实践)。

### 🔴 部分五·顶级视角
顶级安全工程师分：防御(EDR 用**合法回调**做内核级监控，不脏 Hook)、攻击(授权红队研究 PG 绕过，仅研究)。理解内核 Hook=理解 OS 全栈与信任边界。跨领域：与 OS10/OS14、驱动、CPU ring(COMP5)同源。

### 🟠 部分六·安全/合规种子
**明确**：本章**分析不实践**于真实系统。🚫 不写/不加载内核 Hook 驱动到任何机器（含自己）做隐藏/拦截——极易蓝屏且技术即 Rootkit 特征。白帽学它是为：懂 EDR 合法监控机制、识别 Rootkit、理解 PatchGuard 保护。对他人系统更绝不碰。

### ✅ 验收
交：① 对比"用户态 IAT Hook(SEC49)"与"内核系统调用表 Hook"影响范围；② 说明 Windows 官方回调为何比脏 Hook 安全；③ 复述：内核 Hook 不实践于真实系统。

### ⚠️ 常见坑
- 真加载实验驱动→蓝屏/启动失败，教学只读源码。
- 把"合法回调"当"脏 Hook"：官方回调(PsSetXxxNotify)是白帽推荐的安全监控方式。
- 以为内核 Hook 像用户态简单：ring0 无保护，错一处全崩。

### ➡️ 下一步
**SEC55 · API 监控 / 沙箱**——用 Hook/回调观测程序"做了什么"（白帽行为观测终点章）。

---

## SEC55 · API 监控 / 沙箱（行为观测，白帽）

### 🎯 目标
学完能说清"API 监控 = 记录程序调用的系统函数从而知其行为"，并理解沙箱(SEC45/OS15)如何隔离运行+监控；用自己程序演示"打印它调用的 API 列表"。**白帽：行为观测用于自己样本/授权 CTF/本地沙箱**。

### 📋 小白前置
- 方向3：OS15（容器/沙箱）、OS10（系统调用）、OS11（IPC）
- 方向1：C13（dll）、C10（回调）
- 本文件内：SEC48-54（各类 Hook/注入，本章把它们用于"观测"）、SEC45（沙箱）、SEC24/25（导入表=IAT/GOT 监控点）

### 🟢 部分一·最浅层（生活比喻）
API 监控像给程序装"行车记录仪"：它每调一次系统功能（读文件/联网/建进程），你都记一笔。汇总就是"这程序干了啥"的行为报告。沙箱=把它关进围栏(SEC45)同时开记录仪，跑完看报告判断是否可疑。白帽用它分析自己/授权样本。

### 🟡 部分二·动手层（逐字操作）
教学：自己程序 + 打印它调了哪些 API（用 IAT Hook SEC49 思路记录，或简单点——直接在自己代码里打日志）：
```c
// apimon_demo.c —— 自己程序，记录自己行为
#include <stdio.h>
#include <windows.h>
int main(){
    printf("[行为] 打开文件\n"); FILE*f=fopen("x.txt","w"); 
    printf("[行为] 写文件\n"); fputs("hi",f); fclose(f);
    printf("[行为] 弹窗\n"); MessageBoxA(NULL,"demo","",MB_OK);
    return 0;
}
```
编译运行看 `[行为]` 日志——这就是"自己监控自己"的最小形态。进阶：用 Process Monitor(微软官方免费工具) 打开**自己程序**，观察它真实调了 `CreateFile/WriteFile` 等——合法行为观测工具。

### 🔵 部分三·原理层（底层发生了什么）
API 监控实现 = 在目标进程内 Hook 关键 API(SEC49 IAT / SEC50 Inline / SEC54 内核回调)，每次调用先记日志再放行。沙箱 = 容器/虚拟机(SEC45/OPS19)隔离 + 监控(syscall 层/API 层) + 网络隔离，跑完出报告。Process Monitor 用内核回调(SEC54 合法形态)捕获所有文件/注册表/网络事件。

### 🟣 部分四·深挖层（为什么 & 延伸）
为什么行为观测重要？因为静态(SEC27/30)看不出"运行时才干的坏事"，动态监控补上。顶级追问：如何应对"反沙箱"(SEC60 分析)——样本检测自己是否在沙箱(CPU 温差/鼠标活动)就装乖？防御侧用"更真沙箱/仿真(SEC33)"对抗。历史上 Cuckoo 是开源沙箱代表。

### 🔴 部分五·顶级视角
顶级分析师建"行为基线"：正常程序调 API 有规律，异常程序(如突然大量 `CreateRemoteThread`/连陌生 IP)即告警。EDR 本质就是"常驻 API 监控 + 云端判定"。跨领域：与 OS 全栈、Hook(SEC48-54)、沙箱(SEC45)、云(OPS)全通。这是方向7b 的**收口能力**：从逆向→漏洞→Hook，最终服务于"看得见行为、防得住攻击"。

### 🟠 部分六·安全/合规种子
API 监控/沙箱**仅用于**自己样本、授权 CTF、本地隔离环境(如 Windows Sandbox/虚拟机)。🚫 不监控他人程序/他人进程行为以窃隐私。用官方工具(Process Monitor)分析自己程序完全合法。本文件全章(SEC23–SEC55)技术**只对自己/授权样本**，红线贯穿始终。

### ✅ 验收
交：① 用 Process Monitor 打开自己 `apimon_demo.exe` 看到的 `CreateFile` 事件截图；② 说明"行为观测"如何补静态分析不足；③ 复述全方向7b 红线一句。

### ⚠️ 常见坑
- Process Monitor 事件海量，需用 Filter(ctrl+L)筛自己进程名，否则卡。
- 误把监控他人程序当学习：仅自己/授权样本。
- 沙箱里样本若真恶意会外联，务必断网/隔离虚拟机再跑（仅授权样本）。

### ➡️ 下一步
**SEC56（方向7c 第 1 节）· 恶意软件分类**——进入方向7c，从"看懂恶意软件长啥样"开始（病毒/蠕虫/木马/勒索，分类与防御视角）。

---

> **方向7b 完结说明**：SEC23–SEC55 共 33 节已全部展开。全程遵循白帽铁律——所有逆向/调试/漏洞/ Hook 实验**只对自己编译的程序、授权样本、CTF 二进制**；绝不逆向他人商业软件、绝不注入/Hook 他人进程偷密、绝不绕正版验证、绝不写恶意模块。讲利用原理是为学会**防护与检测**。身体优先，手疼就停，课永远在。

---

# SEC-B1 · 栈溢出进阶（栈迁移 / SEH / 泄漏）

## 🎯 目标
学完能理解 **栈溢出的三种进阶打法**：① 栈迁移（stack pivot，栈空间不够时把执行流"挪"到另一块可控内存）② SEH 利用（Windows 结构化异常处理链劫持）③ 栈地址泄漏（绕过 ASLR 先 leak 再打）；并在**自己编译的漏洞程序** 用 GDB/Rizin 观察（仅分析，不攻击他人）。

## 📋 小白前置
- SEC25（栈溢出基础）、SEC34（内存布局）、SEC26（Shellcode）、SEC35（DEP/ASLR 缓解）
- 方向3：ASM1（寄存器）、OS5（虚拟内存）、COMP5（特权/ring）
- 工具：MSYS2 gdb、Rizin（已装）

## 🟢 部分一·最浅层（生活比喻）
栈溢出基础（SEC25）像"往杯子里倒水溢出洒一桌"。进阶打法：
- **栈迁移** = 杯子太小装不下你的"工具包"，你干脆把工具包放桌上（另一块内存），再让程序"转头去桌上拿"——执行流被你指到别处。
- **SEH** = Windows 程序出错时有个"异常处理登记本"（链），你溢出把本子的"出错该找谁"改成你的代码，程序一崩就按你的来。
- **泄漏** = 你先让程序"不小心说漏嘴"打印出自己在内存的地址，再按这地址精确定位打（破 ASLR 随机化）。

## 🟡 部分二·动手层（逐字操作）
仅用**自己编译的**漏洞 demo 观察（合法）。栈迁移最小示意：
```c
// pivot_demo.c —— 自己程序，故意在栈上留"可被控制的长缓冲区"
#include <string.h>
void vuln(char* s){ char buf[16]; memcpy(buf, s, 64); }  // 溢出写 48 字节
int main(int c, char**v){ vuln(v[1]); return 0; }
```
编译（关优化便于观察）：`gcc -fno-stack-protector -z execstack -o pivot_demo pivot_demo.c`（仅本地分析）。用 Rizin 看 `vuln` 的栈帧与返回地址偏移：
```bash
rizin -q -c 'aa; s main; pdf' pivot_demo
```
观察：buf 到返回地址的偏移 = 16 + 8(对齐) = 24。先用**只打印不跳转**的 payload 验证偏移，再理解"若需更长 ROP 链但栈不够 → 用 `leave; ret` 改 RSP 指向堆上布置的链（栈迁移）"。SEH/泄漏同理用自己程序观察寄存器与 SEH 链结构。

## 🔵 部分三·原理层（底层发生了什么）
- **栈迁移** = 控制 `RSP`/`ESP` 指向攻击者布置好的"第二栈"（常在堆/bss），ROP 链放那。触发靠 `leave; ret`（函数收尾指令：把 `RBP` 载入 `RSP` 再 `ret`）——改 RBP 即可改 RSP。
- **SEH（Windows）** = 线程的 `TEB` 含 `ExceptionList` 指针，指向 `_EXCEPTION_REGISTRATION_RECORD` 链（含 `handler` 函数指针）。溢出覆盖该结构 → 异常触发时执行你的 handler（经典 `pop pop ret` 找 gadget  bypass safeseh）。
- **泄漏** = 利用程序自身的输出（如 `printf(buf)` 打印栈上地址、`puts` 泄漏 libc 地址）先获基址，再计算 gadget 绝对地址，绕过 ASLR（SEC35）。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：栈迁移/SEH 是 2000s 主流利用；ASLR+DEP(SEC35) 普及后，单靠栈溢出直接跳 shellcode 失效，演化出"泄漏+ROP(SEC-B4)+迁移"组合拳。
- 顶级视角：**现代栈溢出利用 = 信息泄漏 + 精确偏移 + 缓解绕过** 三段式；CTF 里常先 `leak libc` 再 `one_gadget`。理解这与 SEC35 缓解是"猫鼠博弈"。

## 🔴 部分五·顶级视角
顶级二进制研究员把利用当**"对缓解机制的破解工程"**：先列目标二进制开了哪些保护（`checksec`：canary/PIE/RELRO/NX），再选打法。栈迁移/SEH/泄漏是**绕过 ASLR/NX 的工具箱**，不是目的。他们写利用同时写**防护补丁建议**（开 canary、开 PIE、开 full RELRO）。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 只在**自己编译的 demo / 授权 CTF 题 / 授权渗透** 练习利用；用 gdb/Rizin 观察自己程序完全合法。
- 🚫 不对任何**非授权**程序/服务做栈溢出利用；不写"一键 getshell"工具散发。
- 🚫 不绕实名/监管，不碰他人软件。

## ✅ 验收
交：① `pivot_demo` 的 `vuln` 栈帧偏移计算笔记（buf→ret 偏移）② 说明栈迁移为什么需要"改 RSP" ③ 用 Rizin 截图 `pdf` 反汇编标注返回地址位置。

## ⚠️ 常见坑
- 偏移算错（忘了 8 字节对齐/寄存器保存区）→ payload 崩在错误位置；用 Rizin 看 `stack` 实际布局验证。
- 开了 PIE 还硬编码地址 → 必先 leak。
- 手疼：仅做"算偏移+反汇编观察"，利用链构造可只看思路。

## ➡️ 下一步
**SEC-B2 · 堆利用（Heap Exploitation：tcache / fastbin / UAF）**

### ⚔️ 闯关路线（SEC-B1）
- 🟢 Lv1：编译 pivot_demo，用 Rizin 看 vuln 的 pdf，指出 buf 与 ret 相对位置。🥉
- 🔵 Lv2：人工算出 buf→ret 偏移（24），用 `cyclic` 模式验证崩溃点对应偏移。🥈
- 🟣 Lv3：在自己程序里构造"泄漏栈地址"的输出点，观察 ASLR 每次启动地址不同。🥇
- 🟠 Lv4：写一个"开启 canary/PIE 后利用失效"的对比实验，说明缓解如何挡栈溢出。🏆
- 🔴 Lv5：给一段 CTF pwn 题（授权）写"信息泄漏→栈迁移→ret2win"的完整利用思路文档（不散播 exploit）。👑

---

# SEC-B2 · 堆利用（Heap：tcache / fastbin / UAF）

## 🎯 目标
学完能理解 **glibc 堆（ptmalloc）的核心结构** 与三类经典利用：**UAF（释放后使用）**、**double free**、**tcache/fastbin  poisoning**；在**自己编译的堆 demo** 用 GDB 观察 chunk 结构（仅分析）。

## 📋 小白前置
- SEC34（堆/内存布局）、SEC38（UAF 基础）、SEC35（缓解）、SEC-B1（栈基础）
- 方向3：OS5（虚拟内存/brk）、C1（结构体/指针）

## 🟢 部分一·最浅层（生活比喻）
堆像**公用储物柜系统**（glibc 管理）。每个柜子(chunk)有"使用中小票"(size字段+标志)。
- **UAF** = 你退了柜子(A)，但手里的旧钥匙还能开它——管理员把柜子给了别人(B)后，你用旧钥匙改了里面东西，B 遭殃。
- **double free** = 同一柜子退两次，管理员记账乱了，第二次可能把同柜子发给你两次。
- **tcache poisoning** = 你篡改"空闲柜子链表"的指针，让下次分配直接给你指定的任意地址（写任意内存）。

## 🟡 部分二·动手层（逐字操作）
自己编译观察（合法）。UAF 最小 demo：
```c
// uaf_demo.c
#include <stdlib.h>
#include <string.h>
struct User { char role[8]; };
int main(){
    struct User* a = malloc(sizeof(struct User));
    strcpy(a->role, "user");
    free(a);                       // 释放
    // UAF：a 仍指向已释放内存（demo 仅观察，不利用）
    struct User* b = malloc(sizeof(struct User));
    strcpy(b->role, "admin");      // b 可能复用 a 的块
    // 此时 a->role 内容已被 b 改成 admin —— 观察 UAF 效果
    return 0;
}
```
编译 `gcc -g -o uaf_demo uaf_demo.c`，用 gdb 在 free 后、`b->role` 写入后分别 `x/16xw a` 观察内存变化。理解"释放后未置空指针 = UAF 温床"。

## 🔵 部分三·原理层（底层发生了什么）
- **chunk 结构**：glibc chunk 头部含 `size`（含标志位）、前后向指针（空闲时存于用户数据区）。tcache（2.26+ 单线程快速缓存）是**单链表**，结构最简单，故最易被 poisoning。
- **UAF** = `free` 后指针未置 NULL，`malloc` 复用该 chunk，旧指针读写影响新对象 → 改虚表/改凭证。
- **tcache poisoning** = 改空闲 chunk 的 `next` 指针为任意地址，`malloc` 两次后返回该地址 → **任意地址写**（配合改 `__free_hook`/`__malloc_hook` 为 system → 拿 shell）。
- **fastbin** = 单链表 LIFO，检查较松，可被 double free / 伪造 chunk 利用（比 tcache 多一道 size 校验）。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：堆利用是 2010s 后主流（栈被 canary/DEP 封死）。**House of 系列**（Spirit/Force/Einherjar）、tcache 引入（2.26）催生新打法。**2014 Heartbleed** 本质也是"越界读堆内存"。
- 顶级视角：**现代利用 = 泄露堆/glibc 地址 + 伪造 chunk / 毒化链表 + hook 覆写**。防护：开 `FORTIFY`、`tcache safe-linking`（glibc 2.32+ 对指针异或加密）、用 `safe-linking` 挡 poisoning。

## 🔴 部分五·顶级视角
顶级研究员视堆利用为"**对内存分配器状态的攻击**"。他们既写利用也写防护：推荐**使用内存安全语言(Rust/Go)根除 UAF**、C 代码用 `free` 后置 `NULL`、用 `AddressSanitizer` 在开发期抓 UAF、及时更新 glibc 拿 safe-linking。这呼应 SEC46 内核与 SEC47 fuzzing——都是"内存安全"大局。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 仅在自己编译 demo / 授权 CTF 练习堆利用分析；gdb 观察自己程序合法。
- 🚫 不对非授权程序做堆利用；不写通用堆利用 exploit 散播。
- 🚫 不碰他人软件/绕验证。

## ✅ 验收
交：① uaf_demo 的 gdb 内存观察截图（free 前后 a 指向内容变化）② 说明 tcache poisoning 为何能"任意地址写" ③ 写一条 C 代码防护建议（free 后置 NULL）。

## ⚠️ 常见坑
- 忽略 glibc 版本差异：tcache 2.26+ 才有，老题用 fastbin。
- 没开 `-g` 调试信息，gdb 看不清结构 → 编译加 `-g`。
- 手疼：用 `pwndbg` 的 `vis` 命令可视化堆（若装了），否则手 `x` 看。

## ➡️ 下一步
**SEC-B3 · 格式化字符串利用（泄露 + 任意写）**

### ⚔️ 闯关路线（SEC-B2）
- 🟢 Lv1：编译 uaf_demo，gdb 在 free 后 `x/16xw a` 看内存。🥉
- 🔵 Lv2：构造"free 后置 NULL"的修复版，对比无 UAF。🥈
- 🟣 Lv3：在自己程序里观察 tcache chunk 的 `next` 指针位置（gdb 看 chunk 头）。🥇
- 🟠 Lv4：用 AddressSanitizer 编译跑一遍，让 ASan 报告 UAF 位置。🏆
- 🔴 Lv5：给一段授权 CTF 堆题写"泄露 libc → tcache poisoning → 覆写 hook"利用思路文档。👑

---

# SEC-B3 · 格式化字符串利用（泄露 / 任意写）

## 🎯 目标
学完能讲清 **`printf(user_input)` 的危害**：通过 `%x/%p` 泄露栈内容（泄露 canary/libc/栈地址）、通过 `%n` 向任意地址写值；在**自己程序** 复现泄露并理解修复（`printf("%s", s)` 而非 `printf(s)`）。

## 📋 小白前置
- SEC10（注入本质）、SEC-B1（栈/泄漏）、SEC34（内存）
- 方向1：C9（printf 格式）、PY（字符串格式化）

## 🟢 部分一·最浅层（生活比喻）
`printf` 像"填空打印机"：`printf("我是%s，分%s", name, grade)` 把后面参数填进 `%s`。漏洞是"你把用户写的整张纸直接当模板"——用户写 `%%x %x %x %n`，打印机就把**栈上后面的格子**当参数读出来（泄露），甚至 `%n` 把"已打印字符数"写回某个地址（任意写）。

## 🟡 部分二·动手层（逐字操作）
自己程序复现（合法）：
```c
// fmt_demo.c
#include <stdio.h>
void vuln(){ char buf[64]; gets(buf); printf(buf); }  // 漏洞：buf 当格式串
int main(){ vuln(); return 0; }
```
编译 `gcc -fno-stack-protector -o fmt_demo fmt_demo.c`（仅本地）。运行输入 `%p %p %p %p` → 打印出栈上 4 个值（泄露）。再试 `%n` 概念（仅观察，不真写关键地址）。**修复**：`printf("%s", buf)` —— 用户数据永远当数据不当格式。
```c
void fixed(){ char buf[64]; gets(buf); printf("%s", buf); }  // 安全
```

## 🔵 部分三·原理层（底层发生了什么）
- `printf` 按格式串逐字符扫描，`%x/%p` 从**栈上紧邻格式串的参数位**取 4/8 字节；用户控制格式串 = 控制"读多少格、读哪" → 泄露 canary/返回地址/libc 基址（破 ASLR）。
- `%n` = 把"到目前打印的字符数"写入**下一个参数指向的地址**。控制格式串+栈上地址 → 任意地址写（改返回地址/ GOT / hook）。
- 利用链：leak 基址 → 算目标地址 → `%n` 写 → 控制流。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：格式化字符串是 2000s 经典（如 2000 年 wu-ftpd 漏洞）；现代编译器(Linux `__attribute__((format))`)和静态分析能查。
- 顶级视角：**%n 任意写在现代被缓解**（如 glibc 禁 `%n` 到只读/某些情形），但泄露仍可用。CTF 里 `fmt` 常作"信息泄漏第一步"。

## 🔴 部分五·顶级视角
顶级工程师把 `printf(user_buf)` 列为**代码review 红线**（同 SEC10 SQL 注入一样"数据与代码分离"）；用 `printf("%s", ...)` 或 `fputs`；SAST 工具扫 `printf(buf)` 模式。理解"格式串漏洞 = 又一类执行边界失控"。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 自己程序复现泄露+修复，合法。
- 🚫 不对非授权程序做格式串利用；不写通用 exploit。

## ✅ 验收
交：① fmt_demo 输入 `%p %p %p` 的泄露截图 ② 修复版对比 ③ 一句话"为什么 printf(buf) 危险"。

## ⚠️ 常见坑
- 现代 glibc 对 `%n` 到某些地址拒绝 → 泄露仍可行，利用更难。
- 以为"关了栈保护就没事"——格式串不靠溢出，靠读栈。
- 手疼：仅跑泄露演示即可。

## ➡️ 下一步
**SEC-B4 · ROP 深（Return-Oriented Programming 实战）**

### ⚔️ 闯关路线（SEC-B3）
- 🟢 Lv1：编译 fmt_demo，输入 `%p %p` 看栈泄露。🥉
- 🔵 Lv2：改 `printf("%s",buf)` 修复，验证不再泄露。🥈
- 🟣 Lv3：在自己程序里构造"读 8 个 %p"泄露 canary 位置（观察）。🥇
- 🟠 Lv4：用 `%n` 在**自己变量**上写值，演示任意写原理（不写系统区）。🏆
- 🔴 Lv5：给一个授权 fmt 题写"leak → 算 libc → 改 GOT"利用思路文档。👑

---

# SEC-B4 · ROP 深（Return-Oriented Programming 实战）

## 🎯 目标
学完能讲清 **ROP = 在二进制里找现成 `ret` 结尾的小片段(gadget)，串成"不写新代码也能干坏事"的链**，绕过 DEP/NX（SEC35）；在**自己编译的 ret2win demo + 含 libc 的题** 用 ROPgadget/自己程序构造"调用 system("/bin/sh")"链（仅本地/授权 CTF）。

## 📋 小白前置
- SEC26（Shellcode）、SEC35（DEP/NX/ASLR）、SEC-B1（栈/泄漏）、SEC-B3（泄露）
- 方向3：ASM1（ret/pop）、OS10（系统调用）

## 🟢 部分一·最浅层（生活比喻）
DEP 把"堆/栈"设为不可执行（不能跑你放的 shellcode，SEC26）。ROP 像**不自己写剧本，而是把现成演员（gadget）按顺序叫上台**：每个演员只做一步（如 `pop rdi; ret`、`ret`），串起来就成了"拿 shell"的戏——全程没写一行新可执行代码，只"借用"程序里已有的片段。

## 🟡 部分二·动手层（逐字操作）
ret2win 最小 demo（自己程序）：
```c
// ret2win.c
#include <stdio.h>
void win(){ system("/bin/sh"); }      // 现成的"拿 shell"函数
void vuln(){ char buf[32]; gets(buf); }  // 溢出
int main(){ vuln(); }
```
编译 `gcc -fno-stack-protector -no-pie -o ret2win ret2win.c`（关 PIE 便于初学找 win 地址）。找 gadget：`ROPgadget --binary ret2win | grep "pop rdi"`。构造：padding(40) + win 地址 → 溢出后 `ret` 跳 `win` 得 shell（本地验证 Ctrl+D 退出）。进阶（开 PIE）：先 `%p` leak 出 libc，再 `pop rdi; ret` + `/bin/sh` 地址 + `system` 地址串链。

## 🔵 部分三·原理层（底层发生了什么）
- 栈上布置：`[padding][addr_gadget1][arg][addr_gadget2]...[addr_win]`，每个 `ret` 弹一个地址执行；gadget `pop rdi; ret` 把下一个栈值弹进 `rdi`（system 的参数），再 `ret` 到 `system`。全程**只跳转到已有代码**，违反 NX 的"不可执行"但**利用现有可执行页**——故 NX 拦不住。
- **绕过 ASLR/PIE** = 先 leak 基址（SEC-B1/B3），再用偏移定位 gadget。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：ROP 2007+ 由 Hovav Shacham 系统提出，是 DEP 的直接对策；后演化 **SROP( SigReturn Oriented )**、**JOP( Jump )**、**COP( Call )**。
- 顶级视角：**现代缓解 = 挡 ROP**：① **Stack Canary**(SEC35) 防改返回地址 ② **PIE** 让地址随机 ③ **RELRO/FULL** 锁 GOT ④ **CFG/ CET( 影子栈/ 间接分支追踪 )** 直接验证间接跳转目标是否合法——CET(Intel) 用"影子栈"存真返回地址，ROP 改不了。

## 🔴 部分五·顶级视角
顶级研究员视 ROP 为"**在约束下编程**"：gadget 有限，要凑出所需语义（传参/系统调用）。他们写利用也写对抗：推荐开 **CET/Shadow Stack**、**Full RELRO**、**PIE+ Canary**、用 **control-flow integrity(CFI)**。理解 ROP 才能懂现代 CFI 为何必要——这是漏洞与缓解的永恒博弈。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 自己 ret2win demo + 授权 CTF 练 ROP；本地 `system("/bin/sh")` 仅自己进程合法。
- 🚫 不对非授权程序构造 ROP 利用；不散播通用 exploit。
- 🚫 不碰他人软件。

## ✅ 验收
交：① ret2win 的 ROPgadget 找到 `pop rdi; ret` 截图 ② padding 长度计算过程 ③ 说明 CET/影子栈如何挡 ROP。

## ⚠️ 常见坑
- 偏移算错（padding）→ 崩；用 cyclic 模式定位。
- PIE 下直接用静态地址 → 必先 leak。
- 手疼：ret2win 用 `-no-pie` 先跑通，再学 PIE+leak。

## ➡️ 下一步
**SEC-B5 · 整数溢出与类型混淆（Integer / Type Confusion）**

### ⚔️ 闯关路线（SEC-B4）
- 🟢 Lv1：编译 ret2win，ROPgadget 找 `pop rdi; ret`，构造 padding+win 跳 shell。🥉
- 🔵 Lv2：把 ret2win 加 `-fstack-protector` 重编，说明 canary 如何挡。🥈
- 🟣 Lv3：开 PIE 版，用 `%p` leak 基址后重构造链。🥇
- 🟠 Lv4：用 `checksec` 列出二进制开的保护，对应选打法。🏆
- 🔴 Lv5：给授权 CTF pwn 题写"leak libc → ROP → system"完整思路文档（含 gadget 链）。👑

---

# SEC-B5 · 整数溢出与类型混淆（Integer / Type Confusion）

## 🎯 目标
学完能识别 **整数溢出（运算回绕）/符号错误（signedness）/类型混淆（把 A 类型当 B 用）** 导致的越界；在**自己程序** 复现整数回绕致缓冲区分配过小，并修复（用大数类型/检查）。

## 📋 小白前置
- SEC10（输入校验）、SEC34（内存）、SEC-B1（越界）
- 方向1：C1（整数类型/范围）、C9（数组边界）

## 🟢 部分一·最浅层（生活比喻）
- **整数溢出** = 里程表到顶归零：你算"人数×单价"想分配内存，结果数字太大翻回小数，分配了**过小**的缓冲 → 后面写入越界。
- **符号错误** = 把"欠钱(-1)"当"人数"用，程序以为你要分配 42 亿字节（无符号解释）。
- **类型混淆** = 把"猫"当"狗"喂进只懂狗的函数，函数按狗的结构读，读到猫的奇怪字段 → 越界/逻辑错。

## 🟡 部分二·动手层（逐字操作）
整数回绕 demo（自己程序）：
```c
// intovf_demo.c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
void copy_n(char* src, int n){           // n 应为正数
    if(n < 0) return;                    // 仅防负，未防回绕
    char* buf = malloc(n * sizeof(int)); // 若 n 很大，n*4 回绕成小值
    if(!buf) return;
    memcpy(buf, src, n * sizeof(int));
    free(buf);
}
int main(){ int big = 0x40000001; copy_n("x", big); return 0; }  // n*4 回绕
```
编译 `gcc -g -o intovf_demo intovf_demo.c`；用 ASan(`-fsanitize=address`) 重编跑 → 报 heap-buffer-overflow。修复：用 `size_t` + 检查 `n > MAX` + 用 `calloc`/乘法前判溢出。

## 🔵 部分三·原理层（底层发生了什么）
- C 整数有固定位宽（int 32 位，最大值 2^31-1）。`a*b` 超界**回绕**（无符号）或**未定义行为**（有符号）。攻击者控 `n` 使 `n*sizeof` 回绕成小值 → malloc 小缓冲 → memcpy 大长度 → 堆溢出。
- **符号错误** = `int n` 接收 `-1`，比较 `n < buf_size`(size_t 无符号) 时 `-1` 转成巨大正数 → 绕过长度检查。
- **类型混淆** = C++ 虚函数/JS 引擎里把对象类型字段改掉，后续按错误布局访问 → 越界（如 2018 年多个浏览器 0day）。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：整数/类型混淆是**浏览器/解析器 0day 富矿**（v8/JS 引擎、图像库）。**2021  ForcedEntry** 用类型混淆打 iMessage。
- 顶级修复：用**安全整数类型**（Rust i32 溢出 panic、Go 检查）、关键运算用 `checked_mul`/编译器 `-ftrapv`、输入做**范围+符号双重校验**、内存安全语言根除。

## 🔴 部分五·顶级视角
顶级研究员把整数/类型视为"**类型系统的信任边界**"：错误的类型假设 = 错误内存解释 = 越界。他们推**内存安全语言**、用 **Fuzzing(SEC47)+ sanitizer** 抓这类 bug、code review 重点查"所有 `malloc(size*len)` 是否验溢出"。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 自己程序复现整数回绕+ASan 报告+修复，合法。
- 🚫 不对非授权程序做此类利用。

## ✅ 验收
交：① intovf_demo 的 ASan 报告截图 ② 修复版（size_t+上限+checked）③ 一句话"为何 int*size 会回绕"。

## ⚠️ 常见坑
- 只查 `n<0` 忘了回绕 → 用无符号也要查上界。
- 在 64 位下 `int*int` 回绕需更大数，调试注意位宽。
- 手疼：仅跑 ASan 看报告即可。

## ➡️ 下一步
**SEC-B6 · 二进制防护绕过（Canary / PIE / RELRO / CFG 全景）**

### ⚔️ 闯关路线（SEC-B5）
- 🟢 Lv1：编译 intovf_demo，ASan 跑出 overflow 报告截图。🥉
- 🔵 Lv2：改用 size_t + 上限检查修复。🥈
- 🟣 Lv3：构造"负 n 转无符号变大"的符号错误 demo。🥇
- 🟠 Lv4：用 `-ftrapv` 编译，演示溢出即中止。🏆
- 🔴 Lv5：写一份"二进制整数/类型漏洞自查清单"（乘法前检查/符号一致/用安全语言）。👑

---

# SEC-B6 · 二进制防护全景（Canary / PIE / RELRO / CFG / CET）

## 🎯 目标
学完能**用 `checksec` 读一个二进制的全部防护**，并讲清每项的**绕过思路与残余风险**；理解"防护不是银弹，是抬高攻击成本"。白帽视角：会打也会防。

## 📋 小白前置
- SEC35（DEP/ASLR 基础）、SEC-B1~B5（各类利用）
- 方向3：CMP9（GOT/PLT，关联 RELRO）、OS 内存

## 🟢 部分一·最浅层（生活比喻）
编译选项像给程序穿的"盔甲层"：
- **Canary**（栈保护）= 栈上放根"保险丝"，改了就炸（挡栈溢出改返回地址）。
- **PIE**（地址随机）= 每次起床换门锁位置（挡硬编码地址）。
- **RELRO**（只读 GOT）= 把"函数地址表"焊死，不可改（挡 GOT 覆写）。
- **DEP/NX** = 数据区不可执行（挡 shellcode）。
- **CFG/CET** = 校验每次"跳去哪"是否合法（挡 ROP/JOP）。

## 🟡 部分二·动手层（逐字操作）
装 checksec（`pip install pwntools` 自带 `pwn checksec` 或 `checksec` 工具）。对自己程序跑：
```bash
pwn checksec ret2win
# 输出类似：
# Arch:     amd64-64-little
# RELRO:    Partial RELRO
# Canary:   No (用 -fno-stack-protector 编的)
# NX:       Enabled
# PIE:      No (用 -no-pie)
```
逐项对应：Canary=No → 栈溢出直打；PIE=No → 地址固定可硬编码；RELRO=Partial → GOT 可写（可 GOT 覆写）；NX=Yes → 不能 shellcode，得上 ROP(SEC-B4)。

## 🔵 部分三·原理层（底层发生了什么）
- **Canary** = 函数 prologue 从 `%fs:0x28`(TLS) 取随机值压栈，epilogue 比对，不等则 `__stack_chk_fail`。绕过 = leak canary(SEC-B3) 或 改不了但可改 canary 之后的数据。
- **PIE** = 基址随机，需 leak(SEC-B1/B3)。
- **RELRO** = Full RELRO 在加载时解析完 GOT 并设为只读，挡 GOT 覆写；Partial 仅 `.got.plt` 可写。
- **CFG/CET** = 微软 CFG / Intel CET(影子栈+IBT)：运行时校验间接跳转/返回目标合法性，直接废掉多数 ROP。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 缓解是**层层叠加**：单开一项仍有路，全开极难。但**信息泄漏(SEC-B1/B3)常是破局点**——leak 基址+canary 即破多层。
- 顶级视角：**防护评估 = 列出开的项 → 每项找残余绕过 → 算攻击成本**。现实攻击者优先找"没开的项"或"泄漏源"。

## 🔴 部分五·顶级视角
顶级安全工程师**编译即安全**：默认开 `-fstack-protector-strong -D_FORTIFY_SOURCE=2 -fPIE -Wl,-z,relro,-z,now`、用 CET 硬件、跑 sanitizer CI、依赖内存安全语言。他们理解"防护是成本博弈"，故**多层 + 减攻击面(最小权限) + 持续更新**并举。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 对自己/授权二进制跑 checksec、分析防护，合法。
- 🚫 不对非授权程序做绕过利用；仅学防护与检测。

## ✅ 验收
交：① 自己程序的 checksec 输出截图 ② 逐项说明"该项若关，可用哪节技术打" ③ 写一条编译加固建议。

## ⚠️ 常见坑
- 只看 NX 以为安全 → 忽略 PIE/Canary 状态。
- Partial RELRO 误当 Full → GOT 仍可改。
- 手疼：checksec 输出直接读即可。

## ➡️ 下一步
**SEC-B7 · Fuzzing 进阶（AFL++ / 结构感知 / 语料）**

### ⚔️ 闯关路线（SEC-B6）
- 🟢 Lv1：对自己的 ret2win 跑 `pwn checksec`，逐项读。🥉
- 🔵 Lv2：用 `-fstack-protector-strong` 重编，看 Canary 变 Yes。🥈
- 🟣 Lv3：对开 Full RELRO 的二进制说明 GOT 不可写。🥇
- 🟠 Lv4：列出"某二进制全防护下仍可能的泄漏源"。🏆
- 🔴 Lv5：给团队写"编译加固基线"（flags 清单+CI sanitizer）。👑

---

# SEC-B7 · Fuzzing 进阶（AFL++ / 结构感知 / 持续集成）

## 🎯 目标
学完能在 SEC47 基础上用 **AFL++（现代覆盖率引导 fuzz）** 对自己解析器跑出崩溃，理解**结构感知 fuzz（懂格式）** 与**语料(corpus)工程**；并把 fuzz 接进 CI 做回归防护。仅自己程序/授权开源。

## 📋 小白前置
- SEC47（fuzzing 基础/libFuzzer）
- 方向3：CMP7（编译器插桩）、OS
- 工具：需 AFL++（WSL/容器；或 MSYS2 试装）、clang

## 🟢 部分一·最浅层（生活比喻）
SEC47 的 libFuzzer 是"单机小机器人"。AFL++ 是"升级版机器人军团"：它更会挑输入变异（变异策略多）、懂"这程序解析什么格式就往那变异"（结构感知）、还能 24 小时挂机找深 bug。语料 = 你喂的"好样本"当种子，机器人从种子变异更准。

## 🟡 部分二·动手层（逐字操作）
AFL++ 最小（WSL 或装了 AFL++ 的环境）：
```c
// afl_target.c 同 SEC47 的 LLVMFuzzerTestOneInput
#include <stdint.h>
#include <stddef.h>
#include <string.h>
int LLVMFuzzerTestOneInput(const uint8_t* d, size_t n){
    if(n>4 && !memcmp(d,"PNG",4)){        // 结构感知：只认真 PNG 头
        // 解析宽高字段（故意越界读）
        int w = d[4] | (d[5]<<8);
        volatile char x = d[4+w];          // 若 w 大越界
    }
    return 0;
}
```
编译：`afl-clang-fast -fsanitize=address -o afl_target afl_target.c`（AFL++ 的 clang 包装）。跑：`afl-fuzz -i seeds -o out ./afl_target`。`seeds/` 放一个 `PNG\x10\x00` 种子。几分钟内 ASan 崩溃 → `out/default/crashes/` 存 PoC。

## 🔵 部分三·原理层（底层发生了什么）
- **AFL++** 在编译时插桩记录**边覆盖(edge coverage)**，变异输入保留"走到新边"的，渐进逼近深分支；比 libFuzzer 策略更丰富（Mopt/CMP 扩展）。
- **结构感知 fuzz** = 用 `libprotobuf-mutator` 或自定义 mutator 让变异符"格式语法"（如只产生合法 JSON 子树），命中解析逻辑更快（否则随机字节难触发语义层 bug）。
- **语料工程** = 好种子(corpus)覆盖多格式/边界，加速；持续喂新发现输入回 corpus（通化学习）。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：AFL(2013)→AFL++(社区维护增强)。现实大厂 7×24 fuzz 找 Chrome/OpenSSL 漏洞主力。
- 顶级视角：**fuzz 接 CI**——每次提交自动跑若干分钟 fuzz，回归挡住新 bug；用 **OSS-Fuzz** 给开源项目持续 fuzz。结构感知 + 约束求解(SEC-B8)融合是前沿。

## 🔴 部分五·顶级视角
顶级研究员把 fuzz 当**"缺陷发现基础设施"**：自己项目 fuzz 覆盖率目标 >80%、接 CI、用结构感知打解析器、发现漏洞走负责任披露(SEC100)。理解"fuzz 找不到 ≠ 没有，但能找到绝大多数浅/中层 bug"。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 对自己程序/授权开源项目 fuzz；发现他人漏洞走 SEC100 披露流程。
- 🚫 不对他人服务/网站未授权 fuzz（=攻击）；不散播他人崩溃 PoC。

## ✅ 验收
交：① AFL++ 跑出崩溃的 `crashes/` 文件 + ASan 报告 ② 说明"边覆盖"比"行覆盖"好在哪 ③ 写"fuzz 接 CI"的 YAML 思路。

## ⚠️ 常见坑
- AFL++ 需 clang 插桩，MinGW gcc 不行；用 WSL/容器。
- 没种子或种子太单一 → 覆盖率低，难出 bug；多放边界样本。
- 手疼：用现成 AFL++ docker 镜像跑更省事。

## ➡️ 下一步
**SEC-B8 · 符号执行与污点分析（angr / 约束求解）**

### ⚔️ 闯关路线（SEC-B7）
- 🟢 Lv1：编译 afl_target，放种子跑 AFL++ 出崩溃。🥉
- 🔵 Lv2：用"只认 PNG 头"的结构感知目标，对比随机变异命中率。🥈
- 🟣 Lv3：构造多边界种子（空/超大/畸形）提升覆盖。🥇
- 🟠 Lv4：写一段 CI YAML 在每次 push 跑 60s fuzz。🏆
- 🔴 Lv5：给自己的解析器项目写"fuzz 计划"（目标/语料/CI/披露流程）。👑

---

# SEC-B8 · 符号执行与污点分析（angr / 约束求解）

## 🎯 目标
学完能讲清 **符号执行 = 用符号(而非具体值)跑程序，收集"到达某处需满足的约束"，用 SMT 求解器(Z3)反推输入**；在**自己小程序** 用 angr 自动找到"到达目标分支"的输入（仅本地/授权 CTF）。

## 📋 小白前置
- SEC47（fuzzing，互补）、SEC20（污点分析概念）、SEC-B4（ROP 找路径）
- 方向1：PY（angr 是 Python 库）、逻辑
- 工具：Python `pip install angr z3-solver`

## 🟢 部分一·最浅层（生活比喻）
Fuzzing(SEC47)是"猴子乱敲碰运气"。符号执行是"数学侦探"：它不试具体值，而是说"假设输入是个未知数 X，程序走到第 50 行要求 X>5 且 X*2<20"——把所有约束列出来，交给 Z3 解"满足这些的 X 是多少"。一下就求出能走到目标分支的输入，比乱试精准。

## 🟡 部分二·动手层（逐字操作）
angr 最小（自己程序，合法）：
```python
# find_input.py
import angr, claripy
# 目标：一个函数里 if(x==0xDEADBEEF) 打印 "win"
proj = angr.Project("crackme_demo", auto_load_libs=False)
sym = claripy.BVS("x", 64)              # 64位符号变量
state = proj.factory.entry_state()
# 假设输入读进 sym（简化：直接约束某内存）
simgr = proj.factory.simgr(state)
simgr.explore(find=lambda s: b"win" in s.posix.dumps(1))  # 找打印 win 的状态
if simgr.found:
    print("找到输入:", simgr.found[0].posix.dumps(0))   # 求解 stdin
```
`crackme_demo.c` 自己写个"读 8 字节，等于 magic 则打印 win"。angr 自动求解出那 8 字节（无需爆破）。

## 🔵 部分三·原理层（底层发生了什么）
- **符号执行** = 把程序变量变成**符号表达式**而非具体值，每条路径维护"路径约束"(path constraint)。遇到分支 `if(x>5)` → 产生两条状态，约束分别 `x>5`/`x<=5`。
- **SMT 求解(Z3)** = 当某状态到达"目标"(如 `win` 分支/某地址)，把累积约束丢给 Z3，求一组**满足的具体输入**。
- **污点分析(静态)** = 不执行，直接分析"source→sink 数据流"（SEC20），符号执行是"动态带符号的执行"，互补。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：符号执行 1970s 理论，2000s 实用化（EXE/KLEE/angr）。**优势**：对"深约束分支"比 fuzz 精准；**劣势**：状态爆炸（路径太多）+ 环境建模难（系统调用）。
- 顶级视角：**混合（concolic）** = 具体执行+符号结合（如 Driller 用 fuzz 探路、符号执行 deepen）；**符号执行补 fuzz 的"深分支"短板**。

## 🔴 部分五·顶级视角
顶级研究员用符号执行做：自动漏洞挖掘（找到达危险 sink 的输入）、CTF（crackme/逆向密钥）、测试用例生成。他们理解"状态爆炸是天花板"，用**路径剪枝/摘要/混合 fuzz** 对抗。这也服务 SEC20 审计——自动追 source→sink。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 对自己 crackme/授权 CTF 用 angr；合法。
- 🚫 不对非授权程序做自动化漏洞挖掘利用；不散播求解结果。

## ✅ 验收
交：① angr 求解 crackme 的输入截图 ② 说明"符号执行 vs fuzzing 各自优劣" ③ 一句话"状态爆炸是什么"。

## ⚠️ 常见坑
- angr 分析依赖库慢 → 用 `auto_load_libs=False` 或 `hook` 系统调用。
- crackme 有复杂输入解析 → 状态爆炸，先简化 demo。
- 手疼：用最简单的"读 magic 打印 win"demo 即可。

## ➡️ 下一步
**SEC-B9 · 内核漏洞利用进阶（UAF/竞态/提权链，分析不实践）**

### ⚔️ 闯关路线（SEC-B8）
- 🟢 Lv1：写 crackme_demo，用 angr 求输入打印 win。🥉
- 🔵 Lv2：对比"爆破 vs 符号执行"求解时间。🥈
- 🟣 Lv3：加一个约束分支（x 偶数且 >100），angr 仍能解。🥇
- 🟠 Lv4：用 angr 找"到达某危险函数"的路径（污点+符号）。🏆
- 🔴 Lv5：写"符号执行+fuzz 混合( Driller 思路)用于漏洞挖掘"的方案文档。👑

---

# SEC-B9 · 内核漏洞利用进阶（UAF / 竞态 / 提权链，分析不实践）

## 🎯 目标
学完能理解 **内核级利用的进阶形态**：内核 UAF（释放后内核对象仍被引用）、竞态提权（如 Dirty COW 类）、从漏洞到 **完整提权链（leak→任意读写→改 cred）** 的思路；**仅分析原理，绝不对自己/他人内核加载攻击模块**。

## 📋 小白前置
- SEC46（内核漏洞入门）、SEC38（UAF）、SEC40（竞态）、SEC-B2（堆利用）
- 方向3：OS10（syscall/ring0）、OS14（LKM）、COMP5（特权级）

## 🟢 部分一·最浅层（生活比喻）
用户态利用(SEC-B1~B5)是"在自己房间改东西"。内核利用是"改整栋楼的总电闸"——一旦成功，整栋楼（全系统）听你的。内核 UAF 像"总闸的遥控器退了库但还能用"，竞态像"趁电工换闸时偷偷接根线"。威力最大，所以白帽**只分析、绝不练**。

## 🟡 部分二·动手层（逐字操作）
**仅阅读/画图分析，不加载任何内核模块**。以 **Dirty COW (CVE-2016-5195)** 为案例在笔记还原思路：
1. 漏洞 = `write` 系统调用在 **COW(copy-on-write)** 路径有竞态：先 `madvise(MADV_DONTNEED)` 丢弃映射，再 `write` 触发 COW，时间窗内另一线程可让其写回**只读文件（如 /etc/passwd）**的页。
2. 利用 = 反复竞态把只读系统文件改成可写 → 加 root 用户 → 提权。
3. 修复 = 内核补丁修正 COW 竞态（加锁）。
画"竞态时间窗"示意：线程A `write` 检查权限 → 线程B `madvise` 丢弃 → 线程A 写回（已无权限检查）。

## 🔵 部分三·原理层（底层发生了什么）
- 内核 UAF = 内核对象(如 `struct file`) `kfree` 后，仍有指针引用，攻击者 **spray(堆喷)** 占位该内存，让引用指向伪造对象 → 控制内核数据结构。
- **提权链** = ① 信息泄漏（leak 内核地址）② 任意读写（改内核对象/页表）③ 改当前进程 `cred` 结构 uid=0 → 提权。
- **KASLR/KPTI** 是内核版 ASLR/隔离，增加 leak 难度。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：Dirty COW 影响 2007–2016 几乎所有 Linux；**2021  Dirty Pipe (CVE-2022-0847)** 类似逻辑错。内核竞态/逻辑漏洞比内存破坏更"优雅"。
- 顶级视角：**内核利用 = 对 OS 信任根的攻击**。防护：KASLR/KPTI、SMAP/SMEP(禁内核访问/执行用户内存)、CFG、及时打补丁、用 Rust-for-Linux 根除类漏洞。

## 🔴 部分五·顶级视角
顶级内核研究员分攻防：防御(写 fuzz 找内核 bug SEC47、CFI for kernel)、攻击(授权红队评估)。理解内核=理解 OS 全栈。他们**极度强调打补丁**——因为内核漏洞影响面全系统，且野外利用(0day)极危险。

## 🟠 部分六·安全/合规种子（白帽红线——最高敏）
- 🚫🚫 **绝对不加载任何攻击性内核模块、不对任何真实/虚拟机内核做提权利用试验**（含自己机器，易崩且技术即 rootkit/0day 特征）。
- ✅ 仅**阅读案例、画图分析、写防护建议**。学它是为：懂为何及时更新内核、理解 KPTI/SMAP 防护、做授权评估时识别内核风险。
- 🚫 不碰他人系统；不写/不散播内核 exploit。

## ✅ 验收
交：① Dirty COW 竞态时间窗示意笔记 ② 说明"内核 UAF 为何比用户态更危险（影响全系统）" ③ 写一条"为何要及时更新内核"的防护建议。

## ⚠️ 常见坑
- 真在本机试内核利用 → 极可能崩/启不来，且易踩法律灰区；**仅分析**。
- 把用户态堆喷(SEC-B2)直接当内核堆喷：内核分配器(slab)不同，手法需重学。
- 手疼：案例画图即可，不写一行内核攻击代码。

## ➡️ 下一步
**SEC-B10 · CTF Pwn 解题方法论（全类套路 + 实战节奏）**

### ⚔️ 闯关路线（SEC-B9）
- 🟢 Lv1：读 Dirty COW 案例，画竞态时间窗。🥉
- 🔵 Lv2：说明内核 UAF 提权链三步。🥈
- 🟣 Lv3：列出内核防护 KASLR/KPTI/SMAP/SMEP 各挡什么。🥇
- 🟠 Lv4：写"企业为何要及时更新内核+启用安全启动"建议。🏆
- 🔴 Lv5：写一份"内核漏洞防护基线文档"（更新策略/硬件缓解/监控）。👑

---

# SEC-B10 · CTF Pwn 解题方法论（全类套路 + 实战节奏）

## 🎯 目标
学完能拿到一道 **CTF pwn 题（授权）** 时，按**标准流程**推进：checksec → 逆向后定位漏洞 → 找泄漏 → 构造利用链 → 本地验证 → 远程打；理解各类题型（stack/heap/fmt/rop）对应套路。白帽：仅授权 CTF。

## 📋 小白前置
- SEC-B1~B9（全部利用技术）、SEC27（反编译）、SEC47（fuzz）、SEC-B8（angr）
- 工具：pwntools、Rizin/Cutter、GDB(pwndbg)、AFL++、angr

## 🟢 部分一·最浅层（生活比喻）
CTF pwn 像"官方授权的安全拆弹比赛"：主办方给你一个**故意留洞的二进制**，你要在不破坏规则下"炸出 shell 拿 flag 文件"。流程固定：先看盔甲(checksec)→ 拆开看结构(逆向)→ 找引线(漏洞)→ 接线利用(EXP)→ 本地试→ 远程交。

## 🟡 部分二·动手层（逐字操作）
标准流程伪代码（用自己/授权题）：
```python
from pwn import *
context.arch='amd64'
elf = ELF("./vuln")                  # ① checksec 已做
# ② 逆向(人工+Cutter)定位 vuln 函数、溢出点
# ③ 找泄漏：若程序有 puts(puts@got) 可 leak libc
rop = ROP(elf)
puts_plt = elf.plt['puts']; puts_got = elf.got['puts']
# ④ 构造链：leak -> 算 libc base -> ret2win / system
# ⑤ 本地验证
io = process("./vuln"); io.sendline(payload); io.interactive()
# ⑥ 远程：io = remote("host", port); 同法
```
把 SEC-B1~B5 的技术按题型套：stack→ROP(SEC-B4)、heap→tcache(SEC-B2)、fmt→泄露(SEC-B3)、noflag→angr(SEC-B8)。

## 🔵 部分三·原理层（底层发生了什么）
pwn 题本质是"**给你一个漏洞 + 一个目标(读 flag)**"，你组合已学技术达成。核心通用链：**信息泄漏(破随机) → 控制流劫持(ROP/堆/格式化) → 调用 system/cat flag**。远程与本地差异在 ASLR/ libc 版本，故必须 leak 后按远程 libc 算偏移（用 libc-database 或本地对照）。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 题型演进：早期直接栈溢出 → 现在全防护下需 leak+ROP/堆。新方向：kernel pwn（SEC-B9 分析）、browser pwn（JS 引擎类型混淆 SEC-B5）、vm pwn（自定义虚拟机逃逸）。
- 顶级视角：**pwn 是二进制能力的"考场"**——它逼你把逆向/利用/脚本(pwntools)/调试全部串起来。白帽价值：CTF 练出的能力直接用于**授权渗透、漏洞研究、防护设计**。

## 🔴 部分五·顶级视角
顶级 pwn 选手把每道题当"缓解机制破解工程"：先 checksec 定打法，再用**最小可靠利用**(避免远程不稳定)。他们维护** gadget 库/ libc 库/模板**，并持续学新缓解(CET)的对抗。这也是 OSEE(OffSec 漏洞利用专家)认证的核心能力。

## 🟠 部分六·安全/合规种子（白帽红线）
- ✅ 仅参加**官方授权 CTF**（如 pwnable.kr、CTF 赛事），对自己赛题练，合法且是公认学习途径。
- 🚫 绝对**非授权**服务器/程序做 pwn；不把 CTF 技术用于真实攻击；不散播远程 exploit。

## ✅ 验收
交：① 一套"pwn 题标准解题 checklist"（checksec→逆向→漏洞→泄漏→利用→本地→远程）② 对自己 ret2win 用 pwntools 写通本地 exp ③ 说明"远程为何要先 leak libc"。

## ⚠️ 常见坑
- 本地通远程不通 → libc 版本/偏移不同，必先 leak 远程 libc。
- pwntools `interactive()` 在远程易卡 → 用 `sendline`+`recv` 脚本化。
- 手疼：用 Cutter 看 vuln 函数即可，exp 可只写本地版。

## ➡️ 下一步
**SEC-B11 · 二进制防护工程（编译加固 / sanitizer / 内存安全语言）**

### ⚔️ 闯关路线（SEC-B10）
- 🟢 Lv1：用 pwntools 对自己 ret2win 写本地 exp 跑通。🥉
- 🔵 Lv2：画"pwn 题标准流程"checklist 图。🥈
- 🟣 Lv3：对一道 heap 题(授权)说明用 tcache 还是 fastbin 打法。🥇
- 🟠 Lv4：用 angr 自动解一道 crackme 作为"非溢出类"pwn 补充。🏆
- 🔴 Lv5：写一份"CTF pwn 训练路线图"（从 stack→heap→kernel，配题目源）。👑

---

# SEC-B11 · 二进制防护工程（编译加固 / Sanitizer / 内存安全语言）

## 🎯 目标
学完能从"攻击者"切换回"防御者"：给出一套**二进制防护工程基线**——编译加固 flags、CI 跑 sanitizer/fuzz、优先内存安全语言、依赖最小化；理解"防护是成本博弈，但分层能挡绝大多数"。

## 📋 小白前置
- SEC-B6（防护全景）、SEC47/B7（fuzz）、SEC-B2/B5（漏洞根因）
- 方向10 OPS30（CI/CD）、方向1 C/Rust

## 🟢 部分一·最浅层（生活比喻）
学了一堆"怎么打"，这节教"怎么把自家房子修得打不进"。像装修：① 装多重锁(编译 flags) ② 请保安 24h 巡逻(fuzz/sanitizer CI) ③ 干脆用防火材料盖房(内存安全语言) ④ 少放贵重杂物(减依赖/攻击面)。

## 🟡 部分二·动手层（逐字操作）
给自己 C 项目写加固编译（复制即用）：
```bash
# 加固 flags（gcc/clang 通用）
gcc -O2 -fstack-protector-strong -D_FORTIFY_SOURCE=2 \
    -fPIE -pie -Wl,-z,relro,-z,now -Wall -Wextra \
    -fsanitize=address -g myprog.c -o myprog
# ASan 版本用于测试；生产去掉 -fsanitize 保留其余
```
CI 思路（GitHub Actions 片段）：
```yaml
- name: Build + ASan + Fuzz
  run: |
    gcc -fsanitize=address -g app.c -o app_asan
    afl-fuzz -i seeds -o out ./app_asan   # 或 make fuzz 60s
```
验证：用 SEC-B2 的 uaf_demo 加 ASan 编译 → 运行即报 UAF 位置。

## 🔵 部分三·原理层（底层发生了什么）
- **编译期**：`-fstack-protector-strong`(canary)、`-D_FORTIFY_SOURCE=2`(检查 memcpy/sprintf 边界)、`-fPIE -pie`(ASLR)、`-z relro -z now`(Full RELRO)。
- **运行期**：Sanitizer(ASan/UBSan) 在内存访问处插桩，越界/UB 即时报错；fuzz 持续探 bug。
- **语言层**：Rust/Go 在**编译期**杜绝 UAF/溢出大类（所有权/边界检查），根除而非缓解。

## 🟣 部分四·深挖骨（为什么 & 延伸）
- 顶级视角：**防护是分层 + 减面**：① 默认开全部编译防护 ② CI 跑 sanitizer+fuzz 挡回归 ③ 新组件优先 Rust ④ 依赖最小化(减少被供应链投毒攻击面) ⑤ 及时更新(glibc/内核补丁)。
- 现实：**100% 安全不存在**，但"让攻击成本 >> 收益"即赢。这与 SEC1 威胁建模、方向10 SRE 错误预算同源——安全是持续工程。

## 🔴 部分五·顶级视角
顶级工程师**既懂攻也懂防**，写利用是为了写更好的补丁。他们推"**安全左移**"：漏洞在编译/CI 就拦，而非上线后被 pwn。理解"攻防是同一知识的两面"——这正是白帽的精髓。

## 🟠 部分六·安全/合规种子（白帽红线）
- 本节即"防御落地"，全程合法合规；用 sanitizer/fuzz 保护自己代码。
- 🚫 不把防护知识反转为攻击他人；技术只用于授权/自己的系统。

## ✅ 验收
交：① 自己项目的加固编译命令 + 验证 ASan 抓到 UAF ② 一份"防护基线条目清单"(flags/CI/语言/依赖/更新) ③ 一句话"为何 Rust 能根除 UAF"。

## ⚠️ 常见坑
- 生产开 ASan 跑（性能 2x 慢）→ ASan 仅测试，生产去掉。
- 只开 PIE 忘了 `-pie` 链接 → 实际没随机化。
- 手疼：复制 flags 用即可。

## ➡️ 下一步
**SEC-B12 · 漏洞利用的伦理与负责任披露（白帽边界闭环）**

### ⚔️ 闯关路线（SEC-B11）
- 🟢 Lv1：给自己的 C 项目加全套加固 flags 编译通过。🥉
- 🔵 Lv2：用 ASan 抓到 uaf_demo 的 UAF 位置。🥈
- 🟣 Lv3：写一份"项目防护基线"清单。🥇
- 🟠 Lv4：配一个 GitHub Actions YAML 跑 ASan+fuzz。🏆
- 🔴 Lv5：把现有 C 代码(如 lightning-card 某模块)做编译加固+ sanitizer 审计，输出报告。👑

---

# SEC-B12 · 漏洞利用的伦理与负责任披露（白帽边界闭环）

## 🎯 目标
学完能清晰界定 **"学漏洞利用"与"违法攻击"的边界**，掌握**负责任披露（Responsible Disclosure）/ 赏金计划（Bug Bounty）** 的正规流程；理解白帽黑客的职业道德与法律护栏。这是方向7b 的**价值收口**——能力越大，边界越清。

## 📋 小白前置
- SEC1（安全思维/白帽定义）、方向7b 全部(B1–B11)、方向7c SEC100（披露流程详述）
- 法律常识：中国《网络安全法》《数据安全法》授权测试边界

## 🟢 部分一·最浅层（生活比喻）
你学了"怎么撬锁"(利用)，但**撬锁只该用在：自己家的门、朋友明确请你帮忙开的门、锁匠比赛里主办方给的练习锁**。去撬陌生人/银行的门 = 犯罪。发现锁有设计缺陷 → 正确做法是"告诉厂家怎么修"，而不是"拿去偷东西"——这就是负责任披露。

## 🟡 部分二·动手层（逐字操作）
在自己环境做"合规闭环"演练（合法）：
1. 自己写一个有 UAF(SEC-B2) 的小程序，用 ASan 找到漏洞。
2. 写一份**虚拟漏洞报告**（结构）：
```
标题：demo_app UAF 导致潜在越界写
影响：本地提权(仅自己环境)
复现：ASan 报告 + 触发输入
建议修复：free 后置 NULL + 改用 Rust
```
3. 对照"合法靶场/赏金"：注册一个合法平台（如 **HackTheBox / 漏洞盒子 / 各厂 SRC**，均授权），看其**范围(scope)** 与**披露政策**——只在范围内测。
4. 笔记写："我的利用能力只用于 ① 自己代码 ② 授权靶场 ③ 授权渗透合同 ④ CTF；绝不用于未授权系统。"

## 🔵 部分三·原理层（底层发生了什么）
- **授权边界** = 任何测试必须**书面/平台授权**明确范围（域名/IP/时间），越界即可能违法（中国《刑法》285 条非法侵入/破坏计算机信息系统）。白帽的"白"= 合法授权。
- **负责任披露** = 发现漏洞后**先私下报厂商**、给修复窗口（如 90 天）、修复后或可公开；不公开 0day 给攻击者用。
- **赏金计划(SRC/Bug Bounty)** = 厂商官方悬赏，合规且受保护。

## 🟣 部分四·深挖层（为什么 & 延伸）
- 历史：白帽与黑产技术同源，**区别在于授权与意图**。正规路径：CTF 练手 → 合法靶场(HTB/OverTheWire) → 厂商 SRC 赏金 → 授权渗透测试(持 OSCP/OSEE+合同)。
- 顶级视角：**建立"白帽信用"** —— 合法披露记录、CVE 作者、SRC 排名，是安全职业的资产；违法记录则毁一生。安全社区极度鄙视"未授权攻击/散播 exploit"。

## 🔴 部分五·顶级视角
顶级安全专家把"伦理"当**技术能力的一部分**：他们知道**能力若无边界 = 危险**，故主动把自己锁在授权框架内（合同/平台/法律）。他们推动"安全研究合法化"（如漏洞披露港、安全港条款），让研究者在保护下贡献。这与你"白帽才练"的红线完全一致——是顶级工程师的底线，也是职业生命线。

## 🟠 部分六·安全/合规种子（白帽红线——总结级）
- 🚫 **绝对不**：未授权测试任何系统、入侵他人、散播 exploit、绕过实名/监管、逆向他人商业软件、注入/Hook 他人进程。
- ✅ **只做**：自己代码/自己编译样本、官方授权靶场(HTB 等)、厂商 SRC(在 scope)、授权渗透合同、CTF。
- 技术用于"防护与检测"——你学了利用，是为了**写更安全的代码、做更好的防御、在授权下帮人找洞补洞**。这是"八大方向全主干 + 攻防无阻挡"的真正含义：无阻挡是指**白帽授权范围内**，不是法外。

## ✅ 验收
交：① 一份自己 demo 的"虚拟漏洞报告"（含复现+修复建议）② 列出 3 个合法练习平台及其 scope 要求 ③ 用自己话写"白帽边界一句话"。

## ⚠️ 常见坑
- 以为"技术好就能随便测"——**授权才是白帽的身份证**，无授权=黑产边缘。
- 把 0day 发公开群 → 可能被人拿去攻击，违反披露伦理。
- 手疼：本报告用自己 demo 写，不接触任何真实系统。

## ➡️ 下一步
**方向7c（SEC56+）· 恶意代码 / 渗透红队 / CTF / 云安全 / AIoT**——从"打"延伸到"看恶意、做红队、守云端"。

### ⚔️ 闯关路线（SEC-B12）
- 🟢 Lv1：对自己 UAF demo 写虚拟漏洞报告（含 ASan 复现）。🥉
- 🔵 Lv2：列出 HTB/漏洞盒子/SRC 各平台的 scope 要求。🥈
- 🟣 Lv3：对照《网络安全法》写"授权测试边界清单"。🥇
- 🟠 Lv4：模拟一次"发现漏洞→报 SRC→等修复→公开"时间线。🏆
- 🔴 Lv5：写一份"个人白帽行为准则"（授权范围/披露流程/绝不碰的红线），作为长期职业底线。👑

---

> **方向7b 深度补遗完结（SEC-B1–B12）**：在 SEC23–SEC55 基础上，补入栈迁移/SEH/泄漏、堆利用(tcache/fastbin/UAF)、格式化字符串、ROP 深、整数/类型混淆、防护全景绕过、AFL++/结构感知 fuzz、符号执行 angr、内核利用进阶、CTF pwn 方法论、防护工程、伦理披露 12 节。全部白帽、全部自己/授权样本、全部带 Lv1–Lv5 闯关 + 可运行 Lab。方向7b 现有 33+12=45 节，覆盖从基础逆向到顶级利用研究的完整链路。
