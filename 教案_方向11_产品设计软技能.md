# 教案_方向11_产品设计软技能（PD1–PD17 完整 11 段深教案）

> 本文件覆盖 PD1–PD17，共 17 节，按 11 段深模板 + 实战 Lab 逐节展开。方向11 是"软技能 + 产品设计 + 工程协作"的整合训练，是学员从"会写代码"走向"顶级大佬"的桥梁。
> 学生画像：真·零基础、术后留置针手不便（护手优先：能复制就复制、GUI/网页工具优先、语音输入补位）、时间极充裕、目标成为计算机领域顶级大佬、坚定白帽路线。
> 使用方法：每节先读 🎯→📋→🟢 建立直觉，再照 🟡 动手层逐字操作，做完把结果发我批改，然后回头读 🔵🟣🔴 补理论。卡住随时喊"卡在第 X 步"。
> 工具链说明：浏览器用 Figma 网页版（figma.com，免安装）；文档用 Markdown 在 VS Code 写；命令在 Git Bash 跑（路径用 `/c/Users/qq/WorkBuddy/...`）；作品集实战以你已生成的 `作品集_HR成品版/index.html` 为真实案例。
> 白帽红线贯穿全案：只做合法合规的产品设计与技术表达；简历与作品集不虚构、不冒充、不盗用他人成果；所有"安全产品思维"仅在授权与自我保护前提下讨论。

---

# PD1 · UI 原则 CRAP（对比 / 对齐 / 亲密 / 重复）

## 🎯 目标
学完这节，你能用四条最基础的设计原则（Contrast 对比、Repetition 重复、Alignment 对齐、Proximity 亲密）评价任何一个网页/App 界面"为什么丑、怎么改"。你将用 Figma 网页版亲手做一张"好 vs 差"的名片对比图，并能在自己的作品集站里发现可改进点。

## 📋 小白前置
无硬性前置，但建议你先扫一眼自己作品集 `作品集_HR成品版/index.html`（就在 `C:\Users\qq\WorkBuddy\2026-08-26-13-18-54\作品集_HR成品版\`）。本方向是"软技能"，和写代码互补；可并行推进方向1a（Python）练手感。

## 🟢 部分一·最浅层（生活比喻）
想象你在收拾书桌：重要的书（对比）要放到最显眼、和杂物拉开差距的位置；同类的笔筒、便签（重复）排成一套风格才不乱；所有东西都要贴着一条隐形基准线摆齐（对齐）；相关的回形针和订书机要挨在一起（亲密），别把订书机扔到房间另一头。界面设计也一样——这四条就是"让信息有秩序"的四条铁律，记一个英文缩写 **CRAP**（别被字面吓到，它是 Contrast/Repetition/Alignment/Proximity 的首字母）。

## 🟡 部分二·动手层（逐字操作；浏览器 + Figma 网页版）
1. 打开浏览器，访问 `https://www.figma.com/`，点右上角 **Sign up**（用微软/谷歌账号登录最省事，避免手打太多）。登录后会进入 Figma 文件列表。
2. 点左上角 **+ New design file**，自动进入画板编辑界面。左侧是图层面板，右侧是属性面板，中间是画板。
3. 我们做一个"反面教材"：在中间画板里，用左侧工具栏的 **T（文本）** 工具点两下，输入"王承泽 作品集"和"全栈+逆向安全"。把两行字的颜色都设成灰色、字号都设成 14，左右乱放（不划线）。这就是"没有对比、没有对齐"的丑例子。
4. 再复制一个画板（选中画板按 `Ctrl+C`/`Ctrl+V`）：把标题改成 28 号白色加粗、正文 14 号灰；所有元素左边缘对齐到同一条竖线（拖拽时会出现红色对齐参考线）；标题和正文上下贴近、和底部联系方式拉开距离。这就是"应用 CRAP"的好例子。
5. 把两个画板并排，导出对比图：选中两个画板 → 右侧面板点 **Export** → 格式 PNG → 点 **Export**。下载到桌面。

### 🧪 实战 Lab（PD1）
打开你的 `作品集_HR成品版/index.html`，用浏览器"另存为图片"或截图首屏。对照 CRAP 四条逐条打分（每条 0–2 分，共 8 分），写 3 句话说明"哪条最弱、怎么改"。把截图和打分发我。

## 🔵 部分三·原理层（底层发生了什么）
Figma 是"基于云的矢量设计工具"：你拖的每一个元素在内部都是一条带坐标（x,y）、尺寸、颜色、字体属性的 JSON 数据，浏览器实时渲染成画面。对齐参考线是 Figma 在检测"元素边缘/中心是否落在同一像素列/行"给出的视觉提示；"对比"本质是拉开**视觉权重差**（大小、色相、明暗），让眼睛先看到最重要的。"亲密"利用了格式塔心理学（Gestalt）的"相近性原则"——人会自动把距离近的东西归为一组。"重复"则是通过统一的视觉语言（同一套圆角、同一色板）降低认知负荷。

## 🟣 部分四·深挖层（为什么这样设计、历史、顶级才追问的点）
CRAP 由设计师 Robin Williams 在《The Non-Designer's Design Book》中系统总结，它脱胎于 20 世纪初包豪斯（Bauhaus）"形式追随功能"与现代网格系统（Grid System，如瑞士国际主义风格）。顶级工程师/产品人会追问：CRAP 是"充分条件"吗？不是——它解决"清晰"，但不解决"好用"（可用性要靠 PD3/PD4）。在代码侧，"重复"对应 DRY 原则（Don't Repeat Yourself，方向1 PY12 模块与包），设计侧的"重复"却恰恰是"要有意识地复用同一套视觉 token"。这两者不矛盾：工程 DRY 是减少代码重复，设计重复是增加视觉一致性——方向相反但目标都是"降低混乱"。

## 🔴 部分五·顶级视角
顶级大佬做产品时，CRAP 已经是肌肉记忆。更重要的是：他们把 CRAP 上升到"设计系统"（PD6）层面，用 token（设计变量）固化对比层级（如 `text-primary`/`text-secondary`）和对齐栅格（如 8px baseline grid）。在写代码（方向6 前端）时，CSS 的 `display:flex`、`gap`、`align-items` 就是"对齐+亲密"的工程实现；`rem` 单位 + 设计 token 就是"重复"的工程实现。你作品集站里的 `assets/style.css` 如果用了统一的 CSS 变量（`:root { --accent: ... }`），就已经在无意中实践"重复"原则——后面 PD6 会把它系统化。

## 🟠 部分六·安全/合规种子（白帽视角）
设计本身不违法，但"模仿/盗用他人 UI"要警惕：复制别人网站的视觉是侵权风险；你在 Figma 社区下载的模板、图标，必须看清楚授权（CC0/免费可商用 vs 需署名 vs 禁止商用）。白帽产品人的底线是：**自己原创 + 合法素材 + 注明来源**。绝不用"扒别人站"冒充自己作品——这既是法律红线，也违背你"简历不虚构"的承诺。

## ✅ 验收
交：① Figma 导出的"好 vs 差"对比 PNG；② 对你作品集首屏的 CRAP 四条打分（0–8）与 3 句改进说明。两条都齐算过关。

## ⚠️ 常见坑
- Figma 网页版需要联网；卡顿时刷新即可，文件存在云端不会丢。
- 新手最爱"五颜六色全用上"——那叫"无对比"，不是"有对比"。对比是"拉开差距"，不是"加更多颜色"。
- 留过针手疼就停，截图/复制代替手打。
- 别把画板坐标当像素艺术精细调——先抓"整体秩序"，细节后面再说。

## ➡️ 下一步
PD2 · 色彩 / 排版——在 CRAP 的"对比/重复"之上，学会用色轮和字体层级做出专业感，并直接优化你作品集的配色与标题层级。

---

# PD2 · 色彩 / 排版（色轮 / 层级 / 可读性）

## 🎯 目标
学完这节，你能：① 用色轮选出"和谐且对比足够"的配色；② 建立一套文字层级（标题/副标题/正文/注释）；③ 直接改进你作品集站的 `--accent` 主色与中文字体栈；④ 用 Figma 网页版做一张"配色规范卡"。

## 📋 小白前置
需先懂 PD1（CRAP 四条，尤其是"对比"）。建议你同时开着 `作品集_HR成品版/assets/style.css` 对照看现有配色变量。

## 🟢 部分一·最浅层（生活比喻）
色彩像做菜调味：主色是"主菜味道"（如你的品牌色蓝），辅色是"配菜"（灰、白），强调色是"辣椒"（只点在最关键的按钮上，多了就呛）。色轮就像调色盘，隔得远的两种颜色（互补色，如蓝 vs 橙）放一起最跳、最容易看清；挨着的（邻近色，如蓝 vs 绿）最和谐但不抢眼。排版像排课桌：校长讲话用大喇叭（大标题），老师通知用中号（副标题），学生细则用小字（正文）——层级清楚，谁都不会读错重点。

## 🟡 部分二·动手层（逐字操作）
1. 打开 `https://www.figma.com/`，新建或打开上节的 design file。
2. 在右侧属性面板点 **Fill** 旁的小方块，输入你作品集现有主色（打开 `style.css` 找 `--accent` 的值，例如 `#6c8cff`），把它设为画板背景。
3. 用 **T** 工具放四行字：H1「王承泽 作品集」(32/加粗/白)、H2「全栈 + 逆向安全」(20/常规/浅灰)、Body「我能从零做出可运行系统」(14/常规/灰)、Caption「© 2026」(12/浅灰)。这就是四级文字层级。
4. 做"对比检查"：选中 H1 和 Body，右侧看两者颜色对比度。Figma 不会直接给 WCAG 数值，我们用在线工具：访问 `https://webaim.org/resources/contrastchecker/`，输入前景色和背景色，看是否达到 **4.5:1（正文）** / **3:1（大字）**。把结果记下来。
5. 导出一张"色彩规范卡"（主色、辅色、强调色各一块 + 四级文字示例）PNG。

### 🧪 实战 Lab（PD2）
打开 `作品集_HR成品版/assets/style.css`，找到 `:root` 里的颜色变量，抄下来。用 WebAIM 对比检查器逐对验证（如白字 vs 深色背景、灰字 vs 白背景）。若某对低于 4.5:1，提出一个新色值（可让 AI 帮你生成邻近色），写进一份 `配色改进.md`。把规范卡 PNG + 改进 md 发我。

## 🔵 部分三·原理层（底层发生了什么）
屏幕上的颜色是 RGB 三原色光叠加（每个通道 0–255）。色轮是牛顿把光谱弯成环的产物；"互补色"在色轮上相隔 180°，放一起时边缘会产生"同时对比"的视觉张カ，所以最醒目。"色相/饱和度/明度（HSL）"是比 RGB 更贴近人脑的模型：H 是颜色种类，S 是鲜艳度，L 是亮暗。文字层级本质是**字号 + 字重 + 颜色**三者的组合对比；可读性取决于"前景-背景对比度"和"字体本身的 x-height（小写 x 的高度占比）"——无衬线字体（如思源黑体）屏幕可读性通常优于衬线体。

## 🟣 部分四·深挖层（为什么、历史、延伸）
1931 年 CIE 定下色度图，现代 sRGB 是 1996 年微软+惠普推的屏幕标准——这就是为什么你在 Figma 选的 `#6c8cff` 在不同设备上"看起来差不多但不完全一样"（色彩管理）。WCAG（Web Content Accessibility Guidelines）把对比度量化成数学要求，这是"可访问性"（PD8）的硬指标。顶级工程师追问：为什么用 HSL 比 RGB 好调主题色？因为改 `L` 就能整体调亮暗而不串色——这也正是"暗色模式"切换的原理（你作品集里 `data-theme='dark'` 就是靠换一组变量值实现的）。中文字体栈要写 `"PingFang SC","Microsoft YaHei",sans-serif` 做跨平台回退，否则 Windows 上可能掉成难看的默认字体。

## 🔴 部分五·顶级视角
顶级产品/前端会把"配色 + 字体"固化成设计 token（PD6）：`color.primary`、`color.surface`、`text.h1`…… 在代码侧就是 CSS 变量或 TS 常量。他们极其重视"可读性即尊重用户"——对比度不够不是"审美问题"而是"把一部分人挡在门外"（PD8 延伸）。在可视化（你后续会做图表）里，配色还要考虑"色盲友好"：避免红绿对比，改用蓝橙。你作品集的技能条、统计卡片，如果配色来自一套 token，后面接设计系统（PD6）就能无缝升级。

## 🟠 部分六·安全/合规种子（白帽视角）
配色/字体素材的授权要清白：Google Fonts（如 Noto Sans SC）是开源可商用；某些"精美中文字体"是商用收费的，用到作品集站发布即侵权。白帽产品人不偷不冒充——字体、图标（如 Lucide、Heroicons 是 MIT）用开源的，并在页脚/致谢里标注。绝不扒别人站的配色说成自己"设计"的。

## ✅ 验收
交：① Figma 导出的"色彩规范卡"PNG；② `配色改进.md`（列出原变量、WebAIM 对比结果、改进建议）。两条齐算过关。

## ⚠️ 常见坑
- 不要"为了好看"用浅灰字配白底——美观但对比度崩，可读性归零。
- 中文字体别只写英文 `sans-serif`，Windows/mac 渲染差异大，务必带中文回退字体。
- 强调色用太多 = 没有强调。原则：**一个页面只一种强调色，只点在 1–2 个最关键动作上**。
- 手疼就截图/复制，别硬调像素。

## ➡️ 下一步
PD3 · UX 心智模型（认知 / 可用性）——从"看起来对"走向"用起来对"，理解用户大脑如何预判界面。

---

# PD3 · UX 心智模型（认知 / 可用性）

## 🎯 目标
学完这节，你能解释"为什么用户会在你的作品集站里迷路"，并用"心智模型"方法找出 3 个可用性卡点，写出改进清单。你将第一次把"设计"和"用户脑子里的预期"连起来。

## 📋 小白前置
需先懂 PD1（对比/秩序）、PD2（层级/可读性）。建议把你的作品集站打开，边读边对照。

## 🟢 部分一·最浅层（生活比喻）
心智模型就是"用户脑子里以为东西是怎么工作的"。比如你走进一家超市，默认"入口进、推车在门口、收银在出口"——如果老板把收银台藏在生鲜区，你就会懵。用户打开一个网站，脑子里也有套"预期地图"：点 logo 回首页、右上角有菜单、蓝色字能点。如果你的设计和他的预期对不上，他就"卡住"。可用性就是"别让用户动脑子猜"。

## 🟡 部分二·动手层（逐字操作）
1. 浏览器打开你的 `作品集_HR成品版/index.html`（双击文件或拖进浏览器）。
2. 拿出一张纸（或用系统"画图"截图），做"认知走查（Cognitive Walkthrough）"：假装你是一个**不认识你的 HR**，执行三个任务：
   - 任务 A：找到你的"最拿得出手的项目"在哪里点开。
   - 任务 B：找到你的邮箱/联系方式。
   - 任务 C：切换明暗主题。
3. 每步记录：① 你预期点哪；② 实际点哪；③ 预期和现实是否一致。把"不一致"的步记下来。
4. 用 Figma 网页版把这三个卡点画成"问题便签"（黄色便签 = 卡点，绿色 = 还行），贴在一张画板上。

### 🧪 实战 Lab（PD3）
写一份 `UX走查.md`：列出 3 个你（或假设 HR）在作品集里"预期落空"的地方，每个给一句改进建议（如"联系方式只在页脚，应在导航栏也放一个邮箱图标"）。把 Figma 便签画板 PNG + md 发我。

## 🔵 部分三·原理层（底层发生了什么）
"心智模型"来自认知心理学（Norman《设计心理学》）。用户基于过去经验在脑中建立"这个世界怎么响应我"的模型；界面如果符合模型，学习成本≈0。可用性的经典定义（ISO 9241）：**有效性（做对没）、效率（多快）、满意度**。当界面违反预期，用户会进入"模式错误（Mode Error）"——以为自己在 A 模式其实在 B 模式（如以为搜索框能搜站内其实只能搜项目名）。你的作品集导航 `#projects/#honors/#skills/#compliance` 是锚点跳转，符合"导航预期"，这是好的；但若 HR 找不到邮箱，就是"信息架构"（PD 后续）没把高频动作放显眼处。

## 🟣 部分四·深挖层（为什么、历史、延伸）
Don Norman 在 1988 年提出"可供性（Affordance）"：门上平板暗示"推"、把手暗示"拉"。好的 UI 元素自带"我能在这做什么"的暗示——蓝色下划线文字暗示"可点"。顶级工程师追问：心智模型能"被训练"吗？能——一旦用户习惯你的产品（如 VS Code 的 `Ctrl+P` 快速打开），就会建立新模型；但跨产品时仍依赖"通用惯例"（logo 回首页、购物车在右上）。这引出"一致性"原则：你的作品集和主流作品集站惯例越像，HR 越不用学。这也是为什么"自定义怪异交互"在作品集里是减分项。

## 🔴 部分五·顶级视角
顶级大佬做产品靠"可用性度量"而非"我觉得"：用任务完成率、时间-on-task、SUS（系统可用性量表，10 题打分）量化。在方向6 前端里，UX 直接驱动技术决策（比如首屏加载慢 → 用户心智模型里"这站挂了"直接关掉 → 你必须做性能优化 FE14）。你作品集的 `stats` 数据卡片、"切换明暗"按钮（已用 `toggleTheme()` 实现）都是对心智模型的尊重——用户预期"有暗色模式"你就给了。后续 PD9（用户研究）会把这种"自我走查"升级成"真找用户测"。

## 🟠 部分六·安全/合规种子（白帽视角）
可用性测试本身完全合法且值得提倡。但要守住：① 测试时收集的用户反馈/录屏，若含他人个人信息（如 HR 真实姓名）需脱敏、不公开；② 绝用"暗模式"误导用户（如伪装成系统弹窗骗点击），那是社会工程学滥用（方向7 SEC77 是教育防御视角，不是教你骗人）；③ 作品集展示真实能力，不靠"伪造成就"骗取信任——心智模型里"这人很强"必须建立在真实作品上。

## ✅ 验收
交：① Figma 三个卡点便签画板 PNG；② `UX走查.md`（3 个卡点 + 改进建议）。齐算过关。

## ⚠️ 常见坑
- 自我走查容易"自己知道藏在哪就以为别人知道"——务必假装第一次来的 HR。
- 心智模型不是"用户笨"，是"设计没接住预期"。归因要落在设计上。
- 留过针就停，截图/口述代替长篇打字。

## ➡️ 下一步
PD4 · 交互设计（状态 / 反馈）——把"用户预期"落成具体的"点下去会发生什么"，覆盖按钮、表单、加载、错误的每一种状态。

---

# PD4 · 交互设计（状态 / 反馈）

## 🎯 目标
学完这节，你能为作品集站里的任意一个交互（如"切换明暗""提交联系表单"）列出**完整状态机**：默认态、悬停态、按下态、加载态、成功态、错误态、禁用态，并能在 Figma 里画出关键状态，知道在代码里对应哪些 CSS/JS。

## 📋 小白前置
需先懂 PD3（心智模型/预期）、PD1（对比让状态可辨）。建议开着作品集 `assets/style.css` 与首页 `<script>` 里的 `toggleTheme()` 对照。

## 🟢 部分一·最浅层（生活比喻）
交互就像你和自动门的对话：你走近（悬停/接近）→ 门感应（反馈：灯亮或轻微响动）→ 你推（按下/触发）→ 门开（成功态）→ 如果门卡住会"嘀嘀"报警（错误态）。好的交互每一步都给你"我收到你的动作了"的反馈。如果一个按钮你点了没任何反应，就像对着没声音的电视遥控——你会怀疑"坏了吗？是我没点到？"

## 🟡 部分二·动手层（逐字操作）
1. 打开作品集首页，用浏览器 F12 开发者工具（右键→检查），点左上角"选取元素"箭头，点一下"切换明暗"按钮 `◐`。
2. 观察：右侧 Styles 面板里 `:hover`、`:active` 伪类有没有定义？你的按钮有 `onclick="toggleTheme()"`——点下去会切换 `data-theme`。这就是一个完整状态机：默认(亮) ↔ 按下(切到暗) ↔ 再按(切回亮)。
3. 用 Figma 网页版画"联系表单"的 4 个状态：① 默认（空框 + 灰按钮）；② 聚焦（框变蓝边）；③ 提交中（按钮显示"发送中…"转圈）；④ 成功（显示绿勾"已收到"）。每个状态一张小卡片并排。
4. 在 Figma 里给"悬停态"加一点变化（如按钮颜色加深、轻微放大），导出状态总览 PNG。

### 🧪 实战 Lab（PD4）
挑你作品集里"切换明暗"按钮，写一份 `交互状态表.md`：列出它当前有哪几种状态（默认/悬停/按下/禁用？），哪些缺失（比如有没有"禁用态"？加载态用不到但悬停态有没有？）。对照 Figma 画的"联系表单 4 态"，给作品集提 2 条交互补全建议。把状态总览 PNG + md 发我。

## 🔵 部分三·原理层（底层发生了什么）
交互状态本质是**有限状态机（FSM）**：一组状态 + 触发事件 + 状态转移。浏览器把鼠标/键盘事件（mouseover/click/focus）派发到 DOM 元素，CSS 伪类 `:hover/:active/:focus/:disabled` 是声明式地描述"在某状态下长什么样"，JS 的 `onclick`/`addEventListener` 是命令式地描述"事件发生时执行什么"。你作品集的 `toggleTheme()` 用 `localStorage.setItem('ptheme', 'dark')` 把状态**持久化**——这很重要：用户下次打开还是暗色，符合"预期被记住"。缺反馈的状态会让用户重复点击 → 引发重复提交（方向6 BE20 支付幂等性就是防这个）。

## 🟣 部分四·深挖层（为什么、历史、延伸）
施乐帕克（Xerox PARC）70 年代提出"所见即所得（WYSIWYG）"和即时反馈哲学，后来被苹果/微软发扬。可用性工程里有个铁律：**任何超过 1 秒的操作都必须给进度反馈**（否则用户以为卡死）。顶级工程师追问：状态爆炸怎么办？一个复杂表单可能有几十种组合状态——这时要用"状态归一"：用数据驱动 UI（如 React 的 `status` 变量决定渲染分支，FE10），而不是写几十个 if。你作品集是原生 HTML/JS，状态少尚可控；若以后用框架（方向6 FE9/FE10），状态管理（FE11 Redux/Pinia）就是这节思想的工程化。

## 🔴 部分五·顶级视角
顶级大佬把"反馈"当性能问题来对待：按钮点击 100ms 内必须有视觉响应（否则感知卡顿）；错误态要给"可行动的建议"（不说"出错了"，而说"邮箱格式不对，应像 name@x.com"）。在方向7 安全里，"错误反馈"还有合规维度：登录失败提示要写成"用户名或密码错误"而非"密码错误"，避免泄露哪个错了（SEC6 会话管理）。你作品集的 `toggleTheme` 已是好范式——零延迟、零歧义。把这种"状态思维"带到所有交互，你的产品会显得"贵"。

## 🟠 部分六·安全/合规种子（白帽视角）
交互设计里藏着安全：① 错误信息不能泄露敏感线索（上面登录例子）；② "禁用态"用于防止未授权操作（如未登录时提交按钮禁用）；③ 绝不设计"诱导点击"（dark pattern，如伪装成关闭按钮的广告）——那是合规红线（各国已有反 dark pattern 立法）。白帽产品人做"诚实的交互"：按钮说什么就做什么，不骗、不藏。

## ✅ 验收
交：① Figma "联系表单 4 态"总览 PNG；② `交互状态表.md`（作品集切换按钮的状态盘点 + 2 条补全建议）。齐算过关。

## ⚠️ 常见坑
- 只画"默认态"不做其他态 = 交互设计只做了一半。HR/用户永远在"过程态"里迷茫。
- 反馈要"即时且可辨"：颜色变化 + 形态变化双保险（色盲用户也看得见）。
- 手疼用截图和复制，别逐字手敲 CSS。

## ➡️ 下一步
PD5 · Figma 实操（组件 / 自动布局）——正式把 Figma 网页版用熟，做出可复用的按钮组件和自动布局列表，直接服务你作品集的视觉资产。

---

# PD5 · Figma 实操（组件 / 自动布局）

## 🎯 目标
学完这节，你能在 Figma 网页版里：① 创建一个可复用"按钮组件"并做亮/暗两种变体；② 用 Auto Layout（自动布局）做一个自适应项目卡片列表；③ 把这些设计资产导出，作为你作品集站 `assets/` 的视觉参考或直接转成 SVG 用。这是你"设计工具链"的正式毕业。

## 📋 小白前置
需先懂 PD1（CRAP）、PD2（色彩/层级）、PD4（状态）。最好已登录 figma.com（PD1 建过文件可直接用）。

## 🟢 部分一·最浅层（生活比喻）
组件就像"乐高积木模具"：你雕好一个标准积木（按钮），之后每次要按钮都从模具里"印"一个出来——改了模具，所有印出来的按钮一起变。自动布局像"能自动排队的传送带"：你往里丢 5 张卡，它自动等距排好，加一张就自动让位，不用你手动拖。

## 🟡 部分二·动手层（逐字操作）
1. 打开 `https://www.figma.com/`，进入你的 design file。
2. **做按钮组件**：用 **R（矩形）** 画一个 120×40 圆角 8 的矩形，填你的主色 `#6c8cff`；上面用 **T** 写"查看项目"白色 14 号居中。选中矩形和文字，按 `Ctrl+G` 编组，然后点顶部菜单 **Create Component**（或 `Ctrl+Alt+K`）——它变成紫色描边的主组件。
3. **做变体**：在右侧面板找到 **Variants**，点 `+` 加一个变体，把新变体的填充改成暗色背景 + 浅字（模拟暗色模式按钮）。现在你有了 `Button / light` 和 `Button / dark` 两个状态。
4. **自动布局列表**：用 **F（画板）** 画一个 320×200 卡片，里面放标题/描述占位文字。选中卡片，点右侧 **Auto layout**（或 `Shift+A`），设 Padding 16、Gap 8、Direction 纵向。然后 `Alt+拖拽` 复制出 3 张卡，把它们一起选中再套一层横向 Auto layout（Gap 16）。试着改第一张卡的标题文字，看其它卡是否保持间距不变。
5. **导出**：选中按钮组件 → 右侧 **Export** → SVG；选中卡片列表 → Export PNG @2x。保存到桌面 `figma导出/`。

### 🧪 实战 Lab（PD5）
把你作品集首页的"stats 数据卡片"（hero 下那排统计）在 Figma 里用 Auto layout 重做一版：3–4 个卡片，每个含"数字 + 标签"，间距统一。导出 PNG，并对比你站点现有实现哪里可以改（如统一圆角、统一间距）。把 Figma PNG + 一句对比说明发我。

## 🔵 部分三·原理层（底层发生了什么）
Figma 文件本质是 indesign 的云版本：每个节点是一个对象（node），带 `type`、`x/y/w/h`、`fills`、`componentId` 等属性。组件（Component）是通过 `componentId` 建立的"主-实例"引用关系，改主组件 → 引擎遍历所有实例同步属性（这背后是"观察者模式"，方向1 PY14 装饰器/观察者思想同源）。Auto Layout 是 Figma 在节点上附加的 `layoutMode/flex` 元数据，渲染时按 flex 算法（和方向6 FE3 CSS flex 一模一样）计算子元素位置——所以你在 Figma 里排的 flex，前端用 CSS flex 能 1:1 还原。导出 SVG 是把矢量节点序列化成 `<svg><rect>...</svg>` 文本，可直接嵌网页。

## 🟣 部分四·深挖层（为什么、历史、延伸）
Figma（2016）颠覆了 Sketch（本地文件）靠"浏览器 + 多人实时协作 + 矢量引擎用 WebGL 渲染"取胜——它证明了"重工具也能跑在网页里"，和你作品集"纯静态网页"理念一致。顶级工程师追问：设计稿和代码怎么不"两张皮"？答案是 **Design Token + 代码生成**（PD6）：把 Figma 里的颜色/间距变量通过插件（如 Tokens Studio）导出成 CSS 变量，前端直接 import。这样设计师改主色，代码自动同步，告别"设计稿是蓝的、上线是绿的"。你作品集的 `:root` 变量其实已经是 token 雏形。

## 🔴 部分五·顶级视角
顶级团队里，Figma 不是"美工画图"，而是**单一事实源（Single Source of Truth）**：产品/设计/前端都对着同一份文件沟通，用 FigJam（白板）做脑暴、用 Figma 做高保真、用 Dev Mode 让前端直接拷 CSS。组件库（Component Library）是全公司的资产，新页面必须"拼装已有组件"而非"重新画"——这直接对应工程的"不要重复造轮子"。你在 PD5 做的按钮组件，正是这种思维的迷你版。后续 PD6 设计系统会把它升级成带 token 的完整库。

## 🟠 部分六·安全/合规种子（白帽视角）
Figma 社区（Community）里有海量免费组件/图标，但**授权各异**：有的 CC0（随意用）、有的要求署名、有的禁止商用。下载前看清许可证，在作品集页脚/致谢标注来源。绝不把别人的付费组件扒来当自己"原创设计"——这既侵权也违背你"作品集不盗用"的承诺。导出的 SVG 也要注意：别把别人的 logo 矢量图直接挪用冒充自己品牌。

## ✅ 验收
交：① Figma 导出的"按钮组件（亮/暗变体）"SVG 或 PNG；② 用 Auto layout 做的"项目卡片列表/数据卡片"PNG；③ 与作品集现有实现的 1 句对比。齐算过关。

## ⚠️ 常见坑
- 忘记先"Create Component"就直接复制，结果改一个其它不变——那只是普通复制，不是组件。
- Auto layout 后元素"拖不动"是正常的（被布局接管），要改就在右侧面板调 Padding/Gap。
- 网页版偶发卡，刷新保命；文件在云端。
- 手疼多用键盘快捷键（`Shift+A`、`Ctrl+Alt+K`），少鼠标精细拖。

## ➡️ 下一步
PD6 · 设计系统 / Token（一致性 / 演进）——把你零散的组件和颜色升级成一套"会进化"的设计系统，直接映射到作品集的 CSS 变量。

---

# PD6 · 设计系统 / Token（一致性 / 演进）

## 🎯 目标
学完这节，你能定义一套"设计 Token"（颜色/间距/圆角/字号的命名变量），并把它**直接落地到你作品集的 `assets/style.css`**：用 `:root` 变量统一管理，做到"改一处、全站变"。你将理解为什么大厂（Google Material、腾讯 TDesign）都要搞设计系统。

## 📋 小白前置
需先懂 PD5（组件/Auto layout）、PD2（色彩/层级）。必须开着 `作品集_HR成品版/assets/style.css`。

## 🟢 部分一·最浅层（生活比喻）
设计系统像"公司的统一制服规范"：规定所有人衬衫是某几种蓝、纽扣间距多少、logo 绣在哪。这样不管谁来做衣服，看上都像"一家公司的人"。Token 就是这本规范的"填空条目"：不是写"那个蓝"，而是写"主色 = #6c8cff"——以后换色只改这一条，所有制服自动换新。

## 🟡 部分二·动手层（逐字操作）
1. 用 VS Code 打开 `作品集_HR成品版/assets/style.css`。先看现有 `:root` 里有没有变量（你站里已有 `--accent` 之类）。
2. 在 `:root` 顶部**补一套完整 token**（复制下面这段粘进去，护手优先不手打）：

```css
:root{
  --color-primary:#6c8cff;
  --color-surface:#ffffff;
  --color-text:#1a1a1a;
  --color-text-muted:#6b7280;
  --space-1:4px; --space-2:8px; --space-3:16px; --space-4:24px;
  --radius-1:6px; --radius-2:12px;
  --text-h1:32px; --text-h2:20px; --text-body:14px;
}
[data-theme='dark']{
  --color-surface:#0f1115;
  --color-text:#e8e8e8;
  --color-text-muted:#9aa3b2;
}
```

3. 把 CSS 里散落的硬编码颜色（如 `color:#333`）逐步替换成 `var(--color-text)`。每改一处，浏览器刷新看效果是否一致。
4. 用 Git Bash 验证文件还在（见下节 PD14 也会用）：

```bash
cd /c/Users/qq/WorkBuddy/2026-08-26-13-18-54/作品集_HR成品版
ls assets/
```

### 🧪 实战 Lab（PD6）
在你的 `style.css` 里新增 `--radius-3:20px`，并把它用在"hero 卡片"或"stats 卡片"上（找一个 `.card` 或类似选择器加上 `border-radius:var(--radius-3)`）。写一份 `设计Token说明.md`：列出你定义的 token 名 → 含义 → 用在哪。把改后的 `style.css` 片段 + md 发我。

## 🔵 部分三·原理层（底层发生了什么）
CSS 自定义属性（`--x`）由浏览器在**计算样式阶段**解析，配合 `var(--x)` 做替换，支持级联与继承——这正是"设计 token"在前端的天然载体。`[data-theme='dark']` 是属性选择器，靠 JS 在 `<html>` 上切 `data-theme` 属性（你站里 `toggleTheme()` 已做）来整站换肤。Token 分层是行业实践：最底层是"原始值"（如 `#6c8cff`），往上是"语义层"（如 `--color-primary` 指向原始值），再往上是"组件层"（按钮用 `--color-primary`）。这样改原始值，语义层和组件层全联动——这就是"演进性"：换品牌色不用改每个组件。

## 🟣 部分四·深挖层（为什么、历史、延伸）
设计系统起源于 2013 年 Google Material Design，解决"安卓各 App 长得五花八门"。原子设计（Atomic Design，Brad Frost）把它拆成"原子(按钮)/分子(搜索框)/组织(导航栏)/模板/页面"五层——你 PD5 的"按钮组件"就是原子，"卡片列表"是分子。顶级工程师追问：token 怎么跨平台同步？答案是 **Design Token 工具链**（Style Dictionary / Tokens Studio）：一份 JSON 源 → 编译出 CSS 变量、iOS 常量、Android 资源、Figma 变量，四端统一。你现在是"手动同步"（改 CSS 变量），将来接工具链就能"改 Figma 自动改代码"。

## 🔴 部分五·顶级视角
顶级大佬把设计系统当成**产品来运营**：有版本号、有 changelog、有"组件被多少人用"的埋点、有专门的设计系统团队。它直接降成本：新页面拼装组件，开发速度翻倍、视觉一致性 100%。在方向6 前端里，这和"组件化"（FE9 Vue 组件 / FE10 React 组件）是同一哲学——设计侧的 token 和代码侧的常量本该同源。你作品集现在虽小，但把 `:root` 当成"你的个人设计系统 v0.1"，是顶级习惯的雏形。以后做毕业作"闪电号卡"（方向6 BE26）时，这套 token 直接复用。

## 🟠 部分六·安全/合规种子（白帽视角）
设计系统里用到的开源组件（如 Material、TDesign）都有开源协议（Apache/MIT），商用需保留版权声明——在你的作品集/项目里注明"基于 XX 设计系统"是合规且专业的。绝不把别人的设计系统整包扒来改名冒充"自研"，那既侵权又是简历造假（你已承诺不虚构）。白帽产品人："用了开源就光明正大标注"。

## ✅ 验收
交：① 改后的 `style.css` 含完整 `:root` token 且 hero/卡片用上了 `--radius-3`；② `设计Token说明.md`。齐算过关。

## ⚠️ 常见坑
- token 命名要"语义化"别"值化"：写 `--color-primary` 而非 `--blue`，否则换色时名字和意义脱节。
- 别只定义不替换：定义 20 个变量却还硬编码 20 处颜色 = 白做。
- 暗色 token 漏了某个变量 → 切暗色时局部"发白刺眼"，务必逐个核对。
- 手疼就复制粘贴 CSS 块，少敲。

## ➡️ 下一步
PD7 · 原型 / 线框（低保真 / 高保真）——用 Figma 做"能点"的原型，把纸面设计变成可点击Demo，并可用来给用户测试（PD9）。

---

# PD7 · 原型 / 线框（低保真 / 高保真）

## 🎯 目标
学完这节，你能区分线框图（骨架）与原型（可点 Demo），并在 Figma 网页版里给你的作品集做一个"可点击原型"：从首页点进某个项目详情。你将理解"先画草图再写代码"如何省下 10 倍返工成本。

## 📋 小白前置
需先懂 PD5（Figma 组件/布局）、PD4（交互状态）。开着作品集站对照页面结构。

## 🟢 部分一·最浅层（生活比喻）
线框图像"房子的户型草图"——只画哪间是卧室、门朝哪，不画墙纸颜色。原型像"样板间"——能走进去开门、开灯、感受动线，但家具可能是临时的。两者都不等于"真房子"（真代码）。先画草图再盖房，改起来只要橡皮擦；先盖房再改户型，要砸墙。

## 🟡 部分二·动手层（逐字操作）
1. 打开 Figma，新建一页（顶部 `+` 页签）。
2. **低保真线框**：用 **R** 画几个灰块代表区域——顶部一条导航、中间 hero 大块、下面 3 个方形项目卡、底部联系条。全部用浅灰填充、无文字细节，只表达"布局结构"。这就是线框。
3. **高保真**：复制这页，把灰块换成你真实作品集的配色、加上真实文字（"王承泽 作品集""全栈+逆向安全"）、用 PD5 的按钮组件。看起来和真站差不多。
4. **加交互（Prototype 模式）**：右上角切换开关点到 **Prototype**（或按 `Shift+E`）。选中导航里的"项目"文字，右侧 **Interactions** → **On click** → **Navigate to** → 选你新建的"项目详情"画板（先画一个简单详情页：大标题 + 两段占位文 + 返回按钮）。返回按钮设 **On click → Back**。
5. 点右上角 **Present**（播放键）进入演示：点"项目"能跳详情，点"返回"能回首页。用浏览器分享或录屏保存。

### 🧪 实战 Lab（PD7）
给你作品集的 `#compliance`（合规）板块做一个"高保真 + 可点击"的原型页，并和首页用"导航点击"连起来。写一份 `原型说明.md`：说明哪是低保真、哪是高保真、点击流是怎样的。把 Present 模式截图（首页 + 合规页） + md 发我。

## 🔵 部分三·原理层（底层发生了什么）
Figma 的 Prototype 模式在节点上附加"交互连线"元数据（trigger=click, action=navigate, target=frameId）。点 Present 时，Figma 运行时按这些连线在画板间做转场动画（如同前端路由跳转，方向6 BE9 路由思想同源）。低保真本质是"信息架构的可视化"（PD 后续），高保真是"视觉+交互的可视化"。原型的意义在于：它能在**写第一行代码前**暴露"流程走不通/按钮找不到"的问题——因为改 Figma 比改代码便宜 100 倍。

## 🟣 部分四·深挖层（为什么、历史、延伸）
原型方法来自工业设计（如汽车油泥模型），软件界由 Alan Cooper《About Face》系统化为"交互设计"流程：**先 personas（PD9）→ 再线框 → 再原型 → 再高保真 → 最后开发**。顶级工程师追问：原型到代码怎么不丢信息？靠 **Dev Mode**（Figma 右侧切到 Dev 标签）让前端直接读间距/颜色/代码，或导出 token（PD6）。还有"可点击原型做可用性测试"（PD9）比"看静态图"能发现多 3 倍的问题——因为人是在"用"不是在"读"。

## 🔴 部分五·顶级视角
顶级团队把原型当"沟通合同"：产品用原型对齐老板/客户预期，开发按原型估工期，测试按原型写用例。原型粒度有讲究——MVP 阶段用"纸面线框 + 口头讲"最快；给投资人看要用"高保真可点"显专业。在方向6 前端，你写的每一页都可视为"活的原型"；而 Figma 原型是"死的、不能连真数据"的——所以终极形态是"用真实框架搭可跑原型"（如 Vite + Vue 半小时出可点 Demo，FE9）。你作品集已是"成品级原型"（真实可跑），这是最高形态。

## 🟠 部分六·安全/合规种子（白帽视角）
原型里若用"假数据"演示，必须标注"示例数据"——否则拿假数据原型去融资/应聘冒充真实业绩，是欺诈（你承诺简历不虚构）。白帽产品人：原型归原型、真实业绩归真实业绩，两者界限分明、从不混淆。给客户/老板看高保真原型时，也明确"这是设计演示，功能未实装"，不夸大交付能力。

## ✅ 验收
交：① Figma 带"首页↔合规页"点击流的原型（Present 截图 2 张）；② `原型说明.md`（低保真/高保真区分 + 点击流）。齐算过关。

## ⚠️ 常见坑
- 别在低保真阶段纠结配色——那是高保真的事，过早美化会掩盖"结构问题"。
- Prototype 连线没反应，多半是忘切到 Prototype 标签、或目标画板名拼错。
- 手疼用复制画板（`Ctrl+C/V`）少重画。

## ➡️ 下一步
PD8 · 可访问性（WCAG / 对比）——把"设计好看"升级为"设计对所有人都可用"，直接审计并修复你作品集的可访问性短板。

---

# PD8 · 可访问性（WCAG / 对比）

## 🎯 目标
学完这节，你能用 WCAG 标准给你的作品集做"可访问性审计"：键盘能否导航、对比度是否达标、图片有无替代文字、表单有无标签。你将修掉至少 2 个真实可访问性缺陷，让站点对色盲、视障、纯键盘用户也友好。

## 📋 小白前置
需先懂 PD2（对比度）、PD4（状态/反馈）。开着作品集 `index.html` 与 `style.css`。

## 🟢 部分一·最浅层（生活比喻）
可访问性像"给大楼修无障碍坡道"：健康人走台阶，坐轮椅的人也需要能进。网站也一样——鼠标用户点链接，键盘用户按 Tab 也能走到；视力好的人看颜色，色盲用户靠形状/文字也能分辨；读屏软件（给盲人念网页）靠你写的"图片说明"来介绍图。坡道不挡健康人，却救了另一群人——可访问性从不妨碍美观，只多一层体贴。

## 🟡 部分二·动手层（逐字操作）
1. **键盘走查**：打开作品集站，鼠标拔掉想象（不用点），一直按 `Tab` 键，看页面上有没有"焦点框"跟着跳、能否用回车激活链接/按钮。如果 Tab 怎么都跳不进导航，说明缺 `:focus` 样式或元素不是可聚焦的（`<a>`/`<button>` 才行）。
2. **对比度复检**：用 PD2 的 WebAIM 对比检查器（`https://webaim.org/resources/contrastchecker/`）把你站里所有"文字色 vs 背景色"配对测一遍，标出 < 4.5:1 的。
3. **图片 alt 审计**：在 VS Code 打开 `index.html`，搜索 `<img`，看每张图有没有 `alt="..."` 描述。你的站若是 CSS 背景图，则读屏读不到——考虑关键图改 `<img alt>` 或加 `aria-label`。
4. **修复一处**：在 `style.css` 加一段全局焦点样式（复制即可）：

```css
a:focus-visible, button:focus-visible{
  outline:2px solid var(--color-primary);
  outline-offset:2px;
}
```

5. 用 Git Bash 起本地服务器验证（也练手，路径用 `/c/...`）：

```bash
cd /c/Users/qq/WorkBuddy/2026-08-26-13-18-54/作品集_HR成品版
python -m http.server 8080
```

浏览器开 `http://localhost:8080`，再 Tab 走查一次。

### 🧪 实战 Lab（PD8）
写一份 `可访问性审计.md`：列出你发现的至少 3 个问题（键盘/Tab、对比度、alt 缺漏各算一类），并写明已修复哪一个（如加了 `:focus-visible`）。把审计 md + 修复处 CSS 片段发我。

## 🔵 部分三·原理层（底层发生了什么）
WCAG（Web Content Accessibility Guidelines）由 W3C 制定，核心是四大原则 **POUR**：可感知（Perceivable）、可操作（Operable）、可理解（Understandable）、健壮（Robust）。对比度 4.5:1 来自"正常文字在典型视力下的可辨阈值"实验。键盘导航依赖 DOM 的"可聚焦元素"序列（tab order = DOM 顺序，除非用 `tabindex` 改）；`alt` 属性是给读屏（如 NVDA、VoiceOver）念的"图片替身"。`:focus-visible` 是浏览器智能判断"该不该显示焦点框"的伪类（键盘才显示、鼠标点击不显示），比老 `:focus` 更友好。

## 🟣 部分四·深挖层（为什么、历史、延伸）
可访问性最早为残障人士立法（美国 1990 年 ADA、欧盟 EN 301 549），近年中国《无障碍环境建设法》也生效。顶级工程师追问：可访问性和 SEO 重合度多高？很高——语义化标签（`<nav>/<main>/<h1>`）既帮读屏也帮搜索引擎（方向6 FE1 HTML 语义化），"alt 文本"既帮盲人也被图片搜索收录。还有"动效偏好"：`@media (prefers-reduced-motion: reduce)` 让晕动症用户关掉动画——这是顶级细节。你作品集若加 `aria-label` 和语义标签，搜索引擎和读屏双双受益。

## 🔴 部分五·顶级视角
顶级大佬把可访问性当**默认项**而非"加选项"：组件库天生带 focus 态、语义结构、aria 属性。他们用自动化工具（axe、Lighthouse 无障碍审计，方向6 FE14 性能优化里 Lighthouse 也测这个）做 CI 卡点——每次提交自动扫 a11y 问题。在安全侧，"可访问性"还引申为"不依赖单一感官传递安全信息"：比如登录失败不只用红色（色盲看不出），还配图标+文字（呼应 PD4 反馈双保险）。你作品集做 a11y 修复，是"顶级习惯"的实练。

## 🟠 部分六·安全/合规种子（白帽视角）
可访问性是"合规正道"，和"dark pattern"（PD4 提过）正相反——前者尊重所有用户，后者欺骗部分用户。白帽产品人主动合规：做完站用 Lighthouse 跑一次 a11y 分数并公示，是专业可信的信号。绝不用"假装有无障碍"骗过审计工具却实际不可用——那叫"可及性洗白"，不诚实。

## ✅ 验收
交：① `可访问性审计.md`（≥3 问题 + 已修复说明）；② 加了 `:focus-visible` 等的 `style.css` 片段；③ 本地 `http.server` 能跑。齐算过关。

## ⚠️ 常见坑
- 对比度只看"正文"，忘了"大字/图标按钮"也要测（3:1 即可但常被忽略）。
- 装饰性背景图别加 alt（会烦读屏），用 `alt=""` 或 `aria-hidden="true"` 标记"纯装饰"。
- 手疼多用 Tab 键本身代替鼠标操作，反而护手。

## ➡️ 下一步
PD9 · 用户研究（访谈 / 问卷）——从"自己猜用户"升级为"真找人测"，用访谈和问卷给作品集/未来产品拿真实反馈。

---

# PD9 · 用户研究（访谈 / 问卷）

## 🎯 目标
学完这节，你能设计一份"作品集可用性测试"的轻量方案：招募 1–3 个真实朋友/家人，用半结构化访谈 + 任务法，收集对你作品集的真实反馈，并产出一份有"用户原话+改进项"的研究笔记。你将体验"用数据代替直觉"。

## 📋 小白前置
需先懂 PD3（心智模型/走查）、PD8（可访问性可测）。本节点大量用"说话/语音输入"，对手不便的你尤其友好。

## 🟢 部分一·最浅层（生活比喻）
用户研究像"开饭店前先拉街坊试吃"：你觉得自己菜咸淡刚好，但食客皱眉——你才知道"原来别人味蕾和你不同"。自己做作品集就像厨师尝自己的菜，越看越顺眼；必须让别人真去"用"，你才看得到盲区。访谈是"边看边问为什么"，问卷是"发张表让大家打勾"。

## 🟡 部分二·动手层（逐字操作）
1. **写访谈提纲**（用 VS Code 新建 `用户研究_提纲.md`，复制下面 5 问）：
```
1. 你第一眼觉得这是做什么的人的站点？（心智模型）
2. 找"他最厉害的项目"——你点哪、花了几秒？卡在哪？
3. 想联系他，你预期联系信息在哪、实际找到没？
4. 暗色模式你发现没、用了没、觉得怎样？
5. 整体打 1–10 分，一句最想让他改的话。
```
2. **招募**：找 1–3 个真人（家人/朋友/群友均可），用微信/语音通话进行，你共享屏幕让他们操作、你观察+录音（**先征得同意**）。
3. **记录原话**：边听边在 `用户研究_笔记.md` 记"用户原话 + 你的解读"。例如原话："联系方式我找了半天" → 解读：导航缺邮箱入口（印证 PD3 卡点）。
4. **做问卷（可选）**：用腾讯问卷/问卷星（网页）发 3 题：① 你给这站专业性打几分？② 哪块最看不懂？③ 你还想看什么？把链接发群里收数据。
5. 汇总：把访谈+问卷写成 `用户研究报告.md`，含"3 个高频痛点 + 对应改法"。

### 🧪 实战 Lab（PD9）
完成至少 1 次真实访谈（哪怕只 1 人），产出 `用户研究报告.md`（含原话引用 + 痛点 + 改法）。把报告发我。若实在找不到人，用"自我走查+假设 HR 视角"做一版并明确标注"未做真人测试"，但优先真找人。

## 🔵 部分三·原理层（底层发生了什么）
用户研究分"定性（访谈/观察，找为什么）"和"定量（问卷/数据，找多少/多大）"，两者互补——定性挖深度、定量验广度。半结构化访谈介于"自由聊"和"念稿"之间，既有框架又留追问空间。任务法（Task-based）让用户"做"而非"评"，能暴露真实行为（人说的≠做的，叫 **Say-Do Gap**）。记录原话重要，因为"原话"是证据，避免你事后"美化"成自己想听的。问卷要控制题量（>10 题弃答率飙升）和避免诱导性问题（"你觉得这站很棒吧？"是坏问题）。

## 🟣 部分四·深挖层（为什么、历史、延伸）
用户研究源于人类学和市场调研，70 年代被可口可乐等用于产品测试，90 年代随可用性工程（Nielsen）进软件圈。顶级工程师追问：样本少（1–3 人）有用吗？**极有用**——Nielsen 提出"5 个用户能发现 85% 的可用性问题"，因为问题会重复出现，再多用户只是重复。但定量结论（如"60% 不喜欢"）需要大样本才可信。这引出"混合方法"：先用 5 人定性找问题，再用百人问卷定量验证优先级。你作品集阶段做 1–3 人定性完全够用。

## 🔴 部分五·顶级视角
顶级大佬把"用户研究"做成组织能力：有用户访谈录像库、有"用户原声"墙、有 NPS/满意度看板（PD12 数据驱动）。他们警惕" HIPPO 决策"（Highest Paid Person's Opinion，谁职位高听谁的）——用研究数据对抗"老板拍脑袋"。在方向6 产品里，A/B 测试（PD12）是"研究自动化"的终极形态：不上访谈、直接让 10 万用户用行为投票。你从"找 1 个朋友测作品集"起步，正是顶级方法的迷你实践。

## 🟠 部分六·安全/合规种子（白帽视角）
用户研究有隐私红线：① 录音/录屏必须**事先明确告知并获同意**，否则违法收集个人信息；② 报告里若引用真人原话，去标识化（不写姓名/电话）；③ 绝不拿用户数据去做未经同意的二次利用（如卖给第三方）。白帽产品人："尊重受访者如同尊重用户"——合法、透明、最小化收集。你承诺不盗用成果，研究数据同样不盗用、不泄露。

## ✅ 验收
交：① `用户研究报告.md`（含≥1 真人原话 + 痛点 + 改法）；② 访谈提纲文件。齐算过关。

## ⚠️ 常见坑
- 别"诱导提问"（"这站好看吧？"）→ 改成"你第一印象是什么？"
- 别只记结论不记原话 → 结论会失真，原话才是证据。
- 别因 1 人夸你就飘 → 找不同背景的人（技术/非技术各一）更准。
- 手疼就用语音输入写笔记（Win+H，PRE2 学过），少打字。

## ➡️ 下一步
PD10 · 产品思维 / MVP（问题-方案-度量）——把"做个站"升级为"用一个最小产品验证一个真实问题"，建立顶级产品人的第一性原理。

---

# PD10 · 产品思维 / MVP（问题-方案-度量）

## 🎯 目标
学完这节，你能用"问题 → 方案 → 度量"三段式描述你的作品集（或任何未来项目）的产品逻辑，并定义它的 MVP（最小可行产品）边界：哪些必须有、哪些可以砍。你将写出一份 `产品说明.md`，像顶级 PM 一样讲清"为什么做、给谁做、怎么算成"。

## 📋 小白前置
需先懂 PD9（用户研究，找真实问题）、PD3（心智模型）。本节点偏"思维框架"，适合你时间充裕时慢慢想。

## 🟢 部分一·最浅层（生活比喻）
产品思维像"开小卖部"：先确认"这小区缺不缺便利店"（问题），再决定"先卖水+泡面就开业，冰柜以后再加"（MVP），最后看"每天来几个人、赚没赚"（度量）。很多人一上来就装修豪华、进一堆货——结果没人来，全白搭。MVP 就是"用最小成本先验证有人要"，再砸钱扩建。

## 🟡 部分二·动手层（逐字操作）
1. 在 VS Code 新建 `产品说明.md`，按下面模板填（复制框架）：
```
# 作品集 产品说明
## 1. 问题（给谁、什么痛点）
- 目标用户：招聘 HR / 技术面试官
- 痛点：纸质简历说不清"能不能真做出来"，需要可验证的作品证据
## 2. 方案（MVP 范围）
- 必须有：项目展示 + 可运行证据 + 联系方式 + 合规声明
- 可砍：博客/留言板/动画特效（v2 再加）
## 3. 度量（怎么算成功）
- 指标：访客数、平均停留时长、联系转化率
- 目标：每月 ≥ X 个有效联系
```
2. 对照你现有作品集，勾选"必须有"里哪条已经满足、哪条缺失（如"合规声明"你已有 `#compliance` 板块，很好）。
3. 用 Figma 或纸笔画一张"v1 vs v2 功能清单"：v1 只留 MVP，v2 才加花活。
4. 写一句话"北极星指标"（North Star）：你作品集唯一最重要的成功信号是什么？（如"收到 1 个真实面试邀约"）

### 🧪 实战 Lab（PD10）
完成 `产品说明.md`（三段式 + 北极星指标），并在末尾列"v1 已实现 / v2 待做"清单。把文件发我。

## 🔵 部分三·原理层（底层发生了什么）
"问题-方案-度量"是产品管理的黄金闭环：**问题**来自用户研究（PD9）和市场缺口；**方案**通过 MVP 降低"验证成本"（Eric Ries《精益创业》的 Build-Measure-Learn 循环）；**度量**用指标判断是否"真有用"而非"我觉得有用"。MVP 不是"做烂版"，而是"最小但完整可验证"——作品集的 MVP 是"能让人相信你能力并联系你"，多余特效是 v2。北极星指标是"唯一一个数字代表产品健康"，防止团队被虚荣指标（如下载量）误导（PD12 会深化）。

## 🟣 部分四·深挖层（为什么、历史、延伸）
产品思维在 2000 后随互联网爆发，"PM（产品经理）"成独立职能。MVP 概念由 Frank Robinson 提出、Ries 普及。顶级工程师追问：MVP 会不会"太丑劝退"？边界在"核心价值可感知"——作品集丑但项目真，HR 仍会看；但若丑到打不开，核心价值也传不到。这引出"可接受的 MVP 质量线"。还有"前沿 vs 后沿创新"：你是"用成熟技术做作品集"（后沿，稳）还是要"炫技新框架"（前沿，险）——顶级大佬会按目标选，不盲目追新。

## 🔴 部分五·顶级视角
顶级大佬（无论工程师还是 PM）都是"产品思维"持有者：他们写代码前先问"这解决谁的什么问题、怎么度量"。在方向6 毕业作"闪电号卡"里，你也要先定义"这号卡系统解决什么、MVP 是哪几个接口、成功指标是什么"。技术越强，越要防止"为了炫技术而做"——产品思维是"技术服务于价值"的锚。你作品集已天然是"问题解决型产品"（证明能力→获机会），把它显性写成 `产品说明.md`，是顶级自我定位。

## 🟠 部分六·安全/合规种子（白帽视角）
MVP 里的"度量"要合规：若你加访客统计（如百度统计/GA），必须**告知用户并合规采集**（中国需隐私政策+备案），不偷偷埋点收集隐私。白帽产品人："度量用匿名聚合数据，不用个人追踪"。作品集展示真实项目即可，绝不把 MVP 包装成"已上线的商业产品"夸大——边界如实写。

## ✅ 验收
交：① `产品说明.md`（问题/方案/度量三段 + 北极星 + v1/v2 清单）。齐算过关。

## ⚠️ 常见坑
- MVP 不是"偷工减料"，是"砍非核心"——核心体验必须完整。
- 别把"我想做的酷功能"当"用户问题"→ 先问"谁痛"。
- 北极星指标别选虚荣指标（如"页面炫酷度"无法量化）。
- 手疼就语音口述、复制模板，少打字。

## ➡️ 下一步
PD11 · 需求 / PRD（写好规格）——把"产品想法"写成"开发能照着做"的需求文档，用 Markdown 实战，直接服务你未来所有项目。

---

# PD11 · 需求 / PRD（写好规格）

## 🎯 目标
学完这节，你能写一份标准 PRD（产品需求文档）：背景、目标、用户故事、功能清单（含优先级 MoSCoW）、验收标准、非功能需求。你将用 Markdown 给"作品集增加一个「博客/技术文章」板块"写一份真实 PRD，作为后续开发（或交给未来的你）的施工蓝图。

## 📋 小白前置
需先懂 PD10（问题-方案-度量，PRD 是它的工程化展开）。需用 VS Code + Markdown（PRE4 装过 VS Code）。

## 🟢 部分一·最浅层（生活比喻）
PRD 像"装修施工图纸"：你（业主/产品）告诉工长（开发）"我要三室一厅、主卧朝南、插座留 6 个"，工长照图施工、照图验收。没图纸就口头说"弄好看点"，结果返工十次。PRD 就是把"我想要"翻译成"可验收的规格"，让开发不用猜。

## 🟡 部分二·动手层（逐字操作）
1. VS Code 新建 `PRD_作品集博客板块.md`，复制下面骨架填写：
```markdown
# PRD：作品集新增「博客/技术文章」板块

## 1. 背景与目标
- 背景：HR/面试官想看思考深度，纯项目列表不够
- 目标：访客能读 3–5 篇技术文，停留时长 +30%

## 2. 用户故事
- 作为访客，我想读技术文章，以便判断其技术深度
- 作为站长，我想发文章，以便沉淀影响力（PD17）

## 3. 功能清单（MoSCoW）
- Must：文章列表页 + 详情页（Markdown 渲染）
- Should：标签分类 + 阅读量
- Could：评论区
- Won't：付费订阅（v2）

## 4. 验收标准
- 列表页显示 ≥3 篇文章标题+摘要
- 详情页正确渲染 Markdown（标题/代码块）
- 移动端可读（响应式）

## 5. 非功能需求
- 性能：首屏 < 1.5s
- 安全：文章为静态生成，无用户输入执行（防 XSS）
- 合规：原创声明，不转载未授权内容
```
2. 用 Git Bash 看文件已保存：
```bash
cd /c/Users/qq/WorkBuddy/2026-08-26-13-18-54/全领域教案
ls -la
```
3. 在 VS Code 侧边预览（装 Markdown Preview 插件，或 `Ctrl+Shift+V`）检查排版。

### 🧪 实战 Lab（PD11）
把上面 PRD 改成"你真实想给作品集加的一个功能"（如"项目详情页加「技术栈标签+架构图」"或"加一个可下载 PDF 简历"），补全验收标准。把 `PRD_xxx.md` 发我。

## 🔵 部分三·原理层（底层发生了什么）
PRD 是"需求工程"的产物：把模糊意图经**需求获取→分析→规格化→验证**变成无歧义文本。用户故事（As a... I want... so that...）来自敏捷（PD15），强制从"用户视角+价值"描述，避免"为功能而功能"。MoSCoW（Must/Should/Could/Won't）是优先级框架，帮开发在工期不够时知道"先砍哪"。验收标准（Acceptance Criteria）必须"可测试"——"好看"不行，"首屏<1.5s"才行，这是开发自测和测试同事（方向6 BE22）的共同依据。

## 🟣 部分四·深挖层（为什么、历史、延伸）
PRD 从传统软件工程的"需求规格书（SRS）"演来，互联网时代精简成轻量 PRD（亚马逊的 6 页文、谷歌的 PRD 模板各有风格）。顶级工程师追问：PRD 会不会"写一大堆没人看"？会——所以顶级团队用"轻 PRD + 原型（PD7）+ 用户故事棋盘"替代厚文档，且 PRD 是"活的"（随开发反馈更新，进 Git 版本管理，见 PD14）。还有"需求陷阱"：镀金（做超出需要的）、歧义（一句话两种理解）、隐藏（没写但默认要有）——好 PRD 逐条防这三种。

## 🔴 部分五·顶级视角
顶级大佬既是写 PRD 的人也是读 PRD 的人：他们写 PRD 极克制（一页能说清不写三页），且把"非功能需求"（性能/安全/合规）当一等公民——你 PRD 里写"防 XSS""静态生成"就是顶级安全意识。在方向6 开发里，PRD 直接转成"任务卡"进看板（PD15），验收标准转成测试用例（BE22）。你作品集的每篇文章、每个板块，都能先写 PRD 再动手——这是"顶级工程纪律"的体现，比直接写代码省无数返工。

## 🟠 部分六·安全/合规种子（白帽视角）
PRD 里必须写"安全与合规需求"：① 任何用户输入（如评论区）必须做过滤/转义防 XSS（SEC10）；② 静态博客用构建期生成、运行时无执行，天然安全；③ 内容合规：不转载未授权文章、不搬运他人成果（你承诺不盗用）；④ 若接评论，需防垃圾/滥用且合规采集。白帽产品人的 PRD，"安全需求"从不缺席——这是和"野鸡班只教功能"的本质区别。

## ✅ 验收
交：① 一份自填的 `PRD_xxx.md`（含背景/故事/MoSCoW/验收/非功能）。齐算过关。

## ⚠️ 常见坑
- 验收标准写"用户满意"→ 不可测，必须量化/可观察。
- 功能清单不分优先级 → 开发不知先做什么，工期爆炸。
- 忘写非功能需求 → 上线才发现慢/不安全。
- 手疼复制模板，只填空，不手敲长文。

## ➡️ 下一步
PD12 · 数据驱动产品（指标 / 实验）——把 PD10 的"度量"做成可量化体系，并学会用 A/B 实验做决策，直接可用到你作品集的迭代。

---

# PD12 · 数据驱动产品（指标 / 实验）

## 🎯 目标
学完这节，你能为作品集定义一套"指标体系"（北极星 + 漏斗 + 护栏指标），并用免费工具（如 Google Analytics 4 / 友盟 / 或自建简单日志）采集数据；理解 A/B 测试原理，知道"什么时候该用实验而非拍脑袋"。你将产出 `数据指标定义.md`。

## 📋 小白前置
需先懂 PD10（北极星指标）、PD11（PRD 里的度量）。本节点偏概念+轻量实操，适合你边学边想。

## 🟢 部分一·最浅层（生活比喻）
数据驱动像"称体重管理身材"：你定"目标 70kg"（北极星），每天称（采集），发现"晚上吃多就涨"（归因），于是改晚餐（行动）。不称的人靠"我觉得瘦了"——往往自欺。A/B 测试像"左右脚各试一种跑鞋，跑同样距离看哪只脚更舒服"——同一人、同条件、只变一个变量，结论才可靠。

## 🟡 部分二·动手层（逐字操作）
1. VS Code 新建 `数据指标定义.md`，按模板填：
```
# 作品集 数据指标
## 北极星：每月有效联系数（真能聊工作的）
## 漏斗
- 访客数（进首页）
- 项目页浏览率（点了项目）
- 联系转化率（点了邮箱/表单）
## 护栏指标（不能坏的）
- 首屏加载 < 1.5s
- 报错率 < 0.1%
## 数据采集方式
- 方案A：GA4（免费，需隐私政策+备案）
- 方案B：自建访问日志（python 记录，纯匿名）
```
2. **轻量自建日志（纯匿名、合规）**：在作品集目录用 Git Bash 起服务并看访问（仅本地练习，理解原理）：
```bash
cd /c/Users/qq/WorkBuddy/2026-08-26-13-18-54/作品集_HR成品版
python -m http.server 8080
```
浏览器多开几次，看终端打印的 `GET / HTTP/1.1` 日志——这就是最原始的"访问数据"。
3. 决定：若真要线上统计，选 GA4 但要写隐私政策页（合规），或坚持"纯匿名聚合"不上个人信息。

### 🧪 实战 Lab（PD12）
完成 `数据指标定义.md`（北极星+漏斗+护栏+采集方式）。额外：用 Figma 或纸笔画一张"访客→项目→联系"漏斗图。把 md + 漏斗图发我。

## 🔵 部分三·原理层（底层发生了什么）
指标体系分层：**北极星**代表产品长期价值；**漏斗**暴露流失环节（访客→项目页→联系，每步有转化率）；**护栏指标**防止"为优化北极星而伤害体验"（如为了停留时长放自动播放视频惹人烦）。A/B 测试核心是**控制变量 + 统计显著**：把流量随机分两组，只改一个元素（如按钮文案"查看项目"vs"看我作品"），跑足够样本后用假设检验判断差异是否"真有"还是"随机波动"。采集层：前端埋点（JS 发事件）或服务器日志（HTTP 请求记录），后者更隐私友好。

## 🟣 部分四·深挖层（为什么、历史、延伸）
数据驱动随 2000 后 Web 分析（Omniture→GA）兴起，A/B 测试由亚马逊/谷歌规模化。顶级工程师追问：指标会"被玩坏"吗？会——"古德哈特定律"：当指标成目标，它就不再是好指标（如为降加载时间砍掉真实内容）。所以需"指标组合+护栏"。还有"因果 vs 相关"：A/B 是少数能接近因果的方法（随机化），而看历史数据只能得相关（夏季冰淇淋和溺水同增，但无因果）。你作品集若做 A/B，只能变一个元素、样本要够——否则结论不可信。

## 🔴 部分五·顶级视角
顶级大佬建"指标中台+实验平台"：所有产品共用一套指标定义、一键开 A/B、自动算显著性与置信区间。他们警惕"虚荣指标"（PV、注册数好看但无价值）和"局部最优害全局"（某按钮点击率升但总转化降）。在方向10 SRE 里，SLI/SLO（OPS37）就是"系统版指标体系"；在方向6 里，A/B 是增长黑客核心。你把作品集当"第一个被你数据驱动的产品"，练的就是这套顶级肌肉。

## 🟠 部分六·安全/合规种子（白帽视角）
数据采集的合规红线最硬：① 中国《个人信息保护法》要求告知+同意+最小化，GA4 等需隐私政策与备案；② 绝不采集个人身份（姓名/电话/IP 精确到人）做画像；③ 用"匿名聚合"最安全（如只统计"今日访客数"不记是谁）。白帽产品人："能不采个人数据就不采，要采必告知"。你作品集若只放匿名计数，既合规又专业——这是和"野鸡数据公司乱采"的本质区别。

## ✅ 验收
交：① `数据指标定义.md`；② 漏斗图（Figma/手绘拍照）。齐算过关。

## ⚠️ 常见坑
- 只定北极星忘护栏 → 优化一个坏另一个。
- A/B 样本太小就下结论 → 随机波动当真理。
- 忽视隐私合规 → 上线即违规。
- 手疼用语音写 md，少打字。

## ➡️ 下一步
PD13 · 技术写作 / 文档（清晰 / 结构化）——把你已写的 PRD/报告升级成"顶级工程师级别"的技术文档，用 Markdown + Git 管理，直接复用作品集与未来项目。

---

# PD13 · 技术写作 / 文档（清晰 / 结构化）

## 🎯 目标
学完这节，你能用"顶级工程师标准"写技术文档：读者导向、结构清晰（摘要→前提→步骤→验证→排错）、术语有定义、命令可复制。你将重写下之前某份 md（如 `PRD` 或 `用户研究报告`），并学会用 Markdown 语法（标题/代码块/表格/清单）和 Git 版本管理文档。

## 📋 小白前置
需先懂 PD11（PRD 写作）、PRE4（VS Code）、PRE6（终端）。本节点大量写文档，手不便者用语音输入+复制模板。

## 🟢 部分一·最浅层（生活比喻）
技术写作像"写菜谱"：好菜谱写清"几人份、要啥料、第几步放盐、烤多久、糊了怎么办"；烂菜谱写"适量、看着办、烤到熟"。别人照好菜谱能复现，照烂菜谱只能骂人。文档是"写给未来的自己/同事看的菜谱"——你三个月后看自己写的，也要能照做。

## 🟡 部分二·动手层（逐字操作）
1. 在 VS Code 新建 `技术写作规范.md`，复制下面"好文档骨架"：
```markdown
# 标题（一句话说清这是什么）
## 摘要（30 字内：这是什么、解决啥）
## 适用读者与前提（需先懂 X、需装 Y）
## 操作步骤（ numbered、每步可验证）
1. 打开 X ...
2. 运行 `命令` → 应看到 Y
## 验证（怎样算成功）
## 常见错误与排查（表格）
| 现象 | 原因 | 解决 |
|------|------|------|
| 报错 Z | 没装依赖 | 运行 `npm i` |
## 参考（链接/来源）
```
2. 用 Git Bash 把你的教案目录初始化成 git 仓库（练手，路径 `/c/...`）：
```bash
cd /c/Users/qq/WorkBuddy/2026-08-26-13-18-54/全领域教案
git init
git add 教案_方向11_产品设计软技能.md
git commit -m "docs: 新增方向11产品设计软技能教案 PD1-17"
```
（若提示 user.email 未设，先 `git config --global user.email "you@example.com"` 和 `user.name`，用你自己的邮箱名）
3. 重写你 PD11 的 PRD，套用上面骨架，看是否更清晰。

### 🧪 实战 Lab（PD13）
挑你之前写的一份 md（PRD 或用户研究报告），用"好文档骨架"重写成 `xxx_重写版.md`，重点补"验证/排错表格"。把重写版发我。

## 🔵 部分三·原理层（底层发生了什么）
技术文档的本质是**降低认知负荷**：人脑工作记忆有限（Miller 7±2），好文档用"结构（标题层级）+ 渐进披露（摘要→细节）"把信息分块，让读者按需取用。Markdown 是"带语义的纯文本"——`#` 表标题、` ``` ` 表代码块，渲染器（VS Code/GitHub）转成 HTML，既好写又好看。Git 管文档让"每次改动可追溯、可回退"（和方向6 代码管理同源），多人协作文档不再"最终版_v3_真的最终.docx"混乱。

## 🟣 部分四·深挖层（为什么、历史、延伸）
技术写作是门独立学科（Microsoft/Google 有专职技术作家）。顶级工程师追问：文档和代码谁重要？答案是"同等"——"代码告诉你怎么做的，文档告诉你为什么这么做"（README/架构决策记录 ADR 就是记"为什么"）。还有"文档即代码（Docs as Code）"运动：文档进 Git、用 PR 评审、自动生成站点（如 Docusaurus/VitePress），和你作品集"纯静态站"理念一致。你写的所有 md，未来都能用 VitePress 一键变文档站。

## 🔴 部分五·顶级视角
顶级大佬把写作当"杠杆"：一封清晰的邮件/文档能省 10 次会议；开源项目的 README 质量直接决定 star 数。他们写文档遵循"读者优先"——先想"读的人卡在哪"再下笔，而非"我把知道的倒出来"。在方向7 安全里，"Writeup 写作（SEC97）"是核心能力：把漏洞复现写成别人能照做的文档，是赏金猎人/研究者的基本功。你练的技术写作，未来写 CTF Writeup、写架构文档、写博客（PD17）全用得上——这是"顶级表达力"的地基。

## 🟠 部分六·安全/合规种子（白帽视角）
技术写作的合规边界：① 写安全相关文档（如你的逆向笔记）只记录"自己/授权样本"的分析，不写"如何攻击他人"的实操手册；② 引用他人资料要标注来源、遵守许可，不抄袭（你承诺不盗用）；③ 文档里若含真实配置/密钥，**绝不提交进 Git**（用 `.gitignore` 忽略 secrets，方向10 OPS47 Vault 思想）；④ 白帽 Writeup 只发在授权平台/CTF 官方。文档清白，人清白。

## ✅ 验收
交：① 一份 `xxx_重写版.md`（套用骨架、含验证+排错表）；② Git 提交成功截图/记录。齐算过关。

## ⚠️ 常见坑
- 文档不写"验证"→ 读者不知做对没。
- 命令不带"预期输出"→ 出错了不会发现。
- 把密钥/隐私写进 md 还 commit → 泄露且难清（用 git rm --cached + 改历史）。
- 手疼多用语音+复制模板。

## ➡️ 下一步
PD14 · 沟通 / 协作（跨团队）——从"自己写文档"升级为"用 Git 和团队/未来的自己协作"，含 Git Bash 实战与 Cross-team 沟通话术。

---

# PD14 · 沟通 / 协作（跨团队）

## 🎯 目标
学完这节，你能：① 用 Git Bash 做版本管理与协作（clone/add/commit/push/branch/PR 流程）；② 写出"跨团队沟通"的标准信息结构（背景-请求-期限-影响）；③ 把你教案/作品集文档纳入 Git 协作流。你将真正体验"顶级工程师怎么和人与代码协作"。

## 📋 小白前置
需先懂 PD13（Markdown + Git 初体验）、PRE6（终端）、PRE7（浏览器）。本节点是"协作工程化"的核心。

## 🟢 部分一·最浅层（生活比喻）
协作像"几个人合写一本长篇小说"：每人写一章，用 Git 就像"有个智能笔记本"——你改你的章、他改他的章，笔记本自动合并；万一两人改了同一句，它标红让你裁决。沟通话术像"传话筒"：说清"我要你干啥、为啥、啥时候、不做会怎样"，对方才不懵。

## 🟡 部分二·动手层（逐字操作）
1. 确认 Git 已装：Git Bash 里跑 `git --version`。
2. 在教案目录（已 PD13 init 过）继续练分支协作：
```bash
cd /c/Users/qq/WorkBuddy/2026-08-26-13-18-54/全领域教案
git checkout -b feature/pd14-lab
# 新建一个文件 协作练习.md，写一句"PD14 协作测试"
git add 协作练习.md
git commit -m "docs: PD14 协作练习"
git checkout main
git merge feature/pd14-lab
```
3. **跨团队沟通模板**（复制到 VS Code `沟通模板.md`）：
```
【请求】希望设计同学给作品集导航加邮箱入口
【背景】用户研究(PD9)发现 HR 找不到联系方式
【影响】不做则联系转化率持续低
【期限】本周五前评审方案
【下一步】请回复可否，或约 10 分钟对齐
```
4. 把你作品集也纳入 Git（在作品集目录 `git init` 并提交一次），养成"每次改站都 commit"的习惯。

### 🧪 实战 Lab（PD14）
在教案仓库建一个分支、改一个文件、合并回 main，把 `git log --oneline` 的输出截图。另写一份"给虚构后端同学的协作请求"（用上面模板，请求他给作品集加个接口）。把截图 + 请求文本发我。

## 🔵 部分三·原理层（底层发生了什么）
Git 是**分布式版本控制系统**：每个仓库含完整历史，提交（commit）是不可变快照（内容哈希），分支（branch）只是指向某提交的可移动指针，合并（merge）按"三方 diff"（共同祖先+两分支）自动合。这比"锁定文件"的旧式 VCS 让多人并行不阻塞。PR（Pull Request）是在"合并前"加一道评审闸门——同事看 diff、评注释、过 CI 才合，是质量与知识共享的关键。跨团队沟通模板本质是"降低信息不对称"：把异步沟通必需的 5 要素（请求/背景/影响/期限/下一步）一次说清，避免来回追问。

## 🟣 部分四·深挖层（为什么、历史、延伸）
Git 由 Linus Torvalds 2005 为管 Linux 内核而生（因 BitKeeper 收回免费授权），如今是事实标准。顶级工程师追问：分支模型怎么选？GitHub Flow（主干+短命分支+PR）适合持续交付；GitLab Flow 加环境分支；老牌 Git Flow 较重。你个人项目用"主干+特性分支+PR（哪怕自己审）"最清爽。还有"提交信息规范"（Conventional Commits：`feat:/fix:/docs:`），你 PD13 已用 `docs:` 前缀，正是顶级习惯。 협업 工具链（GitHub Issues/Projects、PD15 敏捷）与 Git 互补。

## 🔴 部分五·顶级视角
顶级大佬的协作观："代码评审（Code Review）不是找茬，是集体所有权与知识扩散"——每个人写的代码团队共有，避免"只有某人懂某模块"的单点故障（Bus Factor）。沟通上，他们践行"书面优于口头、异步优于同步"：重要决策写进文档/Issue 而非只在群里说（群消息会沉）。在方向10 里，GitOps（OPS32 ArgoCD）把"Git 是单一事实源"推到运维极致。你从"自己用 Git 管教案/作品集"起步，已在练顶级协作纪律——未来进团队无缝衔接。

## 🟠 部分六·安全/合规种子（白帽视角）
协作的红线：① 绝不把密钥/令牌提交进 Git（用 `.gitignore` + 环境变量，方向10 OPS47）；② 开源仓库（如你未来 GitHub）不提交他人专有代码/内部资料；③ PR 评审要查"是否夹带敏感信息/后门"（供应链安全，方向10 OPS46 扫描）；④ 跨团队沟通不泄露无权分享的信息（如客户数据）。白帽产品人/工程师："协作透明但边界清晰，能开源的开放、该保密的锁死"。你作品集进 Git 时，确认不含隐私再 push 到公开仓库。

## ✅ 验收
交：① `git log --oneline` 显示分支合并记录截图；② 一份用模板写的"给后端同学的协作请求"。齐算过关。

## ⚠️ 常见坑
- 长期不 commit、一次堆百行 → 合并冲突爆炸，难回退。
- 把 `node_modules/`、密钥文件 commit 进库 → 又大又泄，尽早 `.gitignore`。
- 沟通只说"帮我弄下" → 缺背景/期限，对方无法排期。
- 手疼多用 Git Bash 复制命令，少手敲长路径。

## ➡️ 下一步
PD15 · 敏捷 / 项目管理（Scrum / Kanban）——把"协作"升级为"有节奏的迭代交付"，用看板管理你作品集/学习路线，像顶级团队一样跑 Sprint。

---

# PD15 · 敏捷 / 项目管理（Scrum / Kanban）

## 🎯 目标
学完这节，你能用"看板（Kanban）"管理你整个学习/作品集路线：Backlog（待办）→ Doing（进行）→ Done（完成），并理解 Scrum 的 Sprint/站会/评审概念。你将把 PD1–PD14 的产出物列成一张真实看板，并规划下一个 Sprint（含 PD16 简历/作品集冲刺）。

## 📋 小白前置
需先懂 PD14（协作/沟通）、PD10（MVP/度量）。本节点偏方法论+轻量工具实操。

## 🟢 部分一·最浅层（生活比喻）
项目管理像"收拾一堆待洗衣服"：你把脏衣服堆（Backlog），拿几件去洗（Doing），洗好叠好（Done）。关键是不一次拿太多（会乱），且能一眼看到"还有多少没洗"。敏捷就是"小批快洗、常检查"——别等攒一个月再洗，那样永远完不成。Scrum 是"每两周必洗完一批并晒成果"，Kanban 是"水流式，洗完一件补一件"。

## 🟡 部分二·动手层（逐字操作）
1. 用 GitHub Projects（网页，免费）或 Trello 建看板，三列：**Backlog / Doing / Done**。
2. 把方向11 已完成的 PD1–PD14 各建一张卡拖到 Done；PD15 自己一张 Doing；PD16/PD17 放 Backlog。
3. 给每张卡写"验收标准"（复用前面各节 ✅ 验收），如"PD1：交付 CRAP 对比 PNG + 作品集打分"。
4. **Sprint 规划**（写在 `Sprint计划.md`）：
```
Sprint 1（本周）：完成 PD15 看板 + PD16 简历作品集冲刺
Sprint 2（下周）：PD17 学习方法 + 开技术博客
每日微站会（对自己）：今天做哪张卡？卡哪了？需要啥？
```
5. 用 Git Bash 看进度（你教案已进 Git）：
```bash
cd /c/Users/qq/WorkBuddy/2026-08-26-13-18-54/全领域教案
git log --oneline --since="2 weeks ago"
```

### 🧪 实战 Lab（PD15）
截图你的看板（Backlog/Doing/Done 三列，含 PD1–PD17 卡片），并写一份 `Sprint计划.md`（含本 Sprint 目标 + 每日微站会模板）。把截图 + md 发我。

## 🔵 部分三·原理层（底层发生了什么）
Kanban 源于丰田"Just-In-Time"生产：用"卡+列+在制品上限（WIP Limit）"可视化流动，瓶颈（某列堆满）一眼可见。Scrum 是框架：固定长度 Sprint（通常 2 周）、角色（PO/SM/团队）、 ceremonies（计划会/每日站会/评审/回顾）。站会三问（昨天/今天/障碍）是"同步信息、暴露阻塞"的 15 分钟机制，不是汇报功绩。两者都服务"小批快反、持续交付、快速纠偏"——对抗"大瀑布一次性交付、错了全翻"。

## 🟣 部分四·深挖层（为什么、历史、延伸）
敏捷宣言 2001 年由 17 位软件先驱提出（"个体互动高于流程工具"等四价值观），是对"重型文档瀑布"的反叛。顶级工程师追问：敏捷会变"形式主义"吗？会——"伪敏捷"（天天站会但没人改、Sprint Review 变表演）很常见。真敏捷看"是否能持续交付价值+快速学习"。还有"看板 vs Scrum 怎么选"：维护型/支持流用 Kanban（连续流），新开发用 Scrum（迭代冲刺）。你个人学习路线用 Kanban 更自然（没有固定 Sprint 强制），但借 Scrum 的"定期回顾"反思很值。

## 🔴 部分五·顶级视角
顶级大佬把"交付节奏"当核心竞争力：他们用 OKR（目标-关键结果）对齐战略、用看板管执行、用回顾会持续改进（Retrospective：什么好/什么差/改什么）。在方向10 SRE 里，"错误预算（OPS36）"就是把敏捷"可持续 pace"用于可靠性。你用看板管"从零到顶级大佬"的漫长路线，是把巨型目标拆成可流动的小卡——避免被"17 方向几百节课"压垮。顶级心法："大目标靠流动，不靠突击"。

## 🟠 部分六·安全/合规种子（白帽视角）
项目管理的合规点：① 看板/文档若含客户或内部敏感信息，放私有仓库/内部工具，不进公开 Trello；② 进度汇报诚实——不虚报"Done"凑数（你承诺不虚构，进度也真实）；③ 敏捷"可持续 pace"本身就是护手红线呼应：不强撑突击，手疼就休，课永远在（总纲红线）。白帽产品人："进度真实、信息安全、节奏健康"三位一体。

## ✅ 验收
交：① 看板截图（含 PD1–PD17 卡片分布）；② `Sprint计划.md`。齐算过关。

## ⚠️ 常见坑
- 卡片写太大（"做完作品集"）→ 永远 Done 不了，拆成"加邮箱入口""写 PRD"等小卡。
- WIP 不设限 → Doing 堆 10 张，哪件都半途。
- 站会变闲聊超时 → 严格三问 15 分钟。
- 手疼用网页看板点选，比写长篇省事。

## ➡️ 下一步
PD16 · 面试 / 简历 / 作品集（展示价值）——把 PD1–PD15 所有产出整合成"顶级白帽工程师"的求职资产，直接优化你已做的 `作品集_HR成品版`，并写一份合规简历。

---

# PD16 · 面试 / 简历 / 作品集（展示价值）

## 🎯 目标
学完这节，你能：① 把已生成的 `作品集_HR成品版/index.html` 当成"白帽合规作品集"的范本，列出它的优势与可补强点；② 写一份**真实、不虚构**的简历（Markdown），结构含"技术栈/可验证项目/合规声明"；③ 准备 3 个"用作品集讲故事"的面试回答框架。所有内容严守"不冒充、不盗用、不虚构"。

## 📋 小白前置
需先懂 PD3（心智模型）、PD9（用户研究）、PD10（产品逻辑）、PD12（指标）、PD14（协作）。本节点是方向11 的"出口"，整合前面所有。

## 🟢 部分一·最浅层（生活比喻）
求职资产像"摆摊卖自己"：作品集是"摊位上真能试用的样品"（比嘴说"我厉害"强百倍），简历是"一页宣传单"（写清你卖啥、谁来买），面试是"现场讲解+答疑"。最怕"宣传单吹上天、摊位空空如也"——人家一试用就穿帮。你的作品集已经是"样品真能跑"，这是最大优势。

## 🟡 部分二·动手层（逐字操作）
1. **审计作品集（用前面所学）**：打开 `作品集_HR成品版/index.html`，对照清单打勾：
   - [x] 真实可运行项目（你站强调"可运行可验证有实测数字"——顶级加分）
   - [x] 合规声明板块 `#compliance`（白帽红线已显式呈现——稀缺优势）
   - [ ] 联系方式是否在导航显眼处？（PD3/PD9 发现的可能卡点，检查修复）
   - [ ] 有无"博客/技术文章"沉淀影响力？（PD11 PRD 待做项）
2. **写简历（VS Code 新建 `简历_王承泽.md`，复制骨架）**：
```markdown
# 王承泽 · 全栈 + 逆向安全工程师（求职简历）
## 一句话定位
从零构建可运行系统的全栈工程能力 + 无源码逆向分析能力。
## 技术栈
- 语言：Python / JavaScript / TS（按真实掌握填，不会的不写）
- 前端：HTML/CSS/Vue(学)/React(学)
- 安全：白帽方法论/授权靶场/CTF(学)
- 工具：Git / Figma / VS Code
## 可验证项目（附链接+实测数字）
1. 作品集站：纯静态、可运行、含合规声明（本文件）
2. [真实做过的项目，写实测数据，如"接口压测 QPS X"]
## 合规声明
- 仅进行授权/自有样本的安全研究；不攻击他人系统、不破解收费软件。
- 作品与经历真实，无虚构、无冒充、无盗用。
```
3. **面试故事框架（写 `面试故事.md`）**：
```
故事1（解决问题）：展示一个你真做过的项目，用"背景-行动-结果(量化)"讲。
故事2（白帽底线）：被问"你会不会攻破 XX"时，答"只在授权/自有范围，红线不碰"。
故事3（学习能力）：讲你"从零按系统路线学到 X"的过程，证明可持续性。
```

### 🧪 实战 Lab（PD16）
完成三份交付：① `简历_王承泽.md`（真实、含合规声明，不会的技能不写）；② `面试故事.md`（3 个框架已填真实内容）；③ 一份 `作品集优化清单.md`（基于本节点审计，列 3 条可补强项如"导航加邮箱""加博客"）。把三份发我。

## 🔵 部分三·原理层（底层发生了什么）
求职是"信号传递"：HR/面试官在信息不完整下判断你价值，作品集/简历/回答都是"降低不确定性的信号"。**可验证证据**（能跑的项目、实测数字）是最强信号，因为难伪造；"空泛形容词"（"热爱技术""团队精神"）是最弱信号，因为人人会写。合规声明是把"白帽身份"做成**差异化信任信号**——在安全岗位求职中，明确"只授权范围"反而比含糊更可信（雇主怕招到乱来的人）。简历用 Markdown 便于进 Git 版本管理、可一键转 PDF（如 `mdpdf` 或 VS Code 插件）。

## 🟣 部分四·深挖层（为什么、历史、延伸）
简历从纸质到 LinkedIn/在线作品集演化，"作品集 > 简历"是工程/设计岗共识。顶级工程师追问：怎么应对"简历筛选 ATS（申请人追踪系统）"？要含关键词（真实掌握的技能名词）、避免花哨排版干扰解析。还有"故事化面试"（STAR 法则：Situation/Task/Action/Result）是行为面试标准框架——你 `面试故事.md` 就是 STAR 雏形。白帽合规声明是"防御性求职"：提前划清边界，既显专业又规避"被误解成危险人物"的风险（安全岗特殊敏感）。

## 🔴 部分五·顶级视角
顶级大佬的求职资产是"长期复利"：作品集是活资产（随项目增长）、技术博客（PD17）是影响力资产、GitHub 是代码信用资产。他们把每次面试当"用户访谈"（PD9）——收集反馈优化下次的呈现。在安全领域，顶级白帽的"合规人设"本身就是品牌：雇主抢着要"能力强且绝对守规"的人。你作品集已内置合规板块，这是绝大多数求职者没有的稀缺件——把它讲成核心卖点。

## 🟠 部分六·安全/合规种子（白帽视角，本节点重中之重）
这是全方向红线的集大成：① **简历零虚构**：没做过的项目不写、不会的技能不列（写"学习中"）；② **不冒充**：不写"某大厂实习"若没有，不挂别人项目当自己；③ **不盗用**：作品集代码/设计/文章皆原创或合规开源（已标注），不扒别人站；④ **白帽边界明示**：只授权/自有样本研究，这既是合规也是卖点；⑤ **防隐私泄露**：简历/作品集不含身份证号、真实住址、私密联系方式（用邮箱+GitHub 足够）。你已承诺"不虚构不冒充不盗用"——本节点把它落成可投递的真实资产。

## ✅ 验收
交：① `简历_王承泽.md`（真实+合规声明）；② `面试故事.md`（3 框架填实）；③ `作品集优化清单.md`（≥3 补强项）。齐算过关。

## ⚠️ 常见坑
- 为"显得强"虚构项目/技能 → 面试一问穿帮，永久失信，且违背你承诺。宁写"学至 X"。
- 简历堆形容词无证据 → 改成"做了 X，实测 Y"。
- 作品集暴露隐私（真地址/证件）→ 立即删。
- 手疼用语音写 md + 复制骨架，少敲字。

## ➡️ 下一步
PD17 · 学习方法 / 元认知（高效习得）——把"怎么学"本身变成可方法论的能力，给你"从零到顶级大佬"的终身发动机，并接入技术影响力与博客。

---

# PD17 · 学习方法 / 元认知（高效习得）

## 🎯 目标
学完这节，你能用"元认知（对学习的认知）"规划你的终身学习：建立"费曼输出+间隔重复+项目驱动"的个人学习系统；启动一份技术博客（Markdown 写、可进作品集）；并写出你的"从零到顶级大佬"路线图自检表。这是方向11 的收尾，也是你所有方向的"永动机"。

## 📋 小白前置
需先懂全部 PD1–PD16（尤其 PD10 产品思维、PD13 写作、PD16 作品集）。本节点是"学习如何学习"，适合你时间极充裕的优势发挥到极致。

## 🟢 部分一·最浅层（生活比喻）
元认知像"看着自己吃饭的那个你"：不只"吃"（学），还"看自己吃没消化、哪没嚼烂"。普通人学完就完，顶级学习者会问"我刚真懂了吗？能讲给小白听吗？明天还记不记得？"——这层"对自己学习的观察与调控"，就是元认知。博客像"把嚼烂的食物再吐出来喂别人（费曼）"——最能检验你真懂没。

## 🟡 部分二·动手层（逐字操作）
1. **建学习系统文档**（VS Code 新建 `我的学习系统.md`）：
```markdown
# 我的学习系统（元认知 v1）
## 三件套
- 项目驱动：每学一节必做 Lab（如 PD 各节 ✅ 验收）
- 费曼输出：学完写 1 段能讲给小白的话 / 或一篇博客
- 间隔重复：用 Anki 或每周回顾卡（PD15 看板）复习
## 自检问题（每次学完问自己）
1. 我能不看资料重做一遍吗？
2. 我能给零基础讲清吗？（费曼）
3. 这节和哪个已学方向相通？（如 PD6 token ↔ PY12 模块）
## 路线自检表（对照总纲）
- [ ] 预备层 PRE0-8 完成
- [ ] 方向1a Python PY1-25
- [ ] 方向11 PD1-17（本节即收尾）
- [ ] 方向7 安全（最看重，按路线推进）
```
2. **启动技术博客（Markdown）**：在作品集目录建 `blog/` 文件夹，写第一篇 `blog/我的白帽学习路线.md`，内容就是"你为什么走白帽、路线怎么排、红线是什么"——真实、合规、有观点。
3. **Git 提交博客**（练 PD14 协作流）：
```bash
cd /c/Users/qq/WorkBuddy/2026-08-26-13-18-54/作品集_HR成品版
git init  # 若未初始化
git add blog/
git commit -m "blog: 新增白帽学习路线第一篇"
```
4. 未来可把 `blog/` 接入作品集导航（PD11 写过 PRD 的"博客板块"正好用上）。

### 🧪 实战 Lab（PD17）
交付三样：① `我的学习系统.md`（含三件套+自检+路线自检表）；② 第一篇博客 `blog/我的白帽学习路线.md`（≥300 字，真实合规）；③ 用费曼法给"CRAP 原则（PD1）"写一段"讲给完全外行听的话"（50 字内）。把三样发我。

## 🔵 部分三·原理层（底层发生了什么）
元认知由 Flavell 1970s 提出，指"关于认知的认知"——包含**计划（学之前定策略）、监控（学中觉察卡点）、调节（卡住换方法）**三过程。费曼技巧利用"输出倒逼输入"：要讲清必须自己先结构清晰，暴露知识漏洞。间隔重复对抗"遗忘曲线"（Ebbinghaus：一天不复习忘 70%），用 Anki 按遗忘节奏复习。项目驱动符合"建构主义"——知识在"用"中建构比"看"中牢固。这些不是鸡汤，是认知科学可证的方法。

## 🟣 部分四·深挖层（为什么、历史、延伸）
"学习如何学习"是 20 世纪教育心理学核心（Bloom 掌握学习、Anderson 认知技能习得）。顶级工程师追问：为什么有人"学得快且透"？部分天赋，但更大比例是"元认知强+刻意练习（Deliberate Practice，Ericsson）"——练的是"刚好超出当前能力的、有反馈的"内容，而非重复已会的。还有"迁移学习"：你 PD6 token 和 PY12 模块都讲"复用/不要重复"，本质同一思维在不同领域"迁移"——顶级大佬擅长跨域迁移，所以"新语言几天上手"（总纲目标）。你路线覆盖全方向，正是为最大化迁移机会。

## 🔴 部分五·顶级视角
顶级大佬把"学习力"当终极护城河：技术会过时，但"快速习得+跨界迁移+输出影响"的能力不过时。他们经营"技术影响力"（博客/开源/演讲 PD15 外）：写博客不是炫耀，是"把知识固化+帮别人+建个人品牌"的三赢。在方向9/方向7，写技术文/Writeup（SEC97）直接涨行业声誉。你启动的博客，未来可沉淀 CTF 复盘、逆向笔记、学习路线——既是 PD16 作品集的"影响力板块"，也是顶级习惯的实练。终身学习者不焦虑"学不完"，因为他们有系统，每天流动一点。

## 🟠 部分六·安全/合规种子（白帽视角，全程贯穿的收束）
学习系统的合规底线：① 只学/只练授权与自有范围的内容（总纲红线，从不松动）；② 博客写安全只分享"方法论/防御视角/授权复盘"，不写"攻击他人实操"；③ 不买野鸡包会班、不用盗版资料（你已承诺拒野鸡拒盗版），用正版/开源/官方文档；④ 身体红线是元认知的一部分——"手疼/头晕立刻停"不是软弱，是可持续学习的工程纪律（总纲：课永远在、人必须在）。白帽产品人/工程师的终极画像："能力强、守规、会学习、能输出、身体稳"。

## ✅ 验收
交：① `我的学习系统.md`（三件套+自检+路线表）；② 博客 `blog/我的白帽学习路线.md`（真实≥300字）；③ 费曼版 CRAP 50 字讲解。齐算过关。

## ⚠️ 常见坑
- 只收藏不输出 → 知识虚胖，一讲就卡（费曼治此病）。
- 贪多不复习 → 学 20 节忘 15 节，间隔重复补上。
- 硬撑突击伤手 → 元认知要监测身体信号，疼就停。
- 手疼用语音写博客初稿，再复制整理，少打字。

## ➡️ 下一步（方向11 完结，回到总路线）
方向11 全部 PD1–PD17 完成。按总纲全局路线，你已具备"产品设计 + 软技能 + 协作 + 求职 + 终身学习"的顶级辅助能力。下一步回到主线：**方向1a Python 已铺开 → 继续方向1 全栈地基 → 方向4 网络 → 方向5 数据库 → 方向6 Web 全栈（毕业作闪电号卡）→ 方向7 安全（你最看重）**。每学一节，用本方向的"产品思维/写作/协作/学习系统"去包裹它，你会比"只写代码的人"走得更高。

---

> **本文件完结说明**：覆盖 PD1–PD17 共 17 节，每节严格含 11 段（🎯目标/📋小白前置/🟢最浅层/🟡动手层/🔵原理层/🟣深挖层/🔴顶级视角/🟠安全合规种子/✅验收/⚠️常见坑/➡️下一步），含 Figma 网页版、Git Bash（`/c/...` 路径）、Markdown、本地 `python -m http.server` 等真实可跑操作，并以你已生成的 `作品集_HR成品版/index.html` 为贯穿实战案例。白帽红线（不虚构/不冒充/不盗用/只授权自有）贯穿全案。
