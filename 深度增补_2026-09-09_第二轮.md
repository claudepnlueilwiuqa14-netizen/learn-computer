# 深度增补教案 · 2026-09-09 第二轮

这是一份接在 590 节主线之后的“深水区执行层”。它把八大宏域毕业工程当前最重要的高难缺口拆成可闯关课包。原课、补强卡、历史笔记和学习者证据目录不被覆盖；本文件只增加新关卡与验收合同。

## 使用方式

每个课包按 `L0 -> L1 -> L2 -> L3 -> L4 -> L5` 顺序推进。L0 是环境与术语核对，L1 是可运行最小路径，L2 是单变量对照，L3 是手写机制/证明，L4 是受控故障与恢复，L5 是研究包与设计答辩。任何一关失败都要保留失败输出、根因假设、修复 diff 和重跑结果；机器基线只能作为参考，不会替学习者勾选证据目录。

每个 Lab 的提交最少包含：

```text
evidence/<课号>/labs/<lab1..lab5>/
  README.md       # 目标、前置、授权边界、结论
  commands.txt    # 原样命令与版本
  notes.md        # 机制解释、变量、失败分支、未验证项
  tests/          # 学习者新增测试或性质测试
  artifacts/      # 日志、指标、trace、截图、哈希
```

## 课包 A · 预备与编程：跨语言语义一致性与可复现发布

绑定原课：`C1`、`RS1`、`GO1`、`JS1`、`PY4`、`PAR7`；毕业工程：`capstone/01_预备与编程/`。

### 目标与深度线

- L0：解释词法、递归下降、整数除法、退出码、UTF-8、临时目录和锁文件；确认 Python、MSYS2 Bash、gcc、rustc、go、node 版本。
- L1：运行四种实现的 16 个固定表达式向量；记录标准输出、标准错误、退出码和耗时。
- L2：一次只改变一个变量：空白、括号深度、负数、整数溢出边界、除零、非法字符；比较四种实现的差异并写出兼容策略。
- L3：手写一个新运算符或错误分类，给出语法图、AST/调用栈、错误不变量和跨语言等价性证明草稿。
- L4：隔离缺少依赖、只读输出目录、截断输入和并发重复运行；要求失败可观察、临时制品清理、源文件哈希不变。
- L5：建立可重放发布包：锁定编译器版本、生成 SBOM/哈希、冷启动与包体基准、签名验证和回滚演示。

### 五个 Lab

1. **向量契约**：扩展 `tests/test_cross_language.py`，至少 30 个正常/错误向量；每个向量同时保存四个实现的 stdout、stderr、退出码。
2. **性质与反例**：固定随机种子生成加减乘表达式；故意制造一个实现差异，用最小输入缩减器得到最短反例，再修复并回归。
3. **工具链对照**：在 MSYS2 Bash 和 PowerShell 各运行一次；记录路径转换、换行、编码和权限差异，写出“为何命令在一个 shell 成功、另一个失败”。
4. **故障与回滚**：在临时目录内模拟只读、缺依赖和重复执行；证明失败不会修改源、锁文件或既有输出，提交时间线与清理检查。
5. **发布研究包**：生成版本清单、SBOM、SHA-256、签名验证、冷启动/包体表格；用错误签名触发拒绝，再恢复到上一版本。

资料锚点：Python argparse/hashlib 官方文档、Rust Reference、Go specification、MDN JavaScript、POSIX exit status；每项记录访问日期和已验证命题。

## 课包 B · 算法与数学：证明、基准、并行对照与最小反例

绑定原课：`DSA1`–`DSA25`；毕业工程：`capstone/02_算法与数学/`。

### 目标与深度线

- L0：复述循环不变量、归纳法、摊还分析、概率期望、图最短路的前提。
- L1：运行 Stack、RingQueue、BST、MinHeap、UnionFind、二分查找和 Dijkstra 基线。
- L2：对空、重复、排序、最坏、负权和规模 `10^2/10^4/10^6` 输入做单变量基准，记录 P50/P95、内存和失败类型。
- L3：独立写出二分正确性、并查集逆 Ackermann 上界、Dijkstra 非负权证明；用反例逐条攻击证明前提。
- L4：故意破坏边界或不变量，要求性质测试自动缩减为最小反例；修复后冻结回归样本。
- L5：实现一个并行或外存对照版本，比较线程开销、缓存局部性、吞吐和可解释性，提交设计答辩。

### 五个 Lab

1. **结构不变量**：每个结构写状态机表和断言；至少 200 个固定种子样例，失败时输出最小状态序列。
2. **证明审查**：对 `docs/proofs.md` 的三份证明做“命题-前提-关键步-反例-修复”审查，不能只抄结论。
3. **基准与统计**：重复 10 次，报告中位数、P95、置信区间或误差来源；解释 Python 解释器、缓存和分配器的影响。
4. **故障注入**：注入负权、越界、重复 union、队列满/空；保留失败日志、最小反例和修复前后差异。
5. **并行对照**：选择并行归并排序、并行 BFS 或批量 UnionFind 之一；在固定机器上比较单线程、线程池和进程池，并解释何时并行更慢。

资料锚点：MIT 6.006、Open Data Structures、CLRS、Hypothesis 文档、Python `concurrent.futures`；严禁用“跑得快”代替复杂度证明。

## 课包 C · 计算机系统：从源码、汇编到线程与崩溃根因

绑定原课：`ASM1`、`OS1`、`OS7`、`CMP1`、`CMP8`；毕业工程：`capstone/03_计算机系统/`。

### 目标与深度线

- L0：识别 ABI、栈帧、符号、调试信息、互斥锁、竞态、信号和清理路径；探测工具是否提供 `threads.h`，若无则说明为何使用 pthread。
- L1：编译环形队列与 pthread 计数器，保存 gcc 参数、汇编、`objdump -d`、`nm` 和运行输出。
- L2：只改变优化级别、锁策略或队列容量，测量指令体量、锁等待、吞吐和尾延迟。
- L3：用 GDB batch 模式对自有 `SIGABRT` 探针执行 backtrace，定位源码行并解释信号到进程退出的路径。
- L4：在隔离临时目录注入竞态、非法地址或中断写入；要求观察故障、恢复/清理并保留 core/backtrace 哈希。
- L5：把用户态函数追到系统调用/内核边界，完成 xv6/Nand2Tetris 对照或最小持久化恢复实验。

### 五个 Lab

1. **编译链证据**：同一源文件生成 `-O0`/`-O2` 汇编，标出函数序言、循环和调用约定。
2. **同步对照**：比较 mutex、原子计数器和无锁错误版本；固定线程数，报告丢失更新与锁开销。
3. **崩溃符号化**：运行 GDB `run/bt`，提交源码行、寄存器/栈观察和根因叙述；不要只提交“程序崩了”。
4. **恢复实验**：在临时文件上模拟写入中断或进程退出，验证重启后提交记录保留、未提交尾部丢弃、不变量、清理和幂等恢复；对照 `capstone/03_计算机系统/src/persistence_probe.c`，再由学习者改写一次恢复器。
5. **跨层答辩**：绘制源码 -> 编译器 -> 汇编 -> 装载 -> 系统调用 -> 内核/硬件的路径图，逐节点绑定一条可观察证据。

资料锚点：GDB Manual、System V AMD64 ABI、POSIX pthread、OSTEP、Nand2Tetris、xv6 book；工具缺失必须记录，不得静默跳过。

## 课包 D · 网络与分布式：协议字段、重试、分区与容量

绑定原课：`NET1`–`NET24`、`OPS37`、`OPS43`；毕业工程：`capstone/04_网络与分布式/`。

### 目标与深度线

- L0：解释 HTTP 请求行、状态码、Content-Length、超时、重试、幂等、分区和时钟偏差；服务只绑定 `127.0.0.1`。
- L1：启动两个本地节点，完成 PUT/GET 复制和主节点停止后的副本读取。
- L2：只改变超时、重试次数、退避和节点顺序；记录每次尝试、错误类型、收敛时间和最终状态。
- L3：把可观察 HTTP 字段映射到 RFC 9110 条款；进一步用 Wireshark/pcap（仅回环）核对 TCP/TLS/IPv6 字段。
- L4：注入端口关闭、延迟、丢包、分区和乱序；要求说明哪些故障可自动恢复、哪些必须人工介入。
- L5：完成容量预算、quorum 语义、时钟偏差实验，并比较 HTTP/1.1 与 HTTP/2/3 的取舍。

### 五个 Lab

1. **协议观察**：记录 status、版本、Content-Type、Content-Length、请求/响应时间；每个字段写 RFC 依据。
2. **重试状态机**：画出 attempt/backoff/stop 状态机；用一个故障节点和一个健康节点验证“第二次成功”。
3. **分区实验**：只在两个回环进程间阻断连接，记录错误预算、读写一致性和恢复时间。
4. **副本与延迟**：制造可控复制延迟，计算 lag、staleness 和丢失项；说明为何单文件快照不是流复制。
5. **容量研究包**：给出并发连接、吞吐、P95/P99、带宽和节点成本假设；用压测结果校正预算并保留不确定性。

资料锚点：RFC 9110、RFC 9293、RFC 8446、RFC 9000、Wireshark User's Guide、Kubernetes Service 文档。

## 课包 E · 数据库与数据工程：WAL、复制、权限与血缘

绑定原课：`DB1`–`DB28`、`DAT1`、`DAT16`；毕业工程：`capstone/05_数据库与数据工程/`。

### 目标与深度线

- L0：解释事务原子性、WAL、RPO/RTO、索引选择性、角色权限和血缘图；所有数据为合成订单。
- L1：运行约束冲突、WAL 崩溃恢复、101 行备份、索引执行计划前后对照。
- L2：只改变索引、事务批量、同步/异步快照和查询谓词；记录 P50/P95、WAL 大小、锁等待和备份时间。
- L3：独立解释恢复为什么丢弃未提交 sentinel；审阅第三个查询计划并证明索引是否真的被使用。
- L4：注入杀进程、延迟副本、错误迁移和权限拒绝；要求恢复到已知快照并保留审计事件。
- L5：实现快照主从故障切换的限制说明、角色矩阵和可审阅 lineage/v1 制品；明确它仍不是生产流复制。

### 五个 Lab

1. **WAL 时间线**：提交前、提交后、异常退出、重启四阶段各保存数据库状态和退出码。
2. **计划实验**：至少三个独立工作负载，保存 `EXPLAIN QUERY PLAN`，解释全表扫描/索引扫描变化。
3. **快照复制**：复制到独立 SQLite 文件，停止主文件访问，从副本读取并计算 lag、RPO/RTO。
4. **权限与审计**：用 reader/writer/operator 三角色执行允许/拒绝矩阵；审计每次写入与拒绝。
5. **血缘图**：从合成输入、订单、备份和报表构建 lineage JSON；校验节点/边哈希和删除敏感字段。

资料锚点：SQLite WAL、backup API、EXPLAIN QUERY PLAN、PostgreSQL MVCC/角色权限、OpenLineage 规范。

## 课包 F · Web 产品：持久化、幂等、a11y、性能与回滚

绑定原课：`FE1`–`FE18`、`BE1`–`BE26`、`PD8`；毕业工程：`capstone/06_Web产品/`。

### 目标与深度线

- L0：解释 DOM 语义、键盘焦点、会话签名、CSRF、幂等键、持久化和版本回滚；不接真实支付。
- L1：运行语义页面、套餐 API、HMAC 会话、CSRF、角色授权和订单提交。
- L2：只改变重复请求、会话篡改、缺 token、慢依赖、窄屏和版本号；记录 HTTP 状态与用户可见错误。
- L3：审查订单表唯一约束与 `PaymentLedger` 的并发边界；用键盘和自动化 a11y 工具验证 label、focus、名称/角色/值。
- L4：注入依赖超时、数据库重启和坏版本发布；要求订单状态不重复、错误可恢复、回滚后套餐回到 v1。
- L5：真实浏览器性能 trace，测量 LCP/INP/CLS、API P95、错误率和回滚时间，提交预算与 ADR。

### 五个 Lab

1. **浏览器语义**：用 Playwright 或等价工具完成键盘导航、窄屏截图、语义树和失败截图。
2. **幂等订单**：并发发送相同 `X-Idempotency-Key`，验证只产生一行并返回相同 `order_id`。
3. **认证攻击面**：篡改签名、过期会话、缺 CSRF、reader 写入；记录 401/403 和服务日志。
4. **持久化恢复**：服务重启后 `/api/orders` 仍返回订单；模拟数据库不可用并验证静态套餐回退。
5. **发布回滚**：激活 v2、注入坏配置、回滚 v1；测量恢复时间并提交版本哈希和操作手册。

资料锚点：WCAG 2.2、WAI-ARIA APG、MDN HTTP/Cookies、OWASP ASVS、Web Vitals、Playwright 文档。

## 课包 G · 安全与逆向：修复 diff、回归和授权隔离

绑定原课：`SEC1`–`SEC102`、`SEC-W/B/C`；毕业工程：`capstone/07_安全逆向/`。

### 目标与深度线

- L0：读懂授权范围、停止条件、证据哈希、TP/FP/FN 和最小披露原则；只用自有 `policy.py` 与合成文本。
- L1：运行 20 条合成语料，得到旧扫描器 TP/FP/FN 和输入边界拒绝。
- L2：单变量改变注释、字符串、AST、环境变量和大小写；比较旧行扫描器与 AST v2。
- L3：逐行审查修复 diff，解释为什么 AST 版本减少注释/字符串误报，哪些语法仍可能漏报。
- L4：在有快照的授权隔离靶场制造无害拒绝路径；验证快照恢复、停止条件和证据链完整。
- L5：多版本回归、风险排序、管理层一页纸和技术附录双版本报告；明确残余风险而非宣称“安全”。

### 五个 Lab

1. **授权核对**：提交范围文件哈希、允许动作、禁止动作、有效期和停止条件；任何越界立即停止。
2. **检测对照**：同一 corpus 跑 v1/v2，计算 precision/recall、误报/漏报和检测延迟。
3. **修复 diff**：创建最小修复提交，绑定失败样例、测试、回归输出和变更审查者。
4. **隔离快照**：在本地容器/虚拟机或模拟器恢复快照，运行无害样本，保存输入/输出哈希和清理证明。
5. **双版本报告**：分别写工程师附录与管理摘要，包含影响、证据、修复状态、停止条件和残余风险。

资料锚点：OWASP ASVS、CWE、CVSS、NIST NICE、MITRE ATT&CK（防御映射）、Python `ast` 官方文档。禁止公网扫描、第三方访问、真实凭据、恶意持久化和规避监管。

## 课包 H · AI、云原生与端侧：数据切分、服务边界、资源与 OTA

绑定原课：`DAT19`、`DAT24`、`DAT35`、`DAT45`、`OPS21`、`OPS41`、`MOB45`；毕业工程：`capstone/08_AI云原生端侧/`。

### 目标与深度线

- L0：解释训练/评估切分、泄漏、漂移、模型制品、提示边界、三信号、资源限制和 OTA 签名。
- L1：运行有限值拒绝、数据/模型指纹、导出/加载、漂移分数、提示注入拒绝和 requests/errors/latency 遥测。
- L2：只改变切分比例、漂移幅度、输入类型和模型版本；记录 MSE、指纹、错误率和 P95。
- L3：审查 `deterministic_split` 的泄漏假设和 `serve_prediction` 的 payload 边界；写出威胁模型与数据卡。
- L4：注入评估集污染、依赖不可用、资源上限和模型回退；要求回到上一模型并保留观测时间线。
- L5：容器/Kubernetes readiness/liveness、CPU/内存预算、签名 OTA 回滚和端侧功耗测量；成本与质量同时答辩。

### 五个 Lab

1. **数据版本**：生成训练/评估 manifest、哈希、切分比例和泄漏反例；禁止把同一指纹同时当训练和测试。
2. **推理 API**：把有限 payload 接到本地服务，拒绝额外字段、NaN、超长输入和提示注入标记。
3. **漂移与三信号**：改变特征均值/方差，观察 drift、error rate、P95，并写阈值选择依据。
4. **资源与回退**：在本地容器或受控进程限制 CPU/内存，模拟重启和模型 v2 -> v1 回退。
5. **端侧发布**：为合成模型生成签名 manifest，篡改后拒绝安装；测量离线缓存、恢复时间和功耗代理指标。

资料锚点：NIST AI RMF、scikit-learn cross-validation、Kubernetes probes/resources、OpenTelemetry、TUF/Sigstore、Android/Apple 官方发布文档。

## 本轮增量机器基线（接在第二轮之后）

以下内容是同一份课包的进一步可复核资产，仍不改变学习者的“未执行”状态：

- **Web F**：真实本地浏览器检查 `lang/main/label`、Tab 焦点顺序、按钮提交后的 `role=status` 反馈、390×844 无横向溢出，并测量 5 次重载 P50/P95；无依赖 `a11y_audit.py` 再硬查语义规则；证据 `capstone/06_Web产品/evidence/browser-2026-09-09.json`。
- **AI H**：回环模型服务 `/health`、`/ready`、`/predict`、`/metrics`，超大请求 413 后恢复正常预测；资源合同 JSON 校验 CPU/内存 requests/limits 和探针路径。
- **数据库 E**：`FencedStreamCoordinator` 记录 LSN/lag，分批 apply，epoch promote 后拒绝旧主写入；生产多进程复制仍未宣称完成。
- **网络 D**：`ScriptedPartition` 模拟两次合成丢失后恢复，记录 attempts/errors/backoff；真实 packet-level pcap 仍需单独实验。
- **算法 B**：固定 10000 项输入比较串行排序与两线程归并排序的正确性和 P50；结果只解释线程开销，不宣称加速。
- **安全 G**：`repair-diff.md` 和 `authorized-lab-snapshot.json` 固化修复根因、回归合同、授权范围和停止条件；快照清楚标注 `descriptor-only`。

## 统一毕业闸门

每个课包只有同时满足以下条件，才能把对应宏域从“深度基线”申请为“学习者验收”：

1. 五级关卡均有学习者本人命令、解释和失败证据，不能只引用机器基线。
2. 至少一个反例被缩减或最小化，至少一个受控故障被观察并恢复，且有清理与回滚。
3. 指标包含测量方法、样本量、环境版本、误差/不确定性和适用边界。
4. 安全、逆向、网络故障和端侧发布均有明确授权范围；不处理真实凭据或第三方目标。
5. 资料记录访问日期、版本、章节、已验证命题和未验证项；链接失效时保留替代来源。

当前状态：本轮机器深度基线 8/8 `BASELINE_PASS`，新增测试和指标已写入各项目 `evidence/deep-baseline-2026-09-09.json`；八项仍为 `graduation_decision=NOT_READY`。学习者 590 节、2965 个 Lab 的证据状态不因机器基线改变。
