# 教案_方向5_数据库（DB1–DB28 完整 11 段深教案）

> 本文件覆盖 DB1–DB28，共 28 节，按统一 11 段深模板 + 实战 Lab 逐节展开。
> 学生画像：真·零基础、术后留置针手不便（护手优先：能复制粘贴就别手打）、时间极充裕、目标成为计算机领域顶级大佬、坚定白帽路线。
> 使用方法：每节先读 🎯→📋→🟢 建立直觉，再照 🟡 动手层逐字操作（命令可整段复制进 Git Bash / VS Code 终端），把结果发我批改，然后回头读 🔵🟣🔴 补理论。卡住随时喊"卡在第 X 步"，我立刻接手。
> 工具链：SQLite3 CLI、Docker 起 MySQL / Redis（命令给出）、Python 的 sqlite3 / mysql-connector / redis-py。命令默认在 **Git Bash** 里跑，路径用 `/c/...` 形式。每条 SQL 都给可直接执行的语句与预期结果；事务/隔离级别给出**两个会话并发**的可复现实验步骤。
> 🔴 白帽红线贯穿：只练自己写的程序、授权靶场、自家虚拟机、CTF。绝不攻击他人系统、不登录他人账号、不逆向他人商业软件、不注入他人进程、不编写或部署恶意工具（只分析不实践）、VPN 只讲原理不教规避。涉及"数据"时特别强化：遵守《个人信息保护法》/GDPR，绝不未授权爬取或泄露他人个人信息。

---

# DB1 · 数据库概论（数据模型演化 + ER 建模入门）

## 🎯 目标
学完这节，你能说清楚"数据库"到底解决什么问题；能亲手用 SQLite 建出第一个库和一张表、插入几行、查出来；能画一张最基础的 ER 图（实体-联系图）描述"学生选课"这种现实关系；并理解从"纸质账本"到"关系数据库"再到"NoSQL"的演化脉络。

## 📋 小白前置
需先完成预备层 PRE0–PRE8（认识电脑、文件管理、装好 VS Code、会用终端、浏览器下载）。方向1a 的 PY1–PY3 有助理解"变量=盒子"，但不强制。本节假设你已经会打开 Git Bash 并 cd 到某个文件夹。

## 🟢 部分一·最浅层（生活比喻，不碰术语）
想象你开了一家小卖部。一开始你用**一本纸质账本**记账：今天卖了几瓶水，拿笔写一行。问题来了：账本翻旧了、字迹糊了、你想"查上个月所有卖矿泉水的记录"得一行行翻——累死。于是你进化到 **Excel 表格**：每行一笔，还能筛选排序。但这还是你一个人用，别人同时改会冲突，而且表格之间（比如"商品表"和"销售表"）的勾稽关系容易写错。

数据库就是"超级智能账本"：它把数据按**表**组织（像 Excel 的 sheet），但由专门的"管家程序"（数据库管理系统，DBMS）统一看管——你只管说"我要查卖矿泉水的记录"，管家去翻、去算、去保证多人同时用也不乱。ER 图呢？就是你在动手建表之前，先用"方块+连线"画一张**关系草图**：方块是"东西"（学生、课程），连线是"它们之间啥关系"（学生"选"课程）。先画草图，再照草图建表，就不会乱。

## 🟡 部分二·动手层（逐字操作；Git Bash；可复制）
先确认 SQLite 可用。打开 Git Bash，逐行复制执行（每行回车）：
```bash
cd /c/Users/qq/WorkBuddy/2026-08-26-13-18-54/全领域教案
sqlite3 demo.db
```
如果提示 `sqlite3: command not found`，说明没装；Windows 去 https://sqlite.org 下载预编译包，解压后把 `sqlite3.exe` 所在目录加入 PATH，或用 VS Code 的 SQLite 插件。装好后重开 Git Bash 再试。

进入 SQLite 交互界面（提示符变成 `sqlite>`）后，复制下面整段执行：
```sql
CREATE TABLE 学生(
  学号 TEXT PRIMARY KEY,
  姓名 TEXT NOT NULL,
  年龄 INTEGER
);
INSERT INTO 学生 VALUES('S001','小明',18),('S002','小红',19),('S003','小刚',20);
SELECT * FROM 学生;
```
预期输出三行：`S001|小明|18` 等（SQLite 默认用 `|` 分隔列）。再试：
```sql
SELECT 姓名, 年龄 FROM 学生 WHERE 年龄 >= 19;
```
预期只返回小红、小刚两行。

### 🧪 实战 Lab（DB1）
不退出 sqlite>，继续建一张"课程"表和"选课"关系表，插入数据并查询：
```sql
CREATE TABLE 课程(课号 TEXT PRIMARY KEY, 课名 TEXT, 学分 INTEGER);
INSERT INTO 课程 VALUES('C01','数据库',4),('C02','网络安全',3);
CREATE TABLE 选课(学号 TEXT, 课号 TEXT, 成绩 INTEGER);
INSERT INTO 选课 VALUES('S001','C01',95),('S001','C02',88),('S002','C01',90);
SELECT * FROM 选课;
```
验收：把 `SELECT * FROM 学生;` 和 `SELECT * FROM 选课;` 的结果截图发我。试着自己加一个学生 S004 和他选的一门课。

### ✏️ ER 草图（纸上/脑中即可）
画三个方块：学生、课程、选课。学生—选课 是"一对多"（一个学生选多门），课程—选课 也是"一对多"（一门课被多个学生选）。选课里同时带着"成绩"这个属性。这正是上面三张表的来源。

## 🔵 部分三·原理层（底层发生了什么）
当你执行 `CREATE TABLE 学生(...)`，SQLite 在你的磁盘上创建了一个文件 `demo.db`，并在文件内部的"系统表"（sqlite_master）里记下这张表的结构（列名、类型、约束）。`INSERT` 时，SQLite 把一行数据按行格式编码写进数据页（page，默认 4KB 一块）；`SELECT` 时它读取数据页、按你给的条件逐行过滤（全表扫描），把匹配的行格式化打印。

DBMS 的核心职责有三：① **持久化**——数据写进磁盘，断电不丢；② **高效查询**——用索引/优化器少读磁盘；③ **并发与一致**——多人同时改不乱（后面的事务/锁章节专门讲）。数据库文件本质就是"被精心组织的一堆字节"，和你的 Word 文档一样躺在硬盘上，只是结构特殊。

## 🟣 部分四·深挖层（为什么这样设计、历史、延伸）
数据模型的演化是一段"越来越会描述现实"的历史：
- **层次模型（1960s，IMS）**：像家谱树，只能一对多，兄弟之间没法直接关联——局限大。
- **网状模型（CODASYL）**：允许多对多，但查询要"顺着指针走"，程序员得记路径，太难用。
- **关系模型（1970，Edgar Codd 论文）**：革命性想法——**所有数据都用"表（关系）"表示，用集合代数（关系代数）来查**，你只声明"要什么"，不用管"怎么找"。这奠定了 SQL 基础，Codd 因此获图灵奖。
- **NoSQL（2000s后）**：面对海量网页/社交数据，传统关系库在"水平扩展"上吃力，于是出现键值（Redis）、文档（MongoDB）、列族（Cassandra）、图（Neo4j）等，放宽了某些约束换扩展性。
- **NewSQL / 分布式数据库**：既想要关系的严格一致，又想要 NoSQL 的扩展，如 TiDB、CockroachDB。

顶级工程师追问：为什么"表"能赢？因为**声明式（你说目标，系统找路径）**比**命令式（你写步骤）**更易维护、更易优化。这思想贯穿 SQL、函数式编程（PAR3）、甚至 AI 的声明式规则。

## 🔴 部分五·顶级视角
顶级工程师看数据库，不是"存东西的地方"，而是**系统的真理之源（source of truth）**——几乎所有后端、Web、App 的数据最终都落库。他们思考的是：数据模型怎么设计才既正确又易演进？读写比例？峰值 QPS？要不要分库？备份怎么算 RPO/RTO？数据库也是安全的主战场（后面 DB28 讲注入、最小权限）。在逆向/取证（方向7）里，你甚至会去解析别人 App 的 SQLite 文件（比如微信的 EnMicroMsg.db，当然只对自己授权样本）还原聊天记录——那是把"数据库文件=字节"理解到极致的应用。

## 🟠 部分六·安全/合规种子（白帽视角）
白帽要点：今天你建的是**自己本地**的 `demo.db`，完全合规。但要种下种子：**数据库里往往存着最敏感的信息（账号、聊天、交易）**，所以"谁能看库、谁能改库"是头等大事。将来你搭任何系统，默认遵循"最小权限"——程序账号只给必要权限，绝不拿 root/管理员账号连库。涉及个人信息时，牢记《个人信息保护法》/GDPR：不未经授权收集、不随意导出、不泄露他人数据。绝不扫描或连接任何不属于你/未授权的数据库实例。

## ✅ 验收
交：① `demo.db` 已建好（或截图证明三张表都在）；② `SELECT * FROM 学生;` 与 `SELECT * FROM 选课;` 的结果截图；③ 你自行加的 S004 记录截图；④ 一张"学生-课程-选课"的 ER 草图（拍照/手绘/文字描述均可）。

## ⚠️ 常见坑
- Git Bash 里路径用 `/c/Users/...` 不是 `C:\Users\...`；cd 后可用 `pwd` 确认。
- SQLite 交互里每行要以 `;` 结尾才执行；忘了 `;` 会一直等输入，看到 `...>` 补个 `;` 回车即可。
- 表名/列名用了中文没问题，但**引号必须是英文半角**；中文全角引号会报错。
- 留置针手疼就复制、别硬敲；课永远在，人必须健康。

## ➡️ 下一步
DB2 · 关系模型/范式——学会把"学生选课"这种草图，规范成"不出错、不冗余"的表结构（1NF/2NF/3NF/BCNF），杜绝"改一个电话要改十行"的灾难。

---

# DB2 · 关系模型与范式（1NF/2NF/3NF/BCNF + ER 落地）

## 🎯 目标
学完这节，你能用"范式"这把尺子检查一张表合不合格；能手改一个充满冗余的"野表"成符合 3NF 的干净设计；理解 BCNF 是什么、什么时候要反范式；并真正把 DB1 的 ER 草图落成规范化的建表 SQL。

## 📋 小白前置
需先懂 DB1（表、行、列、主键概念；会建表插数据）。

## 🟢 部分一·最浅层（生活比喻）
范式就像"收纳整理术"。想象你把所有信息塞进一张超大的便利贴：
> 小明 | 18岁 | 手机=123 | 宿舍=A101 | 选了数据库和网络安全

灾难来了：小明退了"网络安全"，你得找到这行把"和网络安全"抠掉；小明换手机，这行要改；更糟的是，如果小明暂时没选课，这整行可能都不存在，他的手机号也跟着丢了。**冗余**和**更新异常**就是这么来的。

范式告诉你"怎么把一张大贴纸裁成几张小贴纸，每张只讲一件事"：学生贴纸只讲学生，课程贴纸只讲课程，选课贴纸只讲"谁选了啥、几分"。这样改手机号只在学生贴纸改一次，全世界都正确。

## 🟡 部分二·动手层（逐字操作）
继续在 `demo.db` 里玩（先 `sqlite3 demo.db` 进入）。先故意建一张"反例表"感受问题：
```sql
DROP TABLE IF EXISTS 选课;
DROP TABLE IF EXISTS 课程;
DROP TABLE IF EXISTS 学生;
-- 一张野表：把所有东西塞一起
CREATE TABLE 野表(
  学号 TEXT, 姓名 TEXT, 年龄 INTEGER,
  课名 TEXT, 学分 INTEGER, 成绩 INTEGER
);
INSERT INTO 野表 VALUES
  ('S001','小明',18,'数据库',4,95),
  ('S001','小明',18,'网络安全',3,88),
  ('S002','小红',19,'数据库',4,90);
SELECT * FROM 野表;
```
观察：小明年龄 18 重复出现两次——这就是冗余。现在改成规范设计（3NF）：
```sql
CREATE TABLE 学生(
  学号 TEXT PRIMARY KEY, 姓名 TEXT NOT NULL, 年龄 INTEGER
);
CREATE TABLE 课程(
  课号 TEXT PRIMARY KEY, 课名 TEXT, 学分 INTEGER
);
CREATE TABLE 选课(
  学号 TEXT, 课号 TEXT, 成绩 INTEGER,
  PRIMARY KEY(学号, 课号),
  FOREIGN KEY(学号) REFERENCES 学生(学号),
  FOREIGN KEY(课号) REFERENCES 课程(课号)
);
INSERT INTO 学生 VALUES('S001','小明',18),('S002','小红',19);
INSERT INTO 课程 VALUES('C01','数据库',4),('C02','网络安全',3);
INSERT INTO 选课 VALUES('S001','C01',95),('S001','C02',88),('S002','C01',90);
SELECT s.姓名, c.课名, x.成绩
FROM 选课 x
JOIN 学生 s ON x.学号=s.学号
JOIN 课程 c ON x.课号=c.课号;
```
预期：三行正确的"姓名-课名-成绩"关联结果。

### 🧪 实战 Lab（DB2）
判断下面这张表符合第几范式，并改写：
```
订单(订单号, 客户名, 客户地址, 商品1, 商品2, 商品3)
```
提示：一个订单可能有任意多商品→"商品1/2/3"是重复的列，违反 1NF（列的原子性）。改成 `订单(订单号,客户名,客户地址)` + `订单明细(订单号,商品,数量)`。在 SQLite 里建这两张表并插数据验证查询正确。

## 🔵 部分三·原理层（底层/形式化）
- **1NF（第一范式）**：每列都是原子的、不可再分；不能有"重复组"（如商品1/2/3 列）。这是关系模型的底线。
- **2NF**：在满足 1NF 且**有复合主键**时，非主键列必须依赖"整个主键"，而不能只依赖主键的一部分。例：若主键是(学号,课号)，"课名"只依赖课号（部分依赖）→ 应拆到课程表。
- **3NF**：非主键列不能依赖"其他非主键列"（传递依赖）。例：学生表里若加"系名"和"系主任"，系主任依赖系名、系名依赖学号→传递依赖→应再拆出"系"表。
- **BCNF（Boyce-Codd）**：更强——**每一个决定因素都必须是候选键**。3NF 漏掉一种特殊情况（多个候选键交叉决定），BCNF 补上。绝大多数业务 3NF 已够，BCNF 多见于多候选键的复杂设计。

`PRIMARY KEY` 保证行唯一；`FOREIGN KEY` 让 DBMS 帮你做**参照完整性**（你没法插入一个不存在的学号到选课表，除非关掉外键检查——SQLite 默认外键关闭，要 `PRAGMA foreign_keys = ON;` 才生效，这本身是个常见坑）。

## 🟣 部分四·深挖层（为什么、历史、延伸）
Codd 在 1971–1972 年提出范式，本意是**用数学（关系代数）证明"无冗余设计不会出更新异常"**。范式越高，冗余越少，但查询时要 JOIN 的表越多→查询越慢。于是工程上出现**反范式（denormalization）**：故意冗余一点（比如订单表里直接存客户名，免得每次 JOIN 用户表）来换读取速度。这是"正确 vs 性能"的经典权衡。

顶级工程师追问：数据仓库（DB22）反而**鼓励反范式**（星型模型把维度冗余进事实表），因为分析查询多、更新少。而在高并发 OLTP（如电商下单）里，则严格范式 + 必要时缓存。范式不是教条，是"根据读写模式做权衡"的工具。

## 🔴 部分五·顶级视角
规范化的思维本质是**消除重复、单一事实来源（single source of truth）**——这和软件工程里"不要重复自己（DRY）"、函数式编程的"不可变单一状态"完全相通。顶级架构师设计表结构时，会在白板上先画 ER、再套范式、再按访问模式反范式调整，并用迁移脚本（migration）让结构演进可版本化（后面 DB26 ORM / DB27 备份会涉及）。数据库建模能力，是区分"会写 SQL 的码农"和"能扛住真实业务的工程师"的分水岭。

## 🟠 部分六·安全/合规种子
白帽要点：规范化还能**减少敏感数据散落**——如果身份证号只存在"用户"表里一处，就比散在十张表里好管控、好脱敏、好合规。涉及个人信息（《个人信息保护法》），冗余越多泄露面越大。设计表时就把"哪些列是 PII（个人身份信息）"标出来，集中存放、加密存储、最小权限访问。绝不把他人数据塞进你无权拥有的库。

## ✅ 验收
交：① 你建的"学生/课程/选课"三表 + 关联查询结果截图；② 实战 Lab 的"订单/订单明细"两张表建表与查询截图；③ 一段文字说明：野表问题、3NF 怎么改、反范式在什么场景有用。

## ⚠️ 常见坑
- SQLite 默认**不强制外键**，要 `PRAGMA foreign_keys = ON;` 才真正拦截非法引用——很多新手以为建了 FOREIGN KEY 就安全，其实没生效。
- 复合主键 `PRIMARY KEY(学号,课号)` 里两列顺序影响"最左前缀"索引（DB8 会讲），别乱排。
- 中文列名可以，但团队项目建议英文（避免编码/驱动兼容坑）。
- 手疼就复制建表语句，别逐字敲。

## ➡️ 下一步
DB3 · SQL CRUD（声明式 + 关系代数 + DDL/DML + 视图基础）——把"我要什么"翻译成标准 SQL，理解 SELECT/INSERT/UPDATE/DELETE，并第一次见到"视图"这个虚拟表。

---

# DB3 · SQL CRUD 与关系代数（DDL/DML + 视图基础）

## 🎯 目标
学完这节，你能写出完整的增删改查（CRUD）语句；理解 SQL 是"声明式"语言——你说**要什么**，数据库决定**怎么取**；能用关系代数的 π/σ/⋈ 符号对应到 SELECT/WHERE/JOIN；并创建第一个视图（虚拟表）。

## 📋 小白前置
需先懂 DB1（建表插数）、DB2（主键外键、规范化）。

## 🟢 部分一·最浅层（生活比喻）
点外卖时你不会对骑手说"先右转再左转"（命令式），而是说"我要一份黄焖鸡米饭送到我家"（声明式）。SQL 就是后者：你写 `SELECT 姓名 FROM 学生 WHERE 年龄>18`，意思是"给我年龄大于18的学生的姓名"，至于数据库是翻整本还是查索引，你不管。

**关系代数**是 SQL 背后的"数学说明书"：π（投影=选列）、σ（选择=过滤行）、⋈（连接=拼表）、∪（并）等。SQL 每个子句几乎都能对应一个代数操作。视图呢？就像你给一个常用查询起个名字存起来，以后当表用——比如"及格同学视图"，每次直接 `SELECT * FROM 及格视图` 就行。

## 🟡 部分二·动手层（逐字操作）
在 `demo.db` 里（先 `sqlite3 demo.db`），确保有学生/课程/选课三表（DB2 已建）。依次执行：

**DDL（定义结构）——建一张新表：**
```sql
CREATE TABLE 社团(
  社团号 TEXT PRIMARY KEY,
  社团名 TEXT,
  指导老师 TEXT
);
```

**DML（操作数据）——增删改查：**
```sql
-- Create 增
INSERT INTO 社团 VALUES('CL01','白帽安全社','泽泽');
INSERT INTO 社团 VALUES('CL02','算法社','王老师');

-- Read 查
SELECT 社团名 FROM 社团;                      -- 投影 π
SELECT * FROM 社团 WHERE 指导老师='泽泽';     -- 选择 σ

-- Update 改
UPDATE 社团 SET 指导老师='李老师' WHERE 社团号='CL02';
SELECT * FROM 社团 WHERE 社团号='CL02';       -- 确认改了

-- Delete 删
DELETE FROM 社团 WHERE 社团号='CL02';
SELECT * FROM 社团;                            -- 只剩 CL01
```

**视图（虚拟表）：**
```sql
CREATE VIEW 及格视图 AS
SELECT s.姓名, c.课名, x.成绩
FROM 选课 x
JOIN 学生 s ON x.学号=s.学号
JOIN 课程 c ON x.课号=c.课号
WHERE x.成绩 >= 60;

SELECT * FROM 及格视图;        -- 像查普通表一样用视图
```
预期：三行关联记录（成绩都≥60）。

### 🧪 实战 Lab（DB3）
用关系代数思路写 SQL：① 查出"选了数据库课且成绩>90 的学生姓名"（提示：JOIN 选课+课程+学生，σ 过滤）；② 给"社团"表加一个 `人数 INTEGER` 列（`ALTER TABLE 社团 ADD COLUMN 人数 INTEGER;`），更新人数后查询。把两条 SQL 与结果发我。

## 🔵 部分三·原理层（底层/执行顺序）
一个 `SELECT` 的真实执行顺序（DBMS 内部，不是你写的顺序）是：
1. **FROM** + **JOIN**：先把表拼起来（笛卡尔积再按 ON 过滤，或走索引连接）。
2. **WHERE**：行级过滤（σ）。
3. **GROUP BY** / **HAVING**：分组聚合。
4. **SELECT**：投影出要的列（π），并计算别名。
5. **ORDER BY / LIMIT**：最后排序截取。

理解这点很重要：你在 `WHERE` 里不能用 `SELECT` 里定义的别名（因为 SELECT 还没执行），但 `ORDER BY` 可以。这就是很多新手写 `SELECT 总分 AS t WHERE t>100` 报错的底层原因。

**视图**不存数据，它只是"保存的查询定义"。你查视图时，数据库现场把视图 SQL 和你的查询合并再执行（"视图展开"）。所以视图不加速查询（除非是"物化视图"，PostgreSQL/部分系统支持），它只是复用与权限隔离的工具。

## 🟣 部分四·深挖层（为什么、延伸）
为什么 SQL 是声明式？因为 1970 年代 Codd 想让"非程序员（如会计）"也能问数据，而命令式（写循环找数）太难。声明式把"意图"和"执行"解耦，数据库优化器才能自由选最快路径（换索引、换连接算法都不用改你的 SQL）——这思想和现代大数据（Spark SQL）、甚至 AI 查询一脉相承。

**关系代数**五大基本操作：σ选择、π投影、⋈连接、∪并、−差，加上笛卡尔积×，足以表达所有查询（关系完备）。SQL 的 `EXCEPT`（差）、`UNION`（并）直接对应。顶级工程师用代数思维做**查询重写优化**（DB9 会深讲）：比如把 correlated subquery 改写成 JOIN。

## 🔴 部分五·顶级视角
CRUD 是任何系统的血肉——前端点一下"下单"，后端本质就是一条 INSERT；刷新列表就是 SELECT。顶级工程师对 CRUD 的敏感度在于：① 每一条写操作都要考虑**事务与并发**（DB6）；② 查询要不要索引（DB8）；③ 会不会触发全表扫描拖垮库（DB9）；④ 涉及个人信息时是否脱敏（DB28）。视图在真实系统里常用于**按角色暴露不同字段**（如给客服看订单但隐藏身份证号中间几位），是"最小权限"的落地手段之一。

## 🟠 部分六·安全/合规种子
白帽要点：CRUD 里最危险的是"改"和"删"——`UPDATE`/`DELETE` 忘了 `WHERE` 会**整表清空**（经典生产事故）。铁律：**任何写操作前先 `SELECT` 看一眼会命中哪些行**，确认后再改成 UPDATE/DELETE。涉及他人数据：未授权绝不查、绝不改、绝不删；个人信息处理遵循《个人信息保护法》——查询最小化、用途限定、不留存超期。绝不写脚本去批量抓取/篡改任何非你所有的数据。

## ✅ 验收
交：① DDL+DML 四步（增删改查）执行与结果截图；② 视图 `及格视图` 创建与查询截图；③ 实战 Lab 两题 SQL 与结果。

## ⚠️ 常见坑
- `UPDATE`/`DELETE` 不带 `WHERE` = 全表操作，生产环境致命；先 SELECT 验证。
- SQLite 的 `ALTER TABLE` 功能有限（不支持改列类型/删列在老版本），改结构常需"建新表+导数据+改名"。
- 字符串比较区分大小写取决于排序规则（collation），`'A'='a'` 在默认设置下可能不等。
- 视图里不能用 `ORDER BY` 保证顺序（除非外层再 ORDER BY），这是 SQL 标准行为。

## ➡️ 下一步
DB4 · 联结 JOIN（NL/JH/SMJ/外连接）——把多张表拼起来才是关系数据库的精髓，理解 INNER/LEFT/RIGHT/FULL 和行为差异，以及数据库底层三种连接算法。

---

# DB4 · 联结 JOIN（NL/JH/SMJ + 外连接）

## 🎯 目标
学完这节，你能熟练写出 INNER / LEFT / RIGHT / FULL / CROSS JOIN；能说清"某学生没选课为什么在 INNER 里消失、在 LEFT 里还在"；并理解数据库底层三种连接算法（嵌套循环、哈希连接、排序归并连接）各自适合什么场景。

## 📋 小白前置
需先懂 DB3（SELECT/JOIN 基础、视图）。

## 🟢 部分一·最浅层（生活比喻）
JOIN 就像把两张名单按"共同信息"对齐贴在一起。INNER JOIN（内连接）是"只保留两边都有的人"——就像相亲只安排双方都点头的。LEFT JOIN（左连接）是"左边的人一个不准少，右边没有就空着"——就像班级合影，全班必须都在，没来的同学位置留白。FULL JOIN 是"两边的人都要，谁缺了谁的位置留白"。CROSS JOIN 是"把左边每个人和右边每个人强行配对"，像发扑克牌的所有组合。

## 🟡 部分二·动手层（逐字操作）
在 `demo.db`（有学生/课程/选课）。先加一个"没选课"的学生和一个"没学生选"的课程，方便看差异：
```sql
INSERT INTO 学生 VALUES('S004','小美',21);     -- 没选课
INSERT INTO 课程 VALUES('C03','操作系统',4);    -- 没人选
```
执行对比：
```sql
-- 内连接：小美和操作系统都消失
SELECT s.姓名, c.课名
FROM 学生 s
JOIN 选课 x ON s.学号=x.学号
JOIN 课程 c ON x.课号=c.课号;

-- 左连接（以学生为左）：小美出现，课名为 NULL
SELECT s.姓名, c.课名
FROM 学生 s
LEFT JOIN 选课 x ON s.学号=x.学号
LEFT JOIN 课程 c ON x.课号=c.课号;

-- 右连接（以课程为右）：操作系统出现
SELECT s.姓名, c.课名
FROM 学生 s
RIGHT JOIN 课程 c ON s.学号=(SELECT 学号 FROM 选课 WHERE 课号=c.课号);
```
> 注：SQLite 不直接支持 RIGHT/FULL JOIN 语法糖，但可用 LEFT JOIN 调换顺序等价实现；MySQL/PostgreSQL 原生支持 RIGHT/FULL。下面用通用写法演示右连接等价：
```sql
-- 等价右连接：以课程为主
SELECT s.姓名, c.课名
FROM 选课 x
RIGHT JOIN 课程 c ON x.课号=c.课号
LEFT JOIN 学生 s ON x.学号=s.学号;
```
CROSS JOIN 笛卡尔积（小心行数爆炸）：
```sql
SELECT s.姓名, c.课名 FROM 学生 s CROSS JOIN 课程 c;  -- 4学生×3课程=12行
```

### 🧪 实战 Lab（DB4）
① 查出"**没有选任何课**的学生姓名"（提示：LEFT JOIN 选课后筛 `x.学号 IS NULL`）。② 查出"**没有任何学生选**的课程名"。③ 用 `COUNT(*)` 配合 JOIN 统计每个学生选了几门课（提示：GROUP BY 学号）。把三题 SQL 与结果发我。

## 🔵 部分三·原理层（底层连接算法）
数据库并不"懂"JOIN 语义，它用三种物理算法实现：
- **Nested Loop Join（NL，嵌套循环）**：对外表每行，扫内表找匹配。外表小、内表有索引时极快（`内表索引查找 = O(1)` 每行）。最通用。
- **Hash Join（HJ，哈希连接）**：先把内表建一张哈希表（按连接键），再扫外表每行去查哈希表。适合**两表都大、无索引**的等值连接，O(M+N)。
- **Sort-Merge Join（SMJ，排序归并）**：先两表按连接键排序，再像归并两段有序数组一样指针滑动匹配。适合**已排序/可利用索引顺序**的大表，避免哈希内存压力。

优化器（DB9）会根据表大小、是否有索引、可用内存，在这三种里选成本最低的。**外连接**比内连接多一步：匹配不上的那侧要补 NULL 行（"Null-Extended"），且左/右决定了哪侧被保留。

## 🟣 部分四·深挖层（为什么、延伸）
为什么需要多种算法？因为数据分布千差万别：小表 join 大表用 NL+索引最优；两个几亿行无索引的表用 Hash 最稳；数据仓库里数据本就按日期排序，SMJ 几乎免费。**外连接转内连接**是常见优化：如果你的 WHERE 里对右表列加了 `IS NOT NULL` 条件，优化器可能把 LEFT JOIN 悄悄降成 INNER JOIN（因为外连接保留的 NULL 行会被 WHERE 过滤掉）——这是"写了 LEFT 却得到 INNER 结果"的诡异 bug 根源，顶级工程师必懂。

## 🔴 部分五·顶级视角
JOIN 是关系模型威力的核心——"用多张小表拼出复杂现实"靠的就是它。但 JOIN 也是性能杀手：N 张表 JOIN 时组合爆炸，优化器的"连接顺序"选择是 NP-hard 近似问题（DB9 讲代价模型）。顶级架构师会控制 JOIN 数量、确保连接键有索引、必要时用"宽表/反范式"（DB2）或"预计算物化视图"减少线上 JOIN。在分布式数据库里，JOIN 还要考虑"数据本地性"（DB14 分片），跨节点 JOIN 极贵。

## 🟠 部分六·安全/合规种子
白帽要点：JOIN 本身安全，但要警惕**通过 JOIN 意外拼接出敏感信息**。例如把"匿名评价表"和"用户表"按弱标识（如生日+城市）JOIN，可能重新识别出具体个人——这违反匿名化承诺与《个人信息保护法》。设计查询时遵循数据最小化：只 JOIN 必要的表、只 SELECT 必要的列、对 PII 脱敏。绝不把多源他人数据私自 JOIN 做画像。

## ✅ 验收
交：① INNER/LEFT 对比截图（能看到小美/操作系统在左连接里的 NULL）；② 实战 Lab 三题 SQL 与结果；③ 一句话说明 NL/HJ/SMJ 各自适用场景。

## ⚠️ 常见坑
- SQLite 不支持 `RIGHT JOIN`/`FULL JOIN` 关键字，要用 LEFT JOIN 调换表顺序等价实现（MySQL/PG 支持原生）。
- `LEFT JOIN ... WHERE 右表列 IS NOT NULL` 会意外变回 INNER，先想清楚要不要保留 NULL。
- 忘记 ON 条件写成 `FROM A JOIN B` 没 ON → 变成 CROSS JOIN，行数 = 两表乘积，可能瞬间卡死。
- 多表 JOIN 时列名冲突要加表别名前缀（如 `s.姓名`），否则报错。

## ➡️ 下一步
DB5 · 子查询与窗口函数——学会"查询里套查询"和强大的 `ROW_NUMBER()`/`RANK()` 分析函数，做出排名、累计、同比这类报表级计算。

---

# DB5 · 子查询与窗口函数（分析函数/排名）

## 🎯 目标
学完这节，你能写"查询里套查询"的子查询（标量子查询、IN、EXISTS）；更重要的是掌握**窗口函数** `ROW_NUMBER()`/`RANK()`/`SUM() OVER()`，做出"每个学生的排名""每门课的成绩累计""同班同比"这类报表级分析——这是 SQL 从"取数"升级到"分析"的关键一跃。

## 📋 小白前置
需先懂 DB3（SELECT/WHERE/GROUP BY）、DB4（JOIN）。

## 🟢 部分一·最浅层（生活比喻）
子查询就像"先问一个问题，拿答案去问第二个问题"：先问"数据库课是哪门"（得到 C01），再问"谁选了 C01 且成绩>90"。一层套一层。窗口函数呢？普通 GROUP BY 会把同组行"压成一行"失去明细；窗口函数像在每行旁边开一扇"小窗"，窗里能看到"这一行所在的整组"，于是既能保留每行明细，又能算出"本组排名/累计和"——好比发工资条时，每人条上既写自己工资，又写"你在部门排第几"。

## 🟡 部分二·动手层（逐字操作）
在 `demo.db`（有学生/课程/选课）。先确保选课数据丰富些（可重复插几门）：
```sql
-- 子查询：谁选了数据库且>90
SELECT 姓名 FROM 学生
WHERE 学号 IN (
  SELECT 学号 FROM 选课
  WHERE 课号='C01' AND 成绩>90
);
```
预期：小明（S001，95分）。

窗口函数（SQLite 3.25+ 支持；请先 `SELECT sqlite_version();` 确认 ≥3.25）：
```sql
-- 每个学生在自己所有课里的成绩排名（按成绩降序，1最高）
SELECT 学号, 课号, 成绩,
       ROW_NUMBER() OVER(PARTITION BY 学号 ORDER BY 成绩 DESC) AS 个人名次
FROM 选课;

-- 每门课内的排名（并列同名次用 RANK，连续名次用 DENSE_RANK）
SELECT 课号, 学号, 成绩,
       RANK() OVER(PARTITION BY 课号 ORDER BY 成绩 DESC) AS 课内排名
FROM 选课;

-- 累计和：每门课按成绩排序的累计总分
SELECT 课号, 学号, 成绩,
       SUM(成绩) OVER(PARTITION BY 课号 ORDER BY 成绩 DESC) AS 课内累计
FROM 选课;
```

### 🧪 实战 Lab（DB5）
① 用 `EXISTS` 写出"至少选了一门课的学生姓名"（对比 IN 写法）。② 用窗口函数求出"每门课成绩最高的那行（第一名）"——提示：`WHERE 课内排名=1` 要用子查询包一层，因为 WHERE 不能直接用窗口别名。③ 计算每个学生"选课门数"（窗口 `COUNT(*) OVER(PARTITION BY 学号)`）。把三题 SQL 与结果发我。

## 🔵 部分三·原理层（底层/执行）
子查询分两类：**非关联子查询**（先算一次，结果当常量用，如 IN 列表）和**关联子查询**（外层每行都重新算一次内层，如 `WHERE 成绩 > (SELECT AVG(成绩) FROM 选课 x WHERE x.学号=外层.学号)`）——关联子查询容易很慢（每行一次子查询），优化器常把它**改写成 JOIN**（DB9 会讲）。

窗口函数的执行发生在 `WHERE/GROUP BY` 之后、`ORDER BY` 之前（所以 WHERE 用不了窗口别名）。`PARTITION BY` 决定"窗的边界"（分组），`ORDER BY` 决定窗内排序，`frame`（`ROWS BETWEEN ...`）决定窗内参与计算的行范围。它不产生分组聚合的"行压缩"，所以每行都保留。

## 🟣 部分四·深挖层（为什么、延伸）
窗口函数（SQL:2003 标准引入）解决了"既要明细又要组内统计"的长期痛点。在此之前要做这种事得用自连接或 correlated subquery，又慢又难读。顶级工程师用它做：移动平均（股价）、Top-N per group（每类销量前3）、会话时长（按时间累计）。延伸：`NTILE()` 分桶、`LAG()/LEAD()` 取前/后行（算环比）、`FIRST_VALUE/LAST_VALUE`。在 Pandas（DAT4）和 Spark（DAT7）里也有等价 window 概念，思想互通。

## 🔴 部分五·顶级视角
窗口函数是"数据分析师 vs 工程师"都必会的武器。顶级工程师把它用在：实时排行榜（游戏 MOB44）、监控指标的滚动均值（Prometheus 思路）、BI 报表（DB50）。它把"集合运算"和"行级上下文"揉在一起，是声明式语言表达力的巅峰之一。理解窗口函数，也帮你读懂 OLAP 引擎（DB22）和流式 SQL（Kafka SQL）的语义基础。

## 🟠 部分六·安全/合规种子
白帽要点：子查询本身无安全问题，但要注意**查询复杂度**——一个写得烂的关联子查询或嵌套 N 层的子查询，可能让数据库 CPU 飙满（类似"资源耗尽"），若这个接口对外暴露又无限流，可能被恶意利用做拒绝服务。这属于"算法复杂度攻击"的雏形。涉及个人信息时，窗口排名别暴露可识别个体（如学号）给无关角色。绝不对外接口跑未经审查的重查询。

## ✅ 验收
交：① 子查询（IN）结果截图；② 三个窗口函数查询截图（个人名次/课内排名/累计）；③ 实战 Lab 三题 SQL 与结果。

## ⚠️ 常见坑
- SQLite 旧版本窗口函数报错——务必 `SELECT sqlite_version();` 确认 ≥3.25，老版本去官网下新的。
- 窗口别名不能在 WHERE 用（执行顺序），要包一层子查询 `SELECT * FROM (窗口查询) t WHERE 课内排名=1`。
- `RANK` 并列跳号、`DENSE_RANK` 并列不跳、`ROW_NUMBER` 永不并列——三者的区别务必分清。
- 忘记 `PARTITION BY` 会变成"全局一个窗"，排名跨组混乱。

## ➡️ 下一步
DB6 · 事务与 ACID/隔离级别——学会用 BEGIN/COMMIT/ROLLBACK 把多个操作绑成"要么全成要么全败"的整体，并亲手复现脏读、不可重复读、幻读，理解四大隔离级别。

---

# DB6 · 事务、ACID 与隔离级别（脏读/不可重复读/幻读）

## 🎯 目标
学完这节，你能用事务保证"转账"这种多步操作原子性；理解 ACID 四性质；并**亲手用两个并发会话复现**脏读、不可重复读、幻读，看清 READ UNCOMMITTED / READ COMMITTED / REPEATABLE READ / SERIALIZABLE 四级隔离各自挡住了什么。

## 📋 小白前置
需先懂 DB3（INSERT/UPDATE/DELETE）、DB4（JOIN 可暂不）。

## 🟢 部分一·最浅层（生活比喻）
事务就像"去银行转账"：从 A 扣 100、给 B 加 100，这两步必须**绑在一起**——要么都发生（A-100 且 B+100），要么都不发生（停电了也不能只扣 A 不增 B，那钱就消失了）。ACID 是四条保证：原子性（全或无）、一致性（总数不变）、隔离性（别人看不到半成品）、持久性（提交后关机也不丢）。

隔离级别像"看别人草稿的权限"：最低级你能看到别人**还没保存**的草稿（脏读）；高一点只能看已保存的（但同一条记录你前后读两次可能不一样，不可重复读）；再高你前后读一致了，但别人**新插入**的行你突然看到了（幻读）；最高级大家完全互不干扰，像排队一个个来（串行化）。

## 🟡 部分二·动手层（逐字操作，SQLite 两会话）
SQLite 支持事务。开**两个** Git Bash 窗口，都 cd 到同一目录，都执行 `sqlite3 demo.db` 进入。为便于实验，先建一张账户表：
```sql
-- 在任一会话建表并初始化
CREATE TABLE 账户(户名 TEXT PRIMARY KEY, 余额 INTEGER);
INSERT INTO 账户 VALUES('A',1000),('B',1000);
```
**实验1：原子性（单会话）**
```sql
BEGIN;
UPDATE 账户 SET 余额=余额-100 WHERE 户名='A';
UPDATE 账户 SET 余额=余额+100 WHERE 户名='B';
COMMIT;          -- 成功则 A=900,B=1100
-- 若中途想反悔：ROLLBACK; 则两人都恢复原值
```
**实验2：脏读复现（两个会话）**
- 会话1：`BEGIN; UPDATE 账户 SET 余额=0 WHERE 户名='A';`（**不提交**）
- 会话2：`PRAGMA read_uncommitted=1; SELECT 余额 FROM 账户 WHERE 户名='A';` → 在 SQLite 中默认其实读不到未提交（SQLite 用锁而非 MVCC，这里演示概念；真复现脏读建议用 MySQL，见下）。
- 会话1：`ROLLBACK;` → 会话2 刚才读到的 0 是"脏"的（根本没真发生）。

### MySQL 两会话真实复现（强烈推荐，最接近教材）
用 Docker 起 MySQL（见 DB12），或两个终端 `mysql -u root -p`。会话A：`SET SESSION TRANSACTION ISOLATION LEVEL READ UNCOMMITTED; BEGIN; UPDATE 账户 SET 余额=0 WHERE 户名='A';` 会话B：`SET SESSION TRANSACTION ISOLATION LEVEL READ UNCOMMITTED; BEGIN; SELECT 余额 FROM 账户 WHERE 户名='A';` → 直接看到 0（脏读）。会话A `ROLLBACK;` 后会话B 再查又变回。改 `READ COMMITTED` 则看不到未提交，脏读消失但**不可重复读**仍在（同一事务内两次 SELECT 同一条记录值变了）。`REPEATABLE READ`（MySQL 默认）挡住不可重复读，但**幻读**可能出现（另一个事务 INSERT 新行并提交，你再查多了行）。`SERIALIZABLE` 全挡住但性能最低。

### 🧪 实战 Lab（DB6）
① 用 SQLite 两会话演示：会话1 BEGIN 改 A 余额但不提交，会话2 在默认隔离下 SELECT，记录你看到的是改前还是改后的值，说明为什么（SQLite 锁机制）。② 写下四级隔离级别各自能防止哪三种读异常的表格（脏读/不可重复读/幻读 × 四级）。③ 写一个"转账"事务 SQL（A 给 B 转 200），含余额不足时 ROLLBACK 的写法（`SELECT 余额...;`（应用层判断）或 SQLite 的 `CHECK`）。把过程和结论发我。

## 🔵 部分三·原理层（底层/实现机制）
- **原子性（Atomicity）**：靠 **undo log（回滚日志）**。每改一行前先记"原值"，ROLLBACK 时按日志反向恢复。COMMIT 后日志可清理。
- **持久性（Durability）**：靠 **redo log（重做日志）**。改动先写日志（顺序写，快），再异步刷数据页。崩溃后靠 redo 重放恢复已提交数据。
- **隔离性（Isolation）**：靠 **锁 + MVCC（多版本并发控制）**。读不加锁、写加锁，并通过"快照"让读看到一致版本（DB7 详讲）。
- **一致性（Consistency）**：是目标——由前三者 + 应用层约束（CHECK/外键/业务规则）共同保证；DBMS 只保证"不破坏已声明的约束"。

隔离级别本质是"隔离性与性能的权衡旋钮"：越严格越安全但并发越低。

## 🟣 部分四·深挖层（为什么、历史、延伸）
ANSI SQL-92 定义了四级隔离，但理论上还有"写偏斜（write skew）""丢失更新"等异常，SQL 标准并未全部覆盖（后来由 Jim Gray 等人用"现象"重新严格定义）。**串行化不一定等于真串行**——现代数据库用 MVCC + 快照 + 冲突检测，在"看起来像串行"的前提下保持高并发（可串行化快照隔离 SSI，PostgreSQL 可开启）。顶级工程师追问：金融核心为什么sometimes要 SERIALIZABLE 甚至用 `SELECT ... FOR UPDATE` 显式加锁防丢失更新？因为钱不能算错。

## 🔴 部分五·顶级视角
事务是"正确性"的基石。顶级工程师 designing 分布式系统时，单库事务不够，要 2PC/TCC/Raft（DB24）；但能用单库事务解决的，绝不引入分布式复杂度（"先把本地事务写对"）。他们极度警惕**长事务**（占锁、拖 MVCC 版本链、撑大 undo）和**死锁**（DB7）。理解 ACID 也帮你看懂消息队列的"事务消息"、Redis 的"弱事务"（DB16）、NewSQL 的 HTAP 混合负载。

## 🟠 部分六·安全/合规种子
白帽要点：事务本身是中性的"正确性工具"，但有两个安全角度：① **并发写竞争**（如两个请求同时给同一账户加钱，若不用事务/锁会"丢失更新"导致资金差错）——这既是 Bug 也是可被利用的逻辑漏洞（类似方向7 SEC18 逻辑漏洞）。② 数据合规：涉及个人金融/健康数据的事务，提交即"已处理"，要有审计日志（《个人信息保护法》要求处理活动可记录）。绝不写任何针对非授权账户的转账/篡改逻辑。

## ✅ 验收
交：① 单会话转账 BEGIN/COMMIT/ROLLBACK 演示截图；② 两会话实验记录（你看到改前/改后，及原因）；③ 四级隔离 × 三种异常的防住表格；④ 实战 Lab 转账 SQL。

## ⚠️ 常见坑
- SQLite 是**文件级锁**，默认几乎串行，脏读现象与 MySQL 不同，学隔离级别建议用 MySQL/Pg 真复现。
- 忘了 COMMIT → 改动在别的会话看不到，还占着锁；退出会话会自动 ROLLBACK 未提交的。
- `BEGIN` 后出错不 ROLLBACK 就再 BEGIN 会报"已有事务"；用 `COMMIT` 或 `ROLLBACK` 收尾。
- 长事务会拖慢整个库（尤其 MySQL InnoDB 的 MVCC 版本堆积）。
- 手疼复制事务语句，别硬敲。

## ➡️ 下一步
DB7 · 锁机制、死锁与 MVCC——搞懂行锁/表锁、共享锁/排他锁、死锁怎么产生又怎么破，以及 MVCC 如何用"多版本快照"让读写互不阻塞。

---

# DB7 · 锁机制、死锁与 MVCC（多版本/快照）

## 🎯 目标
学完这节，你能区分行锁/表锁、共享锁(S)/排他锁(X)；理解死锁四大条件与数据库如何自动检测回滚；并真正搞懂 **MVCC（多版本并发控制）**——为什么"读不阻塞写、写不阻塞读"，以及"快照读"和"当前读"的区别。

## 📋 小白前置
需先懂 DB6（事务、隔离级别、脏读概念）。

## 🟢 部分一·最浅层（生活比喻）
锁就像公用洗手间的"占用牌"。**共享锁**是"多人同时能看这页书"（大家都能读同一行），**排他锁**是"我要改这页，谁都别动"（只有我能写）。死锁像两个人各占一间厕所、都等对方先出来——谁都出不来，只能系统强行"请一个人出去"（回滚他的事务）。

MVCC 更像"草稿纸复印"：你改数据时，数据库不原地改，而是**复制一份新版本**给你改，别人读老版本照样读，互不干扰；等你提交，大家才看到新版本。这样"读的人不挡写的人，写的人不挡读的人"，效率起飞。

## 🟡 部分二·动手层（MySQL InnoDB 两会话，最直观）
用 Docker 起的 MySQL（命令见 DB12）。两个终端分别连入，建表：
```sql
CREATE TABLE t(id INT PRIMARY KEY, v INT);
INSERT INTO t VALUES(1,10),(2,20);
```
**行锁实验（两会话改不同行互不阻塞）：**
- 会话A：`BEGIN; UPDATE t SET v=v+1 WHERE id=1;`
- 会话B：`BEGIN; UPDATE t SET v=v+1 WHERE id=2;` → 立刻成功（不同行，行锁不冲突）
- 会话B：`UPDATE t SET v=v+1 WHERE id=1;` → **阻塞**（等 A 的锁）。A 提交 `COMMIT;` 后 B 才继续。

**死锁复现：**
- 会话A：`BEGIN; UPDATE t SET v=1 WHERE id=1;`
- 会话B：`BEGIN; UPDATE t SET v=2 WHERE id=2;`
- 会话A：`UPDATE t SET v=1 WHERE id=2;` → 阻塞等 B
- 会话B：`UPDATE t SET v=2 WHERE id=1;` → 死锁！MySQL 立刻检测并回滚其中一个事务，报错 `Deadlock found`。

**MVCC 快照读演示（REPEATABLE READ）：**
- 会话A：`BEGIN; SELECT * FROM t WHERE id=1;` → 看到 v=10
- 会话B：`UPDATE t SET v=99 WHERE id=1; COMMIT;`（改并提交）
- 会话A：`SELECT * FROM t WHERE id=1;` → **仍看到 10**（快照读，MVCC 老版本）。只有 A 也 COMMIT 后再查才看到 99。

### 🧪 实战 Lab（DB7）
① 用上面步骤复现一次死锁，把 MySQL 报的死锁错误信息截图。② 在 REPEATABLE READ 下，解释为什么会话A两次读同一行结果一样（即使别人已改）。③ 用 `SELECT ... FOR UPDATE`（当前读）替代普通 SELECT，再观察：会话A `BEGIN; SELECT * FROM t WHERE id=1 FOR UPDATE;` 后，会话B 改这行会不会被阻塞？把实验结论写清楚发我。

## 🔵 部分三·原理层（底层）
- **锁粒度**：表锁（锁整张表，MyISAM 常用，粒度粗并发低）、行锁（InnoDB 默认，只锁命中行，并发高）、间隙锁/Next-Key Lock（防幻读，锁住"范围+间隙"）。
- **S/X 锁兼容矩阵**：S 与 S 兼容，S 与 X 冲突，X 与 X 冲突。写操作加 X，普通读在 MVCC 下不加锁（快照读）。
- **死锁**：四个必要条件（互斥、持有并等待、不可剥夺、循环等待）。数据库用**等待图（wait-for graph）**检测环，发现死锁立即回滚代价较小的事务并抛错，应用层应捕获后重试。
- **MVCC 实现**：每行藏两个隐藏列——创建版本号、删除版本号（或等效的 undo 版本链）。`SELECT` 只返回"版本号 ≤ 当前事务快照且未被删除"的行。写操作在 undo 里留旧版本，供老快照读。这就是"非锁定读"。

## 🟣 部分四·深挖层（为什么、延伸）
为什么要有 MVCC？因为传统"读写都加锁"并发极差（读也阻塞写）。MVCC 用"空间换时间"（多版本）实现读写分离并发。代价是：旧版本不能无限堆积，需要 **purge（清理）** 线程回收没人用的旧版本——长事务会拖慢 purge，版本链变长，查询变慢（这是"别开长事务"的底层原因）。

顶级工程师追问：MVCC 的"快照"是啥时候拍的？READ COMMITTED 每次读都拍新快照（所以同事务两次读可能不同=不可重复读）；REPEATABLE READ 在事务开始时拍一次快照（所以可重复读）。`SELECT ... FOR UPDATE` / `UPDATE` / `DELETE` 是"当前读"——直接读最新已提交版本并加锁，不受快照限制（所以能防幻读/防丢失更新）。

## 🔴 部分五·顶级视角
锁与 MVCC 是数据库性能与正确性的核心战场。顶级工程师 tuning 时，会盯着：锁等待时长、死锁频率、长事务、间隙锁导致的"莫名阻塞"。他们也把 MVCC 思想迁移到：Git 的版本快照、Docker 镜像层、React 的不可变状态（PAR3）——"多版本 + 快照"是计算机科学里反复出现的优雅范式。在分布式里，MVCC 演变成"多版本共识/线性一致性"（DB25）。

## 🟠 部分六·安全/合规种子
白帽要点：① 死锁/锁等待本质上是"可用性"问题，恶意用户可能故意构造热点行高频更新制造锁竞争，拖垮服务（属于资源耗尽型攻击的雏形，方向7 会系统讲，但只分析不实践）。② 锁的正确使用（如 `SELECT ... FOR UPDATE` 防并发篡改）是**防逻辑漏洞**的手段（如防止"同一优惠券被并发核销多次"）。③ 任何涉及他人数据的加锁/更新，必须在你**授权拥有**的系统内，绝不针对非授权目标。

## ✅ 验收
交：① 行锁"改不同行不阻塞、改同行阻塞"截图；② 死锁复现报错截图；③ MVCC 快照读"两次读一致"现象记录；④ 实战 Lab 当前读 FOR UPDATE 的阻塞结论。

## ⚠️ 常见坑
- SQLite 是库级锁，体会不到行锁——学锁一定要用 MySQL/PG。
- 死锁报错后事务已被回滚，要**重新 BEGIN** 再试，别以为还能继续。
- 长事务是万恶之源：占锁、堆版本、拖 purge，生产环境宁可拆小事务。
- `FOR UPDATE` 忘了提交会一直占着行锁，别人全卡。
- 手疼复制 SQL，别逐字敲。

## ➡️ 下一步
DB8 · 索引原理（B+树/哈希/最左前缀/覆盖索引）——搞懂为什么加个索引查询从"翻全书"变"查目录"，B+树为什么是数据库的默认选择，以及怎么建索引才不白建。

---

# DB8 · 索引原理（B+树/哈希/最左前缀/覆盖索引）

## 🎯 目标
学完这节，你能说清"为什么没索引要全表扫描、有索引秒回"；理解 **B+树** 结构为什么适合磁盘；写出"最左前缀"命中/失效的 SQL；理解"覆盖索引"如何避免回表；并知道哈希索引的适用边界。

## 📋 小白前置
需先懂 DB3（SELECT/WHERE）、DB4（JOIN）、DB7（行锁概念可暂不管）。

## 🟢 部分一·最浅层（生活比喻）
没索引的查询就像"在一本没有目录的电话簿里找'张三'"——只能从第一页翻到最后一页（全表扫描）。加了索引，等于书有了**目录/书签**：直接翻到 Z 开头那几页，秒找。B+树就是这种"多层目录"：最上面是总目录，指向下一层分目录，直到最底层才是真正的数据页，而且最底层（叶子）之间还用链表串起来，方便"找 18~25 岁的所有人"这种范围查询。

最左前缀像"按姓+名排序的目录"：你查"姓张的"能用书签，但查"名伟的"不行——因为目录是先按姓排的，跳过了姓直接找名就乱套了。

## 🟡 部分二·动手层（SQLite/MySQL 建索引实测）
SQLite 演示（在 `demo.db`，有选课表）：
```sql
-- 看没索引时怎么找
EXPLAIN QUERY PLAN
SELECT * FROM 选课 WHERE 学号='S001';
-- 预期：SCAN 选课（全表扫）

CREATE INDEX idx_学号 ON 选课(学号);
EXPLAIN QUERY PLAN
SELECT * FROM 选课 WHERE 学号='S001';
-- 预期：SEARCH 选课 USING INDEX idx_学号（命中索引）

-- 复合索引与最左前缀
CREATE INDEX idx_学课 ON 选课(学号, 课号);
EXPLAIN QUERY PLAN SELECT * FROM 选课 WHERE 学号='S001' AND 课号='C01';  -- 命中
EXPLAIN QUERY PLAN SELECT * FROM 选课 WHERE 课号='C01';                  -- 失效（没最左列）
```
MySQL 演示（InnoDB，`SHOW INDEX FROM 选课;` 看索引；`EXPLAIN` 看计划，DB9 详讲）。

### 🧪 实战 Lab（DB8）
① 在 `账户` 表（DB6 建的）的 `户名` 上建唯一索引，验证查某户名走索引。② 故意写一个"对索引列做函数运算"的查询，如 `WHERE SUBSTR(学号,1,1)='S'`，用 EXPLAIN 看是否失效（函数让索引失效）。③ 覆盖索引：建一个 `(学号,成绩)` 索引后，查询 `SELECT 学号,成绩 FROM 选课 WHERE 学号='S001'`，用 EXPLAIN 看是否"不回表"（SQLite 显示 `USING COVERING INDEX`，MySQL 显示 `Using index`）。把三条实验的 EXPLAIN 结果发我。

## 🔵 部分三·原理层（底层/B+树）
**B+树**是一种"多路平衡搜索树"：每个节点存多个键（如几百个），所以树很"矮"（3 层就能存千万级数据）。查找、插入、删除都是 O(log n)，且**所有数据都放在叶子节点**，叶子间用双向链表相连。为什么适合数据库？因为磁盘按"页（4KB/16KB）"读写，一次 I/O 读一个节点（一页）拿到几百个键的指针，三层 I/O 就能定位，远少于二叉树（会很高、I/O 多）。哈希索引则是对索引列算 hash，O(1) 等值查找极快，但**不支持范围查询、不支持排序**（hash 无序），所以只适合"精确等于"场景（如 Redis、Memory 引擎）。

**回表**：二级索引的叶子存的是"主键值"，拿到主键后还要去主键索引（聚簇索引）再查一次真正数据行——这叫回表。若索引本身就含了查询要的所有列（覆盖索引），就免回表，更快。

## 🟣 部分四·深挖层（为什么、延伸）
为什么不用二叉树/红黑树？红黑树一个节点只存 1~2 个键，树高太大，磁盘 I/O 次数爆炸。B+树 vs B树：B树数据散在各层，B+树数据全在叶子且链表连，范围扫描只需顺链表走，极其高效——所以数据库几乎都用 B+树（MySQL InnoDB、PG、SQLite 皆然）。**聚簇索引**（InnoDB）把数据行直接按主键顺序存在叶子，所以"主键查询最快、主键宜短而递增"（UUID 做主键会导致页分裂混乱，性能差）。顶级工程师还要懂**索引选择性**（区分度低的列如"性别"建索引几乎无用）、**索引下推（ICP）**、**索引碎片**与重建。

## 🔴 部分五·顶级视角
索引是"用空间换时间、用写入成本换读取速度"的典范。顶级工程师把索引当**系统架构的一部分**来设计：不是"哪里慢加哪里"，而是根据查询模式（哪些 WHERE、JOIN、ORDER BY、GROUP BY 列）通盘规划**复合索引的顺序**（最左前缀 = 等值列在前、范围列在后）。他们也清楚索引的代价：占空间、拖慢 INSERT/UPDATE/DELETE、太多索引让优化器选择变难。在分布式/分库分表（DB14）、ES 倒排索引（DB21）里，索引思想以不同形态延续。

## 🟠 部分六·安全/合规种子
白帽要点：索引本身无安全威胁，但两点相关：① 创建索引、尤其在生产大表上 `CREATE INDEX` 会**锁表/占资源**，若被滥用或误操作可能影响可用性——这是"操作权限"问题，遵循最小权限、在授权环境操作。② 索引无法保护数据内容；敏感列（PII）即使有索引也明文存储，合规要求对 PII **加密或脱敏**（DB28），而非仅靠索引。绝不给非你所有的库建索引或跑 DDL。

## ✅ 验收
交：① 建索引前后 EXPLAIN 对比截图（SCAN vs SEARCH）；② 最左前缀命中/失效两条 EXPLAIN；③ 实战 Lab 三题（唯一索引、函数失效、覆盖索引）的 EXPLAIN 结果。

## ⚠️ 常见坑
- 复合索引顺序就是"最左前缀"：把**等值查询列放前面、范围/排序列放后面**，否则后半段失效。
- 对索引列用函数/`!=`/`OR`/隐式类型转换（如字符串列传数字）常导致索引失效，全表扫描。
- 索引不是越多越好：拖慢写、占空间、干扰优化器。
- `LIKE '%xx'` 前导通配符用不到 B+树索引（`LIKE 'xx%'` 前缀能用到）。
- 手疼复制建索引语句，别硬敲。

## ➡️ 下一步
DB9 · 查询优化与执行计划（代价模型/索引选择/慢查询）——学会用 EXPLAIN 读懂数据库"打算怎么查"，定位慢 SQL，掌握最左前缀、覆盖索引、避免 SELECT * 等优化铁律，并认识"慢查询日志"。

---

# DB9 · 查询优化与执行计划（EXPLAIN/代价模型/慢查询）

## 🎯 目标
学完这节，你能用 `EXPLAIN`（MySQL）/ `EXPLAIN QUERY PLAN`（SQLite）/ `EXPLAIN ANALYZE`（PG）读懂数据库"打算怎么查、实际怎么查"；能定位慢 SQL 的根因（全表扫、错误连接顺序、回表多）；掌握优化铁律；并学会开启"慢查询日志"抓线上慢句。

## 📋 小白前置
需先懂 DB8（索引、最左前缀、覆盖索引）、DB4（JOIN 算法）、DB6（事务可暂不管）。

## 🟢 部分一·最浅层（生活比喻）
执行计划就像"快递员送货前的路线规划"：他算一下"先去哪个仓、走哪条路最快、要跑几趟"，把方案摆给你看。EXPLAIN 就是让你偷看这个方案。慢查询日志则是"装了个行车记录仪"，自动记下所有超时送货的行程，你回头复盘哪趟最慢、为什么。

## 🟡 部分二·动手层（MySQL EXPLAIN 实战，最标准）
用 Docker MySQL（DB12 起法）。连入后：
```sql
-- 建测试表并灌数据
CREATE TABLE orders(
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_id INT,
  amount DECIMAL(10,2),
  created_at DATETIME,
  KEY idx_user(user_id),
  KEY idx_created(created_at)
);
INSERT INTO orders(user_id,amount,created_at)
SELECT FLOOR(RAND()*1000), ROUND(RAND()*1000,2),
       DATE_ADD('2024-01-01', INTERVAL FLOOR(RAND()*365) DAY)
FROM information_schema.tables a, information_schema.tables b
LIMIT 5000;

-- 看执行计划
EXPLAIN
SELECT * FROM orders WHERE user_id=42 ORDER BY created_at DESC LIMIT 10;
```
观察输出列：`type`（访问类型，ALL=全表扫最差，ref/range/index/const 越好）、`key`（实际用了哪个索引）、`rows`（估计扫描行数）、`Extra`（`Using filesort`/`Using temporary` 是危险信号）。

**慢查询日志开启：**
```sql
SET GLOBAL slow_query_log = 'ON';
SET GLOBAL long_query_time = 1;     -- 超过1秒记日志
SHOW VARIABLES LIKE 'slow_query_log%';
```
跑一条故意慢的：`SELECT SQL_NO_CACHE * FROM orders ORDER BY RAND() LIMIT 1;`（全表+排序），然后去日志文件查。

### 🧪 实战 Lab（DB9）
① 对比两条 SQL 的 EXPLAIN：`SELECT * FROM orders WHERE user_id=42`（走 idx_user） vs `SELECT * FROM orders WHERE amount>500`（无索引→ALL 全表扫），截图 `type`/`key`/`rows` 差异。② 优化：给 `amount` 建索引后重跑，看是否变 range。③ 写出"避免 SELECT *"的理由并用覆盖索引改写上面的查询（`SELECT id,user_id,created_at ...` 建联合索引 `(user_id,created_at,id)` 让它覆盖）。把 EXPLAIN 对比与结论发我。

## 🔵 部分三·原理层（底层/代价模型）
数据库有个**优化器（Optimizer）**，它面对一条 SQL 会有几十甚至上百种"执行路径"（先扫哪张表、用哪个索引、JOIN 顺序、是否排序），然后用**代价模型（Cost Model）**给每条路打分——主要成本 = 磁盘 I/O 次数 + CPU。它选"估计代价最低"的那条。代价来自**统计信息**（每张表多少行、每列多少不同值、数据分布直方图），所以**统计信息过期**会让优化器选错索引（要定期 `ANALYZE TABLE`）。

`EXPLAIN` 给你的是"计划"，`EXPLAIN ANALYZE`（PG/MySQL 8.0+）还会**真正执行**并返回"实际耗时/实际行数"，对比"估计 vs 实际"是定位错的利器。`Using filesort` 表示没法用索引排，要在内存/磁盘做额外排序；`Using temporary` 表示用了临时表（GROUP BY/UNION 常见），都要警惕。

## 🟣 部分四·深挖层（为什么、延伸）
优化器不是万能的：它基于统计"猜"，遇到相关性强的列（如"城市"和"区"）会误判；它也不会跨"参数化查询的多次执行"学习（除非开启自适应计划）。顶级工程师的手段：① 写**能命中索引**的 SQL（避免隐式转换、函数包裹、前导模糊）；② 用 **covering index** 消除回表；③ 控制 **JOIN 表数量与顺序**；④ 必要时用 **optimizer hints**（`FORCE INDEX`、`STRAIGHT_JOIN`）"指挥"优化器（但慎用，可能随数据变化变劣）；⑤ 用**慢查询日志 + APM** 持续监控。

## 🔴 部分五·顶级视角
查询优化是"工程直觉 + 数据验证"的结合。顶级工程师知道：**80% 的性能问题来自 20% 的慢 SQL**，且多数慢 SQL 靠"加对索引 + 改写"就能解决，远比加机器便宜。他们也理解优化是**有界的**——单表再优化也有天花板，到顶了就要上缓存（DB16/Redis）、读写分离（DB13）、分库分表（DB14）、列式存储（DB22）。EXPLAIN 阅读能力，是从"能写 SQL"到"能扛流量"的分水岭。

## 🟠 部分六·安全/合规种子
白帽要点：① 慢查询/全表扫描若发生在**对外接口**且无缓存/限流，可能被恶意用户用复杂查询拖垮（资源耗尽型 DoS 雏形，方向7 讲，只分析不实践）。防御：参数化查询 + 查询超时（`max_execution_time`）+ 限流。② 排查慢 SQL 时可能 `SELECT` 到大量含 PII 的数据——遵循数据最小化与《个人信息保护法》，不在日志里明文落库敏感字段（DB28 讲脱敏）。绝不针对非授权库做优化/探测。

## ✅ 验收
交：① 两条对比 SQL（有/无索引）的 EXPLAIN 截图与 `type`/`rows` 差异说明；② 给 amount 建索引后 EXPLAIN 变化；③ 覆盖索引改写与 EXPLAIN 验证；④ 慢查询日志开启截图。

## ⚠️ 常见坑
- `EXPLAIN` 不真正执行（MySQL），`rows` 是"估计值"可能严重偏差——以 `EXPLAIN ANALYZE` 或实际耗时为准。
- 统计信息过期 → 优化器选错索引：`ANALYZE TABLE orders;` 刷新。
- `ORDER BY` 列不在索引里会 `Using filesort`；`GROUP BY` 同理。
- `SELECT *` 几乎不可能走覆盖索引，且多传无用列浪费 IO/带宽。
- 手疼复制 SQL，别硬敲。

## ➡️ 下一步
DB10 · SQLite 实战（文件即库/WAL）——深入这个"嵌进 App 的数据库"，搞懂单文件库、WAL 写前日志、Python sqlite3 模块实战，做出一个本地记账小工具。

---

# DB10 · SQLite 实战（文件即库 / WAL / Python 驱动）

## 🎯 目标
学完这节，你能独立用 **Python 的 sqlite3 模块**写出"建库、建表、增删改查、事务"的完整程序；理解 SQLite"整个库就是一个文件"的特性与适用场景；搞懂 **WAL（Write-Ahead Logging）** 模式；并做一个本地记账小工具作为实战。

## 📋 小白前置
需先懂 DB3（CRUD）、DB6（事务）、方向1a PY 基础（PY1–PY10 足矣）。

## 🟢 部分一·最浅层（生活比喻）
大多数数据库像"一个需要专门启动的大仓库（MySQL 服务器）"，你要把货（连接）送进去。SQLite 不一样——它把整个仓库**压缩成单个文件**（如 `demo.db`），谁想看直接打开文件就行，不需要服务器。适合"App 自己带个小账本"：手机里的微信、浏览器、你的记账软件，背后都是 SQLite 文件。WAL 就像"先写草稿本再誊正账本"——所有改动先顺序写进 WAL 文件，查的人读原账本、写的人改草稿，互不耽误，崩溃了用草稿恢复。

## 🟡 部分二·动手层（Python sqlite3 实战）
新建 `sqlite_demo.py`（VS Code），复制：
```python
import sqlite3

# 连接（文件不存在则创建）
conn = sqlite3.connect("记账.db")
conn.execute("PRAGMA journal_mode=WAL;")   # 开启 WAL
cur = conn.cursor()

# DDL
cur.execute("""
CREATE TABLE IF NOT EXISTS 账单(
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    项目 TEXT NOT NULL,
    金额 REAL NOT NULL,
    方向 TEXT CHECK(方向 IN ('收入','支出')),
    时间 TEXT DEFAULT (datetime('now','localtime'))
)
""")

# 插入（参数化，防止注入！）
cur.execute("INSERT INTO 账单(项目,金额,方向) VALUES(?,?,?)", ("工资", 8000.0, "收入"))
cur.execute("INSERT INTO 账单(项目,金额,方向) VALUES(?,?,?)", ("午餐", 35.5, "支出"))
conn.commit()   # 事务提交

# 查询
cur.execute("SELECT 项目,金额,方向,时间 FROM 账单 ORDER BY 时间 DESC")
for row in cur.fetchall():
    print(row)

conn.close()
```
运行：`python sqlite_demo.py`。预期打印两行账单。注意 `?` 占位符 + 元组——这就是**参数化查询**，永远用它对外部输入，杜绝 SQL 注入（DB28）。

### 🧪 实战 Lab（DB10）
扩展记账工具：① 加一个函数 `统计(方向)`，用 `SELECT SUM(金额) FROM 账单 WHERE 方向=?` 算出总收入/总支出与结余。② 加"删除某条记录"功能（`DELETE ... WHERE id=?`）。③ 用 `with sqlite3.connect(...) as conn:` 上下文管理器体会自动提交/回滚（RAII 思想，PY15）。把程序与运行结果发我。

## 🔵 部分三·原理层（底层）
SQLite 是**进程内（in-process）**库，没有独立服务器进程；连接就是打开一个文件。`PRAGMA journal_mode=WAL` 后，写操作先追加到 `-wal` 文件，读操作读主库+WAL 合并视图；当 WAL 达到一定大小（或 checkpoint）才写回主库。相比旧有的 rollback journal（先备份原页再改），WAL 让**读写并发**（读者不阻塞写者）且顺序写更快。SQLite 用**文件锁**（Unix 的 fcntl / Windows 的文件锁）做并发控制——所以它是"库级串行"，不适合高并发多写场景（这正是 MySQL/PG 用行级锁+MVCC 的原因，DB7）。

## 🟣 部分四·深挖层（为什么、延伸）
SQLite 由 D. Richard Hipp 于 2000 年发布，设计哲学是"可靠、零配置、单文件、公共领域（无版权）"——如今是**部署量最大的数据库引擎**（数十亿设备）。它通过了 SQLite 自己的"100% 分支覆盖 + 数百万测试用例"质量要求，被广泛用于飞机航电、医疗设备（合规可靠）。顶级工程师用它做：本地缓存、嵌入式、测试替身（单元测试里用内存版 `:memory:` 代替真实库）、桌面/移动 App 存储。延伸：`VACUUM`（整理碎片）、`ATTACH DATABASE`（多文件联合查询）、FTS5 全文搜索。

## 🔴 部分五·顶级视角
SQLite 教会一个顶级思维：**选数据库先看场景，而非追新**。高并发多写→MySQL/PG；海量键值→Redis；文档→Mongo；但"单进程、可靠、零运维"→SQLite 往往是最好答案（连 Notion、Discord、1Password 都在服务端大量用 SQLite 做局部存储）。它也让你直观理解"数据库文件 = 字节 + 格式"，逆向/取证里解析 SQLite（如浏览器 history、App 数据库）是基本功（只对你授权样本）。

## 🟠 部分六·安全/合规种子
白帽要点：① SQLite 文件若被他人拿到，本地无账号体系，**直接可读**——所以存 PII 的 SQLite 文件要加密（SQLCipher）或放受保护目录，符合《个人信息保护法》的"存储安全"。② 仍要坚持**参数化查询**：即便本地库，把用户输入拼进 SQL 字符串（哪怕自己的小工具）是坏习惯，养成就废。绝不拿 SQLite 解析/入侵任何非你所有的 App 数据。

## ✅ 验收
交：① `sqlite_demo.py` 运行截图（两行账单）；② 实战 Lab 的"统计结余"与"删除"功能代码与结果；③ 一句话说明 WAL 为什么比 rollback journal 好（读写并发）。

## ⚠️ 常见坑
- 忘了 `conn.commit()` → 改动只在内存，关连接就没了。
- 用字符串拼接 SQL（`f"INSERT ... {user_input}"`）是注入温床，永远用 `?` 占位。
- WAL 模式会生成 `-wal`/`-shm` 文件，删主库时一起删，否则可能打不开。
- SQLite 不适合"很多客户端同时写"，那是 MySQL 的活。
- 手疼复制，别硬敲。

## ➡️ 下一步
DB11 · PostgreSQL 深入（进程模型/MVCC/扩展）——认识"开源关系库之王"，理解它多进程架构、干净的 MVCC、丰富扩展（JSONB/PostGIS/全文），以及它和 MySQL 的取舍。

---

# DB11 · PostgreSQL 深入（进程模型 / MVCC / 扩展）

## 🎯 目标
学完这节，你能用 Docker 起一个 PostgreSQL、用 `psql` 操作；理解它"一连接一进程"的模型与 InnoDB 不同的 MVCC（靠事务 ID + 元组多版本）；体验 JSONB、PostGIS 等扩展；并说清 PG 与 MySQL 的取舍。

## 📋 小白前置
需先懂 DB3–DB9（SQL 通用）、DB7（MVCC 概念）、Docker 基础（方向10 OPS19 可后补，这里给命令）。

## 🟢 部分一·最浅层（生活比喻）
MySQL 像"精干的快餐店"（默认简单快），PostgreSQL 像"设备齐全的大厨房"（啥都能做，扩展多）。PG 每来一个客人（连接）就派一个专职厨师（进程），彼此隔离稳；MySQL 的 InnoDB 更像"一组协作工位"（多线程）。PG 还允许你往厨房加新设备（扩展）：地理空间（PostGIS）、JSON 文档、甚至自己写函数语言——所以它被称为"最像现代应用数据库的"关系库。

## 🟡 部分二·动手层（Docker 起 PG + psql）
Git Bash 执行（确保 Docker 已装并启动）：
```bash
docker run --name pg -e POSTGRES_PASSWORD=zeze123 -p 5432:5432 -d postgres:16
docker ps | grep pg
# 进入 psql
docker exec -it pg psql -U postgres
```
在 `postgres=#` 提示符下：
```sql
CREATE DATABASE school;
\c school
CREATE TABLE 学生(学号 SERIAL PRIMARY KEY, 姓名 TEXT, 资料 JSONB);
INSERT INTO 学生 VALUES(DEFAULT,'小明','{"爱好":["安全","算法"],"城市":"北京"}');
-- JSONB 查询：找爱好含"安全"的学生
SELECT 姓名 FROM 学生 WHERE 资料->'爱好' ? '安全';
-- 扩展：开启 PostGIS（若镜像含 postgis）
-- CREATE EXTENSION IF NOT EXISTS postgis;
```
退出 `\q`。

### 🧪 实战 Lab（DB11）
① 用 Docker 起 PG 并建 `school` 库、插入含 JSONB 的两名学生。② 写一条查询：找出"城市=北京"的学生（`资料->>'城市' = '北京'`）。③ 体验 MVCC：开两个 `psql` 会话，会话A `BEGIN; UPDATE 学生 SET 姓名='小红' WHERE 学号=1;` 不提交；会话B 查询该学生，看是否仍看到旧名（PG 默认 READ COMMITTED 快照）。把过程和结果发我。

## 🔵 部分三·原理层（底层）
PG 是**多进程**模型：一个 postmaster 主进程 + 每连接一个 backend 进程（由 `max_connections` 限）。优点：一个后端崩了不影响其他（进程隔离）；缺点：连接多时内存/上下文切换开销大（所以生产常配 PgBouncer 连接池）。PG 的 MVCC 用**事务 ID（xmin/xmax）**标记每行版本：INSERT 写 xmin=当前事务，DELETE 写 xmax，UPDATE 实为"标记旧 xmax + 插入新版本"。旧版本靠 **VACUUM** 回收（和 MySQL purge 同理，但 PG 需手动/autovacuum 调优）。

PG 的 **WAL（Write-Ahead Log）** 先记日志再改页，用于崩溃恢复与**流复制**（DB13）。它的类型系统极丰富：ARRAY、JSONB、几何、UUID、自定义域（DOMAIN）、枚举，还能用 `CREATE EXTENSION` 装第三方扩展（PostGIS 把 PG 变空间数据库，TimescaleDB 变时序库）。

## 🟣 部分四·深挖层（为什么、延伸）
PG 起源于 1986 年 Berkeley 的 POSTGRES 项目（Stonebraker，图灵奖），设计上"严格遵循 SQL 标准、可扩展性强"。相比 MySQL 早期"为了快牺牲一些标准"，PG 以"正确性与可扩展"著称。顶级工程师选 PG 当默认 OLTP 的原因：标准兼容好、类型强、扩展生态、对复杂查询/分析友好、开源协议极宽松（可改可商用）。MySQL 则在"简单高并发 Web 读写"场景历史积累深、运维资料多。两者都在进化，界限在模糊（MySQL 也支持 JSON、窗口函数）。

## 🔴 部分五·顶级视角
PG 展示了"数据库作为平台"的顶级范式：通过扩展把单一关系库变成"图/空间/时序/文档"多面手，而不必引入一堆异构系统。顶级架构师会评估：能否用 PG + 扩展满足 80% 需求，避免过早微服务化/多数据库运维灾难。PG 的源码（C 写）也是学习"数据库内核如何实现 MVCC/锁/WAL"的宝库（方向3/方向7 进阶可读）。

## 🟠 部分六·安全/合规种子
白帽要点：① 刚起的 PG 默认监听、密码要强（`POSTGRES_PASSWORD` 别用弱口令），**绝不**把 5432 端口暴露公网且不设防——这是真实世界被勒索的常见入口（数据库安全 DB28）。② 用 `CREATE ROLE app LOGIN PASSWORD '...'` + `GRANT` 最小权限，别用 superuser 连应用。③ 处理 PII 用 `pgcrypto` 扩展加密列。④ 只连你授权拥有的实例。绝不对非授权 PG 实例做连接/探测。

## ✅ 验收
交：① Docker 起 PG + psql 进入截图；② JSONB 插入与 `?` 查询截图；③ 实战 Lab MVCC 两会话实验记录；④ 一句话对比 PG vs MySQL 取舍。

## ⚠️ 常见坑
- Docker 起 PG 后本机要用 `psql -h localhost -U postgres`（或 docker exec），端口映射 5432。
- PG 字符串用单引号，双引号是标识符（表/列名），和 MySQL 习惯相反。
- 忘了 `\c 库名` 会在默认 postgres 库建表，查不到。
- JSONB 的 `?` 是"包含键"操作符，字符串匹配用 `->>`。
- 手疼复制命令，别硬敲。

## ➡️ 下一步
DB12 · MySQL 深入（InnoDB / redo / undo / 缓冲池 / 存储过程 / 触发器 / 实战）——直击最流行的 Web 后端数据库，吃透 InnoDB 存储引擎、事务日志、缓冲池，并用存储过程/触发器实战。

---

# DB12 · MySQL 深入（InnoDB / redo / undo / 缓冲池 / 存储过程 / 触发器 / 实战）

## 🎯 目标
学完这节，你能用 Docker 起 MySQL、用 `mysql` 客户端操作；理解 **InnoDB** 的聚簇索引、缓冲池（Buffer Pool）、redo/undo 日志；掌握 **存储过程（Procedure）** 与 **触发器（Trigger）** 的写法与适用边界；并完成一个"账户流水"小实战。

## 📋 小白前置
需先懂 DB3–DB9（SQL 通用）、DB6（事务）、DB7（行锁/MVCC）、DB8（索引）。

## 🟢 部分一·最浅层（生活比喻）
InnoDB 是 MySQL 里"真正干活的引擎"。它把常用数据页放在内存的"缓存池"（Buffer Pool，像厨师手边的备料台，热门食材随手拿），只有冷数据才去磁盘拿。redo 日志像"每笔改动先记账到流水本，真出事按本子重做"；undo 日志像"橡皮擦记录，改错了能擦回原样（回滚）"。存储过程是把一串 SQL 预编译存好，以后"叫名字就执行"（像微波炉预设程序）；触发器是"在某张表发生 INSERT/UPDATE/DELETE 时自动触发的一段代码"（像门铃，有人推门就响）。

## 🟡 部分二·动手层（Docker 起 MySQL + 实战）
```bash
docker run --name mysql -e MYSQL_ROOT_PASSWORD=zeze123 -p 3306:3306 -d mysql:8.0
docker ps | grep mysql
docker exec -it mysql mysql -uroot -pzeze123
```
进入 `mysql>` 后：
```sql
CREATE DATABASE bank CHARACTER SET utf8mb4;
USE bank;
CREATE TABLE account(
  id INT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(50),
  balance DECIMAL(12,2),
  KEY idx_name(name)
) ENGINE=InnoDB;
INSERT INTO account(name,balance) VALUES('A',1000),('B',1000);

-- 看 InnoDB 状态（缓冲池等）
SHOW ENGINE INNODB STATUS\G
```
**存储过程（转账）：**
```sql
DELIMITER //
CREATE PROCEDURE transfer(IN from_id INT, IN to_id INT, IN amt DECIMAL(12,2), OUT ok INT)
BEGIN
  DECLARE bal DECIMAL(12,2);
  START TRANSACTION;
  SELECT balance INTO bal FROM account WHERE id=from_id FOR UPDATE;
  IF bal >= amt THEN
    UPDATE account SET balance=balance-amt WHERE id=from_id;
    UPDATE account SET balance=balance+amt WHERE id=to_id;
    SET ok=1; COMMIT;
  ELSE
    SET ok=0; ROLLBACK;
  END IF;
END //
DELIMITER ;
CALL transfer(1,2,200,@r); SELECT @r;   -- 成功返回1
SELECT * FROM account;                  -- A=800,B=1200
```
**触发器（自动记流水）：**
```sql
CREATE TABLE ledger(
  id INT PRIMARY KEY AUTO_INCREMENT,
  acc_id INT, delta DECIMAL(12,2), ts DATETIME DEFAULT CURRENT_TIMESTAMP
);
DELIMITER //
CREATE TRIGGER after_balance_change
AFTER UPDATE ON account
FOR EACH ROW
BEGIN
  INSERT INTO ledger(acc_id,delta) VALUES(NEW.id, NEW.balance-OLD.balance);
END //
DELIMITER ;
UPDATE account SET balance=balance-50 WHERE id=1;
SELECT * FROM ledger;   -- 自动多出一条流水
```

### 🧪 实战 Lab（DB12）
① 用上面存储过程尝试 `CALL transfer(1,2,99999,@r)`（余额不足），验证 `@r=0` 且余额不变（事务回滚）。② 改 `DELIMITER` 后创建触发器，验证 UPDATE 自动写 ledger。③ 用 Python `mysql-connector` 调用存储过程：
```python
import mysql.connector
cnx = mysql.connector.connect(host='localhost',user='root',password='zeze123',database='bank')
cur = cnx.cursor()
cur.callproc('transfer', (1,2,100,0))   # 最后一个0是OUT占位
for r in cur.stored_results(): print(r.fetchall())
cnx.commit(); cnx.close()
```
把实验结果与 Python 输出发我。

## 🔵 部分三·原理层（底层）
- **聚簇索引**：InnoDB 把数据行**按主键顺序存在 B+树叶子**（这就是"表就是索引"）。二级索引叶子存主键，回表要再查聚簇树——所以主键宜短、稳定、递增（UUID 致页分裂）。
- **缓冲池（Buffer Pool）**：内存中缓存数据页/索引页，读先查池，写先改池再异步刷盘（脏页）。池越大命中率越高，性能越好。
- **redo log**：保证持久性——改动先写 redo（顺序写，快），崩溃后重放已提交事务。redo 是固定大小循环写（ib_logfile）。
- **undo log**：保证原子性/ MVCC——记录改动前旧值，ROLLBACK 用它恢复；老版本供快照读。
- **Doublewrite Buffer**：防页半写（partial page write）导致损坏，写盘前先写双写区。

## 🟣 部分四·深挖层（为什么、延伸）
为什么 MySQL 默认 InnoDB（而非老 MyISAM）？因为 InnoDB 支持**事务、行锁、崩溃恢复、外键**——MyISAM 只适合只读报表。InnoDB 的 MVCC 靠"undo 版本链 + 事务 ID（trx_id）"实现，读不加锁（快照读）。**存储过程/触发器**是把逻辑放进数据库服务端：好处是减少网络往返、保证一致；坏处是难以版本管理、难调试、易在库里堆积"隐形逻辑"（所以很多团队倾向把业务逻辑放应用层，库只做存储）。顶级工程师对触发器的态度：谨慎——用于审计日志、约束这类"必须有、且不该被应用绕过的"场景，而非业务主逻辑。

## 🔴 部分五·顶级视角
MySQL/InnoDB 是 Web 后端事实标准（LAMP/LEMP 栈核心）。顶级工程师深入掌握：缓冲池命中率调优、`innodb_flush_log_at_trx_commit`（性能 vs 持久性权衡）、死锁监控（`SHOW ENGINE INNODB STATUS`）、慢查询。他们也理解"存储过程是把双刃剑"——金融核心系统常用存储过程保证强一致，互联网公司多避免以保敏捷。InnoDB 源码是理解 B+树/锁/MVCC 的工程范本。

## 🟠 部分六·安全/合规种子
白帽要点：① 刚起的 MySQL root 密码要强，**绝不**暴露 3306 到公网；用专用低权账号连应用（`CREATE USER 'app'@'%' IDENTIFIED BY '...'; GRANT SELECT,INSERT... ON bank.* TO 'app'@'%';`）。② 存储过程/触发器里的 `SQL` 拼接同样会注入，坚持参数化。③ PII 列用 `AES_ENCRYPT` 或应用层加密；审计日志（ledger 触发器）正好满足《个人信息保护法》"处理活动记录"要求。④ 只操作你授权拥有的实例，绝不对非授权 MySQL 做连接/爆破。

## ✅ 验收
交：① Docker 起 MySQL + 建库截图；② 存储过程转账成功与"余额不足回滚"两次结果；③ 触发器自动写 ledger 截图；④ Python mysql-connector 调用存储过程输出。

## ⚠️ 常见坑
- `DELIMITER //` 改变语句结束符，建完存储过程/触发器务必 `DELIMITER ;` 改回，否则后续 `;` 会被吞。
- MySQL 字符串单引号；`\G` 是客户端竖向显示，不是 SQL 标准。
- root 密码若含特殊字符，docker `-pzeze123` 可直接跟（无空格），或用 `-e` 交互输入。
- 触发器 `FOR EACH ROW` 里用 `NEW`/`OLD` 引用新/旧行，INSERT 无 OLD、DELETE 无 NEW。
- 手疼复制，别硬敲。

## ➡️ 下一步
DB13 · 主从复制与读写分离（binlog / 延迟）——学会用 binlog 把数据从一个库同步到多个库，理解读写分离如何扛高并发，并认识"主从延迟"这个经典坑。

---

# DB13 · 主从复制与读写分离（binlog / 延迟）

## 🎯 目标
学完这节，你能说清 MySQL **主从复制**的原理（binlog → 中继日志 → 重放）；理解"一主多从 + 读写分离"如何把读压力分摊；认识**主从延迟**这个经典坑及其应对；并能在 Docker 里搭一个最小主从验证同步。

## 📋 小白前置
需先懂 DB12（MySQL/InnoDB/binlog 概念）、DB3（SQL 通用）。

## 🟢 部分一·最浅层（生活比喻）
主库像"总店"，负责收钱记账（写）；从库像"分店/复印件"，专门给人查账（读）。总店每记一笔，就把账页复印发给各分店，分店照抄一遍——这就是复制。好处：查账的人去分店，不挤总店；总店塌了还有分店顶着（高可用）。但有个小问题：总店刚记完，分店可能还没抄完，你去分店查会看到"旧账"——这就是**主从延迟**。

## 🟡 部分二·动手层（Docker 起一主一从，最小验证）
用 docker-compose 或分别起两个 MySQL。先起主库：
```bash
docker run --name mysql-master -e MYSQL_ROOT_PASSWORD=zeze123 -p 3306:3306 -d mysql:8.0
docker exec -it mysql-master mysql -uroot -pzeze123 -e "CREATE DATABASE reprtest; CREATE TABLE reprtest.t(id INT PRIMARY KEY AUTO_INCREMENT, v INT);"
```
进入主库开 binlog 并建复制账号（MySQL 8 默认已开 binlog）：
```bash
docker exec -it mysql-master mysql -uroot -pzeze123 -e "CREATE USER 'repl'@'%' IDENTIFIED BY 'repl123'; GRANT REPLICATION SLAVE ON *.* TO 'repl'@'%';"
```
> 注：完整主从还需配置 `server-id`、`log-bin`、用 `SHOW MASTER STATUS;` 取 binlog 位点并在从库 `CHANGE MASTER TO ... START REPLICA;`。为护手与篇幅，这里给出**原理与最小验证**：在主库 INSERT 一行，观察从库（若已配好）会出现同样行。你可在本地用同一份数据目录克隆两个容器体会"数据一致"。

**读写分离在应用层（Python 示意）：**
```python
# 写走主库，读走从库（伪代码）
def write(sql, params):   return master.execute(sql, params)
def read(sql, params):    return slave.execute(sql, params)   # 读从库
```

### 🧪 实战 Lab（DB13）
① 文字描述主从复制三步（binlog 记录 → 从库 IO 线程拉取存为 relay log → SQL 线程重放）。② 解释场景："用户刚改完昵称，刷新页面却看到旧名"可能是什么原因（主从延迟）？给出两种缓解方案（强制读主/读后短暂延迟读主/用版本号）。③ 用 `SHOW BINARY LOG STATUS;`（MySQL 8）在主库查当前 binlog 文件与位点，截图。把这三部分发我。

## 🔵 部分三·原理层（底层）
MySQL 复制基于 **binlog（二进制日志）**：主库把所有"改变数据的事件"（行级/语句级）顺序写入 binlog。从库有两个线程：① **I/O 线程**连主库，拉取 binlog 存为本地的 **relay log（中继日志）**；② **SQL 线程**读取 relay log 重放，使从库数据追上主库。复制是**异步**的（主库提交即返回，不等从库），所以天然有延迟。还有**半同步复制**（至少一个从库确认收到才返回，降低丢数据风险）和**组复制（MGR，Paxos）**等更强一致方案。

binlog 三种格式：`STATEMENT`（记 SQL，省空间但有函数/不确定性风险）、`ROW`（记每行变更，最安全、默认）、`MIXED`。redo/undo（DB12）是**崩溃恢复**用的，binlog 是**复制与时间点恢复**用的——两者分工不同。

## 🟣 部分四·深挖层（为什么、延伸）
为什么读写分离能扛量？因为典型业务**读多写少**（如电商浏览远大于下单），把读分流到多个从库，主库只扛写，整体吞吐数倍提升。但延迟带来"读己之写"难题——社交/金融类强一致场景不能容忍。延伸：**多主复制**（双向同步，冲突解决复杂）、**级联复制**（主→从→从，减轻主库压力）、**延迟监控**（`Seconds_Behind_Source`）。顶级工程师追问：复制拓扑怎么选？脑裂怎么防？数据一致性 SLA 是多少？

## 🔴 部分五·顶级视角
复制是"高可用 + 可扩展"的基石，也是分布式系统的缩影（DB24/DB25）。顶级工程师把它和**分库分表（DB14）**、**缓存（DB16）**、**负载均衡（NET13）**组合成生产架构。他们也深刻理解：复制不是备份（从库跟着主库一起删！DB27 会讲真正备份）。在云上，RDS 的主从、只读副本（read replica）就是这个概念的产品化。

## 🟠 部分六·安全/合规种子
白帽要点：① 复制账号（`repl`）只给 `REPLICATION SLAVE` 最小权限，且用强密码、限制来源 IP，绝不给 ALL。② 复制链路若跨网络要加密（SSL），否则 binlog 内容（可能含 PII）在传输中被嗅探——违反《个人信息保护法》传输安全。③ 只搭你授权拥有的主从；绝不对非授权实例做复制/抓包。复制架构本身不提供访问控制，权限仍靠账号体系。

## ✅ 验收
交：① 主从复制三步文字描述；② "改完刷新看到旧名"的原因与两种缓解方案；③ `SHOW BINARY LOG STATUS`（或 `SHOW MASTER STATUS`）截图或输出。

## ⚠️ 常见坑
- 主从 `server-id` 必须不同，否则复制报错。
- 异步复制天然延迟，"刚写入立刻读"可能读不到——设计时要考虑。
- 从库是主库的镜像，**主库误删从库也删**，复制≠备份。
- 忘记 `START REPLICA`/`START SLAVE` 复制不会跑；`Seconds_Behind_Source` 很大说明从库跟不上。
- 手疼复制命令，别硬敲。

## ➡️ 下一步
DB14 · 分库分表与分片（路由 / 热点）——当单库扛不住时，学会按"用户ID取模""按时间区间"把数据切到多库多表，理解分片键选择与热点问题。

---

# DB14 · 分库分表与分片（路由 / 热点）

## 🎯 目标
学完这节，你能解释"为什么单库会到天花板"；理解**垂直拆分**（按业务分库）与**水平拆分（分片/Sharding）**（按行切到多表）；掌握分片键选择、路由算法（取模、范围、一致性哈希）；并认识"热点分片/跨分片查询"难题。

## 📋 小白前置
需先懂 DB13（复制/扩展）、DB8（索引）、DB3（SQL）。

## 🟢 部分一·最浅层（生活比喻）
一个仓库塞太多货会爆（单库 CPU/磁盘/连接数到顶）。分库分表就是"把货分去多个仓库"：垂直拆分像"把衣服放 A 仓、食品放 B 仓"（按种类）；水平拆分像"把全国订单按省份切成 31 个仓，每仓只存本省单"（按行切）。分片键就是"按什么规则切"——选错规则（如全按'北京'切），北京仓爆满其他空着，这就是**热点**。

## 🟡 部分二·动手层（用 SQL 体会分片路由）
分片通常在中间件（如 ShardingSphere）或应用层做。这里用品 SQLite 三个分片表模拟"按 user_id 取模分 2 片"：
```python
import sqlite3
# 两个分片文件
for i in (0,1):
    sqlite3.connect(f"shard{i}.db").execute(
        "CREATE TABLE orders(id INT, uid INT, amt REAL)")
def route(uid): return uid % 2   # 分片键=user_id，取模
def insert(uid, amt):
    s = route(uid)
    sqlite3.connect(f"shard{s}.db").execute(
        "INSERT INTO orders(uid,amt) VALUES(?,?)",(uid,amt))
for uid in (1,2,3,4,5):
    insert(uid, 10.0*uid)
# 查某用户：直接路由到其分片
def find(uid):
    s=route(uid)
    return sqlite3.connect(f"shard{s}.db").execute(
        "SELECT * FROM orders WHERE uid=?",(uid,)).fetchall()
print(find(3))   # 只在 shard1 查，快
```
运行 `python shard_demo.py`。体会"知道分片键就能精准定位一片，避免扫全部"。

### 🧪 实战 Lab（DB14）
① 用上面脚本插入 uid 1~10，手动验证 uid=4 落在 shard0、uid=5 落在 shard1（取模）。② 讨论：若改成分片键为"下单城市"，会出现什么问题（某大城市订单集中=热点）？写 3 条分片键选择原则。③ 跨分片查询难题：要"查所有 uid 的订单总额"，代码该怎么写（遍历所有分片再汇总）？把代码与讨论发我。

## 🔵 部分三·原理层（底层）
- **垂直拆分**：把不同业务表分到不同库（用户库、订单库、商品库），降低单库耦合与负载；缺点是跨库 JOIN/事务难（要分布式事务 DB24）。
- **水平拆分（Sharding）**：同一张表按分片键切成 N 个物理表/库，逻辑上还是一张表。路由算法：
  - **取模/哈希**：`shard = hash(key) % N`，均匀但扩容（N 变）要重新分布数据（麻烦）。
  - **范围**：按时间/ID 段（2024Q1→shard1），易扩容但易热点（新数据全压最新片）。
  - **一致性哈希**：环上分槽，节点增减只影响邻近数据，扩容友好（Redis Cluster/分布式缓存常用）。
- **全局主键**：分片后自增 ID 会撞，要用 **雪花算法（Snowflake）/UUID/号段** 生成全局唯一 ID。

## 🟣 部分四·深挖层（为什么、延伸）
分片是"无共享（shared-nothing）"架构的核心，能把数据摊到成百上千台机器。但代价巨大：① 跨分片 JOIN 要在应用层/中间件做（笛卡尔爆炸风险）；② 跨分片事务只能靠 2PC/TCC（DB24），慢且复杂；③ 运维多个库难。所以顶级工程师的共识：**能不分就不分，先用复制/缓存/优化顶住；真到单库极限再分，且优先垂直拆分**。NewSQL（TiDB/CockroachDB）试图"自动分片 + 强一致"抹平这些痛点，是方向5 的进阶。

## 🔴 部分五·顶级视角
分片思维贯穿分布式系统：Redis Cluster（DB16）用哈希槽分片、Kafka 用分区（partition）、HDFS 用块、Elasticsearch 用 shard——**"把数据按 key 切到多机 + 路由 + 再均衡"**是同一个范式。顶级架构师设计分片时最纠结"分片键"：它决定负载是否均衡、查询是否高效、扩容是否无痛。选错键是生产级事故（如某大厂曾因用户 ID 取模但存在超级大 V 致热点）。

## 🟠 部分六·安全/合规种子
白帽要点：① 分片后仍要**每片都做最小权限 + 加密 + 审计**，不能因为"拆了"就放松任一片的安全。② 跨片聚合查询可能把多片 PII 汇总，扩大泄露面，须遵守《个人信息保护法》的数据最小化与出境规定。③ 分片路由逻辑若可被用户操控 key（如可预测 user_id），要防"分片枚举/探测"——但只在你授权系统内加固，绝不针对他人。

## ✅ 验收
交：① 分片路由脚本运行结果（uid=3 落在哪片）；② 分片键选择三原则；③ 跨分片汇总总额的实现思路/代码。

## ⚠️ 常见坑
- 分片键一旦选定很难改——提前想清查询模式（80% 按什么查）。
- 取模分片扩容（N 变）要迁移大量数据，提前规划或用一致性哈希。
- 跨分片事务极复杂，能用"最终一致 + 补偿"就别上 2PC。
- 全局 ID 别用单库自增，会冲突。
- 手疼复制脚本，别硬敲。

## ➡️ 下一步
DB15 · NoSQL 总览与 CAP（向量数据库预览 / 取舍）——跳出关系模型，认识键值/文档/列族/图/时序/向量五大类，用 CAP 定理理解它们为何"放弃点什么换扩展性"。

---

# DB15 · NoSQL 总览与 CAP（向量数据库预览 / 取舍）

## 🎯 目标
学完这节，你能说出 NoSQL 五大类的代表与适用场景；用 **CAP 定理**解释"为什么没有又一致又永远可用的万能库"；理解"BASE"与"最终一致性"；并预习**向量数据库**（后面 DAT34 深讲）在 AI 检索里的角色。

## 📋 小白前置
需先懂 DB2（关系模型/范式）、DB14（分片扩展思想）、DB8（索引）。

## 🟢 部分一·最浅层（生活比喻）
关系数据库像"规矩严格的图书馆"（书必须按编目上架，查得准但扩容麻烦）。NoSQL 是"各种随性仓库"：键值库像"储物柜（刷码取物，超快）"、文档库像"文件袋（连袋带内容一起存，结构随意）"、列族像"按列归档的档案室"、图库像"朋友圈关系网"、向量库像"按'意思相近'排的相册"。CAP 定理说：网络出问题时，你只能在"数据完全一致"和"服务照常可用"里二选一——就像地震时你要么锁门保安全（一致），要么开门保服务（可用），不能既要又要。

## 🟡 部分二·动手层（用 Docker 快速感受各类）
```bash
# 键值：Redis（DB16 详讲，这里只连一下）
docker run --name redis -p 6379:6379 -d redis:7
docker exec -it redis redis-cli SET hello world
docker exec -it redis redis-cli GET hello      # "world"

# 文档：MongoDB（DB17 详讲）
docker run --name mongo -p 27017:27017 -d mongo:7
```
关系 vs 文档对比（Mongo shell 概念，DB17 实操）：
```js
// 文档库允许同集合里结构不同，灵活
db.users.insertOne({name:"小明", age:18, tags:["安全","算法"]})
db.users.insertOne({name:"小红", city:"北京"})   // 不同字段也能存
```
体会：关系库要预先建表定列，文档库"写啥是啥"。

### 🧪 实战 Lab（DB15）
① 列出五类 NoSQL（键值/文档/列族/图/向量）各一代表及一句适用场景。② 用 CAP 解释：为什么 Redis（主从）在网络分区时可能"读到旧值"（选了 AP，放弃强 C）？为什么银行等选 CP（宁可停服也要一致）？③ 向量数据库预习：用一句话说明"向量数据库存的是'含义的数字化坐标'，靠近似最近邻（ANN）找相似"（如用 embedding 搜相似的文章/图片），并说出它在 RAG（DB33）里干嘛。把这三部分发我。

## 🔵 部分三·原理层（底层/CAP）
**CAP 定理**（Brewer 2000，Gilbert/Lynch 证明）：分布式系统在网络**分区（Partition）**发生时，**一致性（C，所有节点同一时刻看到相同数据）**与**可用性（A，每个请求都有响应）**不可兼得，只能保一个（P 分区容忍是必选项，因为网络必会断）。所以现实是 **CP**（如 ZooKeeper、etcd、HBase——保一致，分区时部分不可写）或 **AP**（如 Cassandra、DynamoDB——保可用，分区时可能读到旧值/最终一致）。

**BASE**：Basically Available（基本可用）、Soft state（软状态，允许中间不一致）、Eventually consistent（最终一致）——这是 AP 系统的设计哲学，接受"短暂不一致，最终对齐"。注意：**CA 在分布式里不存在**（单机能 CA，但单点不分区容忍）。

## 🟣 部分四·深挖层（为什么、延伸）
为什么 NoSQL 兴起？因为 2000s 后互联网**数据量+并发**爆炸，关系库的"强一致+Join+事务"在水平扩展上吃力（DB14）。NoSQL 通过"放宽 ACID 某些保证、用分片"换线性扩展。但注意：**很多 NoSQL 也在补回 ACID**（Mongo 多文档事务、Redis 事务、Cassandra 轻事务）——这是"融合"趋势。顶级工程师不迷信"NoSQL 就好"，而是**按访问模式选型**：强事务+复杂查询→关系；超高频简单 KV→Redis；半结构文档→Mongo；海量写宽行→Cassandra；关系网→图库；语义相似→向量库。

## 🔴 部分五·顶级视角
CAP 是分布式系统的"物理定律级"约束，顶级工程师用它做**架构取舍**：支付/库存核心选 CP（宁可短暂不可用也不出错账），feed/评论/点赞选 AP（短暂不一致用户无感）。他们也能识别"伪 CP/伪 AP"营销话术，看懂每个系统在分区时的真实行为。向量数据库（DAT34）是 AI 时代新秀，把"相似检索"做成一等公民，和关系/文档库互补而非替代。

## 🟠 部分六·安全/合规种子
白帽要点：① NoSQL 不等于"无安全"——MongoDB/Redis 默认无密码曾导致海量数据裸奔被勒索（DB28 讲防护）。任何实例都设强密码、绑内网、最小权限。② 最终一致性下，PII 的"删除/更正"可能延迟生效，要评估是否满足《个人信息保护法》"被遗忘权/更正权"的时效要求。③ 向量库里存的 embedding 可能被视为个人信息衍生物，同样受保护。④ 只搭你授权拥有的实例。

## ✅ 验收
交：① 五类 NoSQL 代表+场景表；② CAP 对 Redis(AP)/银行(CP) 的解释；③ 向量数据库一句话定义 + 在 RAG 中的角色。

## ⚠️ 常见坑
- CAP 的 C 是"强一致/线性一致"，不是"关系库的 ACID 的 C（一致性约束）"——两个 C 别混。
- "CA 系统"只在单机存在；分布式必谈 CP/AP。
- NoSQL 也有事务（程度不同），别以为"NoSQL=无事务"而写出数据错乱。
- 新上手某 NoSQL 先改默认无密码配置，否则公网一秒被扫。
- 手疼复制命令，别硬敲。

## ➡️ 下一步
DB16 · Redis（数据结构 / 持久化 / 集群 / 管道 / 缓存击穿雪崩）——深入最强键值库，掌握五大结构、RDB/AOF 持久化、缓存三大灾难与应对。

---

# DB16 · Redis（数据结构 / 持久化 / 集群 / 管道 / 缓存击穿雪崩）

## 🎯 目标
学完这节，你能用 Redis 的五大基础结构（String/Hash/List/Set/ZSet）解决实际问题；理解 **RDB 与 AOF** 两种持久化取舍；用 Python `redis-py` 实战；搞懂**缓存击穿/雪崩/穿透**三大灾难及应对；并了解集群分片。

## 📋 小白前置
需先懂 DB15（键值库概念）、方向1a PY 基础、DB14（分片/一致性哈希影子）。

## 🟢 部分一·最浅层（生活比喻）
Redis 是"内存里的超大储物柜王国"——所有东西放内存，所以快到离谱（微秒级）。五大结构像五种柜子：String=贴了标签的格子（一个值）、Hash=带多个小格的抽屉（存对象）、List=排队通道（左进右出=队列）、Set=不重不漏的收藏夹、ZSet=带分数的排行榜（按分排序）。持久化就是"每隔一阵把内存快照写磁盘（RDB）"或"每条命令都记日志（AOF）"，免得停电全没。缓存雪崩像"所有储物柜同时过期，大家一拥而上挤爆后端数据库"。

## 🟡 部分二·动手层（Docker 起 Redis + redis-cli + Python）
```bash
docker run --name redis -p 6379:6379 -d redis:7
docker exec -it redis redis-cli
```
在 `127.0.0.1:6379>` 下：
```bash
SET user:1:name "小明"          # String
HSET user:1 age 18 city 北京     # Hash（中文需终端UTF-8）
LPUSH queue job1 job2            # List
SADD tags 安全 算法              # Set
ZADD rank 95 小明 88 小红        # ZSet 排行榜
GET user:1:name
HGET user:1 city
ZREVRANGE rank 0 -1 WITHSCORES   # 从高到低
EXPIRE user:1:name 30            # 30秒过期
TTL user:1:name
```
Python 实战（redis-py）：
```python
import redis
r = redis.Redis(host='localhost', port=6379, decode_responses=True)
r.set("counter", 0)
r.incr("counter")                 # 原子自增，适合计数/限流
print(r.get("counter"))
r.hset("user:2", mapping={"name":"小红","age":19})
print(r.hgetall("user:2"))
# 管道（pipeline）批量，减少网络往返
pipe = r.pipeline()
for i in range(100): pipe.incr("bulk")
pipe.execute()
```

### 🧪 实战 Lab（DB16）
① 用 ZSet 做一个"本周积分排行榜"：插入 5 个同学分数，查出前三名（`ZREVRANGE rank 0 2 WITHSCORES`）。② 用 `EXPIRE` 设一个 key 10 秒过期，期间用 `TTL` 观察倒计时到 -2（已删）。③ 缓存三大灾难简答：
- **穿透**：查一个**根本不存在**的 key（如 id=-1），每次都打到 DB → 解决：缓存空值 / 布隆过滤器。
- **击穿**：某**热点 key 刚过期**瞬间，海量请求同时砸 DB → 解决：互斥锁重建 / 逻辑过期。
- **雪崩**：**大量 key 同时过期**或 Redis 宕机 → 解决：过期时间加随机抖动 / 多级缓存 / 高可用集群。
把排行榜结果 + 三灾难应对发我。

## 🔵 部分三·原理层（底层）
- **内存存储 + 单线程事件循环**：Redis 主要操作在单线程处理命令（6.0 后 IO 多线程，但命令执行仍单线程），避免了锁竞争；性能瓶颈在内存带宽/网络，而非 CPU。数据全在内存，持久化是"异步落盘"。
- **持久化**：
  - **RDB**：定时把内存快照写成紧凑二进制文件（`save 900 1` 等）。恢复快、文件小，但可能丢最后一次快照后的数据。
  - **AOF**：把每条写命令追加到日志，可配置 `everysec`（每秒刷盘，最多丢1秒）。数据更全，但文件大、恢复慢。可 `AOF + RDB` 混合。
- **管道（pipeline）**：客户端把多条命令打包一次发送，服务端批量执行，省去多次网络往返 RTT。
- **集群**：Redis Cluster 用 **16384 个哈希槽**分片，每槽映射到某节点；客户端算 `crc16(key)%16384` 定位节点（一致性哈希思想 DB14）。

## 🟣 部分四·深挖层（为什么、延伸）
为什么 Redis 单线程还快？因为瓶颈在内存/IO 而非 CPU，且单线程**无锁、无上下文切换开销**，配合 IO 多路复用（epoll，NET18）。为什么需要持久化？纯内存一断电全丢，RDB/AOF 让它"快而可恢复"。**缓存与数据库一致性**：更新时先更新 DB 还是先删缓存？经典"Cache-Aside"模式：读时懒加载、写时先更新 DB 再删缓存（delete），并防"并发读写导致脏缓存"需延时双删。顶级工程师追问：Redis 是缓存还是数据库？（它也能做主存，但持久性弱于关系库，宜作"高速缓存/会话/排行榜/限流"）。

## 🔴 部分五·顶级视角
Redis 是"性能杠杆"：把热点数据放 Redis，DB 压力骤降。它支撑限流（DB15 令牌桶）、会话、消息队列（List/Stream）、发布订阅、分布式锁（`SETNX`/Redlock）、实时排行榜。顶级工程师深知缓存不是银弹：一致性、击穿雪崩、内存成本都要算。在云上，Redis 即服务（ElastiCache/Redis Cloud）是标配。其"单线程命令执行"思想与 Node.js 事件循环（JS7）、Actor 模型（OTH7）遥相呼应。

## 🟠 部分六·安全/合规种子
白帽要点：① Redis 默认**无密码、绑 0.0.0.0** 是著名勒索入口——必须设 `requirepass`、绑内网、禁用危险命令（`FLUSHALL`/`KEYS *` 可重命名）。② 缓存里也可能有 PII（如用户会话），要加密或设短过期，遵守《个人信息保护法》。③ 绝不对非授权 Redis 实例做连接/未授权访问测试（那属入侵）。④ 分布式锁用于保护自己系统的并发，绝不用于干扰他人。

## ✅ 验收
交：① Redis 五结构操作截图；② Python pipeline 实战输出；③ 排行榜前三结果；④ 缓存穿透/击穿/雪崩的定义 + 各自一种应对。

## ⚠️ 常见坑
- 中文在 redis-cli 显示取决于终端编码，存没问题，显示乱码可忽略（程序取是 UTF-8）。
- 忘了设密码 + 暴露公网 = 被勒索，第一件事改配置。
- `KEYS *` 在生产会阻塞（遍历全库），用 `SCAN` 替代。
- 缓存和 DB 一致性要设计，不能"写完就不管"。
- 手疼复制，别硬敲。

## ➡️ 下一步
DB17 · MongoDB（文档模型 / 聚合管道 / 分片）——深入文档数据库，理解 BSON、灵活 schema、聚合管道，以及它和关系库的本质差异与取舍。

---

# DB17 · MongoDB（文档模型 / 聚合管道 / 分片）

## 🎯 目标
学完这节，你能用 Docker 起 MongoDB、用 `mongosh` 插入/查询文档；理解**文档模型（BSON）**与关系模型的根本差异（嵌套 vs JOIN）；掌握**聚合管道（aggregation pipeline）**做分组统计；理解 Mongo 的分片与适用边界。

## 📋 小白前置
需先懂 DB2（关系/范式）、DB15（NoSQL 总览）、DB3（查询思想）。

## 🟢 部分一·最浅层（生活比喻）
关系库像"Excel 多张表 + 用编号关联"（订单表存客户ID，要 JOIN 客户表才知道是谁）。文档库像"文件袋"——把一条订单**连同它的客户信息、商品列表整个塞进一个 JSON 文件**存起来，不用到处找。灵活：有的袋子里多塞个"优惠码"字段，有的没有，都不报错（schema 自由）。代价是：要查"所有买过某商品的客户"得翻每个袋子，不如关系库 JOIN 爽。

## 🟡 部分二·动手层（Docker 起 Mongo + mongosh）
```bash
docker run --name mongo -p 27017:27017 -d mongo:7
docker exec -it mongo mongosh
```
在 `test>` 下：
```js
use school;
db.students.insertMany([
  {name:"小明", age:18, tags:["安全","算法"], scores:{math:95, eng:88}},
  {name:"小红", age:19, tags:["安全"], scores:{math:90, eng:92}}
]);
// 查
db.students.find({age:{$gte:18}}).pretty();
// 嵌套字段查询
db.students.find({"scores.math":{$gt:90}}).pretty();
// 聚合：按 tag 统计人数
db.students.aggregate([
  {$unwind:"$tags"},
  {$group:{_id:"$tags", cnt:{$sum:1}}},
  {$sort:{cnt:-1}}
]);
```
Python（pymongo）：
```python
from pymongo import MongoClient
c = MongoClient("mongodb://localhost:27017/")
col = c.school.students
print(col.find_one({"name":"小明"}))
```

### 🧪 实战 Lab（DB17）
① 用 `insertMany` 插入 3 名学生，用 `find` 查 age>18 的。② 写聚合管道统计"每门课（math/eng）平均分"（提示：先 `$group` by 什么？其实要展开 scores——用 `$project`+`$objectToArray` 或分别 avg）。简化版：统计"每个 tag 的人数"（上面例子）。③ 对比关系库：这种"嵌套文档"在 MySQL 里要几张表？写出关系建模方式（学生表 + 标签表），说明 Mongo 为什么在这里更自然。把实验与对比发我。

## 🔵 部分三·原理层（底层）
- **BSON**：二进制的 JSON 超集，支持日期、二进制、ObjectId 等类型，序列化快、空间小，是 Mongo 的存储与传输格式。
- **文档与集合**：集合（collection）≈ 表，文档（document）≈ 行，但文档可自由嵌套子文档/数组——这就是"反范式内嵌"。
- **WiredTiger 存储引擎**：默认用 B+树（也有 LSM 选项），支持文档级并发（与 InnoDB 行锁类似）、压缩、MVCC 快照。
- **聚合管道**：数据像流水线，每个 `$stage` 接收上一批文档、产出下一批（`$match` 过滤、`$group` 分组、`$project` 投影、`$lookup` 做"类 JOIN"）。思想和 SQL 的 WHERE/GROUP BY/SELECT 对应，但更强调"逐步变换"。

## 🟣 部分四·深挖层（为什么、延伸）
文档模型适合**数据形态多变、读多写少、需整体读取**的场景（内容管理、IoT 设备上报、用户画像）。Mongo 4.0+ 支持**多文档 ACID 事务**（弥补早期弱点），但跨分片事务仍重。顶层取舍：**内嵌 vs 引用**——"一对少且常一起读"就内嵌（如订单里的商品快照），"一对多且独立演进"就引用（用 `$lookup`）。顶级工程师会在此和关系库间做清醒选型，而非跟风。

## 🔴 部分五·顶级视角
Mongo 展示了"NoSQL 也能有丰富查询与事务"。顶级工程师用聚合管道做实时分析、用变更流（change stream）做 CDC、用分片（类似 DB14 的哈希/范围分片）撑海量。它也启发了"JSON 在关系库里"（PG 的 JSONB、MySQL 的 JSON 列）——范式边界在融合。理解文档模型，对设计 API 返回结构、前端状态树都有益。

## 🟠 部分六·安全/合规种子
白帽要点：① Mongo 默认**无鉴权**，历史上"裸奔"被勒索无数——必须 `mongod --auth`、建用户、强密码、绑内网。② 文档自由 ≠ 可存任意他人 PII，仍受《个人信息保护法》约束，敏感字段加密、最小化。③ `$lookup` 等聚合也可能跨集合聚合出可识别个人，注意脱敏。④ 只连你授权拥有的实例。

## ✅ 验收
交：① Docker 起 Mongo + 插入/查询截图；② 聚合统计 tag 人数结果；③ 关系建模 vs 文档建模的对比文字。

## ⚠️ 常见坑
- mongosh 里字符串用双引号或单引号均可（JS 语法）。
- 忘了 `use school;` 会建到默认 test 库，查不到。
- `_id` 默认是 ObjectId，不是自增整数，应用层别假设有序。
- 聚合管道顺序很重要（`$match` 越早过滤越省资源）。
- 手疼复制，别硬敲。

## ➡️ 下一步
DB18 · Cassandra（LSM / 列族 / 宽行）——理解为"写多读少、海量数据、全球分布"而生的列族数据库，及其与 B+树库的底层差异。

---

# DB18 · Cassandra（LSM 树 / 列族 / 宽行）

## 🎯 目标
学完这节，你能说清 Cassandra 的设计目标（海量写入、多数据中心、高可用）；理解 **LSM 树（Log-Structured Merge）** 为什么写极快；搞懂"列族/宽行"模型与分区键；并说出它为什么不适合复杂查询/事务。

## 📋 小白前置
需先懂 DB15（NoSQL/CAP）、DB8（B+树对比）、DB3（查询）。

## 🟢 部分一·最浅层（生活比喻）
如果 Redis 是"内存储物柜"、Mongo 是"文件袋"，Cassandra 就是"永不停工、只管往里记的超级日志本"——它专为"疯狂写"（如每秒百万条传感器数据）而生。它用 LSM 树：写的时候先记进内存的小本（快），后台再慢慢合并进大本（顺序写磁盘，极快）。但代价是"读可能慢一点、查法不灵活"——你只能按它设定好的"主键"查，不能随便按任意条件搜。

## 🟡 部分二·动手层（Docker 起 Cassandra + cqlsh）
```bash
docker run --name cass -p 9042:9042 -d cassandra:5
# 等约30秒启动
docker exec -it cass cqlsh
```
在 `cqlsh>` 下（CQL 类似 SQL 但有限）：
```sql
CREATE KEYSPACE IF NOT EXISTS iot
  WITH replication = {'class':'SimpleStrategy','replication_factor':1};
USE iot;
CREATE TABLE sensor_data (
  device_id text,
  day text,
  ts timeuuid,
  value double,
  PRIMARY KEY ((device_id, day), ts)     -- 复合主键：分区键+聚簇列
) WITH CLUSTERING ORDER BY (ts DESC);
INSERT INTO sensor_data(device_id,day,ts,value)
  VALUES ('d1','2024-01-01', now(), 23.5);
SELECT * FROM sensor_data WHERE device_id='d1' AND day='2024-01-01' LIMIT 10;
```
注意：`WHERE` 必须包含分区键，否则报错——这是宽行模型的约束。

### 🧪 实战 Lab（DB18）
① 起 Cassandra 并建表插入查询成功截图（或文字记录键空间/表创建）。② 解释 `PRIMARY KEY ((device_id, day), ts)` 中"分区键"和"聚簇列"各起什么作用（分区键决定数据落到哪台机器、聚簇列决定行内排序）。③ 说明为什么 CQL 不能像 SQL 那样 `WHERE value>20`（无二级索引/需另行建索引，且不是主要用法）。把这三部分发我。

## 🔵 部分三·原理层（底层/LSM）
- **LSM 树**：写入先进 **MemTable（内存）**，写满刷成 **SSTable（磁盘有序文件）**；后台 **Compaction（合并）** 把多个 SSTable 归并去重。全程**顺序写**，回避了 B+树的随机写，所以写入吞吐极高。代价：读可能要查多个 SSTable（用 Bloom Filter 加速），且读放大/写放大需调优。
- **列族 / 宽行**：Cassandra 的"行"可以是极宽的（百万列），同一分区键的行按聚簇列物理相邻，适合"某设备一段时间的所有读数"这种范围扫描。
- **无主架构 + 一致性可调**：节点对等，写入可按 `QUORUM`/`ONE` 调一致级别；依赖 **Gossip** 做节点发现，**Tunable Consistency** 在 CAP 里偏向 AP 但可调。

## 🟣 部分四·深挖层（为什么、延伸）
Cassandra 源自 2007 年 Amazon Dynamo（AP、最终一致）与 Google BigTable（列族）的结合，由 Facebook 开源，目标是**跨数据中心、线性扩展、写主导**。顶级工程师追问：为什么 IoT/日志/时序用它？因为"只追加、按主键范围读、不怕丢一两条最终补齐"。它**不支持跨行事务、不支持 JOIN、不支持灵活二级索引**——这些是它用"放弃"换来的扩展能力。Bigtable/ HBase/ ScyllaDB 都属 LSM 家族。

## 🔴 部分五·顶级视角
LSM 树是"写优化存储"的范式，不仅 Cassandra，RocksDB（被 TiDB/Flink/Kafka 用）、LevelDB、HBase 都用它。顶级工程师在"高写入"场景（时序、日志、消息后端、广告点击流）首选 LSM 系。理解 LSM 也帮你读懂"写放大/读放大/空间放大"三角权衡——这是存储引擎设计的核心张力。

## 🟠 部分六·安全/合规种子
白帽要点：① Cassandra 默认无认证，要开 `authenticator: PasswordAuthenticator` + 角色授权。② 海量日志可能含用户行为 PII，落库前脱敏、遵守《个人信息保护法》与数据驻留要求（跨境合规）。③ 多数据中心复制要评估数据出境合规。④ 只搭你授权拥有的集群。

## ✅ 验收
交：① Cassandra 建表/插入/查询记录；② 分区键 vs 聚簇列解释；③ CQL 为何不能随意 WHERE 的说明。

## ⚠️ 常见坑
- Cassandra 启动慢（Java 堆/JVM），docker 起后需等几十秒再 cqlsh，否则连不上。
- CQL 的 `WHERE` 必须含分区键，这是模型硬约束，不是语法错。
- `SimpleStrategy` 只适合单数据中心测试，生产用 `NetworkTopologyStrategy`。
- 不要把它当关系库用（无 JOIN/事务），选错场景会很痛。
- 手疼复制，别硬敲。

## ➡️ 下一步
DB19 · 图数据库 Neo4j（属性图 / Cypher）——当你的问题是"关系的关系"（社交、风控、知识图谱）时，图库比 JOIN 海量表快得多，学会 Cypher 与最短路径。

---

# DB19 · 图数据库 Neo4j（属性图 / Cypher）

## 🎯 目标
学完这节，你能用 Docker 起 Neo4j、用 **Cypher** 建节点与关系、查询"谁的朋友的朋友"、算最短路径；理解**属性图模型**为何在"关系密集"场景下吊打关系库的多表 JOIN。

## 📋 小白前置
需先懂 DB3（查询思想）、DB4（JOIN——体会"多跳关系用 JOIN 多表很痛"）。

## 🟢 部分一·最浅层（生活比喻）
关系库存"关系"要用 JOIN 把表连起来，查"小明朋友的朋友的朋友"要连三四张表，越深越慢。图库直接把"关系"当一等公民存——就像你在微信里看"二度人脉"，系统本就存着"谁-认识-谁"的边，顺着边走几步就到了，不用翻通讯录配对。节点=人/物，边=关系（可带属性如"认识于2020"）。

## 🟡 部分二·动手层（Docker 起 Neo4j + Cypher）
```bash
docker run --name neo4j -p 7474:7474 -p 7687:7687 -e NEO4J_AUTH=neo4j/zeze123 -d neo4j:5
```
浏览器打开 `http://localhost:7474`，登录（neo4j/zeze123），在查询框执行：
```cypher
CREATE (a:Person {name:'小明'})-[:FRIEND {since:2020}]->(b:Person {name:'小红'})
CREATE (b)-[:FRIEND]->(c:Person {name:'小刚'})
CREATE (a)-[:FRIEND]->(d:Person {name:'小美'})
RETURN a,b,c,d;

// 查小明的朋友
MATCH (p:Person {name:'小明'})-[:FRIEND]->(f) RETURN f.name;

// 朋友的朋友（二度）
MATCH (p:Person {name:'小明'})-[:FRIEND*2..2]->(fof) RETURN fof.name;

// 最短路径
MATCH path=shortestPath((a:Person {name:'小明'})-[*]-(b:Person {name:'小刚'}))
RETURN path;
```

### 🧪 实战 Lab（DB19）
① 在 Neo4j 建上面 4 人 + 关系，截图二度人脉查询结果（应含小刚、小美）。② 加一个新节点"小雷"并和"小红"建立 FRIEND，再查"小明到小雷的最短路径"。③ 对比：同样"朋友的朋友"在关系库要用几张表 JOIN？（学生表 + 好友关系表自连接两次）说明图库优势。把实验与对比发我。

## 🔵 部分三·原理层（底层）
- **属性图（Property Graph）**：节点带标签（Person）和属性（name），边（关系）有类型（FRIEND）和方向、也可带属性（since）。这是 Neo4j 的数据模型。
- **存储**：Neo4j 用"原生图存储"，节点/关系/属性分别存，关系直接存"指向邻居的物理指针"——所以"顺着边走"是 O(1) 指针跳转，不像关系库要去索引/JOIN 拼。
- **Cypher**：声明式图查询语言，模式用 `(节点)-[:关系]->(节点)` 表达，直观对应图结构。`*2..2` 表示关系跳数范围，`shortestPath` 基于图遍历算法（BFS/Dijkstra）。

## 🟣 部分四·深挖层（为什么、延伸）
为什么图库对"关系"快？因为关系库把关系"降维"成外键数字，查多跳要反复索引查找；图库把关系"升维"成物理指针，遍历即跳转。代价：图库不擅长"大范围聚合/报表"——那种还是关系/列存快。图算法家族：PageRank（网页排名）、社区发现、最短路径、图神经网络（GNN，DAT24）。顶级应用：**风控反欺诈**（异常关系网）、**知识图谱**（DB/AI 融合）、**推荐系统**（二度兴趣）、**IT 资产关联**（安全攻击路径分析，方向7）。

## 🔴 部分五·顶级视角
图思维是"关系即数据"的范式，顶级工程师在反欺诈、安全攻击图（ATT&CK 映射）、社交、供应链分析里离不开它。Neo4j 之外还有 Amazon Neptune、NebulaGraph、JanusGraph。理解图也帮你把"系统依赖/调用链/权限传播"建模成图，做影响分析（如"改这个服务会影响哪些下游"）。在白帽里，攻击路径、权限提升链天然是图。

## 🟠 部分六·安全/合规种子
白帽要点：① 知识图谱里若含个人社会关系，属高度敏感 PII，须强权限管控、脱敏、遵守《个人信息保护法》（社交关系可识别个人）。② 图遍历能力强大，但**只对你授权拥有的数据**做关系分析；绝不爬取/构建非授权个人的关系网。③ Neo4j 设强密码、不暴露 7474/7687 到公网。

## ✅ 验收
交：① Neo4j 建图 + 二度人脉查询截图；② 最短路径结果；③ 关系库 vs 图库查"朋友的朋友"对比。

## ⚠️ 常见坑
- 浏览器访问 7474 后首次登录密码是容器设的 `neo4j/zeze123`（用 `NEO4J_AUTH` 指定）。
- Cypher 关系方向要写对：`->` 和 `<-` 不同；不确定就写无向 `-[:FRIEND]-`。
- 忘记 `RETURN` 会报语法错（Cypher 必须有 RETURN/写操作）。
- 图库不擅长大报表，别啥都往图里塞。
- 手疼复制，别硬敲。

## ➡️ 下一步
DB20 · 时序数据库（TSDB / 降采样）——理解"时间为主轴、只追加、按时间范围扫"的数据为何需要专门引擎，以及降采样/留存策略。

---

# DB20 · 时序数据库（TSDB / 降采样）

## 🎯 目标
学完这节，你能说清时序数据的特征（时间戳为主键、只追加、按时间范围查、写多读少）；理解 TSDB 为何用**列存 + 时间分区**而非 B+树行存；掌握**降采样（downsampling）**与**留存（retention）**策略；并预览 Elasticsearch 与向量检索（DB21）。

## 📋 小白前置
需先懂 DB18（LSM/写主导）、DB15（NoSQL 取舍）、DB3（查询）。

## 🟢 部分一·最浅层（生活比喻）
时序数据就是"带时间标签的流水账"：服务器每秒 CPU 使用率、股票价格、心率监测。它有几个怪脾气：① 永远按时间顺序来（新数据永远是最新时刻）；② 只追加不修改（过去的 CPU 值不会变）；③ 查询几乎都是"给我最近一小时曲线""算今天平均"。普通关系库用 B+树按主键查单条很快，但扛不住这种"海量按时间块扫"——就像用通讯录查单人不难，但要"统计所有人这个月的体温"就笨。TSDB 专为这种"按时间块"优化。

## 🟡 部分二·动手层（用 Prometheus 风格概念 + SQLite 模拟时序）
真 TSDB（InfluxDB/Prometheus/TDengine）可后学，这里用 SQLite 模拟"时序表 + 降采样"，先建立直觉：
```sql
CREATE TABLE metrics(
  ts INTEGER,        -- 时间戳（秒）
  host TEXT,
  cpu REAL
);
-- 模拟插入（实际应有百万行）
INSERT INTO metrics VALUES
 (1700000000,'web1',35.2),(1700000060,'web1',41.0),
 (1700000120,'web1',38.7),(1700000180,'web1',55.3);
-- 按分钟降采样：每分钟平均
SELECT (ts/60)*60 AS minute, AVG(cpu)
FROM metrics GROUP BY minute;
```
真实 Prometheus 查询（PromQL）概念：
```
rate(cpu_usage[5m])          # 5分钟平均速率
avg_over_time(cpu_usage[1h]) # 1小时均值
```

### 🧪 实战 Lab（DB20）
① 用上面 SQLite 模拟，插入某主机 10 个时间点（间隔60秒）CPU 值，写出"按5分钟（300秒）降采样求均值"的 SQL（`ts/300*300` 分组）。② 解释留存策略：若只保留 30 天原始 + 1 年降采样，为什么能省空间又不丢趋势？③ 一句话对比：时序库（InfluxDB）vs 关系库存监控，各适合什么。把实验与说明发我。

## 🔵 部分三·原理层（底层）
TSDB 核心优化来自数据特征：
- **按时间分区 + 列存**：把同一指标的时间序列连续存放，压缩率极高（时间戳可用 delta-of-delta、值用 Gorilla 浮点压缩），且"范围扫描"只需读连续块。
- **LSM 写优化**（InfluxDB/TDengine 用）：只追加写入，避免 B+树随机写。
- **降采样流水线**：原始数据高频写入，后台定时聚合为小时/天粒度并存"降采样表"，查询长周期时直接读聚合结果，极快。
- 区别于 DB18 Cassandra：Cassandra 通用宽行；TSDB 更专精"单指标多时间戳"的压缩与滚动聚合。

## 🟣 部分四·深挖层（为什么、延伸）
时序场景爆发于**监控（Prometheus/Grafana）、IoT、金融 tick 数据、APM**。顶级工程师设计监控栈：采集（OpenTelemetry）→ TSDB（Prometheus/VictoriaMetrics）→ 可视化（Grafana）→ 告警（OPS39）。降采样与留存是成本与价值的平衡：原始精度贵（SSD/内存），降采样便宜且够看趋势。延伸：时序 + 向量（异常检测用 embedding）、时序预测（DAT）。

## 🔴 部分五·顶级视角
"时间"是数据最重要的维度之一。顶级工程师把 TSDB 当作可观测性的地基——没有它就没有 SRE（OPS36+）。它也连接"流处理"（Kafka 实时写入 TSDB）、"边缘计算"（设备本地攒批再上报）。理解时序模型，对做性能监控、业务指标（DAU/留存曲线）、安全审计日志分析都直接有用。

## 🟠 部分六·安全/合规种子
白帽要点：① 监控数据可能暴露系统拓扑、甚至含 PII（如用户行为时序），需访问控制与加密。② 审计日志（谁何时访问了什么）本身就是时序数据，留存要满足合规（如等保/《个人信息保护法》处理记录留存）。③ 只采集你授权系统的指标。

## ✅ 验收
交：① SQLite 模拟时序 + 5分钟降采样 SQL 与结果；② 留存策略说明；③ 时序库 vs 关系库对比。

## ⚠️ 常见坑
- 时序查询务必带时间范围，否则全表扫（和普通库一样慢）。
- 原始数据别无限留，成本爆炸——设 retention。
- 时间戳单位要统一（秒/毫秒/纳秒混用会算错）。
- PromQL 的 `rate` 只用于 counter，gauge 用 `avg_over_time`，别混。
- 手疼复制，别硬敲。

## ➡️ 下一步
DB21 · 搜索引擎 Elasticsearch（倒排索引 / 分词 / 相关性）——理解"全文搜索"为何不能靠 LIKE，倒排索引如何秒级搜亿级文档，以及它和数据库的分工。

---

# DB21 · 搜索引擎 Elasticsearch（倒排索引 / 分词 / 相关性）

## 🎯 目标
学完这节，你能说清"全文搜索为什么不能靠 SQL 的 LIKE"；理解**倒排索引（inverted index）**原理；用 Docker 起 Elasticsearch、用 DSL 做分词搜索与相关性排序；并理解 ES 与数据库的分工（ES 搜、DB 存真值）。

## 📋 小白前置
需先懂 DB3（查询）、DB8（索引概念，对比 B+树）、DB15（NoSQL 取舍）。

## 🟢 部分一·最浅层（生活比喻）
数据库像"按编号排的档案柜"，你查"含'安全'的文档"得一本本翻（全表扫 LIKE）。ES 像书末的"索引页"：它把每个词（"安全""算法""漏洞"）列出来，后面跟着"哪些页出现过这个词"。你查"安全"，直接翻索引页就知道是第 3、7、12 页——这就是**倒排索引**：从"词"反查"文档"。分词就像把一句话切成一个个词（"白帽黑客"→"白帽""黑客"），中文还要用专门分词器。

## 🟡 部分二·动手层（Docker 起 ES + 搜索）
ES 较重，给最小启动（开发模式，单节点）：
```bash
docker run --name es -p 9200:9200 -e "discovery.type=single-node" -e "xpack.security.enabled=false" -d elasticsearch:8.13.4
# 等约30秒
curl -X GET "localhost:9200/_cluster/health?pretty"
```
用 curl 建索引并搜索：
```bash
# 建索引（含中文分词可用 ik，这里用默认 standard 演示）
curl -X PUT "localhost:9200/articles" -H "Content-Type: application/json" -d '{
  "mappings": {"properties": {"title":{"type":"text"}, "body":{"type":"text"}}}
}'
# 插入两篇
curl -X POST "localhost:9200/articles/_doc/1" -H "Content-Type: application/json" -d '{"title":"白帽安全入门","body":"学习 SQL 注入防护与最小权限"}'
curl -X POST "localhost:9200/articles/_doc/2" -H "Content-Type: application/json" -d '{"title":"算法基础","body":"排序与动态规划"}'
# 搜索"安全"
curl -X GET "localhost:9200/articles/_search?pretty" -H "Content-Type: application/json" -d '{"query":{"match":{"body":"安全"}}}'
```
预期返回第 1 篇（相关度评分 `_score`）。

### 🧪 实战 Lab（DB21）
① 起 ES 并插入两篇文档、搜索"安全"成功截图（或 curl 输出）。② 解释倒排索引：列出"安全""算法""SQL"三个词各自指向哪些文档ID（根据你插入的内容）。③ 对比：同样搜索若用 MySQL `WHERE body LIKE '%安全%'`，为什么大数据量下慢？（全表逐行扫描 + 不能利用 B+树索引，因前导通配）。把实验与对比发我。

## 🔵 部分三·原理层（底层）
- **倒排索引**：文档入库时先**分词（analysis）**成 term，构建 `term → [文档ID, 词频, 位置]` 的字典（用 FST 压缩）+ 倒排列表。查询时直接查字典拿文档集合，再做布尔/向量算分。
- **相关性（BM25）**：ES 默认用 BM25 算法给每篇文档打分——词频越高、文档越短、词越罕见，分越高，按 `_score` 降序返回。
- **近实时（NRT）**：写入先进内存 buffer，refresh（默认1秒）生成 segment 可被搜；flush 落盘。所以 ES 不是"写入立即可查"，而是近实时。
- **分布式**：索引分成多个 shard（分片，类似 DB14），副本提升可用与读吞吐。

## 🟣 部分四·深挖层（为什么、延伸）
为什么数据库不适合全文搜？B+树索引按"整值/前缀"组织，对"词在文本中间"无能为力，LIKE '%词%' 必全表扫。专门搜索引擎用倒排索引把"词→文档"显式化，再配合分词、同义词、停用词、向量召回（kNN）做语义搜。顶级工程师把 ES 当"查询加速层"：业务真值存 MySQL/PG，变更通过 CDC（DB23）同步到 ES 供搜索/聚合。ES 也是日志分析（ELK，OPS40）的核心。

## 🔴 部分五·顶级视角
搜索/检索是"信息获取"的基石，顶级工程师在电商（商品搜）、内容（文章/视频搜）、日志（全文检索）、安全（日志关联分析）处处用 ES。它和向量数据库（DAT34/RAG）正在融合：关键词召回 + 向量语义召回混合排序（hybrid search）。理解倒排索引也帮你看懂 Lucene（ES 内核）、甚至 Google 的网页索引本质。

## 🟠 部分六·安全/合规种子
白帽要点：① ES 默认无密码曾导致海量数据泄露（著名事件）——务必开安全、设强密码、绑内网。② 索引内容可能含 PII，需访问控制 + 脱敏，遵守《个人信息保护法》。③ 搜索查询本身不泄露，但"可搜到不该搜的信息"是权限设计问题（按角色限制索引可见性）。④ 只连你授权拥有的集群。

## ✅ 验收
交：① ES 起 + 搜索"安全"结果截图/输出；② 三个词的倒排映射表；③ MySQL LIKE vs ES 倒排对比。

## ⚠️ 常见坑
- ES 8 默认开安全（需密码），上面用 `xpack.security.enabled=false` 仅为本地演示，生产务必开。
- 中文分词默认 standard 是按字切，效果差；生产用 IK 分词器插件。
- ES 是近实时，刚写入未必立刻搜到（等 refresh）。
- 端口 9200 是 REST，别和 9300（ transport，旧版）混。
- 手疼复制，别硬敲。

## ➡️ 下一步
DB22 · 数据仓库与 OLAP（星型 / 雪花 / 立方体）——理解"为分析而生的数据库"和"为事务而生的数据库"的根本区别，以及维度建模。

---

# DB22 · 数据仓库与 OLAP（星型 / 雪花 / 立方体）

## 🎯 目标
学完这节，你能区分 **OLTP（事务）vs OLAP（分析）**；理解**星型模型 / 雪花模型**；用 SQL 做多维聚合（GROUP BY 多维度）；并理解"数据立方体"和"列式存储"为何适合分析。

## 📋 小白前置
需先懂 DB2（范式，这里恰恰要反范式）、DB3（GROUP BY/聚合）、DB21（可选）。

## 🟢 部分一·最浅层（生活比喻）
OLTP 是"收银台"——每秒处理一笔笔下单（增删改，快、小、准）。OLAP 是"老板的年度报表"——把一年所有订单按地区、月份、品类汇总，算总营收（查得多、改得少、扫海量）。为做报表，分析师故意把数据**反范式**成一张"大宽表"或"星型"：中间一张"销售事实表"，周围围着"时间/地区/商品"几张小维度表——像星星，所以叫星型模型。

## 🟡 部分二·动手层（用 SQLite 模拟星型 + 多维聚合）
```sql
-- 维度表
CREATE TABLE dim_date(日期ID INT, 年 INT, 月 INT);
CREATE TABLE dim_region(地区ID INT, 地区 TEXT);
CREATE TABLE dim_product(产品ID INT, 品类 TEXT);
-- 事实表（反范式：只存ID+度量）
CREATE TABLE fact_sales(日期ID INT, 地区ID INT, 产品ID INT, 金额 REAL, 数量 INT);
INSERT INTO dim_date VALUES(1,2024,1),(2,2024,2);
INSERT INTO dim_region VALUES(1,'北京'),(2,'上海');
INSERT INTO dim_product VALUES(1,'手机'),(2,'笔记本');
INSERT INTO fact_sales VALUES
 (1,1,1,5000,1),(1,1,2,8000,1),(1,2,1,5000,1),
 (2,1,1,5200,1),(2,2,2,8100,1),(2,2,1,5000,1);

-- 多维聚合：按 年-月-地区 汇总金额
SELECT d.年,d.月,r.地区, SUM(f.金额) 总销售额, SUM(f.数量) 总量
FROM fact_sales f
JOIN dim_date d ON f.日期ID=d.日期ID
JOIN dim_region r ON f.地区ID=r.地区ID
GROUP BY d.年,d.月,r.地区
ORDER BY d.月,r.地区;
```
预期：北京1月13000、上海1月5000、北京2月5200、上海2月13100。

### 🧪 实战 Lab（DB22）
① 在上面星型模型上，加一个"按 品类 汇总年销售额"的查询（`JOIN dim_product` + `GROUP BY 品类`），写出结果与 SQL。② 解释"雪花模型"：若 dim_region 再拆出"国家表"（地区→国家），维度表也规范化，就成雪花——它省空间但 JOIN 多、查询慢，对比星型取舍。③ 一句话说明为什么分析库多用**列式存储**（同一列连续存，聚合只扫需要的列，压缩率高）。把实验与说明发我。

## 🔵 部分三·原理层（底层）
- **OLTP vs OLAP**：OLTP 面向高并发小事务（行存、B+树、强一致）；OLAP 面向大批量只读分析（列存、扫描、弱一致可接受）。二者负载特征完全不同，所以分两套系统（"湖仓分离/一体化"是趋势）。
- **星型 vs 雪花**：星型维度表冗余（地区名直接放 dim_region），查询 JOIN 少、快；雪花把维度再规范化，省空间但多 JOIN。数据仓库普遍选星型（用空间换查询速度）。
- **列式存储（Columnar）**：ClickHouse/Vertica/Parquet 把同一列的值连续存放——做 `SUM(金额)` 只顺序读"金额"这一列，且同列类型相同压缩极好（如字典编码）。这是 OLAP 快的关键。

## 🟣 部分四·深挖层（为什么、延伸）
维度建模由 Ralph Kimball 提出，是数据仓库的事实标准。数据立方体（Cube）是把多个维度预聚合的"超立方体"，可沿任意维度切片（slice）、旋转（pivot）。现代演化：**湖仓一体（Lakehouse）**——用对象存储（S3）存原始数据 + 表格式（Iceberg/Delta/Hudi）支持 ACID 与 SQL 分析（DAT9）。顶级工程师在 BI（DAT50）、风控报表、用户行为分析里天天用 OLAP。

## 🔴 部分五·顶级视角
OLAP 是"数据驱动决策"的引擎。顶级工程师设计**指标体系**（北极星指标、维度字典）、**宽表建模**、**物化视图预聚合**，让报表秒开。他们也理解"实时 OLAP"（ClickHouse/Druid 支撑实时大屏）与"离线数仓"（Hive/Spark 跑 T+1）的分工。维度建模思维甚至适用于产品分析（PD12 数据驱动）。

## 🟠 部分六·安全/合规种子
白帽要点：① 数仓常汇聚全公司数据，是"敏感信息富矿"，访问控制要极严（按角色/列级脱敏）。② 聚合结果也可能"再识别"个人（如"某地区唯一患者"），需 k-匿名/差分隐私保护，遵守《个人信息保护法》。③ 只分析你授权拥有的数据。

## ✅ 验收
交：① 星型模型多维聚合 SQL 与结果截图；② 按品类年销售额查询与结果；③ 星型 vs 雪花、列存优势说明。

## ⚠️ 常见坑
- 事实表放"度量+外键"，维度表放"描述属性"——别把地区名塞进事实表（那成了宽表，非规范星型）。
- OLAP 库（如 ClickHouse）很多不支持事务/UPDATE，语法和 MySQL 不同，别混。
- 聚合忘 GROUP BY 会报"非聚合列没分组"错误。
- 手疼复制，别硬敲。

## ➡️ 下一步
DB23 · ETL 与数据管道（抽取 / 转换 / 加载）——学会把数据从 A 搬到 B 并清洗，理解批处理 vs 流处理，认识 Airflow/dbt/Kafka 这类编排工具的角色。

---

# DB23 · ETL 与数据管道（抽取 / 转换 / 加载）

## 🎯 目标
学完这节，你能说清 ETL（Extract-Transform-Load）三阶段；用 Python 写一个最小 ETL（从 SQLite 抽数、清洗、写回）；理解**批处理 vs 流处理**；知道 Airflow/dbt/Kafka 在管道里的位置；并理解数据质量与合规。

## 📋 小白前置
需先懂 DB3（SQL/读写）、DB22（数仓/星型）、方向1a PY 基础。

## 🟢 部分一·最浅层（生活比喻）
ETL 像"食材供应链"：从农场**抽取（E）**蔬菜 → 在工厂**清洗切割加工（T）**（去掉烂叶、统一切法）→ **装载（L）**进超市冷柜（数据仓库）。数据管道就是把"原始杂乱数据"变成"干净可用数据"的流水线。批处理像"每晚统一送一趟货"；流处理像"传送带实时一件件过"。

## 🟡 部分二·动手层（Python 最小 ETL）
假设源表 `raw_orders`（脏：金额有负、有空），清洗后入 `clean_orders`：
```python
import sqlite3
src = sqlite3.connect("raw.db")      # 源
dst = sqlite3.connect("clean.db")    # 目标数仓
src.execute("""CREATE TABLE IF NOT EXISTS raw_orders(
  id INT, customer TEXT, amount REAL, note TEXT)""")
src.execute("INSERT INTO raw_orders VALUES(1,'A',100,'ok'),(2,'B',-5,'退款'),(3,'C',NULL,'缺失'),(4,'D',200,'ok')")

# E: 抽取
rows = src.execute("SELECT id,customer,amount FROM raw_orders").fetchall()
# T: 转换（清洗：去掉负/空，标准化）
clean = [(i,c,a) for (i,c,a) in rows if a is not None and a > 0]
# L: 加载
dst.execute("""CREATE TABLE IF NOT EXISTS clean_orders(
  id INT, customer TEXT, amount REAL)""")
dst.executemany("INSERT INTO clean_orders VALUES(?,?,?)", clean)
dst.commit()
print("清洗后:", dst.execute("SELECT * FROM clean_orders").fetchall())
# 预期只留 id=1(A,100) 和 id=4(D,200)
```
运行 `python etl_demo.py`。

### 🧪 实战 Lab（DB23）
① 跑上面 ETL，验证脏数据（负/空）被过滤。② 用 SQL 直接在 SQLite 里做等价 ETL（`INSERT INTO clean SELECT ... WHERE amount>0 AND amount IS NOT NULL`），体会"用 SQL 做 T"更简洁。③ 说明批 vs 流：若订单是实时产生，用 `Kafka` 流 + 消费者实时清洗叫流处理；每晚跑一次脚本叫批处理。写 2 条各自适用场景。把实验与说明发我。

## 🔵 部分三·原理层（底层）
- **ETL vs ELT**：传统 ETL 在中间层转换再入仓；现代云数仓（Snowflake/BigQuery）常用 **ELT**——先原样 Load 进仓（廉价存储），再用 SQL/dbt 在仓内 Transform，利用仓的计算力。
- **调度**：Airflow 用 **DAG** 编排任务（每天几点跑、依赖谁先跑），失败重试、告警（OPS42/CMP）。dbt 专门做"仓内 SQL 转换 + 测试 + 文档"。
- **流处理**：Kafka 是"分布式日志/消息总线"，生产者写、消费者读；Flink/Spark Streaming 在其上做实时聚合（DAT8）。CDC（Change Data Capture）用 binlog（DB13）把库变更实时同步到数仓/ES（DB21）。

## 🟣 部分四·深挖层（为什么、延伸）
为什么需要管道而非"直接查源库"？因为源库（OLTP）扛不住分析查询、且数据散落多系统需整合。管道让"原始→可信"有可靠路径。顶级工程师关注**数据质量**（Schema 校验、空值率监控、血缘 lineage）、**幂等**（重跑不产生重复）、** Exactly-Once**（流处理语义，DB24 思想）、**回压（backpressure）**。数据管道是数据工程的主动脉（DAT6）。

## 🔴 部分五·顶级视角
管道思维贯通一切"数据搬运"：从日志采集（Filebeat）→ 消息（Kafka）→ 处理（Flink）→ 存储（数仓）→ 服务（BI/特征，DAT11）。顶级工程师把管道当"产品"做 SLA、监控、告警。理解 ETL 也帮你看懂 ML 特征管道（DAT6/FEAST）、RAG 知识库构建（DB33 切分/向量化）。

## 🟠 部分六·安全/合规种子
白帽要点：① 管道搬运的是**数据本体**，极易在"中间环节"泄露——传输加密（TLS）、落盘加密、最小权限账号。② 合规红线：未授权爬取/汇聚他人个人信息做管道 = 违法；处理 PII 须有合法性基础、告知同意、最小必要（《个人信息保护法》）。③ 管道里的 PII 要脱敏/假名化后再入仓分析。④ 只对你授权拥有的数据建管道。

## ✅ 验收
交：① Python ETL 运行结果（脏数据被过滤）；② SQL 等价 ETL 语句；③ 批 vs 流对比与应用场景。

## ⚠️ 常见坑
- ETL 没做幂等，重跑会重复插入——用 `INSERT ... ON CONFLICT DO NOTHING` 或先删后插。
- 清洗逻辑 bug 会"静默丢数据"，务必对输入输出条数做断言校验。
- 流处理"Exactly-Once"很难，很多系统只保证 At-Least-Once，需业务去重。
- 手疼复制，别硬敲。

## ➡️ 下一步
DB24 · 分布式事务（2PC / 3PC / Raft / Paxos）——当一笔操作要跨多个数据库/服务，如何保证"要么全成要么全败"，以及共识算法如何让多机达成一致。

---

# DB24 · 分布式事务与共识（2PC / Raft / Paxos）

## 🎯 目标
学完这节，你能说清**为什么单库事务不够用**（跨库/跨服务）；理解 **2PC（两阶段提交）**流程与阻塞缺陷；了解 3PC 改进；理解 **Raft/Paxos** 这类共识算法如何让多机"就某件事达成一致"（这是分布式系统的地基）。

## 📋 小白前置
需先懂 DB6（ACID/事务）、DB13（复制）、DB14（分库分表）。

## 🟢 部分一·最浅层（生活比喻）
单库事务像"一个人同时做两件事，要么都做要么都不做"。分布式事务像"两个人（两个数据库/两个服务）合伙干一件事"——A 扣钱、B 加钱，**必须同时成功或同时失败**。2PC 像"主持人先问大家'准备好了吗'（投票阶段），全说好才喊'执行'（提交阶段）"，只要有一个说不行，全体撤销。但坏处：主持人/某人不回话，大家就得一直等（阻塞）。Raft 像"选个班长，班长说啥大家照做；班长挂了再选新的"——保证大家账本一致。

## 🟡 部分二·动手层（用 SQLite 模拟 2PC 思路）
真实 2PC 需协调器，这里用两段提交模拟"跨两库"的 prepare/commit：
```python
import sqlite3
dbA = sqlite3.connect("bankA.db"); dbB = sqlite3.connect("bankB.db")
for db in (dbA,dbB): db.execute("CREATE TABLE IF NOT EXISTS acc(id TEXT,bal REAL)")
dbA.execute("INSERT INTO acc VALUES('A',1000)"); dbB.execute("INSERT INTO acc VALUES('B',1000)")
dbA.commit(); dbB.commit()

def transfer():
    try:
        # 阶段1：prepare（各库先改，但先不最终提交）
        dbA.execute("UPDATE acc SET bal=bal-100 WHERE id='A'")
        dbB.execute("UPDATE acc SET bal=bal+100 WHERE id='B'")
        # 模拟协调器决定提交
        dbA.commit(); dbB.commit()      # 阶段2：commit
        print("转账成功")
    except Exception as e:
        dbA.rollback(); dbB.rollback()  # 任一失败全部回滚
        print("回滚:", e)
transfer()
```
> 真实 2PC 还要"prepare 日志持久化 + 协调器崩溃恢复"，这里只演示"两库要么都提交要么都回滚"的意图。

### 🧪 实战 Lab（DB24）
① 跑上面模拟，验证两库余额正确变化。② 用文字画出 2PC 两阶段时序：协调器→各参与者"CanCommit?"→全 YES→"DoCommit"；任一 NO→"DoAbort"。③ 说明 2PC 的"阻塞"缺陷：若协调器在"发完 DoCommit 但部分参与者已提交、自己崩溃"，恢复后参与者不知道该提交还是回滚，只能等——这体现了为什么分布式事务难。把实验与图示发我。

## 🔵 部分三·原理层（底层）
- **2PC（Two-Phase Commit）**：Phase1 协调器发 `prepare`，各参与者写 undo/redo 并回 `yes/no`；Phase2 若全 yes 发 `commit` 否则 `abort`。强一致但**同步阻塞**（锁资源到提交）、**单点**（协调器挂了卡住）、**数据不一致风险**（提交阶段部分失败）。
- **3PC**：加 `pre-commit` 阶段 + 超时机制，缓解阻塞但网络分区下仍有问题，实践中少用。
- **共识算法（Paxos/Raft）**：解决"多机就一个值/日志序列达成一致"。Raft 把共识拆成：Leader 选举、日志复制、安全性；通过"多数派（quorum）"保证只要多数存活就能达成一致，是 etcd/Consul/TiDB 的底座。共识 ≠ 事务，但**分布式事务依赖共识来协调**。

## 🟣 部分四·深挖层（为什么、延伸）
为什么分布式事务这么难？因为**网络会分区、机器会宕机、时钟不可靠**——这就是 DB25 的 CAP/FLP 不可能定理（异步系统里一致性+可用性+容错不可兼得）。工程上所以演化出：① **TCC（Try-Confirm-Cancel）** 业务补偿；② **Saga** 长事务拆成一串本地事务+补偿；③ **消息队列最终一致**（本地事务+发消息，消费方幂等）。顶级工程师共识：**能避免分布式事务就避免**，用"单一写库 + 异步同步"把分布式事务降级为最终一致。

## 🔴 部分五·顶级视角
共识算法是分布式系统的"宪法"。顶级工程师理解：Raft 让 etcd 存集群元数据、让 TiDB 协调分片、让 Kafka KRaft 管分区——**几乎所有分布式数据库/协调服务底层都是某种共识**。Raft 的可理解性设计（论文即实现）也是顶级工程文档范本。理解它，你才真正懂"多机如何像一个整体"。

## 🟠 部分六·安全/合规种子
白帽要点：① 分布式事务跨多系统，**攻击面更大**——协调器/消息总线要鉴权加密，防中间人篡改提交指令。② 补偿事务（Saga）里的"反向操作"若被恶意触发可能造成资金差错，需审计与权限。③ 只在你授权系统内做分布式协调实验；绝不针对非授权服务构造两阶段/消息篡改。

## ✅ 验收
交：① 2PC 模拟运行结果（两库余额正确）；② 2PC 两阶段时序图（文字/画图）；③ 阻塞缺陷说明。

## ⚠️ 常见坑
- 模拟里 `commit` 两次若中途异常，前面的已提交无法回滚——真实 2PC 靠 prepare 日志保证。
- 不要把分布式事务当银弹，多数场景用"最终一致 + 补偿"更稳。
- 共识算法里"多数派"指 > N/2，不是"全部"。
- 手疼复制，别硬敲。

## ➡️ 下一步
DB25 · CAP 与一致性模型（线性 / 因果 / 最终）——深入"一致性到底有几种"，从强到弱理解线性一致、因果一致、最终一致，以及它们和 CAP 的关系。

---

# DB25 · CAP 与一致性模型（线性 / 因果 / 最终）

## 🎯 目标
学完这节，你能区分**强一致 ↔ 弱一致**光谱上的四种典型模型：线性一致（Linearizable）、顺序一致（Sequential）、因果一致（Causal）、最终一致（Eventual）；理解它们和 CAP 的关系；并能在设计系统时选对一致性级别。

## 📋 小白前置
需先懂 DB15（CAP 初识）、DB13（复制/延迟）、DB24（共识）。

## 🟢 部分一·最浅层（生活比喻）
一致性就是"大家看到的世界是不是同一个"。**线性一致**像"全班看同一块黑板，老师写上去瞬间所有人都看到最新字"——最严格。**因果一致**像"你先说'A 赢了'，我才接着说'所以颁奖给 A'"——有因果关系的事件顺序必须保住，但无关事件可以乱。**最终一致**像"传话游戏，刚开始各说各的，过一会儿大家慢慢都对齐了"——不保证即时，但保证最后一致。越弱越能扛分区/高可用（呼应 CAP）。

## 🟡 部分二·动手层（用 Redis 主从体会"最终一致"）
起 Redis（DB16 命令），主写从读体会延迟：
```bash
docker exec -it redis redis-cli SET x 1          # 主
docker exec -it redis redis-cli GET x            # 从（可能短暂未同步）
```
概念验证：在异步复制（DB13）下，主库刚写 x=1，从库立刻读可能还是旧值——这就是"非强一致"。要强一致得用 `WAIT` 命令等副本确认：
```bash
docker exec -it redis redis-cli SET x 2
docker exec -it redis redis-cli WAIT 1 1000       # 等至少1个副本确认，超时1000ms
```
`WAIT` 返回已确认的副本数；返回 ≥1 说明已同步到从库，读从库更安全。

### 🧪 实战 Lab（DB25）
① 用文字画出"线性一致 vs 最终一致"的时序图：两个客户端 A、B，A 写 x=1，在线性一致下 B 之后任何读都见 1；在最终一致下 B 可能先读到旧值。② 列表说明四种一致性（线性/顺序/因果/最终）各自"保证什么、不保证什么、典型系统"。③ 解释 CAP：为什么说"分区时只能在 C 和 A 选"——给出银行（选 C）和社交点赞（选 A）的例子。把实验与表格发我。

## 🔵 部分三·原理层（底层）
- **线性一致（Linearizability）**：单个对象的操作看起来像"按真实时间顺序原子发生"，读必见最新写。最难实现（需共识/全局时钟），但最符合直觉。ZooKeeper/etcd 的某些原语可达。
- **顺序一致**：所有进程看到的操作顺序一致（满足一个全局序），但不必是真实时间序。
- **因果一致**：有因果关系的操作保序（A 发消息给 B，B 回复——这两件事顺序全世界一致），无因果的可乱序。比顺序一致弱、比最终一致强。
- **最终一致**：无新写后，经过一段时间所有副本收敛到相同值。最弱但最可用（Dynamo/Cassandra/Redis 复制默认倾向）。

一致性强度与**延迟/可用性**此消彼长：越强越慢、越怕分区；越弱越快、越能扛分区。

## 🟣 部分四·深挖层（为什么、延伸）
为什么没有"完美一致又永远可用"？因为**网络分区不可避免**（CAP），分区时要么停服保一致（CP）要么继续服务但可能不一致（AP）。FLP 不可能定理进一步证明：异步系统中即使一个进程崩溃，也无法在有限时间保证达成共识——所以共识算法都是"概率/超时"意义上的。顶级工程师用**可调一致性**：读关键数据用强一致/法定读（quorum read），读资料用最终一致，按数据重要性分级。

## 🔴 部分五·顶级视角
一致性模型是分布式系统的"坐标系"。顶级工程师在设计一个功能时，第一反应是"这块数据需要多强的一致？"——余额必须线性一致（不然资损），头像昵称最终一致就够了（用户无感）。他们也用一致性做**正确性推理**：并发 bug 往往源于"以为强一致实际最终一致"。理解这些，是从"会用数据库"到"设计可靠分布式系统"的关键跃迁。

## 🟠 部分六·安全/合规种子
白帽要点：① 弱一致可能让"删除/更正"延迟生效，影响《个人信息保护法》的"被遗忘权/更正权"时效——设计合规流程时要显式等待一致后再确认完成。② 一致性协议（Raft/Paxos）的通信要加密认证，防止伪造投票节点破坏共识（这属分布式安全，只分析不实践攻击）。③ 只在授权环境做一致性实验。

## ✅ 验收
交：① 线性 vs 最终一致时序图；② 四种一致性对比表；③ CAP 银行/社交例子。

## ⚠️ 常见坑
- "一致性"在 CAP 里指"线性/强一致"，在 ACID 里指"约束不被破坏"——两处 C 不同，别混。
- 最终一致不是"不一致"，是"暂时不一致，最终会一致"，需业务能容忍窗口。
- 强一致成本高，别盲目全用强一致导致系统不可用。
- 手疼复制，别硬敲。

## ➡️ 下一步
DB26 · ORM（SQLAlchemy / 阻抗失配 / N+1）——理解"对象和表的鸿沟"，学会用 ORM 优雅映射，并避开 N+1 查询、懒加载等经典坑。

---

# DB26 · ORM（阻抗失配 / N+1 / SQLAlchemy）

## 🎯 目标
学完这节，你能说清**阻抗失配（Impedance Mismatch）**——为什么"面向对象"和"关系表"天然不合；用 Python **SQLAlchemy** 写出"类 ↔ 表"的映射与增删改查；识别并解决 **N+1 查询**陷阱；理解 ORM 的利弊与何时该手写 SQL。

## 📋 小白前置
需先懂 DB3（SQL）、方向1a PY（PY11 面向对象）、DB12（MySQL 可选）。

## 🟢 部分一·最浅层（生活比喻）
你用 Python 写程序，脑子里是"对象"（一个学生对象，里面有姓名、年龄、选的课列表）。但数据库里是"表"（学生表一行、课程表一行、选课表一行）。把"对象"塞进"表"要拆开重组，这中间的别扭就是**阻抗失配**。ORM 像个"翻译官"：你告诉翻译官"存这个学生对象"，它自动翻译成 INSERT 语句；你问"给我所有学生"，它把查回来的行自动拼回对象。省事，但翻译官有时很笨——比如你要 100 个学生的选课，它可能傻傻地"先查100学生、再每人查一次选课"，发了 101 次查询（N+1）。

## 🟡 部分二·动手层（SQLAlchemy 实战）
先装：`pip install sqlalchemy`（在 Git Bash 跑）。新建 `orm_demo.py`：
```python
from sqlalchemy import create_engine, Column, Integer, String, ForeignKey
from sqlalchemy.orm import declarative_base, relationship, Session

engine = create_engine("sqlite:///orm.db")   # 也可用 mysql+pymysql://...
Base = declarative_base()

class Student(Base):
    __tablename__ = "student"
    id = Column(Integer, primary_key=True)
    name = Column(String)
    # 一对多：一个学生多门选课（关系，不存表里）
    enrollments = relationship("Enrollment", back_populates="student")

class Enrollment(Base):
    __tablename__ = "enrollment"
    id = Column(Integer, primary_key=True)
    sid = Column(Integer, ForeignKey("student.id"))
    course = Column(String)
    student = relationship("Student", back_populates="enrollments")

Base.metadata.create_all(engine)

with Session(engine) as s:
    s.add(Student(name="小明", enrollments=[Enrollment(course="数据库"), Enrollment(course="安全")]))
    s.add(Student(name="小红", enrollments=[Enrollment(course="算法")]))
    s.commit()

    # 查对象（ORM 自动拼回）
    for stu in s.query(Student).all():
        print(stu.name, [e.course for e in stu.enrollments])   # 这里触发 N+1!
```
运行 `python orm_demo.py`。

### 🧪 实战 Lab（DB26）
① 跑上面程序，打印出学生及其课程。② 识别 N+1：循环里每次访问 `stu.enrollments` 都发一条 SQL（懒加载）。改正：用 **eager loading** `s.query(Student).options(joinedload(Student.enrollments)).all()`（需 `from sqlalchemy.orm import joinedload`），让一次 JOIN 搞定。③ 说明何时不该用 ORM：超复杂报表/手写优化 SQL（用 `text()` 原生 SQL）。把实验与修正发我。

## 🔵 部分三·原理层（底层）
- **映射（Mapping）**：ORM 通过"元数据"（类属性 ↔ 列）在运行时生成 SQL。你操作对象，ORM 拦截并转成 INSERT/SELECT。
- **会话（Session）**：ORM 的"工作单元（Unit of Work）"——你改对象，Session 跟踪变更，commit 时批量生成 SQL 并包成事务（DB6）。
- **懒加载（Lazy Load）**：访问 `stu.enrollments` 时才发 SQL 查——方便但导致 N+1（N 个父对象各触发 1 次子查询，共 N+1 次）。
- **N+1 解决**：eager loading（`joinedload` 用 LEFT JOIN 一次取、`subqueryload` 用子查询）、或显式 `contains_eager`。根本是"用一条 JOIN/IN 查询代替 N 条"。

## 🟣 部分四·深挖层（为什么、延伸）
阻抗失配是"范式 vs 对象"的持久张力：关系用"外键+Join"表达联系，对象用"引用/指针"；继承、多态、集合在表里都要额外建模（单表继承/类表继承/具体表继承）。ORM 简化了 80% 的 CRUD，但**抽象泄漏**（Leaky Abstraction）：复杂查询、性能优化终要落回 SQL 与执行计划（DB9）。顶级工程师态度：用 ORM 提效，但在性能关键路径**亲手写 SQL 并用 EXPLAIN 验证**，不让 ORM 替你做性能决策。

## 🔴 部分五·顶级视角
ORM 是"消除样板代码"的工程典范，几乎所有后端框架内置（Django ORM、Hibernate、TypeORM、GORM）。但它也是"抽象误用重灾区"——N+1、误用事务、生成慢 SQL 是线上事故的常客。顶级工程师既会用 ORM 提速，也懂在**边界处下沉到 SQL/存储过程/专用查询层**。理解 ORM 也帮你读懂"活动记录 vs 数据映射器"两种模式（DB 访问层设计）。

## 🟠 部分六·安全/合规种子
白帽要点：① ORM 的**参数化**天然防 SQL 注入（这是它最大的安全红利，DB28）——但如果你用 `text("SELECT ... " + 用户输入)` 拼接，防护瞬间失效，务必用参数绑定。② ORM 自动生成的查询也要审查是否越权（如 `filter_by(user_id=当前用户)` 漏写会泄露他人数据）。③ 只对你授权拥有的库做映射实验。

## ✅ 验收
交：① ORM 程序运行结果（学生+课程打印）；② N+1 识别与 joinedload 修正代码与结果；③ 何时手写 SQL 的说明。

## ⚠️ 常见坑
- ORM 不是"不用学 SQL"，恰恰相反，不懂 SQL/执行计划会被 ORM 坑。
- 忘了 `Base.metadata.create_all(engine)` 表不会建。
- Session 用完要关（用 `with` 最稳），否则连接泄漏。
- 懒加载在 Session 关闭后再访问会报错（detached 实例）。
- 手疼复制，别硬敲。

## ➡️ 下一步
DB27 · 备份恢复与容灾（RPO / RTO / 冷备热备）——理解"数据丢了怎么办"，掌握全量/增量备份、binlog 时间点恢复、3-2-1 备份原则与演练。

---

# DB27 · 备份恢复与容灾（RPO / RTO / 冷备热备）

## 🎯 目标
学完这节，你能定义 **RPO（恢复点目标）** 与 **RTO（恢复时间目标）**；用 SQLite/MySQL 做全量+增量（binlog）备份与恢复演练；理解 3-2-1 备份原则；明白"复制≠备份"；并设计一个最小容灾方案。

## 📋 小白前置
需先懂 DB12（MySQL/binlog）、DB10（SQLite 文件）、DB13（复制）。

## 🟢 部分一·最浅层（生活比喻）
备份就像"重要文件的复印件放在别处"。**RPO** 是"最多允许丢多久的数据"——比如 RPO=5 分钟，意思是出事最多回到 5 分钟前，更早的没了。**RTO** 是"从出事到恢复能用，最多花多久"——RTO=1 小时，意思是哪怕备份在，重装+恢复也要 1 小时内搞定。冷备像"每晚关门后复制一份"（停机/低峰）；热备像"边营业边实时复制"（不影响服务）。关键：**复制不是备份**——主库被误删，从库跟着删（DB13 讲过），所以必须有"独立、可回退"的备份。

## 🟡 部分二·动手层（SQLite 文件备份 + MySQL 时间点恢复）
**SQLite 冷备（最简单）：**
```bash
# 直接复制文件即可（无写时）
cp /c/Users/qq/WorkBuddy/2026-08-26-13-18-54/全领域教案/orm.db /c/.../orm_backup.db
# 或用 SQL 导出
sqlite3 orm.db .dump > orm_dump.sql
# 恢复：
sqlite3 orm_restore.db < orm_dump.sql
```
**MySQL 全量 + binlog 增量（时间点恢复）：**
```bash
# 全量（用 mysqldump，容器里）
docker exec mysql mysqldump -uroot -pzeze123 --single-transaction bank > bank_full.sql
# 之后发生的改动都在 binlog
# 误删后恢复：先恢复全量，再回放 binlog 到误删前
docker exec mysql mysqlbinlog /var/lib/mysql/binlog.000001 | docker exec -i mysql mysql -uroot -pzeze123 bank
```
> 具体 binlog 位点恢复需 `mysqlbinlog --stop-position=xxx`，生产按时间点 ` --stop-datetime="2024-.."` 精确回放。

### 🧪 实战 Lab（DB27）
① 对 `orm.db` 做 `.dump` 导出再导入新库，验证数据一致（对比行数）。② 定义你自己的 RPO/RTO：比如"学习用 demo 库，RPO=1天，RTO=10分钟"，说明这意味着什么备份频率。③ 写一条"3-2-1 原则"应用：3 份拷贝、2 种介质、1 份异地——给 demo.db 设计符合的备份方案（如本地+U盘+云盘）。把实验与方案发我。

## 🔵 部分三·原理层（底层）
- **全量备份**：某时刻完整复制所有数据。恢复简单但体积大、频率低。
- **增量/差异备份**：增量只备份"上次备份后变化"（依赖上一次，恢复链长）；差异备份备份"基于某全量后的所有变化"（恢复只需全量+最新差异）。MySQL 的 binlog 是"连续增量流"，可做**时间点恢复（PITR）**——回放到任意秒。
- **热备 vs 冷备**：热备用快照/一致性读（如 `--single-transaction` 在事务里一致性导出），不锁表；冷备停服复制，最稳但不可用。
- **RPO/RTO** 是 SLA 指标，由业务价值决定，反过来约束备份策略与预算。

## 🟣 部分四·深挖层（为什么、延伸）
为什么"复制≠备份"？因为复制解决"高可用/读扩展"，但**所有写（含 DELETE/DROP）都会被复制**——它防"机器挂"，不防"人删库/勒索加密/逻辑错误"。真正备份要**与源隔离、不可变、可回退**（如对象存储的 WORM/版本化）。顶级实践：**备份不演练等于没备份**——定期做恢复演练（restore drill），验证 RTO 真实可达。延伸：PITR、延迟副本（delayed replica，故意落后几小时防误删）、异地多活容灾。

## 🔴 部分五·顶级视角
备份恢复是"最后的底线"，顶级工程师把它当**一等公民**：自动化（脚本+调度）、监控（备份成功才安心）、演练（定期恢复测试）、加密（备份也含 PII 要加密）。在云上，RDS 自动备份、快照、跨区复制是产品化容灾。SRE（OPS36+）把 RPO/RTO 写进 SLO，事故演练（混沌工程 OPS43）包含"杀库恢复"。

## 🟠 部分六·安全/合规种子
白帽要点：① 备份文件含**全部数据（含 PII）**，是攻击者的金矿——必须加密存储、访问控制、传输 TLS，遵守《个人信息保护法》的"存储安全"与留存期限。② 备份泄露 = 数据泄露，异地备份也要合规（跨境需评估）。③ 恢复演练在自己的授权环境做；绝不备份/恢复任何非你所有的数据库。④ 勒索软件（方向7 SEC63，理论）专门盯备份，故备份要不可变/离线。

## ✅ 验收
交：① SQLite dump 导出+恢复验证（行数一致）；② 你的 RPO/RTO 定义与含义；③ 3-2-1 备份方案（demo.db）。

## ⚠️ 常见坑
- 以为主从复制能防误删——不能，从库会被同步删，必须独立备份。
- 备份了从不恢复测试，真出事发现备份损坏/不全。
- mysqldump 不加 `--single-transaction` 可能锁表（InnoDB 用事务一致性读避免）。
- 备份文件明文含 PII，放公网/弱权限目录 = 泄露。
- 手疼复制，别硬敲。

## ➡️ 下一步
DB28 · 数据库安全（注入防护 / 最小权限 / 加密）——方向5 的收官，把"白帽红线"落到数据库：SQL 注入原理与防御、账号最小权限、静态/透明加密、审计与合规。

---

# DB28 · 数据库安全（注入防护 / 最小权限 / 加密）

## 🎯 目标
学完这节，你能亲手**复现一个 SQL 注入漏洞**（在自己建的测试库，白帽边界内）并理解其原理；掌握**参数化查询**这一根本防御；落实账号**最小权限**与**加密/脱敏**；理解审计与《个人信息保护法》/GDPR 合规要求。这是方向5 的"安全收口"。

## 📋 小白前置
需先懂 DB3（SQL 语法）、DB10（参数化已见过）、DB12（MySQL 账号/权限）、DB26（ORM 参数化）、DB27（备份加密）。

## 🟢 部分一·最浅层（生活比喻）
SQL 注入就像"把本来该填'姓名'的空，被别人填进了'命令'"。比如程序问数据库："找姓名是【用户输入】的人"，你本该填"小明"，坏人填"小明' 或 '1'='1"——结果这句话被篡改成"找姓名是小明、或者 1=1（永远真）的人"，于是**所有人**都被查出来了。更狠的能填"删表命令"，直接把库清空。防御极简单：别把用户输入当"命令的一部分"，而是当"一个纯粹的数据值"交给数据库（参数化）。

## 🟡 部分二·动手层（白帽边界内：自建测试库复现+修复）
新建一个**只属于自己**的测试库与漏洞演示（绝不针对任何外部系统）：
```python
import sqlite3
conn = sqlite3.connect(":memory:")   # 内存库，纯本地实验
conn.execute("CREATE TABLE users(id INTEGER PRIMARY KEY, name TEXT, pwd TEXT)")
conn.execute("INSERT INTO users(name,pwd) VALUES('admin','secret'),('bob','123')")
user_input = "admin' OR '1'='1"     # 攻击者输入

# ❌ 危险写法：字符串拼接（仅演示，绝不在真实代码用）
sql = "SELECT * FROM users WHERE name = '" + user_input + "'"
print("拼接后的SQL:", sql)
print("漏洞结果(返回了所有人):", conn.execute(sql).fetchall())

# ✅ 安全写法：参数化
cur = conn.execute("SELECT * FROM users WHERE name = ?", (user_input,))
print("参数化结果(只查不到或按字面):", cur.fetchall())
```
运行 `python inject_demo.py`，对比：拼接版返回两行（泄露全部），参数化版把 `admin' OR '1'='1` 当成**字面用户名**查，查不到（安全）。

### 🧪 实战 Lab（DB28）
① 跑上面注入演示，截图"拼接版返回所有人"与"参数化版安全"的对比。② 用 MySQL 演示最小权限：创建只讀账号并授权单库单表：
```sql
CREATE USER 'readonly'@'localhost' IDENTIFIED BY 'StrongPass!23';
GRANT SELECT ON bank.account TO 'readonly'@'localhost';
-- 验证：该账号无法 INSERT/DELETE/DROP
```
③ 写一段"敏感字段脱敏"SQL（`SELECT CONCAT(LEFT(name,1),'***') FROM ...` 或应用层掩码），说明它如何满足最小化展示。把实验与结论发我。

## 🔵 部分三·原理层（底层）
- **注入根因**：程序把"数据"和"代码（SQL 指令）"混在一起，且未分离。当用户输入含 `'`/`;`/`--` 等 SQL 元字符，就"逃逸"出数据位变成控制指令。这是**解析器混淆**——数据库无法区分"你写的 SQL"和"用户塞进来的 SQL"。
- **参数化查询（Prepared Statement）**：把 SQL 模板（`WHERE name=?`）和参数值**分两次**发给数据库。数据库先编译模板（确定语法结构），再把参数当纯数据绑定——参数里的 `'` 永远只是字符，无法改变结构。这是**唯一从根本上防注入**的方法（转义/黑名单都是补丁，不可靠）。
- **最小权限**：应用账号只给必要 `SELECT/INSERT` 等，绝不给 `DROP/GRANT/FILE`；即使注入发生，破坏面也被权限锁死。
- **加密/脱敏**：静态数据加密（磁盘/列级 AES）、传输 TLS、展示脱敏（中间掩码），满足合规。

## 🟣 部分四·深挖层（为什么、延伸）
SQL 注入自 1998 年被发现，至今稳居 OWASP Top10（方向7 SEC10），因为"拼接字符串"的坏习惯难根除。延伸攻击面：NoSQL 注入（Mongo 的 `$where` 拼接）、ORM 拼接（`text()` 误用，DB26）、存储过程拼接。防御体系（纵深防御）：参数化 + 输入校验（白名单）+ 最小权限 + WAF（OPS65）+ ORM + 定期代码审计（SEC20）。顶级工程师把"绝不拼接 SQL"刻进肌肉记忆，并把安全当成**设计属性**而非事后补丁。

## 🔴 部分五·顶级视角
数据库安全是"数据安全"的核心战场，也是你白帽路线的关键能力。顶级工程师从**威胁建模（SEC1/SEC7）**出发：识别"信任边界"（用户输入是恶意的）、落实最小权限、加密 PII、审计一切访问。他们也理解"注入"是**一大类漏洞的范式**——命令注入、XSS（HTML 注入）、模板注入（SSTI）、LDAP 注入，本质都是"数据被当作代码执行"，统一防御原则是"边界清晰 + 上下文转义 + 参数化"。这直接连通方向7 安全体系。

## 🟠 部分六·安全/合规种子（白帽红线，郑重收口）
- 🚫 **绝不做**：攻击/扫描/注入任何非你所有的数据库或系统；登录他人账号；逆向他人商业数据库软件；用注入技术窃取/篡改他人数据——这些均属违法，技术再强也不碰。
- ✅ **只练**：自己搭的测试库（如上 `:memory:` 内存库）、授权靶场（如 DVWA/WebGoat 在你本机）、自家虚拟机、CTF。注入原理"只分析不实践于他人"。
- 🔒 **合规**：处理个人信息严格遵循《个人信息保护法》/GDPR——最小必要、目的限定、加密存储、访问控制、留存期限、可更正/删除（被遗忘权）。未授权绝不爬取、汇聚、泄露他人个人信息。
- 💊 **身体优先**：手疼就停、复制代替敲；课永远在，人必须在。白帽之路是马拉松，不是冲刺。

## ✅ 验收
交：① 注入演示"拼接 vs 参数化"对比截图（漏洞暴露所有人 vs 安全）；② MySQL 只读账号创建 + 权限验证；③ 脱敏 SQL 与最小化展示说明；④ 一句话写清"白帽数据库安全红线"。

## ⚠️ 常见坑
- 以为"转义引号"就安全——不够，参数化才是根本，转义只防简单情况。
- ORM 里用 `text("...%s..." % 输入)` 同样注入，必须参数绑定。
- 用 root/sa 账号连应用 = 一旦注入全库任人宰割，务必最小权限。
- 裸奔数据库（无密码/公网）是头号泄露源，起任何库第一件事设密码+绑内网。
- 手疼复制，别硬敲。

## ➡️ 下一步（方向5 收官 · 全局路线）
恭喜走完 DB1–DB28！你已经从"什么是数据库"走到"白帽视角的安全收口"。下一步建议：
- 进入**方向6 Web 全栈（FE1–BE26）**：用数据库做真实网站（闪电号卡毕业作），把 SQL/Redis/ORM 用起来。
- 进入**方向7 安全攻防（SEC1–SEC102）**：系统化白帽能力，在授权靶场（DVWA/WebGoat/HTB/TryHackMe/picoCTF）练注入/XSS/逆向/CTF，始终守住红线。
- 并行**方向9 数据 AI（DAT1–DAT51）**：向量数据库（DAT34）、RAG（DB33）与今天学的存储体系深度互补。
- 任何时候：卡住喊"卡在第 X 步"，我立刻接手。稳扎稳打，一步步成为顶级大佬。

---

> 本文件完。DB1–DB28 全部按 11 段深模板 + 实战 Lab 完成，命令均可在 Git Bash / Docker / Python 真实复现。白帽红线贯穿全案。
