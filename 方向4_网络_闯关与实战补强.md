# 方向 4 · 网络：NET1–NET24 闯关与实战补强

本文件是 `教案_方向4_网络.md` 的逐课增补层。原文件负责 11 段讲解，本文件负责把每节课变成可测量的五级闯关和五类 Lab。每个 Lab 都必须保存 `pcapng`、终端输出、脚本、环境版本和结论；没有证据就不算过关。

## 统一实验环境

- Windows：PowerShell、`curl.exe`、`ipconfig`、`route print`、Wireshark/tshark。
- Linux/WSL：`ip`、`ss`、`curl`、`dig`、`traceroute`/`tracepath`、`tcpdump`、Python 3。
- 只对 `127.0.0.1`、自己的局域网设备、自己创建的容器/虚拟机或明确授权靶场操作。不得扫描公共网络，不得抓取他人流量，不得修改真实路由器配置。
- 每节先记录 `python --version`、`tshark --version`、操作系统和网卡名称；实验结束关闭监听、删除临时规则和容器。

## NET1 · 网络概论 / 分组交换

**资料锚点**：RFC Editor：https://www.rfc-editor.org/；Wireshark User Guide：https://www.wireshark.org/docs/wsug_html/
资料记录：使用时写明页面标题、章节、版本/提交日期、访问日期、适用范围和未验证项。
**必做失败实验**：仅在 127.0.0.1 或自有隔离网络制造端口关闭、超时、丢包、DNS 错误或 MTU 变化；用命令/pcap 记录“现象→假设→证据→修复→回归”。

**深挖主线**：把“分组”从比喻落到 MTU、头部开销、排队、丢包、乱序和抖动；比较电路交换、分组交换、消息交换的资源模型。

**五关**：Lv1 画出端到端分组路径并用 `ipconfig`/`ip addr` 标出本机、网关、DNS；Lv2 修改 MTU 或 payload，记录成功与失败边界；Lv3 用本地 HTTP 服务生成 1 B、1 KB、1 MB 请求，比较包数量和耗时；Lv4 写脚本从 pcap 统计包长分布、重传/丢失迹象和队列延迟；Lv5 设计一份“突发流量下的容量预算”，说明链路带宽、RTT、缓冲和尾延迟的关系。

**Labs**：

1. `python -m http.server 8000`，用 `curl` 下载不同大小文件，保存抓包并标注 Ethernet/IP/TCP/HTTP 层。
2. 用 `ping -f` 不执行；改用受控的 Python UDP 发送器，仅发给 `127.0.0.1`，逐步增加速率，记录接收端丢包。
3. 用 `tc netem`（仅 WSL 自有命名空间）或本地代理注入 50 ms 延迟，比较 P50/P95/P99。
4. 读取 pcap 的时间戳和长度，输出 CSV；验证总字节数与应用层文件大小的差异。
5. 写一页研究笔记：为什么“带宽够”仍可能卡顿，给出三个可证伪假设。

**验收**：图、pcap、统计脚本、容量预算；安全边界和清理命令齐全。

## NET2 · OSI / TCP-IP 模型

**资料锚点**：RFC Editor：https://www.rfc-editor.org/；Wireshark User Guide：https://www.wireshark.org/docs/wsug_html/
资料记录：使用时写明页面标题、章节、版本/提交日期、访问日期、适用范围和未验证项。
**必做失败实验**：仅在 127.0.0.1 或自有隔离网络制造端口关闭、超时、丢包、DNS 错误或 MTU 变化；用命令/pcap 记录“现象→假设→证据→修复→回归”。

**深挖主线**：用封装/解封装和“故障所在层”替代死记七层；指出 OSI 会话层/表示层在现代协议中的实际落点和边界模糊处。

**五关**：Lv1 画 OSI 与 TCP/IP 对照；Lv2 用 `ping`、`dig`、`curl -v`、浏览器开发者工具分别定位层次；Lv3 故意停止 DNS、HTTP 服务和本地网卡，按证据排障；Lv4 写“现象→层→证据→修复”的决策树；Lv5 设计一次跨团队排障演练并给出误判反例。

**Labs**：

1. `ping 127.0.0.1`、`ping` 网关、`curl -v http://127.0.0.1:8000`，记录各自证明了什么、没有证明什么。
2. 启动本地服务后只阻断应用端口（自有防火墙规则或停止服务），比较 `ping` 与 `curl`。
3. 从一次 HTTPS 抓包逐层标注 MAC、IP、TCP、TLS、HTTP/2 字段。
4. 写 PowerShell/ Bash 排障脚本，按“接口→路由→DNS→TCP→HTTP”顺序输出诊断。
5. 复盘一个“ping 通但网页打不开”的案例，要求每个结论绑定命令输出或 pcap 帧。

**验收**：决策树必须包含 DNS、端口、防火墙、证书和应用错误五类分支。

## NET3 · 以太网 / ARP / MAC

**资料锚点**：RFC Editor：https://www.rfc-editor.org/；Wireshark User Guide：https://www.wireshark.org/docs/wsug_html/
资料记录：使用时写明页面标题、章节、版本/提交日期、访问日期、适用范围和未验证项。
**必做失败实验**：仅在 127.0.0.1 或自有隔离网络制造端口关闭、超时、丢包、DNS 错误或 MTU 变化；用命令/pcap 记录“现象→假设→证据→修复→回归”。

**深挖主线**：理解帧、广播域、交换机 MAC 学习、ARP 缓存寿命和 ARP 欺骗的防御，不在真实网络复现攻击。

**五关**：Lv1 读 `arp -a`/`ip neigh`；Lv2 清理本机缓存并观察请求/应答；Lv3 建两个自有网络命名空间做 ARP 变化实验；Lv4 比较静态绑定、DHCP snooping、动态 ARP 检测的取舍；Lv5 写一份局域网分段与异常 ARP 告警设计。

**Labs**：

1. Wireshark 过滤 `arp`，标出广播请求和单播应答。
2. 对比缓存命中前后的 RTT 与包数量。
3. 在本地隔离 namespace 中改变一台测试节点的地址，观察邻居表失效和恢复。
4. 用 tshark 统计单位时间 ARP 请求数，设计正常基线和告警阈值。
5. 写防御报告：交换机隔离、静态网关、端到端 TLS 各自能防什么、不能防什么。

**安全**：不使用 arpspoof/mitm 工具攻击公共或家庭网络；只分析自己产生的正常 ARP 或隔离靶场流量。

## NET4 · IP / 子网 / 路由基础

**资料锚点**：RFC Editor：https://www.rfc-editor.org/；Wireshark User Guide：https://www.wireshark.org/docs/wsug_html/
资料记录：使用时写明页面标题、章节、版本/提交日期、访问日期、适用范围和未验证项。
**必做失败实验**：仅在 127.0.0.1 或自有隔离网络制造端口关闭、超时、丢包、DNS 错误或 MTU 变化；用命令/pcap 记录“现象→假设→证据→修复→回归”。

**深挖主线**：CIDR、最长前缀匹配、广播/网络地址、IPv4/IPv6 对照和路由表的真实决策。

**五关**：Lv1 手算 `/24`、`/26`、`/30`；Lv2 用 `ipcalc` 或自写函数校验；Lv3 建三段 namespace 网络并配置静态路由；Lv4 注入重叠前缀、黑洞和 MTU 问题；Lv5 设计带地址规划、故障域和 IPv6 迁移的园区网络。

**Labs**：

1. `route print`/`ip route` 与目标地址逐条匹配，写出选择的下一跳。
2. 写 Python CIDR 计算器，性质测试覆盖网络号、广播地址、边界前缀。
3. 仅在本地 namespace 配置 `10.10.1.0/24`、`10.10.2.0/24`、路由器转发和回程路由。
4. 改变 MTU，观察 `ping` 大包、DF、分片或 IPv6 PMTUD 的差异。
5. 提交 IPv4/IPv6 双栈设计和地址耗尽、隐私地址、NAT 依赖的分析。

## NET5 · ICMP / ping / traceroute

**资料锚点**：RFC Editor：https://www.rfc-editor.org/；Wireshark User Guide：https://www.wireshark.org/docs/wsug_html/
资料记录：使用时写明页面标题、章节、版本/提交日期、访问日期、适用范围和未验证项。
**必做失败实验**：仅在 127.0.0.1 或自有隔离网络制造端口关闭、超时、丢包、DNS 错误或 MTU 变化；用命令/pcap 记录“现象→假设→证据→修复→回归”。

**深挖主线**：区分连通性、可达性、服务可用性；理解 TTL、Time Exceeded、ICMP 过滤和路径不对称。

**五关**：Lv1 解析 ping 字段；Lv2 修改 payload 和超时；Lv3 对本地多跳拓扑做 traceroute；Lv4 编写带统计的探测器；Lv5 设计不依赖单一 ICMP 结论的健康检查体系。

**Labs**：

1. `ping 127.0.0.1 -n 10` 与 `ping` 网关，记录最小/平均/最大 RTT。
2. `tracert 127.0.0.1` 或 `traceroute` 自有节点，标注每一跳的证据和 `*` 的含义。
3. 本地 Python 服务分别让 TCP 端口关闭、应用返回 500、进程挂起，比较 ping/端口探测/HTTP 检查。
4. 对 RTT 样本计算均值、标准差、P95/P99，解释为什么平均值不够。
5. 写故障 Runbook：ICMP 被禁时如何用 TCP、DNS、HTTP 和 trace 交叉验证。

## NET6 · TCP 详解

**资料锚点**：RFC Editor：https://www.rfc-editor.org/；Wireshark User Guide：https://www.wireshark.org/docs/wsug_html/
资料记录：使用时写明页面标题、章节、版本/提交日期、访问日期、适用范围和未验证项。
**必做失败实验**：仅在 127.0.0.1 或自有隔离网络制造端口关闭、超时、丢包、DNS 错误或 MTU 变化；用命令/pcap 记录“现象→假设→证据→修复→回归”。

**深挖主线**：状态机、序列号/确认号、滑动窗口、重传、SACK、RTO、拥塞控制和优雅关闭；以 RFC 9293 字段为依据。

**五关**：Lv1 抓三次握手与四次挥手；Lv2 改变 `recv` 缓冲和文件大小；Lv3 写可靠 TCP echo 客户端并处理半关闭；Lv4 用 pcap 分析重传/窗口/零窗口；Lv5 比较不同 RTT/丢包下的吞吐模型并提出优化方案。

**Labs**：

1. `python -m http.server 8000` + Wireshark `tcp.flags.syn==1`，标注 ISN、窗口和 MSS。
2. 写 Python socket echo，加入超时、`shutdown()`、异常和长度前缀。
3. 用本地代理延迟/丢弃连接，验证超时、重试和幂等策略。
4. 用 tshark 导出 `tcp.analysis.retransmission`、窗口变化和 RTT。
5. 按 RFC 9293 写一份状态机和“RST 与 FIN 的差异”教学笔记。

## NET7 · UDP

**资料锚点**：RFC Editor：https://www.rfc-editor.org/；Wireshark User Guide：https://www.wireshark.org/docs/wsug_html/
资料记录：使用时写明页面标题、章节、版本/提交日期、访问日期、适用范围和未验证项。
**必做失败实验**：仅在 127.0.0.1 或自有隔离网络制造端口关闭、超时、丢包、DNS 错误或 MTU 变化；用命令/pcap 记录“现象→假设→证据→修复→回归”。

**深挖主线**：无连接、无序、无可靠交付不等于“低延迟万能”；理解应用层可靠性、MTU、校验和、放大风险。

**五关**：Lv1 发收本地 UDP；Lv2 加消息编号和丢包统计；Lv3 实现选择性重传和重排窗口；Lv4 测量 TCP/UDP 在受控损失下的差异；Lv5 设计有速率限制、认证和重放防护的 UDP 协议。

**Labs**：

1. `python -m` 写本地 UDP echo，记录发送数、接收数、重复和乱序。
2. 加 1%/5%/10% 的本地随机丢包，比较无重传与选择性重传。
3. 固定 payload，测量超过路径 MTU 时的失败/分片风险。
4. 加 HMAC、时间戳和 nonce，验证篡改、重放和过期消息会被拒绝。
5. 写“何时选 UDP、何时选 TCP/QUIC”的决策矩阵，含运维成本。

## NET8 · HTTP / HTTP2 / HTTP3

**资料锚点**：RFC Editor：https://www.rfc-editor.org/；Wireshark User Guide：https://www.wireshark.org/docs/wsug_html/
资料记录：使用时写明页面标题、章节、版本/提交日期、访问日期、适用范围和未验证项。
**必做失败实验**：仅在 127.0.0.1 或自有隔离网络制造端口关闭、超时、丢包、DNS 错误或 MTU 变化；用命令/pcap 记录“现象→假设→证据→修复→回归”。

**深挖主线**：方法/状态码/缓存语义、连接复用、HTTP/2 流与队头阻塞、HTTP/3 over QUIC；不把版本号当性能保证。

**五关**：Lv1 用 `curl -i/-v` 读请求响应；Lv2 实现缓存控制和条件请求；Lv3 用本地服务比较 HTTP/1.1 与 HTTP/2 多路复用；Lv4 从 trace/pcap 分析首字节、下载和错误尾延迟；Lv5 设计版本协商、缓存、重试和回滚策略。

**Labs**：

1. 记录 GET/POST/PUT/DELETE、状态码、`Content-Length`、`ETag`、`Cache-Control`。
2. 写一个返回 304 的本地资源服务，验证条件请求减少传输。
3. 用 `curl --http1.1` 与 `curl --http2`（环境支持时）比较连接和请求时间。
4. 构造安全的重试矩阵：只对幂等方法/明确幂等键重试。
5. 提交 HTTP/1.1、2、3 取舍报告，包含代理、监控、调试和失败降级。

## NET9 · HTTPS / TLS 1.3

**资料锚点**：RFC Editor：https://www.rfc-editor.org/；Wireshark User Guide：https://www.wireshark.org/docs/wsug_html/
资料记录：使用时写明页面标题、章节、版本/提交日期、访问日期、适用范围和未验证项。
**必做失败实验**：仅在 127.0.0.1 或自有隔离网络制造端口关闭、超时、丢包、DNS 错误或 MTU 变化；用命令/pcap 记录“现象→假设→证据→修复→回归”。

**深挖主线**：身份认证、密钥协商、前向保密、证书链、SNI/ALPN、TLS 1.3 握手与会话恢复；明确“加密”不等于“端点可信”。

**五关**：Lv1 读浏览器证书和 `curl -v`；Lv2 用本地 CA 签发测试证书；Lv3 分析 ClientHello/ServerHello；Lv4 轮换证书并验证失败路径；Lv5 设计密钥生命周期、HSTS、撤销和可观测性方案。

**Labs**：

1. `curl -vk https://127.0.0.1:8443` 仅访问自有测试服务，保存证书链和协商协议。
2. 用 OpenSSL 生成临时 CA/服务证书，客户端显式信任 CA，实验后删除私钥。
3. Wireshark 过滤 `tls.handshake`，标注 SNI、ALPN、cipher suite。
4. 分别制造过期、主机名不匹配、未信任 CA，记录客户端错误。
5. 写 TLS 运营清单：密钥存储、轮换、最小权限、日志脱敏和回滚。

## NET10 · DNS 详解

**资料锚点**：RFC Editor：https://www.rfc-editor.org/；Wireshark User Guide：https://www.wireshark.org/docs/wsug_html/
资料记录：使用时写明页面标题、章节、版本/提交日期、访问日期、适用范围和未验证项。
**必做失败实验**：仅在 127.0.0.1 或自有隔离网络制造端口关闭、超时、丢包、DNS 错误或 MTU 变化；用命令/pcap 记录“现象→假设→证据→修复→回归”。

**深挖主线**：递归/权威、缓存与 TTL、委派、记录类型、负缓存、DNSSEC 概念和 DoH/DoT 的可观测性。

**五关**：Lv1 用 `nslookup`/`dig` 读 A/AAAA/CNAME/MX；Lv2 修改本地 hosts/TTL；Lv3 搭建自有权威测试区；Lv4 注入 NXDOMAIN、SERVFAIL、过期缓存并排障；Lv5 设计多区域 DNS、DNSSEC 和故障切换策略。

**Labs**：

1. `nslookup example.com` 或 `dig example.com`，记录递归服务器、TTL 和答案。
2. 本地启动测试 DNS（容器/namespace），创建 `app.test`、CNAME 和 TXT。
3. 比较冷缓存与热缓存查询时间，解释负缓存。
4. 用 `dig +trace` 观察委派（只查公开域名，不做扫描）。
5. 写 DNS 变更 Runbook：低 TTL 预热、回滚窗口、DNSSEC 密钥和监控。

## NET11 · DHCP

**资料锚点**：RFC Editor：https://www.rfc-editor.org/；Wireshark User Guide：https://www.wireshark.org/docs/wsug_html/
资料记录：使用时写明页面标题、章节、版本/提交日期、访问日期、适用范围和未验证项。
**必做失败实验**：仅在 127.0.0.1 或自有隔离网络制造端口关闭、超时、丢包、DNS 错误或 MTU 变化；用命令/pcap 记录“现象→假设→证据→修复→回归”。

**深挖主线**：DORA、租约、续租、选项、地址冲突、Relay 和企业网络隔离；区分 DHCP 失败与静态路由失败。

**五关**：Lv1 读本机租约和网关；Lv2 在隔离网络观察 DORA；Lv3 配置短租约并验证续租；Lv4 注入地址池耗尽/错误网关；Lv5 设计 DHCP 高可用、审计和设备准入方案。

**Labs**：

1. Windows `ipconfig /all` 或 Linux `nmcli` 记录租约、DNS、网关和过期时间。
2. 只在本地虚拟网络抓 `bootp`，标注 Discover/Offer/Request/ACK。
3. 配置 60 秒测试租约，观察续租时间线。
4. 使用独立 namespace 模拟错误 DHCP，验证客户端为何拿到 `169.254.x.x` 或错误 DNS。
5. 写地址池容量表、保留地址策略和日志留存规则。

## NET12 · 代理 / 反向代理 / NAT

**资料锚点**：RFC Editor：https://www.rfc-editor.org/；Wireshark User Guide：https://www.wireshark.org/docs/wsug_html/
资料记录：使用时写明页面标题、章节、版本/提交日期、访问日期、适用范围和未验证项。
**必做失败实验**：仅在 127.0.0.1 或自有隔离网络制造端口关闭、超时、丢包、DNS 错误或 MTU 变化；用命令/pcap 记录“现象→假设→证据→修复→回归”。

**深挖主线**：正向/反向代理、连接终止、`X-Forwarded-*` 信任、SNAT/DNAT、端口耗尽和真实客户端地址。

**五关**：Lv1 通过本地反向代理访问服务；Lv2 加超时、健康检查和访问日志；Lv3 让两个后端轮询；Lv4 观察 NAT 映射和端口耗尽风险；Lv5 设计代理信任边界、TLS 终止和回源安全。

**Labs**：

1. 用 Caddy/Nginx 或 Python 代理把 `127.0.0.1:8000` 映射到 `:8080`。
2. 添加 `X-Request-ID`、超时和上游错误日志，验证链路关联。
3. 配两个本地后端，模拟一个慢/一个正常，观察代理行为。
4. 对比直接访问与代理访问的源地址、连接数和 keep-alive。
5. 提交“可信代理列表”和 SSRF/Host 头防护清单。

## NET13 · 负载均衡

**资料锚点**：RFC Editor：https://www.rfc-editor.org/；Wireshark User Guide：https://www.wireshark.org/docs/wsug_html/
资料记录：使用时写明页面标题、章节、版本/提交日期、访问日期、适用范围和未验证项。
**必做失败实验**：仅在 127.0.0.1 或自有隔离网络制造端口关闭、超时、丢包、DNS 错误或 MTU 变化；用命令/pcap 记录“现象→假设→证据→修复→回归”。

**深挖主线**：L4/L7、轮询/最少连接/一致性哈希、健康检查、会话粘性、慢启动和优雅下线。

**五关**：Lv1 运行两个后端；Lv2 比较轮询与权重；Lv3 注入一个后端失败和恢复；Lv4 量测 P50/P95、错误率和连接分布；Lv5 设计跨故障域流量切换和容量模型。

**Labs**：

1. 启动两个返回不同实例 ID 的本地 HTTP 服务。
2. 让代理按权重分发，统计 1000 次请求比例。
3. 让实例返回 503/延迟，验证健康检查和熔断。
4. 用压测工具只压本地服务，记录并发、吞吐、P95/P99 和错误。
5. 写发布策略：连接排空、金丝雀、自动回滚条件。

## NET14 · VPN 原理

**资料锚点**：RFC Editor：https://www.rfc-editor.org/；Wireshark User Guide：https://www.wireshark.org/docs/wsug_html/
资料记录：使用时写明页面标题、章节、版本/提交日期、访问日期、适用范围和未验证项。
**必做失败实验**：仅在 127.0.0.1 或自有隔离网络制造端口关闭、超时、丢包、DNS 错误或 MTU 变化；用命令/pcap 记录“现象→假设→证据→修复→回归”。

**深挖主线**：隧道与加密边界、路由模式/桥接模式、MTU、密钥轮换、分流与全隧道；不做规避监管的配置。

**五关**：Lv1 解释隧道封装；Lv2 在自有 namespace 建点对点测试隧道；Lv3 加路由和 DNS；Lv4 测 MTU、断线和重连；Lv5 设计最小权限的远程访问和审计方案。

**Labs**：

1. 阅读 WireGuard/OpenVPN 官方配置字段，制作不含真实私钥的教学配置。
2. 只在本地虚拟网络建立两端隧道，抓包比较内层/外层地址。
3. 改变 MTU，验证大包和分片/PMTUD。
4. 撤销测试密钥并确认旧端点无法连接。
5. 提交访问矩阵、密钥轮换、日志和紧急撤销 Runbook。

## NET15 · 防火墙 / ACL

**资料锚点**：RFC Editor：https://www.rfc-editor.org/；Wireshark User Guide：https://www.wireshark.org/docs/wsug_html/
资料记录：使用时写明页面标题、章节、版本/提交日期、访问日期、适用范围和未验证项。
**必做失败实验**：仅在 127.0.0.1 或自有隔离网络制造端口关闭、超时、丢包、DNS 错误或 MTU 变化；用命令/pcap 记录“现象→假设→证据→修复→回归”。

**深挖主线**：默认拒绝、状态检测、规则顺序、最小权限、东西向/南北向隔离和变更回滚。

**五关**：Lv1 读懂一条允许/拒绝规则；Lv2 为本地服务开放单端口；Lv3 按来源/目的/状态写最小规则；Lv4 用测试矩阵验证无意放行；Lv5 设计可审计的分层策略与紧急回滚。

**Labs**：

1. 只对本机临时端口做允许/拒绝测试，保存规则前后状态。
2. 编写端口-来源-目的-动作矩阵，覆盖 ESTABLISHED/RELATED。
3. 自动化测试“允许健康检查、拒绝未授权端口”。
4. 模拟规则顺序错误，展示为什么先宽后窄会放行错误流量。
5. 提交变更审批、回滚命令、日志留存和过期规则清理设计。

## NET16 · 无线安全

**资料锚点**：RFC Editor：https://www.rfc-editor.org/；Wireshark User Guide：https://www.wireshark.org/docs/wsug_html/
资料记录：使用时写明页面标题、章节、版本/提交日期、访问日期、适用范围和未验证项。
**必做失败实验**：仅在 127.0.0.1 或自有隔离网络制造端口关闭、超时、丢包、DNS 错误或 MTU 变化；用命令/pcap 记录“现象→假设→证据→修复→回归”。

**深挖主线**：Wi-Fi 关联、认证、加密、漫游、WPA2/WPA3、访客隔离和射频/密码风险；不捕获他人无线流量。

**五关**：Lv1 读取自己设备的安全模式；Lv2 在自有热点验证客户端隔离；Lv3 观察 DHCP/DNS/漫游事件；Lv4 审计自家配置和弱口令风险；Lv5 设计家庭/小企业分 SSID、访客、IoT 和更新策略。

**Labs**：

1. 只查看操作系统显示的自有连接属性和加密类型。
2. 在自己控制的热点上创建访客 SSID，验证不能访问主网段。
3. 测量不同位置的信号、丢包和漫游，不记录邻居网络名称。
4. 写无线资产清单、固件更新和管理员账号保护清单。
5. 输出“无线安全自查报告”，不包含真实密码和 BSSID。

## NET17 · 网络编程 socket

**资料锚点**：RFC Editor：https://www.rfc-editor.org/；Wireshark User Guide：https://www.wireshark.org/docs/wsug_html/
资料记录：使用时写明页面标题、章节、版本/提交日期、访问日期、适用范围和未验证项。
**必做失败实验**：仅在 127.0.0.1 或自有隔离网络制造端口关闭、超时、丢包、DNS 错误或 MTU 变化；用命令/pcap 记录“现象→假设→证据→修复→回归”。

**深挖主线**：阻塞/非阻塞、地址族、连接生命周期、半包/粘包、背压、超时、取消和优雅关闭。

**五关**：Lv1 写本地 TCP/UDP echo；Lv2 加长度前缀和协议版本；Lv3 支持多客户端与超时；Lv4 做并发/内存/连接泄漏测试；Lv5 设计可演进协议和兼容性矩阵。

**Labs**：

1. Python `socket` 仅绑定 `127.0.0.1`，实现 echo 和单元测试。
2. 发送拆分/合并的消息，验证 `recv()` 不等于一次完整消息。
3. 客户端异常退出、半关闭、服务重启，验证状态机和清理。
4. 用连接数和消息大小矩阵测吞吐、P95、内存和文件描述符。
5. 写协议文档：帧格式、版本、错误码、超时、幂等和兼容策略。

## NET18 · 高级 I/O

**资料锚点**：RFC Editor：https://www.rfc-editor.org/；Wireshark User Guide：https://www.wireshark.org/docs/wsug_html/
资料记录：使用时写明页面标题、章节、版本/提交日期、访问日期、适用范围和未验证项。
**必做失败实验**：仅在 127.0.0.1 或自有隔离网络制造端口关闭、超时、丢包、DNS 错误或 MTU 变化；用命令/pcap 记录“现象→假设→证据→修复→回归”。

**深挖主线**：select/poll/epoll/kqueue/io_uring 的事件模型、水平/边沿触发、背压和线程池；避免把“异步”当成无成本。

**五关**：Lv1 对比阻塞与非阻塞；Lv2 用事件循环服务多个本地连接；Lv3 处理部分读写和背压；Lv4 做事件模型基准；Lv5 为目标负载选择模型并写出证据。

**Labs**：

1. 用 Python selectors 或 Go netpoll 写本地多连接 echo。
2. 让客户端发送速度超过服务处理速度，观察队列和内存上限。
3. 比较 1/10/100/1000 个连接的 CPU、延迟和上下文切换。
4. 制造慢客户端、断开和文件描述符耗尽，验证保护措施。
5. 提交“事件循环 vs 线程池 vs async runtime”决策记录。

## NET19 · WebSocket / gRPC / RPC

**资料锚点**：RFC Editor：https://www.rfc-editor.org/；Wireshark User Guide：https://www.wireshark.org/docs/wsug_html/
资料记录：使用时写明页面标题、章节、版本/提交日期、访问日期、适用范围和未验证项。
**必做失败实验**：仅在 127.0.0.1 或自有隔离网络制造端口关闭、超时、丢包、DNS 错误或 MTU 变化；用命令/pcap 记录“现象→假设→证据→修复→回归”。

**深挖主线**：长连接、升级握手、心跳、背压、流式 RPC、schema 演进、超时/取消和幂等。

**五关**：Lv1 建本地 WebSocket/gRPC；Lv2 加心跳和重连；Lv3 实现服务端流/客户端流；Lv4 注入半开连接与 schema 不兼容；Lv5 设计版本化、鉴权和限流。

**Labs**：

1. 本地 WebSocket 回显，记录 upgrade 请求和 close code。
2. 加 ping/pong、指数退避和最大重连次数。
3. 用 protobuf 定义 v1/v2，验证向后兼容和未知字段处理。
4. 限制发送队列，测试慢消费者不会无限占内存。
5. 提交 RPC Runbook：deadline、取消、重试、trace ID、错误码。

## NET20 · CDN

**资料锚点**：RFC Editor：https://www.rfc-editor.org/；Wireshark User Guide：https://www.wireshark.org/docs/wsug_html/
资料记录：使用时写明页面标题、章节、版本/提交日期、访问日期、适用范围和未验证项。
**必做失败实验**：仅在 127.0.0.1 或自有隔离网络制造端口关闭、超时、丢包、DNS 错误或 MTU 变化；用命令/pcap 记录“现象→假设→证据→修复→回归”。

**深挖主线**：边缘缓存、源站回源、Cache-Control/ETag、失效、分层缓存、动态请求和隐私边界。

**五关**：Lv1 用本地代理模拟命中/未命中；Lv2 加 ETag/TTL；Lv3 注入源站慢和失效；Lv4 计算命中率与节省带宽；Lv5 设计静态/动态/个性化内容策略。

**Labs**：

1. 本地代理记录 `HIT/MISS/STALE`，验证同一资源第二次请求。
2. 修改内容但保持 URL，比较 TTL、ETag 和版本化 URL。
3. 对带 Cookie/授权头的响应验证不会错误共享给另一用户。
4. 注入源站 500/超时，验证 stale-if-error 或安全降级。
5. 提交缓存键、失效、隐私、成本和回滚设计。

## NET21 · 抓包 Wireshark / tcpdump

**资料锚点**：RFC Editor：https://www.rfc-editor.org/；Wireshark User Guide：https://www.wireshark.org/docs/wsug_html/
资料记录：使用时写明页面标题、章节、版本/提交日期、访问日期、适用范围和未验证项。
**必做失败实验**：仅在 127.0.0.1 或自有隔离网络制造端口关闭、超时、丢包、DNS 错误或 MTU 变化；用命令/pcap 记录“现象→假设→证据→修复→回归”。

**深挖主线**：抓包边界、过滤、时间线、字段解码、重组、统计和证据保全；只抓自己的接口/靶场。

**五关**：Lv1 抓本地 DNS/TCP/HTTP；Lv2 写显示过滤器；Lv3 用 tshark 导出字段；Lv4 从 pcap 还原一次故障；Lv5 建立可重复的抓包 SOP 与隐私脱敏流程。

**Labs**：

1. Wireshark 过滤 `dns`、`tcp.handshake`、`http` 或本地 HTTPS 的 `tls.handshake`。
2. `tshark -r capture.pcapng -Y "tcp.analysis.retransmission"` 导出重传。
3. 用 IO Graph/统计菜单计算包率、RTT、协议占比。
4. 对 pcap 做时间线：请求 ID、五元组、首包、错误和恢复。
5. 提交证据保全记录：接口、起止时间、过滤器、文件哈希、敏感字段处理。

## NET22 · QUIC

**资料锚点**：RFC Editor：https://www.rfc-editor.org/；Wireshark User Guide：https://www.wireshark.org/docs/wsug_html/
资料记录：使用时写明页面标题、章节、版本/提交日期、访问日期、适用范围和未验证项。
**必做失败实验**：仅在 127.0.0.1 或自有隔离网络制造端口关闭、超时、丢包、DNS 错误或 MTU 变化；用命令/pcap 记录“现象→假设→证据→修复→回归”。

**深挖主线**：UDP 上的可靠流、多路复用、连接 ID、TLS 1.3、0-RTT、拥塞控制和迁移；比较 TCP 队头阻塞。

**五关**：Lv1 读 QUIC 基本概念；Lv2 在本地支持 HTTP/3 的服务上抓握手；Lv3 比较 HTTP/2/3；Lv4 注入 RTT/丢包并分析流级影响；Lv5 设计 0-RTT 重放防护和灰度回退。

**Labs**：

1. 使用明确支持 HTTP/3 的本地或自有测试服务，确认协商协议与证书。
2. 保存 QUIC Initial/Handshake 包，标注连接 ID、流和 TLS 关系。
3. 在本地代理/网络命名空间注入延迟和丢包，比较多资源下载。
4. 检查客户端无法使用 HTTP/3 时是否安全回退 HTTP/2/1.1。
5. 写 QUIC 运营清单：版本、指标、0-RTT、密钥日志保护和回滚。

## NET23 · 网络诊断

**资料锚点**：RFC Editor：https://www.rfc-editor.org/；Wireshark User Guide：https://www.wireshark.org/docs/wsug_html/
资料记录：使用时写明页面标题、章节、版本/提交日期、访问日期、适用范围和未验证项。
**必做失败实验**：仅在 127.0.0.1 或自有隔离网络制造端口关闭、超时、丢包、DNS 错误或 MTU 变化；用命令/pcap 记录“现象→假设→证据→修复→回归”。

**深挖主线**：把症状转成假设，建立最小证据链；覆盖 DNS、连接、TLS、HTTP、资源、容量和依赖故障。

**五关**：Lv1 按固定顺序执行诊断；Lv2 对本地服务制造单一故障；Lv3 做多故障组合；Lv4 用指标/日志/trace 定位根因；Lv5 写可交接的故障手册并主持复盘。

**Labs**：

1. 使用本地服务编写一键诊断脚本：接口、路由、DNS、端口、TLS、HTTP。
2. 分别制造端口关闭、DNS 错误、证书错误、应用 500、慢依赖。
3. 每次只改变一个变量，记录“现象、假设、命令、证据、结论”。
4. 将 pcap、日志、metrics、trace 按请求 ID 对齐。
5. 提交一份含时间线、影响范围、根因、修复、预防和待办的复盘。

## NET24 · 路由协议 BGP

**资料锚点**：RFC Editor：https://www.rfc-editor.org/；Wireshark User Guide：https://www.wireshark.org/docs/wsug_html/
资料记录：使用时写明页面标题、章节、版本/提交日期、访问日期、适用范围和未验证项。
**必做失败实验**：仅在 127.0.0.1 或自有隔离网络制造端口关闭、超时、丢包、DNS 错误或 MTU 变化；用命令/pcap 记录“现象→假设→证据→修复→回归”。

**深挖主线**：自治系统、eBGP/iBGP、路径属性、策略路由、收敛、黑洞、RPKI/ROA 和路由泄漏防御；只用模拟器或自有实验网。

**五关**：Lv1 解释 AS_PATH、LOCAL_PREF、MED、NEXT_HOP；Lv2 在 FRRouting/容器实验网读路由；Lv3 配两条路径和策略；Lv4 注入链路故障并测收敛；Lv5 设计多 AS 出口、过滤、RPKI 验证和事故响应。

**Labs**：

1. 用 Containerlab/FRRouting 或等价模拟器创建至少 3 个本地 AS；不连接真实互联网。
2. 发布私有测试前缀，观察 `show bgp summary`、`show bgp ipv4 unicast`。
3. 改变 LOCAL_PREF/MED/AS_PATH prepend，验证最佳路径变化。
4. 关闭链路或撤销前缀，测量收敛时间、黑洞窗口和告警延迟。
5. 写路由安全策略：前缀过滤、最大前缀数、RPKI 状态、变更审批和紧急撤回。

**安全与伦理**：不得向运营商、云厂商或公共 BGP 设施发布前缀；不得进行劫持、泄漏或 DDoS 实验。毕业证据只来自隔离模拟器和自有网络。

## 方向 4 毕业工程

构建一个完全本地的“可观测多节点服务”：两个应用节点、一个反向代理、一个缓存、一个数据库、一个隔离路由实验网。要求：

1. 用 DNS/HTTP/TCP/TLS/代理/负载均衡串起一次请求，并提供 pcap、trace、日志、metrics。
2. 注入 DNS 失败、一个后端宕机、延迟升高、证书错误、连接泄漏五类故障。
3. 设定 P95、错误率、RTO/RPO 和容量上限，写出测量方法。
4. 使用最小权限、临时凭据、默认拒绝规则和完整清理脚本。
5. 提交架构图、协议字段笔记、故障 Runbook、复盘和“没有验证的假设”清单。

## 权威资料锚点

- [RFC 9293 · Transmission Control Protocol](https://www.rfc-editor.org/rfc/rfc9293)
- [RFC Editor](https://www.rfc-editor.org/)
- [Wireshark User Documentation](https://www.wireshark.org/docs/)
- [MDN HTTP](https://developer.mozilla.org/en-US/docs/Web/HTTP)
- [MDN WebSockets](https://developer.mozilla.org/en-US/docs/Web/API/WebSockets_API)
- [QUIC · RFC 9000](https://www.rfc-editor.org/rfc/rfc9000)
- [BGP · RFC 4271](https://www.rfc-editor.org/rfc/rfc4271)
- [RPKI Origin Validation · RFC 6811](https://www.rfc-editor.org/rfc/rfc6811)

