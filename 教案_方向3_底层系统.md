# 教案_方向3_底层系统（COMP1–7 / ASM1–8 / OS1–17 / CMP1–12 · 宗师级极深版 · 程序如何真正跑起来）

> **宗师版声明**：本文件已按「11 段深模板（🎯目标/📋前置/🟢最浅/🟡动手/🔵原理/🟣深挖/🔴顶级/🟠安全/✅验收/⚠️坑/➡️下一步）+ ⚔️ Lv1–Lv5 闯关（🥉🥈🥇🏆👑）+ ≥5 可运行 Lab」宗师标准升级完毕，与方向2（算法样板）、方向1a/1b 同款。
> 本文件覆盖方向3底层系统的全部 44 节：计算机组成与体系结构（COMP1–7）、汇编（ASM1–8）、操作系统（OS1–17）、编译链接（CMP1–12）。每节严格按 11 段深模板展开，含真实可敲命令/代码与 Lab。
> 学生画像：真·零基础（连电脑配件都不熟）、术后留置针手不便（护手少打字、复制优先、手疼立刻停）、时间极充裕、目标成为计算机领域顶级大佬、坚定白帽路线、愿为学习投资、拒绝野鸡包会班与盗版。
> 闯关框架：🟢Lv1 新手村→🔵Lv2 熟练工→🟣Lv3 进阶者→🟠Lv4 高手→🔴Lv5 宗师。难度只加不跳；卡住就停那关，下次从那关续。集齐 44 个 👑 即方向3 毕业。
> 白帽红线贯穿：汇编/内核/Shellcode 知识**仅用于理解原理、分析自己程序与授权靶场**；绝不写恶意利用、绝不对外投送、逆向只对自己/授权样本。
> 学生画像：真·零基础（连电脑配件都不熟）、术后康复期手有留置针、行动不便但时间极充裕、目标成为计算机领域顶级大佬、坚定白帽路线、愿为学习投资、拒绝野鸡包会班与盗版。
> 教学铁律：极细拆分、单步可执行可验收、动手后必讲理论；身体红线——任何头晕/疼痛立刻停学，复制粘贴代替打字护手优先。
> 工具链（命令真实可跑）：MSYS2（C:\msys64）的 gcc/g++/gdb/make/nasm；在 Git Bash 或 MSYS2 终端跑命令；路径用 /c/...。实战用 nasm 写汇编、gcc 编译 C、gdb 调试、readelf/objdump 解析 ELF、Cutter/Rizin 看二进制结构。
> 白帽红线（贯穿全案，违反即删除相关能力）：只练自己写的程序 / 授权靶场（Juice Shop、WebGoat、HTB、TryHackMe、picoCTF、自家虚拟机）/ 正规赏金计划 / CTF。绝不攻击他人系统、不登录他人账号、不爬受限内容、不绕实名监管、不逆向他人商业软件、不注入/Hook 他人进程。绝不编写或部署恶意工具（Mimikatz、Cobalt Strike 等只分析不实践）；VPN 只讲原理不教规避。任何实战必须在你自己控制的机器或明确授权范围内进行。
> 使用方法：每节先读 🎯→📋→🟢 建立直觉，再照 🟡 动手层逐字操作（能复制就复制），把结果发我批改，最后回头读 🔵🟣🔴 补理论。卡住随时喊"卡在第 X 步"。

---

# COMP1 · 冯·诺依曼 / 哈佛 体系结构

## 🎯 目标
学完这节，你能说清一台电脑"由哪几大件组成、数据怎么流起来"，能区分冯·诺依曼（普林斯顿）与哈佛两种经典结构，并能在自己电脑里指认出运算器、存储器、控制器、输入/输出设备的真实对应物。你将第一次建立"程序也是数据、和数据一起放在内存里"的底层直觉。

## 📋 小白前置
需先完成总纲预备层 PRE0（认识电脑：CPU、内存条、硬盘、键盘鼠标分别是什么、插在哪）。如果连内存条和硬盘都分不清，先回 PRE0 看实物图。本节约等于"把 PRE0 看到的零件用一根逻辑线串起来"。

## 🟢 部分一·最浅层（生活比喻，零术语）
想象一个超简单的"自动厨房"。厨房里有四样东西：① 一张大桌子（存储器，放食材也放菜谱）；② 一个会切会炒的厨师（运算器，真正干活）；③ 一个工头（控制器，盯着手表喊"下一步切洋葱"）；④ 门外的传菜口和订单口（输入/输出，客人下单、菜送出去）。冯·诺依曼结构的核心妙招是：**菜谱和食材放在同一张桌子上**。厨师想做下一道菜，工头就从桌上拿起下一张菜谱照做——这意味着"程序"（菜谱）和"数据"（食材）用同一块空间。哈佛结构则是"菜谱放一个架子、食材放另一张桌"，各走各的、互不抢地方。

## 🟡 部分二·动手层（逐字操作）
我们用 Windows 上的免费工具，指认你电脑里的"四大件"真实对应物。
1. 按 `Win`+`X`，点「设备管理器」，展开「处理器」——这就是"运算器+控制器"合体（CPU）。复制它的名字（例如 `Intel Core i5-xxxx`），待会写进笔记。
2. 还是设备管理器，展开「内存技术设备」或打开「任务管理器」(`Ctrl`+`Shift`+`Esc`)→「性能」→「内存」——这就是"存储器"（RAM，掉电就空的桌子）。记下容量（例如 16.0 GB）。
3. 「此电脑」右键→「属性」→ 看"系统类型"（64 位）——对应"数据总线宽度"。
4. 打开 MSYS2 终端（开始菜单搜 `MSYS2` 或 `C:\msys64\msys2.exe`），敲下面命令看 CPU 真身与缓存（缓存概念后面 COMP4 才细讲，这里只是"指认"）：
```bash
cat /proc/cpuinfo | head -n 20
```
预期能看到 `model name`、`cpu MHz`、`cache size` 等字段，这就是你的"厨师"档案。
5. 再看主板/桥芯片（输入输出的"走廊"）：
```bash
cat /proc/cpuinfo | grep -i 'flags' | head -n 1
```
这一长串 flags（如 `fpu vme de pse tsc msr pae mce cx8 ...`）是 CPU 支持的"技能清单"。

### 🧪 实战 Lab（COMP1）
新建笔记文件 `comp1笔记.txt`，写下四行：① 我的 CPU 型号=____；② 内存容量=____；③ 系统位数=____；④ 用一句话解释"为什么菜谱和食材放同一张桌子（冯·诺依曼）"。验收：把这四行发我即过关。

## 🔵 部分三·原理层（电脑里到底发生了什么）
冯·诺依曼结构的运转靠一个叫"取指-译码-执行"的循环（后面 COMP2 细讲 CPU 微架构）。控制器从内存地址 0 开始，取出第一条指令（比如"把 1 放进寄存器 A"），运算器照做，控制器再把地址+1 取下一条。因为指令本身也存在内存里，所以"边运行边改自己程序"在数学上可能（这也是后面 ASM/漏洞课的安全种子）。哈佛结构把指令和数据分两条总线，取指和取数可同时进行，速度快、且程序不会被当数据误改——所以**现代单片机（MCU）和 DSP 多用哈佛**，而**通用电脑用"改良冯·诺依曼"**：外面看是统一内存，内部 CPU 又用分开的指令缓存/数据缓存（COMP4）偷偷实现哈佛的好处。

## 🟣 部分四·深挖层（为什么这样设计、历史、顶级工程师才追问的点）
1945 年冯·诺依曼在《EDVAC 报告》里定下"存储程序"思想，之前 ENIAC 要换程序得拔插几千根线（等于重接电路），所以"程序=数据"是划时代的抽象。顶级工程师追问：统一内存带来"冯·诺依曼瓶颈"——CPU 太快、内存太慢，两者用一条总线挤着传，带宽成天花板。于是有了缓存（COMP4）、乱序执行（COMP2）、以及"改良哈佛"在 CPU 内部把指令/数据缓存分开。哈佛结构早期用在 Harvard Mark I（1944 机电计算机），今天活在每一个 ARM Cortex-M 单片机里。理解这个分野，你以后看任何芯片手册的"Harvard/Bus"字样都不再懵。

## 🔴 部分五·顶级视角（世界顶尖水平与相通领域）
顶级体系结构工程师（去翻 Hennessy & Patterson 的《Computer Architecture: A Quantitative Approach》，被奉为"体系结构圣经"）用"每秒能搬多少数据、每瓦特能干多少活"量化一切。冯/哈佛之分延伸到今天所有芯片：苹果的 M 系列用统一内存（类冯但极宽总线）喂饱 GPU；RISC-V 开源核让你自己改结构；缓存一致性（COMP7）、乱序（COMP2）都是在此地基上生长。它与编译器（CMP）、操作系统（OS）深度耦合——编译器要懂哈佛缓存才能排布代码，OS 要懂内存层次才能管页表。

## 🟠 部分六·安全/合规种子（白帽）
今天只是认识自己的硬件，绝对安全。种一颗种子：**"程序也是数据"意味着一段可执行的指令可以被当成普通数字来读、改、甚至从网络传过来**。这既是现代计算灵活的根，也是"代码注入""缓冲区溢出"类漏洞的物理根源——后面 ASM/OS/SEC 会系统讲，但前提是永远只在你自己写的程序、授权靶场里实验，绝不把外来数据当指令执行到他人系统上。

## ✅ 验收
交：① `comp1笔记.txt` 四行齐全；② 你能口头（或文字）区分冯·诺依曼与哈佛一句；③ 你能在自己电脑设备管理器指认 CPU/内存真实对应。三条齐算过关。

## ⚠️ 常见坑
- 把"内存(RAM)"和"硬盘(C盘)"混为一谈：内存是掉电空的快桌子，硬盘是永久仓库，冯·诺依曼说的"存储器"主要指 RAM。
- 以为 CPU 只有"运算"：其实 CPU = 运算器(ALU) + 控制器(CU) + 寄存器，控制器才是"工头"。
- 手疼就停，指认硬件用截图代替打字。

## ➡️ 下一步
COMP2 · CPU 微架构——深入"厨师"内部：流水线、超标量、乱序执行、分支预测，看一条指令怎么被拆成十几步飞快跑完。

### ⚔️ 闯关路线（COMP1）
- 🟢 Lv1：用画图或文字说明“存储程序”思想：指令也存内存、CPU 顺序取指 🥉🥉
- 🔵 Lv2：对比冯·诺依曼（指令数据同存）与哈佛（分开）的优劣 🥈🥈
- 🟣 Lv3：写出 CPU 取指-译码-执行-写回四步，并画循环图 🥇🥇
- 🟠 Lv4：解释现代 CPU 仍遵循冯氏循环、但加了流水线/缓存的演化 🏆🏆
- 🔴 Lv5：写 200 字说明“为什么理解五部件模型能帮你分析任何性能瓶颈”👑👑


---

# COMP2 · CPU 微架构（流水线 / 乱序 / 分支预测 / 超标量）

## 🎯 目标
学完这节，你能解释为什么"同样 3GHz，A 芯片比 B 芯片快一倍"，能说清流水线、超标量、乱序执行、分支预测四个概念各自解决什么问题，并能在自己机器上用工具看到这些特性真实存在。

## 📋 小白前置
需先懂 COMP1（知道 CPU 是运算器+控制器、指令存在内存）。建议顺带预习一点 ASM1（指令长什么样），但本节能独立看懂。

## 🟢 部分一·最浅层（生活比喻）
把 CPU 想成一条"洗车流水线"。最笨的做法：一辆车进来，一个人从头擦到尾，擦完下一辆才进——这叫"单发射串行"。流水线是：把擦车拆成"喷水→刷顶→刷侧→冲干"四工位，四辆车同时各占一个工位，每秒出车量翻 4 倍。超标量是"同时开两条流水线"，一次洗两辆。乱序执行是：前面那辆卡在"刷顶"等零件，后面的车不干等，先去冲干——只要不冲突就抢着干。分支预测是：洗车工猜"下一辆是轿车还是卡车"，提前把对应刷子备好；猜对了省时间，猜错了要重来（"分支预测失败惩罚"）。

## 🟡 部分二·动手层（逐字操作）
1. 打开 MSYS2 终端，看 CPU 支持的微架构特性（这些 flag 名字就是上面四个概念的证据）：
```bash
cat /proc/cpuinfo | grep -o -E 'sse|avx|sse4|fma|tsc|hypervisor' | sort -u
```
2. 用 gcc 写一段"故意让分支预测失效"的小程序，亲眼看到流水线被拖累。新建 `branch.c`：
```c
#include <stdio.h>
#include <time.h>
int main(){
    const int N = 200000000;
    int arr[N/10]; // 小一点防栈溢出，下面循环里取模制造不规则分支
    for(int i=0;i<N/10;i++) arr[i]= (i*2654435761u) % 100;
    long sum=0; clock_t t=clock();
    for(int k=0;k<10;k++)
      for(int i=0;i<N/10;i++)
        if(arr[i] > 50) sum += arr[i];  // 分支
    printf("sum=%ld time=%f\n", sum, (double)(clock()-t)/CLOCKS_PER_SEC);
    return 0;
}
```
3. 编译运行：
```bash
gcc -O2 branch.c -o branch && ./branch
```
记下时间。这就是分支预测在"帮忙"的结果（数据不规则但预测器仍猜对大部分）。（注：本机若 N 太大可把 `N` 调小；以能跑出正数结果为验收。）

### 🧪 实战 Lab（COMP2）
把上面 `if(arr[i] > 50)` 改成 `if(arr[i] > 0)`（几乎永远成立，预测器极容易猜对），重新编译运行，对比时间差异——你会直观感到"好预测的分支更快"。验收：把两次运行的时间数字发我。

## 🔵 部分三·原理层（指令在 CPU 里怎么被处理）
现代 CPU 把一条 `add` 指令拆成约 5 级流水：取指(IF)→译码(ID)→执行(EX)→访存(MEM)→写回(WB)。每级是一个独立工位，时钟一响各工位同时推进一格，理想吞吐≈每周期 1 条。**超标量**靠多个执行单元（整数 ALU、浮点、载入/存储分开）实现每周期发射多条。**乱序执行(OoO)**靠"保留站+ reorder buffer"：指令先入队，哪个操作数齐了就先跑，最后按原顺序提交结果，保证"对外看起来还是顺序的"。**分支预测**用 2-bit 饱和计数器/BHT/BPБ记录"这条分支上次跳没跳"，预测错了要清空流水线（pipeline flush），代价几十周期。

## 🟣 部分四·深挖层（为什么这样设计、历史、顶级追问）
1970s 流水线（IBM 360/91）和 1990s 乱序（Pentium Pro、Alpha 21064）是里程碑。顶级工程师追问：乱序窗口多大？寄存器重命名(Register Renaming)如何解决"假相关"（两条指令都用 eax 但本不冲突）？分支预测器如何用小历史表覆盖大程序？今天 CPU 还有"推测执行(Speculative Execution)"——预测分支会走就提前算，错了丢弃；这恰好是 Spectre/Meltdown 漏洞（SEC 方向会讲，白帽只分析原理）的物理土壤。功耗(功耗相关内容在 COMP1-7 收尾处贯穿)也在此：流水线越深、频率越高，漏电越多，于是有"动态调频/电压"(DVFS)。

## 🔴 部分五·顶级视角
读 Agner Fog 的《Microarchitecture》文档（免费、顶级工程师人手一份），他用实测画出每款 CPU 的流水线深度、端口数、预测器结构。苹果 M 系列靠"宽发射(每周期 8 发射)+大重排缓冲"在笔记本上碾压x86。RISC-V 的 BOOM 核是开源乱序实现，可自己改。这些概念直接决定你写出的代码快不快（后面 CMP7 优化、OS 调度都会回来用）。

## 🟠 部分六·安全/合规种子
推测执行/Spectre 是真实漏洞，但今天你只"理解原理、不写利用"。白帽底线：不在他人机器测微架构侧信道，不在未授权环境跑任何探测程序。自己机器上用 gcc 跑性能实验完全合规。

## ✅ 验收
交：① 两次 `branch.c` 运行时间截图/数字；② 你能用一句话分别说出流水线/超标量/乱序/分支预测各自"解决什么"。过关。

## ⚠️ 常见坑
- 以为 GHz 高就一定快：微架构（流水线宽不宽、预测准不准）往往比频率更关键。
- 栈上开 `int arr[200000000]` 会爆栈：用 `static` 或 `malloc`，本 Lab 已调小 N 规避。
- 手疼就复制命令，别手打长代码。

## ➡️ 下一步
COMP3 · 指令集 x86 / ARM / RISC-V——看 CPU"听得懂的菜谱语言"到底长啥样，三种体系的取舍为什么不同。

### ⚔️ 闯关路线（COMP2）
- 🟢 Lv1：查自己 CPU 型号，说明它是几核几线程 🥉🥉
- 🔵 Lv2：用比喻解释流水线：像工厂装配线分段并行 🥈🥈
- 🟣 Lv3：说明分支预测失败为何让流水线清空变慢 🥇🥇
- 🟠 Lv4：对比超标量（多发射）与乱序执行各自的提速点 🏆🏆
- 🔴 Lv5：写 200 字说明“CPU 微架构优化”如何关联你在方向1b 写的 C 代码性能👑👑


---

# COMP3 · 指令集架构 x86 / ARM / RISC-V（ISA 设计权衡）

## 🎯 目标
学完这节，你能说清"指令集(ISA)"是什么、为什么 x86 又老又胖却无处不在、ARM 凭什么统治手机、RISC-V 为什么是开源新星，并能用 nasm/gcc 在自己机器上看到不同 ISA 生成的真实机器码。

## 📋 小白前置
需先懂 COMP1（CPU 四大件）、COMP2（流水线让指令飞快跑）。最好已略看 ASM1（寄存器概念），但不强制。

## 🟢 部分一·最浅层（生活比喻）
指令集就像"厨师听得懂的菜谱语言"。x86 是那种"一道菜可以一句话搞定复杂操作"的老方言（比如一条指令就能"从内存取数再加到寄存器"），词儿多且杂；ARM 是"每条指令只做一件小事、但说得极标准"的新派菜谱，简单统一所以省电；RISC-V 是"开源免费、谁都能自己加词"的公版菜谱，像维基百科式的指令集。

## 🟡 部分二·动手层（逐字操作）
你的机器是 x86-64，我们用它生成并查看 x86-64 机器码；也顺带看 ARM/RISC-V 的"样子"（不交叉编译，只看汇编文本理解差异）。
1. 写 `add.c`：
```c
int add(int a, int b){ return a + b; }
```
2. 用 gcc 生成 x86-64 汇编（人类可读的"菜谱"）：
```bash
gcc -O2 -S add.c -o add.s
cat add.s
```
你会看到类似：
```
add:
  lea (%rdi,%rsi), %eax
  ret
```
这就是 x86-64 的 `add` 实现：把两个参数（rdi, rsi）相加放进 eax 返回。
3. 看"编译器怎么把同样的 C 翻成 ARM 风格"（用网站概念理解即可，本机不装 ARM 工具链也行；若装了 `aarch64-linux-gnu-gcc` 可试 `-S`）。重点记住：ARM 通常写成 `ADD W0, W0, W1`（三操作数，结果放 W0）。

### 🧪 实战 Lab（COMP3）
把 `add.c` 改成 `int mul(int a,int b){return a*b;}` 再 `gcc -O2 -S` 看汇编，找到乘法指令（x86 里是 `imul`）。验收：把 `add.s` 和 `mul.s` 里"实现运算的那一两行"发我，并标注"x86 是二地址风格（结果常覆盖一个源），ARM 是三地址风格"——能说清这点就过关。

## 🔵 部分三·原理层（ISA 到底规定了什么）
ISA 是"软件与硬件的契约"，规定：① 有哪些寄存器、多宽；② 指令的二进制编码（opcode）；③ 寻址方式（操作数从哪来）；④ 特权级（后面 OS10 系统调用会讲 ring 0/3）；⑤ 调用约定（ASM3）。x86 是 CISC（复杂指令集），一条指令能干多步、变长编码（1–15 字节），历史包袱重但兼容极佳；ARM/RISC-V 是 RISC（精简指令集），指令定长、单周期、靠编译器多排几条简单指令拼出复杂效果，流水线好设计、省电。

## 🟣 部分四·深挖层（设计权衡、历史、顶级追问）
x86 的胜利是"生态+二进制兼容"：从 1978 的 8086 一路兼容到今天，Intel/AMD 内部把 CISC 译码成微指令(μop)再走 RISC 式流水线（呼应 COMP2）。ARM 靠"低功耗+授权模式(卖设计不卖芯片)"拿下手机。RISC-V(2010 加州伯克利)用 BSD 开源、模块化（基础指令+I/M/A/F/D 扩展）让学术和 IoT 自由定制。顶级工程师追问：定长指令对缓存友好但编码效率低；变长灵活但译码难。IBM POWER、MIPS 也都曾辉煌。理解 ISA 权衡，是你读懂任何 CPU 手册的钥匙。

## 🔴 部分五·顶级视角
顶级体系结构师在 RISC-V 上做"领域定制指令"（如加 AI 矩阵乘指令），这是今天芯片竞赛前沿（谷歌 TPU 思路同源）。你将来若做二进制逆向(SEC25/27)，面对的就是 x86/ARM 机器码——本节是地基。开源项目：QEMU(模拟任意 ISA)、riscv-gnu-toolchain(自己编 RISC-V 程序)。

## 🟠 部分六·安全/合规种子
ISA 本身中性。白帽注意：逆向他人商业软件违反红线；但你在自己机器上用 `gcc -S` 看自己写的 C 的汇编，是合法学习。RISC-V 开源生态更是"只练自己/开源"的典范。

## ✅ 验收
交：① `add.s`/`mul.s` 关键行；② 一句话说清 CISC vs RISC 核心区别；③ 能举 x86/ARM/RISC-V 各一例说明其市场定位。过关。

## ⚠️ 常见坑
- 把"指令集"和"微架构"搞混：ISA 是契约（菜谱语言），微架构(COMP2)是实现（厨房怎么布置）。同一 x86 ISA，Intel 和 AMD 微架构不同。
- 看汇编害怕：本节只需"认出 add/imul"即可，细节后面 ASM 逐条学。
- 护手：复制 `gcc -O2 -S add.c -o add.s` 这行，别手打。

## ➡️ 下一步
COMP4 · 内存层次（SRAM/DRAM/缓存/TLB）——为什么"内存很慢"却感觉很快，CPU 怎么用一层层缓存骗过你。

### ⚔️ 闯关路线（COMP3）
- 🟢 Lv1：列举 x86 / ARM / RISC-V 各一个代表设备 🥉🥉
- 🔵 Lv2：用比喻说明 CISC（指令多而杂）vs RISC（精简规整）🥈🥈
- 🟣 Lv3：解释 RISC-V 开源ISA为何对安全研究重要 🥇🥇
- 🟠 Lv4：对比同一条加法在 x86 与 ARM 的指令形态差异 🏆🏆
- 🔴 Lv5：写 200 字说明“ISA 是软硬件契约”，及白帽逆向要懂目标 ISA👑👑


---

# COMP4 · 内存层次（SRAM / DRAM / 缓存 / TLB）

## 🎯 目标
学完这节，你能画出"寄存器→L1→L2→L3→内存→硬盘"的速度/容量金字塔，说清 SRAM 与 DRAM 的区别、缓存命中/缺失、以及 TLB 是干嘛的，并能在自己机器上用工具看到各级缓存的真实大小。

## 📋 小白前置
需先懂 COMP1（存储器=内存）、COMP3（寄存器在 ISA 里）。知道"越快越贵、越小"的常识即可。

## 🟢 部分一·最浅层（生活比喻）
想象你办公：手里正在写的便签(寄存器，最快最小)→ 桌上文件夹(L1缓存)→ 身后文件柜(L2/L3)→ 楼下仓库(内存DRAM)→ 城外档案馆(硬盘)。你(CPU)绝不愿老跑档案馆，所以把常用的纸一层层往桌上搬。缓存就是"桌上那几叠最常用的文件"；命中=要的就在桌上，缺失=得下楼跑一趟，慢几百倍。

## 🟡 部分二·动手层（逐字操作）
1. 在 MSYS2 看各级缓存大小（Linux 风格接口在 MSYS2 也可用）：
```bash
cat /proc/cpuinfo | grep -i 'cache size'
```
2. 更直接看缓存拓扑：
```bash
ls /sys/devices/system/cpu/cpu0/cache/ 2>/dev/null && echo "---" && for l in 0 1 2 3; do echo "level $l:"; cat /sys/devices/system/cpu/cpu0/cache/index$l/level 2>/dev/null; cat /sys/devices/system/cpu/cpu0/cache/index$l/size 2>/dev/null; done
```
（注：MSYS2 下 `/sys` 可能不全，若空则用 Windows 任务管理器"性能→CPU→L1/L2/L3"截图替代，同样算验收。）
3. 写个程序体验"缓存命中快、缺失慢"：新建 `cache_demo.c`：
```c
#include <stdio.h>
#include <time.h>
#define N 4096*1024
int arr[N];
int main(){
    long sum=0; clock_t t=clock();
    for(int i=0;i<N;i+=1) sum+=arr[i];      // 顺序访问，缓存友好
    printf("seq time=%f sum=%ld\n",(double)(clock()-t)/CLOCKS_PER_SEC,sum);
    t=clock(); sum=0;
    for(int i=0;i<N;i+=64) sum+=arr[i];     // 大步跳，仍友好(同缓存行内)
    printf("step time=%f\n",(double)(clock()-t)/CLOCKS_PER_SEC);
    return 0;
}
```
```bash
gcc -O2 cache_demo.c -o cache_demo && ./cache_demo
```

### 🧪 实战 Lab（COMP4）
把循环改成"随机大步跳"（用 `i = (i*12347)%N` 这种伪随机索引）对比顺序访问时间，体验缓存缺失的代价。验收：把顺序 vs 跳变的两次耗时发我，并写一句"缓存友好=连续访问"。

## 🔵 部分三·原理层（金字塔怎么工作）
寄存器是 CPU 内的 SRAM，1 周期访问；L1/L2/L3 也是 SRAM（六管存储单元，断电丢、极快、贵）；主存是 DRAM（电容存位，需刷新，慢 100–300 倍）；硬盘是 NAND/磁盘（慢百万倍）。缓存用"局部性原理"（时间局部性：刚用的马上再用；空间局部性：用的附近马上用）做"预取+缓存行(64字节)"搬运。TLB(快表)是把"虚拟地址→物理地址"的页表项也缓存起来（OS5/6 细讲），避免每次地址转换都查内存页表。

## 🟣 部分四·深挖层（为什么分层、顶级追问）
成本/速度不可兼得：SRAM 每比特 6 晶体管、DRAM 1 晶体管+1电容，所以 SRAM 贵而小。顶级工程师追问：缓存多大最划算？"缓存一致性"怎么在多核间维护(COMP7 MESI)？"伪共享"为什么拖慢并行？写策略（写直达 vs 写回）怎么选？预取器怎么猜下一步要哪块？这些直接决定程序性能，也是 CMP7 优化与 OS 调度的核心话题。还有"缓存侧信道"(Spectre)——白帽只分析。

## 🔴 部分五·顶级视角
顶级性能工程师用 `perf`（Linux）或 VTune（Intel）看缓存命中率(L1-dcache-load-misses 等)，把热点循环重排成"缓存友好"。NUMA（非一致内存访问）在服务器多路 CPU 上让"内存离哪个 CPU 近"也成性能因素。读《What Every Programmer Should Know About Memory》(Ulrich Drepper) 是必读经典。

## 🟠 部分六·安全/合规种子
缓存侧信道攻击(Cache Bombing/Prime+Probe)是真实存在的研究课题，但白帽只"理解原理、不写利用、不在他人机器测"。自己机器跑 `cache_demo.c` 完全合规。

## ✅ 验收
交：① 你机器各级缓存大小(命令输出或任务管理器截图)；② 顺序 vs 跳变耗时对比；③ 一句话说清 SRAM/DRAM 区别。过关。

## ⚠️ 常见坑
- "缓存越大越好"是错的：性价比有拐点，且缓存占 CPU 面积挤占计算单元。
- 把"虚拟内存(OS6)"和"缓存"混：缓存解决速度，虚拟内存解决"地址空间隔离与扩容"。
- 手疼复制命令，别手打长数组。

## ➡️ 下一步
COMP5 · 总线与 I/O（DMA / 中断 / MMIO）——数据怎么在 CPU、内存、硬盘、网卡之间"走高速路"而不让 CPU 干等。

### ⚔️ 闯关路线（COMP4）
- 🟢 Lv1：查自己电脑内存容量与缓存层级（L1/L2/L3）🥉🥉
- 🔵 Lv2：用比喻说明 SRAM（快贵小）vs DRAM（慢廉大）🈶🥈
- 🟣 Lv3：解释 TLB 是“页表缓存”，未命中要查多级页表 🥇🥇
- 🟠 Lv4：说明“局部性原理”如何让缓存命中率高 🏆🏆
- 🔴 Lv5：写 200 字说明“缓存友好代码”为何比算法优化还快（接方向1b C15）👑👑


---

# COMP5 · 总线与 I/O（DMA / 中断 / MMIO）

## 🎯 目标
学完这节，你能说清"总线"是什么、CPU 怎么和键盘/硬盘/网卡对话、DMA 为什么能让 CPU 不搬家也能搬数据、中断和"轮询"有何不同、MMIO 又是怎么回事，并能用工具看到你机器上的中断分布。

## 📋 小白前置
需先懂 COMP1（四大件）、COMP4（内存是数据暂存）。知道"外设"指键盘/鼠标/硬盘/网卡/显示器即可。

## 🟢 部分一·最浅层（生活比喻）
总线就是"公司里的主走廊+内部电话线"，所有部门（CPU、内存、硬盘、网卡）都挂上面。最笨的 I/O：CPU 亲自守在门口，每秒问"有信没？有信没？"(轮询)——累死。聪明做法：装个门铃，信到了"叮"一声(中断)，CPU 才放下手头活去取(中断处理)。DMA 更绝：让"快递员( DMA 控制器)"自己把仓库货搬上卡车送走，CPU 只发一句"去搬"，全程睡觉。

## 🟡 部分二·动手层（逐字操作）
1. 看中断控制器记录（你的机器上外设都靠中断"叫门"）：
```bash
cat /proc/interrupts | head -n 20
```
你会看到 `CPU0  CPU1 ...` 列和 `LOC`、`reschedule`、`键盘/鼠标(IRQ1/12 类)` 等行——这就是"谁在按门铃"。
2. 看 PCI 设备（挂在总线上的"部门"）：
```bash
cat /proc/bus/pci/devices 2>/dev/null | head; echo "---lspci 替代---"; lspci 2>/dev/null | head
```
（MSYS2 下可能无 lspci，可用 Windows「设备管理器→系统设备」截图替代。）
3. 用 C 演示"忙等轮询 vs 中断"的概念区别（只写伪代码理解，不真写驱动）：
```c
// 轮询：CPU 一直问
while (status_register & READY == 0) { /* 空转 */ }
data = data_register;
// 中断：注册处理函数，硬件就绪时自动跳进来
// (真实中断需内核权限，用户态只理解思路)
```

### 🧪 实战 Lab（COMP5）
打开 Windows「设备管理器→查看→依类型排序设备→系统设备」，截图数一数你机器上有多少"PCI 总线/中断控制器/直接内存访问控制器(DMA)"，写一句"DMA 控制器存在说明我的电脑用 DMA 搬数据"。验收：截图+这句。

## 🔵 部分三·原理层（数据怎么流动）
CPU 通过"端口 I/O"或"MMIO(内存映射 I/O)"与外设通信：MMIO 把外设寄存器映射到一段物理地址，CPU 像访问内存一样 `mov` 读写它（例如写某地址=让网卡发数据）。中断流程：外设拉 IRQ 线→中断控制器(APIC)转发→CPU 查"中断向量表"跳到对应 ISR(中断服务程序)→处理完 `iret` 返回。DMA：CPU 告诉 DMA 控制器"把磁盘第 X 块搬到第 Y 内存地址"，控制器直接在内存和外设间搬，搬完发中断通知 CPU——全程 CPU 可去做别的事。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
早期 PC 用 ISA 总线(慢、共享)，后来 PCI、PCIe(串行差分、按 lane 倍增带宽)、USB(统一外设)、NVMe(绕过老接口直连 PCIe)。顶级工程师追问：总线带宽怎么算(位宽×频率/8)？中断亲和性(把某中断绑某核)怎么减延迟？MSI-X 怎么让每个队列独立中断？"中断风暴"为何能让系统卡死？这些在 OS9 I/O 模型、OS17 网络栈里都会回来。ARM 的 AMBA/AXI、RISC-V 的 TileLink 是 SoC 内部总线标准。

## 🔴 部分五·顶级视角
写设备驱动(OS14 LKM)的人必须吃透总线与中断；做高性能网络(DPDK、OS9 io_uring)靠"轮询+忙等+绕过中断"反其道而行之换取极致吞吐。顶级系统工程师在 NUMA+PCIe 拓扑上精算数据路径。开源：Linux 内核 `drivers/` 目录是总线/I/O 实现的最佳教材。

## 🟠 部分六·安全/合规种子
中断/DMA 是硬件级能力，用户态程序动不了，写驱动只在自己虚拟机/授权内核模块实验(OS14)。白帽底线：不往他人机器加载任何驱动/钩子；DMA 攻击(如 Thunderbolt DMA 窃取内存)只了解原理用于防御认知。

## ✅ 验收
交：① `/proc/interrupts` 前 20 行或设备管理器截图；② 一句话区分轮询/中断/DMA；③ 能说 MMIO"把外设当内存地址访问"。过关。

## ⚠️ 常见坑
- 以为"中断就是多线程"：中断是硬件异步打断，和线程调度(OS2)不同但相关。
- 用户态写不出真中断处理：本节理解概念即可，真写要内核权限(OS14)。
- 护手：截图代替打字笔记。

## ➡️ 下一步
COMP6 · SIMD / 向量化（AVX / 数据并行）——一条指令同时算 8/16/32 个数，看 CPU 怎么"一锅出"。

### ⚔️ 闯关路线（COMP5）
- 🟢 Lv1：用比喻说明总线像“主干道”、DMA 像“快递绕过 CPU 直达内存”🥉🥉
- 🔵 Lv2：解释中断：外设举牌打断 CPU 处理 🈶🥈
- 🟣 Lv3：说明 MMIO（内存映射 I/O）如何让寄存器像内存地址访问 🥇🥇
- 🟠 Lv4：对比轮询 vs 中断 vs DMA 三种 I/O 策略取舍 🏆🏆
- 🔴 Lv5：写 200 字说明“中断与系统调用”的共通机制（接 OS10）👑👑


---

# COMP6 · SIMD / 向量化（AVX / 数据并行）

## 🎯 目标
学完这节，你能说清什么是 SIMD（单指令多数据）、为什么图像处理/AI/加密离不开它，认出 AVX/AVX2/AVX-512 这些词，并能在自己机器用 gcc 的自动向量化或内建指令写出"一次算 8 个浮点"的程序。

## 📋 小白前置
需先懂 COMP2（超标量是"多指令"，SIMD 是"一指令管多数据"，对比着学）、COMP3（指令集里 SIMD 是扩展指令）、COMP4（数据在内存连续摆放才好向量化）。

## 🟢 部分一·最浅层（生活比喻）
普通指令像"一次切一片面包"；SIMD 像"用带 8 个刀片的大刀，一刀下去同时切 8 片"。面包要并排摆齐（数据连续）才切得动。图像有上百万像素、AI 有上亿参数，如果每个都单独切，累死；SIMD 就是那把多刃刀。

## 🟡 部分二·动手层（逐字操作）
1. 先看你的 CPU 支持哪些 SIMD（AVX 等）：
```bash
cat /proc/cpuinfo | grep -o -E 'avx2|avx512f|avx|sse4_2|fma' | sort -u
```
看到 `avx2` 就说明支持一次 256 位（8 个 float）并行。
2. 写 `simd.c` 用 gcc 自动向量化求和：
```c
#include <stdio.h>
#define N 1000000
float a[N], b[N], c[N];
int main(){
    for(int i=0;i<N;i++){ a[i]=i; b[i]=i*2; }
    for(int i=0;i<N;i++) c[i]=a[i]+b[i];   // 编译器很可能自动向量化
    printf("c[999]=%f\n", c[999]);
    return 0;
}
```
3. 编译并让 gcc 告诉你"我向量化了吗"：
```bash
gcc -O3 -mavx2 -fopt-info-vec simd.c -o simd 2>&1 | head
./simd
```
若输出里有 `vectorized N loops` 之类，说明 8 个 float 一次算完。

### 🧪 实战 Lab（COMP6）
用 GCC 内建函数显式写向量加法（不用手写汇编也行，用 `<immintrin.h>`）：
```c
#include <immintrin.h>
#include <stdio.h>
int main(){
    float x[8]={1,2,3,4,5,6,7,8}, y[8]={10,20,30,40,50,60,70,80}, z[8];
    __m256 va=_mm256_loadu_ps(x), vb=_mm256_loadu_ps(y);
    __m256 vz=_mm256_add_ps(va,vb);
    _mm256_storeu_ps(z,vz);
    printf("%f %f %f %f\n", z[0],z[1],z[2],z[3]);
    return 0;
}
```
编译 `gcc -O3 -mavx2 vec.c -o vec && ./vec`，应打印 `11 22 33 44`。验收：把输出发我。

## 🔵 部分三·原理层（一条指令怎么算 8 个）
SIMD 寄存器很宽（MMX 64位→SSE 128位→AVX 256位→AVX-512 512位）。一条 `vaddps`(AVX 加 8 个单精度)把 256 位寄存器当 8 个 32 位槽，ALU 内 8 个加法器并行。编译器自动向量化靠"循环里每个迭代独立、数据连续"才能安全并行；若有"本次依赖上次结果"就不敢向量化。手动用 intrinsic(如 `_mm256_add_ps`)等于直接发 SIMD 指令，但仍是 C 写法，不用写汇编。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
Intel MMX(1997)→SSE→AVX 演进。顶级追问：AVX-512 宽但"降频"(宽寄存器发热，整颗 CPU 降频)是否划算？"向量化失败"的常见原因(循环里有 if 分支、指针可能重叠用 `restrict` 告知)？ARM 的 NEON/SVE、RISC-V 的 V 扩展怎么设计可变长向量？GPU 本质是"超大规模 SIMD"(数千线程同时做同指令)——所以 AI 训练吃 SIMD 思维。

## 🔴 部分五·顶级视角
高性能计算(HPC)、编解码(ffmpeg 用 SIMD 加速)、加密(AES-NI)、科学计算(BLAS 库)全靠 SIMD。顶级工程师用 `objdump -d` 看编译器生成的 `vaddps`/`vmulps` 验证向量化；用 `-march=native` 让 gcc 榨干本机 SIMD。开源：x264/x265、OpenBLAS、Intel ISPC 编译器。

## 🟠 部分六·安全/合规种子
SIMD 纯计算优化，无安全风险。白帽视角：某些加密侧信道研究会看"常量时间"实现，避免使用依赖数据的分支——这属于防御性知识，自己机器练 SIMD 完全合规。

## ✅ 验收
交：① `vec.c` 输出 `11 22 33 44` 截图；② 你能说清 SIMD 与超标量(COMP2)区别（一指令多数据 vs 多指令并行）；③ 一句话说 AVX2 一次处理几个 float(8)。过关。

## ⚠️ 常见坑
- 忘记 `-mavx2` 编译选项：老默认可能不生成 AVX，intrinsic 编译报错或退回慢路径。
- 数据不连续(结构体数组分散)导致无法向量化：用"数组的结构(AoS)→结构的数组(SoA)"重排。
- 护手：复制 `gcc -O3 -mavx2 vec.c -o vec && ./vec`。

## ➡️ 下一步
COMP7 · 多核 / 缓存一致性（MESI / 伪共享）——多核同时干活时，各自的缓存怎么"对账"，为什么有时反而更慢。

### ⚔️ 闯关路线（COMP6）
- 🟢 Lv1：用 NumPy 或手动演示“一次算 4 个数”的向量化思想 🥉🥉
- 🔵 Lv2：说明 SIMD（单指令多数据）相比标量循环的加速 🈶🥈
- 🟣 Lv3：用比喻解释 AVX 寄存器能装 8 个 float 并行算 🥇🥇
- 🟠 Lv4：写一段 C 用编译器自动向量化（-O3）跑分对比 🏆🏆
- 🔴 Lv5：写 200 字说明“数据并行”在 AI/多媒体/加密中的核心地位👑👑


---

# COMP7 · 多核 / 缓存一致性（MESI / 伪共享）

## 🎯 目标
学完这节，你能说清为什么"多核各自有缓存会出错"、MESI 协议怎么解决、什么是伪共享(False Sharing)以及怎么避开，并能写个小程序制造/缓解伪共享，亲眼看到性能差别。

## 📋 小白前置
需先懂 COMP4（多级缓存）、COMP2（超标量/乱序）、COMP3（多核时代 ISA 含原子指令）。知道"线程(OS1)"概念更好，本节会用到但能独立理解。

## 🟢 部分一·最浅层（生活比喻）
两个厨师各有一份"今日特价"便签(L1缓存)。顾客改了总部的价，厨师 A 改了自己便签，但厨师 B 的便签还是旧的——两人报价不一致，乱套了。MESI 就是"两人便签上贴状态贴纸"的规矩：Modified(我改了,别人的是脏的)/Exclusive(只有我有且干净)/Shared(多人都有,干净)/Invalid(我的作废了,得重取)。伪共享则是：两人便签其实写的是不同菜，但倒霉被印在同一张纸上(同一缓存行 64 字节)，一人改→整张纸作废→另一人也得重抄，白白浪费。

## 🟡 部分二·动手层（逐字操作）
1. 看核心数：
```bash
nproc
cat /proc/cpuinfo | grep -c 'processor'
```
2. 制造伪共享并对比。新建 `false_sharing.c`：
```c
#include <stdio.h>
#include <pthread.h>
#include <time.h>
#define N 100000000
struct { volatile unsigned long a; } s[2];   // 两个字段常在同一缓存行→伪共享
void* work0(void* p){ for(unsigned long i=0;i<N/2;i++) s[0].a++; return 0; }
void* work1(void* p){ for(unsigned long i=0;i<N/2;i++) s[1].a++; return 0; }
int main(){
    pthread_t t0,t1; clock_t t=clock();
    pthread_create(&t0,0,work0,0); pthread_create(&t1,0,work1,0);
    pthread_join(t0,0); pthread_join(t1,0);
    printf("time=%f\n",(double)(clock()-t)/CLOCKS_PER_SEC);
    return 0;
}
```
3. 编译运行（两个线程各改一个字段，但因同缓存行互相 invalidate）：
```bash
gcc -O2 -pthread false_sharing.c -o fs && ./fs
```
4. 修复：把两字段隔开到不同缓存行（加 padding）：
```c
struct { volatile unsigned long a; char pad[64]; } s[2];  // 各占独立缓存行
```
重新编译运行，时间应明显下降。

### 🧪 实战 Lab（COMP7）
对比"伪共享版"与"padding 版"的 `./fs` 耗时，发我两个数字，并写"padding 让两线程不再抢同一缓存行"。验收：两数字+这句。

## 🔵 部分三·原理层（MESI 怎么跑）
每个缓存行有 M/E/S/I 状态。核 A 读一行→E(独占)；核 B 也读→都变 S(共享,干净)；A 写→A 变 M 并向总线发"无效化"消息让 B 那行变 I；B 再读→从 A 或内存取最新→S。这靠"总线嗅探(snooping)"或"目录协议(directory)"在核间广播/查表。atomic 指令(如 `lock xadd`)底层要拿"缓存行独占"再改，故原子操作有缓存一致性开销。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
多核时代(2000s 起)让一致性成刚需。顶级追问：核数上到几十上百，广播总线撑不住→用目录协议/分片；NUMA 下跨节点一致性更贵。伪共享是并行程序的隐形杀手，顶级工程师用 `perf c2c` 工具定位。还有"true sharing"(真要共享同一变量)的锁竞争、内存模型(OS3/CMP 会遇 x86 TSO vs ARM 弱内存序)——写出正确又快的并行代码极难，这是系统编程的巅峰话题。

## 🔴 部分五·顶级视角
顶级并发工程师精算"缓存行边界""false sharing""memory ordering"。Linux 内核用 `____cacheline_aligned` 宏对齐；Java 的 `@Contended`、C++ 的 `alignas(64)` 都是为此。读《Is Parallel Programming Hard?》(Paul McKenney)免费开源书。这与 OS3 同步原语、OS7 物理内存、CMP8 代码生成(寄存器分配)全部咬合。

## 🟠 部分六·安全/合规种子
本节是纯性能/正确性话题，无攻击面。白帽注意：多核竞态本身可被恶意利用成"竞态条件漏洞"(SEC40 TOCTOU)，但那是别人程序的 bug；你只在自己代码里练并行，理解原理用于写出健壮程序。

## ✅ 验收
交：① 伪共享版 vs padding 版耗时；② 能说清 MESI 四个字母含义；③ 一句话解释伪共享。过关。

## ⚠️ 常见坑
- 以为"多开线程一定快"：伪共享/锁竞争可让多线程比单线程还慢。
- `volatile` 不保证原子也不保证跨线程可见性顺序（C11 `_Atomic` 才对）——本节用 volatile 仅为制造效果，真并行要用 C11 atomics 或 OS3 原语。
- 护手：复制代码块，别手打长循环。

## ➡️ 下一步
ASM1 · x86-64 寄存器与寻址——正式动手写汇编：认识 rax/rbx/rcx... 这些"CPU 里的小抽屉"和怎么从内存取数。

### ⚔️ 闯关路线（COMP7）
- 🟢 Lv1：用比喻说明多核像“多个厨师并行做菜”🥉🥉
- 🔵 Lv2：解释缓存一致性问题：两核各改副本导致不一致 🈶🥈
- 🟣 Lv3：说明 MESI 四状态（修改/独占/共享/无效）如何协调 🥇🥇
- 🟠 Lv4：演示伪共享：两线程改同缓存行不同字段反而变慢 🏆🏆
- 🔴 Lv5：写 200 字说明“缓存一致性协议”是多线程正确的地基（接方向1b CPP7）👑👑


---

# ASM1 · x86-64 寄存器与寻址

## 🎯 目标
学完这节，你能用 nasm 写出一个最小的 x86-64 汇编程序并在本机编译运行，认全通用寄存器（rax/rbx/rcx/rdx/rsi/rdi/r8–r15）、知道段寄存器与 SIMD 寄存器（xmm/ymm）是干嘛的，并理解"寄存器寻址/立即数/内存寻址"三种取数方式。

## 📋 小白前置
需先懂 COMP1–COMP3（知道 CPU、指令、ISA 是什么）。会开 MSYS2 终端、会 `gcc` 基本编译（PRE6 终端基础）。最好先翻过 COMP3 看到的 `add.s` 汇编片段，不陌生即可。

## 🟢 部分一·最浅层（生活比喻）
CPU 内部有一排"贴了名字的小抽屉"，叫寄存器——比内存(大仓库)近得多，厨师(CPU)伸手就拿到，最快。rax 是"主算盘"，rbx 是"能存东西的备用抽屉"，rcx 常当"计数器(数到几)"，rdx 当"辅助算盘"。还有 rdi/rsi 是"传参数的手"(第 1、2 个参数)，r8–r15 是额外的手。段寄存器(cs/ds/ss)像"仓库分区标签"，现代 64 位下基本不用你管；xmm/ymm 是"宽抽屉"，专门给 SIMD(COMP6)一次塞 4/8 个数。

## 🟡 部分二·动手层（逐字操作）
1. 确认 nasm 已装（MSYS2 里装：`pacman -S mingw-w64-x86_64-nasm`，若已装跳过）：
```bash
nasm -v
```
2. 新建 `hello.asm`（Linux 风格的 syscall 版，用 GCC 链接成可运行文件）：
```nasm
section .data
    msg db "Hello, ASM!", 10
    len equ $ - msg
section .text
    global _start
_start:
    mov rax, 1          ; syscall 号 1 = write
    mov rdi, 1          ; fd = 1 (stdout)
    mov rsi, msg        ; 缓冲区
    mov rdx, len        ; 长度
    syscall
    mov rax, 60         ; syscall 60 = exit
    mov rdi, 0          ; 退出码 0
    syscall
```
3. 编译链接运行（MSYS2 的 `ld` 可能需 `-o` 指定输出，且用 GCC 链接更稳）：
```bash
nasm -f elf64 hello.asm -o hello.o
gcc -nostdlib hello.o -o hello
./hello
```
预期打印：`Hello, ASM!`

### 🧪 实战 Lab（ASM1）
把 `msg db "Hello, ASM!", 10` 改成 `msg db "泽泽加油!", 10`，重新编译运行，看到中文需终端支持 UTF-8（MSYS2 默认支持）。验收：把运行输出截图发我，并写出"rax 装 syscall 号、rdi/rsi/rdx 装参数"这句话。

## 🔵 部分三·原理层（寄存器与寻址怎么工作）
x86-64 有 16 个 64 位通用寄存器（r0–r15 的别名：rax,rbx,rcx,rdx,rsi,rdi,rbp,rsp,r8–r15）。它们的"低 32 位"叫 eax 等，"低 8 位"叫 al/ah。"寻址"三种：① 立即数 `mov rax, 5`（数字直接写死）；② 寄存器 `mov rax, rbx`（抽屉到抽屉）；③ 内存 `mov rax, [msg]`（去仓库某地址取）。内存地址可计算：`[rax + rbx*4 + 8]`（基址+变址×比例+偏移），这是数组/结构体访问的硬件基础(ASM4)。

## 🟣 部分四·深挖层（为什么这样命名、顶级追问）
历史：8086 只有 ax/bx/cx/dx（16位），386 扩展到 32 位加 e（extended），AMD64 扩到 64 位加 r，并新增 r8–r15。命名约定：a=accumulator(累加)、b=base(基址)、c=counter(计数)、d=data。顶级追问：为什么调用约定(ASM3)规定 rdi,rsi,rdx,rcx,r8,r9 传前 6 参数？因为 CPU 设计时就为"快速取前几个参数"优化。SIMD 寄存器 xmm(128)/ymm(256)/zmm(512) 对应 SSE/AVX/AVX-512(COMP6)。段寄存器在 64 位下"平坦模型"几乎透明，但 fs/gs 仍用于线程局部存储(TLS)。

## 🔴 部分五·顶级视角
逆向工程师(SEC25/27)面对的二进制，第一步就是"认寄存器、还原数据流"。编译器生成的汇编里，每个寄存器扮演特定角色。顶级系统程序员手写汇编优化热点(如 memcpy、加密轮函数)。开源项目：Linux 内核 `arch/x86/entry/` 满是汇编；glibc 的 `syscall` 封装。Rizin/Cutter 能直接把机器码反汇编成你刚学的 `mov/rax` 形式。

## 🟠 部分六·安全/合规种子
写自己的汇编、在自己的机器编译运行，是 100% 合法白帽练习。注意：Shellcode(ASM7)相关技术只用于理解"为什么需要 DEP/ASLR(SEC44)"等防御，绝不把 shellcode 注入任何非自己/非授权程序。Mimikatz 等只分析不实践。

## ✅ 验收
交：① `hello.asm` 运行输出截图；② 列出你能记住的至少 6 个寄存器名及作用；③ 说出三种寻址方式。过关。

## ⚠️ 常见坑
- `nasm -f elf64` 在 Windows 上产出 ELF，需用 `gcc -nostdlib` 链接（不直接 `ld` 也行，但 GCC 更省心）。
- 忘记 `_start` 的 `global`，链接报"undefined reference to _start"。
- 中文串长度 `len equ $ - msg` 只对 ASCII 准确；含多字节 UTF-8 时长度按字节算仍正确（字节数），但显示正常。
- 护手：复制 .asm 代码块，不手打。

## ➡️ 下一步
ASM2 · 算术与逻辑指令——动手做加减乘除、位运算，看清"标志位"这个 CPU 偷偷记的小账本怎么影响后面的判断。

### ⚔️ 闯关路线（ASM1）
- 🟢 Lv1：在 MSYS2 用 gcc -S 把一个 C 函数编出汇编，认出寄存器名 🥉🥉
- 🔵 Lv2：指出 rax/rbx/rcx/rdx 等通用寄存器的作用分工 🈶🥈
- 🟣 Lv3：解释 RIP（指令指针）与 RSP（栈指针）为何特殊 🥇🥇
- 🟠 Lv4：对比 x86-64 与 ARM64 寄存器数量/命名差异 🏆🏆
- 🔴 Lv5：写 200 字说明“懂寄存器=能看懂反汇编”，是逆向基础（接方向7b）👑👑


---

# ASM2 · 算术 / 逻辑指令（标志位 / 运算）

## 🎯 目标
学完这节，你能用 nasm 写加法、减法、乘法、位运算（and/or/xor/not/shift），理解"标志寄存器 RFLAGS"里 ZF/CF/SF/OF 几个关键位怎么被设置、又怎么被条件跳转读到，并写出一个"判断两数相等/大小"的汇编小程序。

## 📋 小白前置
需先懂 ASM1（寄存器、mov、syscall 输出）。知道二进制(0/1)和"位"概念（PY3 字符串与编码里提过补码，本节用到）。

## 🟢 部分一·最浅层（生活比喻）
算术指令就是厨师做加减乘。标志位(Flags)像厨师手里的小便签，记下"刚才这步算出来是 0 吗？(ZF)""溢出了吗？(OF)""有进位吗？(CF)""结果是负数吗？(SF)"。后面的"如果…就跳"(条件跳转)就是看这些小便签决定下一步走哪条路——没有标志位，CPU 就不会"判断"。

## 🟡 部分二·动手层（逐字操作）
1. 新建 `calc.asm`，做几道运算并用 syscall 输出结果（为简单，我们把结果转成 ASCII 打印思路略复杂，这里改用"退出码"验证，用 `echo $?` 看）：
```nasm
section .text
    global _start
_start:
    mov rax, 10
    mov rbx, 5
    add rax, rbx        ; rax = 15
    sub rax, 3          ; rax = 12
    xor rbx, rbx        ; rbx = 0 (惯用清零技巧)
    mov rax, 60
    mov rdi, 12         ; 退出码=12 代表结果
    syscall
```
```bash
nasm -f elf64 calc.asm -o calc.o && gcc -nostdlib calc.o -o calc && ./calc; echo "退出码=$?"
```
预期 `退出码=12`。
2. 看标志位影响：写 `cmp_demo.asm`：
```nasm
section .text
    global _start
_start:
    mov rax, 5
    cmp rax, 5          ; 比较=相减但只设标志不存结果
    je  equal           ; 若 ZF=1(相等)跳
    mov rdi, 1
    jmp end
equal:
    mov rdi, 0
end:
    mov rax, 60
    syscall
```
```bash
nasm -f elf64 cmp_demo.asm -o c.o && gcc -nostdlib c.o -o c && ./c; echo "相等则退出码0=$?"
```

### 🧪 实战 Lab（ASM2）
把 `cmp rax, 5` 改成 `cmp rax, 7`，重新编译运行，退出码应变 1（不等，走 `mov rdi,1`）。再试 `cmp rax, 5` 但把 `je` 改 `jg`(大于才跳)，观察行为。验收：把两种改法的退出码发我，并写"je 看 ZF，jg 看 ZF/SF/OF 组合"。

## 🔵 部分三·原理层（算术与标志怎么工作）
`add rax,rbx` 做二进制加法，结果写回 rax，同时按结果设 RFLAGS：结果为 0→ZF=1；最高位有进位(无符号溢出)→CF=1；结果最高位为 1(符号负)→SF=1；有符号溢出→OF=1。`sub` 同理(借位设 CF)。`mul`/`imul` 做乘(无符号/有符号)，`div`/`idiv` 做除(商在 rax、余数 rdx)。`and/or/xor` 按位运算，`xor rax,rax` 是清零惯用法（比 `mov rax,0` 短且常更优）。`shl/shr` 左/右移位(×2 / ÷2)。`cmp a,b` 实质 `a-b` 只设标志不写回，供 `je/jne/jg/jl/jge...` 读取。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
标志位源自 8080/8086 的状态字，沿用至今。顶级追问：CF 与 OF 区别(无符号 vs 有符号溢出)为何至关重要？整数溢出(SEC39)在 C 里是 UB，但在汇编里"溢出只是设 OF，不会崩"——这正是高层语言漏洞的根源之一。乘法在 x86 上延迟高，编译器爱用 `lea`/`shift-add` 拼乘法(COMP3 见过 `lea`)。条件码(condition codes)让一条 `cmp` 后能接多种跳转，精简指令流。浮点用不同寄存器(x87 st0–st7 或 SSE xmm)，有自己一套状态。

## 🔴 部分五·顶级视角
编译器后端(CMP8)要把 `if(a<b)` 翻译成 `cmp`+`jl`，把 `a+=b` 翻译成 `add`——你写的每句 C 都落到这些指令。顶级工程师读反汇编找"编译器没优化好的算术"(如用乘法代替移位、冗余清零)手动改。密码学常量时间实现(白帽防御)要避开依赖数据的分支/标志，防止侧信道。

## 🟠 部分六·安全/合规种子
整数溢出(SEC39)、符号错误是有害漏洞源头，但今天你只在自己程序里观察标志位，合法。理解 OF/CF 后，将来读到"整数溢出导致缓冲区分配过小"的 CVE 时，你能秒懂根因——这是白帽防御视角。

## ✅ 验收
交：① `calc` 退出码 12 截图；② `cmp_demo` 改 `cmp rax,7` 后退出码 1 截图；③ 能说出 ZF/CF/SF/OF 各代表什么。过关。

## ⚠️ 常见坑
- `cmp` 不改操作数，只设标志——别以为 `cmp rax,5` 后 rax 变了。
- 有符号用 `jg/jl`，无符号用 `ja/jb`——搞混会在负数比较时判断错。
- `div` 除零会触发异常(#DE)，自己写除法要注意。
- 护手：复制汇编块，别手打。

## ➡️ 下一步
ASM3 · 控制流与调用约定——if/循环/函数调用在汇编里长啥样，以及"谁把参数放哪个寄存器"的江湖规矩(cdecl/stdcall/thiscall)。

### ⚔️ 闯关路线（ASM2）
- 🟢 Lv1：写汇编 add/sub 并运行，观察结果 🥉🥉
- 🔵 Lv2：说明标志位 ZF/CF/SF 如何记录运算结果状态 🈶🥈
- 🟣 Lv3：用 cmp + jg 实现“大于则跳” 🥇🥇
- 🟠 Lv4：解释溢出标志 OF 与有符号运算的关系 🏆🏆
- 🔴 Lv5：写 200 字说明“标志位”如何支撑条件跳转（高级语言 if 的底层）👑👑


---

# ASM3 · 控制流 / 调用约定（cdecl / stdcall / thiscall）

## 🎯 目标
学完这节，你能用汇编写出循环（loop/dec+jnz）、if-else（cmp+jmp）、以及带参数的函数调用，并说清 x86-64 System V 调用约定（前 6 参数走 rdi/rsi/rdx/rcx/r8/r9，返回值走 rax），区分 cdecl/stdcall/thiscall 这些 32 位时代的规矩。

## 📋 小白前置
需先懂 ASM1（寄存器/mov/syscall）、ASM2（cmp/标志/条件跳转）。知道"函数(PY6)"和"循环(PY5)"的高层概念即可对照。

## 🟢 部分一·最浅层（生活比喻）
控制流就是"程序走到岔路口怎么选"。if 像"前面红绿灯，红灯绕路、绿灯直行"(cmp 看标志→jmp 选路)。循环像"绕着操场跑，每圈数一下，数够 10 圈就停"(dec 减一→jnz 不为零继续)。调用约定是"公司规定：递材料时第 1 份放左手(rdi)、第 2 份放右手(rsi)… 对方接完把回执放桌上(rax)"——不守规矩两家公司(调用方/被调方)就对不上。

## 🟡 部分二·动手层（逐字操作）
1. 写 `loop_sum.asm` 用循环求 1+2+...+10：
```nasm
section .text
    global _start
_start:
    mov rcx, 10         ; 计数 10
    xor rax, rax        ; 累加器=0
add_loop:
    add rax, rcx        ; rax += rcx
    dec rcx             ; rcx--
    jnz add_loop        ; rcx 不为0继续
    ; rax 现在=55
    mov rdi, rax
    mov rax, 60
    syscall
```
```bash
nasm -f elf64 loop_sum.asm -o l.o && gcc -nostdlib l.o -o l && ./l; echo "和=55? 退出码=$?"
```
2. 用 C 调用约定：写 `call.asm` 定义一个函数，主程序传参调用它：
```nasm
section .text
    global _start
    global add2
add2:
    add rdi, rsi        ; 参数1+参数2
    mov rax, rdi
    ret
_start:
    mov rdi, 7
    mov rsi, 8
    call add2           ; rax = 15
    mov rdi, rax
    mov rax, 60
    syscall
```
```bash
nasm -f elf64 call.asm -o c2.o && gcc -nostdlib c2.o -o c2 && ./c2; echo "7+8=15? 退出码=$?"
```

### 🧪 实战 Lab（ASM3）
把 `call.asm` 改成"传 3 个参数求 a+b+c"（用 rdx 传第 3 个），退出码应为三者之和。验收：把修改后的代码与退出码发我，并写"第 1/2/3 参数分别放 rdi/rsi/rdx，返回值 rax"。

## 🔵 部分三·原理层（控制流与约定怎么落地）
循环 `dec rcx; jnz lab`：减一后若结果非零则跳回 lab，否则落到下一条——这就是 `for` 的底层。`call func` 指令把"返回地址(下一条指令地址)"压栈(ASM4 讲栈)，再跳到 func；`ret` 从栈弹出返回地址跳回。x86-64 System V AMD64 ABI 调用约定：整型参数依次 rdi,rsi,rdx,rcx,r8,r9（第 7 个起走栈），浮点走 xmm0–xmm7，返回值 rax/rdx，调用者保存 rbx/rbp/r12–r15、被调者保存其余——这些规定让不同编译器/语言能互相调用(FFI)。

## 🟣 部分四·深挖层（历史规矩、顶级追问）
32 位时代：cdecl(调用者清栈, 支持变参如 printf)、stdcall(被调者清栈, Windows API 常用)、thiscall(ECX 放 this 指针, C++ 成员函数)、fastcall(前两个参数走寄存器)。64 位统一为 System V(类 Unix)与 Microsoft x64(前 4 参数 rcx/rdx/r8/r9, 略有差异)两套。顶级追问：为什么参数走寄存器比走栈快(避免访存)？可变参数(printf)怎么在寄存器约定下取第 n 个？调用约定不一致是"ABI 崩溃"根源。这些在 CMP9 链接、OS 内核入口都关键。

## 🔴 部分五·顶级视角
逆向(SEC27)第一步就是"识别调用约定还原函数签名"。FFI(如 Python ctypes、Rust FFI、CGo)本质是"按对方约定摆好参数再 call"。顶级工程师手写汇编优化时严格遵守 ABI，否则破坏栈帧。glibc 的 `printf` 实现就是 cdecl 变参的典范。

## 🟠 部分六·安全/合规种子
调用约定/栈操作知识是理解"栈溢出(SEC35)"的前提——返回地址被覆盖就劫持控制流。白帽只在本机自己程序里练 `call/ret`，绝不把返回地址改写技术用到他人程序。thiscall 等用于 C++ 逆向，只在自己/授权样本研究。

## ✅ 验收
交：① 循环退出码 55 截图；② 三参数求和代码+退出码；③ 说出 x86-64 前 3 个整型参数寄存器。过关。

## ⚠️ 常见坑
- 忘了 `ret` 或压栈/出栈不平衡，程序跑飞/段错误。
- 32 位 cdecl 与 64 位 System V 混记——本机是 64 位，以 rdi/rsi/rdx 为准。
- 护手：复制汇编，别手打寄存器名。

## ➡️ 下一步
ASM4 · 栈帧与函数调用——深入"栈"这个 LIFO 抽屉塔：返回地址、局部变量、rbp/rsp 怎么协作，C 函数调用在底层到底发生什么。

### ⚔️ 闯关路线（ASM3）
- 🟢 Lv1：用 C 写 if/for 编出汇编，对照看控制流 🥉🥉
- 🔵 Lv2：解释调用约定 cdecl/stdcall 谁清栈的差异 🈶🥈
- 🟣 Lv3：说明 thiscall 如何把 this 指针塞进寄存器（C++）🥇🥇
- 🟠 Lv4：画一张“调用前参数入栈→call→被调取参”的图 🏆🏆
- 🔴 Lv5：写 200 字说明“调用约定错配”会导致栈损坏（经典漏洞源头）👑👑


---

# ASM4 · 栈帧 / 函数调用（返回地址 / 局部变量）

## 🎯 目标
学完这节，你能手画一个函数调用时的栈布局（返回地址、保存的 rbp、局部变量、参数），用 nasm 写出"带局部变量的函数+调用者压参"，并用 gdb 单步看到 rsp/rbp 的变化，真正理解"函数调用=栈操作"。

## 📋 小白前置
需先懂 ASM1–ASM3（寄存器、cmp/jmp、call/ret、调用约定）。知道"栈(PY4 栈/队列、DSA4)"是后进先出的数据结构概念。

## 🟢 部分一·最浅层（生活比喻）
栈像一摞盘子：只能从顶上放、从顶上取。函数调用时，CPU 做两件事：① 把"办完事回哪"的回程地址压到盘顶；② 给这个函数单独铺一块"工作台"(栈帧)，放它的临时便签(局部变量)。函数结束 `ret`，抽掉回程地址照着回去。每个函数有自己的"盘摞"，互不干扰——这就是递归(PY6/递归)能层层深入的物理基础。

## 🟡 部分二·动手层（逐字操作）
1. 用 C 写 `frame.c`，再反汇编看懂栈帧（比纯汇编写一个 `call` 更易观察）：
```c
int add(int a, int b){ int t = a + b; return t; }
int main(){ int r = add(3, 4); return r; }
```
```bash
gcc -O0 -g -fno-stack-protector frame.c -o frame
objdump -d frame | sed -n '/<add>:/,/ret/p'
```
你会看到 `push rbp; mov rbp, rsp; sub rsp, 16`（开栈帧、留 16 字节给局部变量），以及 `leave; ret` 收尾。
2. 用 gdb 单步看 rsp/rbp：
```bash
gdb -q ./frame
(gdb) break add
(gdb) run
(gdb) info registers rsp rbp
(gdb) stepi
(gdb) info registers rsp rbp
(gdb) continue
```
观察进入 add 时 rsp 变化。

### 🧪 实战 Lab（ASM4）
手画一张"main 调用 add(3,4) 时栈从高地址到低地址"的图：标出返回地址、保存的 rbp、t 的位置、参数 3 和 4。用文字描述也行。验收：把图(拍照/手绘/文字)发我，并写"rsp 指向栈顶、rbp 指向当前栈帧底"。

## 🔵 部分三·原理层（栈帧怎么搭起来）
进入函数典型序：`push rbp`（保存调用者的栈底）→ `mov rbp, rsp`（新栈帧底=当前栈顶）→ `sub rsp, N`（抬高栈顶留 N 字节局部空间）。局部变量在 `[rbp-8]`、`[rbp-16]` 处。参数（超过寄存器能装的）在 `[rbp+16]` 等更高地址（调用者压的）。`call` 把返回地址压栈（在 rbp 之上）；`leave`=`mov rsp,rbp; pop rbp`（拆栈帧还原），`ret` 弹返回地址跳回。栈向低地址增长，所以"压栈 rsp 减小"。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
栈帧(red zone 在 System V 下 rsp 下 128 字节可被叶子函数直接用，省 sub rsp)、帧指针省略(FPO, `-O2` 常省略 rbp 以省指令和寄存器)让反汇编阅读变难(ASM8)。顶级追问：为什么局部变量未初始化是"垃圾值"(栈上残留上次数据)？这正是栈溢出/信息泄露根因。栈_guard/Stack Canary(SEC44)在 rbp 前放随机值，函数返回前检查被改没——防御栈溢出。红区、对齐(16 字节)、变长数组(VLA)怎么在栈上分配？这些都和本节点咬合。

## 🔴 部分五·顶级视角
逆向(SEC27/ASM8)还原高级结构时，第一件事是"识别函数序言 prologue / 尾声 epilogue"来切分函数边界。编译器优化会内联(CMP7)小函数消去栈帧。OS1 上下文切换要保存/恢复整个寄存器现场到内核栈。顶级工程师调栈溢出、看崩溃 backtrace，全靠本节栈帧知识。

## 🟠 部分六·安全/合规种子
栈是"栈溢出(SEC35)""ROP(SEC41)"的战场。白帽：在本机自己程序里用 gdb 观察栈帧 100% 合规；但绝不把"覆盖返回地址/构造 ROP 链"用于他人系统。Canary/ASLR/DEP(SEC44)正是为堵这些，理解它们让你成为更好的防御者。

## ✅ 验收
交：① `objdump` 看到的 add 函数序言/尾声截图；② 手绘栈布局图；③ 说出 rsp 与 rbp 各自指向什么。过关。

## ⚠️ 常见坑
- 以为栈"从高到低"是固定：x86 上确实向低地址增长，但别和"堆向高地址"混淆(OS5/OS7)。
- `-O2` 下可能看不到 `push rbp`（帧指针省略），想观察请用 `-O0 -fno-stack-protector`。
- gdb 里 `stepi` 是单条指令，别和源码级 `step` 混。
- 护手：复制 `gcc -O0 -g -fno-stack-protector frame.c -o frame`。

## ➡️ 下一步
ASM5 · 内联汇编（GCC extended asm）——在 C 里直接嵌汇编，看高层语言和底层指令怎么无缝握手。

### ⚔️ 闯关路线（ASM4）
- 🟢 Lv1：用 gdb 或手画栈帧：返回地址/局部变量/参数布局 🥉🥉
- 🔵 Lv2：解释函数调用时如何保存返回地址、建立新栈帧 🈶🥈
- 🟣 Lv3：演示局部变量在栈上的偏移访问 🥇🥇
- 🟠 Lv4：说明栈帧布局是“栈溢出利用”的攻击面（白帽分析）🏆🏆
- 🔴 Lv5：写 200 字说明“理解栈帧=理解调用栈回溯与溢出原理”（接 SEC35）👑👑


---

# ASM5 · 内联汇编（GCC extended asm）

## 🎯 目标
学完这节，你能用 GCC 的 `asm` 扩展语法在 C 函数里嵌入一段 x86-64 汇编，正确写出"输出/输入/破坏列表(clobber)"三部分，理解约束字符串，并写一个"用汇编读 CPU 时间戳计数器(TSC)"的小程序。

## 📋 小白前置
需先懂 ASM1–ASM4（基础汇编、寄存器）。会写并编译 C（C1）。知道 C 的变量与寄存器对应关系即可。

## 🟢 部分一·最浅层（生活比喻）
内联汇编像"在中文文章里夹一句方言"：文章(C 程序)主体用通用语，遇到特别需要手脚快的地方，直接插一句底层方言(汇编)让 CPU 干特定活。GCC 提供了"翻译官语法"：告诉它"这句方言的输入从哪个 C 变量拿、结果写回哪个 C 变量、顺手碰了哪些寄存器要备案"，翻译官才能安全地把方言缝进文章里。

## 🟡 部分二·动手层（逐字操作）
1. 新建 `rdtsc.c`，用内联汇编读 TSC（每个 CPU 周期 +1 的计数器，测耗时神器）：
```c
#include <stdio.h>
#include <stdint.h>
static inline uint64_t rdtsc(){
    uint64_t t;
    asm volatile ("rdtsc"          // 读 32 位到 eax:edx
                  : "=A"(t)        // 输出：合并 eax/edx 到 64 位 t
                  );
    return t;
}
int main(){
    uint64_t a = rdtsc();
    volatile unsigned long s=0; for(int i=0;i<1000000;i++) s+=i; // 干点活
    uint64_t b = rdtsc();
    printf("用了约 %llu 个周期\n", (unsigned long long)(b-a));
    return 0;
}
```
2. 编译运行：
```bash
gcc -O2 rdtsc.c -o rdtsc && ./rdtsc
```
预期打印一个约几万~几十万的周期数。

### 🧪 实战 Lab（ASM5）
把 `volatile unsigned long s=0;` 的循环次数 `1000000` 改成 `100000`，重编译运行，观察周期数大致按比例变小。再试把 `asm volatile` 去掉 `volatile` 看编译器是否可能优化掉（保留 volatile 更安全）。验收：把两次周期数发我，并写"`=A` 表示输出合并 eax:edx"。

## 🔵 部分三·原理层（extended asm 语法）
GCC 语法：`asm [volatile] ( 汇编模板 : 输出操作数 : 输入操作数 : 破坏列表 )`。例：`asm("add %1,%0" : "=r"(c) : "r"(a), "0"(b))`——`%0` 是输出 c、`%1` 是输入 a，`"0"` 表示和第 0 个同寄存器。约束：`r`=任意寄存器、`m`=内存、`=`（只写）、`+`（读写）、`A`=合并 eax:edx（专用于 64 位值）、`a`/`b`=指定 rax/rbx。clobber 列写 `"cc"`(改了标志)、`"memory"`(改了内存，防编译器乱优化)、具体寄存器名。省略会酿成"编译器不知你改了啥"的诡异 bug。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
内联汇编是"编译器优化与手写指令"的妥协点。顶级追问：为什么 `volatile` 防止指令被移走/删掉？为什么读硬件寄存器必须 `volatile`？`__attribute__((always_inline))` 与内联汇编结合做" intrinsics 之外"的极致优化。ARM 的内联汇编语法(ASM6)不同(`"r"(x)` 类似但模板用 `%0` 还是 `%[name]` 可选命名)。C11 `_Atomic` 与内存屏障(`asm volatile("" ::: "memory")`)是并发(OS3/CMP)基石。内核里内联汇编无处不在(开关中断、读 MSR)。

## 🔴 部分五·顶级视角
Linux 内核 `arch/x86/include/asm/` 里大量 inline asm 封装底层操作；glibc 的原子/锁原语靠它。顶级工程师用它实现"标准 C 没有"的指令(如 `cpuid`、`pause`、`mfence`)。理解后你能读懂任何带 `asm()` 的系统代码，也能在性能热点夹带手写指令。

## 🟠 部分六·安全/合规种子
内联汇编只影响你自己编译的程序，完全合规。注意：用内联汇编写"绕过安全检查"的代码违反白帽精神——只用它做性能/硬件访问等正当用途。

## ✅ 验收
交：① `rdtsc` 运行输出截图；② 能说清 extended asm 四部分（模板/输出/输入/clobber）；③ 写出 `=A` 含义。过关。

## ⚠️ 常见坑
- 忘记 clobber `"memory"`/`"cc"`，导致编译器基于"内存没变"做了错误优化，bug 极难查。
- `rdtsc` 在多核间不同步，跨核测时不准——只用于单线程粗测。
- 护手：复制 `rdtsc.c` 整段，别手打。

## ➡️ 下一步
ASM6 · ARM64 入门——换一套"方言"：ARM64 寄存器/指令和 x86 差在哪，为什么手机和苹果电脑都吃它。

### ⚔️ 闯关路线（ASM5）
- 🟢 Lv1：在 C 里嵌一句 asm("nop") 并编译通过 🥉🥉
- 🔵 Lv2：用 extended asm 读一个 C 变量进寄存器 🈶🥈
- 🟣 Lv3：说明 output/input/clobber 约束列表作用 🥇🥇
- 🟠 Lv4：写一个内联汇编做加法并返回，验证结果 🏆🏆
- 🔴 Lv5：写 200 字说明内联汇编边界与逆向/混淆中的角色（白帽只分析）👑👑


---

# ASM6 · ARM64 入门（寄存器 / 指令差异）

## 🎯 目标
学完这节，你能说清 ARM64 与 x86-64 在寄存器数量、指令风格(三地址 vs 二地址)、条件执行、寻址上的核心差异，并能读懂一段 ARM64 汇编（无需本机交叉编译，用文本对照即可），理解"为什么手机/苹果用 ARM"。

## 📋 小白前置
需先懂 ASM1–ASM5（x86-64 汇编基础）。知道 ISA 概念(COMP3)。本节用"对照阅读"为主，弱动手。

## 🟢 部分一·最浅层（生活比喻）
如果说 x86 是"老方言，一句话能干复杂事但词儿杂"，ARM 就是"新派标准语，每句只做一件小事但极其规律"。ARM 有 31 个通用寄存器(x0–x30，比 x86 的 16 个多近一倍)，说话习惯是"把 A 和 B 加起来，结果放 C"(三地址)，不像 x86 常是"把 B 加到 A 上"(二地址，结果覆盖一个源)。还有个绝活：很多指令能自己带"条件"(比如"如果相等才加")，不用先跳再算。

## 🟡 部分二·动手层（逐字操作）
本机是 x86，不交叉编译 ARM 也能学"读"。我们用文本对照法：
1. 写 `add.c` 并在 x86 上 `gcc -O2 -S` 看 x86 版（复习）：
```bash
echo 'int add(int a,int b){return a+b;}' > addc.c
gcc -O2 -S addc.c -o addc.s && cat addc.s
```
看到 x86：`lea (%rdi,%rsi), %eax`（二地址，结果进 eax）。
2. 看 ARM64 同款（想象中的等效）：
```
// ARM64 版 add(int a,int b)
add:
  add w0, w0, w1    // w0 = w0 + w1  (三地址：结果放 w0, 不破坏 w1)
  ret
```
`w0/w1` 是 32 位寄存器(x0/x1 的低位)，参数 a→w0, b→w1，返回值 w0。
3. （可选，若装了 `aarch64-linux-gnu-gcc`）真生成 ARM64：
```bash
aarch64-linux-gnu-gcc -O2 -S addc.c -o addc_arm.s 2>/dev/null && cat addc_arm.s
```

### 🧪 实战 Lab（ASM6）
把 `add w0,w0,w1` 改成"三数相加 `add w0,w0,w1; add w0,w0,w2`"，在纸上写清"参数 a→w0, b→w1, c→w2, 结果 w0"。验收：把这段 ARM64 伪代码与你的中文注解发我，并写"ARM 是三地址、x86 常是二地址"。

## 🔵 部分三·原理层（ARM64 怎么运作）
ARM64(AArch64)有 31 个 64 位通用寄存器 x0–x30(含栈指针 sp 单独)，参数用 x0–x7，返回值 x0。指令定长 32 位(简洁解码、省电)。关键特性：① 三地址 `ADD Xd, Xn, Xm`；② 条件执行 `ADDGT`（GT=greater than 时执行），让小 if 不必跳；③ 寻址 `LDR X0, [X1, #8]`（基址+立即偏移），无 x86 那种 `scale` 比例因子而是用单独移位；④ PC 相对寻址方便位置无关代码(ASM7)。ARM 还分 A32(32位)/T32(Thumb)/A64，手机多用 A64。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
ARM 靠"低功耗+IP 授权(卖设计)"赢下移动端；苹果 M 系列把 ARM 带到笔记本/台式，靠宽发射乱序(COMP2)补性能。顶级追问：定长指令对缓存友好但编码密度低(32 位存小操作浪费)；条件执行在深流水线里反而难设计，现代 ARM64 已弱化成"条件选择 CSEL"而非每条可带条件。RISC-V(CMP3)与之同源思路。NEON(ARM 的 SIMD,COMP6)是 xmm/ymm 对应物。

## 🔴 部分五·顶级视角
移动逆向(MOB14–19)、iOS 逆向(MOB28–31)面对的就是 ARM64 汇编。顶级工程师在跨平台项目(如游戏引擎 MOB44–47)要同时懂 x86 与 ARM 生成物。QEMU 用户态模拟能在 x86 机器跑 ARM64 程序(`qemu-aarch64`)，是学习利器。Apple Silicon 上的 Rosetta2 是 x86→ARM 翻译典范。

## 🟠 部分六·安全/合规种子
读 ARM 汇编、在自己手机/树莓派(授权设备)跑自己程序合规。逆向他人 App 违反红线(MOB14 强调"合法边界")；白帽只在自己/开源/授权样本研究 ARM 代码。

## ✅ 验收
交：① x86 `addc.s` 截图；② 手写 ARM64 三数相加伪代码+注解；③ 说出 ARM64 与 x86 至少两点差异。过关。

## ⚠️ 常见坑
- 把 `w0`(32位)和 `x0`(64位)混：写运算时注意位数匹配，混用会隐式清零高 32 位。
- 以为 ARM 没有标志位：它有 PSTATE(NZCV)，只是"条件执行"写法不同。
- 护手：复制 `gcc -O2 -S addc.c -o addc.s`。

## ➡️ 下一步
ASM7 · Shellcode 基础（空字节 / 位置无关）——理解"一小段能自己跑的机器码"是什么，以及为什么防御方要专门对付它（白帽只分析原理）。

### ⚔️ 闯关路线（ASM6）
- 🟢 Lv1：指出 ARM64 的 X0–X30 通用寄存器 🥉🥉
- 🔵 Lv2：对比 ARM 的“定长指令”与 x86 变长指令 🈶🥈
- 🟣 Lv3：说明 ARM 无栈帧指针、靠 LR（链接寄存器）返回 🥇🥇
- 🟠 Lv4：对比同逻辑在 x86-64 与 ARM64 的汇编差异 🏆🏆
- 🔴 Lv5：写 200 字说明“移动端逆向（方向8）必懂 ARM64 指令集”👑👑


---

# ASM7 · Shellcode 基础（空字节 / 位置无关）

## 🎯 目标
学完这节，你能从白帽防御视角说清"什么是 shellcode"（一小段位置无关、能自举的机器码）、为什么它要避开 `0x00` 空字节、什么是 PIC（位置无关代码），并能在本机写一个"只调用 exit 的极简 shellcode"并用 C 把它当函数指针跑起来——全程只在你自己程序里，绝不外投。

## 📋 小白前置
需先懂 ASM1–ASM6（写汇编、syscall、寄存器、ARM 对照）。知道 C 函数指针(C10)概念更好。理解"字符串以 0 结尾"(PY2)对理解空字节很关键。

## 🟢 部分一·最浅层（生活比喻）
Shellcode 像一张"自助纸条"：你把它塞进一个本不属于它的程序内存里，它得自己能跑起来（比如打开一个命令行）。难点：这张纸条常被当"字符串"传来传去，而字符串遇到 `0x00`(空字节)就被当作"结束符"截断——所以纸条里不能有空字节，否则半路被切掉失效。还要"走到哪都能用"，不依赖固定地址（位置无关），像随身带地图而不是记死某栋楼。

## 🟡 部分二·动手层（逐字操作）
1. 写一个"仅 exit(0)"的极简 shellcode（x86-64），并演示"空字节问题"与规避。新建 `sc.c`：
```c
#include <stdio.h>
#include <string.h>
#include <unistd.h>
// 机器码: mov rax,60 ; xor rdi,rdi ; syscall  (仅示意, 含空字节版我们看问题)
// 这里直接放一段已知无空字节的 exit(0) shellcode (17字节 classic):
unsigned char sc[] = 
  "\x48\x31\xff"            // xor rdi,rdi
  "\x48\x31\xc0"            // xor rax,rax
  "\x48\x83\xc0\x3c"        // add rax,60  (exit)
  "\x0f\x05";               // syscall
int main(){
    printf("shellcode 长度=%zu, 是否含0x00? %s\n",
           strlen((char*)sc), memchr(sc,0,sizeof(sc))?"含(会被截断)":"不含(可当字符串传)");
    // 白帽: 仅在本进程内把它当函数执行, 验证它能跑, 不投到任何他处
    void (*f)() = (void(*)())sc;
    // 注意: 需可执行内存; 下面用 mprotect 经典做法略, 这里只演示"识别", 不真执行以免环境差异
    (void)f;
    return 0;
}
```
2. 编译运行：
```bash
gcc -O2 sc.c -o sc && ./sc
```
预期打印长度与"是否含 0x00"判断。本 Lab 只"分析 shellcode 结构"，不强制真执行（真执行需 `mprotect` 改页为可执行，涉及 DEP/SEC44，留到安全方向）。

### 🧪 实战 Lab（ASM7）
在上面 `sc[]` 里，注释已说明每段含义。请你手算：去掉 `\x48\x31\xff` 那段会怎样（缺了把 rdi 清零，exit 退出码非 0）。写一句分析。验收：把运行输出+你的分析句发我。

## 🔵 部分三·原理层（空字节与 PIC 怎么来）
x86 指令里很多操作数含 `0x00`（如 `mov rax, 0` 机器码含 00 字节），被 strcpy 之类"遇 0 停"的函数复制时会被截断。规避法：用 `xor rax,rax`(全 0 寄存器但指令本身无 00 字节)代替 `mov rax,0`；用 `add rax,60` 代替 `mov rax,60`（后者 `48 c7 c0 3c 00 00 00` 含 00）。PIC："位置无关"靠 PC 相对寻址(`call $+5` 拿当前地址、或 `lea` 相对 rip)避免硬编码地址——因为 shellcode 被注入时地址未知。这些技巧正是 ASLR/DEP 要封堵的(SEC44)。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
shellcode 概念源自 1980s 缓冲区溢出利用(SEC35)。顶级追问：怎么用英文短句 syscall(如 `execve("/bin/sh")`)构造更长的 shellcode？怎么编码(异或解密 stage)绕过过滤？ROP(SEC41)为何能"无 shellcode 也利用"(靠程序已有 gadget)？这些全是"防御方要先懂攻击思路"的白帽研究范畴。注意：本方向只"分析原理、构造练习在自己内存"，绝不部署到他人进程——Mimikatz/Cobalt Strike 等只分析不实践。

## 🔴 部分五·顶级视角
顶级漏洞研究者(授权 CTF/赏金)要能手写无空字节、PIC 的 shellcode；防御方(你走白帽)更要懂它才能设计 DEP(数据执行保护不可执行栈)/ASLR(地址随机)/CFG(控制流保护)。Linux `execve` shellcode、Windows `WinExec` shellcode 是经典研究对象。开源：Shellcode 仓库(仅学习)、pwntools 框架(CTF 用, 授权)。

## 🟠 部分六·安全/合规种子（本节重点红线）
⚠️ 铁律：shellcode 技术**只用于理解防御原理与授权 CTF/靶场**。绝不在任何非自己、非明确授权的程序/系统里注入或执行 shellcode。不写、不传播可用于攻击他人的载荷。本 Lab 仅在自己进程的"识别/分析"层面操作，内存可执行化步骤留到安全方向在授权靶场做。Mimikatz、Cobalt Strike、msfvenom 生成物只分析不实践。

## ✅ 验收
交：① `sc.c` 输出(长度与空字节判断)截图；② 你写的"缺 xor rdi,rdi 段会怎样"分析句。过关（仅分析层）。

## ⚠️ 常见坑
- 真执行 shellcode 需 `mprotect`/绕 DEP，在自己机器外做即越界——本节刻意不执行，遵守红线。
- 混淆"shellcode"与"正常程序"：正常程序由合法加载器按 ELF/PE 装载，shellcode 是被塞进异常位置的裸机器码，这正是风险点。
- 护手：复制 `sc.c`。

## ➡️ 下一步
ASM8 · 反汇编阅读——给你一段机器码/编译产物，用 objdump/Rizin 还原出"它原本大概是哪段 C"，把汇编读回高级语言。

### ⚔️ 闯关路线（ASM7）
- 🟢 Lv1：解释 shellcode 是“可直接执行的机器码字节”🥉🥉
- 🔵 Lv2：说明空字节（\x00）为何会截断字符串复制类漏洞利用 🈶🥈
- 🟣 Lv3：用比喻说明“位置无关”让代码在哪都能跑 🥇🥇
- 🟠 Lv4：在自写 demo 里演示一段无害的“打印 hello”机器码（仅分析）🏆🏆
- 🔴 Lv5：写 200 字白帽声明：shellcode 原理只在授权靶场/自己程序理解，绝不外投👑👑


---

# ASM8 · 反汇编阅读（还原高级结构）

## 🎯 目标
学完这节，你能拿到一个编译后的二进制/目标文件，用 `objdump` 或 Rizin/Cutter 反汇编，认出其中的"函数 prologue/epilogue""if 分支""循环""数组访问""函数调用"，把汇编逆向读回近似的 C 结构——这是二进制逆向(SEC27)的入门基本功。

## 📋 小白前置
需先懂 ASM1–ASM7（寄存器、控制流、栈帧、调用约定）。会 `gcc` 编译 C（C1）、会 `objdump`（AS上节用过）。建议已学 OS1 进程概念更佳，但可独立。

## 🟢 部分一·最浅层（生活比喻）
反汇编像"看一部译制片倒推原剧本"：你只有演员的动作(汇编指令)，要猜编剧原本写的对话(C 代码)。函数像"一段有开头招牌(prologue)和结尾字幕(epilogue)的戏"；if 像"演到岔道选一条"；循环像"同一段戏重复演 N 遍"；数组像"一排编号相同的椅子，按号取人"。

## 🟡 部分二·动手层（逐字操作）
1. 写个有 if/循环/数组的 `sample.c`：
```c
int sum_pos(int *a, int n){
    int s = 0;
    for(int i=0;i<n;i++){
        if(a[i] > 0) s += a[i];
    }
    return s;
}
```
```bash
gcc -O0 -g sample.c -c -o sample.o
objdump -d -M intel sample.o
```
2. 在输出里找线索：
- `push rbp; mov rbp,rsp` = 函数开始(prologue)
- `cmp ...; jle ...` = `for` 循环的 `i<n` 判断+跳转
- `mov eax, [rax*4 + rdx]` 之类 = `a[i]` 数组访问(基址+变址×4)
- `test ...,...; jle` = `if(a[i]>0)` 的判断
- `leave; ret` = 函数结束(epilogue)
3. 用 Rizin/Cutter（若装了）图形化看：
```bash
rizin -c 'aa; pdf @ main' sample.o 2>/dev/null || echo "rizin 未装，用 objdump 即可"
```

### 🧪 实战 Lab（ASM8）
把 `objdump` 输出中"对应 `if(a[i]>0) s+=a[i]`"的那几行汇编复制下来，旁边用中文逐行标注"这是在取 a[i]、这是在判断是否大于0、这是在累加"。验收：把标注后的几行发我。

## 🔵 部分三·原理层（怎么从汇编写回 C）
编译器把 `for` 翻译成"初始化→条件跳转→循环体→递增→跳回条件"的四条骨架（ASM3 循环模式）。`if` 翻译成 `cmp`+条件跳转，两个分支各一块。`a[i]` 因 int 是 4 字节，地址=`a + i*4`，对应 `lea`/`mov` 的 `基址+变址*4`。函数调用 `call` 配传参寄存器(ASM3)。局部变量在栈上 `[rbp-8]` 等。`-O2` 后这些会变形（循环展开、变量提寄存器、删栈帧），更难读——所以逆向常从 `-O0` 样本练手。

## 🟣 部分四·深挖层（为什么难、顶级追问）
优化把"一一对应"打乱：循环展开、尾递归转循环、内联消去函数、常量传播让 `if` 消失。顶级追问：怎么识别"虚函数调用"(C++ vtable, CPP2)在汇编里是 `call [rax+8]`？怎么从 `memcpy` 展开成 SIMD 流水认出它是拷贝？怎么处理混淆(SEC30/MOB19)后的垃圾指令？控制流图(CFG)、数据流分析(SEC29)是进阶。这些全是 SEC27 反编译(IDA/Ghidra 自动还原伪 C)的依赖底座。

## 🔴 部分五·顶级视角
逆向工程师(SEC23–34)靠本节硬功吃饭。Ghidra/IDA 的"反编译器"把汇编自动翻成近似 C(伪码)，但自动结果常错，必须人工用本节知识校正。CTF Reverse(SEC94)、恶意软件分析(SEC57)第一步就是读反汇编。开源：Rizin/Cutter、Ghidra、objdump、capstone 反汇编引擎。

## 🟠 部分六·安全/合规种子
反汇编/逆向**只限自己写的程序、授权靶场样本、开源软件、CTF 题目**。绝不逆向他人商业软件、不绕过其授权(违反红线)。理解别人二进制的"漏洞模式"(如没检查长度的循环)用于防御性写代码(SEC20 代码审计)，才是白帽正道。

## ✅ 验收
交：① `objdump` 输出截图(含 prologue/循环/数组访问)；② 你标注的 `if` 对应汇编几行中文注解。过关。

## ⚠️ 常见坑
- 用 `-O2` 样本练阅读会懵：初学用 `-O0 -g` 保持结构清晰。
- `objdump -M intel` 用 Intel 语法(和 nasm 一致)，不加则是 AT&T 语法(`mov %rax,%rbx` 操作数反序)——两种都该认识，但新手先固定 Intel。
- 护手：复制 `gcc -O0 -g sample.c -c -o sample.o`。

## ➡️ 下一步
OS1 · 进程与线程——进入操作系统世界：一个"运行中的程序"到底由什么组成，进程和线程差在哪，CPU 怎么在它们之间切换。

### ⚔️ 闯关路线（ASM8）
- 🟢 Lv1：用 objdump/IDA/Cutter 打开一个简单二进制看反汇编 🥉🥉
- 🔵 Lv2：认出反汇编里的 main 函数与调用 🈶🥈
- 🟣 Lv3：把一段汇编还原成等价的 if/循环高级结构 🥇🥇
- 🟠 Lv4：说明编译器优化如何让反汇编“认不出原结构” 🏆🏆
- 🔴 Lv5：写 200 字说明“反汇编阅读”是安全研究（方向7b）的日常基本功👑👑


---

# OS1 · 进程与线程（PCB / TCB / 上下文切换）

## 🎯 目标
学完这节，你能说清"进程"和"线程"的本质区别、一个进程由哪些东西组成（代码/数据/堆/栈/PCB）、线程共享什么又独享什么，并能在自己机器用 `ps`/`top` 看到进程/线程，用 C 起一个线程并观察它们"同时"跑。

## 📋 小白前置
需先懂 COMP1（CPU/内存四大件）、ASM4（栈帧，线程各自有栈）。最好翻过 PY6 函数、DSA4 栈队列。本节能独立。

## 🟢 部分一·最浅层（生活比喻）
进程像"一家独立的公司"：有自己独立的办公楼(地址空间)、自己的保险柜(数据)、自己的员工手册(代码)。线程像"公司里的员工"：同一家公司(进程)的员工共享办公楼和保险柜(内存)，但每个人有自己的办公桌和待办清单(栈+寄存器)。开新"公司"(进程)贵(要建楼)，招新"员工"(线程)便宜(共用楼)。CPU 像"老板的时间"，在多个员工间快速切换——快到你以为他们同时干活(并发)。

## 🟡 部分二·动手层（逐字操作）
1. 看进程与线程：打开 MSYS2，先开一个耗 CPU 的小程序，另开终端看：
```bash
# 终端A: 跑个死循环占用
yes > /dev/null &
# 终端B: 看进程和线程
ps -eLf | head -n 5
cat /proc/$(pgrep yes | head -1)/status | grep -i threads
```
你会看到 `Threads:` 行显示线程数。
2. 用 C 起两个线程，观察它们各自有独立栈：
```c
#include <stdio.h>
#include <pthread.h>
void* worker(void* arg){
    long id = (long)arg;
    for(int i=0;i<3;i++) printf("线程%ld: 第%d次\n", id, i);
    return 0;
}
int main(){
    pthread_t t1,t2;
    pthread_create(&t1,0,worker,(void*)1);
    pthread_create(&t2,0,worker,(void*)2);
    pthread_join(t1,0); pthread_join(t2,0);
    return 0;
}
```
```bash
gcc -O2 -pthread os1.c -o os1 && ./os1
```

### 🧪 实战 Lab（OS1）
把 `worker` 里加一个"局部变量 `long local = id;`"，两个线程各改各的 local 互不影响——证明"线程有独立栈"。再故意用一个"全局变量 `int g=0;`"让两线程各 `g++` 100000 次，最后打印 g 往往不等于 200000（竞态，OS3 解决）。验收：把线程输出截图 + 一句"全局变量竞态说明共享内存需同步"。

## 🔵 部分三·原理层（进程/线程在 OS 里怎么管）
进程 = 独立的"地址空间" + 一个"主线程"。OS 给每个进程建一个 PCB(进程控制块，内核里的结构体)，记 PID、打开的文件、内存映射、调度信息。线程有 TCB(线程控制块)，记寄存器/栈指针/优先级，同一进程的多线程共享进程的地址空间(代码段、堆、全局变量)但各有独立"用户栈+内核栈"。上下文切换(context switch)：OS 把当前线程的寄存器存到它 TCB，载入下一线程的寄存器——代价是"缓存变冷"(COMP4)和几十~几百纳秒。这就是"并发"的真相：单核上靠切换制造同时的假象，多核才是真并行。

## 🟣 部分四·深挖层（为什么这样设计、历史、顶级追问）
早期系统(1960s)没有线程，只有进程(IBMP CP/67、Unix 1970s)。线程(轻量级进程)是后来为"共享内存又要并发"引入(Java 绿色线程、Linux NPTL)。顶级追问：Linux 里线程其实就是"共享地址空间的进程"(clone 系统调用带 CLONE_VM 等标志)，所以 `ps -eLf` 看到 LWP(轻量进程)；"协程"(PY17/GO1)比线程更轻，在用户态切换不进内核。上下文切换成本怎么量化(用 `perf stat` 看 context-switches)？NUMA 下跨核切换更贵？这些决定高并发服务架构。

## 🔴 部分五·顶级视角
顶级后端/系统工程师把"进程 vs 线程 vs 协程"当核心选型：Nginx 多进程、Redis 单线程事件循环、Go 万级 goroutine(GO1 GMP)、Java 线程池(JV6)。理解 TCB/PCB 让你读懂 `top`/`htop`/`perf` 的每一列。内核源码 `kernel/sched/` 与 `kernel/fork.c`(clone)是最佳教材。容器(OS15)、虚拟机都建立在进程/线程抽象之上。

## 🟠 部分六·安全/合规种子
多进程隔离是安全边界之一：一个进程崩了不影响别的(沙箱思想, SEC45)。白帽：在自己机器开任意多线程程序合规；但"注入线程到他人进程"(SEC51 DLL 注入/APC)只分析原理不实践，绝不对他人系统做。理解进程的隔离性是设计安全沙箱的基础。

## ✅ 验收
交：① `os1` 多线程输出截图；② 你能说清进程与线程共享/独享什么；③ 全局变量竞态观察句。过关。

## ⚠️ 常见坑
- 以为"多线程一定快"：锁竞争/伪共享(COMP7)可反伤性能。
- 忘了 `-pthread` 链接，pthread 函数报 undefined reference。
- 护手：复制 `gcc -O2 -pthread os1.c -o os1 && ./os1`。

## ➡️ 下一步
OS2 · 进程调度——OS 怎么决定"下一个轮到谁用 CPU"，公平与实时怎么权衡，Linux 的 CFS 是什么。

### ⚔️ 闯关路线（OS1）
- 🟢 Lv1：用任务管理器/ top 看进程与线程 🥉🥉
- 🔵 Lv2：说明 PCB/TCB 存了“这个进程的一切状态” 🈶🥈
- 🟣 Lv3：解释上下文切换：保存现场→恢复现场 🥇🥇
- 🟠 Lv4：对比进程（隔离）与线程（共享内存）的取舍 🏆🏆
- 🔴 Lv5：写 200 字说明“进程模型”是你写并发程序（方向1b/CPP7/Go）的底层👑👑


---

# OS2 · 进程调度（公平性 / 实时 / CFS）

## 🎯 目标
学完这节，你能说清调度器解决什么问题、常见算法（FIFO/RR/优先级/多级反馈队列）、Linux 的 CFS（完全公平调度）核心思想，并能在自己机器改进程优先级(`nice`/`chrt`)观察效果。

## 📋 小白前置
需先懂 OS1（进程/线程、上下文切换代价）。知道"并发/并行"区分（OS1 讲过）。

## 🟢 部分一·最浅层（生活比喻）
调度器像"公司前台排号"：一群员工(线程)都举手要老板(CPU)时间，前台按规矩决定先叫谁。规矩可以不同：① 先来先服务(谁先举手谁先上)；② 轮流(每人限时 10 秒, 到时换人, 叫时间片轮转)；③ 看紧急程度(VIP 优先)；④ 让每人"累计等待越久越优先"(完全公平, CFS)。前台定错规矩, 有人饿死(永远轮不到), 有人霸占(其他人卡死)。

## 🟡 部分二·动手层（逐字操作）
1. 看当前进程的调度策略与优先级：
```bash
ps -eo pid,comm,ni,cls,pri | head -n 10
chrt -p $$            # 看当前 shell 的调度策略
```
2. 用 `nice` 降优先级跑一个程序，对比 `top` 里 PR 值：
```bash
nice -n 10 yes > /dev/null &
top -b -n 1 | head -n 12
kill %1 2>/dev/null
```
3. 看 CFS 的"虚拟运行时间"统计（Linux 暴露的部分）：
```bash
cat /proc/$(pgrep yes|head -1)/sched 2>/dev/null | head
```

### 🧪 实战 Lab（OS2）
开两个 `yes` 进程，一个 `nice -n -5`（需管理员，MSYS2 可能无权限则跳过），一个默认，用 `top` 看谁的 CPU% 更高，体验优先级影响。验收：把 top 里两进程 CPU 占用截图 + 一句"优先级高的拿更多 CPU"。

## 🔵 部分三·原理层（调度怎么决策）
调度发生在：① 进程用完时间片；② 进程主动睡眠(等 IO/锁)；③ 更高优先级进程就绪；④ 进程退出/阻塞。算法：FIFO(简单但长任务饿死短任务)、RR(时间片轮转, 公平但切换开销)、SJF(最短作业优先, 最优平均等待但需预知时长)、多级反馈队列(新进程进高优队列, 用完时间片降级, 兼顾响应与吞吐)、实时调度(FIFO/RR with priority, 硬实时保证)。Linux CFS：给每个任务记"虚拟运行时间 vruntime"，每次选 vruntime 最小的(最"亏待"的)运行，等价于"按权重公平分 CPU"；权重由 `nice` 值映射。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
调度是"公平 vs 吞吐 vs 延迟 vs 实时性"的多目标权衡。顶级追问：CFS 用红黑树存任务(按 vruntime 排序, O(log n) 选最小)，为何不用堆？实时任务(SCHED_FIFO)如何抢占普通任务保证截止时间？NUMA 调度(把任务绑近内存)、能耗调度(把任务集中到少数核休眠其余, 省电)？SMT(超线程, COMP7)下怎么避免一个物理核的两逻辑核互相抢资源？这些在 SRE(OPS7)、云调度都关键。

## 🔴 部分五·顶级视角
顶级工程师调调度以压延迟：低延迟交易用 `SCHED_FIFO`+核心隔离；云用 CFS + cgroup(OS15)限资源；Go 的 GMP(GO1)在用户态再做一层调度。读《Understanding the Linux Kernel》调度章、Linux `kernel/sched/fair.c`(CFS 源码)。perf 的 `sched` 子系统能追踪每一次上下文切换。

## 🟠 部分六·安全/合规种子
调度本身是资源分配，无攻击面。但"调度侧信道"(恶意程序靠观察自己被调度的时间推断同机他人活动)是云安全(OPS18)课题——白帽只了解原理用于防护。自己机器调 `nice` 完全合规。

## ✅ 验收
交：① `ps -eo pid,comm,ni...` 输出截图；② 一句话说 CFS 选"vruntime 最小"的任务；③ 能列两种调度算法。过关。

## ⚠️ 常见坑
- MSYS2 是 Windows 上的兼容层，`chrt`/优先级可能受限或不准——以能跑出 `ps` 输出为准，深度实验建议用 WSL2/真实 Linux 虚拟机(授权)。
- 把"优先级"和" nice 值"方向搞反：nice 越大(19)优先级越低。
- 护手：复制命令，别手打。

## ➡️ 下一步
OS3 · 同步原语——当多线程抢同一份数据时，怎么用锁/信号量/条件变量让它们不打架，以及自旋锁为何"忙等"。

### ⚔️ 闯关路线（OS2）
- 🟢 Lv1：用比喻说明调度像“餐厅叫号”决定谁先用 CPU 🥉🥉
- 🔵 Lv2：对比 FIFO / 轮转 / 优先级调度的行为 🈶🥈
- 🟣 Lv3：解释 CFS（完全公平调度）用红黑树选最久未跑的 🥇🥇
- 🟠 Lv4：说明实时调度的“死限”要求 🏆🏆
- 🔴 Lv5：写 200 字说明“调度公平性”如何影响你服务端程序延迟（接方向6 BE）👑👑


---

# OS3 · 同步原语（锁 / 信号量 / 条件变量 / 自旋）

## 🎯 目标
学完这节，你能解释"竞态条件"为什么发生，动手用 pthread 的互斥锁(mutex)修掉 OS1 里那个 `g++` 竞态，理解信号量(semaphore)、条件变量(condvar)、自旋锁(spinlock)各自的适用场景，并知道原子操作(atomic)是它们的底层基石。

## 📋 小白前置
需先懂 OS1（线程共享内存、竞态）、ASM5（原子/`lock` 前缀、内存屏障概念）。知道"C 全局变量"(C 语言基础)即可。

## 🟢 部分一·最浅层（生活比喻）
竞态像"两个人同时往同一个账户存钱，都看到余额 100，各加 50 写回，结果变 150 而不是 200"——因为"读-改-写"三步被打断交错。锁(mutex)像"厕所门上的占用牌"：谁进谁挂牌，别人得等。信号量像"只发 3 张的入场券"，超过 3 人得排队。条件变量像"叫号机"：没货时你登记等着，补货了叫你。自旋锁像"你不停敲门问'好了没'直到门开"——占着 CPU 干等。

## 🟡 部分二·动手层（逐字操作）
1. 用 mutex 修 OS1 的竞态。新建 `os3.c`：
```c
#include <stdio.h>
#include <pthread.h>
#define N 1000000
int g = 0;
pthread_mutex_t m = PTHREAD_MUTEX_INITIALIZER;
void* add(void* p){
    for(int i=0;i<N;i++){
        pthread_mutex_lock(&m);
        g++;                    // 临界区受保护
        pthread_mutex_unlock(&m);
    }
    return 0;
}
int main(){
    pthread_t t1,t2;
    pthread_create(&t1,0,add,0);
    pthread_create(&t2,0,add,0);
    pthread_join(t1,0); pthread_join(t2,0);
    printf("g=%d (期望 2000000)\n", g);
    return 0;
}
```
2. 编译运行：
```bash
gcc -O2 -pthread os3.c -o os3 && ./os3
```
这次 g 应稳定等于 2000000。

### 🧪 实战 Lab（OS3）
把 `g++` 改成用 C11 原子(`#include <stdatomic.h>; atomic_int g; atomic_fetch_add(&g,1);`)做无锁版本，对比两种实现结果。验收：把 mutex 版与 atomic 版输出都发我（都应是 2000000），并写"原子操作靠 CPU 的 lock 前缀/原子指令实现"。

## 🔵 部分三·原理层（锁怎么工作）
互斥锁底层靠"原子操作 + 内核等待"：`pthread_mutex_lock` 先尝试原子 `lock xchg` 抢锁，抢到直接过；抢不到则陷入内核( futex)睡眠等待，解锁时唤醒。信号量(sem_wait/sem_post)是带计数的锁，值>0 通过并减一，否则睡眠。条件变量(pthread_cond_wait)必须与 mutex 配合：先锁→检查条件→不满足则 `cond_wait`(原子地放锁+睡)→被 `cond_signal` 唤醒后重新抢锁再查。自旋锁(early Linux spin_lock)在预期等待极短时"忙等"不睡眠，省睡眠/唤醒开销但占 CPU——多核下常用(COMP7 缓存一致性保证可见)。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
Dijkstra 1965 提出信号量；Hoare/Monitors(管程)1974 提出条件变量抽象。顶级追问：死锁怎么产生(OS4)、优先级反转(高优先级等低优先级持有的锁, 火星探路者 bug)怎么用优先级继承解决？读写锁(多读单写)、RCU(读copy更新, Linux 内核高频路径用)、无锁结构(lock-free, 靠 CAS 循环)？内存模型(x86 TSO vs ARM 弱序)决定 `atomic` 的 memory_order 怎么选？这些在并发语言(GO1/CPP7/JV6)和高频交易里是巅峰话题。

## 🔴 部分五·顶级视角
顶级并发工程师能据"临界区长短/争用程度/核数"选 mutex/spinlock/RCU/atomic。内核里 spinlock 用于不可睡眠的硬中断上下文；用户态多用 futex 优化(=快速用户态锁, 失败才进内核)。读《The Art of Multiprocessor Programming》。与 OS16 分配器(ptmalloc 用锁保护)、CMP10 GC(并发标记)全部相关。

## 🟠 部分六·安全/合规种子
同步原语是防御竞态漏洞(SEC40 TOCTOU/条件竞争)的工具——很多安全 bug 正是"该加锁没加"或"检查与使用的间隙被抢"。白帽在自己程序练锁完全合规；理解竞态也能帮你审计(SEC20)他人代码的并发漏洞(仅授权样本)。

## ✅ 验收
交：① `os3` 输出 =2000000 截图；② atomic 版输出；③ 说出 mutex/信号量/条件变量各自一句用途。过关。

## ⚠️ 常见坑
- 忘记 `pthread_mutex_unlock` 或异常路径没释放 → 死锁/卡死。
- 条件变量"假醒(spurious wakeup)"：wait 返回后必须重新检查条件(while 而非 if)。
- 护手：复制 `os3.c` 整段。

## ➡️ 下一步
OS4 · 死锁——四个条件凑齐就卡死，银行家算法怎么预防，怎么检测并打破。

### ⚔️ 闯关路线（OS3）
- 🟢 Lv1：用 Python threading + Lock 演示互斥 🥉🥉
- 🔵 Lv2：说明信号量如何控制“最多 N 个同时进” 🈶🥈
- 🟣 Lv3：解释条件变量解决“等某个条件满足再继续” 🥇🥇
- 🟠 Lv4：对比自旋锁（忙等）与睡眠锁（让出 CPU）🏆🏆
- 🔴 Lv5：写 200 字说明“同步原语”是并发正确性的全部基础（接方向1b CPP7/JV6）👑👑


---

# OS4 · 死锁（4 条件 / 银行家 / 检测）

## 🎯 目标
学完这节，你能背出死锁的四个必要条件、用"哲学家就餐"小模型演示死锁、说清预防(破坏任一条件)/避免(银行家算法)/检测(等资源图)/解除(剥夺/回滚)四种策略，并写一个会死锁的多线程程序再用超时锁解开。

## 📋 小白前置
需先懂 OS3（锁、互斥、等待）。知道"图/环"(DSA10/DSA11)概念更易懂检测部分。

## 🟢 部分一·最浅层（生活比喻）
死锁像"两辆车在独木桥两头, 各占一半不肯退, 都等对方先让"——永远卡住。四个条件齐了才会死锁：① 互斥(资源只能一人用)；② 占有且等待(拿着 A 还等 B)；③ 不可抢占(别人不能抢你手里的)；④ 循环等待(A 等 B、B 等 A 成环)。破其中任意一个, 死锁就组不成。

## 🟡 部分二·动手层（逐字操作）
1. 写死锁演示 `dead.c`（两线程以相反顺序拿两把锁）：
```c
#include <stdio.h>
#include <pthread.h>
#include <unistd.h>
pthread_mutex_t a = PTHREAD_MUTEX_INITIALIZER;
pthread_mutex_t b = PTHREAD_MUTEX_INITIALIZER;
void* t1(void* p){ pthread_mutex_lock(&a); usleep(100); pthread_mutex_lock(&b);
                   pthread_mutex_unlock(&b); pthread_mutex_unlock(&a); return 0; }
void* t2(void* p){ pthread_mutex_lock(&b); usleep(100); pthread_mutex_lock(&a);
                   pthread_mutex_unlock(&a); pthread_mutex_unlock(&b); return 0; }
int main(){ pthread_t x,y; pthread_create(&x,0,t1,0); pthread_create(&y,0,t2,0);
           pthread_join(x,0); pthread_join(y,0); printf("结束(若卡住即死锁)\n"); return 0; }
```
2. 编译运行，大概率卡住（Ctrl+C 终止）。这是"循环等待"的活体证据。
3. 修复：让两线程都"按固定顺序(a→b)拿锁"，或改用 `pthread_mutex_trylock` 超时退避。

### 🧪 实战 Lab（OS4）
把 t2 改成"先 a 后 b"（和 t1 同序），重编译运行，应正常打印"结束"。验收：把修复前后运行结果(卡住 vs 正常)说明发我，并写"统一加锁顺序破坏了循环等待条件"。

## 🔵 部分三·原理层（死锁如何形成与判定）
Coffman 四条件(1971)：互斥、持有等待、不可剥夺、循环等待。检测法：把"进程→资源→进程"画成"等待图"，有环即死锁(单类资源)或更复杂需化简(多类)。银行家算法(Dijkstra 1965)：系统假装"若批准这次请求, 之后是否还能找到一种顺序让所有进程跑完(安全序列)"——能则批准, 不能则拒绝等待, 从而避免进入不安全态。解除：终止进程/回滚/剥夺资源，代价最小优先。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
真系统很少跑银行家(开销大、需预知最大需求)，更多用"预防+检测"。顶级追问：数据库用"两阶段锁+死锁检测线程周期性查等待图"；分布式系统死锁(跨机)更难(需超时/vector clock)；"活锁"(都在退避却都不前进) vs 死锁；"优先级反转"伪装成死锁(OS3)。现代语言用"锁顺序静态检查"(Rust 借用检查 RS3 在编译期防数据竞争)从源头消除。

## 🔴 部分五·顶级视角
顶级工程师用"锁层级(lock ordering)"、RAII(C++ CPP1 析构自动解锁)、`std::scoped_lock`(C++17 死锁避免)、Rust 的 `Mutex`(编译期保证)来结构性避免死锁。数据库/OS 内核是死锁检测重灾区。读《Operating Systems: Three Easy Pieces》锁与死锁章(免费开源)。

## 🟠 部分六·安全/合规种子
死锁是正确性/可用性 bug（属 DoS 风险，如"构造请求让服务死锁"可被滥用为拒绝服务——这是安全视角），但今天你只在自己程序观察与修复，合规。理解死锁有助于写健壮服务、审计 DoS 类漏洞(授权)。

## ✅ 验收
交：① 死锁版运行"卡住"说明；② 修复版正常输出；③ 背出死锁四条件。过关。

## ⚠️ 常见坑
- 死锁不可"靠运气"躲过：本例有时能跑完(调度时序不同)，必须用"统一顺序"从结构上消除，不能靠试。
- 把"活锁"当死锁：活锁在动只是无效循环。
- 护手：复制 `dead.c`，用 Ctrl+C 终止实验。

## ➡️ 下一步
OS5 · 内存管理 / 分页——OS 怎么把"程序以为的地址"翻译成"真实内存地址"，分段与分页怎么回事，多级页表怎么省空间。

### ⚔️ 闯关路线（OS4）
- 🟢 Lv1：背出死锁四条件（互斥/占有等待/不可剥夺/循环等待）🥉🥉
- 🔵 Lv2：用比喻说明“四人各拿一双筷子等对方”🈶🥈
- 🟣 Lv3：解释银行家算法如何“假装分配看会不会死锁” 🥇🥇
- 🟠 Lv4：说明破坏任一条件即可预防死锁 🏆🏆
- 🔴 Lv5：写 200 字说明“数据库/锁服务死锁”在生产中的真实危害与检测👑👑


---

# OS5 · 内存管理 / 分页（分段 / 页表 / 多级）

## 🎯 目标
学完这节，你能说清"逻辑地址/线性地址/物理地址"三者区别、"分页"如何解决碎片与隔离、页表怎么把虚拟页映射到物理帧、多级页表为何省内存，并能在自己机器看进程的内存映射(`/proc/pid/maps`)。

## 📋 小白前置
需先懂 COMP1（内存=RAM）、COMP4（TLB/缓存）、ASM4（栈地址）。知道"地址"概念即可。

## 🟢 部分一·最浅层（生活比喻）
分页像"快递柜编号系统"：你(程序)只知道"我的柜子是 0523 号"(虚拟地址)，但 0523 实际对应仓库里哪个物理格子由物业(OS+MMU)偷偷记录(页表)。好处：① 你的 0523 和别人的 0523 是不同物理格子(隔离, 互不踩)；② 物理格子可以不连续, 你以为连续(灵活, 无外部碎片)；③ 不够了物业把不常用的格子暂存到地下室(硬盘, 即交换/虚拟内存 OS6)。

## 🟡 部分二·动手层（逐字操作）
1. 看自己进程的内存映射（虚拟地址各区域分布）：
```bash
cat /proc/self/maps | head -n 20
```
你会看到一堆区间：代码段(r-xp, 可读执行)、数据段(rw-p)、堆([heap])、栈([stack])、共享库(libc.so 等)。这就是"分页后每个区域是若干页"的地图。
2. 用 C 打印变量地址，验证"虚拟地址"存在：
```c
#include <stdio.h>
#include <stdlib.h>
int g; 
int main(){ int l; printf("全局g虚拟地址=%p 局部l虚拟地址=%p 堆=%p\n",(void*)&g,(void*)&l,(void*)malloc(8)); return 0; }
```
```bash
gcc -O2 addr.c -o addr && ./addr
```
这些地址都是"虚拟的"，每个进程都从类似 0x... 开始，互不冲突。

### 🧪 实战 Lab（OS5）
运行 `cat /proc/self/maps` 并数一数你的进程加载了几个共享库(.so)，写一句"每个库占若干虚拟页，物理内存按需映射"。验收：把 maps 前 20 行 + 这句发我。

## 🔵 部分三·原理层（地址怎么翻译）
程序用"虚拟地址(VA)"，CPU 的 MMU 借"页表"翻译成"物理地址(PA)"。页表是多级数组：x86-64 用 4 级(CR3→PML4→PDPT→PD→PT)，每级 9 位索引，共 48 位虚拟地址，页大小 4KB(或 2MB/1GB 大页)。翻译时逐级查表得物理帧号，拼上页内偏移得 PA。TLB(COMP4)缓存最近翻译结果， miss 才走多级页表(慢)。页表项(PTE)还记权限(R/W、U/S、NX 不可执行)，是 DEP(SEC44)的物理基础。分段(老式 x86)把内存按"代码/数据/栈"分段，现代 64 位基本扁平化(段基址=0)，分页成主力。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
分页解决"外部碎片"(早期分段后会剩小缝)与"隔离"。顶级追问：多级页表怎么省内存(不用的中间目录整张不建)？"大页(huge page)"为何减少 TLB miss、提性能(数据库/DPDK 用)？"页表本身占内存"怎么摊(每进程一份, 影子页表用于虚拟化 EPT)；"缺页"怎么触发(OS6)；写时复制(Copy-On-Write, fork 时父子共享页, 写才复制)省内存？这些与 OS7 物理内存、OS15 容器(cgroup 限内存)咬合。

## 🔴 部分五·顶级视角
顶级工程师调"透明大页/NUMA 内存策略/页表回收"优化数据库与虚拟化。理解 VA→PA 是读 `/proc/pid/pagemap`、做内存分析、逆向(SEC25 ELF 段)、容器隔离的基座。读《Three Easy Pieces》"地址空间/分页"章。MMU 与 TLB 设计(COMP4)在本节汇合。

## 🟠 部分六·安全/合规种子
分页的"用户/内核态分离"+"NX 不可执行"是安全基石(SEC44 DEP/ASLR 靠它)。白帽：在自己机器读 `/proc/pid/maps` 完全合规(看自己进程)；但读取他人进程内存、用分页漏洞越权(如 rowhammer 位翻转)只分析不实践。理解页表权限让你写出更安全的程序(如把敏感数据页标只读)。

## ✅ 验收
交：① `addr` 输出(虚拟地址)截图；② `maps` 前 20 行；③ 说出虚拟地址到物理地址靠什么翻译(页表+MMU+TLB)。过关。

## ⚠️ 常见坑
- 以为程序打印的地址是"真实内存位置"：那只是虚拟地址，物理地址由 OS 动态映射，每次运行/每进程都不同。
- MSYS2 的 `/proc/self/maps` 是模拟的，可能不全；深度实验用 WSL2/真实 Linux VM(授权)。
- 护手：复制 `cat /proc/self/maps | head -n 20`。

## ➡️ 下一步
OS6 · 虚拟内存 / 缺页——当程序要的内存"还没真正到位"时，OS 怎么用"缺页异常"按需补上，以及"抖动"为何让系统变慢。

### ⚔️ 闯关路线（OS5）
- 🟢 Lv1：用比喻说明分页像“书拆成固定页、目录查页在哪”🥉🥉
- 🔵 Lv2：解释页表把虚拟地址映射到物理地址 🈶🥈
- 🟣 Lv3：说明多级页表为何省内存 🥇🥇
- 🟠 Lv4：对比分段（按逻辑块）与分页（按固定页）🏆🏆
- 🔴 Lv5：写 200 字说明“虚拟地址”如何隔离进程、是安全沙箱地基👑👑


---

# OS6 · 虚拟内存 / 缺页（局部性 / 抖动）

## 🎯 目标
学完这节，你能说清"虚拟内存"为何能让程序用到比物理内存更大的空间、"缺页(page fault)"是什么、OS 怎么按需把页从磁盘换入、什么是"局部性原理"和"抖动(thrashing)"，并能在自己机器观察内存与交换区。

## 📋 小白前置
需先懂 OS5（分页、页表、虚拟地址）。知道硬盘比内存慢很多(COMP4 金字塔)。

## 🟢 部分一·最浅层（生活比喻）
虚拟内存让"你以为有 100 个柜子，实际仓库只有 20 个"——物业(OS)把不常用的柜子东西先存到地下室(硬盘交换区)，要用时再搬回仓库(物理内存)。"缺页"就是"你开柜子发现东西在地下室，物业跑去搬"的那一下停顿。局部性=你总在翻最近用过的几本(所以搬动不多)。抖动=你不停要不同柜子，物业疲于奔命往返地下室，电脑卡死。

## 🟡 部分二·动手层（逐字操作）
1. 看内存与交换区：
```bash
cat /proc/meminfo | head -n 5
free -h 2>/dev/null
```
2. 演示"按需分页"：写 `touch.c` 分配大数组但只碰一点，观察实际物理占用(RSS)远小于虚拟(VSZ)：
```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main(){
    size_t gb = 1UL*1024*1024*1024;
    char *p = malloc(gb);            // 申请1GB虚拟内存
    printf("虚拟申请了1GB, 先只写1页\n");
    p[0]=1;                          // 触发一页缺页(物理才真正给)
    printf("已写1字节, 程序继续跑(其余页还没真正占用物理内存)\n");
    getchar();
    return 0;
}
```
```bash
gcc -O2 touch.c -o touch && ./touch
# 另终端看: cat /proc/$(pgrep touch)/status | grep -E 'VmSize|VmRSS'
```

### 🧪 实战 Lab（OS6）
运行后在另终端查 `VmSize`(应≈1GB 虚拟)与 `VmRSS`(远小于 1GB, 因为只碰了 1 页)——证明"虚拟大、物理按需"。验收：把这两个值发我 + 一句"缺页让物理内存按需分配"。

## 🔵 部分三·原理层（缺页怎么发生与处理）
访问某虚拟页时，MMU 查页表发现 PTE 的"存在位(Present)"=0 → 触发缺页异常 → CPU 陷入内核缺页处理程序 → OS 判断：① 合法但未分配(如上面 malloc 后未碰)→ 分配物理帧、建映射(可能写时复制)；② 在交换区/文件映射→从磁盘读入；③ 非法地址(野指针)→ 发 SIGSEGV 杀进程。处理完返回重试那条指令。局部性(时间/空间, COMP4)保证"常用页留在内存"，缺页率低则快。抖动=工作集超过物理内存，疯狂换入换出，CPU 大量时间等 IO。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
虚拟内存(1960s Atlas 机首创, 后 Unix/Windows 继承)是"每个进程独享连续大地址空间"的魔法。顶级追问：页面置换算法(LRU 近似, Linux 的 2Q/时钟扫描)怎么选 victim？"工作集模型"怎么定该留多少页？透明大页/内存压缩(zswap)/NUMA 平衡？OOM Killer(内存耗尽杀进程)怎么选牺牲品？"页错误率监控"怎么用 `perf stat -e page-faults`？这些在 SRE(OPS7 调优)、数据库(OS16)是核心。

## 🔴 部分五·顶级视角
顶级 SRE/内核工程师用 `vmstat`、`sar -B`(看 pgfault/pgmajfault)、`perf` 量化缺页，定位"抖动"。理解虚拟内存是容器内存限制(OS15 cgroup)、K8s OOM(OPS26)、JVM GC 堆外内存(JV7)的前提。读《Three Easy Pieces》"虚拟内存/换页"章、Linux `mm/` 源码。

## 🟠 部分六·安全/合规种子
"堆喷射/内存耗尽"可被当 DoS 手段——理解虚拟内存让你写程序时合理限制分配(防御)。白帽：在自己机器跑 `touch.c` 观察缺页合规；但故意让他人系统 OOM/抖动属攻击，绝不碰。ASLR(SEC44)也建立在虚拟地址随机化之上。

## ✅ 验收
交：① `VmSize`/`VmRSS` 对比截图；② 一句话说缺页=访问未映射页触发的异常；③ 说出局部性两类型。过关。

## ⚠️ 常见坑
- 申请 1GB 后以为立刻占 1GB 物理：其实按需，RSS 很小，这是虚拟内存的精妙。
- `getchar()` 等回车，别直接关导致没看到数据。
- 护手：复制 `touch.c` 整段。

## ➡️ 下一步
OS7 · 物理内存管理——OS 怎么把"真实的内存条"切成块分给进程，伙伴系统(buddy)与 slab 分配器怎么高效管。

### ⚔️ 闯关路线（OS6）
- 🟢 Lv1：用比喻说明虚拟内存让你“内存比实际大”🥉🥉
- 🔵 Lv2：解释缺页：访问的页不在内存→操作系统去磁盘取 🈶🥈
- 🟣 Lv3：说明局部性如何让缺页很少发生 🥇🥇
- 🟠 Lv4：解释抖动（thrashing）：频繁缺页反而更慢 🏆🏆
- 🔴 Lv5：写 200 字说明“虚拟内存”如何支撑你跑大模型/大程序（方向9）👑👑


---

# OS7 · 物理内存（伙伴系统 / slab）

## 🎯 目标
学完这节，你能说清 OS 内核怎么管理真实物理内存页（buddy 系统把内存按 2 的幂切块）、怎么用 slab/slub 给内核对象(如 task_struct)做高效小对象分配，并能在自己机器看伙伴系统状态(`/proc/buddyinfo`、`/proc/slabinfo`)。

## 📋 小白前置
需先懂 OS5/OS6（页、页表、虚拟内存）。知道"内核"是管理硬件的程序(后面 OS10 系统调用会深入)。

## 🟢 部分一·最浅层（生活比喻）
物理内存像一整块大草坪，要分给很多人种菜。伙伴系统：把草坪按"2 的幂"切成 1 块、2 块、4 块… 大小的格子，谁要 N 大小就给最接近的 2 的幂那块；还回来时若"隔壁兄弟块也空着"就合并成更大的(减少碎片)。slab 像"专用育苗盘"：内核经常要一堆同样大小的小对象(比如每个进程的控制块), 提前批量做好空盘, 用时空盘直接取, 极快。

## 🟡 部分二·动手层（逐字操作）
1. 看伙伴系统空闲页分布：
```bash
cat /proc/buddyinfo
```
输出每列是"各阶(order)空闲块数"(order 0=1页,1=2页,2=4页…)，反映碎片化。
2. 看 slab 缓存（内核小对象分配器）：
```bash
cat /proc/slabinfo 2>/dev/null | head -n 15
# 或用更友好命令(若可用)
slabtop -o 2>/dev/null | head -n 20 || echo "slabtop 不可用, 用 /proc/slabinfo"
```

### 🧪 实战 Lab（OS7）
运行 `cat /proc/buddyinfo`，数一数 order 0 那一列有多少空闲块（这些是最碎的 4KB 页），写一句"空闲块按 2 的幂组织, 伙伴系统负责合并减碎片"。验收：把 buddyinfo 输出 + 这句发我。

## 🔵 部分三·原理层（内核怎么切物理页）
内核启动时把物理内存登记成"页帧(page frame)"数组。buddy 分配器管理 2^order 个连续页的空闲块链表；分配时从刚好够的 order 取，若没有则向更高 order 拆分(并挂到低阶链表)；释放时查"伙伴块"(同大小、同阶、地址相邻)是否空闲，是则合并升阶——O(1) 合并是其优雅处。slab(源于 Solaris, Linux 用 slub/slob 变体)在 buddy 给的一页或多页上，按对象大小切分、维护空闲链表与着色(cache coloring 避伪共享 COMP7)，专供内核频繁创建销毁的小对象(如 inode、task_struct、skbuff)。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
buddy 解决"外部碎片"，slab 解决"内部碎片+分配速度"。顶级追问：NUMA 下每个节点各有 buddy(本地分配优先, 减跨节点延迟 COMP4)；内存规整(memory compaction)怎么搬页腾出大块给大页？"内存热插拔/碎片整理"；slub 的 per-CPU 缓存怎么免锁快分配？用户态分配器(ptmalloc, OS16)与内核 slab 是两层不同设计。这些在驱动(OS14)、容器内存限制(OS15)都相关。

## 🔴 部分五·顶级视角
顶级内核/驱动工程师调 `/proc/slabinfo` 找"某对象分配暴涨"(内存泄漏线索)、调 buddy 碎片。理解物理内存管理是写内核模块(OS14)、做内存分析、调数据库大页的基础。读《Understanding the Linux Kernel》内存章、Linux `mm/page_alloc.c` 与 `mm/slub.c`。

## 🟠 部分六·安全/合规种子
内核内存管理漏洞(如 slab 越界、UAF, SEC38)是提权重灾区——理解它让你在白帽视角审计/防御(仅自己/授权样本)。自己机器读 `/proc/buddyinfo` 合规；加载内核模块只在本机虚拟机(OS14)实验。

## ✅ 验收
交：① buddyinfo 输出；② 一句话说 buddy 按 2 的幂管理、释放时合并伙伴；③ 说出 slab 用途(小对象高效分配)。过关。

## ⚠️ 常见坑
- MSYS2 下 `/proc/buddyinfo`、`/proc/slabinfo` 可能为空或不存在(Windows 内核非 Linux)——深度看请用 WSL2/真实 Linux VM。
- 把"用户态 malloc(OS16)"和"内核 buddy/slab"混：两层不同，本节讲内核层。
- 护手：复制 `cat /proc/buddyinfo`。

## ➡️ 下一步
OS8 · 文件系统——"一切皆文件"是怎么回事，inode/目录/数据块怎么组织，日志(journal)为何能防断电丢数据。

### ⚔️ 闯关路线（OS7）
- 🟢 Lv1：用比喻说明伙伴系统像“每次分一半的 memory 健身房”🥉🥉
- 🔵 Lv2：解释 slab 分配器为同尺寸对象预建缓存 🈶🥈
- 🟣 Lv3：对比伙伴系统（大块）与 slab（小对象）🥇🥇
- 🟠 Lv4：说明内核对象（如 task_struct）为何用 slab 🏆🏆
- 🔴 Lv5：写 200 字说明“分配器实现”直接决定性能与 UAF 漏洞面（接方向7b SEC36）👑👑


---

# OS8 · 文件系统（inode / 目录 / 日志）

## 🎯 目标
学完这节，你能说清"文件"在磁盘上不是连续躺着、而是由 inode 指向数据块、目录只是"名字→inode 号"的表，理解 ext4/xfs 的日志(journal)如何保证崩溃一致性，并能在自己机器用 `stat`/`df`/`debugfs` 观察 inode 与块。

## 📋 小白前置
需先懂 OS5（地址/映射思维同样适用磁盘块）、COMP1（硬盘是永久仓库）。知道"路径"概念(PRE3 文件管理)。

## 🟢 部分一·最浅层（生活比喻）
文件系统像"图书馆编目"：每本书(文件)有个"索书号"(inode 号)，书本身散落在书架各格(数据块, 不必连续)。目录不是书, 而是一张"书名→索书号"的表格。你要《三体》, 先查目录表得索书号 1234, 再按 1234 去找书的内容。删文件只是"把目录表里那行划掉"(inode 还在, 数据块标记可复用)——所以误删有时能恢复。

## 🟡 部分二·动手层（逐字操作）
1. 看文件的 inode 信息：
```bash
echo "hello" > /tmp/demo.txt
stat /tmp/demo.txt
```
`stat` 输出里 `Inode:` 那行就是索书号，`Links:` 是硬链接数，`Blocks:` 占了几块。
2. 看磁盘用量与 inode 总数：
```bash
df -h /tmp
df -i /tmp
```
3. 看两个文件名"指向同一 inode"(硬链接)：
```bash
ln /tmp/demo.txt /tmp/demo2.txt
ls -li /tmp/demo.txt /tmp/demo2.txt   # 两文件 inode 号相同
```

### 🧪 实战 Lab（OS8）
运行 `ln` 后 `ls -li` 应看到 demo.txt 与 demo2.txt 的 inode 号相同、Links 变 2。再 `rm /tmp/demo.txt`，`stat /tmp/demo2.txt` 仍在——证明"删一个名字不影响数据"。验收：把 `ls -li` 输出 + 这句说明发我。

## 🔵 部分三·原理层（磁盘上怎么组织）
经典 Unix 文件系统：超级块(文件系统元信息)→ 块位图(哪些数据块已用)→ inode 位图(哪些 inode 已用)→ inode 表(每个文件一个 inode, 记权限/大小/时间戳/数据块指针)→ 数据块(真内容)。inode 用"直接块+间接块+二级间接"指向数据(小文件快, 大文件靠间接)。目录文件的内容就是"文件名→inode 号"的键值对。ext4 用 extents(连续块区间)替代散块减少碎片。日志(journal)：把"准备做的元数据改动"先写日志区, 真正提交后再清日志——崩溃重启时要么重放要么丢弃, 保证文件系统一致(不会半吊子)。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
从 FS 分代：FAT(简单链表, 易碎片)→ ext2(类 Unix, 无日志)→ ext3(加 journal)→ ext4(extents/大文件)→ xfs/btrfs/zfs(写时复制 COW, 快照)。顶级追问：COW 文件系统(zfs/btrfs)怎么靠"不改原块、写新块再换指针"实现免日志还能一致+快照？"fsync 到底多慢"(数据库 WAL, DB10 WAL)？inode 耗尽但空间剩(小文件太多)？FUSE 用户态文件系统？这些与数据库存储(DB8 B+树)、容器镜像层(OS15 overlayfs)相关。

## 🔴 部分五·顶级视角
顶级存储/数据库工程师懂 FS 语义对性能的影响(随机写、fsync、direct IO)。容器镜像(overlayfs)、分布式文件系统(ceph)、对象存储(OPS15)都建立在本节抽象上。读《The Linux Programming Interface》文件系统章。逆向里"文件 carving"(SEC88 取证)靠理解数据块布局恢复删除文件。

## 🟠 部分六·安全/合规种子
"路径穿越"(如 `../../etc/passwd`, SEC10/SEC22)利用文件名解析漏洞读越权文件——理解 inode/路径解析帮你写安全文件处理(白帽防御)。自己机器做硬链接实验合规；篡改他人文件系统/绕过权限属攻击, 绝不碰。

## ✅ 验收
交：① `ls -li` 两文件同 inode 截图；② `stat` 输出(含 Inode/Links)；③ 一句话说目录=名字到 inode 的映射。过关。

## ⚠️ 常见坑
- 误以为"删文件=擦除数据"：其实只删目录项, 数据块滞后回收(这也是恢复软件原理)。
- MSYS2 的 `/tmp` 在 Windows 上是模拟, inode 行为可能不标准; 深度实验用 WSL2/Linux VM。
- 护手：复制 `ln /tmp/demo.txt /tmp/demo2.txt`。

## ➡️ 下一步
OS9 · I/O 模型——程序怎么和磁盘/网络说话：阻塞、非阻塞、多路复用(select/poll/epoll)、以及 io_uring 这个新王，理解"为什么高并发服务器不用一个连接一个线程"。

### ⚔️ 闯关路线（OS8）
- 🟢 Lv1：用 ls -i 看文件的 inode 号 🥉🥉
- 🔵 Lv2：说明目录只是“名字→inode”的映射表 🈶🥈
- 🟣 Lv3：解释日志文件系统如何在崩溃后恢复 🥇🥇
- 🟠 Lv4：对比 ext4 / xfs / NTFS 的设计侧重 🏆🏆
- 🔴 Lv5：写 200 字说明“文件系统”是数据库（方向5）与一切存储的底层👑👑


---

# OS9 · I/O 模型（阻塞 / 非阻塞 / epoll / io_uring）

## 🎯 目标
学完这节，你能说清阻塞 I/O、非阻塞 I/O、I/O 多路复用(select/poll/epoll)、异步 I/O(io_uring)的区别与取舍，理解"一个连接一个线程"为何撑不住高并发，并能在自己机器用 Python/strace 看一个程序在 `read` 上阻塞。

## 📋 小白前置
需先懂 OS1（进程/线程、上下文切换代价）、OS8（文件是 I/O 对象）、COMP5（I/O 与中断）。知道"网络 socket"概念最好(Direction4 NET 可后补，本节用文件/pipe 演示也行)。

## 🟢 部分一·最浅层（生活比喻）
阻塞 I/O 像"你打电话点餐, 拿手机干等直到饭到, 期间啥也不干"。非阻塞像"点完餐挂了, 每隔 10 秒回拨问好了没"(忙等轮询)。多路复用(epoll)像"你给了前台一张清单, 说'这几家谁好了叫我', 你只等一个电话"(一次管上万连接)。异步(io_uring)像"你留了地址, 饭好直接送上门, 你全程没主动等过"。

## 🟡 部分二·动手层（逐字操作）
1. 演示阻塞：写一个读 stdin 就卡住等输入的 `block.c`：
```c
#include <stdio.h>
int main(){ char b[64]; fgets(b,64,stdin); printf("你输入了:%s",b); return 0; }
```
```bash
gcc -O2 block.c -o block && ./block   # 运行后程序卡在等输入, 敲字回车才继续
```
2. 用 strace（若装了）看它在 `read` 上阻塞：
```bash
strace -e trace=read,write ./block 2>&1 | head
```
会看到 `read(0, ...` 卡住直到输入。
3. （概念）epoll 代码片段骨架（仅展示，不必本机跑全）：
```c
int ep = epoll_create1(0);
struct epoll_event ev = {.events=EPOLLIN, .data.fd=fd};
epoll_ctl(ep, EPOLL_CTL_ADD, fd, &ev);
struct epoll_event evs[64];
int n = epoll_wait(ep, evs, 64, -1);  // 一次等多个 fd 可读
```

### 🧪 实战 Lab（OS9）
运行 `./block`，确认它在等你输入（这就是阻塞 I/O 的直观证据）。再用 `strace` 找到 `read` 系统调用那行。验收：把"程序卡住等你输入"的现象描述 + strace 中 read 行发我。

## 🔵 部分三·原理层（五种 I/O 模型）
① 阻塞：调 `read` 后进程睡眠, 数据就绪+拷贝完才返回。② 非阻塞：fd 设 O_NONBLOCK, `read` 立即返回 EAGAIN, 需自己轮询(浪费 CPU)。③ I/O 多路复用：select/poll 遍历 fd 集(低效 O(n))；epoll(Linux)用内核红黑树+就绪链表, 只回报就绪 fd(O(1) 增删、O(就绪数) 返回), 单机撑十万~百万连接。④ 信号驱动(少用)。⑤ 异步 AIO/io_uring：提交请求后立刻返回, 内核完成后通知, 全程不阻塞不轮询。前四种"等数据就绪"方式不同, 但 `read` 拷贝阶段仍可能阻塞; 真异步连拷贝都异步。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
"一个连接一个线程"在 C10K/C100K 问题下, 线程上下文切换(COMP4/OS1)和内存(每线程栈 8MB)撑爆。epoll 是 Linux 高并发基石(Nginx/Redis/Node.js NET 都用)。顶级追问：epoll 的 LT(水平触发, 没处理完下次还报) vs ET(边缘触发, 只报一次, 必须一次读净)差异与坑？io_uring(2019)用"提交/完成环形队列+内核轮询"近乎零拷贝零系统调用开销, 是未来方向。Windows IOCP、FreeBSD kqueue 是对应物。DPDK/内核旁路(NET18)更进一步绕开 OS。

## 🔴 部分五·顶级视角
顶级后端工程师据场景选模型：低频用线程池, 高并发用 epoll/io_uring, 极致用 DPDK/RDMA。理解 I/O 模型是写高性能网络服务(NET17 socket、BE23 部署)、理解 Node 事件循环(JS8)、Go netpoll(GO4)的前提。读《The Linux Programming Interface》I/O 章、C10K/C10M 经典文章。

## 🟠 部分六·安全/合规种子
I/O 多路复用/异步本身是性能技术, 无攻击面。但"慢速连接耗尽 epoll 连接表"可被当成 DoS(如 Slowloris)——理解它帮你写健壮服务端(设超时)。自己机器跑 `block.c` 合规。

## ✅ 验收
交：① `block` 卡住等输入的现象说明；② strace 中 read 行；③ 说出 epoll 比 select 好在哪(不 O(n) 遍历)。过关。

## ⚠️ 常见坑
- strace 在 MSYS2 可能不可用(它是 Linux 工具)；深度实验用 WSL2/Linux VM, 或接受"卡住等输入"作为阻塞证据。
- 把"非阻塞"当"快"：非阻塞轮询反而更费 CPU。
- 护手：复制 `gcc -O2 block.c -o block && ./block`。

## ➡️ 下一步
OS10 · 中断与系统调用——用户程序怎么"请求内核帮忙"(如读文件)，陷阱/门/上下文切换在底层怎么发生，这是"用户态↔内核态"的桥。

### ⚔️ 闯关路线（OS9）
- 🟢 Lv1：用比喻说明阻塞 I/O 像“排队等水接满才走”🥉🥉
- 🔵 Lv2：解释 epoll 如何“一次等多个事件”高效 🈶🥈
- 🟣 Lv3：对比 select/poll/epoll 的复杂度 🥇🥇
- 🟠 Lv4：说明 io_uring 的“提交/完成队列”更省系统调用 🏆🏆
- 🔴 Lv5：写 200 字说明“I/O 模型”决定高并发服务器上限（接方向6 BE23/Net18）👑👑


---

# OS10 · 中断与系统调用（陷阱 / 门 / 上下文）

## 🎯 目标
学完这节，你能说清"用户态 vs 内核态"为什么存在、系统调用(syscall)不是普通函数调用而是"陷入内核"、中断与陷阱(trap)的区别，并能在自己机器用 `strace` 看到程序发了哪些 syscall（如 `write`/`exit`）。

## 📋 小白前置
需先懂 COMP5（中断概念）、ASM1–ASM3（寄存器、call/ret）、OS1（进程）。知道"特权级 ring 0/3"雏形(COMP3 ISA 提过)。

## 🟢 部分一·最浅层（生活比喻）
电脑像一栋楼：你在"公共大厅"(用户态, ring 3)活动，不能随意进"机房"(内核态, ring 0)改总闸。要机房帮忙(比如读硬盘)，你按墙上"求助按钮"(syscall 指令)→ 保安(CPU)验证你是合法请求→ 把你临时带到机房办完事→ 送回大厅。这个"按按钮+被带进机房"就是系统调用。中断则是"机房外来了急电(硬件信号)，保安放下手头活去处理"。

## 🟡 部分二·动手层（逐字操作）
1. 用 strace 看一个简单程序发起了哪些 syscall（若可用）：
```bash
strace -f echo "hi" 2>&1 | grep -E 'write|execve|exit' | head
```
你会看到 `write(1, "hi", ...)`、`exit_group(...)` 等——这就是"用户程序通过 syscall 让内核干活"。
2. 没有 strace 时，用我们 ASM1 的 `hello.asm` 看的 `syscall` 指令本身即"按求助按钮"：
```bash
cat asm_hello.s 2>/dev/null || echo "回看 ASM1 的 syscall 用法: mov rax,1; syscall"
```

### 🧪 实战 Lab（OS10）
运行 `strace -f echo hi`，在输出里找到 `write` 那一行，抄下来。写一句"echo 不是自己写屏幕, 而是请求内核 write 到 fd=1(stdout)"。验收：把 write 行 + 这句发我（无 strace 时用 ASM1 的 syscall 段说明替代）。

## 🔵 部分三·原理层（syscall 怎么陷进去）
x86-64 用户态执行 `syscall` 指令 → CPU 切到 ring 0、跳到内核预设的"系统调用入口"(MSR 寄存器指 LSTAR)→ 内核按 `rax` 里的号查"系统调用表(sys_call_table)"找到对应处理函数(如 `sys_write`)→ 从寄存器取参数(rdi/rsi/rdx…，ASM3 约定)→ 执行(此时有内核特权, 能碰硬件/页表)→ 结果写回 rax → `sysret` 回用户态。整个过程伴随"上下文切换"(保存用户寄存器到内核栈, OS1)。中断(硬件 IRQ)和异常(如缺页 OS6、除零)走类似"门"机制(IDT 中断描述符表)，但来源不同：syscall 是"主动求助", 中断是"被动来信号", 异常是"执行出错"。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
特权级隔离(ring 0/3)是安全基石：用户程序不能直接碰硬件/他人内存, 一切经内核仲裁(SEC44 的防护也依赖此)。顶级追问：syscall 比老式 `int 0x80`(软中断)快在哪(少查 IDT、直接 MSR 跳转)？"vDSO"怎么把少数安全 syscall(gettimeofday)直接映射用户态免陷入？系统调用开销(上下文切换+ Spectre 防护开销)为何在高频路径要用 io_uring(OS9)/批处理？内核入口的"幽灵漏洞"防护(开销)？这些与 COMP2 推测执行、SEC 方向交叉。

## 🔴 部分五·顶级视角
顶级系统/安全工程师精读 syscall 路径(Linux `arch/x86/entry/entry_64.S`、`kernel/sys.c`)。理解 syscalls 是写任何"碰硬件/网络/文件"程序的底座(NET17 socket、OS8 文件、BE 后端)。strace/perf trace 是排查"程序卡在哪个 syscall"的利器(OPS8 日志/SRE)。

## 🟠 部分六·安全/合规种子
syscall 机制本身是防御设计(隔离)。但"系统调用号滥用/未授权 syscall"是沙箱逃逸(SEC45)的研究对象——白帽只分析原理。自己机器用 strace 看自己程序合规；不对自己无权限的程序 strace 他人进程。

## ✅ 验收
交：① strace 里 write 行(或 ASM1 syscall 说明)；② 一句话说 syscall 是"用户态主动陷入内核"；③ 区分 trap(主动)/interrupt(硬件)/exception(出错)。过关。

## ⚠️ 常见坑
- 把 `syscall`(指令)与"系统调用号(rax 值)"混：前者是动作, 后者是"要哪个服务"。
- MSYS2 无 strace；用 WSL2/Linux VM 做深度实验, 或接受 ASM1 的 syscall 段作为证据。
- 护手：复制 `strace -f echo hi 2>&1 | grep -E 'write|execve|exit'`。

## ➡️ 下一步
OS11 · IPC（进程间通信）——孤立的进程怎么互相传话/共享数据：管道、共享内存、消息队列、信号各自怎么用、何时选谁。

### ⚔️ 闯关路线（OS10）
- 🟢 Lv1：用 strace 或 Procmon 看一个程序的系统调用 🥉🥉
- 🔵 Lv2：说明系统调用是“用户态→内核态的受控门” 🈶🥈
- 🟣 Lv3：解释中断与陷阱（trap）如何进入内核 🥇🥇
- 🟠 Lv4：说明系统调用号 + 参数寄存器 的约定 🏆🏆
- 🔴 Lv5：写 200 字说明“系统调用表”是白帽审计/沙箱逃逸的切入点（仅分析）👑👑


---

# OS11 · IPC（管道 / 共享内存 / 消息队列 / 信号）

## 🎯 目标
学完这节，你能说清四大类进程间通信（管道 pipe、共享内存 shm、消息队列、信号 signal）各自的机制与适用场景，并能在自己机器用 C 写一个"父子进程用管道传一句字符串"的小程序。

## 📋 小白前置
需先懂 OS1（进程/线程、独立地址空间）、OS10（用户态/内核态、syscall）。知道"父子进程"(fork, 本节能用但不用深究)。

## 🟢 部分一·最浅层（生活比喻）
两个独立公司(进程)要合作：① 管道(pipe)像"两公司间拉一根管子, 一端倒水一端接"(字节流, 单向)；② 共享内存像"两公司合租一个仓库, 都能直接进去拿"(最快, 但要自己加锁防撞, OS3)；③ 消息队列像"前台留言之类的小纸条盒, 一条条存着按顺序取"；④ 信号像"大喇叭喊一嗓子"(如"下班了!"=SIGTERM), 只能传极简信息不能带数据。

## 🟡 部分二·动手层（逐字操作）
1. 写 `pipe.c`：父进程通过管道给子进程发一句 "hello child"：
```c
#include <stdio.h>
#include <unistd.h>
#include <string.h>
int main(){
    int fd[2]; pipe(fd);            // fd[0]读 fd[1]写
    if(fork()==0){                  // 子进程
        char buf[64];
        read(fd[0], buf, sizeof buf);
        printf("子进程收到: %s\n", buf);
        return 0;
    }
    char *m = "hello child";
    write(fd[1], m, strlen(m)+1);
    wait(0);                        // 等子进程
    return 0;
}
```
2. 编译运行：
```bash
gcc -O2 pipe.c -o pipe && ./pipe
```
预期打印"子进程收到: hello child"。

### 🧪 实战 Lab（OS11）
把消息改成中文"泽泽加油"，重编译运行。再思考：若想"双向聊天"需要几个管道？验收：把运行输出截图 + 一句"双向通信需两个管道(或改用 socketpair)"发我。

## 🔵 部分三·原理层（四种 IPC 怎么实现）
管道：内核里一个环形缓冲区, `pipe()` 返回读写两端 fd, 数据经内核拷贝(父写→内核 buf→子读)。共享内存(`shmget`/`mmap MAP_SHARED`)：把同一物理页映射到两进程虚拟地址, 零拷贝但需配信号量/锁(OS3)同步。"消息队列"(System V/`mq_`)：内核维护的有界消息链表, 支持类型/优先级。信号(`kill`/`sigaction`, OS12)：最轻量, 只通知"某事件", 不带数据, 异步到达。还有关联的：socket(NET17, 可跨机)、信号量(OS3)、文件锁。选择看"数据量/方向/跨机/性能"：大块共享数据用 shm, 流式用 pipe/socket, 通知用 signal。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
管道源自 Unix "一切皆文件"哲学(读写作文件操作, OS8)。顶级追问：共享内存最快但同步最难(伪共享 COMP7、ABA 问题)；"零拷贝"(sendfile/splice)怎么让文件直送 socket 不拷贝进用户态(BE23 静态服务)？命名管道(FIFO)让无亲缘进程也能用 pipe；"共享内存+ futex"是线程/进程同步高性能组合。现代微服务常用网络 IPC(socket/gRPC NET19)而非本地 IPC, 因跨机与版本解耦。

## 🔴 部分五·顶级视角
顶级工程师据延迟/吞吐选型：数据库主从(DB13)用网络协议；同机高频(如 Nginx 多 worker 传缓存)用共享内存；Shell 管道(`ls | grep`)就是 pipe 的经典用法。读《TLPI》IPC 章。理解 IPC 是容器通信(OS15)、微服务(NET19)的底层。

## 🟠 部分六·安全/合规种子
IPC 是进程协作基础, 本身中性。但"共享内存未加锁"可成竞态/信息泄露源(SEC20 审计点)；信号可被用于"信号注入"类攻击(仅分析)。白帽：在自己程序练 pipe/shm 完全合规。理解 IPC 有助于设计安全的进程边界。

## ✅ 验收
交：① `pipe` 输出截图；② 双向通信需两管道的分析句；③ 列出四种 IPC 名。过关。

## ⚠️ 常见坑
- 忘了 `wait(0)`, 父进程先退出导致看不到子输出(或输出乱序)。
- 管道单向：用一个 fd 既读又写会死锁; 双向要两个管道或 socketpair。
- 护手：复制 `pipe.c` 整段。

## ➡️ 下一步
OS12 · 信号（Signal）——异步"通知"机制：信号怎么发、怎么处理、为什么处理函数必须"可重入"，以及 sigaction 比老 signal 好在哪。

### ⚔️ 闯关路线（OS11）
- 🟢 Lv1：用管道 `|` 串两个命令看效果 🥉🥉
- 🔵 Lv2：解释共享内存为何最快（直接读写同一块）🈶🥈
- 🟣 Lv3：说明消息队列与信号的差异 🥇🥇
- 🟠 Lv4：对比各 IPC 方式的性能与安全边界 🏆🏆
- 🔴 Lv5：写 200 字说明“IPC”是容器/微服务通信的远古雏形（接方向6/10）👑👑


---

# OS12 · 信号（Signal / 异步 / 可重入 / sigaction）

## 🎯 目标
学完这节，你能说清信号是"异步中断式通知"、常见信号(SIGINT/SIGTERM/SIGSEGV/SIGCHLD)含义、用 `sigaction` 注册处理函数、理解"可重入(reentrant)函数"为什么在处理函数里是铁律，并写一个捕获 Ctrl+C(SIGINT)优雅退出的程序。

## 📋 小白前置
需先懂 OS11（信号是四大 IPC 之一）、OS1（进程）、COMP5（中断——信号本质是"软件中断"）。知道"回调函数"概念(PY14 装饰器/JS 回调更好懂)。

## 🟢 部分一·最浅层（生活比喻）
信号像"手机突然弹通知"：你正干别的事, 叮一声(信号到), 你放下手头去点开(执行处理函数), 处理完回来继续。和电话(同步调用)不同, 通知是"随时可能来"。但麻烦在于：你正削苹果(主流程)时通知来了, 你去回消息(处理函数), 回消息里又削苹果——如果苹果刀只有一把且状态混乱, 就出事。所以"处理函数里干的事"必须干净利落、不碰主流程正在用的东西(可重入)。

## 🟡 部分二·动手层（逐字操作）
1. 写 `sig.c`：捕获 SIGINT(Ctrl+C)打印一句并退出：
```c
#include <stdio.h>
#include <signal.h>
#include <unistd.h>
volatile sig_atomic_t got = 0;
void handler(int s){ got = 1; printf("收到信号 %d, 准备退出\n", s); }
int main(){
    struct sigaction sa = {.sa_handler = handler};
    sigaction(SIGINT, &sa, 0);
    while(!got){ printf("运行中... (Ctrl+C 退出)\n"); sleep(1); }
    printf("优雅退出\n");
    return 0;
}
```
2. 编译运行，按 `Ctrl+C`：
```bash
gcc -O2 sig.c -o sig && ./sig
# 运行中按 Ctrl+C
```

### 🧪 实战 Lab（OS12）
运行后按 Ctrl+C，观察打印"收到信号 2(=SIGINT), 准备退出"再"优雅退出"。把 `printf` 换成"用 `write` 系统调用而非 printf"(因为 printf 非可重入)是更安全的写法——请你在代码注释里说明"为什么处理函数里 printf 有风险"。验收：把运行输出 + 这句风险说明发我。

## 🔵 部分三·原理层（信号怎么送达与处理）
进程收到信号后, 内核在"从内核态返回用户态"的检查点查"挂起信号集", 若有且未被阻塞, 则暂停原流程, 切到用户态执行注册的处理函数(若 `SIG_DFL` 则执行默认动作如终止, `SIG_IGN` 则忽略)。处理函数返回后 `sigreturn` 回原处继续。"可重入"含义：处理函数可能在任意时刻打断主流程, 故只能调用"可重入/异步信号安全"函数(如 `write`, 列表见 `man 7 signal-safety`), 不能调 `printf/malloc`(内部用全局锁/堆, 可能与主流程冲突死锁)。`sig_atomic_t` 类型保证读写不被信号打断, 适合做标志位。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
信号源自 Unix 早期(1970s), 是"软件中断"抽象。顶级追问：信号不排队(同信号多次到达可能只记一次), 所以不适合传精确数据(用管道/eventfd 替代)；`sigprocmask` 阻塞信号防重入区；实时信号(SIGRTMIN+)支持排队与携带数据；`signalfd`(把信号转成可读 fd, 配合 epoll OS9)是现代事件循环处理信号的安全方式(避免异步陷阱)。"信号掩码/未决集"怎么用？这些在 Nginx/Redis 信号处理、容器优雅退出(OS15)都关键。

## 🔴 部分五·顶级视角
顶级工程师用信号做"热重载(SIGHUP 重新加载配置)"、"优雅关停(SIGTERM 先处理完在途请求)"、"崩溃诊断(SIGSEGV 接住打 core dump)"。理解信号是写 daemon/服务(BE 后端、OPS10 systemd)的必备。与 OS11 IPC、OS10 syscall 构成"进程控制"三件套。

## 🟠 部分六·安全/合规种子
信号可被用于"信号注入/竞态"(如 TOCTOU 利用信号在检查与使用间打断)——白帽只分析原理用于防御(SEC40)。自己机器练 sigaction 合规；不对他人进程发信号(那等同干扰)。

## ✅ 验收
交：① `sig` 按 Ctrl+C 输出截图；② 一句"处理函数里 printf 非异步信号安全, 应用 write"；③ 说出 SIGINT/SIGTERM/SIGSEGV 默认动作。过关。

## ⚠️ 常见坑
- 在处理函数里调用 `printf`/`malloc` 可能死锁(非可重入)——真写服务要避开, 用 `write` 或仅设标志。
- 忘了 `volatile sig_atomic_t`，编译器可能把 `got` 优化进寄存器导致循环不退出。
- 护手：复制 `sig.c` 整段。

## ➡️ 下一步
OS13 · 引导与启动——你按下开机键到看到桌面, 中间发生了什么：BIOS/UEFI、bootloader、内核怎么被装进内存跑起来。

### ⚔️ 闯关路线（OS12）
- 🟢 Lv1：用 Ctrl+C 触发 SIGINT 看程序退出 🥉🥉
- 🔵 Lv2：说明信号是“异步通知”，可能在任意时刻到 🈶🥈
- 🟣 Lv3：解释可重入：信号处理函数里不能调非可重入函数 🥇🥇
- 🟠 Lv4：用 sigaction 注册自定义 handler 替代默认 🏆🏆
- 🔴 Lv5：写 200 字说明“信号处理不当”曾引发大量安全/稳定 bug👑👑


---

# OS13 · 引导与启动（BIOS / UEFI / bootloader）

## 🎯 目标
学完这节，你能讲清从"按电源"到"操作系统就绪"的完整链路：固件(BIOS/UEFI)→ bootloader(GRUB)→ 内核解压/初始化→ 第一个用户进程(init/systemd)，并能在自己机器用工具看启动信息(Windows 用 `msinfo32`/系统日志, Linux 用 `dmesg`)。

## 📋 小白前置
需先懂 COMP1（CPU 四大件、加电即从固定地址取指）、OS1（进程概念，init 是 1 号进程）。知道"固件"是焊在主板上的小程序即可。

## 🟢 部分一·最浅层（生活比喻）
开机像"剧场开演前的准备"：先由"值班大爷"(固件 BIOS/UEFI)检查灯水电(硬件自检), 然后大爷去"拿场记本"(从硬盘指定位置读 bootloader), 场记本告诉大爷"今天的戏(内核)在哪、怎么搬上舞台(内存)", 内核上台后自己布置场地、点亮所有灯, 最后请出"导演"(init 进程)开始正式演出(用户登录界面)。

## 🟡 部分二·动手层（逐字操作）
1. 看固件类型（Windows 上判断是 BIOS 还是 UEFI）：
```powershell
# 在 PowerShell 运行(Windows 原生):
$env:firmware = (Get-ComputerInfo).BiosFirmwareType
echo "固件类型: $env:firmware"
```
或图形化：开始→运行→`msinfo32`→ 看"BIOS 模式"是"传统"还是"UEFI"。
2. 看启动日志（Linux/WSL2 下）：
```bash
dmesg | head -n 20
```
你会看到内核初始化的早期信息(CPU 探测、内存布局等)。
3. Windows 看启动记录：事件查看器→ Windows 日志→ 系统，筛选事件 ID 12/13(开机/关机)。

### 🧪 实战 Lab（OS13）
运行上面的 PowerShell 或 `msinfo32`，确认你的机器是 UEFI 还是传统 BIOS，写一句"UEFI 比 BIOS 新, 支持安全启动(Secure Boot)防引导级恶意软件"。验收：把固件类型结果 + 这句发我。

## 🔵 部分三·原理层（开机链路怎么走）
加电后 CPU 从固件 ROM 的固定复位向量开始执行。BIOS(老, 16 位, MBR 512 字节引导)做 POST(加电自检)→ 读 MBR → 跳 bootloader。UEFI(新, 32/64 位, GPT 分区, 可读文件系统的 .efi 程序)直接加载 bootloader，并支持 Secure Boot(验签名防恶意引导)。bootloader(GRUB/Limine)选内核、传参数、把内核映像和设备树搬进内存、切到保护/长模式跳内核入口。内核自解压、初始化页表(OS5)/中断(OS10)/调度(OS2)/各子系统、挂载根文件系统(OS8)、启动 init(1 号进程, 此后用户态世界展开, OS1)。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
BIOS 源自 1970s IBM PC, 局限(2TB 磁盘上限、慢、无安全)。UEFI 是 Intel EFI 演化, 解决这些。顶级追问：Secure Boot 用公钥验 bootloader/内核签名(防 bootkit, 白帽防御视角)；"可信启动(TPM 度量)"怎么把启动链哈希存进 TPM 供远程证明？"kexec"怎么不重启换内核？initramfs(临时根文件系统, 含驱动)为何必要(内核要先有磁盘驱动才能挂真根)？这些与 OS15 容器、SEC 固件安全(MOB53)相关。

## 🔴 部分五·顶级视角
顶级嵌入式/系统工程师写 bootloader(U-Boot)、移植内核到新板、做安全启动。理解启动链是诊断"黑屏/起不来"、做固化系统(路由器/IoT MOB50-53)、容器/VM 启动优化的基础。开源：coreboot(开源固件)、U-Boot、GRUB、Linux `init/main.c`。

## 🟠 部分六·安全/合规种子
引导级恶意软件(bootkit/rootkit SEC61)在此层最危险——Secure Boot/TPM 是防御。白帽：在自己机器看启动信息合规；但刷写他人固件、绕过 Secure Boot 属攻击, 绝不碰(红线: 不逆向/不绕过他人系统)。

## ✅ 验收
交：① 固件类型(UEFI/BIOS)结果；② 一句话说 UEFI 支持 Secure Boot 防引导恶意软件；③ 能按顺序背出 固件→bootloader→内核→init。过关。

## ⚠️ 常见坑
- 把"BIOS/UEFI 是软件还是硬件"搞混：它是焊在主板 ROM 里的固件程序, 介于软硬之间。
- MSYS2 下 `dmesg` 可能无权限/不可用；用 WSL2 或 Windows 事件查看器替代。
- 护手：复制 PowerShell 的 Get-ComputerInfo 命令(在 PowerShell 跑, 非 Git Bash)。

## ➡️ 下一步
OS14 · 内核模块 LKM——你能给运行中的 Linux 内核"热插拔"一段代码吗：字符设备、模块参数怎么写（仅在本机虚拟机实验）。

### ⚔️ 闯关路线（OS13）
- 🟢 Lv1：用比喻说明 BIOS/UEFI 是“开机后第一个醒来的程序”🥉🥉
- 🔵 Lv2：说明 bootloader 把内核从磁盘搬到内存 🈶🥈
- 🟣 Lv3：解释 UEFI 比 BIOS 更安全（带签名校验）🥇🥇
- 🟠 Lv4：说明“安全启动”如何防恶意 bootloader 🏆🏆
- 🔴 Lv5：写 200 字白帽说明：引导链是信任根，但也是攻击面（仅分析授权样本）👑👑


---

# OS14 · 内核模块 LKM（字符设备 / 模块参数）

## 🎯 目标
学完这节，你能说清"内核模块(LKM)"是什么、为什么能动态加载进内核、一个最简单的 hello 模块怎么写、字符设备(device)怎么向用户态暴露接口，并明确实验只在你自己控制的 Linux 虚拟机里做（绝不在他人/生产机加载任何模块）。

## 📋 小白前置
需先懂 OS10（用户态/内核态、syscall）、OS8（设备文件、/dev）、OS13（内核启动）。C 语言基础(C1–C5)必备，因为模块是 C 写的。

## 🟢 部分一·最浅层（生活比喻）
内核模块像"给运行中的大楼机房临时加装一个功能插件"：大楼(内核)已经在跑, 你不用停工重建, 直接把插件(`.ko` 文件)插进机房的插槽, 它就获得机房的全部权限(ring 0)开始干活；拔掉插件(卸载)功能消失。字符设备像"机房在 /dev 开的一个专属窗口", 用户程序用 `open/read/write` 这个窗口就能和内核插件对话。

## 🟡 部分二·动手层（逐字操作，⚠️ 仅在你自己的 Linux 虚拟机/ WSL2 内做）
1. 确认有内核头文件（Ubuntu/Debian）：
```bash
uname -r
ls /lib/modules/$(uname -r)/build 2>/dev/null && echo "有内核头, 可编译模块" || echo "需 sudo apt install linux-headers-$(uname -r)"
```
2. 写最小模块 `hello_mod.c`：
```c
#include <linux/module.h>
#include <linux/kernel.h>
static int myparam = 1;
module_param(myparam, int, 0444);   // 模块参数, 可在加载时改
static int __init hi_init(void){ printk(KERN_INFO "hello_mod: 加载, 参数=%d\n", myparam); return 0; }
static void __exit hi_exit(void){ printk(KERN_INFO "hello_mod: 卸载\n"); }
module_init(hi_init); module_exit(hi_exit);
MODULE_LICENSE("GPL"); MODULE_AUTHOR("zeze");
```
3. Makefile：
```make
obj-m += hello_mod.o
all: make -C /lib/modules/$(shell uname -r)/build M=$(PWD) modules
clean: make -C /lib/modules/$(shell uname -r)/build M=$(PWD) clean
```
4. 编译加载看内核日志：
```bash
make
sudo insmod hello_mod.ko myparam=7
dmesg | tail -n 3
sudo rmmod hello_mod
dmesg | tail -n 3
```

### 🧪 实战 Lab（OS14）
在虚拟机里完成"加载+看 dmesg 输出+卸载"，把 `dmesg` 中 "加载, 参数=7" 和 "卸载" 两行截图。若你当前只有 Windows/MSYS2 无法编译模块，则写一份"模块参数 myparam 的作用说明 + 为什么模块运行在 ring 0"的笔记代替，并标注"实验待在授权 Linux VM 完成"。验收：截图或笔记 + 一句"模块在内核态, 出错会导致整机崩, 所以只在自己 VM 练"。

## 🔵 部分三·原理层（模块怎么进内核）
模块是独立编译、可动态链接进内核地址空间(OS5 内核映射)的 ELF 对象(`objdump -h hello_mod.ko` 可看段)。`insmod` 走 `init_module` syscall 把 `.ko` 拷入内核、做重定位(CMP9)、调模块 `init` 函数。`module_param` 在内核 `sysfs`(/sys)暴露参数, 加载时 `insmod xxx p=7` 写入。字符设备用 `register_chrdev` 申请主设备号, 建 `/dev/xxx`, 用户态 `open/read/write` 经 VFS→ 设备驱动函数(OS8 文件接口统一)。模块出错(空指针)直接触发内核 oops/panic, 因为无用户态保护(OS5/OS10 隔离在此失效)。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
LKM 让发行版不必把所有驱动编进内核(省内存/易更新)。顶级追问：模块签名(防未签名恶意模块加载, 呼应 OS13 Secure Boot)；"内核与模块版本严格对应"(vermagic)否则拒载；字符设备 vs 块设备(磁盘) vs 网络设备(OS17)三大类；`ioctl` 怎么传复杂控制命令；`procfs/sysfs/debugfs` 调试接口。这些与 OS15 容器(设备透传)、驱动逆向(MOB18)相关。

## 🔴 部分五·顶级视角
顶级内核/驱动工程师写网卡/显卡/存储驱动、eBPF(现代"安全模块"替代, OPS41)程序。理解 LKM 是做底层系统编程、IoT(MOB50-53)、安全研究(仅授权)的基座。开源：Linux `drivers/` 目录、The Linux Kernel Module Programming Guide。

## 🟠 部分六·安全/合规种子（本节重点红线）
⚠️ 内核模块拥有 ring 0 全部权限, 一旦加载便与内核同权。白帽铁律:**只在你自己创建/完全控制的 Linux 虚拟机里编译加载模块做学习**；绝不在他人机器、公司/云生产机、他人设备上加载任何自写或来源不明的模块(那等于给对方内核装后门, 属严重违规/违法)。逆向他人驱动只分析不实践。eBPF 是现代更安全的内核编程入口, 值得优先学。

## ✅ 验收
交：① 虚拟机里 dmesg 加载/卸载两行截图（或替代笔记）；② 一句"模块 ring 0, 只在自己 VM 练"；③ 说出字符设备是什么(/dev 下的窗口)。过关。

## ⚠️ 常见坑
- 在 Windows/MSYS2 直接编模块会失败：模块需 Linux 内核头, 用 WSL2 或 VirtualBox/VMware 里的真实 Linux。
- `MODULE_LICENSE` 不匹配 GPL 会污染内核(taint), 调试信息受限——学习用 GPL 即可。
- 护手：复制代码块；实验在 VM 内做, 本机 Windows 不必强行。

## ➡️ 下一步
OS15 · 容器原理——Docker 的魔法其实不是虚拟机：namespace 隔离、cgroup 限资源、seccomp 收窄 syscall，理解"容器为什么轻"。

### ⚔️ 闯关路线（OS14）
- 🟢 Lv1：在 Linux 虚拟机里加载一个 hello LKM（仅自己 VM）🥉🥉
- 🔵 Lv2：说明字符设备如何暴露 /dev/xxx 给用户态 🈶🥈
- 🟣 Lv3：解释模块参数如何传入内核 🥇🥇
- 🟠 Lv4：说明内核模块崩溃会拖垮整个系统（与用户态不同）🏆🏆
- 🔴 Lv5：写 200 字白帽声明：内核模块/驱动研究只在自己 VM，绝不对外投送👑👑


---

# OS15 · 容器原理（namespace / cgroup / seccomp）

## 🎯 目标
学完这节，你能说清容器为什么"不是虚拟机"——它和宿主机共用同一个内核，靠 namespace 做隔离、cgroup 做资源限制、seccomp 收窄系统调用面，并能在自己机器用 Docker 起一个容器、用 `unshare` 手动体验 namespace 隔离。

## 📋 小白前置
需先懂 OS1（进程是隔离单位）、OS5/OS10（虚拟地址/内核态）、OS14（内核能力）。最好略懂一点命令行(PRE6)与 Linux 基础。

## 🟢 部分一·最浅层（生活比喻）
虚拟机像"在楼里再盖一栋完整小楼(含自己地基/水电/机房)"——重但彻底隔离。容器像"同一栋大楼里用隔板( namespace)隔出独立房间, 各家电表(cgroup)单独算电, 并规定每家只能用某些电器(seccomp)"——共享大楼地基(内核), 所以轻、启动快, 但隔离不如整楼彻底。

## 🟡 部分二·动手层（逐字操作）
1. 若装了 Docker（Windows 上用 Docker Desktop），起一个容器看它"以为自己是独立机"：
```bash
docker run --rm -it alpine sh -c 'echo 容器内PID1; ps' 
```
容器内 `ps` 只看到自己, 像独占——这是 PID namespace 的效果。
2. 不用 Docker, 用 `unshare` 手动体验隔离（Linux/WSL2）：
```bash
unshare --fork --pid --mount-proc bash
# 进入后: ps -ef   # 应只看到自己这棵进程树(仿佛 PID1)
exit
```
3. 看 cgroup 限制（Linux）：
```bash
cat /proc/self/cgroup | head
```

### 🧪 实战 Lab（OS15）
运行 `docker run --rm alpine ps`(或 `unshare --pid --fork bash` 后 `ps`)，观察"容器内/隔离后只看到少量进程"，写一句"namespace 让进程以为自己独占系统, 实际共享宿主机内核"。验收：把输出 + 这句发我（无 Docker 用 unshare 替代）。

## 🔵 部分三·原理层（三种技术怎么协作）
① Namespace(Linux 内核特性)：把全局资源( PID/Mount/Network/UTS/IPC/User/Time)按命名空间"各自一份视图", 容器内看到的 PID 从 1 开始、看不到宿主进程(OS1/OS13 init 概念延伸)。② cgroup(control group)：把进程组绑定到资源配额(CPU 份额、内存上限、IO 权重、pids 上限), 超了就 OOM/节流(OS6/OS7)。③ seccomp(secure computing)：用 BPF 过滤器限制进程能发的 syscall 集合(OS10), 攻击面收窄(比如容器不需要 `reboot`/`mount` 就禁掉)。三者叠加 = 轻量隔离。镜像是只读层+可写层(overlayfs, OS8), 启动只是"建 namespace+跑进程", 秒级。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
容器脱胎于 chroot→ FreeBSD jail→ Solaris zone→ Linux LXC→ Docker(2013 引爆)。顶级追问：namespace 不绝对隔离(共享内核, 内核漏洞一出全漏, 故需"用户命名空间+能力裁剪+seccomp"纵深防御)；"容器逃逸"(SEC83)为何是云安全重点(如脏牛 Dirty COW 提权)；K8s(OPS21)在 cgroup 上做 QoS；gVisor/Kata 用"用户态内核/微 VM"补隔离。理解这些对 OPS19-26 容器/ K8s 安全直接有用。

## 🔴 部分五·顶级视角
顶级云原生工程师用 cgroup/namespace 做多租户隔离、资源核算、限流(OPS15-26)。理解容器原理是学 Docker/K8s、做安全沙箱(SEC45)、理解 CI/CD(OPS30)的前提。开源：runc(OCI 运行时)、containerd、Linux 内核 `kernel/nsproxy.c` 与 `kernel/cgroup/`。

## 🟠 部分六·安全/合规种子
容器是安全边界但非铁壁——白帽应学"最小权限"(禁不必要 syscall、非 root 跑容器、readonly rootfs)。自己机器用 Docker 起容器、用 `unshare` 实验完全合规；但用容器逃逸技术打他人集群属攻击, 只分析不实践(SEC83 红线)。理解 seccomp 让你写更安全的部署。

## ✅ 验收
交：① 容器内/隔离后 `ps` 输出；② 一句"容器共享内核、靠 namespace+cgroup+seccomp 隔离"；③ 说出三者各管什么。过关。

## ⚠️ 常见坑
- 以为容器=轻量虚拟机：错, 它共享宿主内核, 隔离弱于 VM(这是安全重点)。
- Docker 在 Windows 上跑的是"里面的 Linux 虚拟机", 故 `unshare` 实验在 WSL2 最顺。
- MSYS2 非 Linux, 容器实验用 Docker Desktop/WSL2。
- 护手：复制 `docker run --rm alpine ps`。

## ➡️ 下一步
OS16 · 内存分配器——你每次 `malloc`/`new` 背后是谁在干活：ptmalloc/jemalloc/tcmalloc 怎么管用户态堆，为什么内存"free 了却没还给系统"。

### ⚔️ 闯关路线（OS15）
- 🟢 Lv1：用比喻说明 namespace 像“每个容器以为自己独占机器”🥉🥉
- 🔵 Lv2：说明 cgroup 限制容器能用多少 CPU/内存 🈶🥈
- 🟣 Lv3：解释 seccomp 过滤掉危险系统调用 🥇🥇
- 🟠 Lv4：对比容器隔离与虚拟机隔离的强度差异 🏆🏆
- 🔴 Lv5：写 200 字说明“容器安全”是云原生（方向10）的核心命题👑👑


---

# OS16 · 内存分配器（ptmalloc / jemalloc / tcmalloc）

## 🎯 目标
学完这节，你能说清"用户态堆分配器"是什么、`malloc` 不是直接找 OS 要而是一层层缓存、ptmalloc(glibc)/jemalloc/Facebook/tcmalloc(Google)的设计取舍，并能在自己机器用工具看堆行为(如 `MALLOC_TRACE`、valgrind 的 massif)。

## 📋 小白前置
需先懂 OS7（内核 buddy/slab 是更底层）、C 的动态内存(C7 malloc/free)、OS3（分配器内部要加锁）。知道"堆(heap)"概念(ASM4 局部变量在栈, 动态内存在堆)。

## 🟢 部分一·最浅层（生活比喻）
`malloc` 不像"每次要纸就找厂长(OS)批", 那样太慢。分配器像"行政助理"：先向厂长一次性批一大沓纸(向内核要一大块, 用 `mmap`/`sbrk`), 自己裁成各种尺寸的小张, 谁要就发一张, 谁还回来就收进抽屉(空闲链表)下次复用。不同助理(ptmalloc/jemalloc/tcmalloc)整理抽屉的手法不同——有的按大小分格, 有的给每个员工(线程)配独立抽屉(减锁争用)。

## 🟡 部分二·动手层（逐字操作）
1. 用 `MALLOC_TRACE` 跟踪 malloc/free（glibc 自带）：
```c
#include <mcheck.h>
#include <stdio.h>
#include <stdlib.h>
int main(){
    mtrace();                       // 开始跟踪
    void *p = malloc(100);
    void *q = malloc(200);
    free(p);
    malloc(50);
    muntrace();
    return 0;
}
```
```bash
gcc -g -no-pie mt.c -o mt
export MALLOC_TRACE=/tmp/mtrace.log
./mt
mtrace /tmp/mtrace.log             # 若装了 mtrace 工具, 看泄漏报告
```
2. 看不同分配器影响：用 `LD_PRELOAD` 切换 jemalloc(若装了)：
```bash
LD_PRELOAD=/usr/lib/x86_64-linux-gnu/libjemalloc.so.2 ./mt 2>/dev/null || echo "jemalloc 未装, 跳过对比"
```

### 🧪 实战 Lab（OS16）
运行 `mtrace` 实验, 观察"malloc 100 / free 后"的分配记录, 写一句"free 后内存回到分配器空闲链表, 不一定还内核(故 RSS 不立即下降)"。验收：把 mtrace 输出片段/说明 + 这句发我。

## 🔵 部分三·原理层（分配器内部怎么管）
`malloc` 通常向 OS 用 `brk`(堆顶上移)或 `mmap`(匿名映射大块)批量取内存, 自己切成 chunk 管理。ptmalloc(glibc)用"bins"：fastbins(小块 LIFO 快取)、small/large bins(按大小分桶的双向链表)、unsorted bin(回收中转)、top chunk(堆顶剩余)。释放时合并相邻空闲(chunk 边界标记防越界, 即"heap metadata"), 不立即还给 OS(留着复用, 所以 free 后 RSS 不降)。多线程下每线程有 arena 减锁争用(OS3)。jemalloc 用"size class + per-arena + tcache"大幅降碎片与锁；tcmalloc 用"每线程缓存+中心堆", 高并发分配极快。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
分配器是"性能+碎片+并发"的权衡艺术。顶级追问：碎片(外部/内部)怎么度量？"heap spray"(SEC35/溢出利用)正是利用分配器的可预测布局；tcache poisoning(SEC42)等堆利用技术建立在对 bins 的理解上——白帽只分析防御。"安全分配器"( hardened malloc, 如 graphene/alloc 的隔离)怎么防堆漏洞；`MALLOC_PERTURB_` 调试选项怎么暴露 Use-After-Free(SEC38)。这些与 CMP10 GC(另一种内存管理)、OS3 锁性能咬合。

## 🔴 部分五·顶级视角
顶级工程师据负载选分配器：默认 ptmalloc(通用)、Redis 用 jemalloc(降碎片)、高并发服务用 tcmalloc。调分配器能显著降延迟/内存(OPS7 性能)。理解堆布局是逆向(SEC34 内存破坏)、漏洞利用(方向7b)的基础。开源：jemalloc、tcmalloc、mimalloc(Microsoft, 高性能安全)、glibc malloc 源码。

## 🟠 部分六·安全/合规种子
堆漏洞(溢出/UAF/DF 释放后双倍释放)是真实高危 bug 来源(SEC34-42)——理解分配器让你既会防御(写安全 C、用安全分配器)也会"白帽审计自己/授权代码"。Mimikatz 等只分析不实践；自己机器跑 mtrace 完全合规。

## ✅ 验收
交：① mtrace/说明证明 free 不立即还 OS；② 说出 ptmalloc/jemalloc/tcmalloc 各一句特点；③ 能解释"为什么 RSS 在 free 后不降"。过关。

## ⚠️ 常见坑
- 误以为 `free(p)` 后内存立刻还给系统：其实回分配器空闲链表, RSS 不降(除非触发 trim/mmap 归还)。
- MSYS2 的 malloc 是 Windows 的, mtrace 可能不可用；用 WSL2/Linux 或接受文字说明。
- 护手：复制 `mt.c` 整段。

## ➡️ 下一步
OS17 · 网络栈内核侧——当你 `send` 一个包, 它在内核里走了多长的路：协议栈分层、软中断、NAPI、从网卡到用户态的全程。

### ⚔️ 闯关路线（OS16）
- 🟢 Lv1：用比喻说明 ptmalloc 是 glibc 的“默认内存管家”🥉🥉
- 🔵 Lv2：解释 jemalloc/tcmalloc 为何对多线程更友好 🈶🥈
- 🟣 Lv3：说明 tcache（线程本地缓存）加快小对象分配 🥇🥇
- 🟠 Lv4：对比不同分配器的碎片与性能取舍 🏆🏆
- 🔴 Lv5：写 200 字说明“分配器行为”直接决定堆漏洞利用难度（接方向7b SEC36）👑👑


---

# OS17 · 网络栈内核侧（协议栈 / 软中断）

## 🎯 目标
学完这节，你能说清一个数据包从网卡到你的应用程序(以及反向)在内核里经过哪些层：网卡 DMA 收包→ 硬中断→ 软中断(softirq)→ 协议栈(IP/TCP)→ socket 缓冲区→ 你的 `recv`，理解"软中断"为何存在、NAPI 怎么抗高吞吐，并能在自己机器用工具看网卡中断与协议统计。

## 📋 小白前置
需先懂 COMP5（DMA/中断/MMIO）、OS9（epoll/异步 I/O、用户态怎么收）、OS10（内核态/系统调用）。网络基础本节能用到一点(NET1/NET6 后补更好, 但可独立)。

## 🟢 部分一·最浅层（生活比喻）
数据包像"快递包裹"。网卡(收发室)用 DMA(COMP5)把包裹直接搬进仓库(内核内存, 不惊动 CPU)→ 拉一下门铃(硬中断)告诉 CPU"有货"→ 但门铃只说一句"来活了", 真正拆包分拣(软中断, 跑协议栈)稍后批量做, 免得门铃一直响卡住别的→ 包裹按 IP/TCP 规则拆封、排好队, 放进你信箱(socket 缓冲区)→ 你(`recv`)来取。反向寄件同理倒序。

## 🟡 部分二·动手层（逐字操作）
1. 看网络协议统计（收发包、错误）：
```bash
cat /proc/net/dev | head
# 或
netstat -s 2>/dev/null | head -n 20
```
2. 看网卡中断（哪个 CPU 在处理网络）：
```bash
cat /proc/interrupts | grep -i 'eth\|net\|wi' | head
```
3. 看软中断占用（NET_RX 是收包软中断）：
```bash
cat /proc/softirqs | head
```
(注：MSYS2 下这些可能为空/不全；深度用 WSL2/Linux VM。)

### 🧪 实战 Lab（OS17）
运行 `cat /proc/net/dev` 或 `netstat -s`，找一找"接收字节数/RX bytes"那列，写一句"网卡收的包经内核协议栈才到我的程序, 不是直接进应用"。验收：把输出片段 + 这句发我。

## 🔵 部分三·原理层（包在内核的旅程）
收包：网卡 DMA 写入内核环形缓冲区(ring buffer, COMP5 MMIO 映射)→ 触发硬中断(IRQ)→ 中断处理程序只"标记有包"并唤起 NET_RX 软中断(softirq)→ 软中断上下文(不在任何进程里, 跑在中断返回路径)调用 NAPI 轮询驱动收包→ 逐层解封装：以太网→ IP(查路由/分片重组)→ TCP(序号/窗口/重传, NET6)→ 数据放进对应 socket 的接收缓冲区(`sk_buff` 结构贯穿全程)→ 用户态 `recv`/epoll 唤醒(OS9)拷贝到用户空间。发包反向, 经 Qdisc(排队规则)、TCP 分段、IP 封装、DMA 发送。软中断存在是为了"硬中断短平快、重活推迟到可抢占的软中断批量做", 避免关中断太久影响实时性。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
NAPI(中断+轮询混合)解决"高速网卡每包一中断把 CPU 打断死"：有包时转轮询批量取, 空了回中断模式。顶级追问：RPS/RFS(把软中断负载分摊到多核, COMP7 多核)、XDP(eBPF 在驱动层极早处理包, OPS41)、零拷贝(TCP 直接 DMA 到用户缓冲, OS9)、多队列网卡(每队列一中断亲和)？内核协议栈 vs 用户态栈(DPDK 绕开内核, NET18)的取舍？这些在云网络(OPS16 VPC)、高性能网关(BE 后端)是巅峰话题。

## 🔴 部分五·顶级视角
顶级网络/云工程师调"中断亲和、RPS、网卡多队列、XDP、eBPF 可观测"来压网络延迟与吞吐。理解内核协议栈是学 NET(网络方向)、做网络编程(NET17)、调服务器性能(BE24)、容器网络(OPS25 CNI)的基础。读《Linux Kernel Networking》或《Understanding Linux Network Internals》。

## 🟠 部分六·安全/合规种子
内核网络栈是 DDoS/包构造类攻击的作用面(如 SYN flood 耗尽半连接表)——理解它能写防御(限速/SYN cookie)、做网络取证(SEC88)。自己机器看 `/proc/net/dev` 合规；构造恶意包打他人网络属攻击, 绝不碰(红线)。抓包(Wireshark NET21)只在自己/授权网络做。

## ✅ 验收
交：① `/proc/net/dev` 或 `netstat -s` 片段；② 一句"包经内核协议栈+软中断才到应用"；③ 说出硬中断与软中断分工。过关。

## ⚠️ 常见坑
- MSYS2 下 `/proc/net/dev` 等是模拟, 可能为空; 深度实验用 WSL2/Linux VM, 或接受文字说明。
- 把"软中断"当"普通线程"：软中断跑在中断上下文(不可睡眠), 与进程/线程不同(OS1/OS10)。
- 护手：复制 `cat /proc/net/dev | head`。

## ➡️ 下一步
CMP1 · 编译器全景——把"人写的高级语言"变成"CPU 能跑的机器码", 中间到底过了哪几关：前端/中端/后端。

### ⚔️ 闯关路线（OS17）
- 🟢 Lv1：用比喻说明网络栈内核侧像“邮局分拣中心”🥉🥉
- 🔵 Lv2：解释软中断（softirq）如何把收包处理分摊 🈶🥈
- 🟣 Lv3：说明 sk_buff 是内核里“网络包”的通用载体 🥇🥇
- 🟠 Lv4：对比内核态处理与用户态 recv 的边界 🏆🏆
- 🔴 Lv5：写 200 字说明“内核网络栈”是抓包/协议分析（方向4/Net21）的底层👑👑


---

# CMP1 · 编译器全景（前端 / 中端 / 后端）

## 🎯 目标
学完这节，你能画出"源代码 → 编译器 → 可执行文件"的完整流水线，说清前端(词法/语法/语义)、中端(IR/优化)、后端(代码生成/目标码)各自干什么，并能在自己机器用 `gcc -O2 -S` 等命令"停在某一关"观察中间产物。

## 📋 小白前置
需先懂 COMP3（ISA 是什么）、ASM1（汇编长啥样）、C1（C 的编译四阶段初见）。知道"高级语言→机器码"这件事即可。

## 🟢 部分一·最浅层（生活比喻）
编译器像"葡萄牙语翻译机"：你写中文书(源码), 它先"分词断句"(词法/语法, 确认句子合法), 再"理解意思"(语义, 确认名词有定义), 然后翻成"世界语草稿"(中间表示 IR, 与具体语言/机器无关), 在草稿上"润色精简"(优化, 比如把废话删掉), 最后"翻成葡萄牙文"(后端生成目标机器码)。分三段是因为"翻译"和"润色"和"落特定语言"可以各换各的零件。

## 🟡 部分二·动手层（逐字操作）
1. 写 `pipeline.c`：
```c
int square(int x){ return x * x; }
int main(){ return square(5); }
```
2. 停在每一关看产物：
```bash
gcc -E pipeline.c -o pipeline.i    # 前端第1关: 预处理(宏展开/头文件)
gcc -S pipeline.c -o pipeline.s    # 中端前: 生成汇编( human-readable 机器语言)
gcc -c pipeline.c -o pipeline.o    # 后端: 生成目标文件(机器码, 含重定位, CMP9)
gcc pipeline.c -o pipeline         # 最终: 链接成可执行
./pipeline; echo "退出码=$? (应为25, 因 square(5)=25)"
```
3. 对比优化效果：
```bash
gcc -O0 -S pipeline.c -o p0.s && gcc -O2 -S pipeline.c -o p2.s
wc -l p0.s p2.s    # -O2 后代码更短(优化/内联)
```

### 🧪 实战 Lab（CMP1）
运行上面命令, 确认 `./pipeline` 退出码为 25。再打开 `pipeline.i` 看"几百行被展开的头文件", 体验"前端第1关做了什么"。写一句"预处理把 #include 换成真实内容, 编译器才看见完整代码"。验收：退出码 25 截图 + `pipeline.i` 行数 + 这句说明。

## 🔵 部分三·原理层（流水线各关干什么）
① 前端：词法分析(CMP2, 源码→ token 流)、语法分析(CMP3, token→ 语法树)、语义分析(CMP5, 类型检查/作用域/符号表)。前端与源语言绑定、与目标机无关。② 中端：生成 IR(CMP6, 如 LLVM IR/三地址码)、做与目标无关的优化(CMP7, 常量折叠/死代码删/循环优化)。中端是"可移植优化"的核心。③ 后端：指令选择(把 IR 映射到目标 ISA 指令)、寄存器分配(CMP8, 变量→ 寄存器/栈)、指令调度(排顺序利流水线 COMP2)、发射机器码、交给链接器(CMP9)拼成可执行。分层让"N 种语言 × M 种机器"只需 N+M 个部件而非 N×M(LLVM 哲学)。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
最早编译器(1957 Fortran)是一体化黑盒；后来"前端/中端/后端"分离是工程突破。顶级追问：单遍 vs 多遍编译(内存 vs 优化空间)；LLVM 为何用统一 IR 让clang/rustc 等共享后端？"交叉编译"(在 x86 上编出 ARM 程序, 呼应 ASM6)怎么靠分离后端实现？"增量编译/模块系统"(Rust RS1/Go GO6)怎么加速？这些与 OS14 内核模块、CMP11 JIT 全部相关。

## 🔴 部分五·顶级视角
顶级编译器工程师(LLVM/GCC/Rustc 团队)在 IR 上做极致优化、设计新语言后端。理解编译流水线是读任意语言实现、做性能调优(CMP7)、写 DSL(CMP12)、做 JIT(CMP11)的地基。开源：LLVM(llvm.org)、GCC、Crystal/Rustc 都用类似三段式。

## 🟠 部分六·安全/合规种子
编译器本身是中性工具。白帽注意：编译器可被恶意改造插入后门(如"XcodeGhost"事件)——但这是"供应链安全(OPS46)"课题, 你只在自己机器用正规 gcc。理解编译流程让你审计构建链、防供应链投毒(SEC84 DevSecOps), 属防御视角。

## ✅ 验收
交：① `./pipeline` 退出码 25 截图；② `pipeline.i` 行数(体验预处理)；③ 说出前端/中端/后端各一句职责。过关。

## ⚠️ 常见坑
- 以为"编译=一步到位"：其实是多关流水线, 每关产物都能单独看(-E/-S/-c)。
- 把 `.s`(汇编, 文本)和 `.o`(机器码, 二进制)混：前者人读, 后者机器读且含重定位。
- 护手：复制 `gcc -O0 -S pipeline.c -o p0.s`。

## ➡️ 下一步
CMP2 · 词法分析（正则 / NFA / DFA）——编译器怎么把"一串字符"切成一个个有意义的"词"(token), 有限自动机是背后的数学。

### ⚔️ 闯关路线（CMP1）
- 🟢 Lv1：用比喻说明编译器像“把中文菜谱翻译成多国操作手册”🥉🥉
- 🔵 Lv2：指出前端/中端/后端各自的职责 🈶🥈
- 🟣 Lv3：解释为何同一源码可生成不同平台机器码 🥇🥇
- 🟠 Lv4：对比编译型（C/Go）与解释型（Python）的取舍 🏆🏆
- 🔴 Lv5：写 200 字说明“懂编译器”让你理解代码如何从源码变运行（接方向1a）👑👑


---

# CMP2 · 词法分析（正则 / NFA / DFA）

## 🎯 目标
学完这节，你能说清词法分析器(lexer)干啥、token 是什么、正则表达式怎么对应"有限自动机(NFA/DFA)"，并能在自己机器用 Python 的正则或手写一个小 lexer 把 `int x = 42;` 切成 token。

## 📋 小白前置
需先懂 CMP1（前端第一关是词法）、PY16（正则 re 已接触 NFA/回溯概念）。知道"字符串"概念(PY2)。

## 🟢 部分一·最浅层（生活比喻）
词法分析像"读一句话先断词"：把"int x = 42 ;"切成 词: "int"(类型词)、"x"(名字词)、"="(等号词)、"42"(数字词)、";"(结束词)。每个词叫 token, 带着"类别+内容"。背后数学"有限自动机"像"一排会亮灯的轨道小车"：顺着字符一个个走, 走到能亮灯的格子就认定"这是一个完整的词"。

## 🟡 部分二·动手层（逐字操作）
1. 用 Python 手写一个极简 lexer（护手：复制即可）：
```python
import re
src = "int x = 42;"
tok_re = [
    ('TYPE',  r'int|float|void'),
    ('ID',    r'[a-zA-Z_]\w*'),
    ('NUM',   r'\d+'),
    ('OP',    r'[=+*/-]'),
    ('SEMI',  r';'),
    ('WS',    r'\s+'),
]
master = '|'.join(f'(?P<{n}>{p})' for n,p in tok_re)
for m in re.finditer(master, src):
    kind = m.lastgroup
    if kind != 'WS':
        print(f"token: {kind:5} = {m.group()}")
```
2. 运行：
```bash
python lex.py
```
预期逐行打印 TYPE= int / ID= x / OP= = / NUM= 42 / SEMI= ;。

### 🧪 实战 Lab（CMP2）
把 `src` 改成 `"float total = 3 + 4;"`, 重运行, 观察 token 序列变化(出现 TYPE=float, OP=+, 多一个 NUM=4)。写一句"词法分析不关心语法对不对, 只负责切词"。验收：把修改后输出 + 这句发我。

## 🔵 部分三·原理层（正则→自动机）
每个正则可构造 NFA(非确定有限自动机, 允许一处有多个去向/空转移), NFA 经"子集构造"转 DFA(确定有限自动机, 每状态每字符唯一下一状态), DFA 最小化后极快匹配(每个字符 O(1) 状态跳)。词法分析器常由工具(lex/flex)从正则规则自动生成 DFA 表, 运行时按表驱动切 token。token 带"种类(type)+ 值(value)+ 位置(行/列)"供语法分析(CMP3)消费。注意 PY16 讲的"回溯灾难"是 NFA 式(回溯)匹配的代价, 而 DFA 不会回溯——这也是为什么词法用 DFA。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
正则/有限自动机是理论计算机科学基石(乔姆斯基层级之正则语言)。顶级追问：为什么词法用正则、语法用上下文无关文法(CFG, CMP3)——因为正则表达不了"括号配对"这种需要计数的结构；"最长匹配原则"(greedy)怎么定 token 边界；" Lexer 与 Parser 的边界"为何有时模糊(比如 Python 用缩进, 词法就得管缩进)；Unicode 下词法怎么处理(字母不只是 ASCII)。这些在写语言/编辑器语法高亮、SEC 模式匹配都相关。

## 🔴 部分五·顶级视角
顶级工程师用 flex/ANTLR/手写 lexer 做编译器、写 DSL(CMP12)、做代码高亮/格式化、写 WAF 规则引擎(SEC65)。理解 DFA 也是写高效正则(避免 catastrophic backtracking, PY16/SEC 审计点)的前提。开源：flex、re2(Google 的 DFA 正则引擎, 防回溯炸弹)。

## 🟠 部分六·安全/合规种子
"正则回溯灾难"可被当 ReDoS 拒绝服务攻击(SEC65 评估/WAF 绕过评估, 白帽视角)——理解 DFA vs NFA 帮你写"不会被人用变态输入卡死"的安全正则(防御)。自己机器跑 lex.py 合规。

## ✅ 验收
交：① 修改后 lex.py 输出；② 一句"词法只切词不管语法对错"；③ 说出正则对应 NFA/DFA。过关。

## ⚠️ 常见坑
- 把"词法错误"和"语法错误"混：词法错是出现非法字符(如 `@`), 语法错是词对了但顺序不对(如 `= int x`)。
- Python 正则默认回溯(NFA 式), 与词法 DFA 不同——本节概念用 DFA, 实践用 re 演示足矣。
- 护手：复制 `lex.py` 整段。

## ➡️ 下一步
CMP3 · 语法分析（LL / LR / 递归下降）——一堆 token 怎么按"语法规则"组成正确的句子, 解析器怎么建出结构。

### ⚔️ 闯关路线（CMP2）
- 🟢 Lv1：写一个正则匹配简单模式（如 `[0-9]+`）🥉🥉
- 🔵 Lv2：说明词法分析把字符流切成 token 流 🈶🥈
- 🟣 Lv3：解释 NFA→DFA 的确定化（子集构造）🥇🥇
- 🟠 Lv4：对比正则、NFA、DFA 的表达能力与性能 🏆🏆
- 🔴 Lv5：写 200 字说明“词法分析”是写解释器/DSL（CMP12）的第一步👑👑


---

# CMP3 · 语法分析（LL / LR / 递归下降）

## 🎯 目标
学完这节，你能说清语法分析器(parser)把 token 流按"文法规则"组成结构、递归下降/LL/LR 三种主流解析策略的区别，并能在自己机器手写一个"只认 `int x = 数字;`"的递归下降 parser。

## 📋 小白前置
需先懂 CMP2（token 流）、CMP1（语法分析是前端第二关）。知道"树"结构(DSA6 二叉树)概念更好。

## 🟢 部分一·最浅层（生活比喻）
语法分析像"按菜谱判断这桌菜合不合格"：词法只切出"鸡蛋/盐/锅"(token), 语法要确认"先打蛋再下锅"(顺序结构)符合菜谱(文法)。解析器边读 token 边"搭积木", 最终搭成一棵"语法树"——比如"赋值语句"是个积木, 左边是"变量积木"右边是"表达式积木"。搭错了(顺序不对)就报语法错。

## 🟡 部分二·动手层（逐字操作）
1. 手写递归下降 parser（Python, 护手复制）：
```python
tokens = [('TYPE','int'),('ID','x'),('OP','='),('NUM','42'),('SEMI',';')]
pos = 0
def peek(): return tokens[pos] if pos < len(tokens) else None
def eat(t): 
    global pos
    if peek()[0]==t: tok=tokens[pos]; pos+=1; return tok
    raise SyntaxError(f"期望 {t} 但得到 {peek()}")
def parse_assign():
    eat('TYPE'); name = eat('ID'); eat('OP'); val = eat('NUM'); eat('SEMI')
    return f"赋值: {name[1]} = {val[1]}"
print(parse_assign())   # 赋值: x = 42
```
2. 运行：
```bash
python parse.py
```
3. 体验语法错误：把 `tokens` 改成 `[('ID','x'),('OP','='),('NUM','42'),('SEMI',';')]`(少了 TYPE), 重跑会 `SyntaxError`——这就是语法分析拦住的错。

### 🧪 实战 Lab（CMP3）
运行正常版得"赋值: x = 42"; 再跑缺 TYPE 的版本看到 SyntaxError。写一句"解析器按文法规则校验结构, 不合规则报错"。验收：两个输出 + 这句发我。

## 🔵 部分三·原理层（解析策略）
文法用"产生式"描述(如 `Stmt → TYPE ID = NUM ;`)。递归下降：为每个非终结符写一个函数, 互相递归调用, 直观好写(手写 parser 常用, 如 clang 部分)。LL(k)：从左向右、最左推导、向前看 k 个 token 做决定(预测分析法, 表驱动)。LR(k)(含 SLR/LALR, yacc/bison 用)：从左向右、最右推导、用"状态栈+转移表"处理, 能解析更强文法(适合表达式优先级)。解析成功产出"具体语法树(CST)", 常简化为"抽象语法树 AST"(CMP4)。歧义文法(如 `1+2*3` 先算谁)靠"优先级/结合性"消歧。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
巴科斯-诺尔范式(BNF)1959 定义文法; yacc(1970s)普及 LALR。顶级追问：为什么表达式用 LR 更自然(优先级的移进-归约); "左递归"怎么改写成右递归让递归下降能处理; "PEG"(解析表达式文法)为何近年流行(更可控、无歧义, 用于现代语言); "错误恢复"(报错后继续解析出更多错误)怎么做？这些在写语言/SQL 解析器(DB3)、配置解析、SEC 数据解析都相关。

## 🔴 部分五·顶级视角
顶级工程师用 ANTLR(多语言 parser 生成器)、手写递归下降(clang/Go 编译器都用)、或 PEG(树莓派上的高级语言)。理解语法分析是做编译器、写 DSL(CMP12)、做协议/日志解析、SQL 引擎(DB9 查询优化)的基础。开源：ANTLR、bison、tree-sitter(编辑器语法树)。

## 🟠 部分六·安全/合规种子
"解析器混淆"是注入类漏洞根源(SEC10 的 SQLi/XSS/命令注入都因"把数据当代码解析")——理解语法边界让你在写解析器时严格分离"代码与数据"(参数化查询)。自己机器跑 parse.py 合规。

## ✅ 验收
交：① 正常版输出"赋值: x = 42"；② 缺 TYPE 的 SyntaxError；③ 说出递归下降/LL/LR 之一的特点。过关。

## ⚠️ 常见坑
- 把"语法分析"和"词法分析"职责混：词法切词(CMP2), 语法组结构(本节)。
- 手写 parser 遇左递归会无限递归——真实语言文法需改写为右递归或用 LR。
- 护手：复制 `parse.py` 整段。

## ➡️ 下一步
CMP4 · 抽象语法树 AST——解析出的"语法树"怎么变成程序的结构图, 编译器后续所有优化都在这棵树上做。

### ⚔️ 闯关路线（CMP3）
- 🟢 Lv1：用比喻说明语法分析像“检查句子语法对不对”🥉🥉
- 🔵 Lv2：对比 LL（自顶向下）与 LR（自底向上）🈶🥈
- 🟣 Lv3：写一个简单的递归下降解析器（四则运算）🥇🥇
- 🟠 Lv4：说明语法错误如何精准报错位置 🏆🏆
- 🔴 Lv5：写 200 字说明“解析器”是 SQL/JSON/配置处理的共同内核（接方向5/1a）👑👑


---

# CMP4 · 抽象语法树 AST（抽象语法树 / 遍历）

## 🎯 目标
学完这节，你能说清 AST 与"具体语法树"的区别、AST 长什么样、编译器怎么"遍历"它对程序做变换，并能在自己机器用 Python 的 `ast` 模块把一段真实 Python 代码解析成树并打印结构。

## 📋 小白前置
需先懂 CMP3（语法分析产出结构）。知道"树"(DSA6)和"递归遍历"概念。会一点 Python(PY1–PY6)更顺, 因本节用它演示。

## 🟢 部分一·最浅层（生活比喻）
具体语法树像"带所有标点符号的逐字剧本"(连括号、分号都留着), 太啰嗦。抽象语法树(AST)像"只留剧情骨架的漫画分镜"：把"赋值"画成一个框, 框里左边写变量名、右边写值, 括号分号这些"排版噪音"全删。编译器后面所有优化(CMP7)都是在改这棵骨架树——比如发现"右边其实是常数"就直接算好填进去。

## 🟡 部分二·动手层（逐字操作）
1. 用 Python 内置 `ast` 模块看真实代码的树：
```python
import ast
code = "x = 3 + 4 * 2"
tree = ast.parse(code)
print(ast.dump(tree, indent=2))
```
2. 运行：
```bash
python showast.py
```
你会看到树结构: `Module → Assign → (Target: Name('x'), Value: BinOp(Add, 3, BinOp(Mul, 4, 2)))`——这就是 AST, 清楚地表达"x = 3 + (4*2)"的层次。

### 🧪 实战 Lab（CMP4）
把 `code` 改成 `"if a > 0:\n    b = a * 2"`(注意缩进), 重运行看 AST 里出现 `If/Compare/Assign` 节点。写一句"AST 是程序的结构化表示, 优化和代码生成都基于它"。验收：把 if 版 AST 片段 + 这句发我。

## 🔵 部分三·原理层（AST 怎么用）
AST 是"每个节点代表一种语法构造"的树：叶子是字面量/标识符, 内部节点是运算符/语句。编译器遍历它做三件大事：① 语义检查(CMP5, 类型从子节点向上求)；② 优化(CMP7, 如常量折叠把 `3+4*2` 直接算成 `11` 写在树里)；③ 代码生成(CMP8, 后序遍历表达式树产生逆波兰/三地址码)。遍历顺序：前序(根→子, 适合改写结构)、中序(表达式打印)、后序(先算子树值再合成, 适合求值/生成)。AST 与具体语言解耦, 是"中端 IR 的上游"(CMP6)。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
AST 是 1960s Lisp/Algol 时代的产物, 今天所有编译器/解释器核心。顶级追问：AST 与"CST 具体语法树"如何取舍(前者利优化、后者利报错定位/格式化); "语法糖降糖(desugar)"怎么把 `for` 转成 `while` 节点; "宏/元编程"(PAR4/RS9)怎么在 AST 层重写代码; "AST 差分"怎么做代码审查/格式化(prettier); "无 AST 的字节码编译器"(如直接生成, 快但难优化)？这些与解释器(OTH5 WASM/JVM 字节码)、重构工具、SEC 反编译(Ghidra 伪码)都相关。

## 🔴 部分五·顶级视角
顶级工程师用 AST 做静态分析(SEC20 代码审计、ESLint 类工具)、自动重构、语言服务器(LSP 的"跳转到定义"靠 AST)、写编译器/DSL(CMP12)。理解 AST 是学程序分析、做 DevSecOps(OPS84 SAST)、逆向还原(SEC29)的基础。开源：Python `ast`、tree-sitter、LLVM AST(Clang AST)。

## 🟠 部分六·安全/合规种子
"AST 级污点分析"是白帽找注入漏洞(SEC20)的核心方法——跟踪"用户输入(源)是否流到危险函数(汇)如 `eval/exec`"。自己机器用 `ast` 模块做练习合规; 理解它让你写出"输入与代码严格分离"的安全程序。

## ✅ 验收
交：① if 版 AST 片段；② 一句"优化基于 AST 遍历"；③ 说出 AST 与 CST 区别(去排版噪音)。过关。

## ⚠️ 常见坑
- 把"AST"和"运行时的对象树"混：AST 是编译期静态结构, 不是程序跑起来时的数据。
- Python `ast.dump` 输出很长，看 `Assign/If/BinOp` 关键字即可，不必全读。
- 护手：复制 `showast.py` 整段。

## ➡️ 下一步
CMP5 · 语义分析（类型检查 / 符号表）——树建好了, 还得确认"名词都有定义、类型对得上": 符号表和作用域怎么管。

### ⚔️ 闯关路线（CMP4）
- 🟢 Lv1：画一棵 `1+2*3` 的 AST 🥉🥉
- 🔵 Lv2：说明 AST 是“去掉括号等冗余后的结构树”🈶🥈
- 🟣 Lv3：写代码遍历 AST 做“把加法常量折叠”🥇🥇
- 🟠 Lv4：对比 AST 与源代码、与字节码的关系 🏆🏆
- 🔴 Lv5：写 200 字说明“AST”是代码分析/重写/混淆还原（方向7b）的基础👑👑


---

# CMP5 · 语义分析（类型检查 / 符号表）

## 🎯 目标
学完这节，你能说清语义分析做什么(类型检查、作用域、未定义变量检测)、符号表(symbol table)怎么记录"名字→类型/位置"、作用域链怎么解析名字，并能在自己机器用 Python `ast` + 手动遍历检测"使用了未声明的变量"。

## 📋 小白前置
需先懂 CMP4（AST 已建好）、PY18（类型标注/静态类型概念）、PY11（作用域/类）。知道"变量要先声明再使用"常识。

## 🟢 部分一·最浅层（生活比喻）
语法分析确认"句子通顺", 语义分析确认"句子讲得通"：比如"把苹果装进盒子里"语法对, 但如果"苹果"这个词从没在故事里出现过(未声明), 语义分析就报错"苹果是啥?没定义!"。符号表像"故事人物登记表"：每出现一个名字就登记它的身份(是整数?函数?在哪页定义的), 后面用到就查表确认身份合法。

## 🟡 部分二·动手层（逐字操作）
1. 写个小语义检查器, 检测"赋值左部的变量是否在符号表"(简化版)：
```python
import ast
code = """
x = 1
y = x + 2
z = undefined_var + 1
"""
tree = ast.parse(code)
symbols = set()
class Checker(ast.NodeVisitor):
    def visit_Assign(self, n):
        for t in n.targets:
            if isinstance(t, ast.Name): symbols.add(t.id)
        self.generic_visit(n)
    def visit_Name(self, n):
        if isinstance(n.ctx, ast.Load) and n.id not in symbols:
            print(f"语义错误: 变量 '{n.id}' 未声明就被使用 (行{n.lineno})")
Checker().visit(tree)
```
2. 运行：
```bash
python sema.py
```
会打印 `语义错误: 变量 'undefined_var' 未声明就被使用`。

### 🧪 实战 Lab（CMP5）
把 `code` 改成全部变量都先赋值(去掉 undefined_var 那行), 重运行应"无任何语义错误输出"。写一句"符号表记录已声明名字, 语义分析据此查未定义/类型错"。验收：两个运行结果 + 这句发我。

## 🔵 部分三·原理层（符号表与作用域）
语义分析遍历 AST(CMP4), 维护"符号表"：从标识符映射到它的属性(类型、种类、作用域层级、内存偏移/寄存器, CMP8)。遇到声明就把名字登记进"当前作用域", 遇到使用就向上查作用域链(局部→外层→全局, 呼应 PY11 作用域链)找定义, 找不到报"未声明"。类型检查：对 `BinOp` 节点, 查左右子类型是否兼容(如 int+int→int, int+string→错), 结果类型向上传播供上层用。还会检查"函数参数个数/类型匹配""return 类型一致""break 在循环外"等。强类型语言(C/Java/Rust)在编译期做完这些, 弱类型(Python/JS)部分推迟到运行期(但有类型标注 PY18/mypy 可静态查)。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
类型论是计算机科学深分支(λ演算、Hindley-Milner 类型推导, 如 ML/Haskell 自动推断类型)。顶级追问：静态类型(编译期抓错, 慢编译快运行) vs 动态类型(灵活但运行时才知错)的权衡；"类型推断"怎么少写注解；"泛型/Trait"(RS5/CPP3/PAR4)在语义层的表示；"dependent type"(依赖类型)为何能证明程序正确性；"符号表用哈希表还是树"；"重定义/屏蔽(shadowing)"怎么处理。这些与语言设计(PAR1-6)、Rust 所有权(RS2)编译期检查相关。

## 🔴 部分五·顶级视角
顶级语言设计师在语义层做"类型系统"决策, 决定语言安全与表达力。理解语义分析是写编译器/DSL(CMP12)、做静态分析(SEC20 SAST/OPS84)、理解 TypeScript(JS6)/Rust 类型的基础。开源：TypeScript 编译器(tsc, 先 parse 再 type-check)、mypy、LLVM 的 Sema 层(Clang)。

## 🟠 部分六·安全/合规种子
"类型混淆/不安全的类型转换"是内存安全漏洞(如 C 的 cast 错误、Type Confusion SEC38)根源——强类型/语义检查能在编译期挡掉大量 bug。白帽: 在自己程序做类型检查合规; 理解语义分析有助于写"输入先校验类型"的安全代码(防御注入/越权)。

## ✅ 验收
交：① 含 undefined_var 时报错输出；② 全声明版无输出；③ 说出符号表作用。过关。

## ⚠️ 常见坑
- 把"语义错误"和"语法错误"混：语法是结构错(`if` 没冒号), 语义是含义错(用未声明变量/类型不匹配)。
- 作用域是"栈式"的：函数返回要退出其作用域, 符号表要相应弹出。
- 护手：复制 `sema.py` 整段。

## ➡️ 下一步
CMP6 · 中间表示 IR（SSA / 三地址码）——所有高级语言都先翻成一种"统一世界语"再做优化, SSA 为何是现代编译器的秘密武器。

### ⚔️ 闯关路线（CMP5）
- 🟢 Lv1：说明语义分析检查“类型对不对、变量声明没”🥉🥉
- 🔵 Lv2：解释符号表如何记录每个名字的类型与作用域 🈶🥈
- 🟣 Lv3：说明“先使用后定义”为何通常报错 🥇🥇
- 🟠 Lv4：对比静态类型（编译期查）与动态类型（运行期查，接 PY18）🏆🏆
- 🔴 Lv5：写 200 字说明“类型检查”是防 bug 的第一道关，安全也受益👑👑


---

# CMP6 · 中间表示 IR（SSA / 三地址码）

## 🎯 目标
学完这节，你能说清 IR(中间表示)是什么、为什么需要它、三地址码长啥样、SSA(静态单赋值)的核心思想(每个变量只被赋值一次 + φ 函数), 并能在自己机器用 LLVM 工具(`clang -emit-llvm`)看一段 C 的 LLVM IR。

## 📋 小白前置
需先懂 CMP1（中端用 IR）、CMP4（AST 是上游）、CMP5（语义信息已就绪）。会 C 基础(C1)即可。

## 🟢 部分一·最浅层（生活比喻）
IR 像"世界语"：中文、英文、日文(各种源语言)都先翻成世界语(IR), 在世界语上统一润色优化(因为润色规则只写一套), 最后再翻成葡萄牙文/法文(各种目标机器码)。三地址码是"每句话最多干一件事"的极简句(如 `t1 = a + b; t2 = t1 * c`)。SSA 是更狠的世界语规矩: 每个盒子(变量)只能被填一次, 谁想改就新开一个盒子(t1, t2, t3...), 这样"谁在何时赋值"一目了然, 优化器最爱。

## 🟡 部分二·动手层（逐字操作）
1. 用 clang 生成 LLVM IR（若装了 clang；MSYS2 可 `pacman -S mingw-w64-x86_64-clang`）：
```bash
echo 'int f(int a,int b){ return a*a + b; }' > ir.c
clang -O2 -emit-llvm -S ir.c -o ir.ll 2>/dev/null && cat ir.ll
```
你会看到类似：
```
define i32 @f(i32 %a, i32 %b) {
  %1 = mul nsw i32 %a, %a
  %2 = add nsw i32 %1, %b
  ret i32 %2
}
```
这就是 LLVM IR(SSA 风格: %1/%2 各只赋值一次)。
2. 若无 clang, 用 gcc 的 GIMPLE 概念替代(看 `gcc -fdump-tree-gimple`)或仅读上面示例理解。

### 🧪 实战 Lab（CMP6）
运行 clang 生成 `ir.ll`, 找到 `mul` 和 `add` 两行, 抄下来, 写一句"SSA 里每个临时量(%1/%2)只被赋值一次, 便于优化器追踪数据流"。验收：把这两行 IR + 这句发我（无 clang 用文字描述替代并标注"待装 clang 验证"）。

## 🔵 部分三·原理层（IR 与 SSA 怎么工作）
IR 是"与目标机无关、但足够底层"的表示, 让优化(CMP7)可移植。三地址码：每条指令最多一个运算符、三个操作数(`x = y op z`), 便于分配寄存器(CMP8)。SSA 形式：每个变量唯一定义一次, 因分支导致"同一变量在不同路径被不同赋值"时, 用 φ 函数(phi)在汇合点选正确的版本(`%x = phi [ %a, %bb1 ], [ %b, %bb2 ]`)。SSA 让"定值-使用链(def-use)"直接显式, 常量传播/死代码删/公共子表达式消除等优化极高效。后端代码生成时再把 SSA 转回可多次赋值的普通形式并做寄存器分配。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
SSA 由 Rosen/Wegman/Zadeck 等 1988 提出, 是现代优化编译器(LLVM/GCC/HotSpot)的基石。顶级追问：φ 函数怎么插(支配边界 dominator frontier 算法)；"内存 SSA"(对内存也做 SSA 以追踪别名)为何难；为何 JVM/WebAssembly(OTH5)也用类 SSA 栈式 IR；"IR 层级选择"(高级 IR 利于语言特定优化、低级 IR 利于机器相关优化)怎么分层(LLVM 有 IR/HIR/MIR 多级)；这些直接决定优化质量。理解 IR 是读任何现代编译器源码的前提。

## 🔴 部分五·顶级视角
顶级编译器/VM 工程师在 IR 层做"跨语言优化"：同一套 LLVM 优化既服务 C++ 也服务 Rust/Swift。JIT(CMP11)也在运行时生成 IR 再编译。理解 IR 是学 LLVM Pass 开发、写自定义优化、做程序分析(SEC29)、理解 WebAssembly(OTH5)的基础。开源：LLVM IR 语言手册、Cranelift、V8 TurboFan IR。

## 🟠 部分六·安全/合规种子
IR 层是做"污点分析/符号执行(SEC32)"的天然场所——在 SSA 上追踪"用户输入是否流入危险操作"比在汇编上容易。白帽：在自己机器用 clang 生成 IR 分析合规；这是 SAST(OPS84)和漏洞研究的地基。

## ✅ 验收
交：① LLVM IR 的 mul/add 两行（或文字替代）；② 一句"SSA 每变量只赋值一次便于优化"；③ 说出 IR 为何与目标机无关。过关。

## ⚠️ 常见坑
- MSYS2 默认无 clang, 需 `pacman -S mingw-w64-x86_64-clang` 安装; 装不上就用 gcc GIMPLE 概念或文字描述。
- SSA 的 φ 函数不是真指令, 是优化期的记号, 别去找对应机器码。
- 护手：复制 `clang -O2 -emit-llvm -S ir.c -o ir.ll`。

## ➡️ 下一步
CMP7 · 优化（内联 / 循环 / 常量传播）——编译器背着你做的那些"提速把戏", 以及为什么有时 -O2 比 -O0 快十倍。

### ⚔️ 闯关路线（CMP6）
- 🟢 Lv1：用比喻说明 IR 像“介于源码与机器码的中间普通话”🥉🥉
- 🔵 Lv2：解释 SSA（静态单赋值）：每个变量只被赋值一次 🈶🥈
- 🟣 Lv3：说明三地址码形如 `x = y + z` 的简洁 🥇🥇
- 🟠 Lv4：对比高级 IR 与低级 IR 的优化粒度 🏆🏆
- 🔴 Lv5：写 200 字说明“IR 设计”决定编译器优化能做多深（接 CMP7）👑👑


---

# CMP7 · 优化（内联 / 循环 / 常量传播）

## 🎯 目标
学完这节，你能列举常见编译器优化(常量折叠/传播、死代码消除、内联、循环不变代码外提、循环展开、公共子表达式消除)，用 `gcc` 对比 `-O0`/`-O2` 看效果，并理解"未定义行为(UB, C11)"为何让优化变得危险又快。

## 📋 小白前置
需先懂 CMP1（优化在中端）、CMP6（IR/SSA 是优化舞台）、COMP2（流水线/缓存, 优化为利硬件）。会 C 基础(C1–C3)。

## 🟢 部分一·最浅层（生活比喻）
优化像"编辑帮你润色稿子"：① 常量折叠=把"3+4"直接改成"7"(不用现场算)；② 死代码删=删掉"写了但永远不用的句子"；③ 内联=把小函数内容直接抄进调用处(省得跑去找人/压栈, ASM4)；④ 循环不变外提=把"每圈都重算的同一道题"提到圈外只算一次；⑤ 循环展开=把"绕 4 圈"改成"一次写 4 遍", 少跳转更顺 CPU 流水线(COMP2)。

## 🟡 部分二·动手层（逐字操作）
1. 写 `opt.c` 含可优化点：
```c
#include <stdio.h>
int square(int x){ return x*x; }
int main(){
    const int N = 4;
    int total = 0;
    for(int i=0;i<N;i++) total += square(3);   // square(3) 是常量, 循环可优化
    printf("%d\n", total);
    return 0;
}
```
2. 对比两个优化级别：
```bash
gcc -O0 -S opt.c -o opt0.s
gcc -O2 -S opt.c -o opt2.s
echo "=== O0 行数 ==="; wc -l opt0.s
echo "=== O2 行数 ==="; wc -l opt2.s
grep -c 'call' opt0.s; grep -c 'call' opt2.s   # O2 里 square 可能被内联掉, call 更少
```
`opt2.s` 里很可能看不到 `square` 的 `call`(被内联+常量折叠成 `total=12`)。

### 🧪 实战 Lab（CMP7）
对比 `opt0.s` 与 `opt2.s` 的 `call` 出现次数(或行数), 观察 O2 把 `square` 内联、把循环算成常量。写一句"-O2 通过内联/常量传播把循环变成定值"。验收：两次 `grep -c call` 数字 + 这句发我。

## 🔵 部分三·原理层（优化怎么在 IR 上发生）
优化遍历 IR/SSA(CMP6)：① 常量折叠在编译期算 `3*3=9`；② 常量传播把 `square(3)` 结果 `9` 沿使用链替换；③ 死代码消除删掉永不执行的块；④ 内联(inline)把被调函数体复制进调用点(小函数、标 `inline` 的, CPP1)，消 `call` 开销(ASM3/ASM4)；⑤ 循环不变代码外提(LICM)把 `a*b`(与循环变量无关)移出；⑥ 循环展开(unroll)减分支；⑦ 公共子表达式消除(CSE)复用已算结果；⑧ 向量化(COMP6 SIMD)。这些建立在"编译器能证明等价"上——而 UB 会破坏这一前提。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
优化是"正确性 vs 性能"的精密平衡。顶级追问：未定义行为(UB, C11)为何让编译器"大胆假设"(如假设有符号不加溢出, 于是把某分支当死代码删掉)——这正是 C 程序员噩梦( int overflow 被优化掉)；"profile-guided optimization (PGO)"怎么用运行时数据再优化(热点内联/分支预测提示 COMP2)；"Link-Time Optimization (LTO)"跨文件优化；"优化导致的调试困难"(-O0 才方便 gdb)。这些与 CMP8 代码生成、OS 性能调优咬合。

## 🔴 部分五·顶级视角
顶级工程师用 `-O2/-O3/-Os(体积)/-Oz`、`-march=native`、`-flto`、`PGO` 榨干性能；也懂"何时别优化"(过早优化是万恶之源, 先正确再快)。理解优化能读懂反汇编(ASM8)里"我的代码去哪了"、解释性能差异(COMP2)。读《Engineering a Compiler》优化章。开源：LLVM 优化 Pass 列表、gcc 优化flag 文档。

## 🟠 部分六·安全/合规种子
"优化改变行为"在白帽视角是两面的：① 防御上, 编译器可自动加栈保护/缓解(SEC44 部分由编译期插入)；② 风险上, 不安全代码经激进优化可能"看似正常实则绕过检查"——所以写安全 C 要避 UB、用 `-Wall -Wextra` 抓隐患。自己机器做 -O0/-O2 对比完全合规。

## ✅ 验收
交：① O0 与 O2 的 `call` 次数对比；② 一句"-O2 内联+常量传播使循环变定值"；③ 能说出两种优化名。过关。

## ⚠️ 常见坑
- 用 `-O0` 调试、用 `-O2` 发布是常识；但有的 bug 只在 -O2 暴露(UB 被优化触发)——这是真问题不是编译器错。
- MSYS2 下 `grep -c` 可用; 若无 `call` 字样(AT&T 语法用 `callq`), 用 `grep -ciE 'call'` 大小写不敏感。
- 护手：复制 `gcc -O0 -S opt.c -o opt0.s`。

## ➡️ 下一步
CMP8 · 代码生成（寄存器分配 / 指令选择）——编译器最后一步: 把 IR 变成真机器码, 变量塞进寄存器、挑对指令、排好顺序。

### ⚔️ 闯关路线（CMP7）
- 🟢 Lv1：用比喻说明内联像“把小函数体直接贴进调用处”🥉🥉
- 🔵 Lv2：说明常量传播把 `x=3; y=x+1` 直接变 `y=4` 🈶🥈
- 🟣 Lv3：解释循环优化（展开/强度削弱）提速原理 🥇🥇
- 🟠 Lv4：说明过度优化可能让调试困难（保留调试信息）🏆🏆
- 🔴 Lv5：写 200 字说明“优化器”是把你烂代码变快的关键（接方向1a PY24）👑👑


---

# CMP8 · 代码生成（寄存器分配 / 指令选择）

## 🎯 目标
学完这节，你能说清代码生成两大难题——"指令选择"(IR 操作映射到哪条 ISA 指令)和"寄存器分配"(有限的寄存器怎么装下众多的变量), 理解图着色分配法, 并能在自己机器用 `gcc -O0` 看"变量全在栈上"、用 `-O2` 看"变量进寄存器"的差异。

## 📋 小白前置
需先懂 CMP6（IR/SSA 是输入）、CMP7（优化后 IR）、ASM1–ASM4（目标汇编长啥样、寄存器/栈帧）。知道 COMP3（ISA 指令集）。

## 🟢 部分一·最浅层（生活比喻）
代码生成像"把世界语剧本(IR)排成话剧(机器码)"：① 指令选择=把剧本每句"翻成演员能做的动作"(选对 CPU 指令, 比如 `a*b` 用 `imul` 还是 `lea` 拼)；② 寄存器分配=剧场只有 16 把椅子(寄存器), 但剧本有 100 个角色(变量), 导演得决定"谁上场时坐哪把椅、下场时把椅子让给谁", 椅子不够就把人暂时安置到后台(栈, ASM4)。排好顺序还要照顾演员不撞车(指令调度利流水线 COMP2)。

## 🟡 部分二·动手层（逐字操作）
1. 写 `cg.c`，多个局部变量：
```c
int compute(int a,int b,int c,int d){
    int t1 = a + b;
    int t2 = c * d;
    int t3 = t1 - t2;
    return t3 + a;
}
```
2. 看 -O0（变量多在栈, 用 `[rbp-X]`）与 -O2（变量进寄存器）差异：
```bash
gcc -O0 -S cg.c -o cg0.s && gcc -O2 -S cg.c -o cg2.s
echo "=== O0: 栈访问次数 ==="; grep -c 'rbp-' cg0.s
echo "=== O2: 栈访问次数 ==="; grep -c 'rbp-' cg2.s
```
O2 版大概率 `rbp-` 很少甚至 0(帧指针省略+全寄存器化)。

### 🧪 实战 Lab（CMP8）
对比 O0 与 O2 的 `rbp-` 出现次数(或看 `cg2.s` 里直接 `lea/add/sub` 用寄存器), 写一句"寄存器分配把变量塞进寄存器而非栈, 省去内存访问"。验收：两次 grep 数字 + 这句发我。

## 🔵 部分三·原理层（两难题怎么解）
指令选择：用"树重写/窥孔优化"把 IR 操作映射成目标指令。例如 `a*2` 可译成 `shl`(移位)而非 `imul`(更快,COMP3)；`a*3+1` 可能用 `lea` 一条搞定(你早看过 `lea (%rdi,%rsi),%eax`)。寄存器分配：变量多于寄存器时, 用"图着色"算法——以"同时存活(live)的变量"为节点, 有冲突(同时活)的连边, 用颜色(寄存器)给节点染色, 同色可共用; 染不下的变量"溢出(spill)"到栈(ASM4 局部变量位置)。还需考虑"调用约定"(ASM3: 调用者/被调者保存寄存器)、"寄存器别名(ax/ah 重叠)"。指令调度在末尾重排顺序以减少流水线停顿(COMP2)。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
图着色寄存器分配(Chaitin 1981)是经典。顶级追问：线性扫描分配(更轻, JIT 用, CMP11)为何适合运行时；"寄存器压力"怎么影响指令选择(寄存器不够就少生成需多寄存器的指令)；SSA(CMP6)怎么让分配更易(每变量单一定义, 干涉图更小)；"调用约定不符"导致的 ABI bug(ASM3)；"投机指令调度"与 Spectre(COMP2)。这些与 OS3 原子、COMP7 优化、ASM8 反汇编阅读全部咬合。

## 🔴 部分五·顶级视角
顶级编译器工程师写"指令选择 DSL(如 LLVM 的 TableGen)"、调寄存器分配器质量(直接影响性能)。理解代码生成让你读懂"编译器为什么生成这段奇怪汇编"(ASM8)、写手写汇编优化(ASM1)时避开与编译器冲突。开源：LLVM 后端(RegisterAllocator/InstructionSelector)、GCC 的 RTL 后端。

## 🟠 部分六·安全/合规种子
代码生成阶段的"栈帧布局"决定 Canary(SEC44)插在哪; 寄存器分配错误是编译器 bug 而非你代码问题(极罕见但真实)。白帽: 在自己机器对比 -O0/-O2 合规; 理解生成布局有助于逆向还原(SEC25 ELF/ASM8)与防御。

## ✅ 验收
交：① O0 与 O2 的 `rbp-` 次数对比；② 一句"寄存器分配减少栈访问"；③ 说出指令选择/寄存器分配两大任务。过关。

## ⚠️ 常见坑
- AT&T 语法里栈访问写 `-Nb(%rbp)` 或 `-Nb(%rsp)`, grep `rbp-` 可能漏掉 rsp 版; 用 `grep -cE 'rbp-|rsp-'` 更全面(O2 可能用 rsp)。
- 以为"-O2 一定有 rbp-": 帧指针省略后局部全在寄存器, rbp- 为 0 是正常的。
- 护手：复制 `gcc -O0 -S cg.c -o cg0.s`。

## ➡️ 下一步
CMP9 · 链接与装载（ELF / PE / 重定位 / PIC）——多个 .o 怎么拼成一个可执行文件, 重定位/PLT/GOT/位置无关这堆词到底什么意思。

### ⚔️ 闯关路线（CMP8）
- 🟢 Lv1：说明代码生成把 IR 选成目标指令 🥉🥉
- 🔵 Lv2：解释寄存器分配（图着色）为何“寄存器不够就溢出到栈”🈶🥈
- 🟣 Lv3：对比指令选择（模式匹配）的启发式 🥇🥇
- 🟠 Lv4：说明生成代码仍需链接才能跑（接 CMP9）🏆🏆
- 🔴 Lv5：写 200 字说明“代码生成”是编译器最后一环，决定最终性能👑👑


---

# CMP9 · 链接与装载（ELF / PE / 重定位 / PIC）

## 🎯 目标
学完这节，你能说清"编译"和"链接"的区别、ELF( Linux)与 PE(Windows)两大可执行格式、重定位怎么修补跨文件地址、PLT/GOT 怎么支持动态链接、PIC(位置无关代码)为何是共享库必需，并能在自己机器用 `readelf`/`objdump` 拆解一个真实 ELF。

## 📋 小白前置
需先懂 CMP1（链接是最后一步）、ASM4（栈帧/地址）、OS5（虚拟地址）、OS10（syscall）。写过 C 并 `-c` 生成过 `.o`(CMP1)。

## 🟢 部分一·最浅层（生活比喻）
链接像"把几章分散写的书稿拼成一本完整的书"：每章(.o 目标文件)里引用了别章的内容(比如 main 调用了 printf), 但写的时候还不知道最终页码(地址)。链接器(ld)给每章分配真实页码、在引用处填上正确页码(重定位)。动态链接更像"书里写了'详见工具书第X页', 工具书(共享库 .so)读者自己手头有, 运行时才去查那页"(靠 PLT/GOT 跳转表)。PIC 是"书里页码用'相对当前页'标注, 这样书被放到书架任何位置都能读"。

## 🟡 部分二·动手层（逐字操作）
1. 拆一个真实 ELF（用我们 ASM1 的 hello, 或任意程序）：
```bash
nasm -f elf64 hello.asm -o hello.o
gcc -nostdlib hello.o -o hello
readelf -h hello | head -n 20        # ELF 头: 类型/入口/节区数
readelf -S hello | head -n 20        # 节区表( .text/.data/.bss )
objdump -d hello | head -n 30        # 反汇编看代码段
```
2. 看重定位/动态符号（用带 printf 的 C 程序更好）：
```bash
echo 'int main(){ printf("hi\n"); return 0; }' > dyn.c
gcc dyn.c -o dyn
readelf -d dyn | head          # 动态段: 依赖 libc
objdump -R dyn | head          # 重定位表(GOT 项)
```
你会看到 `.dynamic`、对 `libc.so.6` 的依赖——这就是动态链接痕迹。

### 🧪 实战 Lab（CMP9）
运行 `readelf -h hello` 找到 `Entry point address`(程序入口虚拟地址)和 `Type`(EXEC), 抄下来; 再 `readelf -d dyn` 看到 `NEEDED libc.so.6`。写一句"ELF 头部记录入口地址与类型, 动态段记录依赖的共享库"。验收：把入口地址 + NEEDED 行 + 这句发我。

## 🔵 部分三·原理层（链接/装载细节）
目标文件(.o)含"节(section)"：.text(代码)、.data(已初始化全局)、.bss(未初始化, 只占空间)。链接分两步：① 符号解析——把各 .o 的"未定义符号"(如 printf)匹配到定义处；② 重定位——根据"重定位表"把代码中"引用外部符号的占位"改成真实运行地址(因链接时才知最终布局)。ELF(Linux)/PE(Windows)/Mach-O(macOS, SEC26)是三大格式。静态链接把库代码直接拷进可执行(大但独立)；动态链接把 .so/.dll 运行时加载(省内存、可更新)。PLT(过程链接表)+GOT(全局偏移表)实现"延迟绑定"：首次调用 printf 才经 PLT 跳 GOT 解析真实地址, 之后直接走。PIC(位置无关代码)用"相对 PC 寻址"让共享库可被加载到任意地址(配合 ASLR OS6/SEC44)。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
早期直接把程序写死绝对地址(不可重定位), 后来分离编译需要链接。顶级追问：强符号/弱符号(多次定义谁胜)、符号版本化(glibc 兼容性)、"符号裁剪(strip)"减体积、静态链接与 LTO(CMP7)配合；"加载时重定位 vs 运行时(PLT)"；"RELRO/Full RELRO"(把 GOT 设为只读防 GOT 覆写, SEC44 缓解)；容器/PIE(位置无关可执行, 默认开启配合 ASLR)。这些与 SEC25(ELF 深入)、SEC49(IAT Hook, PE)、OS15 容器强相关。

## 🔴 部分五·顶级视角
顶级逆向工程师(SEC25/27)对 ELF/PE/Mach-O 格式了如指掌——段/节/导入表/重定位是还原程序结构的地图。理解链接装载是写构建系统(C13)、做插件系统(动态加载 .so)、理解崩溃 backtrace、做安全缓解(RELRO/canary)的基础。开源：readelf/objdump(本机可用)、Cutter/Rizin(图形化, SEC27)。

## 🟠 部分六·安全/合规种子
GOT/PLT 是"GOT 覆写"类漏洞(SEC37/41)的作用点, RELRO/canary/ASLR 是防御。白帽：在自己机器用 readelf 拆解自己编译的程序 100% 合规(且是逆向入门必备)；绝不逆向他人商业软件、不修改他人二进制(红线)。理解格式才能做防御性构建。

## ✅ 验收
交：① `readelf -h` 的入口地址与 Type；② `readelf -d dyn` 的 NEEDED libc.so.6；③ 说出重定位/PLT/GOT 之一。过关。

## ⚠️ 常见坑
- 把"节(section, 链接视图)"和"段(segment, 装载视图, Program Header)"混：readelf -S 看节, -l 看段(运行时加载的单位)。
- MSYS2 下 `readelf/objdump` 是 MinGW 版, 可分析 ELF(交叉), 但若分析 Windows .exe(PE)要用 `objdump -f` 或 Cutter。
- 护手：复制 `readelf -h hello | head -n 20`。

## ➡️ 下一步
CMP10 · 垃圾回收（标记清除 / 分代 / 引用计数）——不用手动 free 的语言(Java/Python/Go/Rust?)背后, 内存是怎么自动回收的, 以及为什么会有"停顿"。

### ⚔️ 闯关路线（CMP9）
- 🟢 Lv1：用 `file` 看一个 ELF/PE 文件类型 🥉🥉
- 🔵 Lv2：说明链接把多个目标文件拼成可执行 🈶🥈
- 🟣 Lv3：解释重定位：把“暂时空着的地址”事后补上 🥇🥇
- 🟠 Lv4：对比静态链接（大但独立）与动态链接（省但依赖，接方向1b C16）🏆🏆
- 🔴 Lv5：写 200 字说明“ELF/PE 结构”是逆向（方向7b SEC25）与加载器的基础👑👑


---

# CMP10 · 垃圾回收（标记清除 / 分代 / 引用计数）

## 🎯 目标
学完这节，你能说清自动内存管理三种主流方案——标记-清除、分代 GC、引用计数——各自的原理与权衡，理解"Stop-The-World 停顿"从哪来，并能在自己机器用 Java/Python 观察 GC 行为(或读代码理解)。

## 📋 小白前置
需先懂 OS7（物理内存/分配器，GC 在它之上）、OS16（用户态分配器, GC 常自建堆）、CMP9（运行时需链接 GC 支持）。知道"指针/引用"概念(PY11/C3 指针)。

## 🟢 部分一·最浅层（生活比喻）
手动 `free`(C/C++)像"自己吃完饭自己洗碗", 忘了就堆脏碗(内存泄漏)或洗了还在用的碗(悬空指针)。自动 GC 像"餐厅雇了保洁阿姨"：① 标记-清除=阿姨定期巡视, 从老板(根对象)出发顺藤摸瓜, 摸到还在用的碗标"在用", 没被摸到的全扔掉；② 分代=阿姨发现"新碗大多很快用完", 所以新碗区勤打扫、老碗区少打扰；③ 引用计数=每只碗挂个计数器, 多一人用就+1, 没人用(0)立刻扔, 但"两碗互相指着对方"就谁都不扔(循环引用泄漏)。

## 🟡 部分二·动手层（逐字操作）
1. 用 Java 触发并观察 GC（若装了 JDK；或看 Python 的更简单）：
```bash
# Python: 看 GC 模块
python -c "import gc; print('阈值:', gc.get_threshold()); gc.collect(); print('手动回收完成')"
```
2. 制造大量短命对象, 看 GC 介入（Python 演示循环引用需 gc 模块）：
```python
import gc
class Node:
    pass
a, b = Node(), Node()
a.other = b; b.other = a      # 循环引用, 引用计数无法回收
del a, b
print("回收前:", gc.get_stats()[0]['collections'] if gc.get_stats() else 'n/a')
gc.collect()                  # 强制标记-清除回收循环
print("回收后, 强制 collect 完成")
```
```bash
python gctest.py
```

### 🧪 实战 Lab（CMP10）
运行 `gctest.py`, 确认 `gc.collect()` 能回收循环引用(引用计数做不到)。写一句"引用计数无法处理循环引用, 需标记-清除兜底"。验收：运行输出 + 这句发我（无 Python 用文字描述, 标注"待验证"）。

## 🔵 部分三·原理层（三种 GC 怎么工作）
标记-清除(Mark-Sweep)：从 GC roots(栈/全局变量/寄存器中的指针)出发, 顺着指针图做可达性分析, 标记存活对象, 清除未标记者(回收其内存, OS16 的 free 类操作)；缺点是产生碎片、且需暂停程序(Stop-The-World, STW)以保证标记时图不变。分代(Generational, Java/.NET/JVM JV7)：按对象年龄分年轻代/老年代, 大多对象"朝生暮死", 只对年轻代频繁 Minor GC, 老对象少扫, 大幅降停顿。引用计数(RefCount, Python/C++ shared_ptr RS5/ObjC)：每个对象记引用数, 归零即回收, 优点"即时、无全局停顿", 缺点"循环引用泄漏 + 计数原子操作开销(并发下)"。现代常组合：ObjC/Swift ARC(引用计数)+循环检测; Python 引用计数为主+周期 GC 兜底; Java 分代 GC 为主。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
McCarthy 1959 为 Lisp 发明 GC, 是计算机科学里程碑。顶级追问：并发/并行 GC(与程序同时跑, 减 STW, 如 G1/ZGC/Shenandoah)、增量 GC(把标记拆小段)、"写屏障"(跟踪跨代引用)、"三色标记"算法如何避免漏标；Rust 为何"多数情况不用 GC"而靠所有权(RS2 编译期确定释放, 零运行时开销)；Go 的 Tricolor GC(GO 运行时)、JVM ZGC 的 <10ms 停顿怎么做到？"GC 调优"(堆大小/代比例)是后端性能(OPS7/JV7)重头。

## 🔴 部分五·顶级视角
顶级后端/语言工程师据负载选/调 GC: 低延迟交易用无 STW 的 ZGC、大数据用 G1、嵌入式用无 GC 的 Rust/C。理解 GC 是学 JVM(JV7 GC/调优)、Go 运行时(GO)、Python 内存模型、写高性能服务(BE24)的必备。读《The Garbage Collection Handbook》。

## 🟠 部分六·安全/合规种子
GC 本身是内存安全的防线(防悬空指针/UAF, SEC38)——但 GC 实现若有 bug 也会成漏洞。白帽: 在自己机器观察 GC 行为合规; 理解 GC 有助于写"不泄漏"的程序(防御), 也理解"为什么 C 手动管理更易出 UAF 漏洞"从而更警惕。

## ✅ 验收
交：① `gctest.py` 输出(确认 collect 回收循环)；② 一句"引用计数不处理循环引用"；③ 说出标记-清除/分代/引用计数各一句。过关。

## ⚠️ 常见坑
- 以为 GC "万能无泄漏"：循环引用(引用计数方案)和"意外持有引用"仍会泄漏, 需理解其边界。
- 混淆"GC 停顿"与"内存泄漏"：停顿是回收开销, 泄漏是该收没收。
- 护手：复制 `gctest.py` 整段。

## ➡️ 下一步
CMP11 · JIT / 动态编译（热点 / 去优化）——Java/JS/Python 为什么"边跑边编译", 热点探测、去优化、分层编译是怎么让解释型语言也飞快的。

### ⚔️ 闯关路线（CMP10）
- 🟢 Lv1：用比喻说明 GC 像“自动扫地机器人回收垃圾对象”🥉🥉
- 🔵 Lv2：对比标记-清除与分代回收 🈶🥈
- 🟣 Lv3：说明引用计数（Python/Rust 部分）的循环引用问题 🥇🥇
- 🟠 Lv4：解释 GC 停顿对延迟敏感服务的影响 🏆🏆
- 🔴 Lv5：写 200 字说明“垃圾回收”消除一类 C 内存漏洞，但引入调优难题（接方向1b JV7）👑👑


---

# CMP11 · JIT / 动态编译（热点 / 去优化）

## 🎯 目标
学完这节，你能说清 JIT(即时编译)是什么、为什么解释器要引入它、热点方法怎么被探测并编译成本地码、去优化(deopt)为何发生，并能在自己机器用 Java `-Xint`(纯解释) vs 默认(含 JIT)对比运行速度，或用 Node/V8 概念理解。

## 📋 小白前置
需先懂 CMP1（静态编译流程）、CMP6（IR 是 JIT 的中间表示）、CMP10（运行时需 GC 配合）。知道"解释器"概念(逐行执行, 慢)。

## 🟢 部分一·最浅层（生活比喻）
解释器像"同声传译"：每句台词当场翻当场说, 灵活但慢(每句都重新理解)。JIT 像"同传发现某段戏要反复演, 干脆提前把它翻译成地道本地话背下来, 下次直接背"(编译成本地码缓存复用)。热点=那句被反复念的台词。"去优化"则是：背下来的版本基于"假设观众都懂中文", 结果来了个不懂的(类型变了), 只能退回同传重翻(保正确)。

## 🟡 部分二·动手层（逐字操作）
1. Java 对比解释 vs JIT（若装 JDK；无则跳到 2 用概念）：
```bash
# 写个热点循环
cat > Jit.java <<'EOF'
public class Jit {
  public static void main(String[] a){
    long s=0; for(int i=0;i<1_000_000_000;i++) s+=i;
    System.out.println(s);
  }
}
EOF
javac Jit.java
time java -Xint Jit        # 纯解释, 慢
time java Jit              # 默认含 JIT(分层编译), 明显快
```
2. 无 JDK 时, 用 Python 体验"解释慢、靠库走原生"：
```bash
python -c "import time; t=time.time(); s=0
for i in range(10_000_000): s+=i
print('耗时', time.time()-t)"
```
(说明：CPython 是解释器, 数值循环慢; NumPy DSA5 走编译码——呼应"热点交给编译后代码"。)

### 🧪 实战 Lab（CMP11）
若有 JDK, 记录 `-Xint` 与默认两次 `time` 的耗时差(默认应快数倍), 写一句"JIT 把热点循环编译成本地码, 省去逐条解释"。验收：两次耗时 + 这句(无 JDK 用 Python 说明 + 标注"待装 JDK 验证")。

## 🔵 部分三·原理层（JIT 怎么边跑边编）
解释器(字节码 VM, 如 JVM/PyPy/V8)先逐条执行字节码(OTH5)。JIT 在运行时插入"剖析器(profiler)"：统计每个方法/循环的调用次数、参数类型分布。当某方法超过"热点阈值"(如 10000 次), 触发"即时编译"——把该方法的字节码经 IR(CMP6, SSA)做优化(内联 CMP7、类型特化)生成机器码, 并替换原解释入口(后续直接跑机器码)。分层编译(tiered): 先用 C1(快编译少优化)再用 C2(慢编译多优化)升级。去优化(deopt)：若运行中发现假设不成立(如原以为参数都是 int, 来了个 string), 安全回退到解释器或低优化版本保正确——这是 JIT 敢激进优化的安全网。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
Self 语言(1990s)首创 JIT 思想, HotSpot(JVM)将其工程化。顶级追问：OSR(栈上替换, 在循环中间从解释切到编译)怎么实现；"内联缓存(IC)"怎么缓存"上次调用的方法版本"加速动态分派(多态); "逃逸分析"怎么在 JIT 层把堆分配优化到栈(呼应 RS2/CPP1); "GraalVM/V8 TurboFan"用 IR 做跨语言优化; "WASM JIT"(OTH5)为何接近原生。这些与 CMP8 代码生成、CMP12 DSL、SEC 性能分析相关。

## 🔴 部分五·顶级视角
顶级语言/VM 工程师调 JIT(调阈值/分层/去优化率)、写 JIT 编译器(如 LuaJIT、V8)。理解 JIT 是学 JVM 性能(JV7)、JS 引擎(JS7/V8)、Python PyPy、Go 运行时、写高性能脚本的基础。开源：OpenJDK HotSpot、V8、PyPy、LLVM ORC JIT(做自定义 JIT)。

## 🟠 部分六·安全/合规种子
JIT 的"可写可执行内存"历史上是安全弱点(JIT spraying/绕过 DEP, SEC44)——现代做法用 W^X(写时不可执行)或细粒度权限缓解。白帽：在自己机器跑 Java/Node 观察 JIT 合规; 理解 JIT 安全边界有助于防御(仅分析, 不在他人进程做 JIT 注入)。

## ✅ 验收
交：① `-Xint` 与默认耗时差（或 Python 说明）；② 一句"JIT 编译热点省去解释开销"；③ 说出去优化为何发生(假设不成立回退)。过关。

## ⚠️ 常见坑
- 以为"解释器慢就不可用"：JIT 让其长期运行后接近原生, 且保留灵活性(动态类型)。
- 首次运行含 JIT 编译开销, 微基准要"预热(warmup)"才准——否则测到的是编译期而非运行期。
- 护手：复制 Java 片段(若用, 在装 JDK 环境跑)。

## ➡️ 下一步
CMP12 · DSL（领域特定语言设计）——最后一节：当你发现"通用语言写某类任务太啰嗦", 怎么设计一门专属于该领域的"小语言", 以及编译器技术怎么用来造它。

### ⚔️ 闯关路线（CMP11）
- 🟢 Lv1：用比喻说明 JIT 像“边跑边把热路径翻译成机器码”🥉🥉
- 🔵 Lv2：说明热点探测：哪段跑得多就编译哪段 🈶🥈
- 🟣 Lv3：解释去优化：假设不成立时退回解释执行 🥇🥇
- 🟠 Lv4：对比 JIT（Java/JS）与 AOT（C/Go/Rust）取舍 🏆🏆
- 🔴 Lv5：写 200 字说明“JIT”是 JVM/V8 高性能的秘密（接方向1b JV/JS）👑👑


---

# CMP12 · DSL（领域特定语言设计）

## 🎯 目标
学完这节，你能说清 DSL(领域特定语言)是什么、内外 DSL 的区别、用前面学的编译器技术(CMP2–CMP8)怎么造一门小 DSL，并能在自己机器用 Python 写一个"计算器 DSL / 配置文件 DSL"雏形(词法+语法+求值), 串起整条编译流水线。

## 📋 小白前置
需先懂全部 CMP1–CMP11：DSL 的造法就是把它们串起来用。会 Python(PY1–PY13)做演示最省力(护手复制)。

## 🟢 部分一·最浅层（生活比喻）
通用语言(如 C/Python)像"万能瑞士军刀", 啥都能干但干特定活不趁手。DSL 像"专门开红酒的螺旋钻"——只干一件事, 但极顺手。例：SQL 是"查数据库的语言"、正则(CMP2)是"描述文本模式的语言"、HTML( FE1)是"描述网页结构的语言"、makefile(C13)是"描述构建依赖的语言"。你也能为自己的小领域造一把专用螺丝刀。

## 🟡 部分二·动手层（逐字操作）
1. 写一个"迷你计算器 DSL"：支持 `let x = 3 + 4 * 2` 与 `print x`。把 CMP2–CMP4 串起来：
```python
import re, ast as A
src = "let x = 3 + 4 * 2\nprint x"
# 极简: 用正则+eval 演示 DSL 求值(教学版, 真实 DSL 用 AST 遍历)
def run(dsl):
    env = {}
    for line in dsl.splitlines():
        if line.startswith('let '):
            _, rest = line[4:].split('=',1)
            name, expr = rest.strip().split('=') if False else (line[4:line.index('=')].strip(), line[line.index('=')+1:])
            env[name] = eval(expr, {}, env)
        elif line.startswith('print '):
            print(env[line[6:].strip()])
run(src)   # 输出 11
```
2. 运行：
```bash
python dsl.py
```
输出 `11`(证明我们的 DSL 把 `3+4*2` 算成 11, 遵循优先级)。

### 🧪 实战 Lab（CMP12）
把 `src` 改成 `"let a = 10\nlet b = a * 2\nprint b"`, 重运行应输出 `20`。写一句"DSL 把领域任务(算式)用专用语法表达, 底层仍是编译器/解释器那套技术"。验收：运行输出 20 + 这句发我。

## 🔵 部分三·原理层（DSL 怎么造）
DSL 分两类：① 外部 DSL——有独立语法, 需全套编译器前端(CMP2 词法→CMP3 语法→CMP4 AST→CMP5 语义→CMP6 IR→CMP8 代码生成/或解释执行), 如 SQL/正则自己实现解析器；② 内部 DSL——嵌在宿主语言里, 用该语言的语法糖模拟专用语感, 如 Ruby 的 RSpec、Python 的 decorator 链(PY14)、Java 的 Stream API、SQL 的 ORM(DB26)。造外部 DSL 就是复用本课全部技术：用 flex/bison 或手写递归下降(CMP3)、用 AST 遍历求值(CMP4)、用 IR 优化(CMP6/7)、可静态编译(CMP8)或 JIT(CMP11)。设计关键是"限定领域、暴露最少概念、报错友好"。

## 🟣 部分四·深挖层（为什么、历史、顶级追问）
Unix 哲学"每种工具做一件事并做好"催生大量 DSL(awk/sed/sh, SH2)。顶级追问：DSL 与"配置即代码"(IaC, OPS33 Terraform 的 HCL 就是 DSL)的关系；"语言工作台"(MPS/Xtext)怎么可视化造 DSL；"宏系统"(RS9/PAR4)如何实现内部 DSL 且不牺牲类型安全；"DSL 的陷阱"——过度设计成"另一种通用语言"(失败案例); 与"低代码/可视化编程"的边界。这些与 PAR 范式、SEC 规则引擎(SEC65 WAF 规则即 DSL)、BE GraphQL(SQL 类 DSL)都相关。

## 🔴 部分五·顶级视角
顶级工程师为团队/产品造 DSL 提效：游戏脚本(OTH1 Lua)、着色器语言(MOB49 GLSL)、构建脚本(C13)、IaC(OPS33)、查询语言(DB3)。理解 DSL 设计是"抽象能力"的巅峰体现, 也串联了从 COMP(硬件)→ASM→OS→CMP 的全部底层知识——因为造语言就是和"机器怎么执行"对话。读《DSL Engineering》《Domain-Specific Languages》(Fowler)。

## 🟠 部分六·安全/合规种子
DSL/规则引擎若"把用户输入当代码 eval"就是注入温床(SEC10)——白帽铁律: DSL 解释器必须"解释数据而非执行代码", 用 AST 白名单求值而非 `eval` 未知字符串(本节 demo 的 `eval` 仅教学, 真实产品严禁对外部输入 eval)。自己机器造 DSL 练习合规。

## ✅ 验收
交：① `dsl.py` 改后输出 20；② 一句"DSL 底层复用编译器全套技术"；③ 说出内部/外部 DSL 区别。过关。

## ⚠️ 常见坑
- 教学 demo 用了 `eval`——真实产品**绝不对不可信输入 eval**(注入风险!), 应走 AST 解释(CMP4)白名单。
- 把"DSL"和"API/库"混：DSL 有自己语法感, API 只是函数调用; 内部 DSL 介于两者。
- 护手：复制 `dsl.py` 整段。

## ➡️ 下一步
方向3 底层系统（COMP1–7 / ASM1–8 / OS1–17 / CMP1–12，共 44 节）已全部完成！下一步进入方向4 网络（NET1–NET24）或回头巩固：建议按总纲顺序，方向3 之后接方向4 网络、方向5 数据库、方向6 Web 全栈，再深入方向7 安全攻防（你最想成为的"白帽黑客"），全方向铺开。每节都按 11 段深模板、白帽红线贯穿。继续加油，泽泽！

### ⚔️ 闯关路线（CMP12）
- 🟢 Lv1：用比喻说明 DSL 像“给特定领域定制的迷你语言”🥉🥉
- 🔵 Lv2：对比内部 DSL（借宿主语言语法）与外部 DSL（自己 parser）🈶🥈
- 🟣 Lv3：用 Python 写一个极简内部 DSL（如查询构造器）🥇🥇
- 🟠 Lv4：说明何时该上外部 DSL（需要独立语法/安全边界）🏆🏆
- 🔴 Lv5：写 200 字说明“DSL 思维”能让你给团队造趁手工具，但别滥用（安全：别对不可信输入 eval）👑👑

