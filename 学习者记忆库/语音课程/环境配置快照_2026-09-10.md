# 环境配置快照 · 2026-09-10

这是一次只读检查的结果。工具版本会变化；重新执行配置清单中的核验命令，以当前输出为准。

## 已找到

| 工具 | 当前结果 |
|---|---|
| Python | `C:\Python310\python.exe` · 3.10.11 |
| VS Code | 1.133.0 |
| Git | 2.53.0.windows.3 |
| Node.js | v24.17.0 |
| npm | 11.13.0 |
| Java 运行时 | OpenJDK 17.0.20.1；当前只找到 `java`，未找到 `javac` |
| WSL | 找到 `wsl.exe`；状态输出需要你在本机终端再次确认 |
| FFmpeg | 9.0-full_build |
| Docker | 29.7.2 |
| `edge-tts` Python 包 | 已找到 |
| 本地课堂服务 | `127.0.0.1:8765` 正在监听 |

## 尚未找到

这些不是现在必须安装的东西，按课程进入顺序配置：

- `pytest`、`ruff`、`mypy`：进入 Python 工程课前安装。
- `rustc`、`cargo`：进入 Rust 轨道前安装 rustup/stable。
- `go`：进入 Go 轨道前安装稳定版。
- `tshark`：需要抓包命令行时再随 Wireshark 安装。
- `psql`：进入 PostgreSQL 高阶课前安装；入门先用 SQLite。
- `sqlite3` 命令行：不是必需，Python 的 `sqlite3` 模块可先完成入门。
- `javac`：进入 Java 编译课前安装 JDK，而不是只安装 JRE。

## 当前不需要做的事

- 不要删除系统 Python、Node、Java 或 Docker。
- 不要为了课程把所有语言一次装完。
- 不要把密码、Cookie、API Key 写进课程 JSON、视频配置、截图或日志。
- 不要为了抓包或安全实验分析不属于自己的流量。

## 复查命令

```powershell
python --version
code --version
git --version
node --version
npm --version
java --version
ffmpeg -version
docker --version
python -c "import importlib.util; print({m: bool(importlib.util.find_spec(m)) for m in ['edge_tts','pytest','ruff','mypy']})"
Get-NetTCPConnection -LocalPort 8765 -State Listen
```

发现版本变化时，在自己的学习记录中记下日期、版本和影响的课程，不覆盖旧证据。
