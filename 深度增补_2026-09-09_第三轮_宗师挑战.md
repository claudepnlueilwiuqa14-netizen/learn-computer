# 深度增补·第三轮：八大宏域宗师挑战（2026-09-09）

本轮不是再堆一层目录，而是把第二轮机器基线中仍未完成的高难项，拆成真正可执行、可失败、可复盘、可答辩的研究型挑战。它建立在 `深度增补_2026-09-09_第二轮.md`、590 张逐课补强卡和八项 `capstone/` 之上。

机器基线通过只说明探针能运行；本文件的每个 Boss Lab 都要求学习者换输入、换实现或换假设，并把结果写入课号证据目录。没有学习者证据，不得把任何宏域改为“宗师级完成”。

## 使用顺序与证据合同

1. 先完成对应课号的原课 11 段、Lv1–Lv5 和五个基础 Lab，再读本文件的宏域挑战。
2. 选择一个 Boss Lab，复制输入和版本信息到 `evidence/<课号>/boss/<挑战编号>/`；不得覆盖机器基线目录。
3. 先写假设和不变量，再运行控制组；只改变一个主变量，至少三次重复，并记录失败分支和清理动作。
4. 将命令、代码、日志、指标、图表、哈希、资料章节、已验证命题和未验证项一并提交。
5. 完成后做一次五分钟口述答辩：解释模型、反例、取舍、限制、威胁边界和下一步；“结果看起来对”不是验收。

每个挑战目录至少包含：

```text
README.md          # 问题、假设、不变量、授权边界和结论
commands.txt       # 可复制命令、版本和环境变量
tests/             # 控制组、变体、失败回归
artifacts/         # 日志、pcap、trace、图表、二进制或模型卡
notes.md           # 资料命题、反例、未验证项和复盘
```

## L6–L10 统一难度梯度

| 等级 | 目标 | 过关证据 |
|---|---|---|
| L6 复现者 | 独立重放机器基线，确认输入、版本、输出和清理 | 干净目录三次复现，输出哈希一致或解释差异 |
| L7 破坏者 | 只改一个变量，主动制造反例、故障或性能回归 | 失败日志、最小反例、修复前后差异和回归测试 |
| L8 设计者 | 写出第二种机制或架构，对比不变量、成本和威胁 | ADR、控制/实验组、指标置信区间或时间线 |
| L9 研究者 | 换数据、换规模或换环境验证结论边界 | 研究记录、证伪条件、重复实验、资料章节和限制 |
| L10 答辩者 | 教会别人并接受追问，解释为何不选其他方案 | 口述/书面答辩、审阅意见、修订版和下一步实验 |

## I. 预备与编程：可迁移语言与可复现交付

绑定课包：PRE、PY、C、CPP、JV、JS、GO、RS、PAR、OTH、SQL、SH；毕业工程：`capstone/01_预备与编程/`。

### 本轮要消除的缺口

- 从固定 200 个性质样例扩展到学习者自造、可缩减、可解释的反例语料。
- 用同一输入契约比较运行时、内存、退出码、冷启动和包体，而不是只比较“能运行”。
- 生成离线、签名、锁定、可独立复现的发布制品，并说明供应链信任边界。

### L6–L10 闯关

- **L6**：在干净临时目录重放四实现向量套件，锁定编译器/解释器版本、区域设置、换行和编码。
- **L7**：给每种实现注入空输入、截断 UTF-8、NaN、整数溢出、非法参数和重复执行，使用缩减器保存最小反例。
- **L8**：任选一种实现改成流式或增量接口，写 ADR 比较内存峰值、错误恢复、背压和 API 兼容性。
- **L9**：在两台不同运行时或容器镜像上做冷启动/包体/峰值内存实验，至少三次重复并报告置信区间或测量噪声。
- **L10**：向未读过代码的人讲清“同一个字节怎样经过解析、类型、运行时和系统调用”，接受关于未定义行为和版本锁定的追问。

### 五个 Boss Lab

1. **A1 跨语言语义契约**：用同一 JSONL 输入驱动 Python、C/Rust、Go、JS；产出结果/错误/退出码矩阵。故障注入为编码、时区和换行差异；通过条件是 16 类向量全部有可解释的一致或有意差异，且差异绑定语言规范章节。
2. **A2 反例生成与缩减**：故意破坏括号解析或边界检查，用固定种子加随机种子生成至少 1000 个候选，缩减到最小可读样本。通过条件是修复前失败、修复后回归、缩减过程可重放，不能只贴最终输入。
3. **A3 生命周期与并发**：以同一有界生产者/消费者契约实现 asyncio、Go channel、Rust async 或等价方案；测量容量、取消、泄漏、吞吐和尾延迟。注入消费者崩溃，证明生产者不会无限增长。
4. **A4 可复现发布**：锁定依赖、生成 SBOM/哈希、离线构建两次并比较制品；再用自有测试密钥验证签名失败路径。通过条件是缺包、篡改、版本漂移均“响亮失败”，清理不泄露密钥。
5. **A5 最小语言答辩**：扩展第二轮解释器的一个语法特性，提交 EBNF、AST、错误恢复、10 个性质测试和“不加入另一特性”的 ADR；用三条恶意但无害的输入验证资源上限。

资料命题：Python Language Reference、Rust Reference、Go Memory Model、C++ Core Guidelines、TUF/Sigstore 文档；每条命题写 URL、章节、访问日期和未验证边界。

## II. 算法与数学：证明、反例与规模效应

绑定课包：DSA1–DSA25；毕业工程：`capstone/02_算法与数学/`。

### 本轮要消除的缺口

- 机器性质套件之外的独立证明审查和领域反例。
- 并行排序/图算法的规模、缓存和调度实验，不能把并行实现自动宣称为更快。
- 从抽象复杂度到真实分配、缓存、分支和输入分布的可解释基准。

### L6–L10 闯关

- **L6**：独立重写三个证明（循环不变量、交换论证、势能法），再逐行对照反例。
- **L7**：实现一个有已知错误的版本，让性质测试缩减出最小反例；记录生成器为何覆盖该状态空间。
- **L8**：为 10^2、10^3、10^4、10^5、10^6 输入建立控制/变体基准，分离算法时间、分配、缓存和调度开销。
- **L9**：对结果做至少三次重复、异常值说明和置信区间；提出一个结论会失效的输入分布。
- **L10**：以真实但合成的数据集做模型答辩，解释下界、复杂度、工程取舍和未选择算法。

### 五个 Boss Lab

1. **B1 证明审查会**：故意删除一个循环不变量前提，让同伴或自审检查找出反例；产出原证明、审查批注、修订证明和自动回归。
2. **B2 领域反例语料**：为调度、路由、压缩或推荐任选一项，定义输入分布、oracle 和最小化策略，至少产生 50 个可分类反例；通过条件是每个反例能说明哪条假设失效。
3. **B3 缓存与布局**：比较数组、链式结构和分块布局，固定算法不变量，只改变布局；记录峰值内存、分配次数、缓存/分支代理指标并解释噪声。
4. **B4 并行伸缩**：用串行、两线程和进程池实现同一算法，测量并行开销、吞吐、尾延迟和正确性；注入取消或 worker 失败，提交恢复时间线，不以单次 P50 宣称加速。
5. **B5 算法选择答辩**：从真实系统约束写模型、下界、可行候选、实验矩阵和失败结果；通过条件是另一位读者按 README 能重跑并得到同样的选择理由。

资料命题：MIT 6.006、MIT 6.042、Open Data Structures、Hypothesis 文档和 `capstone/02_算法与数学/docs/proofs.md`；证明必须注明适用域。

## III. 计算机系统：从用户态到恢复协议

绑定课包：COMP、ASM、OS、CMP；毕业工程：`capstone/03_计算机系统/`。

### 本轮要消除的缺口

- 无锁错误版本与 mutex/原子版本的定量对照。
- 持久化恢复探针之外的崩溃矩阵、恢复不变量和幂等重放。
- 用户态函数、库、系统调用、内核和硬件边界的连续证据。

### L6–L10 闯关

- **L6**：重放环形队列、pthread、GDB 和 `persistence_probe.c`，保存编译参数、源哈希、退出码和临时目录清理。
- **L7**：删掉一处锁或改变内存序，使用固定线程数和重复运行捕捉丢失更新；不能把“这次没复现”写成通过。
- **L8**：改写恢复器，使提交记录、未提交尾部、重复重放和损坏校验各有明确状态；写恢复状态机。
- **L9**：在 xv6/Nand2Tetris 或等价教学内核追踪一次系统调用，比较 Windows/MinGW 用户态可观察证据与教学内核路径。
- **L10**：以调用链、寄存器、汇编、系统调用计数和恢复时间做答辩，说明哪些硬件/内核事实没有在本机直接观察到。

### 五个 Boss Lab

1. **C1 竞态对照**：实现正确 mutex、原子计数器和明确错误的无锁版本；固定线程/迭代次数，记录错误率、锁等待、吞吐和清理。通过条件是失败版本有可复现反例，修复版本有同步理由。
2. **C2 崩溃矩阵**：在临时文件模拟写入前退出、提交后退出、尾部损坏、重复恢复四类事件；产出状态机、RPO/RTO、恢复日志和幂等测试。
3. **C3 系统调用路径**：为自有小程序选一个文件/线程/时间调用，提交源码、预处理、汇编、符号、strace/等价工具或教学内核路径；标注证据缺失点，不能凭猜测补齐。
4. **C4 内存与页表观察**：用自有程序比较分配、释放、映射、缺页或缓存行为；只使用受控大小，注入分配失败并验证释放。通过条件是指标与模型一致或解释差异。
5. **C5 根因答辩**：对一个自有崩溃提交“症状→最小复现→假设→证据→修复→回归→残余风险”报告，并让另一人只读报告重现。

资料命题：OSTEP 持久化/并发章节、Nand2Tetris、xv6 book、GDB Manual、DWARF 和 RISC-V/Intel 手册；Windows 工具差异必须写入限制。

## IV. 网络与分布式：包级证据与部分失败

绑定课包：NET1–NET24 及相关 OPS 课；毕业工程：`capstone/04_网络与分布式/`。

### 本轮要消除的缺口

- 从应用字段扩展到回环 pcap、RFC 条款和 TCP/TLS/IPv6 证据。
- 真实可控的延迟、丢包、乱序、时钟偏差、quorum 和容量实验；无法使用内核工具时必须使用明确标注的合成状态机。
- 重试、幂等、选主和回滚在部分失败下的边界，而不是只证明最终返回 200。

### L6–L10 闯关

- **L6**：重放双节点 HTTP KV、字段观察和 ScriptedPartition，保存每次尝试、错误和收敛时间。
- **L7**：只改变超时、退避、节点顺序或幂等键，构造重复写、旧读、重试风暴和永久失败。
- **L8**：加入回环 pcap/RFC 映射或等价合成包模型，区分应用、传输、网络和时间层的证据。
- **L9**：做容量预算、quorum 和时钟偏差实验，明确安全/活性牺牲和 RTO/RPO。
- **L10**：现场关闭节点、恢复节点并解释一条请求的包、日志、trace、状态机和回滚决策。

### 五个 Boss Lab

1. **D1 pcap→RFC**：在 `127.0.0.1` 抓取自有服务，标注 DNS/TCP/TLS/HTTP 字段并引用 RFC 9110/9293/8446；故障是过滤器误选，要求提交修正后的过滤器和遗漏说明。
2. **D2 分区与乱序**：用网络命名空间/本机脚本或 `ScriptedPartition` 模拟两次丢失、延迟和乱序；产出尝试时间线、幂等性证明和不能恢复的情况。
3. **D3 协议矩阵**：在可用本地工具上比较 IPv4/IPv6、TLS、HTTP/1.1、HTTP/2/3；没有某协议时提交工具缺失证据与同机构资料替代，不伪造结果。
4. **D4 时钟与 quorum**：为三个合成节点注入可控时钟偏差和网络延迟，比较多数派读写、租约和无时钟方案；通过条件是说明线性一致性承诺的边界。
5. **D5 容量与回滚**：从请求率、并发、连接、重试放大和错误预算推导容量，压测仅限回环；注入单节点故障，验证退化和回滚时间。

资料命题：RFC 9293、RFC 9110、RFC 8446、RFC 9000、RFC 8200、Wireshark User Guide、Raft 论文；每个协议结论必须绑定字段或明确标为模型假设。

## V. 数据库与数据工程：复制、恢复和治理

绑定课包：DB1–DB28、DAT1–DAT24；毕业工程：`capstone/05_数据库与数据工程/`。

### 本轮要消除的缺口

- 教学日志流之外的多进程复制、split-brain 防护和延迟测量。
- 至少三个独立执行计划优化，且用同一 workload 与统计信息解释。
- 数据契约、血缘、脱敏、保留和删除请求与运行故障连成闭环。

### L6–L10 闯关

- **L6**：重放 SQLite WAL、快照、角色矩阵、lineage/v1 和 LSN/lag/epoch fencing。
- **L7**：杀掉 writer、延迟 replica、制造错误迁移和约束冲突，保存事务时间线与恢复结果。
- **L8**：实现第二种复制/回填策略或第三个执行计划，写清一致性和成本牺牲。
- **L9**：将数据质量、血缘、脱敏和删除权加入失败管道，验证“响亮失败”而不是静默丢行。
- **L10**：用 RPO/RTO、锁等待、P95、WAL 大小、质量通过率和权限证据答辩。

### 五个 Boss Lab

1. **E1 多进程复制**：两个真实本地进程写入合成日志，注入网络/进程停止和 epoch 过期；通过条件是旧主写入被拒绝、lag 可解释、恢复后无重复提交。
2. **E2 执行计划三连**：对同一查询只改索引、统计信息或连接顺序，保存三份 EXPLAIN 前后证据；不能以优化器偶然换计划代替原因分析。
3. **E3 崩溃恢复矩阵**：在 WAL/快照前后、检查点前后杀进程，测量恢复时间、丢失范围和校验；提交声明 RPO/RTO 与未覆盖的真实磁盘行为。
4. **E4 契约与血缘**：建立版本化 schema、质量门、OpenLineage 事件和脱敏样例；注入上游字段删除、重复行和个人数据标记错误，验证阻断和清理。
5. **E5 回填与幂等**：对失败窗口做可重跑回填，比较 exactly-once 的实现成本与至少一次加幂等键；产出重复检测、审计和回滚报告。

资料命题：PostgreSQL、SQLite WAL/Backup、CMU 15-445、Arrow、Great Expectations、OpenLineage；数据库产品行为和关系理论必须分开引用。

## VI. Web 与产品工程：可访问、可观测、可回滚

绑定课包：FE、BE、PD；毕业工程：`capstone/06_Web产品/`。

### 本轮要消除的缺口

- 无依赖 a11y 基线之外的 axe/WCAG 全规则或可审计替代方案。
- LCP/INP/CLS、API P95、缓存命中和错误率的可重复 trace。
- 进程外部署、迁移失败、版本回滚和用户研究证据。

### L6–L10 闯关

- **L6**：重放本地浏览器语义 DOM、Tab、状态反馈、窄屏和持久化订单证据。
- **L7**：注入重复提交、数据库不可用、权限错误、慢网和半发布，验证用户可理解的失败状态。
- **L8**：使用 axe/Playwright 或等价工具建立规则差异表，说明工具未覆盖的人工检查。
- **L9**：在固定设备/网络预算下重复采集 Web Vitals、API P95、错误率和回滚时间，保存 trace 哈希。
- **L10**：以真实用户任务（合成参与者也可）做研究、设计、指标、威胁模型和回滚演示答辩。

### 五个 Boss Lab

1. **F1 WCAG 闭环**：对套餐流程做自动化规则扫描和键盘/读屏人工脚本；故意移除 label、名称或 focus 管理，提交发现、修复和回归。
2. **F2 性能 trace**：固定冷/热缓存、网络和设备，测 LCP/INP/CLS、API P95、SQL P95 和错误率；通过条件是说明一个改动的因果而非只给前后数字。
3. **F3 进程外回滚**：把 Web 服务和 SQLite 迁移放入可重启进程，注入半发布/迁移失败，验证旧版本仍可读、回滚时间和数据兼容。
4. **F4 安全与容量**：在本地对认证、CSRF、幂等、限流、缓存和队列做合成负载；不接真实支付、不使用个人数据，提交威胁边界和错误预算。
5. **F5 用户证据**：设计至少三条任务流和失败问卷，记录观察、假设、改动、指标和隐私最小化；通过条件是产品决定能追溯到证据。

资料命题：MDN、W3C WCAG 2.2、WAI-ARIA APG、web.dev Performance、OWASP ASVS、OpenTelemetry、NN/g；版本和工具覆盖范围必须写清。

## VII. 安全与逆向：授权隔离、版本回归与报告

绑定课包：SEC1–SEC102 及 W/B/C 专题；毕业工程：`capstone/07_安全逆向/`。

### 本轮要消除的缺口

- AST v1/v2 合成 corpus 之外的多版本回归和人工审查。
- 真实但明确授权的容器/虚拟机快照（无法提供时，必须完成 descriptor-only 的替代报告）。
- 技术报告与管理报告的证据链、残余风险、披露和停止条件。

### L6–L10 闯关

- **L6**：重放授权范围、合成 corpus、AST 修复 diff 和 TP/FP/FN 指标。
- **L7**：增加跨版本、混淆但无害的自有样本，制造误报/漏报并修复规则。
- **L8**：在隔离容器/虚拟机或教学替代环境执行允许动作，保留快照哈希和停止条件。
- **L9**：把发现映射到 CWE/OWASP/CVSS/ATT&CK 防御控制，写技术版和管理版两份报告。
- **L10**：进行授权边界答辩，解释为何拒绝越权请求、如何保全证据、何时停止和如何通知维护者。

### 五个 Boss Lab

1. **G1 多版本回归**：对自有代码 v1/v2/v3 建立统一 corpus，记录规则版本、TP/FP/FN、修复 diff 和退化；通过条件是旧漏洞不重新出现且误报有解释。
2. **G2 隔离快照**：使用自有容器/VM 或 descriptor-only 教学替代，验证启动、输入、网络隔离、时间限制、清理和快照恢复；禁止公网扫描、真实凭据和恶意持久化。
3. **G3 二进制根因**：对自己编译的教学二进制做静态/动态分析，提交符号、调用链、崩溃分类和安全编译选项回归。
4. **G4 供应链审计**：生成 SBOM、锁定依赖、验证签名和漏洞公告，注入篡改/过期依赖；通过条件是构建阻断和回滚路径可重放。
5. **G5 双版本报告**：从同一证据写开发者修复单和管理者风险摘要，保留授权证明、时间线、残余风险、披露与停止条件。

资料命题：OWASP Top 10/ASVS/WSTG、CWE、CVE/CVSS、NIST NICE/SP 800-61、MITRE ATT&CK、Python `ast`；只把 ATT&CK 用作防御映射。

## VIII. AI、云原生与端侧：可追溯部署与风险闭环

绑定课包：MOB、DAT25–DAT51、OPS；毕业工程：`capstone/08_AI云原生端侧/`。

### 本轮要消除的缺口

- 回环模型服务之外的容器/Kubernetes 资源、探针、节点重启和资源耗尽演练。
- 数据/模型注册表、切分泄漏、漂移、评估污染和模型卡的可追溯链。
- 签名 OTA、灰度/回滚、成本、功耗、离线和端侧隐私的量化证据。

### L6–L10 闯关

- **L6**：重放确定性切分、泄漏拒绝、回环 `/health`/`/ready`/`/predict`/`/metrics` 和运行合同。
- **L7**：注入 413、模型版本不兼容、依赖不可用、探针失败和资源上限，验证自动恢复或人工 Runbook。
- **L8**：建立数据/模型/镜像版本注册表，加入签名、SBOM、模型卡和可撤销发布。
- **L9**：在合成负载上测质量、延迟、吞吐、CPU/内存、成本、漂移和功耗代理指标，写适用边界。
- **L10**：以红队防御、隐私、SLO、灰度/OTA 回滚和事故复盘做端到端答辩；不把离线模型结果当成真实世界安全保证。

### 五个 Boss Lab

1. **H1 资源与探针**：在本地 Docker/Kubernetes 或等价沙箱部署模型服务，注入 CPU/内存上限、Pod 重启、依赖不可用和探针失败；提交资源曲线、恢复时间和人工介入点。
2. **H2 数据/模型注册**：为数据、代码、模型和镜像生成版本指纹，检测跨切分重复、污染和漂移；通过条件是任一指纹不一致都能阻断或进入明确审批。
3. **H3 签名 OTA**：用测试密钥为自有模型/镜像签名，模拟篡改、密钥轮换、灰度失败、断网和回滚；提交 TUF/Sigstore 证据和密钥清理记录。
4. **H4 成本与功耗**：固定任务质量门，比较 CPU/GPU/端侧或量化方案的延迟、内存、成本和功耗代理；报告何时质量下降不可接受。
5. **H5 安全评估**：对合成提示注入、越权工具调用、隐私泄露和错误自信构造测试集；只验证防御和拒绝路径，提交模型卡、红队记录、人工兜底和残余风险。

资料命题：PyTorch、scikit-learn model selection、NIST AI RMF、Kubernetes Concepts/Security、OpenTelemetry、TUF、Sigstore、Android/Apple/Zephyr 文档；每个模型结果必须带数据/代码/硬件/随机种子。

## 宗师挑战统一毕业闸门

一个宏域完成本轮挑战，必须同时满足：

- 五个 Boss Lab 至少完成四个，且至少一个是 L9 或 L10；未完成项要有真实阻塞证据，不得用“以后再做”替代。
- 每个已完成 Lab 有控制组、单变量变体、三次重复、至少一个失败分支、清理记录和资料命题。
- 至少一个结果被学习者自己的反例推翻或限制，并完成修订；所有“没有发现问题”都要说明搜索空间。
- 代码、日志、图表、报告和版本可在干净目录重放；安全课必须有授权证明和停止条件。
- 宏域毕业工程的测试、指标、威胁模型、运行手册、回滚和复盘全部指向这些挑战的证据，而不是只指向机器基线。

第三轮挑战完成后，才有资格申请对应宏域的人工验收；本文件本身、脚本 PASS、自动生成的报告和机器基线都不等于学习者毕业。
