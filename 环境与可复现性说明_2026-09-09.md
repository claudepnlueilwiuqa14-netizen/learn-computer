# 课程实验环境与可复现性说明

版本：2026-09-09  
用途：把教案中的“工具链假设”与当前机器事实分开，避免把安装说明误读成已经安装。

## 当前机器实测

| 工具/能力 | 状态 | 证据或说明 |
|---|---|---|
| Python | ✅ 可用 | `C:\Python310\python.exe`；抽样课程已运行 |
| Node.js | ✅ 可用 | `D:\软件\node.exe`；抽样课程已运行 |
| Java 运行时 | ✅ 可用 | JRE 17；JV1 抽样通过 |
| PowerShell / curl | ✅ 可用 | 本说明、抽样脚本和资料复核使用 |
| Docker / kubectl | ✅ 可用 | 以本地容器/集群为边界；不连接他人资源 |
| SQLite Python 模块 | ✅ 可用 | SQL1、DB 抽样通过 |
| gcc / g++ | ✅ 可用 | MSYS2：`C:\msys64\mingw64\bin`；16.2.0；C1/CPP1 已真实编译运行 |
| Go | ✅ 可用 | MSYS2：`C:\msys64\mingw64\bin\go.exe`；`GOROOT=C:\msys64\mingw64\lib\go`；GO1 已 `go test` |
| Rust cargo | ✅ 可用 | MSYS2：cargo 1.98.1；RS1 已 `cargo test` |
| objdump | ✅ 可用 | MSYS2 GNU Binutils 2.47；ASM1 已对自有二进制反汇编 |
| MSYS2 Bash | ✅ 可用 | `C:\msys64\usr\bin\bash.exe`；5.3.15；SH1 已执行严格模式脚本；Windows 商店别名仍不视为 Bash |
| Android SDK / adb | 未核验 | 端侧课程必须先执行课程内环境检查 |
| PyTorch / CUDA | 未核验 | AI 课程先用 CPU/合成数据基线，GPU 只在实际核验后启用 |

## 逐课最小探针证据

`抽样运行_2026-09-09/SUMMARY.md` 记录 14 条交付轨道全部课号，共 590 课：590 条 PASS、0 条 `BLOCKED_TOOL`、0 条 FAIL。PASS 只代表每课一次受控本地最小探针通过，不代表该课的全部 Lab、毕业工程或宗师级掌握完成。

可复现命令：`& .\运行课程抽样.ps1 -Full`。默认不带 `-Full` 时只执行维护用的 112 课快速抽样；`-Full` 才会从 `逐课补强卡_590课.md` 自动提取全部课号。

## 每次开课前的核验顺序

PowerShell：

```powershell
python --version
node --version
java -version
docker version
kubectl version --client
curl.exe --version
Get-Command gcc,g++,go,cargo,objdump,bash -ErrorAction SilentlyContinue
```

课程需要的原生工具不存在时（本机当前 MSYS2 工具已可用）：

1. 在证据 README 标记 `BLOCKED_TOOL`，记录完整命令、版本检查和阻塞原因。
2. 使用教案给出的官方安装入口或本地容器/模拟器替代路径；替代路径必须明确写“不能证明原生工具语义完全一致”。
3. 安装后重新运行对应抽样课，保留安装前后的版本输出，不覆盖旧证据。
4. 对 Bash 课程优先使用 `C:\msys64\usr\bin\bash.exe`、Git for Windows 的 `bin\bash.exe` 或已安装的 WSL；不得把 Windows 商店别名当成可执行 Bash。

## 工具安装后的最小复核矩阵

| 原阻塞课号 | 已完成的最低复核 | 通过证据 |
|---|---|---|
| C1 | `gcc --version`；编译并运行带 `-Wall -Wextra` 的 Hello | 编译输出、程序输出、退出码 |
| CPP1 | `g++ --version`；启用 C++17/20 警告编译 | 编译输出、测试和清理记录 |
| GO1 | `go version`；`go test ./...` | 测试输出与模块版本 |
| RS1 | `cargo --version`；`cargo test` | 构建锁文件、测试输出 |
| ASM1 | `objdump --version`；对自有二进制反汇编 | 命令输出、二进制哈希、分析笔记 |
| SH1 | Git Bash/WSL 版本；执行 `set -euo pipefail` 的本地脚本 | shell 输出、解释器路径、清理记录 |

## 证据边界

- `evidence/<课号>/runtime-sample/` 是每课一次受控最小探针目录；它不自动勾选五关或五个 Lab。
- 旧的阻塞结果保留在 `error.txt`，成功重跑后脚本会清理同一课的旧错误文件；历史汇总仍以当天 `results.json` 为准。
- 任何网络、安全、逆向、云资源和端侧实验只对自有资产、隔离环境或明确授权靶场执行；不保存真实凭据和个人数据。
