# 公开网址与完整课堂边界

## 免费公开网址

仓库已经加入 GitHub Pages 工作流：`.github/workflows/pages.yml`。它使用 GitHub 提供的免费 `github.io` 子域名，不需要购买国际域名，也不会改变你正常浏览器的网络设置。

首次启用时，在仓库 **Settings → Pages → Source** 选择 **GitHub Actions**。之后每次推送到 `main`，GitHub 会自动发布根目录。公开地址的形式是：

`https://claudepnlueilwiuqa14-netizen.github.io/learn-computer/`

如果仓库所有者或 GitHub 账号设置不同，实际地址以 Pages 页面显示的 `page_url` 为准。GitHub Pages 的启用需要仓库账号本人在 GitHub 完成一次设置；本地工具不能代替账号权限或验证码。

## 每节课的连续陪学聊天

课堂页现在每节课底部都包含连续聊天框。每条用户消息和助教回复会按课程号分别永久写入本机 `课堂记录/助教聊天/课程号.jsonl`，同时追加到总日志 `课堂记录/助教聊天.jsonl`。回复会读取当前课程、模块、本节概念回答、英语练习、关卡和 Lab 记录，并把最近对话带入下一轮。刷新、换课或重启服务后从 `/api/chat?course_id=课程号` 恢复完整历史；旧版 `课堂记录/助教问题.jsonl` 会在首次读取时自动转换显示，不会丢失。

这是本地上下文助教，不会假装已经连接当前 Codex/ChatGPT 会话。没有云端密钥时也能离线继续学习；如需真正的云端模型，应只在本机后端配置密钥，不能写入 HTML、GitHub 或浏览器。

这是一套可离线工作的本地上下文助教，不会假装已经连接云端模型。要接入真正的云端 AI，需要你自己选择服务商、配置后端密钥、身份验证、费用上限和隐私策略；密钥不能写进公开 GitHub，也不能让浏览器直接暴露。GitHub Pages 版本只展示课程文件和界面，问题提交仍需要本地服务。

## 为什么公开站点不是完整答题平台

当前课堂调用 `serve_classroom.py` 提供的 `/api/courses`、`/api/lesson`、`/api/state`、`/api/answer`、`/api/question`、`/api/questions`、`/api/chat`、`/api/history` 和 `/api/videos`。GitHub Pages 只能托管静态 HTML，不能运行这个 Python 后端。因此公开站点适合查看目录、课程文件和视频入口；提交答案、连续助教聊天、老师队列、永久学习记录和离线模式仍使用本地服务。

完整本地课堂：

```powershell
cd .\学习者记忆库\语音课程
python .\serve_classroom.py --root . --port 8765
```

然后打开 `http://127.0.0.1:8765/学习主页.html`。外网视频需要联网；文字课程、英语资产和已有记录可在本地继续使用。

## 如果以后要公网完整交互

需要单独部署 Python 后端（例如 Render、Railway、Fly.io 或 Cloud Run），并配置持久化磁盘/数据库、HTTPS、身份验证、备份和隐私策略。免费层可能休眠、限流或要求信用卡验证；在没有账号授权之前，不会替你注册、绑定付款方式或上传个人学习记录。

公开仓库永远不应包含：`课堂记录/`、`答辩/`、`当前状态.local.json`、Cookie、密码、API Key、真实语音或个人证据。
