# 方向2 · 算法与数据结构（从零基础到宗师 · 极深实战版）

> **定位**：算法是「程序的肌肉」。无论你以后写 Web、做安全、搞 AI，底层都在和「怎么把数据存好、怎么最快找到它、怎么在最坏情况下也不崩」打交道。本方向从「数组是什么」讲起，一路打到「能手写并证明红黑树、网络流，对陌生题 30 分钟内给出最优复杂度解法」的宗师水平。
> **本机工具**：Python 3.13（已装，建议 `python --version` 确认）、VS Code、Git Bash。所有 Lab 都能在你机器上直接跑。
> **闯关框架**：每节末尾有 🟢Lv1→🔵Lv2→🟣Lv3→🟠Lv4→🔴Lv5 五关，难度只加不跳；卡住就停那关，下次从那关续。

---

## DSA1 · 复杂度分析（大 O / 均摊 / 主定理）

**🎯 目标**：学完你能给任意一段代码估算"它要跑多久、占多少内存"，并知道怎么选更快的写法。

**📋 小白前置**：PRE5（第一次让电脑听话）、PY5（循环）。

**🟢 部分一·最浅层**：想象你收拾房间。一种方法是把所有东西摊地上再一件件放好（慢但稳），另一种是预先分好类直接放（快）。复杂度就是"收拾房间要花多少时间"的数学表达。我们不关心 1 分钟还是 2 分钟的差异，只关心"房间变大 10 倍时，时间变成几倍"——这叫**渐进复杂度**。

**🟡 部分二·动手层**：打开 VS Code，新建 `dsa1_bigo.py`，复制下面代码跑一下，观察 n 翻倍时耗时怎么变：
```python
import time
def f1(n):        # O(n)
    return sum(range(n))
def f2(n):        # O(n^2)
    return sum(i*j for i in range(n) for j in range(n))
for n in [1000, 2000, 4000]:
    t = time.perf_counter(); f1(n); print("f1", n, time.perf_counter()-t)
    t = time.perf_counter(); f2(n); print("f2", n, time.perf_counter()-t)
```
你会发现 f1 翻倍时间≈翻倍，f2 翻倍时间≈翻 4 倍。这就是线性 vs 平方。

**🔵 部分三·原理层**：`O(f(n))` 表示"当 n 足够大，运行时间 ≤ c·f(n)"。常数 c 和低位项被丢掉，因为 n→∞ 时它们不重要。常见量级：O(1) < O(log n) < O(n) < O(n log n) < O(n²) < O(2ⁿ)。空间复杂度同理，算的是额外内存。

**🟣 部分四·深挖层**：为什么丢常数？因为不同机器常数不同，渐进分析只比"增长趋势"。但工程上常数也重要（n 小的时候）。**均摊复杂度**：一个操作有时 O(n) 有时 O(1)，平均下来 O(1)（如动态数组扩容：每次翻倍，均摊 O(1) 插入）。**主定理**用来解分治递归 T(n)=aT(n/b)+f(n) 的复杂度，是后面排序/树算法的速算工具。

**🔴 部分五·顶级视角**：顶级工程师看复杂度不是背结论，而是能现场推导。面试/竞赛核心是"找瓶颈操作 + 用对数据结构降维"。例如用哈希表把 O(n²) 降到 O(n)，用堆把 O(n log n) 维持动态最值。

**🟠 部分六·安全/合规种子**：复杂度分析能帮你识别**拒绝服务（DoS）风险**——一段 O(2ⁿ) 的代码被攻击者用大输入就能拖垮服务。写后端时，对用户输入驱动的循环/递归必须评估最坏复杂度。

**✅ 验收**：提交 `dsa1_bigo.py` 运行截图 + 一段文字解释"为什么 f2 是 O(n²)"。

**⚠️ 常见坑**：把 O(n log n) 记成 O(log n)；忽略递归的隐藏复杂度；以为"代码短=快"（递归可能更慢）。

**➡️ 下一步**：DSA2 数组（连续内存与缓存友好）。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 dsa1_bigo.py，记录 3 组 n 的耗时比。🥉
- 🔵 Lv2：把 f2 改成 `sum(i for i in range(n))`（O(n)），重新测，解释差异。🥈
- 🟣 Lv3：写一个 O(n³) 的三重循环函数，测 n=500/1000，画出"时间随 n 增长"的直觉。🥇
- 🟠 Lv4：手写 `amortized_push` 动态数组（初始容量 1，满则翻倍），用计数器证明均摊 O(1)。🏆
- 🔴 Lv5：用主定理推导归并排序 T(n)=2T(n/2)+O(n) 的复杂度；写一篇笔记说明"何时主定理失效需Akra-Bazzi"。👑

---

## DSA2 · 数组（连续内存 / 缓存友好）

**🎯 目标**：理解数组为什么"随机访问快、插入慢"，能手写动态数组并解释缓存局部性。

**📋 小白前置**：DSA1、PY7（列表）。

**🟢 部分一·最浅层**：数组像一排带编号的储物柜，柜子 1 号、2 号紧挨着。你想拿第 5 个东西，直接走过去（O(1)）；但要在中间塞一个新东西，后面所有东西都得往后挪（O(n)）。

**🟡 部分二·动手层**：`arr.py`：
```python
a = [10,20,30,40]
print(a[2])          # O(1) 随机访问
a.insert(1, 99)      # O(n) 中间插入，后面整体后移
print(a)
```
用 `python -m timeit -s "a=list(range(10000))" "a.insert(0,1)"` 看头部插入多慢；再试 `a.append(1)` 对比。

**🔵 部分三·原理层**：数组在内存里是**连续**的一块。CPU 预取器会顺着地址把后面几个元素一起读进缓存（缓存行 64 字节），所以顺序遍历飞快；链表跳跃访问则会缓存未命中。这就是为什么"同样 O(n)，数组比链表实际快数倍"。

**🟣 部分四·深挖层**：静态数组（编译期定长）vs 动态数组（Python list / C++ vector，满则 realloc 翻倍）。扩容涉及"分配新块+拷贝+释放旧块"，但因均摊分析，平均插入仍是 O(1)。二维数组有"行主序"（C/Python）与"列主序"（Fortran），影响遍历性能。

**🔴 部分五·顶级视角**：高性能计算里，把"对列循环"改成"对行循环"能让矩阵运算快 10 倍，纯粹因为缓存局部性。顶级工程师写热路径会刻意保证内存连续（struct of arrays 而非 array of structs）。

**🟠 部分六·安全种子**：数组越界是 C/C++ 最常见漏洞来源（缓冲区溢出）。Python 帮你挡了，但理解边界能帮你写对索引、避免 off-by-one；安全方向后续会讲攻击者如何利用越界读内存。

**✅ 验收**：写 `my_array.py` 实现 `get/set/append/insert`，打印每次扩容的容量变化。

**⚠️ 常见坑**：混淆"数组长度"和"容量"；以为 Python list 是链表（它是动态数组）；负数索引（Python 支持，但易误用）。

**➡️ 下一步**：DSA3 链表。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 arr.py 与 timeit 对比。🥉
- 🔵 Lv2：用 list 实现"删除所有等于 x 的元素"并测 O(n) 写法。🥈
- 🟣 Lv3：把二维列表按"行主序 vs 列主序"两种遍历计时，解释差异。🥇
- 🟠 Lv4：从零实现动态数组类（capacity/resize），演示容量 1→2→4→8 翻倍。🏆
- 🔴 Lv5：用 C（MSYS2 gcc）写一段数组遍历，用 `perf` 或计时对比行/列遍历，写性能报告。👑

---

## DSA3 · 链表（指针 / 哨兵 / 快慢指针）

**🎯 目标**：理解链表"插入快、访问慢"的取舍，能手写单/双链表与经典题（反转、环检测）。

**📋 小白前置**：DSA2、PY6（函数）、C3（指针概念）。

**🟢 部分一·最浅层**：链表像寻宝游戏——每个盒子写着"下一个盒子在 3 号柜"，你顺着线索走。想插一个新盒子？改一下"下一个"指向就行，不用挪动其他。但想找第 100 个盒子，得从头数起。

**🟡 部分二·动手层**：`ll.py`：
```python
class Node:
    def __init__(self, v, nxt=None): self.v, self.nxt = v, nxt
head = Node(1, Node(2, Node(3)))
# 反转
prev = None; cur = head
while cur:
    nxt = cur.nxt; cur.nxt = prev; prev, cur = cur, nxt
print(prev.v, prev.nxt.v)   # 3 2
```

**🔵 部分三·原理层**：节点在内存里**不连续**，靠指针串起来。插入/删除只改指针 O(1)（已知位置）；但随机访问 O(n)。双链表多一个 `prev` 指针，支持 O(1) 删除自身。

**🟣 部分四·深挖层**：**哨兵节点**（dummy head）消除头插特判；**快慢指针**（fast 走 2 步）用于找中点、判环（Floyd 龟兔）；**LRU 缓存**用哈希表+双链表 O(1) 实现。

**🔴 部分五·顶级视角**：链表是很多高级结构的积木——内存分配器用空闲链表、内核用链表挂进程、Redis 用 ziplist 紧凑存储。理解指针操作能让你读任何 C 项目不慌。

**🟠 部分六·安全种子**：C 里链表节点 `free` 后没把指针置 NULL 会产生**悬垂指针/ Use-After-Free**（安全方向核心漏洞）。写 C 链表时必须"释放即置空"。

**✅ 验收**：提交手写单链表（含 `reverse`、`detect_cycle`、`merge_two_sorted`）代码与测试。

**⚠️ 常见坑**：反转链表时丢失 `nxt` 引用导致断链；忘了 `cur is None` 的终止条件；双链表删节点忘改 `prev.nxt`。

**➡️ 下一步**：DSA4 栈/队列。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通反转代码，打印反转后序列。🥉
- 🔵 Lv2：实现 `insert_at(head, idx, val)` 并用哨兵简化。🥈
- 🟣 Lv3：用快慢指针找链表中点 + 用 Floyd 检测环并定位环入口。🥇
- 🟠 Lv4：用哈希表+双链表手写 LRUCache（get/put 均 O(1)）。🏆
- 🔴 Lv5：用 MSYS2 的 C 写双向链表，演示 UAF（free 后读）并说明缓解（free 即置 NULL + 地址 sanitizer 编译选项）。👑

---

## DSA4 · 栈 / 队列（单调栈 / 双端队列）

**🎯 目标**：掌握 LIFO/FIFO 本质，能手写栈与队列，并会用单调栈解"下一个更大元素"类题。

**📋 小白前置**：DSA2、DSA3。

**🟢 部分一·最浅层**：栈像一摞盘子——只能从顶上放、从顶上取（后进先出）。队列像排队买票——先来先服务（先进先出）。

**🟡 部分二·动手层**：`sq.py`：
```python
stack = []; stack.append(1); stack.append(2); print(stack.pop())  # 2
from collections import deque
q = deque(); q.append(1); q.append(2); print(q.popleft())          # 1
```

**🔵 部分三·原理层**：栈基于数组（栈顶指针），push/pop O(1)；队列用数组+头尾指针或链表。Python `list.pop()` 尾端 O(1)，但 `pop(0)` O(n)——所以用 `deque`。

**🟣 部分四·深挖层**：**单调栈**维护递增/递减，O(n) 解决"每日温度/下一个更大元素"；**单调队列**维护滑动窗口最大值（队首即最值）；**双端队列**支持两端 O(1)。

**🔴 部分五·顶级视角**：函数调用栈、撤销操作（undo）、浏览器前进后退、表达式求值（调度场算法）全靠栈。操作系统线程栈、递归本质都是栈。

**🟠 部分六·安全种子**：栈溢出是经典漏洞（覆盖返回地址）。理解栈帧布局（DSA 后续 ASM 会讲）是逆向/漏洞利用的基石；写递归要警惕栈深度爆炸（爆栈）。

**✅ 验收**：用栈实现"括号匹配校验"；用单调栈实现"下一个更大元素"。

**⚠️ 常见坑**：用 list 当队列导致 O(n) popleft；递归转栈时忘压入返回地址；单调栈边界（空栈处理）。

**➡️ 下一步**：DSA5 哈希表。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 sq.py，验证栈/队列行为。🥉
- 🔵 Lv2：栈实现括号匹配 `isValid("([)]")→False`。🥈
- 🟣 Lv3：单调栈解"每日温度"（返回等几天升温）。🥇
- 🟠 Lv4：手写循环数组队列（固定容量，head/tail 指针，满/空判别）。🏆
- 🔴 Lv5：用栈把"中缀表达式"转后缀并求值（调度场算法），处理优先级与括号。👑

---

## DSA5 · 哈希表（开放寻址 / 拉链 / 哈希函数）

**🎯 目标**：理解"为什么哈希表平均 O(1)"，能手写哈希表并解释冲突处理与扩容。

**📋 小白前置**：DSA2、DSA4。

**🟢 部分一·最浅层**：哈希表像按名字首字母归档的文件柜——"张三"放 Z 格。理想情况下你一步走到 Z 格拿到文件，不用翻全部。

**🟡 部分二·动手层**：`hash_demo.py`：
```python
d = {}
d["apple"] = 5; d["banana"] = 3
print(d["apple"])   # O(1) 平均
print(hash("apple"))  # Python 哈希值（随机化加盐）
```

**🔵 部分三·原理层**：哈希函数把 key 映射到桶下标。冲突不可避免（鸽巢原理），两种解法：**拉链法**（每桶挂链表）和**开放寻址**（线性/二次探测找下一个空桶）。负载因子（元素数/桶数）超阈值就 rehash 扩容。

**🟣 部分四·深挖层**：好的哈希函数要"均匀+快+可复现"（加密哈希另算）。Python dict 用开放寻址 + 伪随机探测；攻击者可构造**哈希碰撞 DoS**（安全方向会讲）。一致性哈希用于分布式分片。

**🔴 部分五·顶级视角**：哈希是计算机最基础的思想之一——从数据库索引、缓存、去重、集合判重到区块链 Merkle 树、密码学，处处是哈希。顶级工程师能根据场景选哈希（速度 vs 抗碰撞）。

**🟠 部分六·安全种子**：① 哈希碰撞 DoS：攻击者发大量同桶 key 让服务退化 O(n²)。② 密码绝不能用普通哈希（要用 argon2/bcrypt，见方向7）。③ 字典键若可变（list）会破坏哈希不变性。

**✅ 验收**：手写 `MyHash` 类（拉链法），支持 `put/get`，演示冲突时链表增长。

**⚠️ 常见坑**：把 key 设为可变对象；忽略负载因子导致退化；以为哈希表永远 O(1)（最坏 O(n)）。

**➡️ 下一步**：DSA6 二叉树/BST。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 hash_demo，观察 hash 值。🥉
- 🔵 Lv2：用 dict 统计一段文本词频。🥈
- 🟣 Lv3：手写拉链法哈希表，插入 1000 个 key 统计最大桶长。🥇
- 🟠 Lv4：实现开放寻址（线性探测）+ rehash，对比拉链法性能。🏆
- 🔴 Lv5：实现一致性哈希（带虚拟节点），演示节点增减时仅少量 key 迁移，写报告。👑

---

## DSA6 · 二叉树 / BST（遍历 / 平衡）

**🎯 目标**：理解树结构，能手写二叉搜索树与三种遍历，能识别 BST 性质。

**📋 小白前置**：DSA3、DSA5。

**🟢 部分一·最浅层**：树像公司组织架构——老板在顶，下属往下分。二叉树每个节点最多俩下属（左/右）。BST 是"左边的人都比老板小、右边都比老板大"的排序架构。

**🟡 部分二·动手层**：`bst.py`：
```python
class T:
    def __init__(s,v,l=None,r=None): s.v,v,l,r=v,l,r
def inorder(n):
    if n: inorder(n.l); print(n.v,end=' '); inorder(n.r)
root = T(5, T(3,T(1),T(4)), T(8))
inorder(root)   # 1 3 4 5 8  （中序=升序）
```

**🔵 部分三·原理层**：三种遍历——**前序**（根左右，用于拷贝树）、**中序**（左根右，BST 中序=排序）、**后序**（左右根，用于释放/求值）。查找 BST 平均 O(log n)，但退化成链时 O(n)。

**🟣 部分四·深挖层**：BST 旋转（左旋/右旋）是后续 AVL/红黑的基础；树的递归本质是"子问题分解"。层序遍历用队列（BFS）。

**🔴 部分五·顶级视角**：文件系统目录、DOM、语法树、决策树、数据库 B+ 树都是树的变体。理解树让你能建模一切"层次+分支"问题。

**🟠 部分六·安全种子**：不可信输入构造的树若未平衡会被攻击者退化成链表造成 DoS；解析 XML/JSON 的深层嵌套可能栈溢出（用迭代而非递归遍历防爆栈）。

**✅ 验收**：手写 BST 的 `insert/search/inorder`，并验证中序升序。

**⚠️ 常见坑**：递归遍历忘判 None；混淆三种遍历顺序；BST 删除节点（两子节点）逻辑错。

**➡️ 下一步**：DSA7 平衡树 AVL/红黑。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 inorder，确认升序输出。🥉
- 🔵 Lv2：实现 `search` 与 `insert`，插入 1..7 观察形状。🥈
- 🟣 Lv3：用队列实现层序遍历（BFS）并分层打印。🥇
- 🟠 Lv4：手写迭代版（非递归）前/中/后序遍历（用栈）。🏆
- 🔴 Lv5：实现 BST 删除（处理 0/1/2 子节点，2 子用后继替换），写测试覆盖所有 case。👑

---

## DSA7 · 平衡树 AVL / 红黑（旋转 / 黑高 / 工程取舍）

**🎯 目标**：理解"为什么需要自平衡"，能手写 AVL 插入+旋转，能讲清红黑树设计哲学。

**📋 小白前置**：DSA6。

**🟢 部分一·最浅层**：普通 BST 像没整理的货架，东西一多就歪成一条线。平衡树像定期整理的货架——保证左边右边高度差不大，找东西始终快。

**🟡 部分二·动手层**：`avl.py` 先写节点高度与平衡因子：
```python
def h(n): return n.h if n else 0
def bf(n): return h(n.l)-h(n.r)
def rotate_right(y):
    x=y.l; y.l=x.r; x.r=y; y.h=1+max(h(y.l),h(y.r)); x.h=1+max(h(x.l),h(x.r)); return x
```
插入后自底向上更新高度并在 |bf|>1 时旋转。

**🔵 部分三·原理层**：AVL 要求任意节点左右子树高差 ≤1，保证查找严格 O(log n)，但插入/删除旋转多。红黑树放宽条件（红/黑节点 + 5 条性质），近似平衡，插入删除旋转少，是 C++ map / Linux 的 select 所用。

**🟣 部分四·深挖层**：四种失衡 LL/LR/RL/RR 对应旋转组合；红黑树"黑高"保证最长路径 ≤2×最短；工程上红黑因更少旋转更常用，AVL 因更严格平衡利于查找密集场景。

**🔴 部分五·顶级视角**：数据库用 B+ 树（DSA9）而非二叉平衡树，因为磁盘按页读、要矮胖；内存里用红黑/AVL。选结构看"访问介质 + 操作比例"。

**🟠 部分六·安全种子**：自平衡树若实现有 bug（旋转后高度错）会导致死循环/崩溃；解析不可信数据时限制树深，防止恶意深嵌套。

**✅ 验收**：手写 AVL `insert` 并验证插入 1..10 后根高度与中序升序正确。

**⚠️ 常见坑**：旋转后忘更新高度；LR/RR 旋转组合记混；红黑性质 5 条记不全导致插入修复错。

**➡️ 下一步**：DSA8 堆/优先队列。

**⚔️ 闯关路线**
- 🟢 Lv1：实现 rotate_right 并单测。🥉
- 🔵 Lv2：AVL insert 处理 LL/RR 两种。🥈
- 🟣 Lv3：补全 LR/RL，插入随机序列验证始终平衡。🥇
- 🟠 Lv4：手写红黑树 insert + 修复（不要求删），对比与 AVL 旋转次数。🏆
- 🔴 Lv5：证明"红黑树最长路径 ≤ 2×最短"（用黑高），并写笔记说明为何 Linux 选红黑而非 AVL。👑

---

## DSA8 · 堆 / 优先队列（二叉堆 / 斐波那契堆）

**🎯 目标**：掌握堆的"半有序"特性，能手写二叉堆与堆排序，理解优先队列应用。

**📋 小白前置**：DSA6、DSA4。

**🟢 部分一·最浅层**：堆像叠罗汉——最上面的一定是（最大或最小）那个，但下面顺序不保证。取走最上面的，立刻有人补位。适合"总是先处理最重要的"。

**🟡 部分二·动手层**：`heap.py`：
```python
import heapq
h = []; heapq.heappush(h, 5); heapq.heappush(h, 1); heapq.heappush(h, 3)
print(heapq.heappop(h))   # 1 总是最小
```

**🔵 部分三·原理层**：二叉堆是**数组实现的完全二叉树**，父 i 的子为 2i+1、2i+2。上浮（sift-up）/下沉（sift-down）维护堆性质，push/pop O(log n)，建堆 O(n)。`heapq` 是小顶堆。

**🟣 部分四·深挖层**：**堆排序**=建堆+反复取顶 O(n log n) 原地排序；**Top-K** 用大小为 K 的堆 O(n log K)；**Dijkstra** 用优先队列；斐波那契堆把某些操作降到平摊 O(1)（理论强，工程少用）。

**🔴 部分五·顶级视角**：所有"动态取极值"场景都靠堆——任务调度、带宽分配、中位数维护（双堆）、 merger k 个有序流。顶级工程师看到"实时最值"第一反应是堆。

**🟠 部分六·安全种子**：堆数据结构本身安全，但底层内存堆（heap）是漏洞重灾区（UAF/堆溢出，方向7）；名字撞了别混。

**✅ 验收**：手写 `MyHeap` 实现 push/pop，并用它做 Top-K 问题。

**⚠️ 常见坑**：误以为堆完全有序（只保证堆顶）；手写下沉时左右子比较漏判；大顶堆小顶堆符号错。

**➡️ 下一步**：DSA9 Trie/B/B+树。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 heapq 取最小。🥉
- 🔵 Lv2：手写 MyHeap 的 sift-down。🥈
- 🟣 Lv3：用堆解 Top-K（数组前 K 大）。🥇
- 🟠 Lv4：双堆（最大堆+最小堆）实时维护数据流中位数。🏆
- 🔴 Lv5：实现堆排序并对比 Python 内置 sort 的计时；写"何时堆排优于快排"的分析。👑

---

## DSA9 · Trie / B / B+ 树（前缀 / 磁盘友好索引）

**🎯 目标**：理解 Trie 与 B/B+ 树的适用场景，能讲清"为什么数据库用 B+ 树"。

**📋 小白前置**：DSA6、DSA7、DSA8。

**🟢 部分一·最浅层**：Trie 像字典的部首检索——"app"和"apple"共享前缀 a-p-p。B+ 树像多层目录柜，一层套一层，专门适合"从硬盘一整页一整页地读"。

**🟡 部分二·动手层**：`trie.py` 简单前缀树：
```python
class Trie:
    def __init__(s): s.children={}; s.end=False
    def add(s,w):
        n=s
        for c in w: n=n.children.setdefault(c,Trie())
        n.end=True
    def has(s,w):
        n=s
        for c in w:
            if c not in n.children: return False
            n=n.children[c]
        return n.end
t=Trie(); t.add("apple"); print(t.has("app"), t.has("apple"))  # False True
```

**🔵 部分三·原理层**：Trie 每个节点是字符分支，前缀查询 O(长度)；B 树是"多路平衡查找树"，一个节点存多个 key + 子树指针，树矮；**B+ 树**把数据只放叶子、叶子间用链表串，范围查询极快。

**🟣 部分四·深挖层**：数据库/文件系统用 B+ 树因为：① 节点大小对齐磁盘块（4KB），一次 IO 读一整节点；② 矮胖树减少 IO 次数（IO 比 CPU 慢百万倍）；③ 叶子链表让 `WHERE id BETWEEN a AND b` 顺序扫。

**🔴 部分五·顶级视角**：MySQL InnoDB 聚簇索引就是 B+ 树，索引选择、回表、覆盖索引全源于此（方向5 数据库会深讲）。理解 B+ 树 = 理解数据库一半性能。

**🟠 部分六·安全种子**：索引虽好，但用户输入拼进 SQL 会注入（方向7）；Trie 若无限增长（如恶意外键）可能内存耗尽，需设深度上限。

**✅ 验收**：手写 Trie 的 add/has/startsWith，演示前缀共享。

**⚠️ 常见坑**：B 树和 B+ 树混淆（B+ 数据只在叶子）；Trie 内存爆炸（长串）；忘记叶子链表对范围查询的意义。

**➡️ 下一步**：DSA10 图基础。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 Trie 前缀查询。🥉
- 🔵 Lv2：Trie 实现"返回所有以 prefix 开头的词"。🥈
- 🟣 Lv3：用 Trie + DFS 实现"单词接龙"最短路径。🥇
- 🟠 Lv4：手写 B+ 树叶子节点插入与链表串接（简化版，不要求分裂全实现）。🏆
- 🔴 Lv5：写笔记对比"哈希索引 vs B+树索引"在等值/范围/排序查询的取舍，附 IO 次数估算。👑

---

## DSA10 · 图基础（邻接表 / 矩阵 / 表示）

**🎯 目标**：建立图的建模思维，能手写两种存储并分析时空。

**📋 小白前置**：DSA3、DSA5。

**🟢 部分一·最浅层**：图就像微信群——人是点，聊天关系是线。有的群单向@（有向图），有的双向好友（无向图）。怎么把"群关系"存进电脑？

**🟡 部分二·动手层**：`graph.py`：
```python
# 邻接表（省空间，稀疏图首选）
adj = {0:[1,2], 1:[2], 2:[]}
# 邻接矩阵（查边 O(1)，稠密图）
n=3; mat=[[0]*n for _ in range(n)]; mat[0][1]=1; mat[0][2]=1
```

**🔵 部分三·原理层**：邻接表用"每个点的邻居列表"，空间 O(V+E)，遍历边快；邻接矩阵空间 O(V²)，查"两点是否相连"O(1)，适合稠密图。还有**边列表**、**链式前向星**（竞赛常用）。

**🟣 部分四·深挖层**：图可分无向/有向/带权/负权；连通分量、强连通分量；表示方式影响后续算法复杂度（Dijkstra 在邻接表用堆 O((V+E)logV)，矩阵则 O(V²)）。

**🔴 部分五·顶级视角**：社交网络、依赖关系、地图导航、编译依赖、知识图谱全是图。顶级工程师看到"关系网"就建模成图，再选遍历/最短路/匹配算法。

**🟠 部分六·安全种子**：依赖图被投毒（供应链攻击）会在你不知情时引入恶意包；权限图（谁能访问什么）用图建模做最小权限分析。

**✅ 验收**：给定边列表，分别用邻接表与矩阵建图，并查询"0 是否连 2"。

**⚠️ 常见坑**：有向图忘建反向边；邻接矩阵对称当无向；大规模稀疏图用矩阵爆内存。

**➡️ 下一步**：DSA11 图遍历 BFS/DFS。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通两种表示，查询边。🥉
- 🔵 Lv2：把"边列表"转成两种结构，封装成类。🥈
- 🟣 Lv3：统计图的有向/无向边数与最大度数。🥇
- 🟠 Lv4：实现链式前向星（数组模拟链表）并对比邻接表内存。🏆
- 🔴 Lv5：用图建模"你的技能依赖关系"，找出学习某方向所需前置链（拓扑思想雏形）。👑

---

## DSA11 · 图遍历 BFS / DFS（连通 / 拓扑）

**🎯 目标**：掌握两种遍历的语义差异，能手写并用于连通分量、拓扑排序。

**📋 小白前置**：DSA10、DSA4。

**🟢 部分一·最浅层**：DFS 像走迷宫——一条路走到黑，撞墙才回头（栈/递归）。BFS 像水波——一圈圈往外扩，先到近的再到远的（队列）。

**🟡 部分二·动手层**：`traverse.py`：
```python
def dfs(u, adj, seen):
    seen.add(u); print(u)
    for v in adj[u]:
        if v not in seen: dfs(v, adj, seen)
def bfs(s, adj):
    seen={s}; q=[s]
    while q:
        u=q.pop(0); print(u)
        for v in adj[u]:
            if v not in seen: seen.add(v); q.append(v)
```

**🔵 部分三·原理层**：DFS 用栈（递归隐式栈），适合探路径/回溯/连通；BFS 用队列，首次访问距离最短，天然求"无权最短路"。两者都 O(V+E)。

**🟣 部分四·深挖层**：**拓扑排序**（DAG 才有）用 DFS 后序逆序或 Kahn 入度法，用于编译依赖、课程先修；**连通分量**用 DFS/BFS 标记；**割点/桥**是进阶（方向7 网络/安全可用）。

**🔴 部分五·顶级视角**：DFS/BFS 是图算法基石，PageRank、社区发现、垃圾回收标记、甚至代码分析都建立在遍历上。

**🟠 部分六·安全种子**：恶意软件依赖图、调用图用遍历做污点分析（方向7 逆向）；遍历不可信图要防递归爆栈（用显式栈）。

**✅ 验收**：手写 DFS+BFS，对示例图输出遍历序；用 Kahn 算法做拓扑排序。

**⚠️ 常见坑**：递归 DFS 忘 `seen` 判重导致死循环（有环图）；BFS 用 list.pop(0) 慢应用 deque；拓扑忽略环检测。

**➡️ 下一步**：DSA12 最短路。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 DFS/BFS 输出。🥉
- 🔵 Lv2：用 BFS 求无权图两点最短距离。🥈
- 🟣 Lv3：Kahn 拓扑排序 + 检测环。🥇
- 🟠 Lv4：手写显式栈 DFS 避免递归（防爆栈），对比结果。🏆
- 🔴 Lv5：在有向图上同时求"强连通分量（Tarjan）"并解释其应用（如依赖聚类）。👑

---

## DSA12 · 最短路 Dijkstra / Bellman-Ford

**🎯 目标**：理解非负权最短路与可处理负权/检测负环的区别，能手写两种算法。

**📋 小白前置**：DSA11、DSA8（堆）。

**🟢 部分一·最浅层**：最短路像查地图——Dijkstra 是"每次先去离起点最近的城市再往外扩"（不能处理有"负距离"的诡异地图）；Bellman-Ford 不怕负距离，还能告诉你"存在绕圈越绕越近"的坑。

**🟡 部分二·动手层**：`dijkstra.py`：
```python
import heapq
def dij(s, adj):
    dist={s:0}; pq=[(0,s)]
    while pq:
        d,u=heapq.heappop(pq)
        if d>dist.get(u,1e18): continue
        for v,w in adj[u]:
            if dist.get(v,1e18)>d+w: dist[v]=d+w; heapq.heappush(pq,(d+w,v))
    return dist
```

**🔵 部分三·原理层**：Dijkstra 贪心+优先队列，每次锁定"当前最近未定点"，要求边权非负（否则已锁定的点可能被更短路径推翻）。Bellman-Ford 松弛所有边 V-1 次，再松弛一次若还能更新说明**负环**。

**🟣 部分四·深挖层**：SPFA（队列优化 BF）常被卡；**A\* 算法**用启发式加速（地图导航用）；Floyd 多源最短路 O(V³)；实际工程（OSPF/IS-IS 路由协议）用 Dijkstra 变种。

**🔴 部分五·顶级视角**：最短路是网络路由、物流调度、依赖延迟分析的核心。顶级工程师理解"权重代表什么"（时延/成本/风险）比算法本身更重要。

**🟠 部分六·安全种子**：路由协议若被伪造 LSA（方向7 网络/渗透）可篡改最短路引导流量；权重计算要防被攻击者操控造成黑洞。

**✅ 验收**：手写 Dijkstra 与 Bellman-Ford，对含负权图验证 BF 检测负环。

**⚠️ 常见坑**：Dijkstra 用负权出错不自知；堆里旧距离未跳过（lazy deletion）；Bellman 忘记负环检测轮。

**➡️ 下一步**：DSA13 最小生成树。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 Dijkstra 求单源最短路。🥉
- 🔵 Lv2：手算小图验证 dist 表。🥈
- 🟣 Lv3：实现 Bellman-Ford + 负环检测。🥇
- 🟠 Lv4：实现 A\*（曼哈顿启发）在网格图寻路。🏆
- 🔴 Lv5：对比 Dijkstra/SPFA/Floyd 在 1000 节点随机图上的耗时，写 benchmark 报告。👑

---

## DSA13 · 最小生成树（Prim / Kruskal）

**🎯 目标**：理解"连起所有点且总代价最小"的 MST，能手写两种算法并联系并查集。

**📋 小白前置**：DSA12、DSA20（并查集）。

**🟢 部分一·最浅层**：MST 像用最少的电线把所有房子连上电网，且任意两户能互通，总电线最短。

**🟡 部分二·动手层**：`mst.py` 用并查集的 Kruskal：
```python
def find(p,x):
    while p[x]!=x: p[x]=p[p[x]]; x=p[x]
    return x
edges=[(1,2,3),(1,3,1),(2,3,2)]  # (u,v,w)
edges.sort(key=lambda e:e[2])
p=list(range(4)); total=0
for u,v,w in edges:
    if find(p,u)!=find(p,v): p[find(p,u)]=find(p,v); total+=w
print(total)
```

**🔵 部分三·原理层**：Kruskal 按边权升序加、用并查集跳过成环的边；Prim 从点出发、用堆不断加"最近未连点"。两者都贪心，复杂度 Kruskal O(E log E)、Prim+O(V log V)（堆）。

**🟣 部分四·深挖层**：MST 性质——任意切分，横跨的最小边必在 MST 中。应用：聚类、网络设计、图像分割（基于图）。次小生成树、斯坦纳树是进阶。

**🔴 部分五·顶级视角**：任何"最小化连接成本且保持连通"的问题都可建模 MST。顶级工程师能识别"看似不像图"的问题其实是 MST。

**🟠 部分六·安全种子**：网络拓扑设计若 MST 过度集中某节点会造成单点故障；安全架构要权衡"最小"与"冗余"。

**✅ 验收**：实现 Kruskal（含并查集路径压缩）与 Prim，验证同图同结果。

**⚠️ 常见坑**：并查集忘路径压缩导致退化；Prim 忘维护"已选集"；边权相等时结果不唯一但代价同。

**➡️ 下一步**：DSA14 排序。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 Kruskal 求总代价。🥉
- 🔵 Lv2：手写并查集 find/union 带路径压缩。🥈
- 🟣 Lv3：实现 Prim（堆版）对比结果。🥇
- 🟠 Lv4：用 MST 做"图像像素图分割"简化版（网格图 + 并查集）。🏆
- 🔴 Lv5：证明 MST 的"切分性质"，并推导 Kruskal 正确性。👑

---

## DSA14 · 排序（快排 / 归并 / 堆排 / 计数）

**🎯 目标**：掌握主流排序的原理与取舍，能手写快排/归并并分析稳定性与复杂度。

**📋 小白前置**：DSA1、DSA8、DSA12。

**🟢 部分一·最浅层**：排序像整理扑克牌——有人喜欢一张张插（插排），有人先分两堆再合（归并），有人随机抽一张当中线分两边（快排）。

**🟡 部分二·动手层**：`sort.py` 写快排：
```python
def qsort(a):
    if len(a)<=1: return a
    p=a[len(a)//2]; return qsort([x for x in a if x<p])+[x for x in a if x==p]+qsort([x for x in a if x>p])
print(qsort([3,1,4,1,5,9,2]))
```

**🔵 部分三·原理层**：快排平均 O(n log n)、原地、但最坏 O(n²)（已排序+固定 pivot）；归并稳定 O(n log n)、需额外空间；堆排 O(n log n) 原地但不稳定；计数/基数排序在整数范围小时 O(n+k) 线性。

**🟣 部分四·深挖层**：稳定性（相等元素相对序保持）影响"先按A排再按B排"的多键排序；工程排序（Timsort，Python 用）是归并+插排混合，利用现实中数据的局部有序。

**🔴 部分五·顶级视角**：排序是算法基石——快排思想用于快速选择（第 K 大）、荷兰国旗问题、三路快排去重；理解 partitioning 让你能写任何"按某条件分两堆"的逻辑。

**🟠 部分六·安全种子**：排序比较函数若被恶意构造（如 Java 早期 compareTo 不一致）可造成 DoS/异常；qsort 的 pivot 选择防"对手输入"退化可随机化。

**✅ 验收**：手写快排（随机 pivot）+ 归并排序，验证排序正确与稳定性差异。

**⚠️ 常见坑**：快排递归基准选错退化；归并忘合并两指针；认为快排永远最快（小数组插排更优）。

**➡️ 下一步**：DSA15 查找/二分。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通快排。🥉
- 🔵 Lv2：快排加随机 pivot + 三路分区。🥈
- 🟣 Lv3：实现归并并证明稳定。🥇
- 🟠 Lv4：写"第 K 大元素"用快速选择 O(n) 平均。🏆
- 🔴 Lv5：实现 Timsort 简化版（run 检测+归并），对比内置 sort 计时，写报告。👑

---

## DSA15 · 查找 / 二分（边界 / 旋转数组）

**🎯 目标**：吃透二分查找的边界处理，能手写多种变体。

**📋 小白前置**：DSA14、DSA1。

**🟢 部分一·最浅层**：二分像翻字典——每次翻到中间，看页码太大就往前、太小就往后，几下就找到。但"翻到哪一页停"最容易写错。

**🟡 部分二·动手层**：`binary.py`：
```python
def bs(a, x):
    l, r = 0, len(a)-1
    while l<=r:
        m=(l+r)//2
        if a[m]==x: return m
        elif a[m]<x: l=m+1
        else: r=m-1
    return -1
```

**🔵 部分三·原理层**：二分要求"有序 + 每次能砍掉一半"。易错在边界：`l<=r` vs `l<r`、mid 更新、返回左/右边界。变体：找第一个≥x、最后一个≤x（用 `l` 或 `r` 的落点）。

**🟣 部分四·深挖层**：**旋转数组查找**（先判哪半有序）；**二分答案**（把"最大值最小"类问题转成判定）；浮点二分（精度控制）。复杂度 O(log n)。

**🔴 部分五·顶级视角**：二分是"在单调函数上找临界点"的通用范式——不只是数组，还能用于"能否在 D 天内运完"（判定+二分答案）。

**🟠 部分六·安全种子**：二分依赖数据可信有序，若输入被篡改可能越界；解析不可信数据先做校验。

**✅ 验收**：手写标准二分 + "找第一个≥x" + 旋转数组查找。

**⚠️ 常见坑**：死循环（mid 不推进）；边界返回错；整数溢出 `(l+r)//2` Python 无碍但 C 要 `l+(r-l)/2`。

**➡️ 下一步**：DSA16 递归/分治。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通标准二分。🥉
- 🔵 Lv2：实现"找第一个≥x 的位置"。🥈
- 🟣 Lv3：旋转数组（无重复）中查找 target。🥇
- 🟠 Lv4：二分答案解"使最大子数组和 ≤ x 的最少分段数"。🏆
- 🔴 Lv5：用主定理推导"在 n×n 矩阵每行有序、每列有序中查找"的 O(n) 算法并证明下界。👑

---

## DSA16 · 递归 / 分治（主定理 / 栈深度）

**🎯 目标**：建立递归思维，能手写分治算法并用主定理分析。

**📋 小白前置**：DSA14、DSA1。

**🟢 部分一·最浅层**：递归像俄罗斯套娃——解决大问题的方法是"先解决小一号的，再拼起来"。分治就是"把大问题切成几块小问题分别解决"。

**🟡 部分二·动手层**：`merge_sort` 就是分治：
```python
def ms(a):
    if len(a)<=1: return a
    m=len(a)//2; L,R=ms(a[:m]),ms(a[m:])
    i=j=0; out=[]
    while i<len(L) and j<len(R):
        out.append(L[i] if L[i]<R[j] else R.pop(0) if False else R[j]); 
        i+= (L[i]<R[j]); j+= (L[i]>=R[j])
    return out+L[i:]+R[j:]
```
（上面为示意，实际用双指针更清晰）

**🔵 部分三·原理层**：递归=函数调自己，靠调用栈保存现场。分治三步骤：分解→解决→合并。主定理 T(n)=aT(n/b)+f(n) 速算：比较 f(n) 与 n^log_b(a)。

**🟣 部分四·深挖层**：递归树可视化帮助理解；**尾递归**可被优化（Python 不优化，Scheme/函数式语言优化）；递归改迭代用显式栈防爆栈；记忆化（DSA18）消除重复子问题。

**🔴 部分五·顶级视角**：分治是"divide & conquer"——归并排序、快速排序、FFT、Strassen 矩阵乘、最近点对全是其产物。顶级工程师看大问题先想"能不能切小"。

**🟠 部分六·安全种子**：深度递归处理不可信输入（超深 XML/目录）会栈溢出 DoS；用迭代或设深度上限。

**✅ 验收**：手写正确的归并排序 + 用主定理分析其复杂度。

**⚠️ 常见坑**：递归终止条件错；误以为 Python 会尾递归优化；重复子问题不记忆化导致指数爆炸（如裸斐波那契）。

**➡️ 下一步**：DSA17 回溯。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通归并排序（双指针正确版）。🥉
- 🔵 Lv2：用主定理分析快排/归并/二分查找。🥈
- 🟣 Lv3：递归实现"汉诺塔"并打印步骤。🥇
- 🟠 Lv4：把递归 DFS 改成显式栈迭代版（防爆栈）。🏆
- 🔴 Lv5：实现 Strassen 矩阵乘法（分治）并对比朴素 O(n³) 的实测交叉点。👑

---

## DSA17 · 回溯（状态空间 / 剪枝）

**🎯 目标**：掌握"试错+撤销"的搜索范式，能手写 N 皇后/子集/排列并加剪枝。

**📋 小白前置**：DSA16、DSA11（DFS）。

**🟢 部分一·最浅层**：回溯像走迷宫时每到一个岔口都试试，走不通就原路退回换条路。电脑用"递归进、不符就出"实现。

**🟡 部分二·动手层**：`nqueens.py`：
```python
def solve(n):
    cols=set(); diag1=set(); diag2=set(); res=[]
    def dfs(r, path):
        if r==n: res.append(path[:]); return
        for c in range(n):
            if c in cols or r-c in diag1 or r+c in diag2: continue
            cols.add(c); diag1.add(r-c); diag2.add(r+c); path.append(c)
            dfs(r+1, path)
            path.pop(); cols.discard(c); diag1.discard(r-c); diag2.discard(r+c)
    dfs(0,[]); return res
print(len(solve(8)))   # 92
```

**🔵 部分三·原理层**：回溯=带"约束检查+撤销"的 DFS。关键三动作：选（加入状态）、查（合法？）、撤（pop）。剪枝是提前排除不可能分支，大幅降搜索量。

**🟣 部分四·深挖层**：排列/组合/子集生成都是回溯；数独、单词搜索、括号生成同理；**启发式排序分支**（先试约束多的）能加速；与 DP 关系：回溯暴力，DP 记忆化去重。

**🔴 部分五·顶级视角**：回溯是"搜索问题"的通用框架——约束满足（CSP）、规划、编译器寄存器分配都用。顶级工程师懂得"何时该剪枝、何时该换算法"。

**🟠 部分六·安全种子**：不可信输入的 combinatorial 爆炸可被用于算力 DoS；设分支上限/超时。

**✅ 验收**：手写 N 皇后 + 全排列，验证数量正确。

**⚠️ 常见坑**：忘撤销（pop/discard）导致状态污染；重复解（如含相同元素时）；不设终止/剪枝导致超时。

**➡️ 下一步**：DSA18 动态规划。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 N 皇后求 8 皇后解数。🥉
- 🔵 Lv2：回溯生成"所有合法括号组合（n 对）"。🥈
- 🟣 Lv3：单词搜索（矩阵中找单词路径）。🥇
- 🟠 Lv4：数独求解器（回溯+唯一候选剪枝）。🏆
- 🔴 Lv5：实现带"冲突指向"的约束传播（类 AC-3）加速数独，对比纯回溯速度。👑

---

## DSA18 · 动态规划（状态定义 / 转移 / 优化）

**🎯 目标**：突破"看着像但写不出"的 DP 恐惧，能手写经典 DP 并做空间优化。

**📋 小白前置**：DSA17、DSA16。

**🟢 部分一·最浅层**：DP 像填表格——每个格子的值靠"左边/上边"几个已填好的格子算出来。先解决小问题，把答案记下来，大问题直接拼。

**🟡 部分二·动手层**：`dp.py` 0/1 背包：
```python
def knapsack(w, v, cap):
    dp=[0]*(cap+1)
    for i in range(len(w)):
        for c in range(cap, w[i]-1, -1):   # 逆序防重复使用
            dp[c]=max(dp[c], dp[c-w[i]]+v[i])
    return dp[cap]
print(knapsack([2,3,4],[3,4,5],5))   # 7
```

**🔵 部分三·原理层**：DP=把原问题拆成**重叠子问题**，用表存中间结果（记忆化/递推）。三要素：① 状态定义 ② 转移方程 ③ 边界。背包逆序遍历是"每个物品只用一次"的关键。

**🟣 部分四·深挖层**：常见模型——线性（LIS）、区间（石子合并）、树形（树上 DP）、状压（集合状态）、数位 DP。空间优化：滚动数组、一维化。与回溯关系：DP 是"记忆化+有序化"的回溯。

**🔴 部分五·顶级视角**：DP 是面试与竞赛分水岭。顶级工程师看到"最值/方案数/可行性 + 子结构"就条件反射想 DP。进阶：斜率优化、四边形不等式、DP 套 DP。

**🟠 部分六·安全种子**：DP 表若用不可信大输入可能导致 O(n²) 内存爆炸；设上限/流式处理。

**✅ 验收**：手写 0/1 背包 + 最长递增子序列（LIS），验证正确性。

**⚠️ 常见坑**：状态定义混乱；转移方程漏边界；背包正序变完全背包；空间优化改错遍历方向。

**➡️ 下一步**：DSA19 贪心。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 0/1 背包。🥉
- 🔵 Lv2：LIS 的 O(n²) 与 O(n log n) 两版。🥈
- 🟣 Lv3：编辑距离（两字符串最少操作）。🥇
- 🟠 Lv4：股票买卖系列（多状态 DP）统一框架。🏆
- 🔴 Lv5：用"交换论证"严格证明某贪心题的 DP 解等于贪心解，写证明笔记。👑

---

## DSA19 · 贪心（证明 / 交换论证）

**🎯 目标**：理解贪心"局部最优→全局最优"的适用条件，能手写并会证明。

**📋 小白前置**：DSA18、DSA13。

**🟢 部分一·最浅层**：贪心像每次都拿眼前最划算的——有时候全局刚好也最优（如零钱用大面额先凑），有时候会翻车（如某些路径问题）。

**🟡 部分二·动手层**：`greedy.py` 区间调度（最多不重叠会议）：
```python
def max_meetings(intervals):
    intervals.sort(key=lambda x:x[1])   # 按结束时间早
    end=0; cnt=0
    for s,e in intervals:
        if s>=end: cnt+=1; end=e
    return cnt
```

**🔵 部分三·原理层**：贪心正确需满足"贪心选择性质 + 最优子结构"。证明常用**交换论证**：假设最优解，把其中一步换成贪心选，不劣化，从而最优解可全换成贪心。

**🟣 部分四·深挖层**：哈夫曼编码（最优前缀码）、最小生成树（DSA13）、Dijkstra（DSA12）本质都是贪心。但很多看似贪心其实错的题（如 0/1 背包）必须 DP。

**🔴 部分五·顶级视角**：顶级工程师先用贪心试（快、简单），证明不了再上 DP。能识别"这题贪心对不对"是核心判断力。

**🟠 部分六·安全种子**：资源调度贪心若被攻击者喂"结束时间都被篡改"的恶意输入可能失衡，需校验。

**✅ 验收**：手写区间调度 + 哈夫曼编码，并用交换论证说明区间调度正确性。

**⚠️ 常见坑**：盲目贪心不证明；把 0/1 背包当贪心（按性价比错）；忽略"最优子结构"前提。

**➡️ 下一步**：DSA20 并查集。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通区间调度。🥉
- 🔵 Lv2：实现哈夫曼树并输出编码。🥈
- 🟣 Lv3：用交换论证写一段区间调度的正确性说明。🥇
- 🟠 Lv4：分数背包（可拆）用贪心证明最优；对比 0/1 背包为何不行。🏆
- 🔴 Lv5：实现"任务调度最小化最大延迟"（Hodgson 算法）并证明。👑

---

## DSA20 · 并查集（路径压缩 / 按秩合并）

**🎯 目标**：掌握并查集，能手写带优化的版本，理解其近乎常数的均摊复杂度。

**📋 小白前置**：DSA19、DSA3。

**🟢 部分一·最浅层**：并查集像"找大家长"——每个圈子有个代表，问两个人是不是一伙的，就看他们的大家长是否相同。合并两伙就让一个大家长认另一个当老大。

**🟡 部分二·动手层**：`uf.py`：
```python
class UF:
    def __init__(s,n): s.p=list(range(n)); s.r=[0]*n
    def find(s,x):
        while s.p[x]!=x: s.p[x]=s.p[s.p[x]]; x=s.p[x]
        return x
    def uni(s,a,b):
        a,b=s.find(a),s.find(b)
        if a==b: return False
        if s.r[a]<s.r[b]: a,b=b,a
        s.p[b]=a
        if s.r[a]==s.r[b]: s.r[a]+=1
        return True
```

**🔵 部分三·原理层**：`find` 用**路径压缩**（顺路把节点直接挂根），`uni` 用**按秩合并**（矮树挂高树），两者结合均摊近乎 O(α(n))（反阿克曼，几乎常数）。

**🟣 部分四·深挖层**：应用——Kruskal MST（DSA13）、连通分量、判定无环（加边前查是否同组）、最近公共祖先（离线 Tarjan）、朋友圈统计。

**🔴 部分五·顶级视角**：并查集是"动态连通性"的标准答案，代码短但思想深。顶级工程师在"集合合并查询"类题秒用。

**🟠 部分六·安全种子**：并查集本身安全，但若用于不可信数据规模巨大需防内存；逻辑上它是"信任关系图"基础。

**✅ 验收**：用 UF 实现"判断是否成环"（加边前查同组）。

**⚠️ 常见坑**：find 忘路径压缩；union 秩处理错导致树退化；初始化漏 rank。

**➡️ 下一步**：DSA21 字符串 KMP/前缀。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 UF 的 find/uni。🥉
- 🔵 Lv2：用 UF 检测无向图是否成环。🥈
- 🟣 Lv3：用 UF 求图的连通分量数。🥇
- 🟠 Lv4：实现按 size 合并（替代秩）并对比性能。🏆
- 🔴 Lv5：用离线 Tarjan + UF 实现 LCA（最近公共祖先），写原理笔记。👑

---

## DSA21 · 字符串 KMP / 前缀（失配函数 / 后缀数组）

**🎯 目标**：理解字符串匹配的"利用已知信息跳过"，能手写 KMP 与失配函数。

**📋 小白前置**：DSA20、DSA9（前缀思想）。

**🟢 部分一·最浅层**：在文章里找"abc"，普通法每次失配就往后挪一格重比；KMP 像记笔记——失配时知道"前面这段已匹配，能直接跳到下一个该比的位置"，不用重头。

**🟡 部分二·动手层**：`kmp.py` 构建失配数组：
```python
def pi(p):
    m=len(p); f=[0]*m; j=0
    for i in range(1,m):
        while j and p[i]!=p[j]: j=f[j-1]
        if p[i]==p[j]: j+=1
        f[i]=j
    return f
def kmp(s,p):
    f=pi(p); j=0; res=[]
    for i,c in enumerate(s):
        while j and c!=p[j]: j=f[j-1]
        if c==p[j]: j+=1
        if j==len(p): res.append(i-len(p)+1); j=f[j-1]
    return res
```

**🔵 部分三·原理层**：`pi[i]`=前缀 p[0..i] 的"最长真前缀同时也是后缀"长度。匹配失败时 j 回退到 f[j-1] 而非 0，利用已匹配信息，复杂度 O(n+m) 而非暴力 O(nm)。

**🟣 部分四·深挖层**：**后缀数组**（所有后缀排序）支持子串查询、最长公共前缀；**Z-algorithm** 是另一种线性匹配；字符串哈希（DSA5）可做 Rabin-Karp 滚动哈希。

**🔴 部分五·顶级视角**：字符串算法是生物信息（基因比对）、搜索引擎、 plagiarism 检测、入侵检测（payload 匹配）的基石。

**🟠 部分六·安全种子**：NIDS（入侵检测）用多模匹配（Aho-Corasick，KMP 的多模扩展）扫描流量特征；但特征匹配可被变形绕过（混淆），需多层检测。

**✅ 验收**：手写 KMP，验证在长文本中找所有匹配位置。

**⚠️ 常见坑**：pi 数组边界（f[j-1] 当 j=0）；匹配后回退错；混淆 KMP 与 Sunday 算法。

**➡️ 下一步**：DSA22 位运算技巧。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 KMP 找匹配。🥉
- 🔵 Lv2：手算小模式串的 pi 数组验证。🥈
- 🟣 Lv3：用 KMP 找"字符串最小循环节"。🥇
- 🟠 Lv4：实现 Rabin-Karp 滚动哈希多模式匹配。🏆
- 🔴 Lv5：实现 Aho-Corasick 自动机（多关键词一次性匹配），写原理笔记。👑

---

## DSA22 · 位运算技巧（子集枚举 / lowbit）

**🎯 目标**：掌握位运算在算法中的妙用，能手写子集枚举、lowbit、状态压缩。

**📋 小白前置**：PY3（数字与运算）、DSA20。

**🟢 部分一·最浅层**：计算机的开关只有 0/1。位运算直接拨这些开关，极快。比如"第 3 个开关开着吗"= `x & (1<<3)`。

**🟡 部分二·动手层**：`bit.py`：
```python
x=0b10110
print(x & (1<<2))     # 第2位(0基)是否为1
print(x | (1<<0))     # 打开第0位
print(x ^ (1<<1))     # 翻转第1位
print(bin(x & (x-1))) # 清最低位的1
print(bin(x & -x))    # lowbit: 只留最低位的1
```

**🔵 部分三·原理层**：`x & (x-1)` 清最低 1；`x & -x` 取最低 1（lowbit，树状数组核心）；`x ^ y` 统计不同位用 `popcount`；位运算 O(1) 且硬件级快。

**🟣 部分四·深挖层**：**状态压缩 DP**（用整数位表示集合，如 TSP 旅行商）；**子集枚举** `for s in subsets: s=(s-1)&full`；**Bitset** 加速布尔数组；**快速幂**用位拆分。

**🔴 部分五·顶级视角**：位运算是"压榨硬件"的极致。密码学、压缩、SIMD、网络协议标志位、游戏碰撞掩码全靠它。顶级工程师用它把 O(n) 降到 O(n/64)（bitset）。

**🟠 部分六·安全种子**：位运算实现权限掩码（如 Linux 的 rwx 用 3 位）是最小权限基础；但移位溢出（C 里移位数≥位宽）是未定义行为漏洞。

**✅ 验收**：手写 lowbit 函数 + 用位运算统计 1 的个数（两种写法）。

**⚠️ 常见坑**：混淆 `>>`（逻辑/算术，Python 是算术）与 C 的符号位；popcount 忘记循环清位；状态压缩位序错。

**➡️ 下一步**：DSA23 数论。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 bit.py 各操作。🥉
- 🔵 Lv2：用位运算实现"判断奇偶/交换两数/取绝对值"。🥈
- 🟣 Lv3：状态压缩枚举 n≤20 的所有子集。🥇
- 🟠 Lv4：用 lowbit 实现树状数组（前缀和/单点更新）。🏆
- 🔴 Lv5：实现快速幂 `pow(a,b,mod)`（位拆分），并用于 RSA 模幂场景说明。👑

---

## DSA23 · 数论（素数 / 模逆 / 中国剩余）

**🎯 目标**：掌握算法竞赛与密码学常用的数论工具，能手写欧几里得、快速幂、模逆。

**📋 小白前置**：DSA22、方向7a SEC4（密码学基础，可先读）。

**🟢 部分一·最浅层**：数论是"整数之间的关系学"。比如"两个人约好每 3 天和每 5 天见面，哪天同时见"——这背后是最大公约数和同余。

**🟡 部分二·动手层**：`math_mod.py`：
```python
def gcd(a,b):
    while b: a,b=b,a%b
    return a
def egcd(a,b):
    if b==0: return a,1,0
    g,x,y=egcd(b,a%b); return g,y,x-(a//b)*y
def modinv(a,m):
    g,x,_=egcd(a%m,m)
    return x%m if g==1 else None
print(gcd(12,18), modinv(3,11))  # 6 4
```

**🔵 部分三·原理层**：欧几里得（辗转相除）求 gcd；**扩展欧几里得**解 `ax+by=gcd`；模逆存在当且仅当 `gcd(a,m)=1`。费马小定理 `a^(p-1)≡1 mod p` 给模逆 `a^(p-2)`。

**🟣 部分四·深挖层**：**中国剩余定理**（多同余方程组合并）、素数筛（埃氏/欧拉线性筛）、Miller-Rabin 素性测试、Pollard-Rho 大数分解。这些是 RSA/Diffie-Hellman 的数学地基。

**🔴 部分五·顶级视角**：现代密码学=数论+群论。顶级安全/后端工程师理解模运算才能在 JWT、签名、零知识证明里不踩坑。

**🟠 部分六·安全种子**：此处直接关联方向7——RSA 安全性依赖"大数分解难"；模逆是签名验证核心；切勿用弱素数/小指数。白帽：只自己实现理解，不碰他人密钥。

**✅ 验收**：手写 gcd/egcd/modinv，并验证 `a*modinv(a,m)%m==1`。

**⚠️ 常见坑**：modinv 在 gcd≠1 时返回 None 未处理；负模结果未 `%m`；快速幂忘取模溢出。

**➡️ 下一步**：DSA24 计算几何。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 gcd/modinv。🥉
- 🔵 Lv2：实现埃氏筛求 N 以内素数。🥈
- 🟣 Lv3：用 CRT 合并两个同余方程。🥇
- 🟠 Lv4：实现 Miller-Rabin 判断大数素性。🏆
- 🔴 Lv5：从零实现 RSA 密钥生成+加密+解密（小素数演示），解释为何大数分解难。👑

---

## DSA24 · 计算几何（叉积 / 扫描线）

**🎯 目标**：建立几何问题的算法建模能力，能手写凸包、线段相交判断。

**📋 小白前置**：DSA22、PY2（坐标）。

**🟢 部分一·最浅层**：计算几何是"用算法处理点和线"。比如给一堆散点，求把它们全包住的最小橡皮圈（凸包）；或判断两根棍子是否交叉。

**🟡 部分二·动手层**：`geom.py` 叉积判断方向：
```python
def cross(o,a,b): return (a[0]-o[0])*(b[1]-o[1])-(a[1]-o[1])*(b[0]-o[0])
# cross>0 左转, <0 右转, =0 共线
def seg_intersect(p1,p2,p3,p4):
    d1=cross(p3,p4,p1); d2=cross(p3,p4,p2); d3=cross(p1,p2,p3); d4=cross(p1,p2,p4)
    return (d1*d2<0) and (d3*d4<0)
```

**🔵 部分三·原理层**：**叉积**（二维）给出有向面积，判断三点转向；**凸包**用 Graham 扫描（按极角排序+单调栈）或 Andrew 单调链；扫描线处理区间/矩形面积并。

**🟣 部分四·深挖层**：最近点对（分治）、点在多边形内（射线法）、圆与几何、旋转卡壳。精度用整数叉积避免浮点误差。

**🔴 部分五·顶级视角**：计算几何是 GIS、计算机图形、碰撞检测、芯片布局（VLSI）的基石。顶级工程师处理"空间关系"时精确且防浮点坑。

**🟠 部分六·安全种子**：几何算法在游戏/图形引擎里若输入不可信可能触发精度边界崩溃；用整数运算+epsilon 防护。

**✅ 验收**：手写叉积 + 线段相交判断，验证多组输入。

**⚠️ 常见坑**：浮点精度导致等号失效（用 epsilon）；凸包忘处理共线；叉积符号方向记反。

**➡️ 下一步**：DSA25 高级结构。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通叉积与线段相交。🥉
- 🔵 Lv2：实现 Graham 扫描凸包。🥈
- 🟣 Lv3：用扫描线求多个矩形覆盖面积。🥇
- 🟠 Lv4：实现"点在凸多边形内"的 O(log n) 二分法。🏆
- 🔴 Lv5：实现旋转卡壳求凸包直径（最远点对），写几何笔记。👑

---

## DSA25 · 高级结构（跳表 / 树状数组 / 线段树）

**🎯 目标**：掌握应对"动态区间查询/更新"的进阶结构，能手写线段树与树状数组。

**📋 小白前置**：DSA8（堆）、DSA20（lowbit）、DSA7（平衡树）。

**🟢 部分一·最浅层**：树状数组和线段树像"分层统计员"——不改原数，只在几个汇总点记小计，问"1 到 5 的和"时把几个小计一加就出，不用每次从头数。

**🟡 部分二·动手层**：`fenwick.py` 树状数组：
```python
class BIT:
    def __init__(s,n): s.t=[0]*(n+1)
    def add(s,i,v):
        while i<len(s.t): s.t[i]+=v; i+=i&-i
    def sum(s,i):
        r=0
        while i>0: r+=s.t[i]; i-=i&-i
        return r
```

**🔵 部分三·原理层**：树状数组用 lowbit 把区间和拆成 O(log n) 个桶，单点更新+前缀和都 O(log n)，代码极短。**线段树**用二叉树把区间二分，支持区间更新/查询（加懒惰标记），更通用但代码长。

**🟣 部分四·深挖层**：线段树可持久化（主席树）做"历史版本查询"；**跳表**用多层随机指针达到 O(log n) 且实现简单（Redis zset 用）；**树套树**处理二维区间。

**🔴 部分五·顶级视角**：任何"高频更新+高频区间统计"系统（实时榜单、监控聚合、订单统计）都靠这些结构。顶级工程师按"查询/更新比例+维度"选结构。

**🟠 部分六·安全种子**：计数器/限流器后端常用 BIT/滑动窗口；恶意高频请求需限流防护（方向6 BE15）。

**✅ 验收**：手写 BIT 的 add/sum，并用其实现"区间和查询"。

**⚠️ 常见坑**：树状数组下标从 1 开始（0 会死循环）；线段树懒惰标记下推忘做；维度升了还用一维结构。

**➡️ 下一步**：方向3 底层系统（COMP1 起），把"数据怎么存"接到"电脑怎么跑"。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 BIT add/sum。🥉
- 🔵 Lv2：用 BIT 实现区间和（两次前缀和相减）。🥈
- 🟣 Lv3：手写线段树（build/query/update）。🥇
- 🟠 Lv4：线段树加懒惰标记做"区间加+区间和"。🏆
- 🔴 Lv5：实现可持久化线段树（主席树）查询"第 k 小（带历史版本）"，写原理笔记。👑

---

> **方向2 完结 · 共 25 节（DSA1–DSA25）**。每节 11 段 + Lv1–Lv5 闯关 + ≥5 Lab。建议从 DSA1 起逐节闯关，集齐 25 个 👑 即方向2 毕业。下一步进方向3 底层系统。白帽红线贯穿：只实现/分析，不越权。
