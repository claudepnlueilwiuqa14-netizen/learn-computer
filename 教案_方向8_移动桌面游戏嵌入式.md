# 教案_方向8_移动桌面游戏嵌入式（MOB1–MOB53 完整 11 段深教案）

> 本文件覆盖 MOB1–MOB53，共 53 节，按 11 段深模板 + 实战 Lab 逐节展开。
> 学生画像：真·零基础（连键盘都不熟）、术后留置针手不便（护手少打字、GUI/复制优先）、时间极充裕、目标成为计算机领域顶级大佬、坚定白帽路线。
> 使用方法：每节先读 🎯→📋→🟢 建立直觉，然后照 🟡 动手层逐字操作（能复制就复制，护手优先），做完把结果发我批改，再回头读 🔵🟣🔴 补理论。卡住随时喊"卡在第 X 步"，我立刻接手。
> 红线贯穿（移动/桌面/游戏/嵌入尤其重要）：
> - ✅ 白帽才练：只练【你自己写的程序 / 授权靶场 / 自家虚拟机 / CTF】。
> - 🚫 Frida、jadx、IDA、Hook 工具只用于【你自己编译的 App、或明确授权的测试样本】；绝不逆向他人商业 App、不 Hook 他人应用、不绕过他人付费/授权、不攻击他人系统。
> - 🔒 只讲移动/桌面/游戏/嵌入式的"开发"与"对自己产物的安全检测（白帽）"。
> - 💊 身体优先，手疼/头晕立刻停；课永远在、人必须在。

---

# MOB1 · 移动开发概论

## 🎯 目标
学完这节，你能说清楚"手机 App、电脑软件、游戏、单片机"四类东西分别跑在哪、用什么语言写、难点在哪；并在本机搭好后面所有实验的"地基"——认识 Android Studio、知道"模拟器/真机/虚拟机"是什么。

## 📋 小白前置
需先完成总纲预备层 PRE0–PRE8（认识电脑、鼠标、键盘、文件管理、装 VS Code、终端、浏览器、备份）。建议已粗读方向1（至少 Python 入门 PY1–PY11），否则后面 Kotlin/Swift/C# 会吃力。

## 🟢 部分一·最浅层（生活比喻）
手机 App 像"便利店"——只能在一个品牌的货架上摆（安卓货架 / 苹果货架），受超市规矩管（权限、审核）。电脑软件像"自家仓库"——空间大、你说了算，但什么都能放也容易乱。游戏像"主题乐园"——每秒要刷新几十次画面，最吃显卡。单片机（Arduino）像"智能插座"——只有一点点脑容量，但永远在线、不关机。四类东西共同点：都是"程序"，都跑在某种"芯片"上。

## 🟡 部分二·动手层（逐字操作）
1. 打开浏览器，访问 Android Studio 官网 `https://developer.android.com/studio`（仅下载，不破解）。
2. 点"Download"，下载 Windows 版（约 1GB）。下载完双击安装，一路 Next，勾选"Android Virtual Device"。
3. 安装完打开 Android Studio，首次会下载 SDK（约 2GB，耐心等）。
4. 不写代码，先点 "More Actions" → "Virtual Device Manager" → "Create device" → 选 Pixel 6 → 选 API 34 系统镜像 → 下载 →  Finish。
5. 点 ▶ 启动模拟器（绿色机器人开机画面 = 一台"虚拟手机"跑在你电脑里）。

### 🧪 实战 Lab（MOB1）
把你成功启动的"虚拟手机"桌面截图发我。再在浏览器开一个标签页，访问 `https://flutter.dev`（只浏览，不装），看它首页怎么描述"一套代码多平台"。验收：截图里有运行中的 Android 模拟器。

## 🔵 部分三·原理层
模拟器本质是个"虚拟机"：你电脑的 CPU 用软件模拟出另一颗 ARM 芯片，让手机系统以为自己跑在真机上。Android App 是"给这套虚拟环境编译的安装包（APK）"。桌面软件直接调用你电脑的操作系统（Windows 的 Win32、mac 的 Cocoa）。游戏额外要"图形 API（OpenGL/Vulkan）"直接和显卡对话。单片机上跑的是"固件"，没有操作系统，上电就执行第一条指令。

## 🟣 部分四·深挖层
为什么手机 App 受限多？历史原因：早期塞班/Java ME 各自为政，Google 用"开源 Android + 统一运行时 ART"统一了安卓；苹果用"封闭 iOS + 严格审核"换稳定。顶级工程师看这层会说："平台约束不是麻烦，是它替你做的取舍"——安卓重自由、iOS 重体验、桌面重能力、嵌入式重成本。理解这点，你就不会抱怨"为什么苹果不能侧载"。

## 🔴 部分五·顶级视角
顶级移动/客户端工程师的视野是"全平台同源"：用 Flutter/React Native 一份代码出安卓+iOS，用 Qt 出 Win/Mac/Linux，用 Unity/Godot 出游戏全平台。他们还能"下沉"——同一个 Kotlin 能写 Android，也能用 Kotlin/Native 写桌面；同一个 C++ 引擎跨所有平台。方向8 全部 53 节，就是在为你铺这条"什么平台都能写"的路。

## 🟠 部分六·安全/合规种子
白帽第一粒种子：今天你下载的是官方 Android Studio，永远只从官网/官方商店装开发工具。凡是"破解版 Android Studio""绿色版 Xcode""内购绕过补丁"都是黑产温床，既违法又可能带木马。记住：你学的是"造 App 的人"，不是"拆别人 App 的人"。逆向/检测（MOB14 起）只对你自己编译的 App 做。

## ✅ 验收
交：① 运行中的 Android 模拟器截图；② 一句话说明"手机 App / 桌面软件 / 游戏 / 单片机"各跑在哪。两条齐算过关。

## ⚠️ 常见坑
- 模拟器首次启动极慢（几分钟），不是卡死，耐心等。
- SDK 下载失败多为网络问题，重试即可，别信"网盘破解版"。
- 手有留置针，安装这类大软件交给下载进度条，别一直戳屏幕。

## ➡️ 下一步
MOB2 · 移动 UI 范式——搞懂"命令式 vs 声明式"这两种写界面的根本思路，后面 Android（XML）和 Flutter/SwiftUI（声明式）都靠它。

---

# MOB2 · 移动 UI 范式

## 🎯 目标
学完这节，你能区分"命令式 UI"（一步步告诉电脑画什么）和"声明式 UI"（描述想要什么样子，电脑自己算差异），并明白为什么现代开发（Jetpack Compose、SwiftUI、Flutter、React）都转向声明式。

## 📋 小白前置
MOB1（知道 App 跑在哪）。

## 🟢 部分一·最浅层
命令式像"保姆式做菜"：你一步步说"先放油、再放蛋、炒 30 秒"。声明式像"点外卖"：你只说"我要一份番茄炒蛋"，厨房（框架）自己决定怎么做。手机界面每秒可能变几百次（来消息、转圈），声明式让框架只更新"变了的那一小块"，又快又不易错。

## 🟡 部分二·动手层（看代码，不运行）
在 VS Code 新建 `ui对比.txt`，复制下面两段"伪代码"对比（只阅读，建立概念）：

命令式（像 Android 老式 XML + Java）：
```
按钮 = 新建按钮()
按钮.文字 = "点我"
按钮.点一下 = 函数(){ 按钮.文字 = "已点" }
父容器.加(按钮)
```
声明式（像 Flutter）：
```
界面(
  按钮(文字: 已点 ? "已点" : "点我", 点一下: (){ 已点 = 真 })
)
```
注意声明式里你只描述"界面长什么样取决于状态 `已点`"，不手动改控件。

### 🧪 实战 Lab（MOB2）
用你自己的话，各写一句生活类比给"命令式"和"声明式"，发我。再想一个你手机里"状态变化"的例子（如未读红点→已读消失），说明它适合哪种范式。

## 🔵 部分三·原理层
命令式 UI 你持有"控件对象"的引用，手动调 `.setText()` 改它。声明式 UI 里控件是"一次性描述"，每次状态变，框架把你写的"界面函数"重新算一遍，产出一棵"新的界面树"，再用"调和（reconciliation）"算法对比新旧树，只把不同的节点发给系统渲染。代价是每帧重算函数，收益是逻辑极简、不易乱。

## 🟣 部分四·深挖层
历史：1990 年代 Delphi/VB 是命令式拖控件；2000 后 Web 用 HTML（半声明）；2013 React 把"声明式+虚拟DOM"带回大众视野；2019 起 Android 的 Compose、Apple 的 SwiftUI、Google 的 Flutter 全跟进。根因：屏幕变复杂、团队变大，"手动同步界面与状态"成为头号 bug 源，声明式用"单一数据源驱动界面"解决了它。

## 🔴 部分五·顶级视角
顶级工程师把 UI 范式上升到"状态管理"：界面只是状态的投影（Projection）。无论是前端 React、移动 Compose、游戏 UI、甚至嵌入式屏显，核心都是"状态→视图"的纯函数。懂了这一条，你学任何新框架都只是换语法。后面 MOB8（布局）、MOB24（SwiftUI）、MOB34（Flutter）全是这思想的落地。

## 🟠 部分六·安全/合规种子
范式本身无安全之分，但声明式框架常内置"安全默认"：比如 Compose 的文本组件默认不执行 HTML、Flutter 不解析外部脚本。白帽种子：将来你渲染"用户输入的文字/富文本"时，永远用框架的安全文本组件，别为了好看手写 `loadHtml()` 式危险接口（那是 XSS 的近亲）。

## ✅ 验收
交：① 两段伪代码你已读并理解；② 你写的两句类比 + 一个状态变化例子。

## ⚠️ 常见坑
- 别纠结"伪代码"不能运行，本节是建立思维，运行在后面真框架里做。
- 容易把"声明式=不用写逻辑"误解，其实逻辑都在"状态怎么变"里。

## ➡️ 下一步
MOB3 · 应用生命周期——App 从点开到退到后台再到被杀，电脑经历了什么，为什么你要关心。

---

# MOB3 · 应用生命周期

## 🎯 目标
学完这节，你能讲清 App 的"生老病死"：启动→前台→后台→销毁，以及为什么"后台时不能乱干活""回来时要恢复现场"。这是移动开发不翻车的根基。

## 📋 小白前置
MOB1、MOB2。

## 🟢 部分一·最浅层
App 像人一天：早上起床（启动）→ 干活（前台，你看着）→ 去吃饭（切后台，屏幕关了但还活着）→ 晚上睡觉（系统内存不够把你杀掉）。系统像物业：谁占地方多、谁不常用，就先请谁"走"。你写程序得配合物业，别在"吃饭时"还大声放音乐（后台狂跑）。

## 🟡 部分二·动手层（概念对照 Android）
在 VS Code 新建 `生命周期.txt`，抄下这张"状态表"（先记名字，后面 MOB7 写代码亲历）：

```
onCreate    刚建好，第一次出现
onStart     即将可见
onResume    在前台，能交互
onPause     要走了（来电话/切后台）
onStop      完全看不见
onDestroy   被销毁
```

### 🧪 实战 Lab（MOB3）
画一张"时间轴"：你点开 App（写 onCreate→onResume）→ 按 Home 键（onPause→onStop）→ 再点回来（onStart→onResume）→ 系统杀掉（onDestroy）。用纸或画图软件画，发我照片/截图。

## 🔵 部分三·原理层
移动 OS 是"多任务但单前台"：同一时刻只有一个 App 占屏幕。系统用一个"活动栈（Activity Stack）"管理谁在上。当内存吃紧，Linux 内核的"低内存杀手（LMK）"按优先级杀进程。你的 `onPause/onStop` 就是系统给你的"收拾现场"信号：存进度、停动画、释放相机。忽视它，轻则卡顿，重则数据丢、被系统强杀。

## 🟣 部分四·深挖层
为什么手机不能像电脑一样"开一堆窗口常驻"？电池+内存有限，且小屏只够一个 App。Android 用"生命周期回调"把"控制权"交还给系统，这是它的核心设计哲学（对比 iOS 的"应用状态保存"、桌面的"窗口随便开"）。顶级工程师会做"状态持久化"：用 `onSaveInstanceState` 把临时数据塞进一个 Bundle，被杀后重建时还原，用户无感。

## 🔴 部分五·顶级视角
"生命周期"概念贯穿一切：后端有"请求生命周期"（BE 课）、游戏有"场景加载/卸载"、嵌入式有"上电初始化→主循环→掉电保存"、甚至容器有"Pod 起停"。顶级工程师把"资源在何时申请、何时释放"当成第一等公民——这和 RAII（CPP1）、上下文管理器（PY15）是同一个思想。

## 🟠 部分六·安全/合规种子
白帽角度：生命周期回调里常做"敏感数据清理"——切后台时清掉内存里的密码、停止录屏。这是合规（如等保、GDPR）要求。反例：有的恶意 App 在 `onStop` 不释放，后台偷偷定位/录音——这是黑产行为，我们分析其原理用于"检测自己 App 是否泄露"，绝不模仿去侵害他人。

## ✅ 验收
交：① 状态时间表已抄；② 你画的生命周期时间轴图。

## ⚠️ 常见坑
- 新手常把所有初始化塞 `onCreate` 却忘了 `onDestroy` 释放，导致内存泄漏（MOB11 细讲）。
- 别把"后台"当"关掉"，后台 App 仍可能被系统杀，不能假设数据还在。

## ➡️ 下一步
MOB4 · 权限与隐私模型——为什么 App 要"问你同意才能用相机/定位"，以及你写 App 时怎么正确申请。

---

# MOB4 · 权限与隐私模型

## 🎯 目标
学完这节，你理解"运行时权限"：为什么 App 不能偷偷用相机/通讯录/定位，必须弹窗问你；以及你自己写 App 时如何"最小必要"地申请权限（白帽合规底线）。

## 📋 小白前置
MOB1、MOB3。

## 🟢 部分一·最浅层
权限像"门禁卡"：App 想进"相机室"，得先找你要卡（弹窗授权）。以前安卓 6.0 之前是"装的时候一次性给全部权限"——等于买房交了所有房间钥匙给装修队，太危险。现在改成"用的时候才问"，你还能随时在设置里收回。

## 🟡 部分二·动手层（看 AndroidManifest 概念）
在 VS Code 新建 `权限.txt`，复制这段"清单声明"（先认识，MOB7 真写）：
```
<uses-permission android:name="android.permission.CAMERA"/>
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
```
再记一句代码思路：调用相机前先 `checkSelfPermission`（问系统"我有卡吗"），没有就 `requestPermissions`（弹窗问用户）。

### 🧪 实战 Lab（MOB4）
列出你手机里 3 个 App，各写一个"它最该申请的权限"和"它绝对不该要的权限"（如：手电筒 App 不该要通讯录）。发我你的清单。这训练"最小必要"思维。

## 🔵 部分三·原理层
Android 权限分"普通"（联网，装完自动给）和"危险"（相机/定位/通讯录，必须运行时弹窗）。系统把"权限组"管理，用户授权一组后同组其他权限也通。每次调用受保护 API，框架底层走 Binder IPC 问"系统服务（System Server）"："这 UID 有权吗？" 没权直接抛异常 `SecurityException`。

## 🟣 部分四·深挖层
历史：2015 年 Android 6.0 引入运行时权限，是吸取了"过度授权"教训（很多 App 借权限偷通讯录卖广告）。iOS 更早（iOS 6，2012）就弹窗问定位。隐私模型演进方向是"按需、可撤回、可细化"（如 iOS 的"大致位置"、安卓 12 的"近似位置"）。顶级工程师视权限为"信任边界"——越少权限，攻击面越小，也越赢得用户信任。

## 🔴 部分五·顶级视角
"最小权限原则（Principle of Least Privilege）"是安全领域的基石，不只手机：Linux 的 `sudo`、容器的 `securityContext`、后端的 IAM 策略（OPS18）、数据库的库表权限（DB28）全是一回事。你将来做任何一个系统，都会反复用到"只给刚好够用的权限"。

## 🟠 部分六·安全/合规种子
白帽合规双底线：① 自己写 App 时，绝不申请用不到的权限，更不能在用户拒授权后"功能瘫痪要挟"（这是违规甚至违法）；② 越权收集（如后台静默定位）是黑产/违规红线，我们分析它只为"检测自己 App 是否误收集"。等保 2.0、GDPR、个人信息保护法都要求"告知-同意-最小必要"。

## ✅ 验收
交：① 清单声明已抄；② 3 个 App 的权限清单（该要/不该要）。

## ⚠️ 常见坑
- 在 Manifest 声明了权限，运行时仍要弹窗请求，"声明≠已授权"。
- 用户拒绝后别反复弹窗骚扰，应给"去设置开启"的引导。

## ➡️ 下一步
MOB5 · 移动安全基础——建立你这个白帽学员的"移动威胁模型"，后面所有安全节都挂在它下面。

---

# MOB5 · 移动安全基础（威胁模型）

## 🎯 目标
学完这节，你建立"移动威胁模型"：谁可能害你的 App（攻击者画像）、攻击面在哪（数据存哪、网络走哪、进程边界）、以及白帽该练什么（只练自己的 App）。这是 MOB14–MOB22 所有逆向/检测/加固节的"总纲"。

## 📋 小白前置
MOB1–MOB4。

## 🟢 部分一·最浅层
威胁模型像"画小区的危险地图"：哪里有门（攻击入口）、小偷可能从哪进（攻击面）、你家有什么值得偷（资产）。手机 App 的"资产"通常是：账号密码、聊天内容、支付凭证。攻击面：你存本地的文件、你发的网络请求、你装机的权限。

## 🟡 部分二·动手层（画一张威胁模型表）
在 VS Code 新建 `威胁模型.txt`，复制下面三栏并填空（用你将来要写的"记账 App"举例）：

```
资产：      用户账本数据、登录 Token
攻击面：    本地数据库(Room)、HTTPS 请求、导出备份文件
威胁者：    捡到手机的人、同 WiFi 的嗅探者、想作弊改余额的自己(练手)
防护：      数据库加密、证书绑定、完整性校验
```

### 🧪 实战 Lab（MOB5）
为你"设想中的第一个 App"填上面四栏。发我。不用真写代码，先把"什么东西要保护"想清楚——顶级工程师动手前先建模。

## 🔵 部分三·原理层
移动安全的核心是"信任边界"：App 跑在"不可信环境"（用户完全控制的手机上，能 root、能改内存、能抓包）。所以**任何客户端里的校验都不可信**——比如"余额不能为负"如果在 App 里判断，作弊者改内存就能绕过；正确做法是服务器校验。这就是"客户端防君子不防小人"，白帽检测的意义是"提高作弊成本"，不是"绝对安全"。

## 🟣 部分四·深挖层
STRIDE 威胁建模（方向7 SEC1）同样适用移动：仿冒（Spoofing，假 App）、篡改（Tampering，改 APK）、抵赖（Repudiation）、信息泄露（本地明文）、拒绝服务（狂点）、提权（root 利用）。顶级工程师用"攻击树"把"如何改余额"拆成若干路径（改内存/改数据库/改网络包），逐一评估成本。移动特有难点：设备归用户所有，你永远在"敌人的地盘"运行代码。

## 🔴 部分五·顶级视角
移动安全是"攻防不对称的战场"：防守方（你）要堵所有口子，攻击方只需找一个。所以顶级做法是"纵深防御 + 可检测"：代码混淆拖慢逆向、运行时检测 root/调试、关键逻辑放服务器、异常行为上报。后面 MOB17（Frida 自己 App）、MOB20（加固评估）、MOB22（root 检测）都是这一体系的零件。

## 🟠 部分六·安全/合规种子（本节核心红线）
**白帽移动四不**：① 不逆向/修改他人商业 App；② 不 Hook 他人应用进程；③ 不绕过他人付费/授权/广告；④ 不攻击他人账号系统。你练的一切（jadx 看自己 APK、Frida hook 自己 App、root 检测写给自己 App）都限定在**你自己编译的产物**或**明确授权的测试样本（如 CTF、自家靶机）**。越线即违法，技术再强也不碰。

## ✅ 验收
交：① 威胁模型四栏已填（用你设想的 App）；② 能口述"白帽移动四不"。

## ⚠️ 常见坑
- 误以为"客户端校验能防作弊"——错，客户端校验只是体验层，安全校验必须在服务端。
- 把"检测 root"当成"绝对防护"——它只是提高门槛，真高手能过，但你练的是自己 App 的防护意识。

## ➡️ 下一步
MOB6 · Android 入门/Kotlin——正式写第一行 Kotlin，装好开发环境，跑通"Hello World"。

---

# MOB6 · Android 入门 / Kotlin

## 🎯 目标
学完这节，你能用 Kotlin（Android 官方首选语言）写出第一个程序，理解"变量/函数/类"在 Kotlin 里怎么写，并在 Android Studio 跑出"Hello World"。Kotlin 语法会贯穿 MOB6–MOB13。

## 📋 小白前置
MOB1（装好 Android Studio + 模拟器）。建议已学 Python（PY1–PY11）建立编程直觉，Kotlin 会快很多。

## 🟢 部分一·最浅层
Kotlin 像"更聪明的 Python + 更安全的 Java"：它帮你管"空盒子"（null），免得程序因为"盒子里没东西"崩溃；它写法简洁，少打很多字（护手友好）。Android 官方 2019 年宣布 Kotlin 优先，所以你学它最对路。

## 🟡 部分二·动手层（逐字操作）
1. 打开 Android Studio → "New Project" → 选 "Empty Views Activity" → 语言选 **Kotlin** → 最小 SDK 选 API 24 → Finish。
2. 等 Gradle 同步完（底部进度条）。
3. 左侧 `app/java/.../MainActivity.kt`，找到 `onCreate`，在 `setContentView` 下加一行：
```kotlin
println("Hello World 来自泽泽学员")
```
4. 点右上 ▶（Run 'app'），选你的模拟器。看底部 "Logcat" 面板搜 "Hello"，能看到打印。

### 🧪 实战 Lab（MOB6）
在 `MainActivity.kt` 里加：
```kotlin
val 名字 = "小明"
val 年龄 = 18
println("$名字 今年 $年龄 岁，明年 ${年龄 + 1} 岁")
```
运行看 Logcat。再试 `var`（可变）与 `val`（不可变）区别：把 `val 年龄` 改成 `var 年龄` 再 `+1` 赋值，理解可变。

## 🔵 部分三·原理层
Kotlin 先编译成 Java 字节码（跑在 JVM/ART 上），所以能和 Java 互调。`val` 是不可变引用（类似 `final`），`var` 可变。`${}` 是字符串模板。`println` 在 Android 里最终走到 Logcat（通过 `Log`，但 `println` 也会进 Logcat 缓冲）。Kotlin 的"空安全"：类型分 `String`（必不为空）和 `String?`（可空），编译器逼你处理空，从源头消灭 `NullPointerException`。

## 🟣 部分四·深挖层
Kotlin 由 JetBrains（做 IDE 的公司）2011 年发布，2017 被 Google 捧为安卓首选。它许多语法糖（数据类 `data class`、扩展函数、协程）都是为"少写样板、少出 bug"设计。顶级工程师爱它"表达力强且安全"，但也会提醒：Kotlin 编译产物仍是 JVM 字节码，逆向（MOB15）看到的是类似 Java 的结构，这点在安全节很关键。

## 🔴 部分五·顶级视角
Kotlin 一处写、多处跑：Android、后端（Ktor）、桌面（Compose for Desktop）、跨平台（Kotlin Multiplatform）。顶级工程师视语言为"工具"，Kotlin 的价值是"在不牺牲安全的前提下提高生产力"。其"协程"（MOB11）更是现代并发的标杆设计，和 JS 的 async、Python 的 asyncio、Go 的 goroutine 思想相通。

## 🟠 部分六·安全/合规种子
Kotlin 的"空安全"本身就是安全特性——大量崩溃/漏洞来自空指针。白帽种子：写代码时默认用 `val` 和不可空类型，减少"意外状态"；将来处理"用户输入"时，显式用 `?` 标注可空并做校验，别用 `!!` 强行断言（它会崩，且掩盖问题）。今天只跑自己的 Hello World，安全无虞。

## ✅ 验收
交：① 模拟器跑出你的 App（哪怕空白界面）；② Logcat 显示你的"明年年龄"打印；③ 说明 `val`/`var` 区别。

## ⚠️ 常见坑
- 第一次跑 Gradle 要下载很多东西，慢是正常的，别反复点。
- `println` 在真机可能不显示，App 里正式用 `Log.d("标签", "内容")`。
- 中文变量名 Kotlin 支持，但团队项目一般用英文，练习随意。

## ➡️ 下一步
MOB7 · Activity/四大组件——真正写一个有按钮、能跳转的 App，理解安卓四大组件。

---

# MOB7 · Activity / 四大组件

## 🎯 目标
学完这节，你能写一个有按钮的界面，点按钮跳到第二个页面；理解 Android 四大组件（Activity、Service、BroadcastReceiver、ContentProvider）各自干啥。这是你第一个"真能交互"的 App。

## 📋 小白前置
MOB6（Kotlin 基础 + 跑通空项目）。

## 🟢 部分一·最浅层
Activity 像一个"舞台幕布"——一屏内容就是一幕。点"设置"跳到新幕（新 Activity）。Service 是"后台默默干活的人"（放音乐不占屏）。BroadcastReceiver 是"耳朵"——听到系统广播（电量低/开机）就行动。ContentProvider 是"对外的数据窗口"——别的应用经它读你的通讯录。

## 🟡 部分二·动手层（逐字操作）
1. 在 Android Studio 的 `res/layout/activity_main.xml` 里，把默认 TextView 删掉，从左侧 Palette 拖一个 `Button` 进来，右侧 Attributes 把 `text` 改成"去第二页"，把 `id` 改成 `@+id/btnGo`。
2. 在 `MainActivity.kt` 的 `onCreate` 加：
```kotlin
btnGo.setOnClickListener {
    val i = Intent(this, SecondActivity::class.java)
    startActivity(i)
}
```
3. 右键包名 → New → Activity → Empty Views Activity，命名 `SecondActivity`。
4. 运行，点按钮，跳到第二页即成功。

### 🧪 实战 Lab（MOB7）
在 SecondActivity 加一个"返回"按钮，点它 `finish()` 回到第一页。再给第一页 TextView 显示"欢迎，泽泽学员"。运行截两张图（第一页/第二页）发我。

## 🔵 部分三·原理层
`Intent` 是安卓的"信使"：显式 Intent（指定目标类，如本例）用于内部跳转；隐式 Intent（只说"我要拍照"）由系统匹配能干的组件。Activity 由"活动栈"管理，startActivity 压栈，finish 出栈（回顾 MOB3 生命周期）。四大组件都要在 `AndroidManifest.xml` 注册（Activity 自动注册，Service/Receiver 常需手动）。

## 🟣 部分四·深挖层
为什么用"组件 + 意图"而非直接 `new SecondActivity()`？因为跨进程：别的应用也能通过 Intent 启动你的 Activity（如微信调起你的分享页），这是安卓"松耦合"哲学。Service 分前台（有通知、不易被杀）和后台（受限，安卓 8+ 限制后台启动）。顶级工程师用 `ViewModel` + `SavedStateHandle` 解决"旋转屏幕 Activity 重建丢数据"的经典坑。

## 🔴 部分五·顶级视角
"组件化/声明式意图"思想在分布式系统也见：微服务用"消息"而非直接函数调用（BE16）、容器用"声明式 YAML"而非命令（OPS21）。顶级客户端工程师把 Activity 栈当成"导航状态机"管理，复杂 App 用 Navigation 组件做"单一可信导航图"。

## 🟠 部分六·安全/合规种子
白帽要点：Activity 默认可被同应用内启动，但若在 Manifest 设 `exported="true"` 则**任何 App 都能调起它**——这是常见漏洞（如暴露的登录页被绕过）。你自己练手时理解 `exported` 含义；将来写正式 App，敏感 Activity 务必设 `exported="false"` 或加权限保护。绝不借它去调起他人 App 的私有界面。

## ✅ 验收
交：① 两页截图（含跳转）；② 能说明四大组件各管什么。

## ⚠️ 常见坑
- 忘在 Manifest 注册 SecondActivity → 运行时崩溃 `ActivityNotFoundException`（用向导新建通常会自动注册）。
- 按钮 id 和代码里 `R.id.xxx` 对不上 → 空指针，检查拼写。
- 手不便时拖控件比手敲 XML 友好，多用 Designer 视图。

## ➡️ 下一步
MOB8 · 布局/UI 组件——系统学 TextView/Button/RecyclerView 等，用 XML 和 ConstraintLayout 排界面。

---


# MOB8 · 布局 / UI 组件

## 🎯 目标
学完这节，你能用 ConstraintLayout（约束布局）把一个登录界面排得整齐自适应，认识常用组件（TextView/EditText/Button/ImageView/RecyclerView），理解"视图树"和"绘制流程"。

## 📋 小白前置
MOB7（会拖按钮、跳转）。

## 🟢 部分一·最浅层
界面像"贴海报"：控件是照片，布局是胶水规则——"这张照片左边贴墙、上边贴顶"。ConstraintLayout 就是"用约束（相对关系）贴"，而不是写死"离左边 100 像素"，所以换大屏小屏都不乱。RecyclerView 像"会自动伸缩的相册"——1000 条数据它也只渲染看得见的几张，省内存。

## 🟡 部分二·动手层（逐字操作）
1. 在 `activity_main.xml` 切到 "Code" 视图（底部），把内容替换为约束布局登录框：
```xml
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent" android:layout_height="match_parent">

    <TextView android:id="@+id/tvTitle"
        android:text="登录" android:textSize="24sp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="80dp"/>

    <EditText android:id="@+id/etUser"
        android:hint="用户名" android:layout_width="0dp" android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/tvTitle" android:layout_marginTop="24dp"
        app:layout_constraintStart_toStartOf="parent" app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginStart="32dp" android:layout_marginEnd="32dp"/>

    <Button android:id="@+id/btnLogin"
        android:text="登录" android:layout_width="0dp" android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/etUser" android:layout_marginTop="16dp"
        app:layout_constraintStart_toStartOf="parent" app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginStart="32dp" android:layout_marginEnd="32dp"/>
</androidx.constraintlayout.widget.ConstraintLayout>
```
2. 运行，看到居中登录框。

### 🧪 实战 Lab（MOB8）
在 `MainActivity.kt` 给 `btnLogin` 加监听：`Toast.makeText(this, "你好 ${etUser.text}", Toast.LENGTH_SHORT).show()`。运行，输入名字点登录弹出欢迎。截图发我。

## 🔵 部分三·原理层
布局文件是"声明式描述"，运行时被"布局填充器（LayoutInflater）"解析成"视图树（View Tree）"——每个控件是一个节点。绘制分三步：测量（measure，算多大）、布局（layout，放哪）、绘制（draw，画像素）。ConstraintLayout 用"约束求解"一次性算出位置，比多层嵌套 LinearLayout 性能更好（嵌套越深，测量次数指数涨）。

## 🟣 部分四·深挖层
历史：早期 AbsoluteLayout（写死坐标，废弃）→ LinearLayout/RelativeLayout（嵌套性能差）→ ConstraintLayout（2016，扁平化）。RecyclerView 复用机制（ViewHolder）是性能关键：滚走的项"回收"给新项填数据，避免反复 new 对象（呼应 PY7 动态扩容/缓存思想）。顶级工程师用"约束+扁平"把过度绘制（Overdraw）压到最低，60fps 不掉帧。

## 🔴 部分五·顶级视角
"声明式 UI 树 + 单向数据流"是跨平台共识（MOB2）。视图树概念在 Web（DOM）、Flutter（Widget Tree）、游戏（场景树 MOB47）、甚至 GUI 框架（Qt 对象树 MOB40）都存在。懂 Android 视图树，你学任何 UI 框架都只是换 API 名字。

## 🟠 部分六·安全/合规种子
白帽种子：UI 上的"禁用按钮"只是视觉，作弊者可绕过（改内存/改布局让按钮可点）。所以"权限校验、付费校验"绝不能在客户端 UI 层做（回顾 MOB5 信任边界）。你自己练手时理解这点即可；将来检测自己 App 是否被篡改 UI，可加完整性校验（MOB20）。

## ✅ 验收
交：① 运行中的登录界面截图；② 点登录弹出带用户名的 Toast 截图。

## ⚠️ 常见坑
- `0dp` + 约束 = "匹配约束"，不是 0 宽；新手常误用 `wrap_content` 导致不撑满。
- 约束必须"闭环"（上下左右有锚点），否则控件飞到 (0,0)。
- 中文文案用 `res/values/strings.xml` 管理更规范，练习可硬编码。

## ➡️ 下一步
MOB9 · 存储/数据库 Room——把数据存到手机里，关机也不丢。

---

# MOB9 · 存储 / 数据库 Room

## 🎯 目标
学完这节，你能用 Room（Android 官方数据库框架）把"记账记录"存进手机 SQLite，重启 App 数据还在。理解"文件存储 / SharedPreferences / 数据库"三种本地持久化及其适用场景。

## 📋 小白前置
MOB7、MOB8（Kotlin + 界面）。建议看过方向5 数据库概论 DB1–DB3（知道"表/行/列"）。

## 🟢 部分一·最浅层
手机存储像"三种柜子"：① 抽屉（文件，存图片大块头）；② 便利贴（SharedPreferences，存"是否夜间模式"这种小设置）；③ 档案柜（数据库 Room，存成千上万条结构化记录，能按条件查）。记账要查"本月花了多少"，用档案柜最合适。

## 🟡 部分二·动手层（逐字操作）
在 `app/build.gradle` 的 `dependencies` 加（复制，版本以 Android Studio 提示为准）：
```gradle
implementation "androidx.room:room-runtime:2.6.1"
kapt "androidx.room:room-compiler:2.6.1"
```
1. 新建数据类：
```kotlin
@Entity
data class 账单(@PrimaryKey(autoGenerate = true) val id: Int = 0,
                val 项目: String, val 金额: Double)
```
2. 建 DAO：
```kotlin
@Dao
interface 账单Dao {
    @Insert suspend fun 插入(b: 账单)
    @Query("SELECT * FROM 账单") suspend fun 全部(): List<账单>
}
```
3. 建数据库（@Database 标注），在 Activity 里 `插入(账单("咖啡",18.0))` 并 `全部()` 读出打印。

### 🧪 实战 Lab（MOB9）
写个按钮"加一笔"，点击插入一条随机金额账单；另一个按钮"显示全部"，把全部记录拼成字符串 `Toast` 出来。重启 App 再点"显示全部"，确认数据还在。截图发我。

## 🔵 部分三·原理层
Room 是"SQLite 的优雅封装"：你写 Kotlin 接口（DAO），编译时 Room 用注解处理器（kapt）生成真正的 SQL 代码。`suspend` 函数配合协程在后台线程跑数据库（主线程禁止数据库操作，否则崩溃——ANR）。底层仍是 SQLite 文件存在 `/data/data/你的包名/databases/`。

## 🟣 部分四·深挖层
为什么不直接用 SQLite API？原始 API 又臭又长还易 SQL 注入（拼字符串）。Room 用编译期 SQL 校验 + 类型安全消灭这类问题（呼应方向7 SEC10 注入防护）。数据库文件本质是 B+ 树（DB8），查询走索引。顶级工程师会做"数据库迁移（Migration）"：App 升级时表结构变了，写迁移脚本保留老用户数据，否则升级即闪退。

## 🔴 部分五·顶级视角
"ORM（对象关系映射）"无处不在：后端 SQLAlchemy（BE8）、iOS Core Data、.NET Entity Framework（MOB39）。Room 的 `@Entity/@Dao/@Query` 思想和它们同构。顶级工程师视"本地存储"为"离线优先（Offline First）"战略的支点——没网也能用，联网再同步。

## 🟠 部分六·安全/合规种子
白帽重点：本地数据库默认不加密，root 后的手机任何人能 `adb pull` 走你的 `/data` 文件（MOB5 攻击面）。敏感数据要用 SQLCipher 加密，或存到系统凭据库（Android Keystore）。你自己练手时理解"明文存储=风险"；将来检测自己 App 是否泄露明文密码，就是这条线（呼应 MOB20 加固评估）。绝不偷看他人 App 的数据库文件。

## ✅ 验收
交：① 插入+显示全部截图；② 重启后数据仍在的验证截图。

## ⚠️ 常见坑
- 主线程操作数据库 → `IllegalStateException`，务必 `suspend` + 协程或 `viewModelScope`。
- 改了 Entity 没升数据库版本 → 崩溃，学习期可设 `fallbackToDestructiveMigration()` 清库。
- Gradle 加依赖后要"Sync Now"。

## ➡️ 下一步
MOB10 · 网络 / Retrofit——让 App 联网取数据（如查天气）。

---

# MOB10 · 网络 / Retrofit

## 🎯 目标
学完这节，你能用 Retrofit（主流网络库）让 App 发 HTTP 请求、把返回的 JSON 解析成 Kotlin 对象并显示。理解"REST API / JSON / 序列化"在移动端的落地。

## 📋 小白前置
MOB9（Kotlin + 协程）。建议看过方向4 NET8（HTTP）、方向6 BE9（REST API 设计）。

## 🟢 部分一·最浅层
App 联网像"派快递员去仓库取货"：你写"去哪个网址（URL）、要什么（GET）"，仓库（服务器）回一箱"JSON 清单"，快递员（Retrofit）帮你把清单自动拆成你认识的 Kotlin 对象。不用你手动拆箱（解析字符串）。

## 🟡 部分二·动手层（逐字操作）
用公开的测试 API `https://jsonplaceholder.typicode.com/users/1`（免费、无需密钥）。加依赖：
```gradle
implementation "com.squareup.retrofit2:retrofit:2.9.0"
implementation "com.squareup.retrofit2:converter-gson:2.9.0"
```
定义：
```kotlin
data class 用户(val id: Int, val name: String, val email: String)
interface 用户接口 {
    @GET("users/{id}")
    suspend fun 取用户(@Path("id") id: Int): 用户
}
```
调用：
```kotlin
val r = Retrofit.Builder().baseUrl("https://jsonplaceholder.typicode.com/")
    .addConverterFactory(GsonConverterFactory.create()).build()
val api = r.create(用户接口::class.java)
lifecycleScope.launch {
    val u = api.取用户(1)
    Log.d("用户", "${u.name} / ${u.email}")
}
```
加网络权限 `<uses-permission android:name="android.permission.INTERNET"/>`。运行看 Logcat。

### 🧪 实战 Lab（MOB10）
把取到的用户名显示到 MOB8 的 TextView 上。再改成取 `posts`（帖子）列表，用 RecyclerView 展示前 10 条标题。运行截图发我。

## 🔵 部分三·原理层
Retrofit 用"动态代理"在运行时生成接口实现：你只写 `@GET` 注解，它拼 URL、用 OkHttp 发请求、用 Gson 把 JSON 反序列化成你的 `data class`。`suspend` 让请求挂起不阻塞主线程（MOB11 细讲协程）。JSON 是"JavaScript 对象记号"的文本格式，本质是键值对（回顾 PY2 字符串、DB3 结构化）。

## 🟣 部分四·深挖层
为什么用 Retrofit 而非裸 `HttpURLConnection`？它把"样板（建连接/读流/解析）"全收走，你只关心"业务数据"。OkHttp 底层做连接池、HTTP/2、拦截器（可统一加签名/日志）。顶级工程师用"拦截器"做鉴权头注入、用"超时/重试"做弱网容错；还会做"离线缓存"（OkHttp Cache）实现无网可读旧数据。

## 🔴 部分五·顶级视角
"声明式 API 客户端"是通用范式：前端用 axios/fetch、Python 用 requests、Go 用 net/http 封装、.NET 用 HttpClient+Refit（和 Retrofit 同名思想）。顶级工程师把"网络层"做成独立模块，和业务解耦，便于测试（Mock 接口）和换后端。

## 🟠 部分六·安全/合规种子
白帽红线：① 只访问你有权调用的 API；绝不扫别人服务器、不发越权请求（方向7 SEC11 IDOR）。② 网络传输必须 HTTPS（NET9），明文 HTTP 会被中间人窃听。③ 你练手用的 jsonplaceholder 是公开测试站，合法。④ 将来你做"证书绑定（SSL Pinning，MOB21）"只用于保护**自己 App** 的通信，绝不用于绕过他人。

## ✅ 验收
交：① Logcat 显示取到的用户名；② 把用户名显示到界面的截图（或 RecyclerView 列表截图）。

## ⚠️ 常见坑
- 忘加 `INTERNET` 权限 → 请求静默失败/异常。
- 主线程网络请求（非 suspend 旧写法） → `NetworkOnMainThreadException`。
- 真机调试若用 `10.0.2.2` 访问本机后端（模拟器特例），别写成 `localhost`。

## ➡️ 下一步
MOB11 · 多线程 / 协程——搞懂"主线程不能卡"，用 Kotlin 协程优雅并发。

---

# MOB11 · 多线程 / 协程

## 🎯 目标
学完这节，你理解"主线程（UI 线程）不能做耗时事"，能用 Kotlin 协程在后台下载/算账、回主线程更新界面。协程是 Android 并发的标配。

## 📋 小白前置
MOB10（网络请求用到 suspend）。建议看过方向1 并发（PY17 asyncio、GO1 goroutine）建立并发直觉。

## 🟢 部分一·最浅层
主线程像"柜台收银员"：必须时刻响应你点按钮（60fps）。如果你让它去"搬一车货（下载大文件）"，柜台就卡住没人理——用户以为死机。所以搬货要派"后厨（后台线程）"去做，做完喊一声"货到了"再让收银员上架。协程就是"轻量后厨工人"，开成千上万个也不卡。

## 🟡 部分二·动手层（逐字操作）
在 Activity 加按钮"算大数"，点击：
```kotlin
btnCalc.setOnClickListener {
    lifecycleScope.launch(Dispatchers.IO) {
        var s = 0L
        for (i in 1..100_000_000) s += i      // 假装耗时
        val 结果 = s
        withContext(Dispatchers.Main) {
            tvTitle.text = "和=$结果"          // 回主线程改 UI
        }
    }
}
```
对比：若不加 `Dispatchers.IO` 直接在 `launch` 里算，界面会卡（验证主线程约束）。

### 🧪 实战 Lab（MOB11）
把 MOB10 的网络请求也包进 `lifecycleScope.launch(Dispatchers.IO)`，体验"后台取数、主线程显示"。再加一个 `delay(2000)` 看协程怎么"挂起不阻塞"。运行截"和="结果图发我。

## 🔵 部分三·原理层
Android 主线程（UI 线程）有"严格模式"：网络/数据库/大量计算会抛异常或掉帧。协程是"可被挂起的函数"（suspend），挂起时**不占用线程**，让出 CPU 给别人；`Dispatchers.IO` 是后台线程池，`Dispatchers.Main` 是主线程。`withContext` 切换上下文。对比线程：线程切换贵（内核态），协程切换便宜（用户态，类似 Go 的 goroutine 思想 GO1）。

## 🟣 部分四·深挖层
历史：早期 `AsyncTask`（已废弃，易泄漏）→ `Handler/Thread` 样板多 → `RxJava`（强大但陡）→ `Coroutines`（2018，语法最优雅）。协程底层是"状态机 + 续体（Continuation）"：编译器把 `suspend` 函数切成多段，挂起点保存现场。顶级工程师用"结构化并发（CoroutineScope）"：scope 取消时所有子协程一起取消，避免泄漏（呼应 CPP5 资源释放）。

## 🔴 部分五·顶级视角
"异步/挂起"是现代所有语言的刚需：JS 的 Promise/async（JS3）、Python asyncio（PY17）、Go goroutine（GO1）、Rust async（RS10）、甚至后端响应式（PAR5）。顶级工程师把"并发模型"当核心能力——懂得"什么该并行、什么必须顺序、怎么不泄漏"，是区分菜鸟和老手的分水岭。

## 🟠 部分六·安全/合规种子
白帽种子：后台线程里处理敏感数据要小心"竞态"（TOCTOU，SEC40）——比如两线程同时改余额。协程也要防"在挂起后用了过期状态"。你自己练手时理解并发安全；将来检测自己 App 是否有"并发漏洞"（如双花），属于白帽自查范畴，绝不用它去攻击他人服务。

## ✅ 验收
交：① 显示"和="的截图；② 能口述"为什么主线程不能算大数"。

## ⚠️ 常见坑
- 在 `Dispatchers.IO` 里直接 `tvTitle.text=...` → 崩溃（只有主线程能改 UI）。
- 忘记 `lifecycleScope`，Activity 销毁后协程还在跑 → 内存泄漏。
- `suspend` 函数只能在协程或其他 suspend 里调。

## ➡️ 下一步
MOB12 · NDK / JNI / C++——让 Android 跑 C/C++ 代码（性能/复用/安全壳）。

---

# MOB12 · NDK / JNI / C++

## 🎯 目标
学完这节，你能让 Android 调用一段 C/C++ 代码（通过 JNI），理解"为什么有的功能要用原生（Native）"：高性能计算、复用已有 C 库、把敏感逻辑藏进 so（但也更难过审）。

## 📋 小白前置
MOB11（Kotlin 协程）。强烈建议先学方向1b 的 C1–C7（C 基础）、CPP1–CPP5（C++），否则 native 代码看不懂。

## 🟢 部分一·最浅层
JNI 像"双语翻译"：Kotlin 说中文（Java 世界），C++ 说英文（原生世界）。你想让中文和英文对话，得有个翻译官（JNI 接口）把"中文的参数"翻成英文传进去、把"英文的结果"翻回中文。Android 把 C++ 编译成 `.so` 文件（共享库），App 运行时加载它。

## 🟡 部分二·动手层（逐字操作）
Android Studio 新建项目时勾选 "Include C++ Support"（或手动）：
1. `app/src/main/cpp/native-lib.cpp`：
```cpp
#include <jni.h>
extern "C" JNIEXPORT jstring JNICALL
Java_com_example_myapp_MainActivity_字符串FromJNI(JNIEnv* env, jobject) {
    return env->NewStringUTF("你好，我来自 C++");
}
```
2. Kotlin 声明：
```kotlin
external fun 字符串FromJNI(): String
companion object { init { System.loadLibrary("native-lib") } }
```
3. 在 `onCreate` 调 `tvTitle.text = 字符串FromJNI()`，运行看到 C++ 返回的文字。

### 🧪 实战 Lab（MOB12）
把 C++ 函数改成算斐波那契第 n 项（用循环），Kotlin 传 `n=30` 取结果显示。对比纯 Kotlin 实现的耗时（用 `System.currentTimeMillis()` 计时）。截图发我。

## 🔵 部分三·原理层
`external` 关键字告诉 Kotlin"这函数实现在别处"。`System.loadLibrary` 用 `dlopen` 加载 `.so`。JNI 函数名规则 `Java_包名_类名_方法名` 让 JVM 在调用时查表找到原生函数。参数 `JNIEnv*` 是"翻译官手册"，提供 `NewStringUTF` 等把 C 字符串转 Java 字符串。类型映射：jint=Int、jstring=String 等。

## 🟣 部分四·深挖层
为什么用 NDK？① 性能：游戏/图像/加密用 C 更接近硬件（COMP 系列）；② 复用：把已有的 C/C++ 库（如 OpenCV、SQLite、游戏引擎）直接搬来；③ 保护：把关键算法放 native 增加逆向难度（但非绝对，MOB18 调试 so）。代价：失去 JVM 内存安全、JNI 桥有开销、需交叉编译（在 x86 电脑编 ARM 的 so）。

## 🔴 部分五·顶级视角
"FFI（外部函数接口，RS11）"是跨语言协作的通用机制：Python 用 ctypes 调 C、Rust 用 FFI 调 C、.NET P/Invoke 调 Win32（MOB39）、Unity C# 调 C++（MOB45）。顶级工程师视 NDK 为"当需要极致性能或复用遗产代码时的逃生舱"，但默认优先 Kotlin——可维护性与安全更重要。

## 🟠 部分六·安全/合规种子
白帽视角（重点）：native 层（so）是 MOB17–MOB22 的核心战场——Frida hook、IDA 调试都常针对 so。但**白帽四不**不变：你只能在**自己编译的 App / 自己写的 so** 上做 hook 与调试练习。把算法藏进 so 只是"提高门槛"，不是"绝对安全"，且过度依赖 native 反而引入内存安全漏洞（C 的缓冲区溢出 SEC34）。今天只练自己写的斐波那契 so。

## ✅ 验收
交：① 运行显示 C++ 文字/斐波那契结果截图；② 说明 JNI 是"翻译官"的角色。

## ⚠️ 常见坑
- 包名/类名改了但 C++ 函数名没改 → `UnsatisfiedLinkError`。
- 忘了 `extern "C"` 导致 C++ 改名（mangling）→ 找不到函数。
- CMake/ndk-build 配置错误时 `.so` 编不出，看 Build 输出。

## ➡️ 下一步
MOB13 · Gradle / 构建——理解 APK 是怎么"造"出来的，怎么做多渠道/多环境。

---

# MOB13 · Gradle / 构建

## 🎯 目标
学完这节，你理解 Gradle 如何把"源码 + 资源 + 依赖"变成一个 APK；会做"构建变体（productFlavors）"区分测试/正式环境，并会用命令行打包 `assembleRelease`。

## 📋 小白前置
MOB6–MOB12（已做出多文件 App）。建议看方向1 构建相关（C13 make/CMake、JV8 Maven/Gradle）。

## 🟢 部分一·最浅层
Gradle 像"自动化工厂流水线"：你往上游丢进"源码、图片、第三方零件（依赖库）"，它按配方（build.gradle）编译、链接、打包、签名，下游吐出"安装包 APK/AAB"。你不用手动一步步点，改一句配方它就重跑相关环节。

## 🟡 部分二·动手层（逐字操作）
1. 打开 `app/build.gradle`，在 `android{}` 内加：
```gradle
flavorDimensions "env"
productFlavors {
    dev { dimension "env"; buildConfigField "String", "BASE_URL", '"https://dev.example.com"' }
    prod { dimension "env"; buildConfigField "String", "BASE_URL", '"https://api.example.com"' }
}
```
2. 代码里用 `BuildConfig.BASE_URL` 代替硬编码网址。
3. 终端（Android Studio 底部 Terminal，或 Git Bash）运行：
```bash
./gradlew assembleDevDebug
```
生成的 APK 在 `app/build/outputs/apk/dev/debug/`。

### 🧪 实战 Lab（MOB13）
分别打 `assembleDevDebug` 和 `assembleProdDebug`，用 `unzip -l app-...-dev-debug.apk | head` 看产物。再用 `./gradlew dependencies` 看依赖树。把两条命令输出截图/粘贴发我。

## 🔵 部分三·原理层
Gradle 是"基于任务的构建系统"：每个动作（compileJava、mergeAssets、packageApk）是一个 Task，Task 间有依赖图（呼应 C13 依赖图）。它用"增量构建"——只重跑受影响的 Task，省时。依赖坐标 `group:name:version` 从仓库（Google Maven、Maven Central）下载。"变体"是"维度（如 env）组合"叉乘出的构建目标。

## 🟣 部分四·深挖层
历史：Android 早期用 Ant（XML 脚本）→ Maven（约定优于配置）→ Gradle（2013，Groovy/Kotlin DSL，灵活）。Gradle 的"依赖解析"要处理"传递依赖 + 版本冲突"（选高版本或强制），这和方向1 的 venv/pip（PY20）、npm、Cargo（RS1）同源。"构建缓存/配置缓存"是大项目提速关键。顶级工程师写自定义 Gradle 插件统一团队构建规范。

## 🔴 部分五·顶级视角
"构建即代码（Build as Code）"是 DevOps 根基：Gradle/Maven（Java）、Cargo（Rust）、go build（GO6）、CMake（C13）、npm scripts（JS）、Bazel（巨石仓库）全是一家人。顶级工程师把"可复现构建"当信仰——同样的源码，任何人任何机器编出**位级相同**的产物（供应链安全 OPS46 的基础）。

## 🟠 部分六·安全/合规种子
白帽视线：构建环节是"供应链攻击"高发地（恶意依赖投毒，SEC84 SCA）。你自己练手时：① 只用官方仓库的正规依赖，别引"来路不明的 aar"；② 正式包必须"签名"（后面发布节），防被替换；③ `debug` 包带调试权限，`release` 包要混淆（MOB20）。绝不在构建里塞"破解/绕过"逻辑去害他人。

## ✅ 验收
交：① 两个 flavor 的 APK 已生成；② `dependencies` 输出或 `unzip -l` 结果。

## ⚠️ 常见坑
- `./gradlew` 在 Windows 要用 `gradlew.bat`；Git Bash 里 `./gradlew` 也可（脚本自适应）。
- 忘 Sync 导致新加的 flavor 不识别。
- 国内访问 Google Maven 慢，可配阿里镜像（仅加速，不引盗版）。

## ➡️ 下一步
MOB14 · Android 逆向概论（合法边界）——正式进入白帽视角，划清"能练 / 严禁"的线。

---

# MOB14 · Android 逆向概论（合法边界）

## 🎯 目标
学完这节，你建立"移动逆向的合法边界"：明确哪些能做（自己 App、授权靶场、CTF）、哪些绝对不能（他人商业 App、他人付费绕过）。认识逆向的工具链地图（jadx/APKTool/Frida/IDA），为 MOB15–MOB22 打框架。

## 📋 小白前置
MOB1–MOB13（已能自己造 App、打包 APK）。务必先内化 MOB5 威胁模型与"白帽移动四不"。

## 🟢 部分一·最浅层
逆向像"拆自己拼好的乐高"：你拼的乐高（自己写的 App），拆了研究结构完全合法、还能学怎么搭更稳。但拆**别人**的乐高（他人商业 App）还照着仿、或找机关卡（付费）撬开，就是偷——既违法也不道德。靶场/CTF 是"主办方明说'欢迎你来拆'的乐高"，那是练功场。

## 🟡 部分二·动手层（只看自己产物，马上做）
1. 找 MOB13 生成的 `app-dev-debug.apk`。
2. 安装 jadx（GitHub 搜 `skylot/jadx`，下载 releases 的 `jadx-x.x.x.zip`，解压即可，免费开源）。
3. 双击 `jadx-gui.bat`，把**你自己**的 APK 拖进去，左侧能看到反编译出的 Kotlin/Java 源码（类名、方法体）。

### 🧪 实战 Lab（MOB14）
对**你自己 MOB7/MOB8 的登录 App** 反编译，在 jadx 里找到 `MainActivity`，确认能看到你写的那句 `Toast`。截图发我，并写一句"我确认这只对自己的 App 做"。这是你白帽自证的第一步。

## 🔵 部分三·原理层
APK 本质是 ZIP：含 `classes.dex`（Dalvik 字节码，所有 Java/Kotlin 编译后的指令）、`resources.arsc`（编译后的资源）、`lib/`（so）、`AndroidManifest.xml`（二进制清单）。jadx 把 DEX 里的字节码"反编译"回近似 Java 源码（不是 100% 还原，变量名可能丢失）。对比：你写的源码 → javac/Kotlin 编译器 → class → dx/d8 → DEX → 打包 APK → 别人（或你）用 jadx 看回源码。

## 🟣 部分四·深挖层
为什么能"看回源码"？因为 Java/Kotlin 字节码保留了大量结构信息（类名、方法签名、字符串常量），不像 C++ 编译后符号被 Strip。这也是为什么"纯靠混淆才能增加逆向难度"。顶级逆向工程师的合法用途：审计自己产品漏洞、分析恶意软件（授权样本）、做 CTF、参与漏洞赏金（SEC100）。法律边界以"你是否有授权"为准——自己产物=默示授权；他人产物=必须书面授权或属公开靶场。

## 🔴 部分五·顶级视角
"逆向思维"是顶级工程师的元能力：读别人的好代码学设计（开源项目）、定位第三方库 bug（看它反编译/源码）、做兼容层。但约束是"授权 + 不侵权"。在方向7（SEC23–SEC55）你会系统学二进制逆向，移动端只是它的一棵分支（DEX/ART 是特化的"字节码 + 运行时"）。

## 🟠 部分六·安全/合规种子（全案最高优先级之一）
**移动逆向红线（写下来贴在显示器）**：
1. 只对自己编译的 App、或主办方明确授权的靶场/CTF 样本做逆向。
2. 绝不逆向他人商业 App 窃取算法/资源/用户数据。
3. 绝不 Hook/修改他人应用绕过付费、广告、授权、风控。
4. 绝不用逆向技术攻击他人账号/服务器。
违反任一条 = 违法（著作权法、刑法第 285/286 条）、违背白帽初心。本文件所有 MOB15–MOB22 的实战，**默认对象都是你自己的 App 或授权样本**。

## ✅ 验收
交：① 对自己 App 的 jadx 反编译截图（能看到你写的 Toast）；② 手抄/复述"移动逆向红线 4 条"。

## ⚠️ 常见坑
- 把"能反编译别人的 App"误当能力炫耀——那是雷区，永远别做。
- 混淆后 jadx 显示 `a.b.c()` 看不懂就慌——正常，后面 MOB20 讲混淆原理。
- 真机 root 后反编译系统 App = 越线，只练自己 user 空间的 App。

## ➡️ 下一步
MOB15 · APK/DEX 结构——深入 APK 内部，看 DEX 字节码和 ART 运行时。

---

# MOB15 · APK / DEX 结构

## 🎯 目标
学完这节，你拆开 APK 看内部真实结构：DEX 文件头、Dalvik 字节码、ART 运行时如何把 DEX 编译成机器码。会用 `apkanalyzer`（Android SDK 自带）和 `dexdump` 看 DEX 的方法列表。

## 📋 小白前置
MOB14（逆向边界 + jadx 入门）。

## 🟢 部分一·最浅层
APK 就是个"压缩包"：你改后缀 `.apk` 为 `.zip` 解压，能看到一堆文件。其中 `classes.dex` 是"程序的说明书"——所有你写的函数都被翻成一种叫"Dalvik 字节码"的精简指令。手机里的 ART 虚拟机读这份说明书，现场翻译成手机芯片能跑的机器码。

## 🟡 部分二·动手层（逐字操作，只对自己 APK）
1. 找自己 MOB13 的 `app-dev-debug.apk`，复制一份改名 `demo.zip`，右键解压看目录结构。
2. 用 Android SDK 自带工具分析（路径按你的 SDK 位置改）：
```bash
/c/Users/qq/AppData/Local/Android/Sdk/tools/bin/apkanalyzer apk summary demo.apk
/c/Users/qq/AppData/Local/Android/Sdk/build-tools/34.0.0/dexdump -l classes.dex | head -40
```
（若路径不同，在 Android Studio 的 SDK Manager 看 SDK Location；Git Bash 用 `/c/...` 写法。）
3. 看输出里的 `method` 列表，应能找到你自己的 `MainActivity.onCreate`。

### 🧪 实战 Lab（MOB15）
对自己 APK 跑上面两条命令，把 `dexdump` 里 `MainActivity` 相关的方法名贴给我。再说一句：DEX 和 class 文件（Java 的）是什么关系。

## 🔵 部分三·原理层
DEX（Dalvik Executable）是 Android 专为"内存小、读写慢"优化的字节码格式：它把多个 class 合并、共享常量池（节省空间），指令是 16 位定长（省内存）。ART（Android Runtime，4.4 后默认）在 App 安装时做"AOT 预编译"——把 DEX 直接编成机器码存盘，运行更快；旧 Dalvik 是"解释执行 + JIT"。`dexdump` 是官方反汇编器，列出每个方法的 Dalvik 指令。

## 🟣 部分四·深挖层
为什么安卓不用标准 JVM class？早期手机内存只有几十 MB，class 格式太占空间。DEX 把"常量池去重"做成全局共享，单 APK 体积骤降。多 DEX（65536 方法数上限，MultiDex）是历史坑：一个 DEX 最多 65535 个方法引用，大项目要拆 `classes2.dex`。顶级工程师懂这些才能在"包体优化/启动加速"上做文章。ART 的" profiles 引导优化（PGO）"是进阶：先收集常用路径，再针对性编译。

## 🔴 部分五·顶级视角
"字节码 + 运行时"模式普遍存在：JVM 的 class（JV1）、.NET 的 IL（MOB39）、Python 的 `.pyc`、WebAssembly（OTH5）。顶级工程师理解"中间表示（IR）"——任何语言都能降到 IR，再升到目标机器码（呼应 CMP6 SSA）。DEX 不过是 IR 家族一员的特化版。

## 🟠 部分六·安全/合规种子
白帽标注：本节所有命令只对自己的 APK。DEX 里**字符串常量明文可见**（你写死的 API 地址、密钥提示）是常见泄露源——这正是 MOB20 混淆要藏的。提醒：绝不用 `apkanalyzer/dexdump` 去探查他人 App 的内部结构（那是越线）。你练的是"读懂自己产物的构成"。

## ✅ 验收
交：① 解压后的 APK 目录截图/列表；② `dexdump` 中 MainActivity 方法名摘录；③ 说明 DEX 与 class 关系。

## ⚠️ 常见坑
- SDK 路径因人而异，先打开 Android Studio → Settings → Android SDK 看 Location 再拼路径。
- `dexdump` 输出很长，用 `| head` 或 `grep MainActivity` 缩小。
- 多 DEX 时可能不止一个 classes.dex，要逐个看。

## ➡️ 下一步
MOB16 · 反编译 jadx/apktool/smali——静态分析自己 App，看 Java 层，并理解 smali（Dalvik 汇编）。

---

# MOB16 · 反编译 jadx / apktool / smali

## 🎯 目标
学完这节，你能用 jadx 看回 Java 层源码、用 apktool 反编译/重打包资源、读懂 smali（Dalvik 汇编）改一行再回编。全程只对自己 App。理解"静态分析"三件套的分工。

## 📋 小白前置
MOB14、MOB15（已会 jadx 看自己 App、懂 DEX）。

## 🟢 部分一·最浅层
jadx 像"高中译回初中"——把难懂的字节码翻成你能读的 Java；apktool 像"拆装大师"——把整个 APK 拆成资源+smali，你改完还能装回去；smali 是"最底层方言"，像汇编一样一行一个动作，jadx 看不清的细节（如某寄存器赋值）要在 smali 里找。

## 🟡 部分二·动手层（逐字操作，只对自己 App）
1. jadx（已装）：打开自己 APK，Export Gradle/Gradle Frankenstein 不需要，直接看 `MainActivity`。
2. 装 apktool（GitHub `iBotPeaches/Apktool`，下载 jar + `apktool.bat` 包装）。反编译：
```bash
java -jar apktool.jar d demo.apk -o demo_out
```
看 `demo_out/smali/com/.../MainActivity.smali`。
3. 改一行 smali 再回编（示例：把 `Toast` 显示的常量字符串改掉），然后：
```bash
java -jar apktool.jar b demo_out -o demo_rebuilt.apk
```
（回编出的 APK 需重签名才能装，见后面发布节；本节只练"拆-看-改-回编"流程，不强制安装。）

### 🧪 实战 Lab（MOB16）
在 smali 里找到 MOB8 那条 Toast 的字符串，记下行号和它所在的 `const-string` 指令。把这段 smali 贴给我，并说明"smali 和 jadx 看到的有什么不同（信息量差异）"。

## 🔵 部分三·原理层
jadx = DEX → Java 反编译器（基于 dex2jar 思路 + 自研 IR）。apktool = 把 `resources.arsc`、XML、DEX 全反编译，DEX 部分转成 smali（而非 Java）。smali 是 Dalvik 字节码的文本表示：`const-string v0, "你好"` 是把字符串装寄存器 v0；`invoke-virtual` 是调虚方法。回编时 apktool 把 smali 重新汇编成 DEX。注意：jadx 的 Java 是"近似还原"，smali 才是"一字不差的真身"。

## 🟣 部分四·深挖层
为什么需要 smali 而不是只用 jadx？因为 jadx 在"控制流复杂/被混淆"时会还原失败或出错，而 smali 永远准确（它本就是字节码的直译）。修改逻辑（如绕过某判断）常需在 smali 改 `if-eqz`（等于则跳）这类分支指令。顶级逆向/白帽工程师把 smali 当"汇编"来读；但请记住：这套本领**只用于自己产物与授权样本**。

## 🔴 部分五·顶级视角
"反汇编/反编译"是方向7（SEC27–SEC30）的通用技能：IDA 反编译原生 so、Ghidra 反编译 ELF、jadx 反编译 DEX，本质同一套"字节码→可读代码"技术。顶级工程师能在三者间切换，因为底层都是"指令流→控制流图→高级语义"的重建。

## 🟠 部分六·安全/合规种子（再敲红线）
本节的"改 smali 回编"练习，**仅限你自己的 App**（例如你故意在 App 里放一个"测试开关"，然后自己改 smali 关掉它，体验"白帽自查"）。**严禁**对他人 App 做 apktool 反编译+重打包（这是盗版/破解的典型手法，违法）。如果你将来做 CTF 的 Android 题，那是主办方授权的样本，合法。

## ✅ 验收
交：① 你找到的 Toast 对应 smali `const-string` 片段；② 说明 smali 与 jadx 的信息量差异；③ 复述"改 smali 只对自己 App"。

## ⚠️ 常见坑
- apktool 回编需 jdk 的 `java` 在 PATH；版本不匹配会报 minSdk 错，换匹配版本。
- 回编 APK 必须重签名（用 `apksigner` 或 `jarsigner`）才能安装，否则 `INSTALL_PARSE_FAILED`。
- 别在他人 App 上重复本节动作——红线。

## ➡️ 下一步
MOB17 · Frida 动态分析（hook 自己 App）——运行时"拦截/修改"自己 App 的函数调用。

---

# MOB17 · Frida 动态分析（hook 自己 App）

## 🎯 目标
学完这节，你能用 Frida（动态插桩框架）在**自己 App 运行时**拦截一个函数、打印它的参数、甚至改它的返回值。这是白帽检测"自己 App 是否能被 hook"的核心能力。

## 📋 小白前置
MOB14–MOB16（逆向边界 + 静态分析）。需在电脑装 Python（方向1a 已学）与一台模拟器/真机（MOB1）。

## 🟢 部分一·最浅层
Frida 像"潜入话剧现场的替身演员"：戏正演到"主角说台词"这句，Frida 悄悄把演员换掉——让他说你写的词、或把他的台词记下来。它不拆剧本（静态），而是趁演出时实时动手。你只能在"自己剧团演的戏（自己 App）"里换演员。

## 🟡 部分二·动手层（逐字操作，只对自己 App）
1. 电脑装 Frida：`pip install frida frida-tools`（Git Bash 跑）。
2. 模拟器里装 `frida-server`：去 GitHub `frida/frida` releases 下载匹配架构（x86 模拟器选 `frida-server-xx-android-x86.xz`），解压 `adb push` 进 `/data/local/tmp`，`chmod 755` 后运行。
3. 对自己 MOB8 登录 App 写脚本 `hook.js`：
```javascript
Java.perform(function () {
  var Main = Java.use("com.example.myapp.MainActivity");
  Main.字符串FromJNI.implementation = function () {
    console.log("原方法被调用！");
    return "已被 Frida 替换的返回值";
  };
});
```
4. 注入：`frida -U -f com.example.myapp -l hook.js --no-pause`
5. 触发该函数，看终端打印与原返回值变化。

### 🧪 实战 Lab（MOB17）
对自己的 App 写脚本 hook 一个你自己定义的方法（如 `计算总价`），打印入参并把返回值 +100。运行截图/贴终端输出给我。重点：体验"运行中被改"的感觉。

## 🔵 部分三·原理层
Frida 用"注入 + 动态重写"：它把 `frida-agent` 注入目标进程，在运行时通过修改"函数入口指令"把原实现替换为你的 JS 回调（类似 inline hook，SEC50，但 Frida 用跨平台抽象）。`Java.perform` 确保在 ART 环境就绪后操作；`Java.use` 拿到类引用，覆写 `.implementation` 即替换方法体。它还能 hook native（Interceptor.attach 到 so 地址），后面 MOB18 用。

## 🟣 部分四·深挖层
Frida 的"动态插桩"比"改 smali 重打包"更轻量：不用重新签名、即时生效、可脚本化。它支持 spawned（启动即注入）和 attached（附加到已运行进程）两种模式。顶级白帽用它做"自动化检测"：写脚本批量验证自己 App 的"敏感函数"是否在不该被调用的时机被调用。但 Frida 也是作弊/外挂常用工具——**正因如此，你更要明白它的原理，才能在自己 App 里检测并防御它**（MOB22）。

## 🔴 部分五·顶级视角
"动态插桩/ Hook"是跨生态通用技术：Windows 的 Detours、Linux 的 LD_PRELOAD、macOS 的 method swizzling（MOB26）、游戏 CE/Unity（MOB48）。顶级工程师视 hook 为"观察与测试的显微镜"——在授权范围内，它是定位 bug、做无侵入式监控的利器。

## 🟠 部分六·安全/合规种子（最高频红线）
本周红线重申：**Frida 只在你自己编译的 App / 授权靶场 / CTF 样本中使用**。绝不：
- 对微信、某游戏、某银行 App 注入 Frida；
- 写"自动改他人 App 数值"的外挂脚本；
- 用 Frida 绕过他人付费/风控。
你练 Frida 的正当目的只有两个：① 理解 hook 原理；② 检测**自己** App 的薄弱点并加固（MOB22）。越线即违法，技术强也不碰。

## ✅ 验收
交：① 对自己 App 的 hook 脚本 `hook.js`；② 终端显示原方法被调用 + 返回值被改的截图；③ 复述 Frida 使用红线。

## ⚠️ 常见坑
- 模拟器没装/没启动 frida-server → `failed to spawn`；先 `adb shell` 确认 agent 在跑。
- 包名写错 → `process not found`；包名在 Manifest 的 `package=` 看。
- 真机需 root 或 frida-gadget 注入（更复杂），新手先用模拟器。

## ➡️ 下一步
MOB18 · 动态调试 / IDA（原生 so）——对自己写的 C++ so 做动态调试，看寄存器。

---

# MOB18 · 动态调试 / IDA（原生 so）

## 🎯 目标
学完这节，你能对自己在 MOB12 写的 `.so`（C++ 原生库）做动态调试：下断点、单步、看寄存器与内存。工具用 IDA Free（免费版）或 Ghidra（开源）。全程只对自己 so。

## 📋 小白前置
MOB12（写过 so）、MOB17（Frida 基础）。建议先看方向7 SEC28（汇编级调试）建立概念。

## 🟢 部分一·最浅层
调试 so 像"给工厂流水线装监控摄像头"：流水线（CPU 执行指令）每走一步，摄像头拍下"当前机器状态（寄存器里是什么数字、内存里存了什么）"。你能在某道工序（函数）前喊"停！"，慢慢看它怎么加工的。这只是看**你自己工厂**的流水线。

## 🟡 部分二·动手层（逐字操作，只对自己 so）
1. 从自己 APK 解压出 `lib/x86/libnative-lib.so`（模拟器用 x86 版）。
2. 用 IDA Free 打开该 `.so`：左侧 Functions 窗口找 `Java_..._字符串FromJNI` 或你的斐波那契函数。
3. 看反编译（F5，若 IDA Free 支持）出的伪 C 代码，对照你写的源码。
4. （进阶）用 `gdb`/`lldb` 附加模拟器进程调试：
```bash
adb forward tcp:23946 tcp:23946   # 若用 gdbserver
```
（完整 so 动态调试需 NDK 的 `gdbserver` + ndk-gdb，新手先完成"IDA 静态看伪代码"即过关。）

### 🧪 实战 Lab（MOB18）
用 IDA 打开自己 `libnative-lib.so`，找到你的斐波那契函数，把 IDA 反编译出的伪 C 截图发我，并对照"和你在 MOB12 写的 C++ 像不像"。

## 🔵 部分三·原理层
`.so` 是 ELF 格式（方向7 SEC25）：含 `.text`（机器码）、`.symtab`（符号表，debug 版保留函数名）、`.dynamic`（动态链接信息）。IDA 加载后做"反汇编"（机器码→汇编）和"反编译"（汇编→伪 C）。调试时，调试器通过 `ptrace` 系统调用挂到进程，控制执行流、读写寄存器（呼应 OS10 系统调用、ASM4 栈帧）。

## 🟣 部分四·深挖层
为什么 native 比 Java 难逆向？因为 C++ 编译后符号默认 strip、无类型信息、无字节码结构——IDA 只能靠"控制流分析 + 启发式"猜出伪 C（这可能不准确，需人工校正）。这也是 MOB12 说"把算法放 so 增加难度"的原因。但过度依赖 so 会引入内存漏洞（溢出 SEC34），且分析自己 so 也能帮助发现这些 bug——白帽视角是"双向的"。

## 🔴 部分五·顶级视角
"二进制逆向/调试"是方向7 整条 SEC23–SEC55 的主干。IDA/Ghidra 反编译思想通用：x86 so、ARM so、Windows PE、macOS Mach-O 都能分析。顶级工程师在"没有源码只有二进制"时（如第三方闭源库出 bug、固件 MOB53）靠这套本领定位问题。

## 🟠 部分六·安全/合规种子
白帽边界：你调试的是**自己编译的 so**。绝不调试/逆向他人 App 的 so（如某游戏的反作弊逻辑、某付费库的校验）。IDA 本身是正经安全工具（官方授权使用），但用它分析他人产物 = 越线。你练的是"理解 native 层发生了什么、以及它为何更难保护"。

## ✅ 验收
交：① IDA 打开自己 so 的截图（含函数列表）；② 斐波那契伪 C 与源码对照说明。

## ⚠️ 常见坑
- 要用 debug 版 so（未 strip）才看得到函数名；release 版 strip 后只剩地址。
- 模拟器 x86 so 与真机 ARM so 不同，别拿错架构文件。
- IDA Free 功能有限，伪 C（F5）新版已支持，老版需 Ghidra 替代。

## ➡️ 下一步
MOB19 · 脱壳 / 对抗（加固类型，仅分析）——认识商业加固（360/腾讯/梆梆）的原理与"壳"的分类，只分析不实践绕过。

---

# MOB19 · 脱壳 / 对抗（加固类型，仅分析）

## 🎯 目标
学完这节，你建立"加固/壳"的知识框架：知道市面商业加固（360 加固保、腾讯乐固、梆梆、爱加密）在做什么、壳的几种类型（加载型/抽取型/VM 型），**仅作原理分析，不实践绕过他人产物**。

## 📋 小白前置
MOB15–MOB18（DEX 结构、Frida、so 调试）。

## 🟢 部分一·最浅层
"加固"像给 App 套一层保险箱：原本裸露的说明书（DEX）被锁进箱子，运行时才由箱子里的"钥匙程序（壳）"临时解锁给手机看。小偷（逆向者）拿到的是空箱子。但保险箱再贵也挡不住"厂家自己有钥匙"——所以加固是"提高成本"，不是"绝对防住"。

## 🟡 部分二·动手层（观察自己，不绕过）
1. 用自己 MOB8 的 App，去任意一家**免费版**加固平台（仅对自己测试包）做一次加固，再 jadx 打开加固后的 APK，观察：原本的 `MainActivity` 是否变成了壳的 `StubApplication`/代理类。
2. 记录现象（不尝试脱壳）："我自己的 App 加固后，jadx 里看不到我原来的业务代码了，只看到壳的加载逻辑。"

### 🧪 实战 Lab（MOB19）
写一段"加固前后对比笔记"：① 加固前 jadx 能看到哪些你的类；② 加固后看到的是什么；③ 你理解壳在"启动时做了什么"。发我笔记。全程只对自己测试包。

## 🔵 部分三·原理层
壳分三代：① 加载型（落地加载）：把真 DEX 加密存 assets，运行时解密到内存 `ClassLoader` 加载（可被内存 dump 取出）；② 抽取型：把方法体抽走，运行时回填（反编译看到空方法）；③ VM 型（如梆梆）：把指令换成自研虚拟机字节码，执行时自研解释器翻译（最难）。加固常在 native 层做解密，所以绕壳常要结合 MOB18 的 so 调试。

## 🟣 部分四·深挖层
加固与"加壳"同源：Windows 时代的 UPX（SEC31）、Linux 的 packer，都是"压缩/加密 + 自解压头"。移动端因"DEX 明文在内存"的天然弱点，壳的核心战场在"对抗内存 dump + 反调试 + 反 Frida"。顶级攻防研究者会写"脱壳机"，但那属于高难逆向，**且只对授权样本/CTF 合法**。

## 🔴 部分五·顶级视角
"对抗式安全"是猫鼠游戏：加固方加壳、检测方（Frida/调试器）想绕、加固方再加反绕过。顶级工程师理解这是"防御成本 vs 攻击成本"的博弈，最优解是"纵深防御 + 服务端兜底"（回顾 MOB5）。在方向7 SEC31/SEC60/SEC66 你会系统学脱壳与反调试原理。

## 🟠 部分六·安全/合规种子（关键：只分析不实践）
本节**明确只讲原理、不要求也不鼓励你实践"绕过他人加固"**。你自己做加固（对自己 App）是合法的安全实践；但"脱他人 App 的壳"是侵权/违法的灰色甚至黑色行为，绝不碰。你学这节的目的：① 知道市面有哪些保护；② 评估"我要不要给自己的 App 加固"（MOB20）；③ 理解攻击者思路以更好防御。

## ✅ 验收
交：① 加固前后对比笔记；② 能说出壳的三种类型；③ 明确"只分析、不对他人产物实践绕过"。

## ⚠️ 常见坑
- 免费加固平台也可能收集你的包信息，仅用自己无价值的测试包。
- 加固后调试/热更新可能受影响，正式前充分测试。
- 把"我会脱壳"当炫耀 = 危险信号，本路线坚决不培养这种心态。

## ➡️ 下一步
MOB20 · 加固 / 混淆评估（白帽评估）——用 R8/ProGuard 给自己 App 做混淆与资源压缩，并评估强度。

---

# MOB20 · 加固 / 混淆评估（白帽评估）

## 🎯 目标
学完这节，你能用 Android 官方的 R8（含 ProGuard 规则）给**自己 App** 做代码混淆、资源压缩、移除无用代码；并学会"评估混淆强度"（白帽视角：自己产品抗逆向能力自评）。

## 📋 小白前置
MOB13（Gradle）、MOB14–MOB16（看懂反编译结果）。

## 🟢 部分一·最浅层
混淆像"把自己家装修图纸上的房间名全改成 A/B/C"：小偷拿到图纸也看不懂哪是金库。R8 就是官方"改名 + 拆墙（删没用的房间）"工具。它不是保险箱（MOB19），只是让看图纸的人头大——但配合加固，效果加倍。

## 🟡 部分二·动手层（逐字操作，只对自己 App）
1. 在 `app/build.gradle` 的 `buildTypes` 里开启：
```gradle
release {
    minifyEnabled true          // 开启 R8 混淆
    shrinkResources true        // 压缩无用资源
    proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
}
```
2. 打 release：`gradlew assembleRelease`。
3. 对生成的 release APK 用 jadx 打开，对比 debug 版：类名变成 `a.b.c`、方法名变 `a()`。

### 🧪 实战 Lab（MOB20）
对自己 App 分别打 debug 与 release，都 jadx 打开，把"同一个功能函数在两版的命名差异"截图发我。再在 `proguard-rules.pro` 里加一行保留规则：
```
-keep class com.example.myapp.MainActivity { *; }
```
重新打 release，确认 MainActivity 没被混淆。

## 🔵 部分三·原理层
R8 是"收缩器 + 混淆器 + 优化器 + 转换器"四合一：它先做"可达性分析"删掉没被调用的代码（shrink），再把类/方法/字段名换成短随机名（obfuscate），并做内联等优化。ProGuard 规则控制"哪些不能动"——比如反射调用的类、native 方法（JNI 函数名必须和 C++ 对应，MOB12）、序列化实体，若被混淆就找不到，需用 `-keep` 保护。

## 🟣 部分四·深挖层
混淆的局限：① 字符串常量仍明文（如 API 地址、密钥提示）——可用"字符串加密"类商业方案补；② 控制流结构仍在，高手能还原逻辑；③ 资源名（res 里的 id）默认不混，需 `shrinkResources` + 资源混淆（如 AndResGuard）。顶级工程师做"混淆强度自评"：用 jadx 看自己 release 包，问"一个外人在无任何上下文时，能否读懂核心逻辑？"——能，则强度不足。

## 🔴 部分五·顶级视角
"代码保护/混淆"是软件供应链安全（SEC84、OPS46）的一环：开源依赖也可能被投毒，自家敏感逻辑要保护。混淆思想跨语言存在：.NET 的 ConfuserEx、Unity 的 IL2CPP、JS 的 terser/minify、Python 的 pyarmor。顶级工程师把"保护策略"和"可调试性"平衡——过度混淆会让线上排障变难。

## 🟠 部分六·安全/合规种子
白帽评估的边界：你给自己 App 做混淆/加固，是合法且推荐的安全实践。但本节评估的**对象只能是你自己的产品**；你"评估混淆强度"不是为了去破解别人，而是为了"把自家产品做得更安全"。绝不下载他人 App 测其混淆强度、更不借机逆向。

## ✅ 验收
交：① debug/release 同函数命名差异截图；② 加了 `-keep` 后 MainActivity 保留的验证；③ 一句话"混淆的局限"。

## ⚠️ 常见坑
- native 方法名被混淆 → `UnsatisfiedLinkError`；JNI 相关类务必 `-keep`。
- 用反射/序列化/Gson 的 data class 被混淆 → 运行时找不到；需 keep 或加 `@Keep` 注解。
- release 包崩溃但 debug 正常，第一反应查混淆规则。

## ➡️ 下一步
MOB21 · SSL Pinning 绕过（仅自己 App / 授权）——理解证书绑定原理，并只在自己 App 上演示"为何需要它"。

---

# MOB21 · SSL Pinning 绕过（仅自己 App / 授权）

## 🎯 目标
学完这节，你理解 HTTPS 的中间人（MITM）原理、为何 App 要做"证书绑定（SSL Pinning）"、以及 Frida 如何在**自己 App** 上演示"若没绑定会被抓包"。全程只对自己 App / 授权测试样本。

## 📋 小白前置
MOB10（HTTPS 网络）、MOB17（Frida）。建议看方向4 NET9（TLS）、方向7 SEC9/SEC16。

## 🟢 部分一·最浅层
HTTPS 像"带封条的保密信封"。但如果你雇的"邮差"（代理如 Charles/Burp）自己造了个假封条，而你手机又信任了他，邮差就能拆开看信。证书绑定就是"我只认这一枚特定封条，别的都不信"——哪怕手机信任了假邮差，信封也对不上。

## 🟡 部分二·动手层（只对自己 App，演示原理）
1. 在自己 MOB10 的 App 上，用 Charles 设代理抓包：默认情况下**若你手机装了 Charles 根证书并信任**，能看到明文 JSON（证明没绑定的风险）。
2. 给自己 App 加官方证书绑定（OkHttp + `CertificatePinner`）：
```kotlin
val pinner = CertificatePinner.Builder()
    .add("api.example.com", "sha256/AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=")
    .build()
```
（真实值用你域名的证书指纹；练习可先用占位并观察"绑定后 Charles 抓不到"的现象。）
3. 用 Frida 在自己 App 上临时禁用校验（仅演示）：
```javascript
// 仅自己 App：hook SSLContext 的 checkServerTrusted 让它放行
```
观察：禁用后 Charles 又能抓到——说明"客户端校验可被绕过，故关键校验必须在服务端"。

### 🧪 实战 Lab（MOB21）
写一段说明发我：① 没绑定时 Charles 能否抓到你 App 的包；② 加了绑定后能否抓到；③ 用 Frida 在自己 App 上临时绕过后能否抓到；④ 结论"客户端校验不可信"。全程只对自己 App。

## 🔵 部分三·原理层
TLS 握手时，客户端验证服务器证书链是否由"受信任 CA"签发。MITM 代理用自签 CA 证书，若手机"用户安装的根证书"里信任了它，验证就过（这是为何 Android 7+ 默认不信任用户 CA，需 App 显式 `networkSecurityConfig` 允许）。Certificate Pinning 是"再额外校验证书公钥指纹是否在我预设名单"，绕过它需 hook `checkServerTrusted` 或 `OkHttp CertificatePinner` 的校验函数——这正是 Frida 擅长的。

## 🟣 部分四·深挖层
为什么绑定也不绝对安全？因为 App 跑在用户设备上（MOB5 信任边界），Frida 能 hook 掉任何校验。所以业界共识：Pinning 只是"提高抓包门槛、防普通嗅探"，真正的安全靠"服务端鉴权 + 业务层加密 + 行为风控"。iOS 的 `NSPinning`、Android 的 `networkSecurityConfig` 是平台方案。顶级白帽用 Pinning 检测来验证"自己 App 在普通代理下不泄密"，而不是去破解他人。

## 🔴 部分五·顶级视角
"信道安全 vs 端点安全"是安全架构的永恒主题：TLS 保护传输、但端点（客户端/服务器内存）仍可能被攻破。顶级工程师把"纵深防御"用到极致——传输加密 + 证书绑定 + 请求签名 + 服务端二次校验。此思路贯通 Web 安全（CSP、HSTS）、API 安全（SEC21）、物联网（MOB53）。

## 🟠 部分六·安全/合规种子（红线再申）
**SSL Pinning 绕过技术，只许在自己 App / 明确授权的测试样本上做**。用途是：① 理解 MITM 风险；② 验证自己 App 的 Pinning 生效；③ 做安全评估报告。绝不：用 Frida 绕过**他人** App 的 Pinning 去抓包分析其接口、盗数据、造外挂。那是违法的"未经授权访问计算机信息系统"。今天所有操作对象都是你自己。

## ✅ 验收
交：① 自己 App 的抓包对比结论（无绑定/有绑定/Frida 绕过）；② 复述"客户端校验不可信，关键在校验放服务端"；③ 明确红线。

## ⚠️ 常见坑
- Android 7+ 默认不信任用户 CA，Charles 抓不到时先确认手机已正确安装并"信任"根证书（仅测试机）。
- 真机抓包要设代理 + 装证书；模拟器可直接 `adb` 配。
- 别在他人 App 上重复本节 Frida 绕过动作。

## ➡️ 下一步
MOB22 · Root 检测对抗（评估，非教学规避）——写"检测自己 App 是否在 root 环境运行"的代码，作为白帽自评。

---

# MOB22 · Root 检测对抗（评估，非教学规避）

## 🎯 目标
学完这节，你能给自己 Android App 写一套"运行环境自检"代码：检测是否 root、是否跑在模拟器、是否挂了 Frida/调试器；并理解其边界（只能提高门槛，不能绝对防）。这是白帽"加固评估"的收尾。

## 📋 小白前置
MOB17（Frida）、MOB20（混淆）、MOB21（Pinning）。

## 🟢 部分一·最浅层
Root 检测像"银行柜员看顾客神色"：发现对方戴了面具（root 权限）、在拍你操作（调试器）、身后还有个导演（Frida），就提高警惕（限制敏感功能）。但高手能化妆骗过柜员——所以检测只是"多一道门"，不是"铁门"。

## 🟡 部分二·动手层（只对自己 App 写检测代码）
在 Kotlin 里加几个自检函数（复制即可，护手）：
```kotlin
fun 是否Root(): Boolean {
    val 路径 = arrayOf("/system/app/Superuser.apk", "/sbin/su", "/system/bin/su")
    return 路径.any { java.io.File(it).exists() }
        || try { Runtime.getRuntime().exec("which su").inputStream.bufferedReader().readLine() != null }
           catch (e: Exception) { false }
}
fun 是否调试中(): Boolean = android.os.Debug.isDebuggerConnected()
```
在 `onCreate` 里若 `是否Root()` 为真，就 Toast 提示"检测到风险环境"并限制某些功能（你自己定义）。

### 🧪 实战 Lab（MOB22）
对自己 App 集成上面检测，运行看日志。再写一段笔记：列出"你还知道哪些 root 特征"（如 Magisk 路径、buildTags=test-keys、ro.debuggable）。发我笔记。重点：理解"检测=提高门槛"。

## 🔵 部分三·原理层
Root 检测本质是"找越权痕迹"：① 文件特征（su 二进制、Magisk 模块目录）；② 系统属性（`getprop ro.debuggable`）；③ 运行时检测（是否能执行特权命令、是否被 ptrace 附加）。调试器检测靠 `ptrace(PTRACE_TRACEME)` 自附加（已被 trace 则失败）或读 `/proc/self/status` 的 TracerPid。Frida 检测可查端口（27042）或 `/proc` 下 frida 相关 maps。

## 🟣 部分四·深挖层
为什么"检测总能被绕过"？因为检测代码本身就在攻击者控制的设备内存里，他可 hook 你的 `是否Root()` 让它永远返回 false（MOB17 刚学过）。于是业界做"检测代码混淆 + 多处校验 + 服务端行为风控"组合。顶级白帽工程师写"自检 SDK"时，会把检测点分散、加密、并配合"服务端异常行为识别"，而非依赖单一客户端判断。

## 🔴 部分五·顶级视角
"环境完整性校验"是零信任（Zero Trust）思想在端上的体现：不默认设备可信，持续验证。它和后端"设备指纹/风控"、容器" attestation（证明）"、云"机密计算"同源。顶级工程师明白：端上的任何校验都是"概率性威慑"，真正的安全网在后端。

## 🟠 部分六·安全/合规种子
白帽定位：你写 root 检测，是为了**保护你自己用户的数据安全**（比如 root 环境更易被恶意软件窃取你的 App 里的令牌），这是正当的安全工程。但**绝不**把检测技术用于"破解他人 App 的反作弊/反 root 机制"——那是侵权对抗，越线。本节的"对抗"指"理解攻击者如何想绕过你的检测"，以便你自己做得更好。

## ✅ 验收
交：① 自己 App 的 root/调试检测代码片段；② "root 特征"笔记；③ 说明"检测只能提高门槛"。

## ⚠️ 常见坑
- 误把"检测到 root 就崩溃"当防护——这会误伤正常 root 用户且易被绕过，正确做法是"降级敏感功能"。
- 真机测试 root 检测需真实 root 环境，新手可在模拟器体验基础逻辑即可。
- 别为了"防破解"过度收集用户设备信息，注意隐私合规（MOB4）。

## ➡️ 下一步
MOB23 · iOS 入门 / Swift——进入 iOS 世界，学 Swift 语法（仅讲不练黑苹果；无 Mac 则只学语法概念）。

---

# MOB23 · iOS 入门 / Swift

## 🎯 目标
学完这节，你理解 Swift 语言的核心：类型安全、可选值（Optional）、结构体 vs 类、协议（Protocol）；知道 iOS App 怎么开发（Xcode + SwiftUI）。**本路线不要求你装黑苹果/破解 macOS，无 Mac 时只学语法思维**。

## 📋 小白前置
MOB6（Kotlin，对比学更快）。方向1b 的 JV2/CPP2（类/继承）有助。

## 🟢 部分一·最浅层
Swift 像"更严格的 Kotlin"：它也管"空盒子"（Optional），但更激进——默认任何东西都可能"有值/没值"必须你写明。它把"值类型（结构体，复制）"和"引用类型（类，共享）"分得清清楚楚，像"复印件 vs 原件"——结构体是复印件，改了不影响别人；类是原件，大家共看一份。

## 🟡 部分二·动手层（有 Mac 可练；无 Mac 仅阅读）
若你日后有 Mac，安装 Xcode（App Store 官方，免费），新建 iOS App → 语言 Swift → 在 `ContentView.swift` 写：
```swift
struct 账单 {
    var 项目: String
    var 金额: Double
}
var a = 账单(项目: "咖啡", 金额: 18)
var b = a          // 值拷贝，b 与 a 互不影响
b.金额 = 30
print(a.金额, b.金额)   // 输出 18.0 30.0
```
无 Mac 时，把这段贴进 Playground 在线工具（如 swiftfiddle.com）理解即可。

### 🧪 实战 Lab（MOB23）
用你自己的话对比 Kotlin 的 `data class` 和 Swift 的 `struct` 有什么相似（都可存数据、都值类型）。写两句发我。无 Mac 不强制运行。

## 🔵 部分三·原理层
Swift 编译到"原生 ARM 机器码"（区别于 Kotlin 跑 ART），所以 iOS App 性能更接近 C++。`Optional<T>` 本质是枚举 `enum { none; some(T) }`，编译器强制你 `if let`/`guard let` 解包，从类型层面消灭空指针（呼应 Kotlin 空安全 MOB6）。`struct` 默认在栈/内联、拷贝语义；`class` 在堆、引用语义、支持继承。协议（Protocol）类似 Java 接口/Kotlin 接口，是 Swift 的"组合优于继承"基石。

## 🟣 部分四·深挖层
Swift 由 Apple 2014 年发布，目标是取代 Objective-C（MOB26）。它的"值类型优先"哲学影响了 Rust（RS2 所有权）、Kotlin 的 `data class`。Swift 还有"面向协议编程（POP）"——用协议+协议扩展替代深继承树。顶级工程师欣赏 Swift 在"安全（强类型/可选）"与"性能（值类型/ARC）"间的平衡。注意：Apple 生态强绑定，且**开发必须 macOS + 官方 Xcode，不鼓励也不教学黑苹果/破解**。

## 🔴 部分五·顶级视角
"类型系统即文档/即防护"是顶级工程师的信条：Swift 的 Optional、Kotlin 的空安全、Rust 的所有权、Haskell 的类型魔法，都在用编译器替你把 bug 挡在运行前。学一门语言，先学它的"类型哲学"，比记语法更重要。Swift 与 Kotlin 的高度相似也说明：现代语言在"安全+表达力"上已收敛到同一套最佳实践。

## 🟠 部分六·安全/合规种子
合规明确：iOS 开发只走 Apple 官方渠道（Mac + Xcode + 开发者账号，均需正版/正规）。本路线**不教学黑苹果安装、不提供破解 Xcode/绕过 Apple 签名**，既违法又破坏生态。你学 Swift 是为了"懂 iOS 平台的安全特性与开发范式"，将来若做白帽 iOS 分析，也只针对**自己账号下、或授权的测试设备/样本**（MOB29）。

## ✅ 验收
交：① Kotlin data class 与 Swift struct 的对比两句；② 说明 Optional 是什么（有值/无值的枚举）。

## ⚠️ 常见坑
- 无 Mac 时别陷入"怎么装黑苹果"——那是越线且低效，本路线只讲语法思维。
- `if let` 解包忘处理 nil 分支 → 编译报错，正是 Swift 在保护你。
- 混淆 struct/class 的拷贝语义 → 改 b 影响 a 的诡异 bug（只有 class 会）。

## ➡️ 下一步
MOB24 · SwiftUI / UIKit——iOS 两种 UI 框架：老牌命令式 UIKit 与新声明式 SwiftUI。

---

# MOB24 · SwiftUI / UIKit

## 🎯 目标
学完这节，你理解 iOS 的两种 UI 写法：UIKit（命令式，拖 Storyboard + 代码改控件）与 SwiftUI（声明式，像 Flutter/Compose）。无 Mac 时只建立概念对照。

## 📋 小白前置
MOB23（Swift 基础）、MOB2（声明式范式）、MOB8（Android 布局）。

## 🟢 部分一·最浅层
UIKit 像"管家式"：你亲手把每个家具（按钮）摆好、再写"点击后把文字换成 XX"。SwiftUI 像"拍立得"：你描述"界面该长这样（取决于状态）"，框架自动生成并随状态刷新。SwiftUI 和你在 MOB2 学的声明式是一家人。

## 🟡 部分二·动手层（有 Mac 时；无 Mac 阅读）
SwiftUI 最小例子：
```swift
struct ContentView: View {
    @State private var 计数 = 0
    var body: some View {
        VStack {
            Text("点了 \(计数) 次")
            Button("加一") { 计数 += 1 }
        }
    }
}
```
对比 UIKit（命令式）：
```swift
let 按钮 = UIButton()
按钮.setTitle("加一", for: .normal)
按钮.addTarget(self, action: #selector(点), for: .touchUpInside)
@objc func 点() { 计数 += 1; 标签.text = "点了 \(计数) 次" }
```

### 🧪 实战 Lab（MOB24）
用文字画出"同一个计数器界面"在两种范式下的"你分别要做什么"（UIKit：建控件+加事件+改文本；SwiftUI：声明状态+声明视图）。发我对照。无 Mac 不跑。

## 🔵 部分三·原理层
UIKit 基于 `UIView` 对象树，你持有引用并命令式修改属性；事件用 Target-Action 或 delegate 回调。SwiftUI 用"函数式视图 + 状态驱动"：视图是 `body` 计算属性，`@State` 变化触发"差异比对（diffing）"重绘只变更部分（呼应 MOB2 调和算法）。底层 SwiftUI 仍用 UIKit/AppKit 渲染，是"声明式 DSL 包装"。

## 🟣 部分四·深挖层
Apple 2019 推 SwiftUI，是受 React/Flutter 声明式浪潮影响。但 UIKit 仍是海量存量代码的基础，且 SwiftUI 早期 bug 多，所以"UIKit + SwiftUI 混用"是现实。顶级 iOS 工程师精通两者并能渐进迁移。Storyboard（可视化拖控件）是 UIKit 的配套，XIB/纯代码各有拥趸——团队大项目常偏纯代码以便 code review。

## 🔴 部分五·顶级视角
"声明式 UI"已成跨平台共识（MOB2 已点题）：SwiftUI、Jetpack Compose（Android）、Flutter、React、Vue 全同构。顶级工程师掌握"状态→视图"这一元模式后，换任何框架都只是学语法糖。UIKit 的命令式经验也不浪费——它让你理解"视图树与事件循环"的底层（和 Win32 消息循环 MOB38 同源）。

## 🟠 部分六·安全/合规种子
白帽种子（通用）：无论哪种 UI 框架，"界面上的限制（禁用按钮/隐藏功能）都不是安全边界"（回顾 MOB8）。敏感判断必须在业务逻辑/服务端。学 SwiftUI/UIKit 是为"懂 iOS 平台上自己 App 该怎么写才安全"，以及将来做 iOS 白帽评估时理解目标结构——但只针对自己/授权样本。

## ✅ 验收
交：① 两种范式的"你分别要做什么"对照；② 说明 SwiftUI 的"状态驱动重绘"是什么。

## ⚠️ 常见坑
- 无 Mac 别强求运行，概念对照已达标。
- SwiftUI 的 `@State` 必须在结构体里且 `private`，位置错会不刷新。
- 别把 SwiftUI "看起来简单"误解为"不用懂 UIKit"——面试/实战都要懂底层。

## ➡️ 下一步
MOB25 · 组件 / 生命周期——iOS 的 ViewController/App 生命周期，对比 Android（MOB3）。

---

# MOB25 · 组件 / 生命周期（iOS）

## 🎯 目标
学完这节，你理解 iOS 的 App/ViewController 生命周期（启动→前台→后台→终止），并与 Android 的 MOB3 对照，建立"跨平台生命周期"通用心智模型。

## 📋 小白前置
MOB3（Android 生命周期）、MOB23/MOB24。

## 🟢 部分一·最浅层
iOS 的 ViewController 像"一幕戏的导演"：戏开场（viewDidLoad 布景）、即将上场（viewWillAppear）、正在演（viewDidAppear）、要下场（viewWillDisappear）、下场了（viewDidDisappear）。App 本身也有"开场/退到后台/回前台/被杀"的全局剧本。

## 🟡 部分二·动手层（有 Mac；无 Mac 阅读）
UIKit 生命周期回调：
```swift
override func viewDidLoad() { super.viewDidLoad(); print("布景完成") }
override func viewWillAppear(_ a: Bool) { print("即将上场") }
override func viewDidAppear(_ a: Bool) { print("正在演") }
override func viewWillDisappear(_ a: Bool) { print("要下场，存进度") }
```
App 级在 `SceneDelegate`/`App` 里：`applicationDidEnterBackground`（进后台）、`applicationWillEnterForeground`（回前台）。

### 🧪 实战 Lab（MOB25）
画一张"时间轴"对照表：左列 Android（onCreate/onPause/onStop/onDestroy），右列 iOS（viewDidLoad/viewWillAppear/.../applicationDidEnterBackground）。发我这张对照表，标注"哪个阶段该存数据、哪个该释放相机"。

## 🔵 部分三·原理层
iOS 用"引用计数（ARC，Automatic Reference Counting）"管理对象生命周期：每个对象有引用计数，归零自动释放（和 Rust 的"无 GC 显式释放"思想不同，是编译期插入 retain/release）。ViewController 由"导航栈/窗口"持有，切走时引用减一。App 生命周期由 `UIApplication` 单例通过 delegate 回调通知你。

## 🟣 部分四·深挖层
iOS 与 Android 生命周期的差异根源：iOS 自诞生就是"单前台 + 严格后台"（墓碑机制，后台 App 常被冻结），Android 早期真多任务。所以 iOS 的 `applicationDidEnterBackground` 必须"快速保存现场"（你有约 5 秒，超时被杀）。顶级工程师做"状态持久化 + 后台任务延长（BGTask）"，和 Android 的 `onSaveInstanceState` 思路同构但时限更严。

## 🔴 部分五·顶级视角
"生命周期/资源管理"是跨范式元概念：Activity（MOB3）、ViewController、游戏场景（MOB47）、容器 Pod（OPS22）、协程作用域（MOB11）、RAII（CPP1）。顶级工程师把"何时申请/何时释放"当成肌肉记忆——这是防止内存泄漏与资源耗尽的第一原则。

## 🟠 部分六·安全/合规种子
白帽种子：生命周期回调是"清理敏感数据"的关键时点（回顾 MOB3 安全种子）——进后台时清内存里的令牌、停录屏。这是你自己 App 的合规实践。分析他人 App 的生命周期行为（如后台窃密）是黑产特征，我们只"懂原理以自检"，绝不模仿侵害。

## ✅ 验收
交：① Android↔iOS 生命周期对照时间轴；② 标注"何时存数据/释放资源"。

## ⚠️ 常见坑
- iOS 后台时限短，"在后台做重活"会被系统杀，别照搬 Android 思路。
- `viewDidLoad` 只调一次，别把"每次出现都要刷新的逻辑"放错位置（应放 viewWillAppear）。
- 无 Mac 时别纠结运行，对照理解即可。

## ➡️ 下一步
MOB26 · Objective-C 运行时——消息机制与方法交换（Method Swizzling），理解 iOS 底层动态性。

---

# MOB26 · Objective-C 运行时

## 🎯 目标
学完这节，你理解 Objective-C 的"消息发送（msgSend）"与"方法交换（Method Swizzling）"原理——这是 iOS hook 的底层机制，也是你理解自己 App 动态性的基础。**只讲原理与对自己代码演示，不用于他人 App**。

## 📋 小白前置
MOB23–MOB25（iOS 基础）。建议看过方向7 SEC48（Hook 总论）。

## 🟢 部分一·最浅层
Objective-C 调用方法不像"按地址打电话"，而像"发短信给对象说'请做XX'"，对象自己查通讯录（方法列表）找怎么做。好处是极其灵活——你能偷偷改通讯录（Swizzling）：把"接电话"换成"录音"，对象自己都不知道。这正是 iOS hook 的原理。

## 🟡 部分二·动手层（原理示意；有 Mac 可对自己类试）
```objc
// 原理示意：交换 originalMethod 与 swizzledMethod
Method m1 = class_getInstanceMethod([self class], @selector(原方法));
Method m2 = class_getInstanceMethod([self class], @selector(替换方法));
method_exchangeImplementations(m1, m2);
```
（仅对**你自己定义**的类演示；理解"调用原方法时实际走替换方法"即可。）

### 🧪 实战 Lab（MOB26）
用中文写清："消息机制"和"方法交换"分别是什么，各举一个生活比喻。发我。不强制写 OC 代码（无 Mac 不影响）。

## 🔵 部分三·原理层
OC 调用 `obj 方法]` 编译成 `objc_msgSend(obj, @selector(方法))`：运行时去对象的"方法列表（method list）"按 selector 找实现（IMP 函数指针），找不到就走"消息转发（forwarding）"链。Method Swizzling 就是改这张表的映射——把 selector A 指向 IMP B。这比 Android 的 Frida hook 更"原生"，是 iOS 生态 hook（如 JSPatch、越狱 tweak）的基石。

## 🟣 部分四·深挖层
"运行时可塑"是 OC 的设计哲学（源自 Smalltalk），Swift 虽更静态但仍能通过 `@objc` 暴露给运行时。代价：动态性带来安全隐忧——任意 Swizzling 能改系统行为。Apple 因此收紧（如禁用 `dlopen` 随机路径、加强代码签名）。顶级工程师理解：动态性=灵活+风险，要在"能力"与"安全"间权衡（呼应方向7 SEC48 Hook 用途边界）。

## 🔴 部分五·顶级视角
"消息/方法交换"思想跨生态：Android 的 Xposed（ART 方法替换）、Windows 的 IAT/Inline Hook（SEC49/50）、游戏的函数钩子（MOB48）、甚至 Web 的 monkey-patch。顶级工程师视"运行时替换"为双刃剑——自己写框架/测试用它做无侵入监控，但绝用它侵害他人。

## 🟠 部分六·安全/合规种子（重点红线）
**Method Swizzling 只在你自己的类/自己 App 里做教学演示**。绝不：写 tweak 注入他人 App、用 Swizzling 改系统/他人应用行为（如绕过付费、篡改第三方 SDK）。这类行为在越狱环境里常见，但属于侵权/违法的"未授权修改"。你学它，是为了：① 懂 iOS hook 底层；② 将来检测自己 App 是否被 Swizzling 攻击（反 hook 自检）。

## ✅ 验收
交：① "消息机制""方法交换"的中文解释+比喻；② 复述"Swizzling 只对自己代码演示"的红线。

## ⚠️ 常见坑
- 无 Mac 别卡在环境，原理理解达标。
- Swizzling 若交换了系统方法且没妥善还原，会导致全局诡异 bug——这也是为什么生产代码慎用。
- 把"我会 hook"当炫耀 = 危险信号，本路线坚决不培养。

## ➡️ 下一步
MOB27 · iOS 沙盒安全——理解 iOS 的权限隔离、数据保护与钥匙串。

---

# MOB27 · iOS 沙盒安全

## 🎯 目标
学完这节，你理解 iOS 的"沙盒"机制：每个 App 在自己的隔离目录、权限模型（相册/定位/通知的运行时授权）、数据保护（文件加密等级）、Keychain（凭据保险箱）。这是 iOS 白帽/合规的基座。

## 📋 小白前置
MOB4（Android 权限模型，对照学）、MOB23。

## 🟢 部分一·最浅层
iOS 沙盒像"每人一个带锁的独立公寓"：你的 App 只能进自己那间（Documents/Library/tmp），不能串门进别人公寓（别的 App 目录），也不能随便进物业机房（系统区）。要进别人公寓（如读相册）得先找业主（用户）要钥匙（授权弹窗）。

## 🟡 部分二·动手层（概念对照，有 Mac 可练）
在 `Info.plist` 里加权限描述（示意）：
```xml
<key>NSLocationWhenInUseUsageDescription</key>
<string>需要定位以提供附近服务</string>
```
代码请求：
```swift
let m = CLLocationManager()
m.requestWhenInUseAuthorization()
```
对比 Android 的 `requestPermissions`（MOB4）——思路一致，只是写法不同。

### 🧪 实战 Lab（MOB27）
列一张"iOS vs Android 权限模型对照表"：行是"定位/相册/相机/通讯录"，两列各写"怎么申请/用户能否撤回"。发我。无 Mac 不跑。

## 🔵 部分三·原理层
iOS 沙盒由"代码签名 + 沙盒 entitlement + 内核 MAC（Mandatory Access Control）"三层保障：App 签名保证来源，entitlement 声明它要的特权（如 iCloud），内核强制每个 App 只能访问自己容器目录。文件按"数据保护等级"加密（如 `NSFileProtectionComplete` 锁屏即不可读）。Keychain 用硬件密钥（Secure Enclave）存密码/令牌，比沙盒目录更安全。

## 🟣 部分四·深挖层
iOS 沙盒比 Android 更严格（早期 Android 各 App 同 UID 可互访，后改）。Apple 的"隐私营养标签""App Tracking Transparency（ATT，需弹窗问能否跟踪）"是监管驱动。顶级工程师重视"数据分级"：普通缓存放 tmp、用户文件放 Documents（会 iCloud 备份）、密钥放 Keychain、超敏感用 Secure Enclave。合规（GDPR/个保法）要求"最小化收集+明确告知"。

## 🔴 部分五·顶级视角
"沙盒/最小权限/凭据隔离"是安全架构的通用三件套：容器 namespace（OS15）、浏览器同源（FE15）、Linux 沙盒（seccomp，SEC45）、云 IAM（OPS18）全同构。顶级工程师把"隔离边界"当系统设计的起点——任何模块默认不可跨边界，需要才显式授权。

## 🟠 部分六·安全/合规种子
白帽合规：你写 iOS App 时，严格走官方授权流程（`Info.plist` 描述 + 运行时请求），**不**用私有 API 绕过授权（如直接调未公开接口读相册，那是违规上架甚至侵权）。分析他人 App 的沙盒越权行为属黑产特征，我们只"懂原理以自检 + 在授权靶场练习"。

## ✅ 验收
交：① iOS↔Android 权限对照表；② 说明 Keychain 比沙盒目录更安全的原因。

## ⚠️ 常见坑
- 忘了 `Info.plist` 的 usage description → 一调权限就崩。
- 把用户文件存 tmp（系统会清）→ 数据丢失；应分清 Documents/Library/Caches。
- 无 Mac 时别纠结环境，对照表达标。

## ➡️ 下一步
MOB28 · IPA / Mach-O——iOS 安装包与可执行文件结构（对比 Android APK/DEX）。

---

# MOB28 · IPA / Mach-O

## 🎯 目标
学完这节，你理解 iOS 的 IPA（本质也是 ZIP）与 Mach-O（可执行格式，对标 ELF/PE）的结构；知道"加密/瘦身后"的 App 长什么样。仅原理+对自己/授权样本分析。

## 📋 小白前置
MOB15（APK/DEX 结构，对照学）、MOB27。

## 🟢 部分一·最浅层
IPA 就像 APK 一样是个"压缩包"，里面装着 App 的执行文件（Mach-O，相当于 Android 的 DEX+so 合体）和资源。Mach-O 是 macOS/iOS 的"可执行文件格式"，如同 Windows 的 EXE（PE）、Linux 的 ELF——只是苹果家的"文件说明书"写法不同。

## 🟡 部分二·动手层（仅自己/授权样本；无 Mac 用在线资料）
若你有自己用开发者账号打包的 IPA：改后缀 `.zip` 解压，进 `Payload/xxx.app/` 看内容。用工具 `otool`（Mac 自带）看 Mach-O：
```bash
otool -l YourApp   # 看 load commands
file YourApp       # 看架构（arm64）
```
无 Mac 时，读 Apple 官方《Mach-O Programming Topics》建立概念即可。

### 🧪 实战 Lab（MOB28）
画一张"APK vs IPA 结构对照表"：APK 含 classes.dex/lib/arsc；IPA 含 Mach-O/Payload/签名。发我。说明 Mach-O 对应 Android 里的什么。

## 🔵 部分三·原理层
Mach-O 由"Header + Load Commands + Segments(__TEXT/__DATA) + Sections"组成。Load Commands 告诉内核怎么加载（如 `__PAGEZERO` 防护、代码签名段）。App Store 下载的 Mach-O 有"App Store 加密"（fairplay），需越狱/授权解密（**越狱破解他人付费 App 违法，绝不碰**）。开发者自己 archive 出的 IPA 是未加密的（你自己的可分析）。

## 🟣 部分四·深挖层
Mach-O 支持"通用二进制（Universal/Fat）"——一个文件含多架构（arm64/x86_64），按需选。这与 Android 的"多 ABI 多 so"同理。对比方向7 SEC24/25/26：PE（Windows）、ELF（Linux）、Mach-O（Apple）是三大可执行格式，逆向工程师都要懂。顶级白帽分析 Mach-O 用 Hopper/IDA/Ghidra，与分析 ELF 同一套手艺。

## 🔴 部分五·顶级视角
"可执行文件格式"是"程序如何被操作系统加载"的规范，是所有平台软件的基石。顶级工程师理解：无论 PE/ELF/Mach-O，核心都是"头部元信息 + 段/节 + 导入导出符号 + 重定位 + 签名"。学通一种，其余触类旁通（呼应 CMP9 链接装载）。

## 🟠 部分六·安全/合规种子
白帽边界：你分析 Mach-O 的对象只能是**你自己开发者账号打包的 IPA** 或**授权样本**。App Store 加密的付费 App 解密/破解 = 侵犯著作权，违法。本路线不教学越狱破解、不提供任何绕过 Apple DRM 的方法。你学结构是为了"懂 iOS 平台安全特性、能审计自己产物"。

## ✅ 验收
交：① APK↔IPA 对照表；② 说明 Mach-O 对标 Android 的什么（DEX+so）。

## ⚠️ 常见坑
- 无 Mac 别强求工具，对照表+概念达标。
- 别尝试"解密他人 App Store 包"——红线。
- Mach-O 的 `__PAGEZERO` 是安全段（捕获空指针解引用），不是无用。

## ➡️ 下一步
MOB29 · iOS 逆向（授权）——在授权范围内（自己 App / 越狱测试机 / CTF）理解 iOS 逆向流程，不针对他人商业 App。

---

# MOB29 · iOS 逆向（授权）

## 🎯 目标
学完这节，你建立"iOS 逆向的方法论地图"：砸壳（仅自己未加密包/授权样本）、class-dump 看 ObjC 类、Hopper/IDA 反编译、Frida/objc 注入（MOB30）。**全部只针对自己 App 或授权 CTF 样本，绝不逆向他人商业 App**。

## 📋 小白前置
MOB26（OC 运行时）、MOB28（Mach-O）、MOB14–MOB17（Android 逆向已打底）。

## 🟢 部分一·最浅层
iOS 逆向和 Android 逆向（MOB14–MOB18）是同一门手艺换平台：Android 拆 DEX，iOS 拆 Mach-O；Android 用 jadx，iOS 用 Hopper/IDA；Android hook 用 Frida/Java，iOS hook 用 Frida/objc。你已会一半，这节是"换地图"。

## 🟡 部分二·动手层（仅自己 App，有 Mac+越狱测试机/或自签未加密包）
对自己开发者账号打包的（未加密）App：用 `class-dump` 导出头文件看类结构（公开练习）：
```bash
class-dump -H YourApp -o 头文件目录
```
看导出的 `.h`，理解你自己 App 的类和方法布局。

### 🧪 实战 Lab（MOB29）
写一段"iOS 逆向合规边界声明"发我：明确你会/不会做什么。再简述"对自己 App 用 class-dump 看头文件"这一步的意义（验证自己代码结构是否如预期）。无 Mac 不强制运行。

## 🔵 部分三·原理层
iOS 逆向工具链：① 砸壳（dumpdecrypted）取出未加密 Mach-O——但官方 App Store 包加密，砸壳他人付费 App = 违法，故**只对自己未加密包/授权样本**；② class-dump 解析 Mach-O 的 OC 元数据（方法名、属性）导出头；③ Hopper/IDA 反编译 C/Swift 部分为伪码；④ Frida/objc 运行时注入（MOB30）。OC 因保留丰富元数据，比 Swift/C++ 更易逆向。

## 🟣 部分四·深挖层
为什么 OC App 逆向特别容易？因为它运行时要靠 selector/方法列表（MOB26），这些元数据必须保留，所以 class-dump 能完整还原类结构。Swift 因更多静态化、名字混淆，略难；C++ 最难（strip）。顶级 iOS 白帽会"先 OC 层快速定位，再下沉到 Mach-O 汇编定位核心算法"——但这套本领**只用于自己/授权样本**，绝不用作破解他人付费/绕过风控。

## 🔴 部分五·顶级视角
逆向是"二进制阅读理解"的极致：跨平台（Android DEX、iOS Mach-O、Windows PE、Linux ELF、固件 MOB53）共用"反汇编→控制流→语义还原"方法论（方向7 SEC27–SEC30）。顶级工程师的逆向能力，核心价值在"审计自己/客户授权产品、分析恶意软件授权样本、CTF、漏洞赏金"，始终框定在合法授权内。

## 🟠 部分六·安全/合规种子（全案红线在此重申）
iOS 逆向的高危红线：① 绝不越狱破解他人付费 App 提取/分发；② 绝不逆向他人商业 App 窃取算法/资源；③ 绝不用逆向成果做外挂/绕过。你练的一切以"自己 App + 开发者账号 + 授权 CTF 样本"为唯一边界。越线触犯著作权法、刑法 285 条，技术再强也不碰。

## ✅ 验收
交：① 你的"iOS 逆向合规边界声明"；② 说明 class-dump 对自己 App 的意义；③ 复述三条红线。

## ⚠️ 常见坑
- 把"会 iOS 逆向"误解为"能破解任何 App"——大错，本路线只培养白帽合规视角。
- 无 Mac/越狱测试机时别强求工具，方法论+边界达标。
- 任何"提供破解ipa/绕过Apple"的资源都拒之门外。

## ➡️ 下一步
MOB30 · Frida / objc hook——对自己 iOS App 用 Frida 做 Objective-C 方法替换演示。

---

# MOB30 · Frida / objc hook

## 🎯 目标
学完这节，你能用 Frida 在**自己 iOS App（越狱测试机或自签调试）** 里 hook 一个 Objective-C 方法、打印参数、改返回值。与 MOB17 的 Android hook 对应，理解跨平台 hook 的共性。

## 📋 小白前置
MOB17（Frida Android）、MOB26（OC 运行时）、MOB29。

## 🟢 部分一·最浅层
这节和 MOB17 几乎一样，只是"演员"换成了 iOS 的 OC 对象。Frida 仍能潜入你的 App，把某个 OC 方法（如 `计算总价`）换成你写的替身。记住：只换"你自己剧团"的演员。

## 🟡 部分二·动手层（仅自己 App，有 Mac+越狱/调试环境）
Frida 脚本 `hook_ios.js`：
```javascript
ObjC.perform(function () {
  var 类 = ObjC.classes["MyViewController"];
  var 原方法 = 类["- 计算总价"];
  原方法.implementation = Interceptor.replace(原方法.implementation, new NativeCallback(
    function (self, sel) {
      console.log("计算总价被调用");
      return 原方法.call(self, sel);   // 也可 return 改后的值
    }, 'double', ['pointer', 'pointer']));
});
```
注入：`frida -U -f com.yourapp -l hook_ios.js --no-pause`

### 🧪 实战 Lab（MOB30）
写一段笔记发我：对比"Android Frida（Java.use）"和"iOS Frida（ObjC.classes）"的异同——共同点（注入、替换实现、打印）、差异点（Java 层 vs OC 运行时）。无环境不强制运行。

## 🔵 部分三·原理层
Frida 的 `ObjC` 模块封装了对 OC 运行时的访问：`ObjC.classes` 枚举所有类、`ObjC.perform` 确保在 runtime 就绪后操作、`Interceptor.replace` 在原生层替换函数指针（类似 Android native hook）。由于 OC 元数据极丰富，Frida 能直接按方法名定位，比 hook C 函数容易。其底层仍是"代码段写权限 + 指令 trampoline"（呼应 SEC50 Inline Hook）。

## 🟣 部分四·深挖层
iOS hook 的三种层次：① OC 层（Frida ObjC / CaptainHook / MSHookMessageEx，基于 Method Swizzling MOB26）；② Swift/CC++ 层（Frida Interceptor 按地址 hook，需先定位）；③ 内核层（越狱 tweak，Cydia Substrate）。顶级白帽理解分层后，能选最优切入点。但**所有层只用于自己/授权样本**——对他人 App 的任何层 hook 都是越线。

## 🔴 部分五·顶级视角
"动态插桩（Frida）"是跨平台显微镜：Android（Java/native）、iOS（OC/native）、桌面（Windows/Linux 进程）、甚至嵌入式（MOB53）。顶级工程师用它在授权范围内做"无侵入监控、协议分析、bug 复现"。Frida 的 JS 脚本化让自动化检测自己 App 成为可能——这是它最正当、最强大的白帽用途。

## 🟠 部分六·安全/合规种子（与 MOB17 同等红线）
Frida 在 iOS 上同样**只用于自己 App / 授权靶场 / CTF**。严禁：hook 他人 iOS App 改数值做外挂、绕过 Apple 内购、逆向他人 SDK。你练 Frida 的正当目的：① 理解 hook 原理；② 检测**自己** App 是否易被 hook（反 Frida 自检，呼应 MOB22）；③ 在合法 CTF 中解题。越线即违法。

## ✅ 验收
交：① Android vs iOS Frida 的异同笔记；② 复述 iOS Frida 红线；③ （有环境者）自己 App 的 hook 脚本与终端输出。

## ⚠️ 常见坑
- 越狱测试机有风险（安全/变砖），新手优先用 Android 模拟器练 Frida（MOB17），iOS 仅学概念。
- OC 方法名带 `-`（实例）/ `+`（类）前缀，写错找不到。
- 对他人 App 重复本节动作 = 红线，坚决不做。

## ➡️ 下一步
MOB31 · 反调试 / 反注入（评估，分析）——理解常见反调试/反 Frida 手段，作为白帽对自己 App 的自评，不教学规避他人。

---

# MOB31 · 反调试 / 反注入（评估，分析）

## 🎯 目标
学完这节，你理解常见的"反调试 / 反注入 / 反 Frida"手段原理（如 ptrace 自附加、检测 Frida 端口与 maps、syscall 检测），并把它作为**白帽对自己 App 的防护自评**。只分析原理、不教学如何突破他人防护。

## 📋 小白前置
MOB17（Frida）、MOB22（root 检测）、MOB30（iOS Frida）。

## 🟢 部分一·最浅层
反调试像"金店防扒手"：装监控（检测调试器）、查可疑人物名单（检测 Frida 特征）、收银台加暗锁（关键逻辑加校验）。扒手（逆向者）和店家（你）玩的是同一套"你能检测我、我也能伪装"的猫鼠游戏。你学这节是"学会当店家自检"，不是去当扒手。

## 🟡 部分二·动手层（只对自己 App 加简单自检）
Android 简单反调试（原理演示，自己 App）：
```kotlin
fun 是否被调试(): Boolean {
    return try { android.os.Debug.isDebuggerConnected() }
           catch (e: Exception) { false }
}
// 另：检测 Frida 常见端口
fun 有Frida端口(): Boolean =
    (1..65535).none { /* 实际只查 27042 等少数端口 */ true } // 示意
```
集成到自己 App，在敏感操作前判断。仅演示原理，不追求"绝对防住"。

### 🧪 实战 Lab（MOB31）
写一张"反调试手段 → 原理 → 你能怎么在自己 App 加（白帽自评）"的三列表格（如：ptrace 自附加 / 检测 / 加自检；Frida 端口 / 检测 / 加端口检查；maps 特征 / 检测 / 加特征扫描）。发我。

## 🔵 部分三·原理层
反调试核心手段：① `ptrace(PTRACE_TRACEME)` 自我附加，使别的调试器无法再附加（一个进程只能被一个 tracer 跟）；② 扫描 `/proc/self/status` 的 `TracerPid`；③ 检测 Frida 的默认端口 27042、或 `/proc/self/maps` 里的 `frida-agent` 字符串；④ 用 `syscall` 直接调 `ptrace` 绕过 libc 封装检测。反注入则校验自身代码完整性（哈希比对）、检测子进程/钩子。

## 🟣 部分四·深挖层
为什么"反调试总能被绕过"？因为检测代码和被保护代码同在一台用户控制的设备内存里，攻击者可用 Frida hook 掉 `isDebuggerConnected` 或 patch 检测分支（MOB17/MOB30 你已会）。于是业界做"反反调试"的对抗升级：检测代码混淆（MOB20）、多处分散校验、与服务端心跳联动。顶级白帽工程师视其为"成本博弈"，而非"绝对胜负"。

## 🔴 部分五·顶级视角
"检测与反检测"是安全对抗的缩影：EDR 检测恶意软件（SEC64）、WAF 检测攻击（SEC65）、游戏反作弊检测外挂（MOB48）、固件校验（MOB53）都同源。顶级工程师的产出不是"一个攻不破的壳"，而是"一套让攻击者成本远大于收益、且能在服务端兜底"的体系。

## 🟠 部分六·安全/合规种子（只分析不突破）
本节**只讲"防护侧"原理，不教学如何突破他人反调试/反 Frida**。你学它是为了：① 给自己 App 加基础防护；② 评估自家产品抗逆向能力；③ 理解攻击者思路以更好防御。绝不把"突破他人反调试"作为练习目标——那是黑产对抗，越线。本路线培养的是白帽自查者，不是破解者。

## ✅ 验收
交：① 反调试手段/原理/自评 三列表；② 说明"反调试为何总能被绕过（同在一台设备内存）"。

## ⚠️ 常见坑
- 误以为加了反调试就安全——错，只是提高门槛。
- 把"研究反调试"滑向"研究突破他人反调试"——红线，及时收手。
- 过度反调试会影响正常调试排障，自己 App 要留开关。

## ➡️ 下一步
MOB32 · 微信小程序——双线程模型、WXML/WXSS、审核机制（合规开发视角）。

---

# MOB32 · 微信小程序

## 🎯 目标
学完这节，你理解微信小程序的架构：双线程（逻辑层 JS + 渲染层 WebView）、WXML/WXSS 写法、以及平台审核机制。会写一个最小"Hello 小程序"。从合规开发者视角，不碰任何绕过审核/抓包他人小程序的行为。

## 📋 小白前置
MOB2（UI 范式）、方向6 FE5（JS 基础）、FE1–FE3（HTML/CSS 概念）。

## 🟢 部分一·最浅层
小程序像"微信里的一个小网页"：但它不是普通网页——微信把"写界面的活"和"算数据的活"分开在两个线程，像厨房（逻辑）和前台（展示）用对讲机通信，互不阻塞。你写的代码跑在微信提供的"沙盒"里，受平台规矩管（审核、不能乱要权限）。

## 🟡 部分二·动手层（逐字操作）
1. 下载安装"微信开发者工具"（官方，免费），微信扫码登录。
2. 新建项目 → 选择"小程序" → 模板选"不使用云开发"的最小模板。
3. 打开 `pages/index/index.wxml`，改成：
```xml
<view class="容器">
  <text>{{问候}}</text>
  <button bindtap="点我">打招呼</button>
</view>
```
4. 打开 `index.js`，加：
```javascript
Page({ data: { 问候: "你好，小程序" },
  点我() { this.setData({ 问候: "你点到我啦！" }) } })
```
5. 模拟器点按钮，文字变化即成功。

### 🧪 实战 Lab（MOB32）
把问候语改成你自己的名字，加一个"计数 +1"的按钮（用 `setData` 更新 `次数`）。运行截图发我。

## 🔵 部分三·原理层
小程序分两层：逻辑层（App Service，独立的 JS 引擎线程，跑你的 `Page`/数据）与渲染层（WebView，渲染 WXML）。两者通过"微信客户端"中转 `setData` 消息——这是"数据驱动视图"（呼应 MOB2 声明式）。WXML 是类 HTML、WXSS 是类 CSS，但能力和标签受限（没有 `<a>`、样式隔离）。原生能力（相机/定位）走微信的 `wx.api` 桥，受用户授权与平台审核约束。

## 🟣 部分四·深挖层
为什么双线程？因为 WebView 渲染一旦被重 JS 阻塞会卡白屏，分离后逻辑计算不卡渲染。这是"Web 技术 + 原生桥"混合架构的变体（和 MOB36 Cordova、MOB41 Electron 同源）。平台审核（类 App Store）是"中心化管控"——牺牲自由度换安全与体验一致。顶级工程师理解：小程序是"受限 Web 运行时"，优化重点是 `setData` 频率/数据量（高频全量 setData 会卡）。

## 🔴 部分五·顶级视角
"宿主内受限运行时"是重要范式：小程序、快应用、插件（浏览器扩展）、Electron 内嵌页、游戏内 UI（MOB49）都类似——你写代码，宿主管沙盒与能力边界。顶级工程师能在"受限环境"里用巧劲（虚拟列表、差分更新）做出原生级体验。

## 🟠 部分六·安全/合规种子
白帽合规：小程序的"用户数据/接口"都走微信平台与你的服务器，务必 HTTPS、最小权限、不收集无关信息（呼应 MOB4/MOB27）。**绝不**：逆向/抓包他人小程序窃取接口与资源、绕过审核上架违规内容、利用小程序漏洞刷量。你学它是为了"合规开发一个自己的小程序"，以及理解"宿主沙盒"这一通用安全模型。

## ✅ 验收
交：① 运行截图（含按钮交互）；② 说明"双线程"是什么。

## ⚠️ 常见坑
- `setData` 路径写错（如 `this.data.问候 = ...` 直接赋值不刷新）→ 必须用 `setData`。
- WXML 标签是小程序特有，不能直接写 HTML 的 `<div>`。
- 真机预览需开发者账号，练习用模拟器即可。

## ➡️ 下一步
MOB33 · React Native——JS 桥与原生渲染，跨平台产物分析（只讲开发与对自己产物的安全）。

---

# MOB33 · React Native

## 🎯 目标
学完这节，你理解 React Native 的"JS 线程 + 原生 UI 线程 + Bridge 通信"架构；会初始化一个最小 RN 工程并跑在模拟器；理解其产物（JS Bundle + 原生壳）。只做自己产物的开发与安全评估。

## 📋 小白前置
方向6 FE9/FE10（React 概念）、MOB32。

## 🟢 部分一·最浅层
RN 像"用 JS 写剧本、让原生演员演"：你用 React（JS）写界面逻辑，RN 把它翻译成真正的原生按钮/文字（不是 WebView 套壳，MOB32 那种）。中间有个"翻译桥（Bridge）"把 JS 的意图传给原生去画。好处：一份 JS 出安卓+iOS。

## 🟡 部分二·动手层（逐字操作）
用官方脚手架（需装 Node，方向1 JS7）：
```bash
npm install -g expo-cli
npx create-expo-app 我的App
cd 我的App
npx expo start
```
按提示用 Android 模拟器或 Expo Go App 扫码预览。改 `App.js`：把文字改成你的名字，保存热更新看变化。

### 🧪 实战 Lab（MOB33）
在 `App.js` 里加一个 `useState` 计数器按钮（借鉴 FE10 的 React hooks 写法），点一下数字 +1。运行截图发我，并说明"JS 改了状态、原生界面跟着变"这条链路。

## 🔵 部分三·原理层
RN 架构：JS 线程跑 React 与你的业务逻辑，通过 Bridge（异步、批量的 JSON 消息）把"该创建什么原生组件、设什么属性"发给原生 UI 线程，原生线程用平台控件（Android View / iOS UIView）渲染。早期 Bridge 是性能瓶颈（序列化开销），新版"Fabric/JSI"让 JS 直接持有原生引用（同步调用）。产物是"原生壳 App + JS Bundle（可明文/可加密）"。

## 🟣 部分四·深挖层
RN 的"Learn once, write anywhere"区别于 Flutter 的"自绘"（MOB34）：RN 借原生控件，体验更原生但受平台差异拖累；Flutter 自绘，一致性高但要带引擎。JS Bundle 默认可被 jadx/解包看到逻辑（呼应 MOB15/MOB28），敏感逻辑要么放原生（MOB12 bridge 到 native），要么放服务端。顶级工程师按"体验/一致性/团队技能"三维度选型。

## 🔴 部分五·顶级视角
"JS 驱动原生"是混合开发主流之一：RN、Weex、NativeScript 同类。它与"Web 技术渗透原生"（Cordova MOB36、Electron MOB41）共同构成"跨端"大局。顶级工程师视跨端为"用一条产品线覆盖多平台"的战略能力，但清楚其性能/安全边界，关键路径仍下沉原生。

## 🟠 部分六·安全/合规种子
白帽视角：RN 的 JS Bundle 是"明文资产"（可反编译/篡改），因此：① 不要把密钥/核心算法放 JS；② 通信必须 HTTPS（MOB21 思想）；③ 关键校验放原生或服务端（MOB5 信任边界）。你只对自己 RN 产物做安全评估；绝不逆向他人 RN App 的 Bundle 窃取业务。

## ✅ 验收
交：① 计数器运行截图；② 说明"JS→Bridge→原生渲染"链路；③ 说明 RN Bundle 的安全注意点。

## ⚠️ 常见坑
- 环境装 node/expo 较慢，给下载留时间，别用破解版。
- Bridge 异步导致"连续 setState 非即时"，复杂动画要谨慎。
- 把敏感逻辑放 JS Bundle = 风险点，记住要下沉。

## ➡️ 下一步
MOB34 · Flutter / Dart——Skia 自绘引擎与 AOT，跨平台一致性之王。

---

# MOB34 · Flutter / Dart

## 🎯 目标
学完这节，你理解 Flutter 的"自绘引擎（Skia/Impeller）+ Dart 语言 + AOT 编译"架构；会创建一个最小 Flutter App，理解它为何"一套代码多端一致"。只做自己产物的开发与评估。

## 📋 小白前置
MOB2（声明式范式，Flutter 是典型声明式）、方向1 任意语言基础。

## 🟢 部分一·最浅层
Flutter 不像 RN 借原生控件，而像"自己带了全套画笔（Skia 引擎）"——无论在安卓还是苹果，它都自己一笔笔画界面，所以两边长得一模一样。Dart 是它专用的"画笔语言"，编译时直接变成机器码（AOT），跑得飞快。

## 🟡 部分二·动手层（逐字操作）
1. 装 Flutter SDK（官网 flutter.dev，免费开源），`flutter doctor` 检查环境。
2. `flutter create 我的flutter` → `cd 我的flutter` → `flutter run`（选模拟器）。
3. 打开 `lib/main.dart`，把 `Text('Hello World')` 改 `Text('你好，泽泽学员')`，热重载看变化。

### 🧪 实战 Lab（MOB34）
在 `main.dart` 里用 `StatefulWidget` 做一个"点按钮数字+1"计数器（模仿 MOB33 但用 Dart）：
```dart
int 次数 = 0;
// 在 build 里：
Text('点了 $次数 次'),
ElevatedButton(onPressed: () => setState(() => 次数++), child: Text('加一'))
```
运行截图发我。

## 🔵 部分三·原理层
Flutter 三层：Framework（Dart 写的 Widget/动画/手势）、Engine（C++ 写的 Skia/Impeller 图形引擎 + Dart 运行时）、Embedder（平台适配层，把引擎塞进 Android/iOS/桌面窗口）。Widget 是"配置描述"，每次 `setState` 重建 Widget 树，Engine 用"差分算法（Diff）"只刷新变化的渲染对象（呼应 MOB2 调和）。Release 产物：Dart 代码经 AOT 编成机器码（安卓进 `libapp.so`、iOS 进 Mach-O），不是解释执行。

## 🟣 部分四·深挖层
Flutter 的"自绘"带来像素级一致，但代价是包体更大（带引擎）、且与原生控件"质感"有细微差异（如滚动回弹）。Dart 的 AOT 让逆向难度高于 RN 的 JS Bundle（但仍是二进制可逆向，MOB15/28 思想适用）。顶级工程师选 Flutter 当"多端一致性 + 高性能动画"优先；选 RN 当"团队已熟 React + 要原生质感"优先。

## 🔴 部分五·顶级视角
"自绘 UI 引擎"是跨端另一条技术路线（对比 RN 的桥接原生、Web 的原生组件）：Chrome（Skia）、Flutter、甚至游戏引擎（MOB44–MOB47）都自绘。顶级工程师理解"渲染管线"（光栅化、合成、图层）是图形学根基（MOB49），Flutter 只是把这条管线做成了跨端 SDK。

## 🟠 部分六·安全/合规种子
白帽视角：Flutter 的 AOT 产物（libapp.so）可被 IDA/反编译分析（MOB18 思想），敏感逻辑仍要下沉原生（平台通道 MethodChannel 调 native，呼应 MOB12）或服务端。只对自己 Flutter 产物做评估；绝不逆向他人 Flutter App 窃取代码/资源。平台通道传参也要做类型与边界校验，防注入。

## ✅ 验收
交：① Flutter 计数器运行截图；② 说明"自绘 vs 借原生控件"的差异；③ 说明 Flutter 产物是 AOT 机器码。

## ⚠️ 常见坑
- `flutter doctor` 常报缺 Android Studio/SDK，按提示装齐即可，别找破解。
- `setState` 必须包住状态变更，否则不刷新。
- 首次 `flutter run` 下载依赖多，慢属正常。

## ➡️ 下一步
MOB35 · Xamarin / .NET MAUI——C# 跨平台，IL 与反编译（仅自己产物）。

---

# MOB35 · Xamarin / .NET MAUI

## 🎯 目标
学完这节，你理解微软系跨平台方案：Xamarin（旧）/ .NET MAUI（新）用 C# 写、编译成 IL（中间语言）、再 AOT 或 JIT 到各平台。会创建最小 MAUI 工程（若有 Windows + VS）。只对自己产物。

## 📋 小白前置
MOB39 预告（.NET/C#），方向1b JV 系列（CLR/字节码概念有助于理解 IL）。

## 🟢 部分一·最浅层
MAUI 像"微软牌的 Flutter"：你用 C# 写一套界面，它翻译成安卓/iOS/Windows/Mac 各自的原生控件。C# 代码先编成"中间语言（IL，类似 Java 的字节码）"，运行时再变成本地机器码。好处：.NET 生态（C# 强类型）直接复用。

## 🟡 部分二·动手层（有 Windows + Visual Studio 可练；无则阅读）
VS 新建 ".NET MAUI App"，改 `MainPage.xaml` 的 `<Label Text="Hello"/>` 为你的名字，F5 运行（需配安卓模拟器或 Windows 本地）。XAML 是"声明式界面描述"（类 Android XML MOB8 / WPF MOB39）。

### 🧪 实战 Lab（MOB35）
写一段"MAUI vs Flutter vs RN"的三方对比（语言/渲染方式/适用人群）发我。无环境不跑。

## 🔵 部分三·原理层
C# 编成 IL 存于程序集（.dll），在 Android 上由 Mono/ .NET Runtime 经 AOT（或 JIT）执行，UI 通过"处理器（Handler）"映射成原生控件（MAUI 的 `Handler` 架构）。IL 是强类型中间表示，可被 `ildasm`/`dnSpy` 反编译回接近源码的 C#（呼应方向7 SEC35）。这与 JVM 字节码（MOB15）、DEX（MOB15）同属"字节码家族"。

## 🟣 部分四·深挖层
Xamarin 2011 起、MAUI 2022 接棒（统一 Xamarin.Forms 与多端）。它的"原生控件映射"路线与 RN 类似（非自绘），优点是 C#/MVVM 生态、与 Azure/Windows 集成好；缺点是包体、冷启动、社区热度不及 Flutter/RN。顶级工程师按"团队栈（是否已用 C#/.NET）"选型——已有 .NET 后端时 MAUI 复用成本极低。

## 🔴 部分五·顶级视角
".NET 跨端"体现"语言/运行时统一"战略：一套 C# + 一个 BCL（基础类库）覆盖云（ASP.NET）、桌面（WPF）、移动（MAUI）、游戏（Unity 也用 C#，MOB45）。顶级工程师视"共享核心业务逻辑"为降本增效关键——把领域模型/算法用跨端语言写一次，UI 适配各端。

## 🟠 部分六·安全/合规种子
白帽视角：IL 极易被 dnSpy 反编译（比 DEX/Flutter 更易还原），故 MAUI 的敏感逻辑更该下沉原生（PlatformInvoke 调 C++）或服务端，并对程序集做混淆（ConfuserEx 类工具，仅自己用）。只对自己 MAUI 产物评估；绝不反编译他人 .NET 程序集窃取代码。

## ✅ 验收
交：① MAUI/Flutter/RN 三方对比；② 说明 IL 是什么、为何易被反编译。

## ⚠️ 常见坑
- MAUI 环境重（VS +  workloads），无 Windows/VS 时只学概念即可。
- IL 反编译极易，敏感逻辑别裸放 C#。
- 别把"会反编译 .NET"用于他人软件——红线。

## ➡️ 下一步
MOB36 · Cordova / Capacitor——WebView 混合开发（Web 技术套原生壳）。

---

# MOB36 · Cordova / Capacitor

## 🎯 目标
学完这节，你理解"WebView 混合开发"：用 HTML/CSS/JS 写界面、塞进一个原生 WebView 里跑，配插件调原生能力。会理解 Cordova 与 Capacitor 的区别。只做自己产物的开发。

## 📋 小白前置
方向6 FE（HTML/CSS/JS）、MOB32（小程序双线程对照）。

## 🟢 部分一·最浅层
Cordova/Capacitor 像"把网页打包进 App 外壳"：你写的就是个网站，外面套了个原生 App 的壳，壳里有个"浏览器（WebView）"显示你的网页，再通过插件让网页能调相机。最省事（Web 技能直接复用），但体验不如原生/Flutter 顺滑。

## 🟡 部分二·动手层（逐字操作，用 Capacitor 现代方案）
```bash
npm init vite@latest 我的混合App -- --template vanilla
cd 我的混合App && npm install
npm install @capacitor/core @capacitor/cli
npx capacitor init 我的混合App com.example.myapp
npx cap add android        # 生成原生壳
npx cap open android       # 用 Android Studio 打开跑模拟器
```
改 `index.html` 的文字，运行看 WebView 显示。

### 🧪 实战 Lab（MOB36）
对比"纯 Web（浏览器打开）"与"Capacitor 包成 App"两种运行方式，写一句说明发我：它们看到的页面是不是同一份 HTML。无环境只阅读也可。

## 🔵 部分三·原理层
混合 App = 原生壳（Activity 持有 WebView）+ Web 资源（HTML/JS 打包进 assets）。JS 与原生通过"桥（Bridge）"通信：Cordova 用 `cordova.exec` 经 `prompt`/iframe 传 JSON 到原生插件；Capacitor 用更现代的 `Native Bridge`（postMessage + 原生 handler）。原生能力（相机/定位）由插件以原生代码实现，JS 调插件间接受益。

## 🟣 部分四·深挖层
Cordova（PhoneGap 后裔）是混动开山，但体验/维护下滑；Capacitor（Ionic 团队）现代替代，更好支持现代前端（React/Vue/Vite）与原生互操作。这类方案的上限受 WebView 性能与平台 Web 标准差异限制（iOS/Android WebView 内核不同）。顶级工程师在"内部工具/快速验证/MVP"用混合，在"高性能/强体验"用原生或 Flutter。

## 🔴 部分五·顶级视角
"WebView 套壳"是"受限 Web 运行时"（MOB32 小程序同族）的更开放形态。它与 Electron（MOB41，桌面 WebView）共享"Web 技术跨端"战略。顶级工程师理解其定位：用 Web 的迭代速度换一点原生体验——是成本与质量的经典权衡。

## 🟠 部分六·安全/合规种子
白帽重点：WebView 混合 App 的攻击面=Web 安全面（XSS、CSP，FE15/SEC15/SEC16）+ 桥注入。务必：① 不加载远程不可信 HTML（防 XSS 控原生）；② 桥方法做白名单与参数校验；③ 启用 WebView 的 `setAllowFileAccess(false)` 等 hardening。只对自己混合 App 做安全评估；绝不借 WebView 漏洞攻击他人 App（如 file:// 越权读本地）。

## ✅ 验收
交：① "纯 Web vs Capacitor App" 对比说明；② 列出混合 App 的两类攻击面（Web + 桥）。

## ⚠️ 常见坑
- 把敏感逻辑放前端 JS = 任何人可看，必须下沉/放服务端（MOB5 信任边界）。
- WebView 缓存/文件访问配置不当是经典漏洞，开发时收紧。
- 别用混合方案硬做高性能游戏——选 MOB44+。

## ➡️ 下一步
MOB37 · 跨平台安全差异——对比各框架的攻击面与加固弱点（白帽评估视角）。

---

# MOB37 · 跨平台安全差异

## 🎯 目标
学完这节，你能横向对比 RN / Flutter / MAUI / Cordova / 原生 五种方案的安全特征（逆向难度、攻击面、密钥存放、供应链），形成"选型即选安全姿态"的白帽评估能力。

## 📋 小白前置
MOB33–MOB36（四种跨端方案）、MOB14–MOB22（Android 安全基础）。

## 🟢 部分一·最浅层
五兄弟各有"软肋"：原生 App 逻辑在 DEX/so（需反编译，较硬）；RN 的 JS Bundle 像"摊开的剧本"一抓就明；Flutter 的 AOT 机器码像"加密日记"难读些；MAUI 的 IL 像"带注释的草稿"最易被还原；Cordova 的 Web 代码几乎"裸奔"。选型就像选保险箱，得懂各自锁多牢。

## 🟡 部分二·动手层（做一张评估矩阵）
在 VS Code 新建 `跨平台安全对比.md`，填下表（用你学到的知识）：

| 方案 | 主要产物 | 逆向难度 | 典型攻击面 | 敏感逻辑建议 |
|---|---|---|---|---|
| 原生 Android | DEX+so | 中 | so 调试、Frida | native/服务端 |
| RN | JS Bundle | 低 | Bundle 明文本、Bridge | 下沉 native |
| Flutter | AOT so | 中高 | libapp.so、引擎 | native/服务端 |
| MAUI | IL | 低 | dnSpy 还原 | 混淆+下沉 |
| Cordova | Web 资源 | 极低 | XSS、桥注入 | 服务端 |

### 🧪 实战 Lab（MOB37）
把你填好的矩阵发我，并对"如果你的 App 要存用户令牌，你会选哪种方案 + 怎么做密钥保护"写一段结论。

## 🔵 部分三·原理层
"逆向难度"取决于产物形态：① 解释型/中间语言（JS/IL）天然可读；② AOT 机器码需反汇编（IDA）；③ 原生 so 需 native 调试。攻击面随"桥/WebView/运行时"增多而扩大——每多一层抽象就多一个可被 hook/注入的点（呼应 SEC48 Hook 总论）。密钥绝不可存客户端明文（MOB5 资产保护），应放 Keystore/Keychain 或服务端下发短期令牌。

## 🟣 部分四·深挖层
框架选择本质是"开发效率 vs 安全可控性 vs 体验"的三角。顶级团队常用"混合分层"：跨端框架做 UI 与通用逻辑，关键/敏感模块用原生（或 Rust，RS11 FFI）实现并做混淆+完整性校验。供应链角度：跨端框架本身也是依赖（MOB13 供应链安全），需锁版本、查 SCA（SEC84）。白帽评估要覆盖"框架漏洞（如 RN 旧版 Bridge RCE）"。

## 🔴 部分五·顶级视角
"架构即安全"是顶级工程师的信条：技术选型决定了你未来的攻击面大小与加固成本。他们不会问"哪个框架最安全"，而会问"在我的威胁模型（MOB5）下，哪套分层能让攻击者成本最高、且我能持续维护"。这种"以威胁驱动选型"的思维，适用于所有系统（后端框架、云、数据库）。

## 🟠 部分六·安全/合规种子
白帽评估边界：你做"跨平台安全对比"是为了**设计和保护自己的产品**，以及向团队提合规建议。绝不：用这些差异去"挑软柿子框架攻击他人 App"、逆向他人跨端产物窃取业务。所有分析对象限定自己产物/授权样本/CTF。合规上还须注意各平台商店审核规则（如禁止动态下发可执行代码）。

## ✅ 验收
交：① 填好的五方案安全矩阵；② "存令牌的方案选择 + 密钥保护"结论。

## ⚠️ 常见坑
- 误以为"选了难逆向框架就安全"——客户端校验皆不可信（MOB5 反复强调）。
- 把对比表用于"挑他人 App 框架弱点攻击"——红线。
- 忽略供应链（框架本身漏洞），只盯自己代码。

## ➡️ 下一步
MOB38 · Windows 桌面 Win32 / WinUI——消息循环与控件，桌面开发的起点。

---

# MOB38 · Windows 桌面 Win32 / WinUI

## 🎯 目标
学完这节，你理解 Windows 桌面程序的"消息循环（Message Loop）"核心机制与 Win32 API 的基本写法；知道现代 WinUI/WinForms 是对它的封装。会跑通一个最小 Win32 窗口（C，方向1b 已学 C 后更易）。

## 📋 小白前置
方向1b C1–C7（C 基础）、MOB3（生命周期对照）。

## 🟢 部分一·最浅层
Win32 程序像"前台接待处"：它开个窗口，然后进入"等消息"状态——鼠标点了、键盘敲了、窗口要重画了，系统就塞一张"消息纸条"进队列，程序一张张处理。这个"不断取消息→处理"的循环就是消息循环，是 Windows 桌面程序的心脏。

## 🟡 部分二·动手层（逐字操作，需 Windows + 编译器）
用 MinGW-w64 或 MSVC 写最小窗口（`win.c`）：
```c
#include <windows.h>
LRESULT CALLBACK WndProc(HWND h, UINT m, WPARAM w, LPARAM l){
    if(m==WM_DESTROY){PostQuitMessage(0);return 0;}
    return DefWindowProc(h,m,w,l);}
int WINAPI WinMain(HINSTANCE h, HINSTANCE, LPSTR, int){
    WNDCLASS wc={0}; wc.lpfnWndProc=WndProc; wc.hInstance=h; wc.lpszClassName="c";
    RegisterClass(&wc);
    CreateWindow("c","你好Win32",WS_OVERLAY|WS_VISIBLE,0,0,400,300,0,0,h,0);
    MSG msg; while(GetMessage(&msg,0,0,0)){TranslateMessage(&msg);DispatchMessage(&msg);}
    return 0;
}
```
用 `gcc win.c -o win.exe -mwindows` 编译运行，看到窗口即成功。

### 🧪 实战 Lab（MOB38）
把窗口标题改成你的名字，加一个"点击关闭"的处理（已在 WndProc 的 WM_DESTROY）。运行截图发我，并说明"消息循环"哪几行。

## 🔵 部分三·原理层
WinMain 是入口；RegisterClass 注册窗口类（含窗口过程 WndProc）；CreateWindow 创建窗口并触发 WM_CREATE；GetMessage 从线程消息队列取消息，DispatchMessage 分派给 WndProc。所有交互（鼠标/键盘/绘制/关闭）都被封装成"消息（UINT 编号 + wParam/lParam 参数）"。这是"事件驱动"编程的鼻祖之一（呼应 MOB2/JS 事件）。控件（Button 等）本质是对话框里的子窗口，也走消息。

## 🟣 部分四·深挖层
Win32 是 1980 年代 API，庞大且 C 风格（句柄 HWND、宏、回调）。现代封装：WinForms（.NET，拖控件）、WPF（MOB39，XAML）、WinUI 3（现代 Fluent）。但底层都回到消息循环。顶级桌面工程师理解：即便用高级框架，卡顿时仍要回到"消息队列是否阻塞"这一根本问题——UI 线程被耗时操作堵住，消息循环转不动，界面就假死。

## 🔴 部分五·顶级视角
"消息循环/事件循环"是 GUI 的通用元模式：Windows 消息循环、macOS 的 RunLoop、Web 的事件循环（JS8）、Android 的 Looper/Handler、游戏主循环（MOB44）都同源。顶级工程师视"单线程事件循环 + 耗时任务异步化"为桌面/前端/移动的共同铁律。理解 Win32，等于握住所有 GUI 框架的底层神经。

## 🟠 部分六·安全/合规种子
白帽视角：桌面程序是 Hook/注入的高发地（方向7 SEC48–SEC55）：SetWindowsHookEx 键盘钩子、DLL 注入、Inline Hook 都作用于桌面进程。你学 Win32 是为了理解这些机制以做防御（如检测自身被注入），但**绝不**写键盘记录器、注入他人进程、Hook 他人程序窃取输入。合法用途仅限自己进程/授权测试。

## ✅ 验收
交：① 运行窗口截图（标题为你名字）；② 指出消息循环的 3 行；③ 复述"桌面程序防注入红线"。

## ⚠️ 常见坑
- 编译需 Windows 工具链（MinGW/MSVC），纯 Git Bash 无编译器；Windows 上用对应终端。
- `WS_OVERLAY` 应为 `WS_OVERLAPPED`（示例代码简化了，实际用 `WS_OVERLAPPEDWINDOW`）。
- 忘记 `PostQuitMessage` → 关窗口后进程不退出。

## ➡️ 下一步
MOB39 · .NET / C# / WPF——CLR 运行时与 XAML 声明式 UI。

---

# MOB39 · .NET / C# / WPF

## 🎯 目标
学完这节，你理解 .NET 的 CLR（公共语言运行时）、C# 语言、以及 WPF 用 XAML 声明式写桌面 UI 的方式。会跑通一个最小 WPF 窗口（需 Windows + Visual Studio）。

## 📋 小白前置
方向1b JV（CLR 类字节码概念）、MOB35（MAUI 已接触 C#/XAML）。

## 🟢 部分一·最浅层
WPF 像"用 XML 画界面、用 C# 管逻辑"的搭档：你写一份 XAML（类似网页 HTML，但更强类型）描述按钮摆哪，再写 C# 说"按钮点了干嘛"。底层 CLR 像 JVM，把 C# 编成 IL 再跑。比 MOB38 的 Win32 手敲 C 舒服太多。

## 🟡 部分二·动手层（有 Windows + VS 可练）
VS 新建 "WPF App (.NET)"，改 `MainWindow.xaml`：
```xml
<StackPanel>
  <TextBlock x:Name="tb" Text="你好 WPF"/>
  <Button Content="点我" Click="按钮点击"/>
</StackPanel>
```
`MainWindow.xaml.cs`：
```csharp
private void 按钮点击(object s, RoutedEventArgs e){
    tb.Text = "你点到我啦！";
}
```
F5 运行，点按钮看文字变化。

### 🧪 实战 Lab（MOB39）
加一个 TextBox 和 Button，点击把 TextBox 内容显示到 TextBlock。运行截图发我。无 VS 时只阅读 XAML 结构。

## 🔵 部分三·原理层
C# 源码 → 编译器 → 程序集（含 IL + 元数据）→ CLR 加载，JIT 把 IL 编成本地机器码执行（呼应 JV1 JVM 字节码）。WPF 用"逻辑树/视觉树"管理 UI，XAML 是声明式描述，运行时由 XAML 解析器构建对象树；数据绑定（Binding）实现"状态→视图"单向/双向同步（呼应 MOB2 声明式）。WPF 渲染走 DirectX（硬件加速），不同于 Win32 GDI。

## 🟣 部分四·深挖层
.NET 历史：2002 年 C# 1.0 对标 Java；CLR 提供 GC、类型安全、跨语言（VB/C#/F# 互通）。WPF（2006）引入 XAML 与 MVVM 模式（Model-View-ViewModel），影响深远（iOS 的 MVVM、前端 Vue 数据绑定）。IL 易被 dnSpy/ILSpy 反编译（MOB35 已提）——所以 .NET 桌面程序保护靠混淆+原生下沉。顶级工程师用 MVVM 把"界面与逻辑"解耦，便于测试与维护。

## 🔴 部分五·顶级视角
"CLR/IL/JIT"与"JVM/字节码/JIT"（JV1）、"ART/Dex/AOT"（MOB15）是同一个"中间语言 + 运行时"家族。顶级工程师理解"托管运行时"的共性：GC、类型安全、反射、跨语言——这让他们在 .NET/Java/Android 间无缝切换（呼应 OTH5 字节码）。

## 🟠 部分六·安全/合规种子
白帽视角：.NET 程序极易被反编译（dnSpy），故：① 密钥/授权逻辑别裸放 C#；② 用混淆（ConfuserEx，仅自己）提高门槛；③ 关键校验放服务端（MOB5）。桌面程序还要注意"提权/自动更新"安全（MOB43）——绝不写"静默下载执行远程代码"的自更新（那是恶意软件特征）。只对自己 .NET 产物评估。

## ✅ 验收
交：① 按钮交互截图（或 XAML 阅读笔记）；② 说明 CLR 把 C# 变成什么（IL→JIT→机器码）；③ 说明 IL 易被反编译的对策。

## ⚠️ 常见坑
- 无 Windows/VS 时别强求，XAML 概念达标。
- 事件处理方法签名必须匹配 `(object, RoutedEventArgs)`，否则编译错。
- 把敏感逻辑放 C# 明文 = 风险，记住下沉/服务端。

## ➡️ 下一步
MOB40 · Qt / C++ 跨平台——信号槽机制与一套代码多桌面平台。

---

# MOB40 · Qt / C++ 跨平台

## 🎯 目标
学完这节，你理解 Qt 的"信号-槽（Signal-Slot）"通信机制与"对象树"内存管理；会用 Qt Creator 创建跨平台（Windows/Linux/macOS）桌面程序。需先学 C++（方向1b CPP1–CPP5）。

## 📋 小白前置
方向1b CPP1–CPP5（C++ 类/智能指针），MOB38（消息循环对照）。

## 🟢 部分一·最浅层
Qt 的"信号-槽"像"呼叫铃 + 服务员"：按钮按下的瞬间"响铃（发信号）"，你提前约好的"服务员（槽函数）"就自动来干活——不用你手动在消息循环里 if 判断"是不是按钮被点了"。它把 MOB38 的手动消息分流自动化了，且一套代码能在 Win/Mac/Linux 跑。

## 🟡 部分二·动手层（逐字操作，装 Qt Creator）
1. 官网下 Qt Creator + Qt 库（开源版 LGPL，免费合法）。
2. 新建 "Qt Widgets Application"，在 `mainwindow.ui` 拖一个 PushButton（text="点我"）和一个 Label。
3. 在 `mainwindow.cpp` 的构造函数加：
```cpp
connect(ui->pushButton, &QPushButton::clicked, this, [this](){
    ui->label->setText("你点到我啦！");
});
```
4. 运行（绿色 ▶），点按钮 Label 变字。

### 🧪 实战 Lab（MOB40）
把 Label 初始文字改成你的名字，点击后变成"已点击 N 次"（用个 `static int n` 计数）。运行截图发我。

## 🔵 部分三·原理层
信号-槽是 Qt 的元对象系统（moc 预处理器生成额外代码）实现：信号（如 `clicked`）在事件发生时发射（`emit`），Qt 的事件循环（QEventLoop，同源 MOB38 消息循环）把连接好的槽函数调用过去。它比 Win32 回调更安全（类型检查、自动断开）。对象树：父 Widget 销毁时自动销毁子 Widget（RAII 思想 CPP1），避免内存泄漏。

## 🟣 部分四·深挖层
Qt 诞生于 1991 年，是 C++ 跨平台 GUI 的先驱。其"元对象编译器（moc）"在 C++ 上加了反射/信号槽（C++ 本身无反射）。LGPL 授权允许动态链接下闭源使用，商业授权则无限制——合规上要分清。QML（Qt 新声明式 UI，类 JSON/JS）是现代化方向。顶级工程师赞 Qt 的"跨平台一致性 + 性能 + C++ 生态"，常用于工业软件/嵌入式界面（MOB50 物联网屏）。

## 🔴 部分五·顶级视角
"信号-槽/观察者模式"是响应式与事件驱动的工业级实现（呼应 PAR5 响应式、JS 事件委托 FE6）。跨平台抽象层（Qt 的 QPA）与 Flutter 引擎、游戏引擎（MOB44）、Electron（MOB41）的"平台适配层"同源。顶级工程师视 Qt 为"把 C++ 能力带到桌面/嵌入式的桥梁"。

## 🟠 部分六·安全/合规种子
白帽视角：Qt/C++ 程序是原生二进制（PE/ELF），攻击面在 native 层（缓冲区溢出 SEC34、注入 SEC51 DLL）。你学 Qt 是为"写安全的跨平台客户端"，并理解原生桌面的加固（代码签名、防 DLL 劫持）。绝不：借 Qt 写跨平台恶意软件、做他人程序的注入器。合法用途仅限自己产物/授权测试。Qt 开源版合规使用即可（注意 LGPL 动态链接要求）。

## ✅ 验收
交：① 点击计数截图；② 说明"信号-槽"是什么、类比 MOB38 的什么；③ 说明对象树自动释放子控件。

## ⚠️ 常见坑
- 忘记 `connect` 的槽（lambda 或成员函数）签名需匹配信号参数。
- Qt 开源版商用要动态链接遵守 LGPL，闭源静态链接需商业授权——合规注意。
- 无 Qt 安装时只学概念亦可。

## ➡️ 下一步
MOB41 · Electron——Chromium + Node.js 融合的桌面 Web 技术。

---

# MOB41 · Electron

## 🎯 目标
学完这节，你理解 Electron 如何把"网页（Chromium）+ Node.js"包成桌面 App；会创建最小 Electron 工程。理解其"主进程/渲染进程"与"安全沙盒"。只做自己产物。

## 📋 小白前置
方向6 FE（HTML/JS）、方向1b JS7（Node.js 概念）、MOB36（WebView 混合对照）。

## 🟢 部分一·最浅层
Electron 像"把 Chrome 浏览器打包进 App，里面只开你指定的一个网页"：网页部分（HTML/JS）负责界面，浏览器内核（Chromium）负责显示，Node.js 部分负责干"网页干不了"的活（读写文件、调系统）。VS Code、Discord 都是它做的。

## 🟡 部分二·动手层（逐字操作）
```bash
npm init -y
npm install electron --save-dev
```
`package.json` 加 `"main": "main.js"` 与脚本 `"start": "electron ."`。建 `main.js`：
```javascript
const { app, BrowserWindow } = require('electron');
app.whenReady().then(() => {
  const w = new BrowserWindow({ width: 600, height: 400 });
  w.loadFile('index.html');
});
```
建 `index.html`：`<h1>你好 Electron</h1>`。运行 `npm start` 看到窗口。

### 🧪 实战 Lab（MOB41）
在 index.html 加一个按钮，用 `<script>` 写"点一下改标题"。运行截图发我（说明：这是渲染进程里的 JS）。无环境只阅读。

## 🔵 部分三·原理层
Electron = 主进程（Node.js，管窗口/生命周期/系统 API）+ 渲染进程（Chromium，跑你的网页）。两者通过 IPC（进程间通信）交换数据。渲染进程默认有沙盒（不能直接用 Node 全部 API，防网页被 XSS 后控系统）。`BrowserWindow` 创建时 `webPreferences` 的 `nodeIntegration`/`contextIsolation` 是安全关键开关（见下安全节）。

## 🟣 部分四·深挖层
Electron 缺点：包体大（每个 App 带整只 Chromium，百 MB 起）、内存占用高。优点是"Web 技能直接复用、跨三端（Win/Mac/Linux）"。它和 Cordova（MOB36）都"用 Web 技术"，但 Electron 是桌面原生窗口而非移动 WebView。顶级工程师在"内部工具/跨端桌面"用 Electron，在意体积时用 Tauri（MOB42）替代。版本管理上需常更新 Chromium 以修漏洞（供应链 SEC84）。

## 🔴 部分五·顶级视角
"浏览器内核 + 系统桥"是混合开发的通用范式（Electron、NW.js、Tauri、Cordova、小程序宿主）。顶级工程师理解：把 Web 运行时当"受限沙盒"管理，所有跨进程边界都是"信任边界"（呼应 MOB5）。Electron 的 IPC 与移动端的 Bridge（RN MOB33）、游戏的脚本桥（MOB48）同源。

## 🟠 部分六·安全/合规种子（重点）
Electron 高危配置：① **绝开 `nodeIntegration: true` + 关 `contextIsolation`**——这会让渲染进程（万一被 XSS）直接拿到 Node 权限执行系统命令（RCE），是著名漏洞模式（如旧版 Discord/Slack 历史问题）；② 渲染进程加载的内容必须可信（别 `loadURL` 不可信远程页）；③ 定期升级 Electron 修 Chromium CVE。只对自己 App 做安全评估；绝不用 Electron 写"加载任意远程代码"的恶意壳。

## ✅ 验收
交：① 运行窗口截图；② 说明"主进程 vs 渲染进程 + IPC"；③ 列出 Electron 的两个安全红线配置。

## ⚠️ 常见坑
- `npm install electron` 下载慢/大，耐心等或用国内镜像（仅加速不盗版）。
- 误开 nodeIntegration 是经典 RCE 漏洞，务必 contextIsolation 默认开。
- 把不可信 URL 给 loadURL = 危险，只加载本地/可信源。

## ➡️ 下一步
MOB42 · Tauri / Rust——轻量桌面（系统 WebView + Rust 后端），性能与安全更优。

---

# MOB42 · Tauri / Rust

## 🎯 目标
学完这节，你理解 Tauri 如何用"系统自带 WebView + Rust 后端"做出比 Electron 小得多的桌面 App；理解其与 Rust 安全模型（方向1b RS）的结合。需先学 Rust（RS1–RS6）更易。

## 📋 小白前置
方向1b RS1–RS6（Rust 入门/所有权）、MOB41（Electron 对照）。

## 🟢 部分一·最浅层
Tauri 像"精装版 Electron"：它不自带整只浏览器，而是借你系统已经有的"小浏览器（WebView2/WebKit）"显示网页，后端用 Rust（比 Node 更省内存更安全）干重活。结果：同样的 App，体积可能从 100MB 降到 5MB，且更少安全漏洞。

## 🟡 部分二·动手层（逐字操作，需装 Rust）
```bash
npm create tauri-app@latest 我的tauri -- --template vanilla
cd 我的tauri
npm install
npm run tauri dev
```
改 `src/index.html` 的文字，运行看到窗口。Rust 侧在 `src-tauri/src/main.rs`，默认有 `tauri::Builder` 启动逻辑。

### 🧪 实战 Lab（MOB42）
对比 Electron 与 Tauri 的"产物体积理念"写一句结论发我（Electron 自带内核大、Tauri 借系统 WebView 小）。无环境只阅读。

## 🔵 部分三·原理层
Tauri 前端仍是你写的 Web（HTML/JS），渲染用 OS 原生 WebView（Windows WebView2/Edge、macOS WKWebView、Linux WebKitGTK），不打包浏览器。后端是 Rust 编译的原生二进制，通过"进程间命令（invoke）"与前端通信——Rust 端用 `#[tauri::command]` 暴露函数。Rust 的所有权/借用检查（RS2/RS3）在编译期消灭内存安全 bug，比 Node/C++ 后端更稳。

## 🟣 部分四·深挖层
Tauri 兴起于 2021，正是"Electron 太重"的反思产物。它把"安全默认值"做得很严：前端不能直接调系统，必须经 Rust command 显式授权（最小权限，呼应 MOB4）。但这要求开发者懂 Rust。顶级工程师视 Tauri 为"用 Rust 的安全 + Web 的开发效率"的优雅折中，适合对体积/安全敏感的场景。

## 🔴 部分五·顶级视角
"系统 WebView + 安全后端"是混合开发的新趋势（Tauri、.NET MAUI 的 Blazor Hybrid、Capacitor）。它与"自绘引擎（Flutter）""全内核（Electron）"形成光谱。顶级工程师按"体积/安全/生态/团队技能"在光谱上选点，没有银弹。

## 🟠 部分六·安全/合规种子
白帽视角：Tauri 的安全模型值得学习——"前端最小权限、后端强类型校验"。你自己写 Tauri 时：① command 函数对所有入参做校验（防注入）；② 不暴露危险系统调用；③ 用 Rust 的安全规避整类内存漏洞。绝不：用 Tauri/Rust 写恶意桌面程序、做他人进程注入。合规使用开源 Tauri 即可。

## ✅ 验收
交：① Electron vs Tauri 体积理念结论；② 说明 Tauri 的"Rust command 最小权限"是什么。

## ⚠️ 常见坑
- 需先装 Rust 工具链（rustup），新手可能卡环境，耐心。
- WebView 在不同 OS 行为略有差异，跨端要测。
- 把危险操作无校验暴露为 command = 漏洞，须校验入参。

## ➡️ 下一步
MOB43 · 桌面应用安全——注入/更新/签名（白帽评估自己产物）。

---

# MOB43 · 桌面应用安全

## 🎯 目标
学完这节，你建立"桌面应用威胁模型"：DLL 劫持、进程注入、自动更新劫持、代码签名缺失等风险；并知道如何对自己桌面产物做白帽加固评估。对照 MOB5 的移动威胁模型。

## 📋 小白前置
MOB5（威胁模型）、MOB38–MOB42（各桌面框架）、方向7 SEC48–SEC55（Hook/注入原理）。

## 🟢 部分一·最浅层
桌面程序像"开在自家店里的收银机"：坏人可能① 在你店旁放个假零件盒（DLL 劫持，程序加载了恶意同名库）；② 偷偷塞个遥控器进你机器（进程注入）；③ 骗你点"假升级"（更新劫持下木马）；④ 你连防伪标都没贴（缺代码签名）被人随意改。你要学会给自己店装监控（白帽评估）。

## 🟡 部分二·动手层（做一张桌面风险清单）
在 VS Code 写 `桌面安全清单.md`，列风险与对策：

| 风险 | 原理 | 自己产物的对策 |
|---|---|---|
| DLL 劫持 | 程序先加载同目录恶意 dll | 签名校验、KnownDLLs、绝对路径加载 |
| 进程注入 | 远程线程注入 LoadLibrary | 反注入自检、代码完整性 |
| 更新劫持 | 更新器下载未校验的远程包 | 强制 HTTPS + 签名校验 |
| 无签名 | 任意篡改无察觉 | 代码签名（Authenticode/Apple 公证） |

### 🧪 实战 Lab（MOB43）
对你自己写的任意一个桌面/Android 程序，写一句"它目前缺哪条防护、你打算怎么补"。发我。

## 🔵 部分三·原理层
桌面程序是"用户完全控制的环境"，与移动（MOB5）同理但约束更少（无沙盒强制）。DLL 搜索顺序漏洞（Secure Loading）让同目录恶意 dll 优先加载；进程注入靠 `CreateRemoteThread`+`LoadLibrary`（SEC51）；更新劫持因更新器未校验签名而下载执行恶意包（著名案例：某游戏平台被投毒）。代码签名（Authenticode / Apple Notarization）让 OS 验证"这文件确实出自你且未被改"。

## 🟣 部分四·深挖层
为什么桌面比移动更易被本地攻击？因为桌面 OS 默认允许多进程、多用户程序共存、用户有管理员可能。防护靠"纵深"：① 代码签名（防篡改）；② 完整性校验（运行时比对自身哈希）；③ 最小权限运行（不以管理员跑）；④ 更新走签名+HTTPS；⑤ 敏感逻辑下沉+混淆。顶级白帽工程师把"本地不可信"当前提（和 MOB5 同），所有信任放服务端。

## 🔴 部分五·顶级视角
"端点安全"是安全工程一大支柱：桌面、移动、IoT（MOB53）、甚至服务器（主机入侵检测）共享"代码签名、完整性度量、最小权限、可信更新"四件套。顶级工程师的视野从单一 App 上升到"供应链+端点+服务端"的端到端信任链（呼应 SEC84 DevSecOps、OPS18 责任共担）。

## 🟠 部分六·安全/合规种子（红线重申）
你学桌面安全只为**保护自己用户与自己产物**：做代码签名、防注入自检、安全更新。绝不：写键盘记录器（SEC 恶意软件类）、做 DLL 注入器攻击他人进程、造"破解补丁"篡改他人软件、做"away 更新"投毒。这一类工具的编写与传播均属违法（刑法 285/286）。本路线培养白帽自查者，桌面安全只用于防御与授权测试。

## ✅ 验收
交：① 桌面风险清单表；② 你对自己某程序的"缺防护+补法"一句。

## ⚠️ 常见坑
- 把"学注入/注入原理"滑向"写注入器攻击他人"——红线，及时收手。
- 忽略"更新安全"——很多真实入侵来自假更新。
- 无签名在 Win10/11 会被 SmartScreen 拦，正式发布必须签名。

## ➡️ 下一步
MOB44 · 游戏开发概论——引擎/游戏循环/创作管线全景。

---

# MOB44 · 游戏开发概论

## 🎯 目标
学完这节，你建立游戏开发的全局认知：游戏引擎是什么、游戏主循环（Game Loop）长什么样、2D/3D/物理/着色器各自在哪。为 MOB45–MOB49 打框架。白帽视角：只做自己游戏的安全检测。

## 📋 小白前置
MOB2（声明式范式对照）、MOB38（消息循环同源）、方向3 COMP（GPU/渲染相关背景）。

## 🟢 部分一·最浅层
游戏引擎像"乐高主题乐园建造套装"：它把"重复造轮子"的脏活（画图、算碰撞、放声音、管输入）全包了，你只管"设计关卡、摆角色、写规则"。游戏主循环像"每秒 60 次的心脏跳动"：每跳一次就"读输入→算物理→更新画面→画出来"，循环不止。

## 🟡 部分二·动手层（概念 + 选引擎）
1. 了解三大引擎定位：Unity（C#，综合最强、MOB45）、Unreal（C++/蓝图，3A 画质、MOB46）、Godot（GDScript，开源轻量、MOB47）。
2. 理解"游戏循环"伪代码（先记思路，不跑）：
```
while(运行){
  处理输入();
  更新逻辑(dt);      // dt=两帧间隔，保证不同帧率速度一致
  物理step(dt);
  渲染();
  sleep(到下一帧);
}
```

### 🧪 实战 Lab（MOB44）
画一张"游戏开发知识地图"发我：中心"引擎"，四周连出"循环/渲染/物理/音频/输入/脚本/资源"。标出 MOB45–MOB49 各对应哪块。

## 🔵 部分三·原理层
游戏循环是"固定/可变时间步长"的更新：用 `dt`（delta time）让移动速度与帧率无关（60fps 和 144fps 物体速度一致）。渲染把场景图（Scene Graph，树状层级，呼应 MOB8 视图树）变成一帧像素。引擎把"对象组件化"（Unity 的 GameObject+Component，MOB45），逻辑靠"脚本"挂组件上。资源（模型/贴图/音频）经"创作管线（DCC→引擎导入）"进入运行时。

## 🟣 部分四·深挖层
为什么需要引擎？因为"渲染/物理/音频/输入/网络"每样都极复杂，重复造轮子不可行。引擎演进：早期固定管线（OpenGL 1.x）→ 可编程着色器（MOB49）→ 物理引擎（PhysX/Havok，MOB 物理节）→ ECS 架构（数据驱动，性能极致）。顶级游戏工程师视引擎为"工具"，核心能力在"实时图形/数学/性能优化"，能换引擎而不换脑子。

## 🔴 部分五·顶级视角
"实时循环 + 场景图 + 组件化"思想溢出游戏：数据可视化、仿真（数字孪生）、VR/AR、甚至嵌入式 HMI（MOB50 屏显）都用同一套。实时系统的"确定性/低延迟/帧预算"挑战，也见于高频交易、机器人控制。顶级工程师理解游戏技术是"实时图形与系统编程的交叉巅峰"。

## 🟠 部分六·安全/合规种子
白帽游戏视角：你做游戏时，安全重点是"反作弊（自己游戏）"与"资源/通信保护"（呼应 MOB5 信任边界）：① 单机存档别明文（易改）；② 联机校验放服务器；③ 理解内存修改器（如 Cheat Engine）如何改自己游戏的数值——**只对自己游戏练手以做反作弊**，绝不用外挂改他人游戏、做破坏公平的工具（那是侵权+违法，方向7/网安法红线）。

## ✅ 验收
交：① 游戏知识地图图；② 说明"dt 为什么重要"；③ 复述"只对自己游戏做反作弊练习"。

## ⚠️ 常见坑
- 误以为"游戏开发=学一个引擎"——核心是实时图形/数学能力，引擎是载体。
- 把"学内存修改"用于他人游戏外挂——红线。
- 真要做游戏先用轻量引擎（Godot）建立循环直觉，再上 Unity/Unreal。

## ➡️ 下一步
MOB45 · Unity / C#——组件化架构、场景与脚本，做一个可运行小游戏。

---

# MOB45 · Unity / C#

## 🎯 目标
学完这节，你理解 Unity 的"GameObject + Component"组件化架构、场景（Scene）、用 C# 写脚本控制物体；会做一个"点击方块变色/移动"的最小可运行游戏。需先学方向1b C# 概念（MOB39 已接触）。

## 📋 小白前置
MOB44（游戏循环/概论）、方向1b CPP/JV（面向对象）、MOB39（C# 已入门）。

## 🟢 部分一·最浅层
Unity 像"数字舞台"：舞台上一件件"道具（GameObject）"——一个方块、一盏灯、一台相机。每个道具可以挂"功能牌（Component）"：挂个"碰撞体"牌就能被撞、挂个"脚本"牌就能按你的 C# 指令动。你写的 C# 就是"道具的使用说明书"。

## 🟡 部分二·动手层（逐字操作，装 Unity Hub）
1. 装 Unity Hub（官网免费个人版，正规授权），装一个带 "Windows Build Support" 的 Unity 版本，新建 3D 项目。
2. 场景里：Hierarchy 点 "+" → 3D Object → Cube（方块）。
3. Project 面板右键 → Create → C# Script，命名 `变色`. 双击打开写：
```csharp
using UnityEngine;
public class 变色 : MonoBehaviour {
    void Update(){
        if(Input.GetKeyDown(KeyCode.Space))
            GetComponent<Renderer>().material.color = Color.red;
    }
}
```
4. 把脚本拖到 Cube 上，点 ▶ 运行，按空格方块变红。

### 🧪 实战 Lab（MOB45）
改脚本：让方块每按一次空格"旋转 90 度"（`transform.Rotate(0,90,0)`）。运行截图发我。无 Unity 时只阅读脚本结构。

## 🔵 部分三·原理层
Unity 场景是"GameObject 树"，每个 GameObject 挂若干 Component（Transform 必有，记录位置/旋转；Renderer 负责画；Collider 负责碰撞；你写的 Script 也是 Component）。`Update()` 每帧调用（呼应 MOB44 游戏循环）。脚本编成托管 DLL（IL，同 MOB39 .NET），运行时由 Unity 的 Mono/IL2CPP 后端执行（IL2CPP 把 IL 转 C++ 再编，反编译更难，呼应 MOB35）。`Input`/`GetComponent` 是引擎提供的 API。

## 🟣 部分四·深挖层
Unity 的"组件化"是 ECS（Entity-Component-System）思想的温和版：组合优于继承，一个物体由"挂了哪些组件"定义，而非"继承哪个类"。IL2CPP 后端让 C# 游戏能在无 JIT 的平台（如 iOS）跑（AOT）。顶级游戏工程师用"对象池、LOD、遮挡剔除"优化帧率，用"协程/C# async"管理异步（同源 MOB11）。Unity 还有 DOTS（纯 ECS）走数据导向极致性能。

## 🔴 部分五·顶级视角
"组件化/数据导向"是顶级工程师的通识：前端组件（FE9 Vue）、Unity 组件、甚至微服务（BE16）都是"小单元组合"。Unity 的 C# 与 .NET（MOB39）、游戏脚本（MOB48）同语言生态。理解 Unity 等于握住"实时 3D 应用"最大入口——VR/数字孪生/工业仿真都用它。

## 🟠 部分六·安全/合规种子
白帽游戏视角：Unity 游戏的反作弊/资源保护（呼应 MOB48）是正当安全工程：① 存档加密防改；② 联机逻辑校验放服务器；③ 用 IL2CPP+混淆增加逆向难度（仅自己游戏）。你**只对自己 Unity 游戏**做安全评估；绝不：逆向他人 Unity 游戏提取付费资源/破解内购、做外挂（那是侵权+违法，刑法红线）。练习反作弊的对象永远是自己或授权 CTF。

## ✅ 验收
交：① 空格变色/旋转运行截图（或脚本结构笔记）；② 说明"GameObject 与 Component"关系；③ 复述"只对自己游戏做安全"。

## ⚠️ 常见坑
- Unity 个人版有收入门槛（目前年营收超阈需用付费版），学习完全免费合规。
- 脚本类名必须与文件名一致，否则 Unity 不识别。
- 把反作弊技术用于他人游戏 = 红线，坚决不碰。

## ➡️ 下一步
MOB46 · Unreal / C++——蓝图可视化编程与反射系统。

---

# MOB46 · Unreal / C++

## 🎯 目标
学完这节，你理解 Unreal Engine 的两大特色：C++ 底层 + 蓝图（Blueprint）可视化脚本；理解其"反射/UObject"系统。会创建最小 Unreal 项目并让一个物体响应输入。仅讲不强行要求高配机。

## 📋 小白前置
方向1b CPP1–CPP5（C++ 类）、MOB45（Unity 对照）。

## 🟢 部分一·最浅层
Unreal 像"电影级舞台"：它偏爱用 C++ 写"重型机器"，但给不会写代码的人一个"积木连线板（蓝图）"——你拖出"当按下键→旋转方块"的彩色积木连起来，不用写一行字也能让游戏动起来。C++ 是引擎的钢筋，蓝图是装修。

## 🟡 部分二·动手层（有高性能机器可练；无则阅读）
UE 项目新建 → 选 "Third Person" 模板（含可动角色）。打开 Level Blueprint 或给 Cube 加事件：在蓝图编辑器里拖 "Event Tick" → "Add Actor Local Rotation"，运行看物体旋转。C++ 侧（示意）：
```cpp
void AMyActor::Tick(float dt){
    Super::Tick(dt);
    AddActorLocalRotation(FRotator(0, 90*dt, 0));
}
```

### 🧪 实战 Lab（MOB46）
写一段"Unreal 蓝图 vs Unity C# 脚本"的对比发我：蓝图适合谁、C++ 适合谁、各举一个场景。无机器不跑。

## 🔵 部分三·原理层
Unreal 的 `UObject` 是所有引擎对象的基类，配 `UCLASS`/`UPROPERTY` 宏实现"反射"（运行时知道类有哪些属性/函数，可序列化、可在蓝图暴露）——这是 C++ 本身没有的能力（靠 UHT 代码生成器补，类似 Qt 的 moc MOB40）。蓝图本质是"可视化脚本"，编译后也变字节码由虚拟机执行，并能调 C++ 暴露的函数。Tick 是每帧回调（呼应 MOB44 循环）。

## 🟣 部分四·深挖层
Unreal 由 Epic 1998 年起，是 3A 大作主流。其"蓝图/C++ 双轨"降低了门槛又保留性能：玩法原型用蓝图快速迭代，性能热点用 C++ 重写。反射+序列化让编辑器能"所见即所得"编辑任意属性并保存。顶级工程师用"C++ 写系统、蓝图写玩法"的分层；重视"对象生命周期/GC（Garbage Collection）"避免悬空引用。

## 🔴 部分五·顶级视角
"反射/元数据驱动"是大型框架的共性：Java 注解（JV）、C# 特性（MOB39）、Qt moc（MOB40）、Unreal UHT、甚至 ORM（DB26）都用"运行时自描述"实现序列化/编辑器/依赖注入。顶级工程师理解"代码生成器补语言短板"这一工业级手法（呼应 CMP 编译器系列）。

## 🟠 部分六·安全/合规种子
白帽视角：Unreal 游戏同样面临"内存修改/联机作弊"（MOB48）。你学 Unreal 是为"做自己的游戏 + 对自己游戏做反作弊评估"，绝不逆向他人 UE 游戏提取资产（如 MetaHuman/付费素材）、破解内购、做外挂。UE 有 EULA，使用正版引擎、不破解 Marketplace 资源是合规底线。

## ✅ 验收
交：① 蓝图 vs C# 脚本对比；② 说明 Unreal 的"反射(UObject)"是什么、靠什么实现（UHT 代码生成）。

## ⚠️ 常见坑
- UE 对机器配置要求高（显卡/内存），低配机可能卡，学习可用低画质模板。
- 蓝图只是"慢一点的 C++"，性能热点仍需 C++。
- 破解 UE Marketplace 资源 = 盗版红线，拒之。

## ➡️ 下一步
MOB47 · Godot——开源引擎、场景与脚本（GDScript），轻量入门首选。

---

# MOB47 · Godot

## 🎯 目标
学完这节，你理解 Godot 的"场景（Scene）即一切"（场景可嵌套复用）、用 GDScript（类 Python 的易语言）写脚本；会做最小可运行 Demo。Godot 开源免费（MIT），是合规学习的良心选择。

## 📋 小白前置
方向1a Python（GDScript 像 Python，学过快）、MOB44/MOB45（引擎概念对照）。

## 🟢 部分一·最浅层
Godot 像"俄罗斯套娃的舞台"：它把"一个角色、一间房、一段逻辑"都叫做"场景（Scene）"，场景能塞进场景里——你拼小场景成大游戏。脚本语言 GDScript 像"Python 的简体版"，专为本引擎设计，写起来飞快，护手友好（少打字）。

## 🟡 部分二·动手层（逐字操作，装 Godot）
1. 官网下 Godot 标准版（单文件，免费开源 MIT）。
2. 新建项目 → 2D 或 3D 都行。场景里加 Node2D → 子节点 Sprite2D（放个图标）。
3. 给 Node2D 加脚本：
```gdscript
extends Node2D
func _process(delta):
    if Input.is_key_pressed(KEY_SPACE):
        rotation += 1   # 每帧转一点
```
4. 点 ▶ 运行，按空格看旋转。

### 🧪 实战 Lab（MOB47）
把旋转改成"按一次空格转 30 度"（用 `_unhandled_key_pressed` 或计数）。运行截图发我。对比 GDScript 和 Python 的相似处写一句。

## 🔵 部分三·原理层
Godot 的核心是"场景树（SceneTree）"：一切皆 Node，节点有类型（Node2D 管 2D、Control 管 UI、Spatial 管 3D）。`_process(delta)` 是每帧回调（呼应 MOB44 循环，delta=dt）。GDScript 是动态语言，编译成字节码由 Godot 虚拟机跑，官方也支持 C#（通过 Mono）和 C++（GDExtension）做性能模块。场景可"实例化（instance）"——同一场景当模板复用，是组合复用的极致。

## 🟣 部分四·深挖层
Godot 2014 开源，4.x 大改渲染（Vulkan）。它的"节点树 + 信号（Signal）"通信类似 Qt 信号槽（MOB40）与 Unity 事件：节点发信号、别的节点连上响应，解耦清晰。顶级独立开发者爱 Godot 因"无抽成、源码可读、轻量"。理解它后，换 Unity/UE 只是换 API，引擎心智模型通用（MOB44）。

## 🔴 部分五·顶级视角
"节点树 + 信号 + 场景复用"是 GUI/游戏引擎的通用范式（Qt 对象树+信号槽、Flutter Widget 树+回调、Unity 层级+事件）。顶级工程师视 Godot 为"理解引擎本质的干净样本"——它代码量适中、设计清晰，适合反推"一个引擎如何组织实时循环、输入、渲染、脚本"。

## 🟠 部分六·安全/合规种子
白帽视角：Godot 游戏的反作弊/资源保护原则同 MOB45/46（只对自己游戏）。开源引擎的好处是你甚至能读引擎源码理解"存档怎么存、通信怎么发"，从而更好地保护自己游戏。绝不：用 Godot 做他人游戏的外挂、逆向他人 GDScript 资产谋利。开源≠可随意侵权他人基于它的作品，尊重各自授权。

## ✅ 验收
交：① 旋转 Demo 截图（或脚本笔记）；② 说明"场景可嵌套复用"是什么；③ 对比 GDScript 与 Python。

## ⚠️ 常见坑
- Godot 4 与 3 的 GDScript/API 有差异，教程要对版本。
- 误把开源当"可破解他人作品"——错，开源是引擎本身自由，他人游戏资产仍受保护。
- 无机器时只学概念与脚本结构即可。

## ➡️ 下一步
MOB48 · 游戏逆向 / 内存（自己游戏 / 授权）——理解内存修改与外挂原理，只对自己游戏练手以做反作弊。

---

# MOB48 · 游戏逆向 / 内存（自己游戏 / 授权）

## 🎯 目标
学完这节，你理解"游戏内存修改"（如改金币/血量）的原理：数值在内存里是什么样、怎么搜、怎么定位基址。全程**只对自己写的游戏/授权 CTF** 练手，目的是"做反作弊"，绝不做他人游戏外挂。

## 📋 小白前置
MOB44–MOB47（做过自己的游戏）、方向7 SEC34（内存布局基础）、MOB5（威胁模型）。

## 🟢 部分一·最浅层
游戏的"金币"其实就是内存里一个数字。内存修改器（Cheat Engine 类）像"在仓库里逐排找贴着'金币'标签的箱子"：你先记"我现在有 100 金币"，搜内存里数值 100 的格子；花掉 10 变 90，再搜 90——几次就锁定那一格，直接改成 99999。你练这个，是为了知道"小偷怎么找钱箱"，好给自家钱箱加锁。

## 🟡 部分二·动手层（仅对自己游戏，概念+工具）
1. 用 Godot/Unity 做一个"金币"小游戏（变量 `金币=100`，按按钮 -10）。
2. 在电脑装 Cheat Engine（正规内存查看工具，免费），打开**你自己游戏的进程**，用"精确数值扫描"：搜 100 → 改成花到 90 → 搜 90 → 定位地址 → 改成 99999，验证游戏里金币变化。
3. 记录："我成功改了自己游戏的金币，说明'客户端数值不可信'"。

### 🧪 实战 Lab（MOB48）
对自己游戏做完上面的内存修改实验后，写一段"反作弊思路"发我：比如"把金币放服务器校验""客户端只显示、不存真值""检测内存被改则踢出"。说明你这是**自己游戏**的练习。

## 🔵 部分三·原理层
游戏数值是进程内存里的变量（栈/堆/全局区，呼应 OS5/CMP）。修改器通过 `OpenProcess` + `ReadProcessMemory/WriteProcessMemory`（Windows API，SEC51 同底层）扫描匹配值的内存页，逐步收敛到目标地址。指针基址（多级偏移）用于"重启后地址变但基址+偏移不变"的定位。这本质是对**自己进程内存**的操作——但若用于他人进程改数值，就是外挂（侵权/违法）。

## 🟣 部分四·深挖层
反作弊思路（仅保护自己游戏）：① 服务端权威（Server Authoritative）——真值在服务端，客户端只收"显示值"；② 客户端完整性校验（哈希比对代码/内存）；③ 数值范围/速率异常检测（一秒加 99999 必是改的）；④ 反调试/反内存读写（呼应 MOB31）。顶级游戏安全工程师的对抗是"检测修改器特征（如 CE 特征码）+ 行为风控"的组合战。

## 🔴 部分五·顶级视角
"内存读写/进程注入"是方向7 SEC48–SEC55 的核心技术，在游戏、桌面、逆向里通用。顶级工程师理解：端上任何数据都"可被拥有设备的用户读取修改"——这和 MOB5 信任边界、MOB43 端点安全一脉相承。真正的安全不靠"客户端藏好"，而靠"服务端权威 + 行为检测"。

## 🟠 部分六·安全/合规种子（最高优先级红线之一）
**本节的内存修改实验，对象只能是：① 你自己写的游戏；② 主办方明确授权的 CTF/靶场样本**。严禁：对他人任何游戏（含单机）做内存修改做外挂、制作/传播"修改器"、破解他人内购。制作传播外挂在我国属提供侵入/非法控制计算机信息系统程序工具罪（刑法 285 第三款），是犯罪。你学内存原理的唯一正当目的：**保护自己游戏的公平性（反作弊）**。记住：白帽不碰外挂。

## ✅ 验收
交：① 对自己游戏的内存修改实验记录（金币被改）；② "反作弊思路"三条；③ 明确复述"外挂红线：只对自己/授权样本"。

## ⚠️ 常见坑
- 把"我会对自己游戏改数值"误解为"那我也能改别人的"——大错，红线在"对象是谁"。
- Cheat Engine 是双刃剑，只装来学原理+自检，绝不参与外挂社群。
- 单机游戏改自己存档玩 ≠ 联网破坏公平，但联网改他人/竞技场仍越线。

## ➡️ 下一步
MOB49 · 图形学 / Shaders——GPU 渲染管线与着色器入门（GLSL/HLSL）。

---

# MOB49 · 图形学 / Shaders

## 🎯 目标
学完这节，你理解 GPU 渲染管线：顶点→光栅化→片段，以及"着色器（Shader）"是什么（跑在 GPU 上、并行处理每个顶点的小程序）。写一段最小 GLSL 让方块变色。无需顶级数学，建立直觉。

## 📋 小白前置
MOB44（渲染概念）、方向3 COMP（GPU/并行背景有助）。

## 🟢 部分一·最浅层
Shader 像"工厂流水线上的上色工"：屏幕有几百万个像素点，CPU 一个个涂太慢；GPU 派几万个"上色工"同时干——每个工只负责一个点，按你写的"上色规则（Shader）"决定这点是红还是蓝。规则用 GLSL/HLSL 写，跑在显卡上，飞快。

## 🟡 部分二·动手层（用 Shadertoy 在线练，零安装）
打开 shadertoy.com（免费），把默认代码改：
```glsl
void mainImage(out vec4 o, in vec2 f){
    vec2 uv = f / iResolution.xy;     // 0~1 坐标
    o = vec4(uv.x, uv.y, 0.5, 1.0);  // 左红右绿渐变
}
```
运行看屏幕出现"红到绿的渐变条"。

### 🧪 实战 Lab（MOB49）
把上面 shader 改成"随时间闪动的颜色"（`iTime` 用上：`o = vec4(0.5+0.5*sin(iTime), uv.y, 0.5, 1.0);`）。把效果截图/描述发我。

## 🔵 部分三·原理层
渲染管线：① 顶点着色器（Vertex Shader）处理每个顶点（位置变换、投影）；② 图元装配+光栅化（把三角形变成屏幕上的像素片段）；③ 片段着色器（Fragment Shader）决定每个像素颜色。Shader 是跑在 GPU 的专用小程序，成千上万线程并行。GLSL（OpenGL）/HLSL（DirectX）/WGSL（WebGPU）是不同方言。ShaderToy 用片段着色器，输入 uv（像素坐标）、输出颜色。

## 🟣 部分四·深挖层
为什么 GPU 快？因为它是"SIMD 大规模并行"（COMP6）：同一段 shader 同时跑在百万像素上。现代引擎把"光照/后处理/粒子"全写成 shader。GPU 编程与 CPU 思维不同：无全局状态共享、要避免分支发散。顶级图形工程师用 shader 做"屏幕空间反射、体积雾、风格化渲染"等；理解管线是学一切 3D 效果的地基。

## 🔴 部分五·顶级视角
"数据并行 + 专用小内核"思想溢出图形：GPU 计算（CUDA/OpenCL）做 AI 训练（方向9 DAT21+）、科学计算；着色器的"每元素独立计算"模式即 Map 操作（PAR3 函数式、DSA 并行）。顶级工程师能把"图形管线"与"通用并行计算"打通——这是高性能计算的底层语言。

## 🟠 部分六·安全/合规种子
白帽视角：Shader/图形资源也是"知识产权"——你学的是**自己写 shader**（Shadertoy 上创作的归你）。绝不：从他人游戏/软件里提取着色器与贴图资源据为己有（那是盗用美术/代码资产，侵权）。合规使用引擎自带的、或商店正版授权的素材；自己创作的 shader 受著作权保护，也可开源分享。

## ✅ 验收
交：① 渐变/闪动 shader 的截图或描述；② 说明"顶点着色器 vs 片段着色器"各干啥；③ 复述"图形资源也要尊重版权"。

## ⚠️ 常见坑
- GLSL 语法严格（类型、精度限定符），少个分号就编不过，报错看控制台。
- 把"提取他人游戏 shader"当学习素材 = 盗用，红线索要拒绝。
- 无显卡编程基础别慌，本节只建直觉，数学深坑后面再挖。

## ➡️ 下一步
MOB50 · 嵌入式概论——单片机/资源约束/固件，打开"没有屏幕的计算机"世界。

---

# MOB50 · 嵌入式概论

## 🎯 目标
学完这节，你理解"嵌入式系统"是什么：资源极受限（KB 级内存、无 OS 或 RTOS）、直接操作硬件寄存器、跑固件。为 MOB51（Arduino/ESP32）、MOB52（RTOS）、MOB53（固件安全）打地基。

## 📋 小白前置
方向3 COMP（CPU/内存/总线）、MOB12（native/C 已学），方向1b C1–C7（C 基础，嵌入式主力语言）。

## 🟢 部分一·最浅层
嵌入式设备像"只会做一件事的智能插座"：它没屏幕、没键盘，可能只有 2KB 内存，但能 24 小时不关机地读温度、控继电器。你写的程序（固件）直接"摸"芯片的引脚——高电平灯亮、低电平灯灭，没有"操作系统帮你兜底"，一切自己来。

## 🟡 部分二·动手层（概念 + 后续 MOB51 真做）
理解"资源对比"表（先建立量级概念）：
```
设备           RAM        存储        OS
你的手机      数 GB      128GB+      Android
树莓派        1-8GB      数 GB       Linux
Arduino Uno   2KB        32KB        无（裸机）
ESP32         320KB+     数 MB       无/RTOS
```
记牢：嵌入式是"以 KB 计"，所以代码要极省（不用大库、不用动态分配滥）。

### 🧪 实战 Lab（MOB50）
写一段"如果让你做一个'自动浇花器'，它需要哪些硬件（传感器/执行器）和哪些软件能力"的发我。用生活语言即可。

## 🔵 部分三·原理层
嵌入式程序直接跑在 MCU（微控制器，如 AVR/ARM Cortex-M）上，通过"寄存器映射"控制外设：写某个内存地址（如 `0x40021018`）的某几位 = 让 GPIO 引脚输出高电平。无 MMU（内存管理单元），没有虚拟内存、没有进程隔离。程序常是"一个死循环 + 中断"结构（对比 MOB44 游戏循环，但更简单）。启动代码（startup）先初始化时钟/堆栈，再进 `main`。

## 🟣 部分四·深挖层
嵌入式分层：① 裸机（bare-metal，无 OS，MOB50/51）；② RTOS（实时操作系统，MOB52，如 FreeRTOS）；③ 跑 Linux 的"嵌入式 Linux"（树莓派，功能全但重）。为什么分这层？成本/功耗/实时性权衡：智能插座用 8 位 MCU 几块钱，服务器用 x86。顶级嵌入式工程师精通"在 2KB RAM 里把事做对"——这与"极致性能优化"同源，也培养对硬件的敬畏。

## 🔴 部分五·顶级视角
"裸机/寄存器级编程"是顶级工程师的硬核底层功力，和方向3（组成/汇编/OS）、方向7 二进制逆向（SEC25 ELF）、固件安全（MOB53）直接连通。理解"软件如何最终变成引脚上的电信号"，让你在面对任何抽象（云、容器、浏览器）时都能向下追溯到硅。这是"全栈到底"的最后一里路。

## 🟠 部分六·安全/合规种子
白帽视角：嵌入式/物联网是安全重灾区（默认密码、未加密固件、开放串口），但你学它是为"安全地构建自己的设备 + 对自己固件做安全评估"，呼应 MOB53。绝不：入侵他人物联网设备（摄像头/路由器）、利用固件漏洞攻击他人（那是刑事犯罪，网安法/刑法红线）。只分析自己购买且拥有完全控制权的设备、或授权测试样本。

## ✅ 验收
交：① 资源对比量级已记；② "自动浇花器"的软硬件清单；③ 复述"嵌入式无 OS、直接摸寄存器"。

## ⚠️ 常见坑
- 误以为嵌入式=小电脑——错，裸机 MCU 没有 OS、没有动态内存自由。
- 把"研究物联网安全"滑向"入侵他人设备"——红线，只对自己/授权设备。
- 真上手 MOB51 时，引脚接错会烧板，务必按教程接线。

## ➡️ 下一步
MOB51 · Arduino / Raspberry Pi——GPIO 与外设，点亮第一颗 LED。

---

# MOB51 · Arduino / Raspberry Pi

## 🎯 目标
学完这节，你能用 Arduino IDE 给自己买的 Arduino 板写程序，控制 GPIO 让 LED 闪烁；理解树莓派（Linux 嵌入式）与 Arduino（裸机 MCU）的区别。需自购合规开发板（学生可投资正版硬件）。

## 📋 小白前置
MOB50（嵌入式概论）、方向1b C1–C7（C 基础）。

## 🟢 部分一·最浅层
Arduino 像"电子积木的控制器"：你写几行 C，它让某个引脚"通电/断电"，接在上面的 LED 就亮/灭。树莓派像"信用卡大小的电脑"——它跑 Linux，能连网、能跑 Python，适合做"带脑子的小服务器"。前者控硬件引脚，后者跑完整系统。

## 🟡 部分二·动手层（逐字操作，需 Arduino 板+USB线+LED+电阻）
1. 官网下 Arduino IDE（免费开源），装好板子驱动。
2. 工具 → 开发板选你的型号（如 Arduino Uno），端口选识别到的 COMx。
3. 文件 → 示例 → 01.Basics → Blink，直接上传（板上 LED 会闪）。
4. 接外部 LED：13 脚→电阻→LED 长脚，短脚→GND。改代码让自定义脚闪：
```cpp
int led = 13;
void setup(){ pinMode(led, OUTPUT); }
void loop(){ digitalWrite(led, HIGH); delay(1000); digitalWrite(led, LOW); delay(1000); }
```
上传，看 LED 每秒闪。

### 🧪 实战 Lab（MOB51）
把闪烁改成"呼吸灯"（用 `analogWrite(led, 亮度)` 配 `for` 循环渐变，需支持 PWM 的脚如 9/10/11）。运行拍照/描述发我。无硬件时，用 Arduino 模拟器（如 Tinkercad Circuits，免费网页）替代。

## 🔵 部分三·原理层
`pinMode(OUTPUT)` 配置引脚为输出（写对应 GPIO 方向寄存器）；`digitalWrite(HIGH)` 置高电平（写数据寄存器某位→引脚输出电压）；`delay` 空转延时；`loop()` 无限循环（裸机程序的"主循环"，呼应 MOB50）。Arduino 框架把"寄存器操作"封装成易用函数，底层仍是写 `PORTB/DDDR` 等寄存器（MOB50 原理层）。PWM（analogWrite）靠定时器产生占空比调光。

## 🟣 部分四·深挖层
Arduino Uno 是 AVR ATmega328P（8 位 MCU），生态极大、教学友好。树莓派是 ARM Cortex-A 跑完整 Linux，能用 Python/GPIO 库（如 `RPi.GPIO`）控引脚，适合"边缘计算/网关"。选谁？要"低成本控硬件"选 Arduino；要"联网/AI/复杂逻辑"选树莓派。顶级嵌入式工程师两者皆通，并按需求选型（成本/功耗/算力三角）。

## 🔴 部分五·顶级视角
"GPIO 编程"是软硬交汇点：你写的每一行 C，最终变成引脚电平、电流、继电器吸合。这与"操作系统驱动开发（OS14 LKM）""固件（MOB53）""甚至芯片验证"同源。顶级工程师能在"应用代码→驱动→寄存器→电信号"全链路推理，这是纯软件工程师难及的纵深。

## 🟠 部分六·安全/合规种子
白帽视角：你玩的 Arduino/树莓派是**自己拥有完全控制权的设备**，在其上做任何实验（包括之后学固件提取，MOB53）都是合法的自家实验。但绝不：把树莓派改造成"入侵他人 WiFi/监听他人"的工具（那是违法的无线攻击，NET16 仅授权环境）、用自建设备干扰他人。硬件实验也守"不侵害他人"底线。

## ✅ 验收
交：① LED 闪烁/呼吸灯的运行证据（照片或模拟器截图）；② 说明 Arduino（裸机 MCU）与树莓派（Linux）区别；③ 复述"自家设备可实验、不侵害他人"。

## ⚠️ 常见坑
- 接 LED 必须串电阻，否则烧 LED 甚至板子；按教程接线。
- 端口（COM）选错 → 上传失败，看设备管理器确认。
- 把自家硬件实验当成"可攻击他人设备"的跳板——红线。

## ➡️ 下一步
MOB52 · RTOS——实时操作系统与任务调度（给自己 MCU 上 FreeRTOS）。

---

# MOB52 · RTOS

## 🎯 目标
学完这节，你理解 RTOS（实时操作系统，如 FreeRTOS）是什么：多任务、确定性调度、优先级抢占；会在 Arduino/ESP32 上跑两个"任务"并行闪灯。理解它与通用 OS（Linux/Android）的差异。

## 📋 小白前置
MOB51（Arduino/MCU 已玩）、方向3 OS1–OS3（进程/调度概念）、MOB3（生命周期对照）。

## 🟢 部分一·最浅层
裸机程序像"一个人同时 juggle 三个球"（全挤在一个 loop 里，容易乱）。RTOS 像"雇了三个工人（任务），每个只干一件事，工头（调度器）按紧急程度安排谁先干"。关键是"实时"——高优先级的工人喊一声，立刻抢过活来，绝不让"浇花"耽误了"火警"。

## 🟡 部分二·动手层（用 ESP32 + Arduino FreeRTOS，或纯概念）
ESP32 的 Arduino 框架自带 FreeRTOS，可直接用：
```cpp
#include <Arduino.h>
void 任务A(void* p){ for(;;){ digitalWrite(13, HIGH); vTaskDelay(500/portTICK_PERIOD_MS); } }
void 任务B(void* p){ for(;;){ digitalWrite(12, HIGH); vTaskDelay(800/portTICK_PERIOD_MS); } }
void setup(){
  pinMode(13, OUTPUT); pinMode(12, OUTPUT);
  xTaskCreate(任务A, "A", 1024, NULL, 1, NULL);
  xTaskCreate(任务B, "B", 1024, NULL, 1, NULL);
}
void loop(){}   // 交给 RTOS 调度
```
上传，两灯各自按不同节奏闪，证明"并行任务"。

### 🧪 实战 Lab（MOB52）
给自己 MCU 建两个任务：一个每 1 秒闪 LED，一个每 3 秒串口打印 "hello RTOS"。运行描述/截图发我。无硬件用伪代码说明两个任务如何被调度也可。

## 🔵 部分三·原理层
RTOS 提供：任务（轻量线程，无独立地址空间，共享内存）、调度器（基于优先级的抢占式，高优先级就绪即抢 CPU）、IPC（队列/信号量/互斥量，呼应 OS3 同步原语）、定时器。与通用 OS 区别：① 无 MMU/虚拟内存（任务共享地址空间）；② 强调"确定性"——中断响应时间有上界（硬实时）；③ 极小 footprint（几 KB）。`vTaskDelay` 让出 CPU 给别的任务（协作/抢占结合）。

## 🟣 部分四·深挖层
为什么嵌入式要 RTOS 而非 Linux？因为 Linux 是"公平调度、吞吐优先"，实时性无硬保证；而刹车系统、呼吸机要求"中断 1ms 内必响应"，必须 RTOS（如航天用 VxWorks、汽车用 AUTOSAR）。FreeRTOS 因免费小巧成教学/物联网主流。顶级嵌入式工程师按"硬实时需求"选型，并精通"优先级反转"（高优先级任务被低优先级持有的锁阻塞，用优先级继承解决）等经典坑。

## 🔴 部分五·顶级视角
"调度/并发/同步"思想贯穿所有层级：RTOS 任务 ≈ OS 线程（OS1）≈ 协程（MOB11）≈ goroutine（GO1）。顶级工程师理解"无论在哪层，并发的正确性都依赖锁/队列/无共享"，且"实时系统额外要确定性"。这把你在方向3（OS）、方向1（并发）学的东西在硬件上落了地。

## 🟠 部分六·安全/合规种子
白帽视角：RTOS 设备（物联网）常是脆弱入口（默认密码、未加密 OTA 升级），你学 RTOS 是为"安全地构建自己的联网设备"。绝不：入侵他人 RTOS 设备（摄像头/工控）、利用固件漏洞搞破坏（那是危害计算机信息系统安全，重罪）。只对自己购买且拥有控制权的设备、或授权靶场做实验。固件安全细节见 MOB53。

## ✅ 验收
交：① 双任务并行运行证据（截图/描述）；② 说明 RTOS 与 Linux 的实时性差异；③ 复述"只对自己设备实验"。

## ⚠️ 常见坑
- 任务栈大小（1024 字）给太小会栈溢出崩溃，按需求调。
- 两任务共享变量无保护 → 竞态（呼应 OS3），用互斥量。
- 把自家 RTOS 设备当"入侵跳板"——红线。

## ➡️ 下一步
MOB53 · 固件 / 物联网安全——提取/仿真/总线（只分析自己设备），方向8 收官。

---

# MOB53 · 固件 / 物联网安全（收官）

## 🎯 目标
学完这节，你建立"固件安全"全景：固件是什么、如何对**自己设备**做安全评估（固件完整性、默认凭证、通信加密、调试接口 JTAG/UART 风险），以及"提取/仿真/总线分析"的技术边界（仅限自己拥有控制权的设备/授权样本）。方向8 在此收官，串联全部 53 节。

## 📋 小白前置
MOB50–MOB52（嵌入式/RTOS）、方向7 SEC102（IoT 固件安全总论）、MOB5/MOB43（威胁模型）。

## 🟢 部分一·最浅层
固件像"设备的灵魂芯片"：它烧在 MCU 的 Flash 里，上电就被读出来执行。固件安全就是"检查这块灵魂有没有被偷换、有没有留后门、对外喊话（通信）有没有被偷听"。你只检查**自己家电器的灵魂**——绝不拆别人家的。

## 🟡 部分二·动手层（仅对自己设备，合规实验）
1. 对自己拥有的路由器/Arduino，阅读其官方固件更新包（厂商公开下载的 `.bin`），用 `binwalk`（开源工具，自己电脑装）看内部结构：
```bash
binwalk 固件.bin        # 看里面有哪些分区/文件
```
（仅分析你拥有设备对应的、厂商公开提供的固件；不下载他人设备的泄露固件。）
2. 写一份"我的设备安全自检清单"：默认密码改了吗？固件是最新版吗？调试串口封了吗？

### 🧪 实战 Lab（MOB53）
对你自己的一台智能设备（或 Arduino 项目），写一份"安全自检清单 + 改进建议"发我。明确标注"这是我自己拥有控制权的设备"。

## 🔵 部分三·原理层
固件是"直接跑在硬件上的软件镜像"，常见格式含 bootloader、内核（若跑 Linux）、文件系统（squashfs/UBIFS）、配置。安全分析手段：① 静态（binwalk 解包、IDA 反汇编 MCU 固件、找硬编码密码/密钥）；② 动态（仿真 QEMU 跑固件、观察行为）；③ 硬件（UART 串口接出看启动日志、JTAG/SWD 调试、SPI 直接读 Flash 芯片）。这些技术**前提是设备归你所有或授权**。

## 🟣 部分四·深挖层
IoT 安全经典风险（用于"自查+防御"）：默认弱口令、未加密的 OTA 更新（可被替换固件）、硬编码密钥、开放调试接口（UART/JTAG 未熔丝）、明文通信。顶级白帽 IoT 工程师的合法工作：帮厂商做"固件安全审计（授权）"、参与漏洞赏金（SEC100）、做 CTF（如路由器的固件逆向题）。技术深度上，MCU 固件逆向需懂对应指令集（ARM Thumb MOB/OTH4、MIPS），与方向7 SEC25/ASM 一脉相承。

## 🔴 部分五·顶级视角
MOB53 是方向8 的汇流点：它把"嵌入式（MOB50-52）+ 逆向（MOB14-22 思路）+ 网络（方向4）+ 威胁建模（MOB5）"全部收束。顶级工程师看 IoT 安全，视野是"设备-云- App"全链路（呼应 MOB21 的通信安全、MOB43 的端点、方向6 的后端）。而这一切都建立在"你懂每个层面的实现"之上——这正是你走完方向1→7→8 全栈的意义。

## 🟠 部分六·安全/合规种子（方向8 终章红线）
全案最高优先级重申：**固件/物联网安全研究，只针对你自己购买且拥有完全控制权的设备、或厂商明确授权的测试样本、或 CTF/靶场**。严禁：入侵他人路由器/摄像头/工控设备、利用固件漏洞攻击他人、提取他人设备的固件进行破解、把自家实验设备用作攻击跳板。此类行为触犯《网络安全法》《刑法》第 285/286 条，是犯罪。你学 MOB53，是为了：① 安全地构建自己的物联网作品；② 对自己作品做安全加固评估；③ 在合法授权范围内（赏金/审计/CTF）发挥白帽价值。

## ✅ 验收
交：① 对自己设备的"安全自检清单 + 改进建议"；② 能复述"固件分析的合法边界（自己设备/授权样本）"；③ 说明 binwalk 在做什么（解包固件看结构）。

## ⚠️ 常见坑
- 下载"他人设备的泄露固件"分析 = 越线，只用厂商对自身设备公开提供的固件。
- 把 UART/JTAG 技术用于他人设备 = 违法，仅对自己板子练。
- 忽视"默认密码"这一最普遍漏洞——自查先从改密码做起。

## ➡️ 下一步（方向8 完结 · 总前进路线）
✅ 方向8 53 节已全部走完。下一步建议：
- 横向回看：MOB 各平台"安全/合规种子"已为你织成一张"白帽边界网"，未来任何新平台都可套用"只对自己/授权样本"这一原则。
- 纵深补强：回去精修方向7（安全攻防逆向）——MOB 的逆向/检测节是方向7 在移动/嵌入式的投影。
- 实战整合：方向9（数据AI）可为你的 App/游戏加智能；方向10（云/SRE）可把你的后端部署上线；方向11（产品）可包装作品集。
- 终极目标"计算机领域顶级大佬"= 全方向主干精通 + 白帽授权内无阻挡。你已在路上。💊 身体优先，手疼/头晕即停。

---
