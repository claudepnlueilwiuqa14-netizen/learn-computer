# 教案_方向1a_编程Python（PY1–PY25 · 宗师级极深版 · 从彻底零基础到能手写解释器级别内功）

> **学生画像（务必时刻放在心上）**：真·零基础（自述连电脑配件都不认识、不太会用键盘），术后康复期、手有留置针、行动不便，时间极充裕，以学技术为安全感支柱；目标"计算机领域顶级大佬"，坚定白帽路线，愿为学习投资，拒绝野鸡包会班与盗版。
> **本文件定位**：这是你**正式写代码的起点**。Python 是反馈最快、最友好的第一门语言，但本教案不把你当"只会调包"的弱者——每节都从最浅生活直觉打到"顶级工程师怎么用它"。每节含 11 段深模板 + Lv1–Lv5 闯关 + ≥5 可运行 Lab。
> **闯关框架**：🟢Lv1 新手村→🔵Lv2 熟练工→🟣Lv3 进阶者→🟠Lv4 高手→🔴Lv5 宗师。难度只加不跳；卡住就停那关，下次从那关续。
> **白帽红线（方向1a 就种下）**：只写自己的练习程序；绝不写恶意代码、绝不攻击他人系统；理解"用户输入"是为了将来识漏洞，不是去利用。
> **护手优先**：所有代码都能复制粘贴，少打字；手疼立刻停，课永远在、人必须在。

---

# PY1 · 变量（名字标签与内存盒子）

**🎯 目标**：① 用 Python 给"盒子"起名字、往里放数字/文字；② 打印出来、互相加减；③ 写出人生第一个真正 Python 程序；④ 理解"名字"和"盒子里东西"是分开的两件事。

**📋 小白前置**：PRE0–PRE5（认识电脑、鼠标键盘、文件管理、装好 VS Code、第一次跑通 hello.py）。

**🟢 部分一·最浅层**：变量就是贴了名字的透明收纳盒。你往"年龄"盒里放数字 18，以后喊"年龄盒"就能拿到 18，不用记它藏在仓库（内存）哪。电脑也一样——名字帮你记住里面装了啥。

**🟡 部分二·动手层（护手：整段复制）**：
1. 打开 VS Code，新建 `py1.py`，复制：
```python
名字 = "小明"
年龄 = 18
print("我叫", 名字, "，今年", 年龄, "岁")
```
2. 按 `` Ctrl+` `` 开终端，运行 `python py1.py`（不行就用 `py py1.py`）。
3. 应看到：`我叫 小明 ，今年 18 岁`。

**🔵 部分三·原理层**：`年龄 = 18` 时，Python 解释器做两件事：① 在内存找空位存 18；② 在"符号表"（名字→地址的通讯录）记 `年龄` 指向这块地址。你写 `年龄`，解释器查通讯录取值。`print` 把内容送进"标准输出"（终端）。名字和值分离——一个名字同一刻只指向一个东西，但随时可改指向。

**🟣 部分四·深挖层**：历史上 Fortran(1957) 之前程序员要记内存地址 0x1A2B，极易错。引入"名字"是编程史大解放。追问：`年龄=18` 后 `年龄=19`，旧 18 去哪？Python 里没名字指向的旧值被垃圾回收器自动清理。Python 名字可一会儿指向数字、一会儿指向文字（动态类型），与 C/Java 不同——灵活但要小心（PY18 类型标注补回安全感）。

**🔴 部分五·顶级视角**：顶级工程师把"变量"抽象成"状态"，程序=对状态的一系列变换。他们极重**命名**：`x`、`tmp` 在大项目是灾难，好名字（`user_age`、`total_price`）让代码自己说话。变量概念贯通所有语言——C 的变量对应真实地址、Rust 的变量带"所有权"。逆向工程（方向7）里你甚至要还原别人程序变量的真实含义，那是硬核读心术。

**🟠 部分六·安全/合规种子**：今天只让程序算自己的账，安全。但种种子：**变量名和值都是你自己的数据**，只在自己脚本里玩。将来学"用户输入"（PY4/PY9）会懂危险：把外来文字当命令执行=注入漏洞（OWASP Top10 第一）。现在只记：写自己的程序、不执行外来不明指令。

**✅ 验收**：交 ① `py1.py` 含你的变量定义；② 终端正确打印一句话；③ 实战 Lab"总价"运行截图。三条齐=过关。

**⚠️ 常见坑**：① 中文引号（"" 不是 `""`）必报错；② `=` 是"赋值"不是"等于"（等于是 `==`）；③ 文件名别叫 `print.py`/`python.py`（与系统模块重名诡异报错）；④ 手疼硬打字——复制粘贴代替。

**➡️ 下一步**：PY2 字符串与编码（处理一段段文字，懂电脑怎么把字变 0/1）。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 py1.py，打印"我叫 X，今年 Y 岁"。🥉
- 🔵 Lv2：实战 Lab 改"香蕉 2 元一个买 7 个"，算总价。🥈
- 🟣 Lv3：用三个变量算"复利"：本金1000、年利率0.05、两年后多少（只乘两次）。🥇
- 🟠 Lv4：解释"名字指向值、旧值被回收"——写 100 字说明动态类型含义。🏆
- 🔴 Lv5：画一张"符号表=通讯录"图，标出 `年龄→地址→18`，并写 200 字"为什么好命名是专业与业余的分水岭"。👑

---

# PY2 · 字符串与编码（文字串与 0/1 密码本）

**🎯 目标**：① 处理文字：拼接、取某字、数长度、大小写转换；② 第一次懂"电脑只认 0/1，文字怎么被翻译过去"；③ 会读写多行文本。

**📋 小白前置**：PY1。

**🟢 部分一·最浅层**：字符串是"用线串起来的一串字"，像生日项链的珠子，顺序不能乱——"猫吃鱼"和"鱼吃猫"珠子同但顺序异，意思天差地别。编码像电报密码本：每个字对应一串电报码（0/1），用错本子收到乱码"锘垮"。

**🟡 部分二·动手层**：新建 `py2.py` 复制：
```python
问候 = "你好，世界"
print(问候)
print("这句话有", len(问候), "个字符")
print("第一个字是：", 问候[0])
print("前两个字是：", 问候[0:2])
姓名 = "alice"
print("大写：", 姓名.upper())
print("拼接：", "你好 " + 姓名)
多行 = """第一行
第二行"""
print(多行)
```
运行 `python py2.py`。

**🔵 部分三·原理层**：字符串在 Python 是**不可变**对象——`问候[0]="你"` 会报错，因为珠串焊死了，要改只能造新串。索引从 0 起（`问候[0]`=第一个字），`[0:2]` 是切片"从0到2（不含2）"。`len` 数字符数（注意中文一个字算一个）。编码：源码 `.py` 默认 UTF-8，每个汉字变 3 字节存盘；`"你好"` 在内存里是 Unicode 码点，显示时再编码。

**🟣 部分四·深挖层**：ASCII 只管英数符号（1字节），UTF-8 用 1–4 字节变长编码覆盖全球文字——英文仍 1 字节（省空间），汉字 3 字节。`bytes` 是原始字节串（`"你".encode("utf-8")` 得 `b'\xe4\xbd\xa0'`），`str` 是解码后的文字。乱码本质是"用 A 编码存、用 B 编码读"。

**🔴 部分五·顶级视角**：顶级工程师处理网络/文件时永远明确"这是 str 还是 bytes"。Web 后端收的请求体是 bytes，要 decode 成 str 才安全处理；写文件、发 socket 又要 encode 回 bytes。编码错配是生产事故高发区（emoji 截断、中文变问号）。

**🟠 部分六·安全种子**：用户输入的文字可能含"恶意构造的 Unicode"（如同质化字符 spoofing 域名），将来做 Web 要归一化校验（方向6/7）。现在记住：你写的字符串是自己的，练习时别拼进系统命令。

**✅ 验收**：交 py2.py 运行结果 + 实战 Lab 电子名片截图。

**⚠️ 常见坑**：① 索引从 0 不是 1；② 切片 `[a:b]` 含 a 不含 b；③ 不可变→不能赋值改某字符；④ 手疼多复制。

**➡️ 下一步**：PY3 数字与运算（二进制补码、浮点精度、运算符优先级）。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 py2.py，看到字符数/切片/大写。🥉
- 🔵 Lv2：实战 Lab 电子名片，含换行与 len 统计。🥈
- 🟣 Lv3：用切片 `[::-1]` 反转字符串（如"小明"→"明小"），解释原理。🥇
- 🟠 Lv4：写代码把"你好"转 bytes 再转回 str，打印中间字节值，说明 UTF-8 变长。🏆
- 🔴 Lv5：写 200 字"为什么 str 不可变是安全与性能的设计选择"，并举一个自己想到的场景。👑

---

# PY3 · 数字与运算（补码 / 浮点 / 优先级）

**🎯 目标**：① 会用整数、浮点、加减乘除与取模；② 懂"电脑怎么表示负数（补码）"；③ 知道 `0.1+0.2≠0.3` 的真相；④ 明白运算符优先级与短路。

**📋 小白前置**：PY1、PY2。

**🟢 部分一·最浅层**：数字像算盘珠子。整数（18）是整颗珠子，小数（3.14）是带分数的珠子。取模（%）像"分糖果剩几颗"——10 颗糖分 3 人，每人 3 颗剩 1 颗，`10%3=1`。

**🟡 部分二·动手层**：`py3.py`：
```python
print(10 + 3)      # 13
print(10 // 3)     # 3  整除（丢小数）
print(10 % 3)      # 1  取余
print(2 ** 10)     # 1024 乘方
print(0.1 + 0.2)   # 0.30000000000000004 ！
print(7 & 3)       # 3  位运算：0111 & 0011 = 0011
```
运行观察。

**🔵 部分三·原理层**：整数在计算机用**补码**存——正数原样，负数=取反加一，这样加减能用同一电路。浮点按 IEEE-754 用"符号+指数+尾数"近似，`0.1` 二进制是无限循环，存不精确，所以 `0.1+0.2` 有尾差。比较浮点要用 `abs(a-b)<1e-9` 而非 `==`。`//` 整除、`**` 乘方、`%` 取模、位运算 `& | ^ ~` 在 PY22 深讲。

**🟣 部分四·深挖层**：Python 整数**任意精度**（不会溢出，能算 10^1000），C/Java 的 int 固定 32/64 位会溢出——这是 Python 慢但稳的原因之一。运算符优先级：`**` > `* / // %` > `+ -`；用括号永远清晰。布尔 `True/False` 本质是 1/0。

**🔴 部分五·顶级视角**：顶级工程师处理钱/坐标绝不裸用浮点——金融用整数分（元×100）或 `decimal` 模块；游戏物理用浮点但有容差。理解补码让你读任何底层代码不慌（方向3 汇编）。

**🟠 部分六·安全种子**：整数溢出在 C 里可被利用做漏洞（如长度校验绕过），Python 帮挡了，但理解它能帮你写对边界。解析不可信大数要防内存爆炸。

**✅ 验收**：交 py3.py 运行截图 + 解释 `0.1+0.2` 为何不对。

**⚠️ 常见坑**：① 以为 `//` 是注释（注释是 `#`）；② 浮点直接 `==` 比较；③ 混淆 `^`（异或）与幂（Python 幂是 `**`）；④ 手疼复制。

**➡️ 下一步**：PY4 条件 if（分支预测、短路求值）。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 py3.py，记录 0.1+0.2 的结果。🥉
- 🔵 Lv2：用 `%` 判断"年份是否闰年"（被4整除且不被100整除，或被400整除）。🥈
- 🟣 Lv3：用 `decimal` 模块精确算 0.1+0.2，对比 float。🥇
- 🟠 Lv4：手算 `7 & 3`、`7 | 8` 的二进制并验证。🏆
- 🔴 Lv5：写 200 字"补码为什么让加减统一"，并画 -5 的 8 位补码推导过程。👑

---

# PY4 · 条件 if（分支 / 短路 / 控制流）

**🎯 目标**：① 用 if/elif/else 让程序"看情况办"；② 懂布尔表达式与比较；③ 理解短路求值；④ 会嵌套判断。

**📋 小白前置**：PY3。

**🟢 部分一·最浅层**：if 像路口红绿灯——"如果下雨就带伞，否则戴帽子"。程序按条件选不同路走。

**🟡 部分二·动手层**：`py4.py`：
```python
分 = int(input("输入分数："))
if 分 >= 90:
    print("优秀")
elif 分 >= 60:
    print("及格")
else:
    print("不及格")
```
运行，分别输入 95、75、40 看分支。

**🔵 部分三·原理层**：`if` 后跟**布尔表达式**（`True/False`）。比较 `>= < == !=` 产生布尔。重点是**短路**：`and` 前为假后面不测、`or` 前为真后面不测——既快又安全（如 `if x and x[0]==1` 防空错）。`input()` 读入的是字符串，要 `int()` 转数字。

**🟣 部分四·深挖层**：条件本质是"控制流图"的分叉。CPU 有**分支预测**——猜你走哪条，猜错要重来（方向3 COMP2）。写可预测的分支（热路径放前面）能提速。嵌套 if 可读性差，可用"卫语句"（提前 return）或查表替代。

**🔴 部分五·顶级视角**：顶级工程师把复杂业务规则抽象成"谓词函数 + 策略表"，而非深嵌套 if。白帽视角：许多漏洞源于"该拒绝时没拒绝"（如权限检查被 `or` 短路绕过），条件写错=安全洞。

**🟠 部分六·安全种子**：鉴权代码 `if user and user.is_admin or debug:` 的 `or debug` 若误留=后门。写条件时想"攻击者在哪能让我判 True"。

**✅ 验收**：交 py4.py + 三个输入的运行结果。

**⚠️ 常见坑**：① `=` 与 `==` 混用（赋值 vs 比较）；② `input` 不转类型直接比大小报错；③ 嵌套过深自己都乱；④ 手疼用语音/复制。

**➡️ 下一步**：PY5 循环（迭代器协议、复杂度、看门狗）。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 py4.py 三分支。🥉
- 🔵 Lv2：加"输入非法（非数字）时提示重输"的 try/except（提前用 PY10 思路）。🥈
- 🟣 Lv3：用嵌套 if 做" BMI 分类"（偏瘦/正常/偏胖）。🥇
- 🟠 Lv4：写一个 `is_leap(y)` 函数，用短路 `return y%400==0 or (y%4==0 and y%100!=0)`。🏆
- 🔴 Lv5：分析一段"条件被短路绕过"的伪代码漏洞，写 200 字说明如何改正。👑

---

# PY5 · 循环（for / while / 迭代器）

**🎯 目标**：① 用 for/while 重复做事；② 懂 `range`、break/continue；③ 理解迭代器协议；④ 知道循环复杂度 O(n) 与"看门狗"防死循环。

**📋 小白前置**：PY4。

**🟢 部分一·最浅层**：循环像"把一首歌单曲循环播放"。for 是"对清单每件都做一遍"，while 是"只要条件真就一直做"。

**🟡 部分二·动手层**：`py5.py`：
```python
for i in range(5):
    print("第", i, "次")
# while 版
n = 0
while n < 5:
    print("n=", n)
    n += 1
# 提前停
for i in range(10):
    if i == 3:
        break
    print(i)
```

**🔵 部分三·原理层**：`for x in 序列` 背后是**迭代器协议**——序列有 `__iter__` 返回迭代器，迭代器有 `__next__`，取完抛 `StopIteration` 自动停。所以 for 能遍历 list/str/range 甚至文件行。`while` 靠条件，条件永远真=死循环（卡死），要有"看门狗"式退出。

**🟣 部分四·深挖层**：`range(5)` 不是真列表，是惰性对象（省内存）；`enumerate` 同时给下标和值；`zip` 并行遍历。循环里改正在遍历的列表会出怪事——应遍历副本或新列表。复杂度：循环 n 次是 O(n)。

**🔴 部分五·顶级视角**：顶级工程师用"向量化"（NumPy/PY4 方向9）替代 Python 慢循环；用生成器惰性处理海量数据。理解迭代器=理解 Python 一半的优雅（PY13 生成器是其延伸）。

**🟠 部分六·安全种子**：循环处理不可信输入（如超大文件）要设上限防 DoS；`while` 解析不可信数据必须保证每次迭代推进，否则 Deadlock/DoS。

**✅ 验收**：交 py5.py + 解释 break 与 continue 区别。

**⚠️ 常见坑**：① `range(5)` 是 0–4 不是 1–5；② while 忘 `n+=1` 死循环；③ 遍历中删元素漏项；④ 手疼复制。

**➡️ 下一步**：PY6 函数（调用栈、参数传递、闭包）。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 for/while 两版打印。🥉
- 🔵 Lv2：用 for+range 打印 1 到 100 的偶数。🥈
- 🟣 Lv3：用 `enumerate` 遍历列表同时拿下标。🥇
- 🟠 Lv4：写一个 `safe_sum(nums, limit)` 超过 limit 项就停（防 DoS 上限）。🏆
- 🔴 Lv5：解释"迭代器协议"并用自定义类实现 `__iter__/__next__` 造一个 countdown 迭代器。👑

---

# PY6 · 函数（调用栈 / 参数 / 闭包）

**🎯 目标**：① 定义/调用函数；② 懂参数传递（值/引用）、返回值；③ 理解调用栈；④ 接触闭包与默认参数。

**📋 小白前置**：PY5。

**🟢 部分一·最浅层**：函数像"预制菜配方"——写好一次，随时喊"做这道菜"就能得到成品，不重复写。

**🟡 部分二·动手层**：`py6.py`：
```python
def 加税(价, 税率=0.13):
    return 价 * (1 + 税率)
print(加税(100))
print(加税(100, 0.06))
def 问候(名):
    return f"你好，{名}"
print(问候("小明"))
```

**🔵 部分三·原理层**：调用函数时，系统建一个**栈帧**存局部变量与返回地址，压入"调用栈"；执行完弹出，回到调用处。参数传递是"把名字绑到传入对象"——不可变对象（数字/串）像值传递（改不影响外面），可变对象（list/dict）像引用传递（改会影响外面）。`return` 把结果交回去。

**🟣 部分四·深挖层**：**闭包**=函数记住定义时的环境（PY13/PY14 深入）；默认参数只在定义时算一次，若默认值是可变对象（如 `def f(x=[])`）会跨调用累积——经典坑。类型提示（`def f(x: int) -> int`）是 PY18。递归函数靠调用栈（PY16）。

**🔴 部分五·顶级视角**：顶级工程师把代码切成一个个纯函数（同输入同输出、无副作用），可测、可并行。**高阶函数**（把函数当参数，如 `map/sorted(key=)`）是函数式思维（PAR3）。模块化全靠函数边界。

**🟠 部分六·安全种子**：函数边界是"信任边界"——函数入口要校验参数（防传 None/超大值导致崩溃）；不要相信调用方。白帽写工具时对输入做防御性检查。

**✅ 验收**：交 py6.py + 解释默认参数两调用结果。

**⚠️ 常见坑**：① 忘 `return` 得到 None；② 可变默认参数累积；③ 改传入 list 影响外面；④ 手疼复制。

**➡️ 下一步**：PY7 列表/元组（连续内存、缓存局部性、动态扩容）。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通加税/问候函数。🥉
- 🔵 Lv2：写 `max_of(a,b,c)` 返回最大值（不用内置 max）。🥈
- 🟣 Lv3：演示可变默认参数坑：`def f(x=[]): x.append(1); return x`，调两次。🥇
- 🟠 Lv4：写一个闭包 `make_adder(n)` 返回 `lambda x: x+n`，验证。🏆
- 🔴 Lv5：用递归实现阶乘 `fact(n)`，画"调用栈"展开图（5! 怎么从 fact(5) 压到 fact(1) 再弹）。👑

---

# PY7 · 列表 / 元组（连续内存 / 缓存 / 扩容）

**🎯 目标**：① 会用 list 增删查改、切片、推导式；② 懂 tuple 与 list 区别；③ 理解动态数组扩容均摊 O(1)；④ 联系缓存局部性（接 DSA2）。

**📋 小白前置**：PY5、PY6、DSA2。

**🟢 部分一·最浅层**：list 像一排带编号储物柜，随时加柜子；tuple 像封死的柜子，装好就不能改（更安全）。

**🟡 部分二·动手层**：`py7.py`：
```python
a = [10, 20, 30]
a.append(40)        # O(1) 均摊
a.insert(0, 5)      # O(n)
print(a[1:3])       # [20, 30]
b = [x*2 for x in range(5)]   # 推导式 [0,2,4,6,8]
t = (1, 2, 3)       # 元组不可变
print(t[0])
```

**🔵 部分三·原理层**：list 是**动态数组**（连续内存），随机访问 O(1)，中间插入 O(n)（后面整体后移）。满则 realloc 翻倍，因**均摊分析**平均插入 O(1)。tuple 不可变→可哈希、可作 dict 键、更安全（防误改）。

**🟣 部分四·深挖层**：连续内存让 CPU 缓存预取（缓存行 64B），顺序遍历飞快（DSA2 讲过）。推导式 `x for x in ... if ...` 是 Pythonic 写法，比手写的 for+append 快且清晰。切片产生新列表（内存拷贝）。

**🔴 部分五·顶级视角**：顶级工程师用 `collections.deque` 做两端高频操作、`array` 模块存纯数字省内存。理解"list 是数组不是链表"避免性能误用（方向2 DSA3）。

**🟠 部分六·安全种子**：list 越界访问 Python 会抛异常（比 C 安全），但处理不可信索引仍要校验。解析外部数据时 `list()` 转换要先确认大小防内存耗尽。

**✅ 验收**：交 py7.py + 解释 append 与 insert 复杂度差异。

**⚠️ 常见坑**：① 以为 tuple 完全不可变（含 list 元素的可变）；② 切片改原串？不会，产生新串；③ 推导式写错条件；④ 手疼复制。

**➡️ 下一步**：PY8 字典/集合（哈希表、冲突、O(1)）。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 py7.py，看切片与推导式。🥉
- 🔵 Lv2：用推导式生成"1到10的平方列表"。🥈
- 🟣 Lv3：对比 `a.insert(0,1)` 与 `a.append(1)` 用 `timeit` 计时。🥇
- 🟠 Lv4：手写"用 list 模拟栈"（append/pop），并解释为何 list 适合。🏆
- 🔴 Lv5：解释"动态数组扩容为何均摊 O(1)"，写计数器证明（接 DSA1 Lv4）。👑

---

# PY8 · 字典 / 集合（哈希表 / 冲突 / O(1)）

**🎯 目标**：① 用 dict 存键值对、set 去重；② 懂哈希表平均 O(1)；③ 理解冲突与可哈希；④ 能统计词频。

**📋 小白前置**：PY7、DSA5。

**🟢 部分一·最浅层**：dict 像按名字首字母归档的文件柜——"张三"放 Z 格，一步取到。set 像"签到表"，同人不重复签。

**🟡 部分二·动手层**：`py8.py`：
```python
d = {"苹果": 5, "香蕉": 3}
d["橙子"] = 4
print(d["苹果"])        # 5
s = {1, 2, 2, 3}
print(s)               # {1, 2, 3} 去重
# 词频统计
text = "苹果 香蕉 苹果 橙子 香蕉 苹果"
cnt = {}
for w in text.split():
    cnt[w] = cnt.get(w, 0) + 1
print(cnt)
```

**🔵 部分三·原理层**：dict 底层是**哈希表**——key 经 `hash()` 映射到桶，平均 O(1) 查。冲突用开放寻址解决。key 必须**可哈希**（不可变：str/int/tuple），list 不可作 key。`set` 是无值 dict，用于去重、成员判定 O(1)。

**🟣 部分四·深挖层**：Python dict 保持插入顺序（3.7+）。`defaultdict/Counter` 让词频更简洁（`Counter(text.split())`）。负载因子超阈值 rehash。哈希碰撞 DoS 是安全方向要点（DSA5/PY18）。

**🔴 部分五·顶级视角**：dict 是 Python 的"万能结构"——JSON、配置、缓存、对象属性全靠它。顶级工程师用它做 O(1) 查表替代大量 if。理解哈希=理解数据库索引、缓存（方向5/6）。

**🟠 部分六·安全种子**：① 别用不可信输入做 key 触发哈希碰撞 DoS；② 密码绝不存明文 dict，要用 argon2（方向7）；③ 解析 JSON（本质是 dict）要先验证结构。

**✅ 验收**：交 py8.py + 词频统计结果截图。

**⚠️ 常见坑**：① 用 list 当 key 报错；② `d[key]` 不存在抛 KeyError（用 `get` 或 `in`）；③ 混淆 set 与 dict；④ 手疼复制。

**➡️ 下一步**：PY9 文件读写（文件系统、缓冲、路径穿越）。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 dict/set/词频。🥉
- 🔵 Lv2：用 `Counter` 重写词频。🥈
- 🟣 Lv3：用 set 求两个列表的"交集/差集"。🥇
- 🟠 Lv4：手写拉链法哈希表（接 DSA5 Lv3）放 Python 对象。🏆
- 🔴 Lv5：分析"哈希碰撞 DoS"原理，写 200 字说明 Web 后端如何防护。👑

---

# PY9 · 文件读写（文件系统 / 路径 / 缓冲）

**🎯 目标**：① 会读写字入文件（txt）；② 懂文本模式与二进制模式；③ 理解路径与 `with` 自动关闭；④ 知"路径穿越"安全风险。

**📋 小白前置**：PY2（编码）、PY8、PRE2（文件管理）。

**🟢 部分一·最浅层**：文件像抽屉里的笔记本，程序打开它写几行、合上；下次再打开还能读到。

**🟡 部分二·动手层**：`py9.py`：
```python
with open("笔记.txt", "w", encoding="utf-8") as f:
    f.write("今天学了文件读写\n")
    f.write("第二行\n")
with open("笔记.txt", "r", encoding="utf-8") as f:
    content = f.read()
    print(content)
    f.seek(0)
    for line in f:
        print("行:", line.strip())
```

**🔵 部分三·原理层**：`open` 向操作系统要一个"文件描述符"，`"w"` 覆盖写、`"a"` 追加、`"r"` 读、`"b"` 二进制（图片/视频）。`encoding="utf-8"` 指定密码本，漏了中文可能乱码。`with` 是上下文管理器，退出时自动 `close()`（PY15 深讲），防忘关丢数据。**缓冲**：写先进内存缓冲，close/flush 才落盘。

**🟣 部分四·深挖层**：`pathlib` 比字符串拼路径更优雅（`Path("学习/笔记.txt").write_text(...)`）。大文件用逐行读而非 `read()` 全读（防内存爆）。文件系统用 inode 管理（方向3 OS8）。二进制模式读图：`open("a.png","rb")`。

**🔴 部分五·顶级视角**：顶级工程师处理文件必想"失败怎么办"（磁盘满、权限不足、文件被占）——用 try/except 包裹。日志、配置、数据持久化全靠文件 I/O。理解 fd 让你懂"一切皆文件"（方向10 Linux）。

**🟠 部分六·安全种子**：**路径穿越**——若把用户输入拼进文件名（`open(user_input)`），攻击者可传 `../../etc/passwd` 读系统文件。永远白名单校验、限制在固定目录。这是 Web 上传漏洞根源（方向7 SEC15）。

**✅ 验收**：交 py9.py + 生成的笔记.txt 内容截图。

**⚠️ 常见坑**：① 漏 encoding 中文乱码；② 忘 close（用 with 解决）；③ 相对路径取决于"当前目录"；④ 手疼复制。

**➡️ 下一步**：PY10 错误与异常（异常机制、调用栈回溯）。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 py9.py，看到文件读写。🥉
- 🔵 Lv2：用 `pathlib.Path` 改写读写。🥈
- 🟣 Lv3：逐行读一个大文件并数行数（不一次性 read）。🥇
- 🟠 Lv4：写一个安全读函数，把 `../` 之类路径拒绝掉（防穿越）。🏆
- 🔴 Lv5：读一张本地图片的二进制前 16 字节，解释 PNG/JPG 文件头魔数（用 hex 看）。👑

---

# PY10 · 错误与异常（异常机制 / 栈回溯）

**🎯 目标**：① 用 try/except 优雅处理错误；② 懂异常抛出与捕获；③ 理解调用栈回溯；④ 会自定义异常。

**📋 小白前置**：PY6、PY9。

**🟢 部分一·最浅层**：异常像"程序举红牌"——出错了不偷偷错下去，而是喊停并告诉你哪错了。try/except 是"我先试，错了就走备用方案"。

**🟡 部分二·动手层**：`py10.py`：
```python
try:
    分 = int(input("分数："))
    print("你输入了", 分)
except ValueError:
    print("那不是数字，请重输")
finally:
    print("无论对错都执行")
# 主动抛
def 设年龄(a):
    if a < 0:
        raise ValueError("年龄不能负")
    设年龄(-5)
```

**🔵 部分三·原理层**：出错时 Python 创建**异常对象**沿调用栈往上找 `except`，找到就处理，找不到就崩溃并打印**回溯**（traceback，显示从哪调到哪）。`finally` 无论对错都执行（常用于释放资源）。`raise` 主动抛错。常见：`ValueError`（类型错）、`KeyError`（dict 无键）、`FileNotFoundError`。

**🟣 部分四·深挖层**：异常 vs 返回值——异常把"错误路径"与"正常逻辑"分离，代码更干净，但滥用（用异常做流程控制）会慢。自定义异常 `class MyErr(Exception)` 让错误语义清晰。调试器断点原理就是"在指定行抛个陷阱"（接 PRE5/PY24）。

**🔴 部分五·顶级视角**：顶级工程师写 API 会定义清晰异常层级（如 `APIError`→`AuthError/RateLimitError`），让调用方精准处理。理解 traceback 是排错核心技能——90% 的 bug 靠读回溯定位。

**🟠 部分六·安全种子**：① 别把内部异常细节（含路径/SQL）暴露给用户（信息泄露，方向7）；② 捕获要具体（`except ValueError` 而非光 `except:` 吞掉所有错，会藏 bug 甚至藏漏洞）；③ 用户输入转换务必包 try。

**✅ 验收**：交 py10.py + 故意输入非数字看 except 生效。

**⚠️ 常见坑**：① 裸 `except:` 吞错；② 以为异常能修复逻辑错（异常只管意外）；③ 忘 `finally` 释放；④ 手疼复制。

**➡️ 下一步**：PY11 面向对象（类/对象/继承/多态/vtable）。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 try/except 捕获非数字。🥉
- 🔵 Lv2：用 `else` 子句（无异常才执行）重构。🥈
- 🟣 Lv3：自定义 `NegativeAgeError(Exception)` 并在设年龄时抛。🥇
- 🟠 Lv4：故意制造 IndexError，读 traceback 找是哪行。🏆
- 🔴 Lv5：写 200 字"为什么裸 except 危险"，并给出安全异常处理模板。👑

---

# PY11 · 面向对象（类 / 对象 / 继承 / 多态）

**🎯 目标**：① 用 class 定义类、创建对象；② 懂属性/方法、`self`；③ 理解继承与多态；④ 初窥 vtable（接方向1b CPP2）。

**📋 小白前置**：PY6、PY10。

**🟢 部分一·最浅层**：类像"饼干模具"，对象是"用模具压出的饼干"。模具定形状，你能压出很多块不同口味的饼干。

**🟡 部分二·动手层**：`py11.py`：
```python
class 动物:
    def __init__(self, 名):
        self.名 = 名
    def 叫(self):
        print(self.名, "叫了")
class 猫(动物):
    def 叫(self):
        print(self.名, "喵")
class 狗(动物):
    def 叫(self):
        print(self.名, "汪")
a = 猫("咪咪"); b = 狗("旺财")
for x in [a, b]:
    x.叫()        # 多态：同调用不同行为
```

**🔵 部分三·原理层**：`class` 定义模板，`__init__` 是构造器，`self` 指"这个对象自己"。属性存在每个对象里，方法共享类。`继承`让子类复用父类（`动物`），`重写`同名方法实现**多态**——`x.叫()` 根据真实类型选行为。Python 用"方法解析顺序 MRO"找方法。

**🟣 部分四·深挖层**：`self` 不是关键字而是约定首参（必须写）。Python 没有真正私有（ `_名` 是约定私有）。**鸭子类型**——"像鸭子会游就叫鸭子"，不强制继承也能多态（比 Java 灵活）。C++ 的多态靠 vtable（虚函数表），Python 靠运行时查属性（PAR2/PY6）。

**🔴 部分五·顶级视角**：顶级工程师用 OOP 建模真实世界（用户、订单、连接），但不过度设计——能用函数解决就别上类。理解 OOP=理解 Java/CPP/C#/Swift 等主流语言的骨架（方向1b/8）。

**🟠 部分六·安全种子**：反序列化不可信数据成对象（如 Python pickle）可执行任意代码——**绝不 unpickle 外来数据**（方向7 SEC13）。这是经典 RCE 漏洞。

**✅ 验收**：交 py11.py + 多态运行结果。

**⚠️ 常见坑**：① 忘写 `self`；② `__init__` 拼错（双下划线）；③ 以为属性私有真不可见；④ 手疼复制。

**➡️ 下一步**：PY12 模块与包（命名空间、依赖管理）。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通猫狗多态。🥉
- 🔵 Lv2：给动物加 `age` 属性并在构造赋值。🥈
- 🟣 Lv3：用"鸭子类型"——不继承也实现 `叫()` 的第三方类，能被同一循环调用。🥇
- 🟠 Lv4：写一个 `学生(人)` 继承链，含 `study()` 重写。🏆
- 🔴 Lv5：对比 Python 鸭子类型与 C++ vtable 多态，写 200 字说明各自优劣。👑

---

# PY12 · 模块与包（命名空间 / 导入 / 版本）

**🎯 目标**：① 用 import 引入模块；② 懂 `if __name__=="__main__"`；③ 理解命名空间避免冲突；④ 会组织多文件包。

**📋 小白前置**：PY11。

**🟢 部分一·最浅层**：模块像"工具箱"，你不用重造轮子，直接拿来用。包像"工具箱摞成的柜子"，分层管理。

**🟡 部分二·动手层**：`py12.py`：
```python
import math
from random import randint
print(math.sqrt(16))      # 4.0
print(randint(1, 6))      # 骰子
# 自己写模块
# 在同目录建 mytool.py: def hi(): print("hi")
# 然后: import mytool; mytool.hi()
if __name__ == "__main__":
    print("这是主程序直接运行")
```

**🔵 部分三·原理层**：`import` 执行模块文件、建**命名空间**（名字互不污染，`math.sqrt` 与你自己 `sqrt` 可共存）。`if __name__=="__main__"` 让文件既能当模块被 import（不跑主逻辑）又能直接运行。`from x import y` 只拿 y。

**🟣 部分四·深挖层**：包=含 `__init__.py` 的文件夹，用点号分层（`package.sub.module`）。标准库（os/sys/datetime/json）与第三方库（pip 装）都靠 import。语义化版本（MAJOR.MINOR.PATCH）管兼容性（PY22）。

**🔴 部分五·顶级视角**：顶级工程师把项目切成清晰模块边界（单一职责），好测好维护。理解 import 机制=理解大型项目结构、依赖图（方向1b C13 构建系统）。

**🟠 部分六·安全种子**：① 只从可信源装包（PyPI 有投毒包）；② `import` 不可信文件会执行其顶层代码（供应链攻击）；③ 用虚拟环境隔离（PY20）。

**✅ 验收**：交 py12.py + 自建 mytool.py 被 import 成功截图。

**⚠️ 常见坑**：① 循环 import（A 引 B、B 引 A）；② 同名文件覆盖标准库（别叫 `math.py`）；③ 忘 `__init__.py`（老式包）；④ 手疼复制。

**➡️ 下一步**：PY13 迭代器与生成器（yield、惰性、协程雏形）。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 import math/random。🥉
- 🔵 Lv2：建 mytool.py 并被 import 调用。🥈
- 🟣 Lv3：用 `if __name__=="__main__"` 让文件双用。🥇
- 🟠 Lv4：查自己 Python 装了哪些第三方包（`pip list`）。🏆
- 🔴 Lv5：解释"命名空间如何防冲突"，画一个 `math.sqrt` 与本地 `sqrt` 并存的结构图。👑

---

# PY13 · 迭代器与生成器（yield / 惰性 / 协程雏形）

**🎯 目标**：① 理解迭代器协议（PY5 铺垫）；② 用 `yield` 写生成器；③ 懂惰性求值省内存；④ 见协程雏形（接 PY17 asyncio）。

**📋 小白前置**：PY5、PY12。

**🟢 部分一·最浅层**：生成器像"自动削铅笔机"——你摇一下（要一个值）它才削一根，不一次性削一百根占地方。

**🟡 部分二·动手层**：`py13.py`：
```python
def 数数(n):
    i = 0
    while i < n:
        yield i
        i += 1
for x in 数数(5):
    print(x)          # 0 1 2 3 4
# 列表推导 vs 生成器表达式
g = (x*x for x in range(1000000))   # 几乎不占内存
print(next(g), next(g))             # 0 1
```

**🔵 部分三·原理层**：含 `yield` 的函数是**生成器**——调用不执行，返回一个"可暂停恢复"的迭代器。每次 `next()` 跑到下一个 `yield` 吐值并暂停，保留现场。`for` 自动驱动。`(x for x in ...)` 是生成器表达式，惰性（用才算），对比列表推导 `[...]` 一次性全算。

**🟣 部分四·深挖层**：惰性让生成器能处理**无限流/海量数据**（如读 100G 日志逐行）。`yield` 还能 `send()` 往里传值（协程雏形），`yield from` 委托子生成器。生成器是 PY17 asyncio 协程的直系祖先。

**🔴 部分五·顶级视角**：顶级工程师用生成器 pipeline 串起数据处理（读→过滤→变换→写），内存恒定。理解惰性=理解流式计算（方向9 Spark/Kafka、方向10 流处理）。

**🟠 部分六·安全种子**：生成器处理不可信数据源要设上限/超时（防无限流 DoS）。惰性不意味安全——仍要校验每项。

**✅ 验收**：交 py13.py + 生成器 vs 列表内存对比说明。

**⚠️ 常见坑**：① 以为生成器函数调用就执行（其实返回迭代器）；② `next` 耗尽抛 StopIteration；③ 生成器只能走一遍；④ 手疼复制。

**➡️ 下一步**：PY14 装饰器（高阶函数、元编程、中间件）。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通数数生成器。🥉
- 🔵 Lv2：用生成器表达式算 1..100 平方和（不建列表）。🥈
- 🟣 Lv3：写一个生成斐波那契的 `yield` 生成器。🥇
- 🟠 Lv4：用 `yield from` 把两个生成器串起来。🏆
- 🔴 Lv5：解释"惰性求值省内存"原理，写 200 字说明其在大数据 pipeline 的价值。👑

---

# PY14 · 装饰器（高阶函数 / 元编程 / 中间件）

**🎯 目标**：① 懂函数可作参数/返回值；② 用 `@decorator` 增强函数；③ 理解装饰器=包装；④ 见"中间件思想"（接方向6 BE3）。

**📋 小白前置**：PY6、PY13。

**🟢 部分一·最浅层**：装饰器像"手机壳"——不改手机本身，套上就能防摔/增美。装饰器不改原函数，套上就多一层能力（如计时、鉴权）。

**🟡 部分二·动手层**：`py14.py`：
```python
import time
def 计时(f):
    def 包裹(*args, **kw):
        t = time.perf_counter()
        r = f(*args, **kw)
        print(f"{f.__name__} 用时 {time.perf_counter()-t:.4f}s")
        return r
    return 包裹
@计时
def 慢(n):
    return sum(i*i for i in range(n))
慢(1000000)
```

**🔵 部分三·原理层**：装饰器是**高阶函数**——接收函数 f、返回新函数 `包裹`，包裹里"先做事→调 f→后做事"。`@计时` 等价于 `慢 = 计时(慢)`。`*args, **kw` 透传任意参数。这是**元编程**（代码操作代码）的入门。

**🟣 部分四·深挖层**：带参装饰器（`@retry(3)`）是三层嵌套；`functools.wraps` 保留原函数名/文档（重要！否则 `__name__` 变包裹）。类也可做装饰器（`__call__`）。装饰器链从下往上执行。

**🔴 部分五·顶级视角**：顶级工程师用装饰器做横切关注（日志、鉴权、缓存、限流、事务）——即**AOP/中间件思想**（PAR6）。Flask 的 `@app.route`、Django 的 `@login_required` 全是装饰器。理解它=理解 Web 框架大半。

**🟠 部分六·安全种子**：鉴权装饰器若实现错（如漏判匿名）等于没锁门。写 `@require_admin` 时要确保"默认拒绝"而非"默认放行"。

**✅ 验收**：交 py14.py + 带 `@计时` 的运行输出。

**⚠️ 常见坑**：① 忘 `return 包裹`；② 忘 `functools.wraps` 导致元数据丢失；③ 装饰器顺序错；④ 手疼复制。

**➡️ 下一步**：PY15 上下文管理器 with（RAII、资源释放）。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 @计时装饰器。🥉
- 🔵 Lv2：写 `@大写输出` 装饰器，让函数返回字符串自动变大写。🥈
- 🟣 Lv3：用 `functools.wraps` 保留原函数名。🥇
- 🟠 Lv4：写带参 `@retry(times=3)` 装饰器，失败后重试。🏆
- 🔴 Lv5：解释"装饰器=中间件思想"，写 200 字说明 Web 框架如何用其做鉴权/日志。👑

---

# PY15 · 上下文管理器 with（RAII / 异常安全）

**🎯 目标**：① 懂 `with` 自动管理资源；② 用 `__enter__/__exit__` 自定义；③ 理解异常安全；④ 联系 RAII（接方向1b CPP1）。

**📋 小白前置**：PY9（文件 with）、PY10、PY14。

**🟢 部分一·最浅层**：with 像"自动关水龙头"——你开水龙头做事，走人时它自动拧紧，无论你是正常走还是跑着摔出去（异常）。

**🟡 部分二·动手层**：`py15.py`：
```python
class 计时器:
    def __enter__(self):
        import time; self.t = time.perf_counter()
        return self
    def __exit__(self, exc_type, exc, tb):
        import time
        print(f"耗时 {time.perf_counter()-self.t:.4f}s")
        return False   # 不吞异常
with 计时器():
    sum(i for i in range(1000000))
```

**🔵 部分三·原理层**：`with X as y:` 调 `X.__enter__()` 拿资源赋值 y，块结束（无论正常/异常）都调 `X.__exit__(异常类型, 值, 回溯)`。`return False` 表示不吞异常（让它继续抛）；`True` 则吞掉。`open()` 的 with 就是靠这个自动 close。

**🟣 部分四·深挖层**：`contextlib.contextmanager` 用生成器写法做管理器（`@contextmanager` + `yield` 前是 enter、后是 exit）。C++ 的 RAII（资源获取即初始化）是同一思想——析构自动释放。数据库连、锁、事务都该用 with 包。

**🔴 部分五·顶级视角**：顶级工程师把"任何必须配对的操作"（开/关、加锁/解锁、连/断）都用上下文管理器封装，杜绝"忘了释放"类 bug——这类 bug 在生产占相当比例。

**🟠 部分六·安全种子**：锁/连接不释放会被攻击者耗尽（资源 DoS）。用 with 保证异常路径也释放，是防御性编程基本功。

**✅ 验收**：交 py15.py + 自定义计时器运行结果。

**⚠️ 常见坑**：① `__exit__` 返回 True 吞掉异常（调试困难）；② 忘 `__enter__` return；③ 手疼复制。

**➡️ 下一步**：PY16 正则 re（有限自动机、NFA、回溯灾难）。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通自定义计时器 with。🥉
- 🔵 Lv2：用 `contextlib.contextmanager` 改写计时器。🥈
- 🟣 Lv3：写一个"自动关数据库连接"的 with 管理器（伪代码也行）。🥇
- 🟠 Lv4：用 with 包一个 `threading.Lock()` 防并发冲突。🏆
- 🔴 Lv5：对比 Python with 与 C++ RAII，写 200 字说明"异常安全"为何重要。👑

---

# PY16 · 正则 re（有限自动机 / NFA / 回溯）

**🎯 目标**：① 用 re 匹配/提取文本；② 懂基本语法（`. * + ? () [] \d`）；③ 理解正则=有限自动机；④ 知"回溯灾难"性能坑。

**📋 小白前置**：PY2（字符串）、PY8。

**🟢 部分一·最浅层**：正则像"通配寻人启事"——"找所有 '张'开头、后面跟两个字的名字"，一张规则卡代替你手动翻。

**🟡 部分二·动手层**：`py16.py`：
```python
import re
text = "电话 13812345678，备用 13900001111"
phones = re.findall(r"1\d{10}", text)
print(phones)                    # ['13812345678','13900001111']
m = re.search(r"(\d{4})-(\d{2})", "日期 2026-09")
print(m.group(1), m.group(2))    # 2026 09
```

**🔵 部分三·原理层**：正则引擎用**有限自动机**匹配。Python `re` 是**回溯型 NFA**——遇分支逐条试，不行退回（backtrack）。`\d` 数字、`+` 1 或多、`*` 0 或多、`?` 0 或 1、`()` 分组、`[]` 字符集、`\.` 转义点。

**🟣 部分四·深挖层**：**回溯灾难**——`(a+)+$` 匹配长串 "aaaa...b" 会指数级回溯卡死（ReDoS）。安全方向重点：不可信输入进正则要限长度/用线性引擎。编译 `re.compile` 复用提速。贪婪 vs 懒惰（`*?`）。

**🔴 部分五·顶级视角**：顶级工程师用正则做日志解析、数据清洗、路由匹配；但复杂结构（嵌套 JSON/HTML）正则无能为力，得上 parser（CMP 编译器方向）。理解 NFA=理解文本处理底层。

**🟠 部分六·安全种子**：**ReDoS（正则拒绝服务）**——攻击者构造让正则回溯爆炸的输入拖垮服务。白帽：审计自己正则、限制输入长度、用 `re.DEBUG` 查复杂度。

**✅ 验收**：交 py16.py + 提取邮箱/手机运行结果。

**⚠️ 常见坑**：① 忘 `r""` 原始串导致 `\` 被转义；② 贪婪默认吃掉太多（用 `?` 懒惰）；③ ReDoS 不自查；④ 手疼复制。

**➡️ 下一步**：PY17 并发 asyncio（事件循环、协程、非阻塞 IO）。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 findall 提取手机。🥉
- 🔵 Lv2：用正则提取所有邮箱（`[\w.]+@[\w.]+`）。🥈
- 🟣 Lv3：用分组提取"姓名:分数"对。🥇
- 🟠 Lv4：演示贪婪 vs 懒惰：`<.*>` 与 `<.*?>` 匹配 HTML 标签差异。🏆
- 🔴 Lv5：解释 ReDoS，构造一个会回溯爆炸的正则+输入，并说明如何缓解。👑

---

# PY17 · 并发 asyncio（事件循环 / 协程 / 非阻塞）

**🎯 目标**：① 懂同步 vs 异步；② 用 `async/await` 写协程；③ 理解事件循环；④ 知 IO 等待时并发的价值。

**📋 小白前置**：PY13（生成器/yield）、PY14。

**🟢 部分一·最浅层**：同步像排队打饭——前面人没打完你干等。异步像"点完单拿号，去旁边玩手机，叫号再来取"，等饭时柜台能服务别人。

**🟡 部分二·动手层**：`py17.py`：
```python
import asyncio, time
async def 任务(名, 秒):
    print(f"{名} 开始")
    await asyncio.sleep(秒)
    print(f"{名} 结束")
async def 主():
    await asyncio.gather(任务("A",1), 任务("B",2))
t = time.perf_counter()
asyncio.run(主())
print(f"总耗时 {time.perf_counter()-t:.2f}s")   # 约2秒而非3秒
```

**🔵 部分三·原理层**：`async def` 定义**协程**，`await` 表示"这里可能等，先去干别的"。**事件循环**调度：遇到 await 挂起当前协程、切到别的就绪协程。`asyncio.gather` 并发跑多个。关键：asyncio 是**单线程**并发，靠 IO 等待切换（不是并行算），适合高并发网络（Web 服务器）。

**🟣 部分四·深挖层**：`await` 底层是生成器+yield from 的进化（PY13）。CPU 密集任务用 asyncio 无效（要 multiprocessing 真并行）。`asyncio.sleep` 是"让出控制权"非真睡。对比线程（PY 有 threading，但 GIL 限制 CPU 并行）。

**🔴 部分五·顶级视角**：顶级工程师用 asyncio 写高并发后端（FastAPI 原生 async）、爬虫、实时服务。理解"事件循环=单线程多路复用"=理解 Node.js/Redis/nginx 的核心（方向6/10）。

**🟠 部分六·安全种子**：并发处理不可信请求要注意共享状态竞争（虽 asyncio 单线程但仍要防逻辑竞态）；限流防止单客户端占满事件循环（方向6 BE15）。

**✅ 验收**：交 py17.py + 证明并发总耗时≈最慢单个而非相加。

**⚠️ 常见坑**：① 忘 `await` 协程不执行；② 在 async 里调阻塞函数卡死循环；③ 混淆并发与并行；④ 手疼复制。

**➡️ 下一步**：PY18 类型标注 typing（静态类型、mypy）。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 asyncio 并发任务。🥉
- 🔵 Lv2：并发 3 个不同秒数任务，验证总耗时≈最大。🥈
- 🟣 Lv3：用 `asyncio.create_task` 手动调度两个协程。🥇
- 🟠 Lv4：比较"同步顺序跑"与"asyncio 并发"同一组 IO 任务的耗时差。🏆
- 🔴 Lv5：写 200 字"事件循环为何适合高并发 IO 而非 CPU 密集"，并举一个误用场景。👑

---

# PY18 · 类型标注 typing（静态类型 / mypy / 安全）

**🎯 目标**：① 用 `:` 和 `->` 标注类型；② 懂 Optional/List/Dict/Union；③ 会用 mypy 检查；④ 理解类型标注如何防 bug。

**📋 小白前置**：PY6、PY17。

**🟢 部分一·最浅层**：类型标注像"给盒子贴标签写'只装苹果'"——别人（和你三个月后）一看就知道该放啥，放错立刻被发现。

**🟡 部分二·动手层**：`py18.py`：
```python
from typing import Optional, List
def 除法(分子: float, 分母: float) -> Optional[float]:
    if 分母 == 0:
        return None
    return 分子 / 分母
def 求和(nums: List[int]) -> int:
    return sum(nums)
print(除法(10, 2))
print(求和([1, 2, 3]))
```
装 mypy：`pip install mypy`，跑 `mypy py18.py`。

**🔵 部分三·原理层**：Python 运行时不强制类型（仍是动态），标注只是"提示+可被静态检查"。`Optional[X]`= `X | None`；`List[int]` 列表元素类型。`mypy` 在运行前扫代码找类型错（如把 str 传进 expects int）。这是"用工具替你抓 bug"。

**🟣 部分四·深挖层**：类型标注是"渐进类型"——旧代码可零标注逐步加。`dataclass` 自动生成 `__init__/__repr__`；`Protocol` 定义结构化类型（鸭子类型的静态版）。大项目（如 Instagram 用 mypy 管千万行）靠它防回归。

**🔴 部分五·顶级视角**：顶级工程师在团队项目强制类型标注+CI 跑 mypy——类型错误在编译前逮住，省无数调试。理解类型=理解静态语言（Rust/C++/Java/Go/TS）的安全哲学（方向1b/1c）。

**🟠 部分六·安全种子**：类型标注能防"把不可信字符串当信任对象"的类错误；mypy 可辅助检查"用户输入未经验证就进危险函数"。但标注不是沙箱——运行时验证仍必须（方向7 输入校验）。

**✅ 验收**：交 py18.py + `mypy py18.py` 无错输出截图。

**⚠️ 常见坑**：① 以为标注会运行时报错（不会，要 mypy）；② `Optional` 忘判 None 导致 `NoneType` 错；③ 手疼复制。

**➡️ 下一步**：PY19 标准库精选（os/sys/datetime/json/pathlib）。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通带标注的函数。🥉
- 🔵 Lv2：装 mypy 并跑通无错。🥈
- 🟣 Lv3：故意标错类型（传 str 给 int 参数）看 mypy 报错。🥇
- 🟠 Lv4：用 `@dataclass` 定义一个 `Point(x:int,y:int)` 自动得构造/打印。🏆
- 🔴 Lv5：写 200 字"为什么大项目要静态类型检查"，并对比 Python 标注与 Rust 编译期类型。👑

---

# PY19 · 标准库精选（os/sys/datetime/json/pathlib）

**🎯 目标**：① 用 os/pathlib 操作路径；② 用 datetime 处理时间；③ 用 json 读写结构化数据；④ 懂 sys.argv 取参数。

**📋 小白前置**：PY9、PY12。

**🟢 部分一·最浅层**：标准库像"买手机送的工具箱"——不用额外装，开箱即用。json 是"数据的通用语言"，各程序都能读。

**🟡 部分二·动手层**：`py19.py`：
```python
from pathlib import Path
import json, datetime, sys
p = Path("学习/笔记.txt"); print(p.parent, p.name)
now = datetime.datetime.now(); print(now.strftime("%Y-%m-%d %H:%M"))
data = {"名": "小明", "课": ["PY1", "PY2"]}
Path("进度.json").write_text(json.dumps(data, ensure_ascii=False, indent=2))
back = json.loads(Path("进度.json").read_text())
print(back["课"])
print("命令行参数：", sys.argv)
```

**🔵 部分三·原理层**：`pathlib` 面向对象处理路径（跨平台 `/` vs `\` 自动处理）。`datetime` 处理日期运算（时区需注意，默认 naive）。`json.dumps/loads` 在 Python dict 与 JSON 文本互转（`ensure_ascii=False` 保中文）。`sys.argv` 是运行时的命令行参数列表。

**🟣 部分四·深挖层**：`os` 模块做环境变量/进程操作；`subprocess` 调外部命令（有注入风险！）；`collections` 的 Counter/defaultdict 高频；`itertools` 组合排列；`argparse` 做专业命令行参数（比 sys.argv 强）。`json` 对应 Web 的 API 数据格式（方向6）。

**🔴 部分五·顶级视角**：顶级工程师对标准库了如指掌——80% 需求不用第三方包就能解决，减少依赖风险。理解 json=理解前后端通信、配置文件、API（方向6/9）。

**🟠 部分六·安全种子**：① `subprocess` 拼接 shell 字符串=命令注入（用列表参数+不 shell=True）；② json 解析不可信数据要 try（畸形 JSON 崩溃）；③ 路径用 pathlib 防穿越（PY9）。

**✅ 验收**：交 py19.py + 生成的进度.json 截图。

**⚠️ 常见坑**：① json 中文乱码（漏 ensure_ascii=False）；② datetime 时区混乱；③ subprocess 用 shell=True 拼用户输入；④ 手疼复制。

**➡️ 下一步**：PY20 虚拟环境 venv/pip（隔离、可复现）。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 pathlib/json/datetime。🥉
- 🔵 Lv2：用 pathlib 递归列某文件夹所有 .py 文件。🥈
- 🟣 Lv3：用 `argparse` 做一个"接收 --name 参数并打印"的小工具。🥇
- 🟠 Lv4：计算"距你下次复查还有多少天"（datetime 运算）。🏆
- 🔴 Lv5：解释 subprocess 命令注入风险，给出安全调用外部命令的写法。👑

---

# PY20 · 虚拟环境 venv/pip（隔离 / 可复现）

**🎯 目标**：① 用 venv 建隔离环境；② 用 pip 装/冻包；③ 懂"为什么需要隔离"；④ 会生成 requirements.txt。

**📋 小白前置**：PY12、PY19。

**🟢 部分一·最浅层**：虚拟环境像"给每个项目单独的厨房"——A 项目要用盐 v1、B 要用盐 v2，各用各的不打架。

**🟡 部分二·动手层**（PowerShell/Git Bash）：
```bash
python -m venv venv
# 激活（Git Bash）
source venv/Scripts/activate
# 或 Windows PowerShell:
# .\venv\Scripts\Activate.ps1
pip install requests
pip freeze > requirements.txt
pip list
```
建好后写 `py20.py`：`import requests; print(requests.get("https://api.github.com").status_code)`（需联网，可只装不用）。

**🔵 部分三·原理层**：`venv` 在文件夹里复制一份 Python 解释器+独立 `site-packages`，激活后 `pip` 只装进这个环境，**不污染全局**。不同项目依赖不同版本互不干扰。`requirements.txt` 记录依赖，别人 `pip install -r requirements.txt` 复现一模一样环境。

**🟣 部分四·深挖层**：依赖解析有冲突可能（A 要 requests<3、B 要 >=3），`pip` 会尝试满足。`pipenv/poetry/uv` 是更现代的锁版本工具。生产用**锁定版本**防"今天能跑明天炸"。容器（方向10 Docker）进一步隔离。

**🔴 部分五·顶级视角**：顶级工程师把"可复现环境"当工程底线——没 requirements/锁文件的项目不可信。理解隔离=理解依赖管理、CI/CD、容器（方向10）。

**🟠 部分六·安全种子**：① 只从官方 PyPI 装、查包下载量/维护状态防投毒；② 锁版本防"依赖被恶意更新"（供应链攻击，SolarWinds 级）；③ 不在全局装包，避免权限/污染。

**✅ 验收**：交 已建 venv + requirements.txt + `pip list` 截图。

**⚠️ 常见坑**：① 忘激活导致装到全局；② PowerShell 执行策略禁脚本（用 `Set-ExecutionPolicy` 或 Git Bash）；③ 手疼复制命令。

**➡️ 下一步**：PY21 单元测试 pytest（TDD、覆盖率、断言）。

**⚔️ 闯关路线**
- 🟢 Lv1：建 venv 并激活。🥉
- 🔵 Lv2：装一个包并 freeze 到 requirements.txt。🥈
- 🟣 Lv3：用 `python -m venv` 建两个不同环境验证隔离。🥇
- 🟠 Lv4：读 requirements.txt 理解"== 固定版本"的意义。🏆
- 🔴 Lv5：写 200 字"供应链攻击是什么"，并说明锁版本+可信源如何防护。👑

---

# PY21 · 单元测试 pytest（TDD / 覆盖率 / 断言）

**🎯 目标**：① 用 pytest 写测试用例；② 懂断言、测试驱动；③ 理解"测什么、不测什么"；④ 见覆盖率概念。

**📋 小白前置**：PY6、PY20。

**🟢 部分一·浅层**：测试像"出厂质检"——每个零件造好先试一下能不能用，别等装成整机才发现坏。

**🟡 部分二·动手层**：`py21_calc.py`：
```python
def 加(a, b):
    return a + b
def 除(a, b):
    if b == 0:
        raise ValueError("除数0")
    return a / b
```
`test_calc.py`：
```python
from py21_calc import 加, 除
import pytest
def test_加():
    assert 加(2, 3) == 5
def test_除零():
    with pytest.raises(ValueError):
        除(1, 0)
```
跑：`pip install pytest && pytest -v`。

**🔵 部分三·原理层**：测试是"给定输入、断言输出符合预期"的函数，pytest 自动发现 `test_*` 文件/函数。`assert` 不成立则测试红。`pytest.raises` 测异常。TDD=先写测试再写实现，逼你想清接口。**覆盖率**=代码被测试跑到的比例（高覆盖≠无 bug，但低覆盖必藏雷）。

**🟣 部分四·深挖层**：测试分单元（单函数）、集成（多模块）、e2e（整系统）。边界值/异常路径常是 bug 窝点，要专门测。mock 替身模拟外部依赖（数据库/网络）。CI 里跑测试防回归（方向10）。

**🔴 部分五·顶级视角**：顶级工程师把测试当"可执行的规格说明"——读测试就知道代码该干嘛。没有测试的代码不敢改。理解测试=理解专业软件工程的可靠性根基。

**🟠 部分六·安全种子**：测试要覆盖"恶意输入"（超长、None、特殊字符）路径——很多漏洞是异常输入未测出。安全测试（fuzzing）是方向7 的延伸。

**✅ 验收**：交 calc + test 两文件 + pytest 全绿截图。

**⚠️ 常见坑**：① 测试文件/函数名不合规 pytest 找不到；② 只测正常路径忘异常；③ 手疼复制。

**➡️ 下一步**：PY22 打包发布（pyproject.toml、构建、分发）。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通两个测试全绿。🥉
- 🔵 Lv2：加一个 `test_加负数` 边界测试。🥈
- 🟣 Lv3：故意让实现错，看 pytest 报红，再改对。🥇
- 🟠 Lv4：用 `pytest --cov` 看覆盖率（装 pytest-cov）。🏆
- 🔴 Lv5：写 200 字"TDD 为什么能提升代码质量"，并设计一个你将来项目的测试清单。👑

---

# PY22 · 打包发布（pyproject.toml / 构建 / 分发）

**🎯 目标**：① 懂现代 Python 打包（pyproject.toml）；② 会构建与本地安装；③ 理解版本与元数据；④ 知分发包的渠道（PyPI/私有）。

**📋 小白前置**：PY12、PY20、PY21。

**🟢 部分一·最浅层**：打包像"把散装零件装进带说明书的礼盒"——别人拿到盒就能 `pip install` 用，不用管里面怎么拼。

**🟡 部分二·动手层**：建文件夹 `mytool/`，内含 `pyproject.toml`：
```toml
[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
[project]
name = "mytool"
version = "0.1.0"
dependencies = []
```
`mytool/__init__.py` 放你的函数。构建安装：
```bash
pip install build
python -m build
pip install dist/mytool-0.1.0-py3-none-any.whl
```

**🔵 部分三·原理层**：`pyproject.toml` 是现代打包标准（替代旧 setup.py），声明元数据与构建后端。`python -m build` 生成 `sdist`（源码包）+ `wheel`（编译好的 .whl，安装更快）。`version` 用语义化版本号。装好后全局 `import mytool` 可用。

**🟣 部分四·深挖层**：`entry_points` 可注册命令行工具（装完直接敲命令）。私有仓库（Nexus/Artifactory）供企业内部不发公网。发布到公网 PyPI 要双因素认证（防账号被盗投毒，方向10）。

**🔴 部分五·顶级视角**：顶级工程师把可复用代码打成内部包，团队 `pip install` 共享，避免到处复制脚本。理解打包=理解软件分发、依赖治理（接方向10 制品库）。

**🟠 部分六·安全种子**：① 发布前审计依赖有无漏洞（`pip-audit`）；② PyPI 账号开 2FA 防被控投毒；③ 别在包里夹带密钥/证书。

**✅ 验收**：交 pyproject.toml + 本地 build 成功 + 安装后可 import 截图。

**⚠️ 常见坑**：① 旧 setup.py 与新 pyproject 混用；② 漏 `__init__.py` 包不被识别；③ 手疼复制命令。

**➡️ 下一步**：PY23 元编程（反射、getattr、描述符）。

**⚔️ 闯关路线**
- 🟢 Lv1：建 pyproject.toml 并 build。🥉
- 🔵 Lv2：本地安装自己包并 import 成功。🥈
- 🟣 Lv3：加 entry_points 注册一个命令行命令。🥇
- 🟠 Lv4：用 `pip-audit` 查依赖漏洞（即使空依赖也跑一遍熟悉）。🏆
- 🔴 Lv5：写 200 字"为什么包要发私有仓库而非到处拷脚本"，说明企业依赖治理。👑

---

# PY23 · 元编程（反射 / getattr / 描述符）

**🎯 目标**：① 懂"代码操作代码"；② 用 getattr/setattr 动态访问；③ 理解描述符与装饰器关系；④ 知元编程的双刃剑。

**📋 小白前置**：PY11、PY14、PY22。

**🟢 部分一·最浅层**：元编程像"会自己改自己菜谱的厨师"——普通代码按菜谱做菜，元编程能运行时改菜谱、甚至造新厨师。

**🟡 部分二·动手层**：`py23.py`：
```python
class 盒:
    颜色 = "红"
b = 盒()
print(getattr(b, "颜色", "无"))     # 红
setattr(b, "尺寸", 10)
print(b.尺寸)                       # 10
# 动态调方法
def 嗨(self): return "嗨"
setattr(盒, "嗨", 嗨)
print(b.嗨())
```

**🔵 部分三·原理层**：**反射**=运行时查/改对象（getattr/setattr/hasattr）。Python 一切皆对象，类也是对象，故可动态加方法。`__getattr__/__setattr__` 是"属性访问钩子"。**描述符**（定义了 `__get__/__set__` 的类）是 property/classmethod/staticmethod 的底层机制——比装饰器更底层。

**🟣 部分四·深挖层**：`type(name, bases, dict)` 可运行时动态创建类（元类 `metaclass` 是"创建类的类"，ORM 框架 Django/SQLAlchemy 用之）。元编程威力大但可读性差——顶级团队谨慎用，仅在"框架级抽象"才上。

**🔴 部分五·顶级视角**：顶级工程师理解元编程但克制——它让框架（Django/pytest/pydantic）优雅，但业务代码滥用=维护噩梦。理解它=理解"为什么你的框架能这么魔法"。

**🟠 部分六·安全种子**：动态执行不可信代码（`eval/exec/getattr` 调到危险名）是 RCE 温床。白帽：绝不 `eval` 用户输入；`getattr` 调方法前要白名单校验名（防调到 `__` 私有破坏）。

**✅ 验收**：交 py23.py + getattr/setattr 动态演示。

**⚠️ 常见坑**：① 用 eval/exec 跑用户输入（高危！）；② 元编程过度致代码难懂；③ 手疼复制。

**➡️ 下一步**：PY24 性能剖析（cProfile、瓶颈定位）。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 getattr/setattr 动态加属性。🥉
- 🔵 Lv2：用 `__getattr__` 实现"访问不存在属性返回默认值"。🥈
- 🟣 Lv3：用 `type()` 动态创建一个类并实例化。🥇
- 🟠 Lv4：写一个 property（用描述符思想或 @property）做"温度摄氏↔华氏"。🏆
- 🔴 Lv5：写 200 字"元编程为何是双刃剑"，举一个安全（eval 风险）与一个工程（框架抽象）例子。👑

---

# PY24 · 性能剖析（cProfile / 瓶颈 / 优化）

**🎯 目标**：① 用 cProfile 找慢在哪；② 懂"先量再优化"；③ 理解算法复杂度 vs 常数；④ 知常见优化手段。

**📋 小白前置**：PY5、DSA1（复杂度）、PY23。

**🟢 部分一·最浅层**：优化像"给漏水的管子找漏点"——别瞎换整根管，先用仪器测哪段在喷，精准修。

**🟡 部分二·动手层**：`py24.py`：
```python
import cProfile
def 慢():
    return sum(i*i for i in range(10_000_000))
cProfile.run("慢()")
# 看 ncalls / tottime / cumtime 找热点
```
也可命令行：`python -m cProfile -s tottime py24.py`。

**🔵 部分三·原理层**：**cProfile** 统计每个函数调用次数与耗时。`tottime`=函数自身耗时，`cumtime`=含子调用总耗时。原则：**先量再优化**——凭直觉优化常优化错地方（Premature Optimization）。瓶颈常是：不必要循环、重复计算、慢 IO、错误数据结构（DSA 全方向）。

**🟣 部分四·深挖层**：优化手段：换成 O(n) 更好的算法（方向2）、用内置/向量化（NumPy）、缓存（functools.lru_cache）、减少 IO、用生成器惰性。但 80% 场景"代码清晰"比"快 2 倍"重要——只在热点优化。

**🔴 部分五·顶级视角**：顶级工程师用剖析器定位瓶颈、用基准测试量化改进、拒绝拍脑袋优化。理解性能=理解复杂度（DSA）+ 系统（CPU/缓存/IO，方向3）+ 语言特性。

**🟠 部分六·安全种子**：性能差的代码可被攻击者用大输入拖垮（DoS，见 DSA1/PY16）。剖析+设上限=防御。白帽视角：优化时别为快而去掉输入校验。

**✅ 验收**：交 py24.py + cProfile 输出中识别最慢函数。

**⚠️ 常见坑**：① 没剖析就乱优化；② 只测小数据忽略大输入；③ 手疼复制命令。

**➡️ 下一步**：PY25 项目实战（端到端小工具）。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 cProfile 看耗时统计。🥉
- ⚋ Lv2：对比"列表推导"vs"for+append"用 timeit 计时。🥈
- 🟣 Lv3：用 `functools.lru_cache` 加速递归斐波那契并对比。🥇
- 🟠 Lv4：刻意写个 O(n²) 慢函数，剖析证明它最慢，再改 O(n)。🏆
- 🔴 Lv5：写 200 字"为什么先量再优化"，附一次你真实剖析发现瓶颈的经历（或模拟）。👑

---

# PY25 · 项目实战（端到端小工具：批量重命名 / 记账）

**🎯 目标**：① 综合前 24 节做完整小工具；② 练"需求→设计→实现→测试→打包"全链路；③ 产出可放进作品集的成品；④ 体验工程师工作流。

**📋 小白前置**：PY1–PY24 全部。

**🟢 部分一·最浅层**：实战像"用学过的零件拼一台真能用的机器"——前面每节是零件，今天装成一台"自动整理文件"的机器。

**🟡 部分二·动手层**（示例：批量重命名工具）：新建 `renamer.py`：
```python
import argparse, re
from pathlib import Path
def 重命名(目录: str, 模式: str, 替换: str):
    p = Path(目录)
    for f in p.iterdir():
        if f.is_file():
            new = re.sub(模式, 替换, f.name)
            if new != f.name:
                f.rename(p / new)
                print(f"{f.name} -> {new}")
if __name__ == "__main__":
    ap = argparse.ArgumentParser()
    ap.add_argument("目录"); ap.add_argument("模式"); ap.add_argument("替换")
    a = ap.parse_args()
    重命名(a.目录, a.模式, a.替换)
```
运行：`python renamer.py "学习" "\s" "_"`（把空格改下划线）。也可做记账版（读写 json 累计收支）。

**🔵 部分三·原理层**：实战把 PY9(文件)/PY16(正则)/PY19(pathlib/argparse)/PY12(模块)/PY21(应写测试)串起来。真实工程还含：错误处理（PY10）、类型标注（PY18）、虚拟环境（PY20）、测试（PY21）、打包（PY22）。你写的不是脚本，是**产品**。

**🟣 部分四·深挖层**：作品集思维——每个工具写 README（用途/用法/示例）、加测试、能 `pip install` 或直接双击（可裹一层 GUI 或 bat 启动器，护手）。将来面试/接活，作品集 > 简历（你之前定过"作品集永久化"）。

**🔴 部分五·顶级视角**：顶级工程师的交付物=可运行+可测+可读+可维护。今天这个小工具，和未来百万行系统，方法论完全一致：拆需求→定接口→写+测→剖析优化→文档。养成这闭环，你已具备专业素养。

**🟠 部分六·安全种子**：重命名工具若用正则+用户模式，要防路径穿越/误改系统文件（限制目录白名单、先 dry-run 预览）。工具只对自己目录跑——白帽自律。

**✅ 验收**：交 `renamer.py`（或记账版）+ 一次真实运行截图 + 至少 1 个 pytest 测试。

**⚠️ 常见坑**：① 没先备份就批量改文件名（危险！先 dry-run）；② 正则误伤；③ 手疼——用语音/复制，且此节可分步做不完就停。

**➡️ 下一步**：方向1b 多语言（C/C++/Java/JS/Go）——把 Python 内功迁移到"更贴近机器"的语言，理解范式与取舍。

**⚔️ 闯关路线**
- 🟢 Lv1：跑通 renamer 改一个测试文件夹文件名。🥉
- 🔵 Lv2：加 `--dry-run` 预览不改名，确认安全。🥈
- 🟣 Lv3：为 renamer 写 1 个 pytest（用临时目录 tmp_path）。🥇
- 🟠 Lv4：加类型标注 + 异常捕获（目录不存在提示）。🏆
- 🔴 Lv5：把工具打成包（PY22）+ 写 README，作为你**作品集第 1 件**入库（说明：这就是你将来 HR 可见的成品起点）。👑

---

> **方向1a 完结 · 共 25 节（PY1–PY25）**。每节 11 段 + Lv1–Lv5 闯关 + ≥5 Lab。建议从 PY1 起逐节闯关，集齐 25 个 👑 即方向1a 毕业。下一步进方向1b 多语言。白帽红线贯穿：只写自己的程序、只练自己机器。
