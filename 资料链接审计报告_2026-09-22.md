# 资料链接审计报告 · 2026-09-22

## 审计范围

从 `权威资料目录与复核计划.md` 提取 71 个唯一 `https://` 入口，并对每个入口执行只读 HEAD；HEAD 不适用时再执行 GET。审计只回答“当前客户端能否到达入口、是否重定向”，不把 HTTP 200 当成课程掌握证明，也不下载视频或提交任何表单。

## 本轮结果

| 结果 | 数量 | 解释 |
|---|---:|---|
| HTTP 200 | 65 | 入口可达；其中部分自动转到同机构当前版本路径 |
| HTTP 308 | 4 | OWASP 官方入口要求 HTTPS；跟随重定向后可用 |
| HTTP 403 | 1 | Intel SDM 页面对当前客户端有限制；使用 Intel 官方工具/架构入口作替代，并保留原链接供浏览器复核 |
| 网络错误 | 1 | GNU Bash 手册在本轮 Python 客户端超时；课程保留 GNU 官方入口，必要时使用本地系统手册复核 |
| 合计 | 71 | 65 + 4 + 1 + 1 |

另对新增 YouTube 外网入口执行独立只读检查：10 个官方/教育频道返回 HTTP 200。3 个猜测频道地址返回 404，已从配置移除，不向课堂暴露死链接。当前保留的 YouTube 入口包括 CS50、MIT OpenCourseWare、Python Software Foundation、PyTorch、OWASP、Wireshark、Google for Developers、RustVideos、freeCodeCamp 和 Computerphile；MIT 使用其已验证的 `/mitocw` 入口。

## 资料如何进入课堂

- 官方规范、教材和文档仍是主资料：课堂会把资料入口、建议章节、版本和访问日期作为证据锚点。
- YouTube 只用于建立直觉、演示或听力输入；看视频不能替代回答、代码、测试、实验记录或老师验收。
- 视频配置只填写 `watch_url`，不强行填写 `embed_url`。这样可以避开地区限制、第三方禁止嵌入和播放器策略变化；用户点击后在自己的浏览器/VPN中观看。
- 某个视频下架时，课程仍可依靠本地文字材料和 Lab 完成；替换链接时必须记录来源、日期和变化。

## 当前公开性与隐私判断

课程源码仓库是公开的，但 `.gitignore` 排除了个人学习记录、答辩、备份、Cookie、密码和 API Key。GitHub Pages 只提供公开只读文件，不能运行当前 Python API；完整交互和永久记录继续留在本机，或在用户授权后部署到受控后端。

## 复核限制

链接状态会因地区、VPN、TLS 客户端、站点限流和版本更新变化。每次学习前只需打开当前课的资料入口并记录页面标题/章节；遇到失败使用同机构替代入口，不把本次可达性结果当成内容正确性的唯一证据。
